# Requirements: Scrypath — Milestone v1.21

**Defined:** 2026-05-22  
**Shipped:** 2026-05-23  
**Milestone:** v1.21 — *Query Toolkit And Phoenix Edge Helpers*  
**Core value:** Make search indexing feel native to Ecto and ergonomic for Phoenix teams without hiding the operational realities of keeping search in sync.

## Milestone goal

Ship a narrow-balanced public edge contract for request-shaped search params: a framework-light query-param toolkit plus thin optional Phoenix URL/form/LiveView helpers over the existing `Scrypath.search/3` runtime, while keeping contexts as the application boundary and avoiding public exposure of current internal query structs.

## Shipped requirements

### Public query toolkit

- [x] **QTK-01**: Apps can cast browser-shaped request params into a stable plain-data search-args shape without exposing `%Scrypath.Query{}` or other current internal query structs as public API.
- [x] **QTK-02**: The public toolkit normalizes text, filters, sort, pagination, facets, and facet-filter input once at the edge while preserving explicit defaults and limits.
- [x] **QTK-03**: Invalid edge input returns structured, field-scoped errors that host apps can render directly instead of relying on ad hoc controller or LiveView branching.
- [x] **QTK-04**: Toolkit output feeds the existing `Scrypath.search/3` path cleanly and does not create a second runtime or move orchestration out of contexts or context-owned search modules.

### Optional Phoenix edge helpers

- [x] **PHX-01**: Optional Phoenix helpers support URL and form round-tripping over the toolkit without introducing a hard Phoenix dependency in runtime core.
- [x] **PHX-02**: Optional LiveView helpers support param-driven search flows and error display while keeping URL params as the shareable UI state.

### Docs and verification

- [x] **DOC-01**: Guides and examples make the boundary explicit: contexts stay canonical, helpers are wrappers, and no schema-generated runtime verbs or UI layer ship in this milestone.
- [x] **VRFY-01**: Tests and contract coverage fail on drift around plain-data toolkit output, field-scoped errors, helper optionality, and canonical runtime delegation.

## Future requirements carried forward

- [ ] **CMP-01**: Reusable composition presets and scopes over the query-toolkit layer once real app pressure proves the common patterns are worth freezing.
- [ ] **CMP-02**: Stronger UI metadata exposure for declared filters, sorts, facets, and paging once the public query-param contract has settled in real apps.

## Out of scope

- Public `%Scrypath.Query{}` or other current internal normalization structs as semver-stable contract.
- Schema-generated runtime search verbs, controller/LiveView macros, or any helper that turns Scrypath into a framework facade.
- Reusable UI widgets, search-page scaffolds, or form-builder layers.
- Broader composition/preset systems.
- Public backend-expansion work or adapter-surface widening.

## Traceability

| Requirement | Planned phase | Final status | Outcome |
|-------------|---------------|--------------|---------|
| QTK-01 | Phase 80 | Complete | Validated as shipped |
| QTK-02 | Phase 81 | Complete | Validated as shipped |
| QTK-03 | Phase 81 | Complete | Validated as shipped |
| QTK-04 | Phase 80 | Complete | Validated as shipped |
| PHX-01 | Phase 81 | Complete | Validated as shipped |
| PHX-02 | Phase 81 | Complete | Validated as shipped |
| DOC-01 | Phase 82 | Complete | Validated as shipped |
| VRFY-01 | Phase 82 | Complete | Validated as shipped |

---

*Archived: 2026-05-23 — `v1.21` requirements satisfied across phases `80`–`82`.*
