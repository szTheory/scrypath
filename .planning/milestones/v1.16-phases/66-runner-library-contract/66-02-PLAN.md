---
phase: 66
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/test/scrypath_ops/playbook/runner_test.exs
  - scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs
autonomous: true
requirements:
  - OPS3-03
must_haves:
  truths:
    - "Representative tests fail if `Runner.run_validated/3` diverges from direct `Scrypath.search/3` or `search_many/2` semantics on the chosen fixtures."
    - "At least one UI-through-runner test proves the raw reason still reaches `PlaybookLive` as structured state before formatting."
    - "The phase catches contract drift without freezing UI strings or introducing a generalized meta-harness."
  artifacts:
    - path: "scrypath_ops/test/scrypath_ops/playbook/runner_test.exs"
      provides: "Representative runner-vs-library parity matrix"
    - path: "scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs"
      provides: "Downstream boundary regression proving raw reasons survive into LiveView state"
  key_links:
    - from: "scrypath_ops/test/scrypath_ops/playbook/runner_test.exs"
      to: "lib/scrypath.ex"
      via: "direct assertions against `Scrypath.search/3` and `Scrypath.search_many/2` outcomes"
      pattern: "Scrypath\\.search|Scrypath\\.search_many"
    - from: "scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs"
      to: "scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex"
      via: "assertions on raw `run_error` or equivalent assigned reason"
      pattern: "run_error|run_failure_enriched"
---

# Plan 02 — Representative parity matrix

<objective>
Add the narrow but explicit Phase 66 parity matrix that compares runner outcomes to direct `Scrypath` outcomes and proves downstream layers still consume raw reasons instead of redefining the contract.

Purpose: implement D-08 through D-15 and Phase 66 success criteria #2 and #3.
Output: cross-boundary parity tests for search success, search_many success, pre-dispatch/config failure, backend/runtime failure, and one operator-relevant multi-search edge, plus one downstream regression check against silent divergence.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/66-runner-library-contract/66-CONTEXT.md
@.planning/phases/66-runner-library-contract/66-RESEARCH.md
@.planning/phases/66-runner-library-contract/66-PATTERNS.md
@.planning/phases/66-runner-library-contract/66-VALIDATION.md
@.planning/phases/66-runner-library-contract/66-01-PLAN.md
@scrypath_ops/test/scrypath_ops/playbook/runner_test.exs
@scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs
@scrypath_ops/lib/scrypath_ops/search_playground/adapter.ex
@scrypath_ops/test/support/search_playground_stub_adapter.ex
@scrypath_ops/lib/scrypath_ops/playbook/runner.ex
@scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
@test/scrypath/search_many_test.exs
@lib/scrypath.ex
@lib/scrypath/search.ex

<interfaces>
From `scrypath_ops/lib/scrypath_ops/search_playground/adapter.ex`:
```elixir
def search(schema, text, opts), do: Scrypath.search(schema, text, opts)
def search_many(entries, opts), do: Scrypath.search_many(entries, opts)
```

From `scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex`:
```elixir
def handle_async({@playbook_run_async_key, run_id}, {:ok, {:error, reason}}, socket) do
  enriched = enrich_run_failure(reason, socket)
  ...
  |> assign(:run_error, reason)
  |> assign(:run_failure_enriched, enriched)
end
```

From `test/scrypath/search_many_test.exs`:
```elixir
assert {:error, {:validation_failed, SearchablePost, {:unknown_facet, :nope}}} =
         Scrypath.search_many([{SearchablePost, "a", facets: [:nope]}], @base_opts)
```
</interfaces>
</context>

<source_audit>
- GOAL: Phase 66 requires automated parity tests and no hidden `{:error, term}` divergence; this plan covers the executable regression half.
- REQ: `OPS3-03` is covered here via explicit cross-boundary assertions.
- RESEARCH: The recommended representative matrix and direct adapter delegation pattern are implemented here without a meta-harness.
- CONTEXT: D-08 through D-15 are implemented here; deferred string-parity or `%RunError{}` work remains out of scope.
</source_audit>

<tasks>

<task id="66-02-01" type="auto" tdd="true">
<name>Task 1: Add runner-vs-library parity cases in `runner_test.exs`</name>
<files>scrypath_ops/test/scrypath_ops/playbook/runner_test.exs</files>
<read_first>
- scrypath_ops/test/scrypath_ops/playbook/runner_test.exs
- scrypath_ops/lib/scrypath_ops/playbook/runner.ex
- scrypath_ops/lib/scrypath_ops/search_playground/adapter.ex
- scrypath_ops/test/support/search_playground_stub_adapter.ex
- test/scrypath/search_many_test.exs
- lib/scrypath.ex
- lib/scrypath/search.ex
- .planning/phases/66-runner-library-contract/66-CONTEXT.md
</read_first>
<acceptance_criteria>
- `grep -n "Scrypath.search(" scrypath_ops/test/scrypath_ops/playbook/runner_test.exs` returns at least one direct core-library parity assertion.
- `grep -n "Scrypath.search_many(" scrypath_ops/test/scrypath_ops/playbook/runner_test.exs` returns at least one direct multi-search parity assertion.
- `grep -n "search happy path\\|search_many happy path\\|pre-dispatch\\|backend/runtime\\|multi-search" scrypath_ops/test/scrypath_ops/playbook/runner_test.exs` returns matches from named parity cases or comments covering the five required seams.
- `mix test scrypath_ops/test/scrypath_ops/playbook/runner_test.exs` exits 0.
</acceptance_criteria>
<action>
Rewrite or extend `runner_test.exs` into an explicit representative matrix per D-08 through D-10. Use direct `Scrypath.search/3` and `Scrypath.search_many/2` calls as the library reference path, and compare them to `Runner.run_validated/3` using the real `ScrypathOps.SearchPlayground.Adapter.Scrypath` in the parity tests so the runner is exercising the same core functions as production. Keep setup explicit: for the parity cases, switch `:search_playground_adapter` from the stub to `ScrypathOps.SearchPlayground.Adapter.Scrypath` inside the test or setup block, and feed the same backend/runtime opts to both sides.

Cover exactly this matrix unless an equivalent fixture is clearer:
1. `search` happy path returning `{:ok, %Scrypath.SearchResult{}}`.
2. `search_many` happy path returning `{:ok, %Scrypath.MultiSearchResult{}}`.
3. One pre-dispatch/config failure from the runner boundary such as `{:config, :missing_backend}` or `{:config, :no_schema}`.
4. One backend/runtime failure that originates from the underlying `Scrypath` path, not from UI formatting or the stub adapter shortcut.
5. One multi-search-specific operator-relevant edge, preferably a known `search_many/2` contract such as `{:validation_failed, schema, reason}` or a federation/native-search-many restriction.

Assert structural parity only: exact reason heads/tuples and result struct modules. Do not assert UI copy, docs links, or a generalized meta-harness. If a helper is added, keep it local and narrow.
</action>
<verify>
  <automated>mix test test/scrypath/search_many_test.exs scrypath_ops/test/scrypath_ops/playbook/runner_test.exs</automated>
</verify>
<done>`runner_test.exs` fails when the runner’s raw result/error contract drifts from direct `Scrypath` semantics on the chosen representative fixtures.</done>
</task>

<task id="66-02-02" type="auto" tdd="true">
<name>Task 2: Lock the downstream boundary without freezing UI strings</name>
<files>scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs</files>
<read_first>
- scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs
- scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
- scrypath_ops/lib/scrypath_ops/playbook/run_failure.ex
- scrypath_ops/lib/scrypath_ops/playbook/runner.ex
- .planning/phases/66-runner-library-contract/66-CONTEXT.md
- .planning/phases/66-runner-library-contract/66-VALIDATION.md
</read_first>
<acceptance_criteria>
- `grep -n "run_error\\|run_failure_enriched" scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` returns at least one new assertion that inspects raw reason state before or alongside enriched output.
- `grep -n "stub_hard_failure\\|timed_out\\|cancelled\\|validation_failed" scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` returns at least one reason-identity assertion without requiring exact flash-copy equality.
- `mix test scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` exits 0.
</acceptance_criteria>
<action>
Add one focused downstream regression test to `PlaybookLiveTest` that proves the boundary stays honest per D-03, D-12, and D-14: trigger a failing run, then assert the LiveView keeps the raw reason in assign state (`run_error` or equivalent) while `run_failure_enriched` remains a downstream projection built from that reason. Use `:sys.get_state(view.pid)` or the existing LiveView test seam if needed, but do not turn the test into a copy snapshot. The assertion should care about reason identity and presence of enriched state, not the exact flash string.

Keep the test narrow: one failure path is enough here because the full matrix lives in `runner_test.exs`. Its job is to prove there is no silent rescue/swallow or UI-only normalization that hides the raw `{:error, term}` divergence from operators or future tests.
</action>
<verify>
  <automated>mix test scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs</automated>
</verify>
<done>The UI-facing tests prove downstream formatting happens after the raw reason exists and would catch a future swallow or contract rewrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| direct `Scrypath` call -> runner parity test | Tests compare two surfaces that must stay semantically aligned |
| runner failure -> `PlaybookLive` assigns | Raw reasons can be swallowed or rewritten before operators see them |
| fixtures/test payloads -> diagnostics/UI | Tests must avoid embedding secrets while still exercising failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-04 | R | `scrypath_ops/test/scrypath_ops/playbook/runner_test.exs` | mitigate | Compare exact reason heads and result struct modules against direct `Scrypath` calls so drift cannot hide behind UI formatting or indirect helpers. |
| T-66-05 | I | `scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` | mitigate | Use synthetic fixture values only and assert reason identity/enriched-state presence rather than copying full diagnostics payloads or secrets into tests. |
| T-66-06 | R | `scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex` | mitigate | Add a regression test that inspects assign state after failure so a future swallow/rewrite of `run_error` fails immediately. |
</threat_model>

<verification>
Run:
- `mix test test/scrypath/search_many_test.exs scrypath_ops/test/scrypath_ops/playbook/runner_test.exs`
- `mix test scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs`
- `mix verify.opsui`
</verification>

<success_criteria>
- The parity suite covers the five required representative seams and fails on semantic drift.
- At least one downstream test proves raw reasons survive into LiveView state before formatting.
- No test in this plan freezes UI copy or creates a broad runner meta-harness.
</success_criteria>

<output>
After completion, create `.planning/phases/66-runner-library-contract/66-02-SUMMARY.md`
</output>
