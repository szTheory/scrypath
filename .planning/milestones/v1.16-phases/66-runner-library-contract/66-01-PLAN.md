---
phase: 66
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scrypath_ops/lib/scrypath_ops/playbook/runner.ex
  - scrypath_ops/docs/playbook-schema-v1.md
autonomous: true
requirements:
  - OPS3-03
must_haves:
  truths:
    - "Maintainers can point to `ScrypathOps.Playbook.Runner` as the single execution-contract reference for playbook runs."
    - "`Runner.run_validated/3` remains a raw `{:ok, result}` or `{:error, reason}` seam whose stable key is the reason term, not UI wording."
    - "Wire-format docs stay authoritative for JSON shape only and link outward to the runner contract instead of restating runtime result semantics."
  artifacts:
    - path: "scrypath_ops/lib/scrypath_ops/playbook/runner.ex"
      provides: "Canonical runner-library contract docs and guarded raw tuple seam"
    - path: "scrypath_ops/docs/playbook-schema-v1.md"
      provides: "Wire-format authority with runner-contract cross-link"
  key_links:
    - from: "scrypath_ops/docs/playbook-schema-v1.md"
      to: "scrypath_ops/lib/scrypath_ops/playbook/runner.ex"
      via: "direct contract link to the Runner moduledoc section"
      pattern: "Runner-library contract|runner-library-contract"
    - from: "scrypath_ops/lib/scrypath_ops/playbook/runner.ex"
      to: "lib/scrypath.ex"
      via: "matching Errors vs raises language and raw reason semantics"
      pattern: "SearchResult|MultiSearchResult|\\{:error, reason\\}"
---

# Plan 01 — Canonical contract and boundary freeze

<objective>
Freeze the Phase 66 execution contract at the `ScrypathOps.Playbook.Runner` boundary and keep the playbook schema doc strictly scoped to JSON wire format.

Purpose: implement D-01 through D-07 and D-14 without widening the public API.
Output: canonical runner contract docs, a schema-doc link to that contract, and a narrowed boundary audit that does not introduce new silent error swallowing.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/66-runner-library-contract/66-CONTEXT.md
@.planning/phases/66-runner-library-contract/66-RESEARCH.md
@.planning/phases/66-runner-library-contract/66-PATTERNS.md
@.planning/phases/65-playbook-run-lifecycle-opsui/65-03-SUMMARY.md
@scrypath_ops/lib/scrypath_ops/playbook/runner.ex
@scrypath_ops/docs/playbook-schema-v1.md
@scrypath_ops/lib/scrypath_ops/playbook/run_failure.ex
@scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
@lib/scrypath.ex
@lib/scrypath/search.ex
@lib/scrypath/errors.ex
@lib/scrypath/cli/operator_task.ex

<interfaces>
From `scrypath_ops/lib/scrypath_ops/playbook/runner.ex`:
```elixir
@spec run_validated(map() | {:ok, map()}, [module()], keyword()) ::
        {:ok, term()} | {:error, term()}
```

From `lib/scrypath.ex`:
```elixir
@spec search(module(), String.t(), keyword()) :: {:ok, term()} | {:error, term()}
@spec search_many(list(), keyword()) :: {:ok, term()} | {:error, term()}
```

From `lib/scrypath/search.ex`:
```elixir
@spec search(module(), String.t(), keyword()) :: {:ok, SearchResult.t()} | {:error, term()}
@spec search_many(list(), keyword()) :: {:ok, MultiSearchResult.t()} | {:error, term()}
```
</interfaces>
</context>

<source_audit>
- GOAL: Phase 66 requires one canonical contract reference and no new silent swallow path; this plan covers the documentation and boundary half.
- REQ: `OPS3-03` is covered here via the documented stable result/error shapes.
- RESEARCH: Canonical `Runner` `@moduledoc`, wire-format link-out, and narrow rescue auditing are planned exactly as recommended.
- CONTEXT: D-01 through D-07 and D-14 are implemented here; deferred `%RunError{}` or presentation-string contracts are excluded.
</source_audit>

<tasks>

<task id="66-01-01" type="auto">
<name>Task 1: Freeze the canonical runner-library contract in `Runner`</name>
<files>scrypath_ops/lib/scrypath_ops/playbook/runner.ex</files>
<read_first>
- scrypath_ops/lib/scrypath_ops/playbook/runner.ex
- .planning/phases/66-runner-library-contract/66-CONTEXT.md
- .planning/phases/66-runner-library-contract/66-RESEARCH.md
- lib/scrypath.ex
- lib/scrypath/search.ex
- scrypath_ops/lib/scrypath_ops/playbook/run_failure.ex
- scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
- lib/scrypath/errors.ex
- lib/scrypath/cli/operator_task.ex
</read_first>
<action>
Update `Runner` `@moduledoc` with a dedicated `## Runner-library contract` section per D-05 and D-06. State the accepted input after `ScrypathOps.Playbook.V1.validate/1`, the two success shapes by mode (`search` returns `%Scrypath.SearchResult{}`, `search_many` returns `%Scrypath.MultiSearchResult{}`), and that all failures stay `{:error, reason}` with reason identity as the stable compatibility key per D-01 and D-02. Explicitly call out that `RunFailure`, `PlaybookLive`, `Scrypath.Errors`, and Mix/operator tasks format reasons after the raw reason exists per D-03 and D-12 through D-13. Mirror the root-library “Errors vs raises” language by noting the relationship to bang APIs and CLI/UI formatting, but do not add a public `%RunError{}` or any UI-ready response shape per D-04.

In the same file, audit the runner boundary for D-14: keep `module_in_allowlist/2` and any adjacent error-normalization code narrowly scoped so only the existing module-resolution `ArgumentError` path becomes `nil`/config failure. Do not add generic `rescue`, `try`, or silent conversion paths that would hide a divergent `{:error, term}` outcome from tests or operators.
</action>
<acceptance_criteria>
- `grep -n "## Runner-library contract" scrypath_ops/lib/scrypath_ops/playbook/runner.ex` returns one match.
- `grep -n "%Scrypath.SearchResult{}\\|%Scrypath.MultiSearchResult{}\\|\\{:error, reason\\}" scrypath_ops/lib/scrypath_ops/playbook/runner.ex` returns matches from the new contract section.
- `grep -n "RunFailure\\|PlaybookLive\\|Scrypath.Errors\\|Mix" scrypath_ops/lib/scrypath_ops/playbook/runner.ex` returns matches documenting downstream formatting ownership.
- `rg -n "%RunError\\{|defexception|rescue\\s*$|rescue\\n" scrypath_ops/lib/scrypath_ops/playbook/runner.ex` does not introduce a new public error struct or a broad rescue block beyond the existing module-resolution guard.
</acceptance_criteria>
<verify>
  <automated>mix test scrypath_ops/test/scrypath_ops/playbook/runner_test.exs</automated>
</verify>
<done>`Runner` has the single canonical execution-contract section, preserves the raw tuple seam per D-01/D-02/D-04, and does not add a new silent swallow path.</done>
</task>

<task id="66-01-02" type="auto">
<name>Task 2: Keep the playbook schema doc as wire-format authority only</name>
<files>scrypath_ops/docs/playbook-schema-v1.md</files>
<read_first>
- scrypath_ops/docs/playbook-schema-v1.md
- scrypath_ops/lib/scrypath_ops/playbook/runner.ex
- .planning/phases/66-runner-library-contract/66-CONTEXT.md
- .planning/phases/66-runner-library-contract/66-PATTERNS.md
</read_first>
<action>
Add a short top-level note in `scrypath_ops/docs/playbook-schema-v1.md` linking to the `ScrypathOps.Playbook.Runner` `## Runner-library contract` section per D-07. Keep this file authoritative for JSON shape, allowed keys, caps, and troubleshooting anchors only. Do not duplicate the runtime success/error matrix, UI copy, or bang-vs-non-bang semantics here; the link should explicitly send maintainers to `Runner` for execution results while preserving existing troubleshooting anchors consumed by `DocResolver`.
</action>
<acceptance_criteria>
- `grep -n "Runner-library contract\\|ScrypathOps.Playbook.Runner" scrypath_ops/docs/playbook-schema-v1.md` returns at least one new contract-link line.
- `grep -n "%Scrypath.SearchResult{}\\|%Scrypath.MultiSearchResult{}\\|\\{:error, reason\\}" scrypath_ops/docs/playbook-schema-v1.md` returns no matches.
- `grep -n "## Troubleshooting" scrypath_ops/docs/playbook-schema-v1.md` still returns a match after the edit.
</acceptance_criteria>
<verify>
  <automated>mix test test/scrypath/docs_contract_test.exs</automated>
</verify>
<done>The schema guide points to `Runner` for execution semantics while remaining the sole wire-format authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| playbook JSON -> `Runner.run_validated/3` | Untrusted schema/module strings and opts enter the execution seam here |
| runner result -> `RunFailure` / `PlaybookLive` / Mix formatting | Raw reasons cross into presentation layers after contract evaluation |
| docs text -> operators/maintainers | Canonical contract wording can drift or over-promise if duplicated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | D | `scrypath_ops/lib/scrypath_ops/playbook/runner.ex` | mitigate | Keep module resolution on `String.to_existing_atom/1` only and do not widen rescue scope beyond the current `ArgumentError` guard so playbooks cannot trigger atom exhaustion or hidden normalization. |
| T-66-02 | I | `scrypath_ops/docs/playbook-schema-v1.md` | mitigate | Keep runtime contract text in `Runner` only and link from the schema doc so docs do not leak or duplicate UI-facing details as a fake contract. |
| T-66-03 | R | `scrypath_ops/lib/scrypath_ops/playbook/runner.ex` | mitigate | Preserve raw `{:error, reason}` propagation and forbid new generic rescue/swallow paths that would erase divergent failures before tests or operators can see them. |
</threat_model>

<verification>
Run:
- `mix test scrypath_ops/test/scrypath_ops/playbook/runner_test.exs`
- `mix test test/scrypath/docs_contract_test.exs`
</verification>

<success_criteria>
- A maintainer can cite `ScrypathOps.Playbook.Runner` as the single runner-library contract location.
- The playbook schema guide links to that contract instead of restating execution semantics.
- No new broad rescue or error-swallow branch is added at the runner boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/66-runner-library-contract/66-01-SUMMARY.md`
</output>
