---
phase: 67
plan: "01"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs
  - scrypath_ops/test/scrypath_ops/playbook/run_failure_test.exs
  - scrypath_ops/test/scrypath_ops/playbook/doc_resolver_test.exs
  - scrypath_ops/test/scrypath_ops/playbook/examples_contract_test.exs
  - test/scrypath/docs_contract_test.exs
  - scrypath_ops/lib/scrypath_ops/playbook/doc_resolver.ex
  - scrypath_ops/docs/playbook-schema-v1.md
autonomous: true
requirements:
  - OPS3-04
must_haves:
  truths:
    - "Execution-surface tests fail if the bounded PlaybookLive run contract drifts on running state, `Run finished` summary prefix, failure panel selector, `Cancel run`, or `Copy diagnostics`."
    - "`DocResolver` tests freeze repo-relative doc path+fragment targets and fail when a mapped anchor is missing on disk."
    - "Root maintainer docs contracts fail if contributor instructions, `mix verify.opsui`, `mix scrypath_ops.playbooks.validate examples/playbooks`, or the canonical JTBD fixture filenames drift."
    - "`mix verify.opsui` remains an orchestration-only umbrella around `scrypath_ops` tests rather than becoming a second contract source."
  artifacts:
    - path: "scrypath_ops/test/scrypath_ops/playbook/doc_resolver_test.exs"
      provides: "Relative path+fragment and on-disk anchor coverage for `DocResolver`"
    - path: "scrypath_ops/test/scrypath_ops/playbook/examples_contract_test.exs"
      provides: "Shipped-fixture contract coverage for canonical playbook examples and docs references"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Maintainer-facing command and canonical example filename contract checks"
  key_links:
    - from: "scrypath_ops/lib/scrypath_ops/playbook/doc_resolver.ex"
      to: "scrypath_ops/docs/playbook-schema-v1.md"
      via: "repo-relative fragment mappings whose anchors must exist on disk"
      pattern: "invalid_entries|invalid_entry_shape|no_schema|invalid_query|page_size_out_of_range|troubleshooting"
    - from: "scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs"
      to: "scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex"
      via: "bounded selector and phrase assertions only"
      pattern: "Running playbook|Run finished|run-failure-panel|Cancel run|Copy diagnostics"
    - from: "test/scrypath/docs_contract_test.exs"
      to: "CONTRIBUTING.md"
      via: "exact contributor-command and canonical fixture filename assertions"
      pattern: "mix verify\\.opsui|mix scrypath_ops\\.playbooks\\.validate examples/playbooks|sync_triage_posts_recent|federation_inspect_posts_and_comments"
---

# Plan 01 — Bounded execution verification contracts

<objective>
Add the layered Phase 67 contract suite that protects the operator-facing execution surface without freezing the entire OPSUI copy surface or turning `mix verify.opsui` into a parser.

Purpose: implement D-01 through D-07 against the canonical JTBD fixtures and contributor-doc wording established in plan 02.
Output: focused `scrypath_ops` contract tests, root maintainer docs-contract checks, and a resolved `DocResolver` anchor drift between code and `playbook-schema-v1.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/67-verification-jtbd-examples-milestone-bookkeeping/67-CONTEXT.md
@.planning/phases/67-verification-jtbd-examples-milestone-bookkeeping/67-RESEARCH.md
@.planning/phases/67-verification-jtbd-examples-milestone-bookkeeping/67-PATTERNS.md
@.planning/phases/65-playbook-run-lifecycle-opsui/65-VERIFICATION.md
@.planning/phases/66-runner-library-contract/66-01-PLAN.md
@.planning/phases/66-runner-library-contract/66-02-PLAN.md
@lib/mix/tasks/verify.opsui.ex
@test/scrypath/docs_contract_test.exs
@CONTRIBUTING.md
@scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
@scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs
@scrypath_ops/lib/scrypath_ops/playbook/doc_resolver.ex
@scrypath_ops/lib/scrypath_ops/playbook/run_failure.ex
@scrypath_ops/test/scrypath_ops/playbook/run_failure_test.exs
@scrypath_ops/docs/playbook-schema-v1.md
@scrypath_ops/docs/team-playbook-persistence.md
@scrypath_ops/docs/operator-ia.md
@scrypath_ops/test/scrypath_ops/mix/playbooks_validate_test.exs

<interfaces>
From `scrypath_ops/lib/scrypath_ops/playbook/doc_resolver.ex`:
```elixir
@spec resolve(doc_ref()) :: %{primary: String.t(), related: [String.t()]}
def resolve(doc_ref) when is_atom(doc_ref) do
```

From `scrypath_ops/lib/scrypath_ops/playbook/run_failure.ex`:
```elixir
@spec enrich(term(), context()) :: map()
def enrich(reason, context \\ []) do
```

From `scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex`:
```elixir
defp run_result_summary(%SearchResult{} = r), do: "Run finished — hits: #{length(r.hits)}"
defp run_result_summary(%MultiSearchResult{} = r),
  do: "Run finished — #{length(r.ordered)} schema(s)."
```
</interfaces>
</context>

<source_audit>
- GOAL: Phase 67 success criterion #1 requires execution UI strings, doc anchors, and contributor instructions to fail loudly on drift; this plan owns that bounded verification layer.
- REQ: `OPS3-04` is covered here via `scrypath_ops` execution-surface contracts plus root maintainer docs-contract checks.
- RESEARCH: Follow the researched split exactly: LiveView bounded phrases/selectors, `DocResolver` path+fragment coverage with on-disk anchors, fixture contract coverage, and no parser logic in `mix verify.opsui`.
- CONTEXT: D-01 through D-07 are implemented here, including D-06 relative path+fragment testing and the research-flagged `invalid_entries` / `invalid_entry_shape` anchor drift.
- PREREQ: `scrypath_ops` test commands and `mix verify.opsui` require a responding local PostgreSQL service; root `docs_contract_test.exs` remains runnable without that dependency.
</source_audit>

<tasks>

<task id="67-01-01" type="auto" tdd="true">
<name>Task 1: Lock `DocResolver` and `RunFailure` to truthful relative doc targets</name>
<files>scrypath_ops/lib/scrypath_ops/playbook/doc_resolver.ex, scrypath_ops/test/scrypath_ops/playbook/doc_resolver_test.exs, scrypath_ops/test/scrypath_ops/playbook/run_failure_test.exs, scrypath_ops/docs/playbook-schema-v1.md</files>
<read_first>
- scrypath_ops/lib/scrypath_ops/playbook/doc_resolver.ex
- scrypath_ops/lib/scrypath_ops/playbook/run_failure.ex
- scrypath_ops/test/scrypath_ops/playbook/run_failure_test.exs
- scrypath_ops/docs/playbook-schema-v1.md
- .planning/phases/67-verification-jtbd-examples-milestone-bookkeeping/67-CONTEXT.md
- .planning/phases/67-verification-jtbd-examples-milestone-bookkeeping/67-RESEARCH.md
</read_first>
<behavior>
- Test 1: every `DocResolver` mapping exercised by `RunFailure.doc_ref/1` resolves to the expected repo-relative `path#fragment` target, not a frozen absolute URL.
- Test 2: each mapped primary fragment for `playbook-schema-v1.md` exists on disk, including the currently drifting `invalid_entries` and `invalid_entry_shape` cases.
- Test 3: `RunFailure.enrich/2` still returns the stable bounded fields (`failure_class`, `reason`, `message`, `copy`, `doc`) while leaving the base URL configurable.
</behavior>
<action>
Implement D-02, D-04, and D-06 by adding a focused `DocResolver` contract test module and tightening `RunFailureTest`. The tests should assert repo-relative path+fragment mappings and on-disk anchor existence, then verify the public `RunFailure.enrich/2` payload still contains only the stable response-shape fields called out in D-04.

Resolve the researched drift truthfully: `DocResolver` currently points `playbook_schema_invalid_entries` and `playbook_schema_invalid_entry_shape` at anchors missing from `playbook-schema-v1.md`. Fix that by making code and docs agree in the narrowest honest way. Either add those troubleshooting anchors to `playbook-schema-v1.md` with symptom/cause/fix text matching the existing schema guide style, or remap the affected reasons to existing truthful anchors if that is more accurate. Do not add a new manifest file, do not freeze absolute GitHub URLs, and do not widen the `DocResolver` API beyond the smallest helper needed for relative-path assertions per D-06.
</action>
<acceptance_criteria>
- `rg -n "invalid_entries|invalid_entry_shape" scrypath_ops/lib/scrypath_ops/playbook/doc_resolver.ex scrypath_ops/docs/playbook-schema-v1.md` shows matching code/doc coverage for whichever truthful resolution was chosen.
- `rg -n "failure_class|reason|message|copy|doc" scrypath_ops/test/scrypath_ops/playbook/run_failure_test.exs` returns assertions on the bounded `RunFailure` response fields.
- `mix test scrypath_ops/test/scrypath_ops/playbook/doc_resolver_test.exs scrypath_ops/test/scrypath_ops/playbook/run_failure_test.exs` exits 0.
</acceptance_criteria>
<verify>
  <automated>mix test scrypath_ops/test/scrypath_ops/playbook/doc_resolver_test.exs scrypath_ops/test/scrypath_ops/playbook/run_failure_test.exs</automated>
</verify>
<done>`DocResolver` path+fragment mappings and schema-guide anchors are truthful and regression-tested, and `RunFailure` still exposes only the bounded stable fields per D-04.</done>
</task>

<task id="67-01-02" type="auto" tdd="true">
<name>Task 2: Add bounded execution and maintainer docs contracts without enlarging `mix verify.opsui`</name>
<files>scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs, scrypath_ops/test/scrypath_ops/playbook/examples_contract_test.exs, test/scrypath/docs_contract_test.exs</files>
<read_first>
- scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs
- scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
- test/scrypath/docs_contract_test.exs
- CONTRIBUTING.md
- lib/mix/tasks/verify.opsui.ex
- scrypath_ops/docs/team-playbook-persistence.md
- scrypath_ops/docs/operator-ia.md
- .planning/phases/67-verification-jtbd-examples-milestone-bookkeeping/67-CONTEXT.md
</read_first>
<behavior>
- Test 1: the bounded PlaybookLive contract remains present for `Running playbook`, `Run finished` prefix, `data-testid="run-failure-panel"`, `Cancel run`, and `Copy diagnostics`.
- Test 2: shipped canonical fixtures validate on disk and are the exact filenames referenced from contributor/operator docs.
- Test 3: root maintainer docs contracts fail when `mix verify.opsui`, `mix scrypath_ops.playbooks.validate examples/playbooks`, or the two canonical fixture filenames drift from contributor docs.
</behavior>
<action>
Implement D-01 through D-05 and D-07 with two ExUnit layers. First, extend `PlaybookLiveTest` only around the bounded operator contract: stable selectors and phrases, not full copy snapshots. Keep helper flashes, long success summaries after the `Run finished` prefix, and non-contract phrasing flexible per D-05. Second, add a fixture contract test under `scrypath_ops/test/` that validates the shipped `examples/playbooks/` fixtures and asserts the docs references introduced by plan 02 point at the exact canonical filenames.

Then extend the root `DocsContractTest` only for maintainer-facing truth per D-03: contributor instructions still name `mix verify.opsui`, still name `mix scrypath_ops.playbooks.validate examples/playbooks`, and reference the exact `sync_triage_posts_recent.json` and `federation_inspect_posts_and_comments.json` filenames. Do not pull optional-app UI parsing into the root suite, and do not modify `lib/mix/tasks/verify.opsui.ex` unless it has drifted away from its existing orchestration-only `cd scrypath_ops && mix deps.get && mix test` role.

Keep the execution prerequisites explicit: `scrypath_ops` tests run against the optional Phoenix app’s existing Postgres-backed setup, so plan verification must separate root docs contracts from the delegated `scrypath_ops` test commands instead of mixing both path families into one `mix test` invocation.
</action>
<acceptance_criteria>
- `rg -n "Running playbook|Run finished|run-failure-panel|Cancel run|Copy diagnostics" scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` returns bounded execution-contract assertions.
- `rg -n "sync_triage_posts_recent\\.json|federation_inspect_posts_and_comments\\.json" scrypath_ops/test/scrypath_ops/playbook/examples_contract_test.exs test/scrypath/docs_contract_test.exs CONTRIBUTING.md scrypath_ops/docs/team-playbook-persistence.md scrypath_ops/docs/operator-ia.md` returns the canonical fixture filenames across tests and docs.
- `rg -n "cd scrypath_ops && mix deps.get && mix test|verify\\.opsui" lib/mix/tasks/verify.opsui.ex` still shows orchestration-only behavior, with no contract parsing or fixture manifest logic.
- `mix test scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs scrypath_ops/test/scrypath_ops/playbook/examples_contract_test.exs` exits 0.
- `mix test test/scrypath/docs_contract_test.exs` exits 0.
</acceptance_criteria>
<verify>
  <automated>mix test scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs scrypath_ops/test/scrypath_ops/playbook/examples_contract_test.exs && mix test test/scrypath/docs_contract_test.exs && mix verify.opsui</automated>
</verify>
<done>The execution contract is frozen at the intended bounded seams, canonical fixture/docs drift is caught automatically, and `mix verify.opsui` stays a wrapper rather than a second truth source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `PlaybookLive` UI -> operator expectations | Stable affordances and honesty wording can drift without backend failures |
| `RunFailure` / `DocResolver` -> docs on disk | Resolver mappings can point at non-existent anchors or stale docs |
| contributor docs -> maintainers | Verify commands and canonical example filenames can drift from reality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | R | `scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` | mitigate | Freeze only the bounded selectors/phrases in D-04 so operators get stable truth without converting the suite into brittle prose snapshots. |
| T-67-02 | I | `scrypath_ops/lib/scrypath_ops/playbook/doc_resolver.ex` | mitigate | Add path+fragment and anchor-existence tests so `DocResolver` cannot silently point to missing or misleading docs. |
| T-67-03 | T | `test/scrypath/docs_contract_test.exs` | mitigate | Lock maintainer commands and canonical filenames in root docs contracts so contributor instructions cannot drift silently. |
</threat_model>

<verification>
Run:
- `mix test scrypath_ops/test/scrypath_ops/playbook/doc_resolver_test.exs scrypath_ops/test/scrypath_ops/playbook/run_failure_test.exs`
- `mix test scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs scrypath_ops/test/scrypath_ops/playbook/examples_contract_test.exs`
- `mix test test/scrypath/docs_contract_test.exs`
- `mix verify.opsui`

Prerequisite:
- local PostgreSQL is running for the `scrypath_ops` test commands and `mix verify.opsui`
</verification>

<success_criteria>
- Phase 67 bounded execution contracts fail on the intended UI drift points and stay silent on non-contract copy polish.
- `DocResolver` and `playbook-schema-v1.md` no longer disagree about troubleshooting anchors.
- Contributor docs, fixture filenames, and `mix verify.opsui` usage are locked by automation without making the root suite parse optional-app HTML.
</success_criteria>

<output>
After completion, create `.planning/phases/67-verification-jtbd-examples-milestone-bookkeeping/67-01-SUMMARY.md`
</output>
