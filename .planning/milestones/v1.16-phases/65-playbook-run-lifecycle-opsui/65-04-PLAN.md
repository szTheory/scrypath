---
phase: 65
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs
autonomous: true
requirements:
  - OPS3-01
  - OPS3-02
---

# Plan 04 — LiveView tests and CI proof

<objective>
Extend `PlaybookLiveTest` to cover async run completion with `render_async`, catalog **run_now** path, forced failure showing structured fields and a working **primary** doc URL, and assert telemetry `:start`/`:stop` events fire (via `:telemetry.attach` in test).
</objective>

<threat_model>
**ASVS L1:** Tests must not embed real API keys; use existing stub adapter env from setup only.
</threat_model>

<tasks>

<task id="65-04-01" type="execute">
<read_first>
- scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs
- scrypath_ops/test/support/search_playground_stub_adapter.ex
- scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
</read_first>
<action>
Update existing `"run with stub adapter shows success"` (and similar) to call `render_click` then **`render_async(view)`** (or documented LV 1.0 helper) before asserting **Run finished** text.

Add test **"catalog run_now runs playbook async"**: ensure workspace or examples list has a row — use paste+save pattern from OPS-PB-05 to create a temp basename, then `render_click(view, "run_now", %{"name" => basename})`, `render_async(view)`, assert success panel or flash + summary string.

Add test **"forced failure shows failure_class and primary doc link"**: set `Application.put_env(:scrypath_ops, :search_stub_variant, :hard_error)` inside test after setup (or in test body before run), use `search_many` playbook JSON from existing test, trigger run + `render_async`, assert rendered HTML includes substring **`failure_class`** OR the exact CSS/testid `data-testid="run-failure-panel"` (add that attribute in Plan 03 if not present — if adding here, patch LiveView in this plan), and assert HTML contains **`https://`** substring for doc link.

Attach telemetry handler in test: `:telemetry.attach("test-playbook-#{unique}", [:scrypath_ops, :playbook_run, :stop], fn _event, meas, meta, _ -> send(self(), {:telemetry_stop, meas, meta}) end, nil)` and assert one `:stop` event per completed run with `meta.run_id` present.
</action>
<acceptance_criteria>
- `grep -n "render_async" scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` returns at least one line.
- `grep -n "run_now" scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` returns at least one line.
- `grep -n "hard_error\|:stub_hard_failure\|failure_class\|run-failure" scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` matches at least one line (forced failure coverage).
- `mix test scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` exits 0.
- `mix verify.opsui` exits 0 from repository root.
</acceptance_criteria>
</task>

</tasks>

<verification>
`mix verify.opsui`
</verification>

<success_criteria>
- Roadmap success criterion #2 satisfied: forced failure shows non-empty structured class/message and at least one working outbound `https` link in rendered output (validated by substring assertions).
</success_criteria>
