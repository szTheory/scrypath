---
phase: 159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov
plan: "08"
type: execute
wave: 6
depends_on: [159-07]
files_modified:
  - .github/workflows/ci.yml
  - scripts/ci_monitor.cjs
  - test/scripts/ci_monitor_test.exs
  - test/scrypath/no_human_gates_contract_test.exs
  - lib/mix/tasks/verify.phase99.ex
  - test/mix/tasks/workflow_wiring_test.exs
  - CONTRIBUTING.md
  - AGENTS.md
  - .planning/PROJECT.md
  - .planning/v1.37-MILESTONE-AUDIT.md
  - .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-VALIDATION.md
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "D-22: maintainers read one internally consistent final v1.37 audit disposition, not simultaneous passed and failed conclusions."
    - "The final audit derives its 31 requirement dispositions, 11 phase-validation results, integration result, flow result, and closure verdict from the current canonical matrix, retrospective triples, validation indexes, and exact-SHA closure receipt."
    - "TEST-05 is closed by the scheduled/manual coverage lane and exact-SHA hosted artifact proof, while TEST-01 retains only the four-probe historically-unprovable chronology waiver from D-11/D-22."
    - "D-04/D-05: executable regression guards bind always-upload behavior to the named coverage upload step and bind the artifact SHA label to the coverage checkout source."
    - "The final passed disposition is authorized by a successful exact-SHA GitHub Actions closeout attestation, never by an inferred or simulated human identity."
    - "Future incomplete plans and active verification artifacts cannot introduce post-implementation human verification or unresolved UAT debt."
    - "Original Phase 148–158 requirement ownership and the established required/advisory/path-scoped CI posture remain unchanged."
  artifacts:
    - path: test/mix/tasks/workflow_wiring_test.exs
      provides: "Step-scoped coverage upload and checkout/source-SHA regression guards"
      contains: "TEST-05 scheduled/manual informational coverage"
    - path: .planning/v1.37-MILESTONE-AUDIT.md
      provides: "Single final v1.37 audit with current scores, evidence links, requirement/phase dispositions, and bounded TEST-01 waiver"
      contains: "## Final Verdict"
    - path: .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-VALIDATION.md
      provides: "Two-stage candidate and final exact-SHA machine-attestation policy for the corrected audit"
      contains: "## Automated exact-SHA closeout authority"
  key_links:
    - from: .planning/v1.37-MILESTONE-AUDIT.md
      to: .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-EVIDENCE-MATRIX.md
      via: "31 requirement rows preserve canonical evidence class and original owner"
      pattern: "159-EVIDENCE-MATRIX\\.md"
    - from: .planning/v1.37-MILESTONE-AUDIT.md
      to: .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-CLOSURE-RECEIPT.md
      via: "TEST-05 and hosted conclusions cite the exact-SHA run and retained coverage artifact"
      pattern: "159-CLOSURE-RECEIPT\\.md"
    - from: .planning/v1.37-MILESTONE-AUDIT.md
      to: .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-HISTORICAL-PROBES.md
      via: "TEST-01 waiver stays limited to four failed parent-snapshot probes"
      pattern: "159-HISTORICAL-PROBES\\.md"
---

<objective>
Close the remaining audit contradiction and actionable provenance/regression-guard warnings with one truthful final disposition per D-04, D-05, and D-22.

Purpose: Make the audit a reliable closeout artifact whose active verdict, scores, tables, evidence links, residual limitation, regression guards, and exact-SHA machine authority agree.
Output: Step-scoped TEST-05 workflow assertions, a semantically verified `.planning/v1.37-MILESTONE-AUDIT.md`, durable zero-human repository contracts, and two-stage exact-SHA GitHub Actions evidence, with TEST-05 closed and only the narrow TEST-01 chronology waiver retained.

No new requirement is assigned to Phase 159; `requirements: []` preserves original Phase 148–158 ownership per D-16.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.37-MILESTONE-AUDIT.md
@.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-CONTEXT.md
@.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-VERIFICATION.md
@.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-EVIDENCE-MATRIX.md
@.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-HISTORICAL-PROBES.md
@.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-CLOSURE-RECEIPT.md
@.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-VALIDATION.md
@.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-07-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind the TEST-05 regression guard to the actual upload and checkout steps</name>
  <reversibility rating="reversible">The focused assertions and local parsing helper are version-controlled test changes that can be revised without changing the workflow contract.</reversibility>
  <files>test/mix/tasks/workflow_wiring_test.exs</files>
  <read_first>
    test/mix/tasks/workflow_wiring_test.exs
    .github/workflows/ci.yml
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-VERIFICATION.md
    prompts/elixir-oss-lib-ci-cd-best-practices-deep-research.md
  </read_first>
  <behavior>
    - D-04/D-05: the named `Upload informational coverage report` step itself contains `if: always()`, the pinned upload action, the SHA-labelled artifact name, `cover/`, warn-on-missing, and seven-day retention.
    - D-04/D-05: the coverage job's checkout step has no `ref` override, so `${{ github.sha }}` labels the same source snapshot that the job checks out and tests.
    - Moving either property to an unrelated step or adding a coverage checkout override makes the focused test fail.
  </behavior>
  <action>Refactor only the TEST-05 coverage test and its private parsing helpers. Extract the named upload step from the already-isolated `coverage` job and perform every upload-policy assertion against that step, not against the whole job. Extract the coverage checkout step separately; assert its immutable v6 action pin and `fetch-depth: 0`, reject a `ref:` override within that checkout step, and keep the SHA-labelled artifact assertion inside the upload step. Use indentation/step-boundary parsing local to the test module so an unrelated `if: always()` or artifact name elsewhere cannot satisfy the contract. Preserve all existing job-level event, advisory, canonical-command, secret, permission, and cardinality assertions per D-01–D-05; do not change `.github/workflows/ci.yml` because the verifier found the current workflow correct.</action>
  <acceptance_criteria>
    - The coverage upload policy is asserted against the named upload step rather than `coverage_job` as a whole.
    - The SHA-labelled artifact is coupled to a coverage checkout step with the pinned v6 action, full history, and no source-ref override.
    - Existing TEST-05 event, advisory, command, least-privilege, and single-job assertions remain intact.
    - The focused workflow-wiring suite passes with warnings fatal.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/workflow_wiring_test.exs --warnings-as-errors</automated>
  </verify>
  <done>The executable TEST-05 contract fails if always-upload behavior drifts away from the coverage upload step or if the report's SHA label can diverge from the checked-out source.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the final audit with semantic provenance checks and an explicit pending-exact-SHA state</name>
  <reversibility rating="reversible">The audit is a version-controlled evidence transform and can be regenerated from its cited immutable inputs.</reversibility>
  <files>.planning/v1.37-MILESTONE-AUDIT.md</files>
  <read_first>
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-VERIFICATION.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-CONTEXT.md
    .planning/REQUIREMENTS.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-EVIDENCE-MATRIX.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-HISTORICAL-PROBES.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-CLOSURE-RECEIPT.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-VALIDATION.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-07-SUMMARY.md
    test/mix/tasks/workflow_wiring_test.exs
    .planning/v1.37-MILESTONE-AUDIT.md
  </read_first>
  <action>Rewrite `.planning/v1.37-MILESTONE-AUDIT.md` as a single final audit rather than appending another preface. Replace stale inventories and active body with current facts derived from the cited artifacts: requirement score `30/31 plus narrow TEST-01 chronology waiver`; phase score `11/11`; integration score `10/10`; flow score `8/8`; no remaining repairable implementation/evidence gaps; and a separately named waiver register limited to TEST-01 and the four extraction-SHA probe identifiers that `159-HISTORICAL-PROBES.md` classifies `historically unprovable`. Until Task 3 completes the two-stage hosted promotion, keep frontmatter status and the closeout decision explicitly `pending_exact_sha_ci`; do not claim final passed closeout from the unattributed historical `reviewer: approved` record. Build the canonical 31-row table per D-13 from `159-EVIDENCE-MATRIX.md`, using requirement ID and original owner as the first two columns so they can be mechanically diffed against both the matrix and `.planning/REQUIREMENTS.md`; preserve every Phase 148–158 owner per D-16 together with its evidence class, limitation, and disposition. Make TEST-01 the only requirement row with a historically-unprovable disposition and never describe it as a historical pass. Build an 11-row phase table whose Phase 148–158 rows each contain one relative Markdown link to that phase's existing retrospective `SUMMARY.md`, `VERIFICATION.md`, and `VALIDATION.md`; every link must resolve from the audit's directory. State that TEST-05 is wired through `mix verify.coverage`, the scheduled/manual advisory job, the now step-scoped regression guard, hosted run `33014343041`, exact source SHA `a35874178b79392caa0f3c1dcc010ea149e1e5bf`, and its retained SHA-bound artifact, citing `159-CLOSURE-RECEIPT.md`. Keep required, advisory, and ScrypathOps path-scoped lanes in their established categories and retain the no-Hex-publish/no-release-parity-reopening boundary. Include final verdict, scores, source inputs, requirement coverage, phase/Nyquist coverage, integration, flows, exact-SHA evidence, waiver register, machine authority, and closeout decision. State that final authorization comes only from the `closeout-attestation` job and its exact-SHA artifact. Remove superseded failure narratives from the active document without embedding an archived copy. Do not alter canonical matrix/probe/receipt/retrospective evidence or requirement ownership; perform final closeout only per D-22.</action>
  <acceptance_criteria>
    - Frontmatter, final verdict, scores, requirement table, phase table, integration table, flow table, and closeout decision all express the same evidence disposition and the same pending-exact-SHA-CI state.
    - Exactly 31 original requirement-ID/original-owner pairs match both `REQUIREMENTS.md` and the canonical matrix, and all 11 phase rows point to three existing retrospective files.
    - TEST-05 cites the exact hosted run/source SHA, retained coverage artifact, and focused step-scoped test result as closed; TEST-01 alone retains exactly the four canonical probe identifiers in the historically-unprovable waiver register.
    - The audit links the canonical matrix, historical-probe receipt, closure receipt, and Phase 159 validation without copying raw logs or inventing contemporaneous execution history.
    - No active statement reports the pre-closure coverage topology, missing retrospective evidence, or zero formal score; final closeout remains explicitly pending exact-SHA CI.
    - No file other than `.planning/v1.37-MILESTONE-AUDIT.md` changes in this task.
  </acceptance_criteria>
  <verify>
    <automated>audit=.planning/v1.37-MILESTONE-AUDIT.md; matrix=.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-EVIDENCE-MATRIX.md; probes=.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-HISTORICAL-PROBES.md; scratch=$(mktemp -d); trap 'rm -rf "$scratch"' EXIT; pair_rows() { awk -F'|' '/^\| (SAFE|ARCH|TEST|CI|PERF|QUAL)-[0-9][0-9] \| Phase 1(4[8-9]|5[0-8]) \|/ {id=$2; owner=$3; gsub(/^[[:space:]]+|[[:space:]]+$/, "", id); gsub(/^[[:space:]]+|[[:space:]]+$/, "", owner); print id "|" owner}' "$1" | sort; }; pair_rows .planning/REQUIREMENTS.md &gt; "$scratch/requirements"; pair_rows "$matrix" &gt; "$scratch/matrix"; pair_rows "$audit" &gt; "$scratch/audit"; test "$(wc -l &lt; "$scratch/requirements" | tr -d ' ')" -eq 31 &amp;&amp; diff -u "$scratch/requirements" "$scratch/matrix" &amp;&amp; diff -u "$scratch/requirements" "$scratch/audit" &amp;&amp; test "$(awk -F'|' '/^\| (SAFE|ARCH|TEST|CI|PERF|QUAL)-[0-9][0-9] \|/ &amp;&amp; tolower($0) ~ /historically unprovable/ {gsub(/ /,"",$2); print $2}' "$audit")" = TEST-01 &amp;&amp; awk -F'|' '/^\| (SAFE|ARCH|TEST|CI|PERF|QUAL)-[0-9][0-9] \|/ &amp;&amp; tolower($0) ~ /waiv/ {gsub(/ /,"",$2); print $2}' "$audit" | sort -u &gt; "$scratch/waived_ids" &amp;&amp; test "$(cat "$scratch/waived_ids")" = TEST-01 &amp;&amp; awk -F'|' '/^\| [^|]+ \| `[0-9a-f]{40}` \| `[0-9a-f]{40}` \|/ {sha=$3; gsub(/[` ]/,"",sha); print sha}' "$probes" | sort -u &gt; "$scratch/probes" &amp;&amp; awk -F'|' '/^\| TEST-01 \|/ {while (match($0, /[0-9a-f]{40}/)) {print substr($0,RSTART,RLENGTH); $0=substr($0,RSTART+RLENGTH)}}' "$audit" | sort -u &gt; "$scratch/audit_probes" &amp;&amp; test "$(wc -l &lt; "$scratch/probes" | tr -d ' ')" -eq 4 &amp;&amp; diff -u "$scratch/probes" "$scratch/audit_probes" &amp;&amp; for phase in $(seq 148 158); do row=$(rg "^\| $phase \|" "$audit"); test "$(printf '%s\n' "$row" | rg -o '\(phases/[^)]*/'"$phase"'-(SUMMARY|VERIFICATION|VALIDATION)\.md\)' | wc -l | tr -d ' ')" -eq 3 || exit 1; printf '%s\n' "$row" | rg -o '\(phases/[^)]*/'"$phase"'-(SUMMARY|VERIFICATION|VALIDATION)\.md\)' | tr -d '()' | while IFS= read -r link; do test -f ".planning/$link" || exit 1; done || exit 1; done &amp;&amp; rg -q '^status: pending_exact_sha_ci$' "$audit" &amp;&amp; rg -q '159-EVIDENCE-MATRIX\.md' "$audit" &amp;&amp; rg -q '159-HISTORICAL-PROBES\.md' "$audit" &amp;&amp; rg -q '159-CLOSURE-RECEIPT\.md' "$audit" &amp;&amp; rg -q 'closeout-attestation' "$audit" &amp;&amp; ! rg -q 'GAPS FOUND|0/31|no scheduled/manual workflow invokes|all phase evidence missing|\| Missing \| Missing \|' "$audit" &amp;&amp; git diff --check -- "$audit"</automated>
  </verify>
  <done>The corrected audit is semantically reconciled to canonical ownership, all retrospective triples, and exactly four TEST-01 probes, with its sole remaining closeout condition truthfully marked as exact-SHA CI.</done>
</task>

<task type="auto">
  <name>Task 3: Promote the corrected audit through two exact-SHA CI attestations</name>
  <reversibility rating="reversible">The branch remains unmergeable until both hosted gates pass; failed candidates are repaired in new commits without rewriting historical evidence.</reversibility>
  <files>.github/workflows/ci.yml, scripts/ci_monitor.cjs, test/scripts/ci_monitor_test.exs, test/scrypath/no_human_gates_contract_test.exs, lib/mix/tasks/verify.phase99.ex, test/mix/tasks/workflow_wiring_test.exs, CONTRIBUTING.md, AGENTS.md, .planning/PROJECT.md, .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-VALIDATION.md, .planning/v1.37-MILESTONE-AUDIT.md</files>
  <read_first>
    .planning/v1.37-MILESTONE-AUDIT.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-VALIDATION.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-VERIFICATION.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-CLOSURE-RECEIPT.md
  </read_first>
  <action>Add a workflow-dispatch-only `closeout-attestation` job that depends on the five required jobs and coverage, consumes the coverage step outcome plus immutable artifact outputs, fails closed on any missing or non-success input, and uploads a SHA-bound JSON attestation. Add an observable `scripts/ci_monitor.cjs closeout` command that optionally pushes, dispatches only the requested branch, selects only a newly created `workflow_dispatch` run whose `headSha` equals the requested full SHA, waits, verifies the exact job set and both live SHA-bound artifacts, and emits JSON without changing tracked files. Add offline fake-CLI coverage for success, required-job failure, and missing artifact. Add a required repository contract that rejects post-implementation human verification markers in incomplete plans and rejects active human-needed/UAT debt. Document the machine-only policy in project and contributor instructions. Run a candidate exact-SHA attestation while the audit is pending. After it passes, update the audit and validation to final passed-with-bounded-waiver machine authority, create SUMMARY/VERIFICATION and shared completion tracking, commit all final tracked state, then run the exact-SHA attestation again. Make no tracked writes after the final run. On failure, return the audit to pending in the repair commit and repeat without a human gate.</action>
  <acceptance_criteria>
    - Candidate and final hosted runs both use `workflow_dispatch`, match their requested exact SHA, and contain successful required, coverage, and `closeout-attestation` jobs.
    - Both coverage and closeout artifacts are live, SHA-bound, and carry non-empty immutable digests.
    - The final audit cites machine authority and changes to `status: passed` only after the candidate gate succeeds; final phase completion is authoritative only after the final gate succeeds.
    - No incomplete plan, active verification, or active UAT artifact requires post-implementation human verification.
    - Existing required/advisory/path-scoped topology, original ownership, and the exact four-probe TEST-01 waiver remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/workflow_wiring_test.exs test/scripts/ci_monitor_test.exs test/scrypath/no_human_gates_contract_test.exs --warnings-as-errors &amp;&amp; mix verify.repository_contracts &amp;&amp; node scripts/ci_monitor.cjs check-actions .github/workflows/ci.yml &amp;&amp; node scripts/ci_monitor.cjs closeout --push --branch "$(git branch --show-current)" --sha "$(git rev-parse HEAD)"</automated>
  </verify>
  <done>The exact final commit is authorized by GitHub Actions and immutable SHA-bound artifacts, with no human verification or UAT gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical evidence artifacts → milestone audit | A summary transform can misstate requirement ownership, evidence class, or limitation. |
| local/hosted receipt → closure verdict | A report can claim closure from a mismatched SHA, failed producer, or artifact upload alone. |
| superseded audit → final audit | Retained stale findings can contradict the active disposition and make provenance repudiable. |
| workflow source → executable guard | Job-wide string checks can pass after step-local upload or checkout provenance regresses. |
| exact-SHA workflow → durable validation | A stale, mismatched, or incomplete hosted run cannot authorize the corrected closeout. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-22 | Tampering / Repudiation | 31-row requirement disposition | high | mitigate | Extract and diff exact requirement-ID/original-owner pairs across REQUIREMENTS, the canonical matrix, and the audit; separately constrain historically-unprovable disposition to TEST-01. |
| T-159-23 | Spoofing / Repudiation | exact-SHA hosted proof | high | mitigate | Cite the closure receipt's run `33014343041` and exact source SHA, and keep producer success distinct from artifact retention. |
| T-159-24 | Repudiation | final audit verdict | high | mitigate | Replace the stale active body, require one final verdict, and fail verification if superseded closure claims remain. |
| T-159-25 | Tampering | TEST-01 chronology | high | mitigate | Link the immutable probe receipt and limit the waiver to its four historically-unprovable parent probes without changing the requirement. |
| T-159-28 | Tampering | TEST-05 workflow regression test | high | mitigate | Parse the named upload and checkout steps independently so unrelated job text cannot satisfy always-upload or source-SHA coupling assertions. |
| T-159-29 | Spoofing / Repudiation | corrected-audit authority | high | mitigate | Require a newly dispatched run at the exact candidate/final SHA, all named jobs, and both immutable SHA-bound artifact digests. |
| T-159-26 | Information Disclosure | audit evidence | low | accept | The audit records bounded public repository provenance and links receipts instead of embedding raw logs or secrets. |
| T-159-27 | Denial of Service | closure verification | low | accept | Verification uses one focused ExUnit suite plus bounded local comparisons and invokes no hosted workflow or package operation. |
</threat_model>

## Artifacts this phase produces

- `.planning/v1.37-MILESTONE-AUDIT.md`: one final passed-with-bounded-TEST-01-waiver audit derived from the canonical 31-row matrix, eleven retrospective triples, validations, and exact-SHA closure receipt.
- `test/mix/tasks/workflow_wiring_test.exs`: coverage upload and checkout assertions scoped to their actual workflow steps.
- `159-VALIDATION.md`: two-stage exact-SHA machine authority for the corrected final audit.
- New requirement ownership, runtime/API behavior, CI topology, external services, packages, releases, or UI changes: none.

<verification>
Run the focused workflow-wiring suite, semantic pair/triple/waiver comparisons in Task 2, and the post-approval provenance gate in Task 3. The final gate must prove the attributed validation record exists before the audit changes from pending approval to passed.
</verification>

<success_criteria>
- Maintainers encounter one internally consistent v1.37 closure disposition.
- Every requirement and phase disposition is traceable to the already-completed canonical evidence set.
- TEST-05 is closed by actual scheduled/manual hosted coverage proof.
- TEST-01 remains truthful: current behavior is covered, but four historical chronology predicates stay explicitly unprovable under the narrow waiver.
- The workflow regression test binds upload policy to the named coverage upload step and binds the report SHA label to checkout without a ref override.
- Candidate and final exact-SHA GitHub Actions attestations are green and durably linked through immutable artifacts.
- The gap closes without modifying product code, CI policy, requirement ownership, or historical evidence artifacts.
</success_criteria>

<output>
Create `.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-08-SUMMARY.md` when done.
</output>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Close v1.37 with truthful exact-SHA provenance while preserving ownership and limitations | 159-01–159-08 | COVERED | Existing plans produced coverage/provenance; Plan 08 makes the final consumer internally consistent. |
| REQ | — | Phase 159 has no newly mapped requirement IDs | — | COVERED | `requirements: []`; original Phase 148–158 ownership remains canonical. |
| RESEARCH | — | Re-audit only after coverage, retrospective triples, validation, and receipts exist | 159-07, 159-08 | COVERED | Inputs exist; Plan 08 performs the final consistent transform. |
| RESEARCH | — | Preserve evidence-class discipline and fail closed on unrecoverable chronology | 159-02–159-08 | COVERED | Plan 08 consumes existing classes and retains the precise TEST-01 waiver. |
| CONTEXT | D-01–D-06 | Scheduled/manual advisory coverage evidence lane | 159-01, 159-08 | COVERED | Plan 01 implemented the lane; Plan 08 closes the verifier's two step-scoping weaknesses without changing workflow policy. |
| CONTEXT | D-07–D-17 | Evidence classes, probes, canonical matrix, retrospective records, original ownership | 159-02–159-06 | COVERED | Already executed and verified; Plan 08 consumes these artifacts without rewriting them. |
| CONTEXT | D-18–D-21 | Local/exact-SHA hosted proof and preserved release/CI boundaries | 159-07 | COVERED | Already executed and verified; Plan 08 cites the receipt. |
| CONTEXT | D-22 | Final audit only after all prerequisites; close repairable gaps and retain only precise TEST-01 waiver | 159-08 | COVERED | Semantic provenance gates close the audit contradiction, and final authority comes from two exact-SHA hosted attestations. |
| CONTEXT | Deferred ideas | Coverage dashboards/annotations/trends/thresholds, signed supply-chain attestations for published assets, permanent commit-structure enforcement, UI/brand work | — | EXCLUDED | Explicitly out of scope. |
