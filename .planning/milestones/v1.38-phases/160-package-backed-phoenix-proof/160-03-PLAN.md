---
phase: 160-package-backed-phoenix-proof
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - .planning/phases/160-package-backed-phoenix-proof/160-VALIDATION.md
  - .planning/phases/160-package-backed-phoenix-proof/160-01-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [PKG-01, PKG-02, PROOF-01]
estimate:
  tokens: 5000
  raw_tokens: 5000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "The existing phoenix-example job ran on the exact candidate commit containing the package provenance correction and the Phase 160 workflow."
    - "The path-backed command succeeded before the package-backed command, and the latter completed artifact build, staged dependency resolution, consumer compile, and the four live integration scenarios against Postgres and Meilisearch."
    - "The evidence record gives the candidate SHA, workflow run and job IDs, source URL, both step conclusions, package stage markers, and test result; a green advisory run conclusion alone cannot satisfy acceptance."
  artifacts:
    - path: ".planning/phases/160-package-backed-phoenix-proof/160-VALIDATION.md"
      provides: "Exact-SHA hosted service result and acceptance evidence"
    - path: ".planning/phases/160-package-backed-phoenix-proof/160-01-SUMMARY.md"
      provides: "Reconciled Plan 01 live acceptance status"
  key_links:
    - from: "candidate Git commit"
      to: "GitHub Actions phoenix-example job"
      via: "workflow run headSha equals the captured candidate SHA"
    - from: "package proof step"
      to: "existing Phoenix integration tests"
      via: "successful mix verify.phoenix_example --package step and its stage/test log"
---

<objective>
Close the sole Phase 160 verification gap with exact-SHA hosted, real-service evidence for the package-backed Phoenix example.

Purpose: Prove PKG-01 and PKG-02 on the committed candidate through the already configured advisory service lane, while retaining PROOF-01's path-then-package contract.
Output: A reproducible run receipt in the existing validation and Plan 01 summary artifacts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-package-backed-phoenix-proof/160-CONTEXT.md
@.planning/phases/160-package-backed-phoenix-proof/160-VERIFICATION.md
@.planning/phases/160-package-backed-phoenix-proof/160-VALIDATION.md
@.planning/phases/160-package-backed-phoenix-proof/160-01-SUMMARY.md
@.planning/phases/160-package-backed-phoenix-proof/160-02-SUMMARY.md
@.github/workflows/ci.yml
@lib/mix/tasks/verify/phoenix_example/package.ex

The source implementation, CI wiring, failure contracts, docs contracts, and service-free suite are already green. The uncommitted package provenance corrections must become part of the candidate commit before its SHA can be accepted. Local Postgres and Meilisearch are unavailable. GitHub CLI authentication and a read-only repository API request succeeded on 2026-09-24, so the remaining precondition is the maintainer commit. No source implementation or human UAT is part of this plan.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Obtain and record the exact-SHA hosted package proof</name>
  <precondition>The package provenance corrections in lib/mix/tasks/verify/phoenix_example/package.ex and test/mix/tasks/verify_phoenix_example_package_test.exs are committed in the candidate HEAD, and both gh auth status and a read-only GitHub API request for szTheory/scrypath succeed.</precondition>
  <files>.planning/phases/160-package-backed-phoenix-proof/160-VALIDATION.md, .planning/phases/160-package-backed-phoenix-proof/160-01-SUMMARY.md</files>
  <action>Capture CANDIDATE_SHA from git rev-parse HEAD before writing evidence files, and confirm git show for that SHA contains the package provenance correction and the existing path-then-package workflow. Push the candidate branch normally without force; if an exact-SHA CI run already exists, reuse it, otherwise dispatch ci.yml on that branch and locate the resulting run with gh run list --workflow ci.yml --commit CANDIDATE_SHA. Poll the run through gh run view, then require its headSha to equal CANDIDATE_SHA. Inspect the individual phoenix-example job steps because continue-on-error can leave an advisory run green when a command failed. Require the no-argument mix verify.phoenix_example step to pass before the mix verify.phoenix_example --package step, and require the latter to pass. Read the job log to confirm the package artifact/tag, staged dependency provenance, consumer compile, and integration completion PASS markers, plus a nonzero test count with zero failures and no skipped integration tag; the existing four @moduletag :integration modules are the inline, Oban, related-inline, and related-Oban scenarios. Record candidate SHA, run ID/URL, job ID, job and step conclusions, the relevant log markers/test result, and the source workflow SHA in 160-VALIDATION.md; reconcile the halted live acceptance in 160-01-SUMMARY.md only after every check passes. If authentication is still invalid, use the executor's blocking-human authentication checkpoint and resume after the operator restores it; do not invent credentials, accept a different SHA, or substitute synthetic tests for the service run. This verifies D-01 and D-02 command paths, D-03 order and services, D-04 and D-05 visible safe diagnostics, and D-06 and D-07 behavior already covered by focused lifecycle tests.</action>
  <verify>
    <automated>gh run view "$RUN_ID" -R szTheory/scrypath --json headSha,jobs | jq -e --arg sha "$CANDIDATE_SHA" '.headSha == $sha and ([.jobs[] | select(.name == "phoenix-example (advisory)" and .conclusion == "success") | .steps[] | select(.name == "Run mix verify.phoenix_example" or .name == "Run mix verify.phoenix_example --package") | .conclusion] == ["success", "success"])'</automated>
    <automated>gh run view --job "$JOB_ID" --log -R szTheory/scrypath | awk '/PASS package proof: artifact created and tagged/{a=1} /PASS package proof: staged dependencies resolve/{b=1} /PASS package proof: consumer compiled/{c=1} /PASS package proof: integration scenarios completed/{d=1} END {exit !(a &amp;&amp; b &amp;&amp; c &amp;&amp; d)}'</automated>
    <automated>gh run view --job "$JOB_ID" --log -R szTheory/scrypath | awk '/package proof: running integration scenarios/{in_package=1} in_package &amp;&amp; /[1-9][0-9]* tests?, 0 failures/{result=1} in_package &amp;&amp; /Excluding tags: .*integration/{excluded=1} /PASS package proof: integration scenarios completed/{complete=1} END {exit !(complete &amp;&amp; result &amp;&amp; !excluded)}'</automated>
    <fails_when>Either command exits nonzero, the run SHA differs, either ordered proof step is absent or failed, any package PASS stage marker or the successful nonzero integration test result is absent, or integration scenarios were excluded.</fails_when>
  </verify>
  <done>The recorded hosted job ran both commands in order on CANDIDATE_SHA, its package step built and compiled the artifact-backed consumer and passed all four live scenarios, and the two existing evidence artifacts cite the exact run and job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local branch to hosted CI | A pushed commit and workflow dispatch select the code GitHub Actions executes. |
| GitHub API and logs to phase evidence | Hosted data must be checked against the candidate SHA and individual advisory job steps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-160-06 | Tampering | Hosted proof attribution | high | mitigate | Match run headSha to the captured candidate SHA and inspect both named job steps before recording acceptance. |
| T-160-07 | Repudiation | Phase evidence | medium | mitigate | Record run URL, run/job IDs, source SHA, step outcomes, and stage/test results in the existing validation artifact. |
| T-160-08 | Information disclosure | CI logs and evidence | low | mitigate | Record stage and test outcomes only; do not copy service environment values or secrets into planning artifacts. |
</threat_model>

<verification>
Use the named gh and jq checks on the candidate's completed hosted run, then read the phoenix-example job log for the four PASS stages and executed integration result. Re-run Phase 160 verification after this plan; PKG-02 remains open if no exact-SHA live success exists.
</verification>

<success_criteria>
The Phase 160 verifier can cite a successful exact-SHA package-backed Phoenix run with both ordered job steps and real-service integration results, without human UAT or a new source feature.
</success_criteria>

<output>
Create .planning/phases/160-package-backed-phoenix-proof/160-03-SUMMARY.md when done.
</output>
