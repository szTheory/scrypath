---
phase: 161-release-and-tidy-closeout
plan: 04
type: execute
wave: 3
depends_on: [161-03]
files_modified:
  - .planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md
  - .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md
  - .planning/phases/161-release-and-tidy-closeout/161-04-SUMMARY.md
autonomous: true
requirements: [HYGIENE-01, REL-01, CLOSE-01]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Only branches, worktrees, service stacks, temporary package spaces, and generated files proven to belong to v1.38 are removed; pre-existing unrelated changes remain and are reported."
    - "The final release record says shipped only with Hex, HexDocs, tag/source parity, and green main evidence; otherwise it says release-ready with the exact external blocker and resume action."
    - "The final committed phase artifacts receive a fresh exact-SHA closeout, and any later tracked edit triggers another closeout."
  artifacts:
    - path: .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md
      provides: Owned-resource inventory, cleanup results, and final release disposition
    - path: .planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md
      provides: Requirement-to-check map and candidate/final SHA references
    - path: .planning/phases/161-release-and-tidy-closeout/161-04-SUMMARY.md
      provides: Final plan execution and disposition handoff
  key_links:
    - from: Pre-cleanup Git and service inventory
      to: Final cleanup record
      via: Per-resource owner and before/after disposition
    - from: Final commit SHA
      to: ci.yml closeout artifacts
      via: Fresh workflow_dispatch run and immutable artifact digests at that SHA
---

<objective>
Remove only milestone-owned residue and close v1.38 on evidence tied to the final committed state.

Purpose: HYGIENE-01 and CLOSE-01 require tidy ownership-scoped closeout, while REL-01 requires final-SHA checks and accurate release status.
Output: Final validation, cleanup/release evidence, and exact-SHA handoff.
</objective>

## Artifacts this phase produces

This plan completes `161-VALIDATION.md`, `161-RELEASE-EVIDENCE.md`, and `161-04-SUMMARY.md`. The verifier may then produce `161-VERIFICATION.md` from the observed outcome. If it or normal milestone state updates change tracked files, the exact-SHA closeout must run again on the resulting final commit.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/161-release-and-tidy-closeout/161-CONTEXT.md
@.planning/phases/161-release-and-tidy-closeout/161-RESEARCH.md
@.planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md
@.planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md
@.planning/phases/161-release-and-tidy-closeout/161-03-SUMMARY.md
@CONTRIBUTING.md
@scripts/ci_monitor.cjs
@lib/mix/tasks/verify/phoenix_example/package.ex

Research observed unrelated dirt in Phase 134 and 136 UAT files, `.planning/research/.cache/`, and `.planning/state.json`, plus the pre-existing `gsd/v1.37-code-quality-ratchet` branch. These are preservation baselines, not deletion targets. Recheck the live inventory immediately before cleanup.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean only proven v1.38 resources and record the surviving worktree</name>
  <files>.planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md</files>
  <read_first>.planning/phases/161-release-and-tidy-closeout/161-RESEARCH.md; .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md; lib/mix/tasks/verify/phoenix_example/package.ex; examples/phoenix_meilisearch/README.md</read_first>
  <action>For HYGIENE-01 and CLOSE-01, take a fresh `git status --short`, `git worktree list --porcelain`, local/remote branch inventory, example service inventory, and task-owned temporary/generated-file inventory before deleting anything. For each candidate resource, record its exact path/name, owner evidence, and whether it existed before v1.38. Remove only positively owned v1.38 branches/worktrees after their content is merged or preserved, package task temporary spaces with the task's ownership-safe cleanup path, generated debris, and example services started for this milestone; do not stop a shared or pre-existing service by name alone. Record before/after state, any owned item that external permissions prevent cleaning, and the unrelated changes that survived. Preserve the two historical UAT modifications, research cache, state.json, and pre-existing v1.37 branch unless separate ownership evidence overturns the baseline. Inspect affected canonical docs and examples for stale v1.38 claims or leftover scaffolding and fix only milestone-owned drift before final attestation.</action>
  <verify>
    <automated>git diff --check "$(git merge-base HEAD origin/main)" HEAD</automated>
    <fails_when>Git reports whitespace errors or malformed changed lines in owned tracked files.</fails_when>
    <automated>git status --short</automated>
    <fails_when>The command errors, or the evidence record omits any surviving status entry, including pre-existing unrelated changes.</fails_when>
    <automated>git worktree list --porcelain</automated>
    <fails_when>The command errors, or an owned worktree remains without a recorded owner-specific blocker.</fails_when>
  </verify>
  <acceptance_criteria>The release evidence file lists every cleanup category, identified owner, before/after state, surviving unrelated entry, and exact external cleanup limitation. No unproven branch, service, file, or worktree was deleted.</acceptance_criteria>
  <done>The repository and local services are tidy to the extent ownership evidence permits, with unrelated work preserved and reported.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the final disposition and attest its exact SHA</name>
  <files>.planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md, .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md, .planning/phases/161-release-and-tidy-closeout/161-04-SUMMARY.md</files>
  <read_first>CONTRIBUTING.md; scripts/ci_monitor.cjs; .planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md; .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md; .planning/phases/161-release-and-tidy-closeout/161-03-SUMMARY.md</read_first>
  <action>For D-01, D-03, D-04, REL-01, and CLOSE-01, reconcile DOC-01/HYGIENE-01/REL-01/CLOSE-01 rows in `161-VALIDATION.md` with the actual tests, Mint audit, candidate SHA, PR review state, release result, green merged main if it exists, and ownership cleanup. Write `161-04-SUMMARY.md` and the final release evidence before capturing FINAL_SHA; use `shipped` only if Hex package, versioned HexDocs, consumer and tag/tarball checks passed from the Release Please tag, otherwise use the precise `release-ready` external blocker/resume action when all available checks passed. Commit all intended tracked files and push the exact final branch SHA without staging unrelated dirt. Run the existing closeout helper on FINAL_SHA and record its run URL, five required jobs, coverage, attestation, artifact digests, and conclusion in the executor's final response or external run receipt without editing tracked files afterward. If any summary, verification, STATE, ROADMAP, or other tracked change later lands, rerun closeout at the resulting SHA. If the milestone PR merged, additionally require green required jobs on the exact merged `main` SHA and record its CI run; if no authorized merge happened, require Plan 03's recorded authorization refusal, real review/merge prerequisite, or observed permission/service blocker before using `release-ready`. PR absence alone stays pending with its prepared submission action. Set MAIN_SHA, MAIN_CI_RUN_ID, and DISPOSITION from observed evidence for the conditional verifier; never supply guessed values or represent a prior candidate run as final authority.</action>
  <verify>
    <automated>node scripts/ci_monitor.cjs closeout --push --branch "$(git branch --show-current)" --sha "$(git rev-parse HEAD)"</automated>
    <fails_when>The helper exits nonzero, the branch remote differs from final HEAD, any required job, coverage, or attestation fails, or either SHA-bound artifact lacks a digest.</fails_when>
    <automated>if [ -n "$MAIN_SHA" ]; then gh run view "$MAIN_CI_RUN_ID" --json headSha,jobs | jq -e --arg sha "$MAIN_SHA" --argjson required '["core (required)","package (required)","repository-contracts (required)","backend (required)","ecommerce-mounted (required)"]' '. as $run | ($run.headSha == $sha) and ([$run.jobs[] | select(.name as $name | $required | any(. == $name))] | (map(.name) | sort) == ($required | sort) and all(.[]; .conclusion == "success"))'; else test "$DISPOSITION" = "release-ready"; fi</automated>
    <fails_when>A merged main SHA lacks one of the five successful required jobs, the run SHA differs, or missing main evidence is coupled with a shipped disposition.</fails_when>
  </verify>
  <acceptance_criteria>The final closeout run targets the exact commit containing phase summaries and release/cleanup evidence, no later tracked edit is treated as covered by that run, main is green after any merge, and the shipped or release-ready status matches real external evidence.</acceptance_criteria>
  <done>Final source, release disposition, main status when merged, and cleanup record have exact-SHA machine authority or an explicit still-blocked prerequisite.</done>
</task>

</tasks>

<threat_model>
ASVS level 1; block on unresolved high-severity findings.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worktree/service inventory to deletion | Cleanup may affect user data or shared services unless ownership is proven. |
| Final tracked evidence to hosted CI | A new tracked commit invalidates evidence for the prior SHA. |
| Release workflow result to milestone status | External publication and parity must be observed before the shipped label. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-09 | Tampering | Unrelated user work | high | mitigate | Inventory exact Git/worktree/service entries before cleanup and remove only v1.38 resources with per-entry ownership evidence. |
| T-161-10 | Repudiation | Final closeout SHA | high | mitigate | Commit all evidence and summaries before `ci_monitor.cjs closeout`, retain the SHA-bound run receipt externally, and rerun after any later tracked edit. |
| T-161-11 | Repudiation | Milestone release status | high | mitigate | Require Release Please Hex/HexDocs/parity evidence for shipped; otherwise record `release-ready`, `Published: no`, named blocker, and exact resume action. |
</threat_model>

<verification>
Run the ownership inventory before and after scoped cleanup, the final exact-SHA helper after all tracked writes, and the main required-job check after any merge. A later GSD verification or milestone-state commit creates a new final SHA and requires the same closeout helper again.
</verification>

<success_criteria>
The closeout record proves owned-resource cleanup, preserved unrelated work, exact final-source CI, and an evidence-correct shipped or release-ready disposition.
</success_criteria>

<output>
Create `.planning/phases/161-release-and-tidy-closeout/161-04-SUMMARY.md` before the final exact-SHA run; retain the final run receipt outside tracked files.
</output>

## Spec-less probe disposition

| Probe row | Plan coverage | Bounded assumption or observable case |
|-----------|---------------|----------------------------------------|
| DOC-01 unclassified | 01 | Existing example command and requirement wording define the documentation contract; no new capability is inferred. |
| HYGIENE-01 unclassified | 01, 04 | Canonical docs, package checks, and owned-resource inventory define hygiene; no blanket worktree cleanup is inferred. |
| REL-01 adjacency | 03, 04 | SHA, CI run, jobs, artifacts, version, tag, Hex, and HexDocs evidence are linked in adjacent records. |
| REL-01 empty | 03 | An absent PR gets a prepared submission path and remains pending until explicit authorization refusal or an observed external blocker; absent release run, credential, or service gets its specific pending/blocker result. |
| REL-01 ordering | 03, 04 | Review and candidate checks precede merge; Release Please tag precedes publish; publish precedes parity; final check follows tracked edits. |
| CLOSE-01 adjacency | 04 | Each owned resource has owner evidence beside before/after disposition. |
| CLOSE-01 empty | 04 | Empty cleanup categories and absent external permissions are recorded explicitly. |
| CLOSE-01 ordering | 04 | Inventory precedes deletion, evidence commits precede final-SHA proof, and later tracked edits trigger a repeat. |
