---
phase: 70-papercuts-and-readiness-checkpoint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scrypath/docs_contract_test.exs
  - README.md
  - guides/golden-path.md
  - guides/support-and-compatibility.md
  - examples/phoenix_meilisearch/README.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - INTG-05
must_haves:
  truths:
    - "Phase 70 closes exactly three bounded adopter-friction papercuts and no broader polish sweep."
    - "Public docs make it explicit that the Phoenix example is a repo or GitHub proof surface rather than part of the Hex package payload."
    - "The support contract states narrow, defended Ecto and Phoenix expectation anchors without turning them into a broad compatibility matrix."
    - "Maintainer verification guidance teaches `mix verify.adopter` as the default integration-confidence path before older specialist verify tasks."
    - "Each papercut has a bounded regression assertion in docs contracts that fails if the confusion returns."
  artifacts:
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Bounded regression assertions for all three papercuts"
    - path: "guides/golden-path.md"
      provides: "Explicit repo/GitHub handoff for the Phoenix example proof surface"
    - path: "guides/support-and-compatibility.md"
      provides: "Canonical support contract with explicit Ecto/Phoenix expectation anchors"
    - path: "README.md"
      provides: "Concise public wayfinding that keeps README short while clarifying example and verify boundaries"
    - path: "CONTRIBUTING.md"
      provides: "Default maintainer verification order centered on `mix verify.adopter`"
  key_links:
    - from: "test/scrypath/docs_contract_test.exs"
      to: "mix.exs"
      via: "Hex package boundary assertion"
      pattern: "files:"
    - from: "guides/support-and-compatibility.md"
      to: "examples/phoenix_meilisearch/mix.exs"
      via: "explicit Phoenix and Ecto expectation anchors"
      pattern: "phoenix|phoenix_ecto|ecto_sql"
    - from: "CONTRIBUTING.md"
      to: "lib/mix/tasks/verify.adopter.ex"
      via: "default maintainer command guidance"
      pattern: "mix verify\\.adopter"
---

<objective>
Close the last three evidence-backed adopter papercuts without widening Scrypath's public surface.

Purpose: satisfy INTG-05 by tightening authority boundaries and maintainer wayfinding around the already-shipped example, support contract, and adopter verify spine.
Output: three bounded papercut fixes across docs plus recurrence-proof doc-contract assertions.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/70-papercuts-and-readiness-checkpoint/70-RESEARCH.md
@.planning/phases/70-papercuts-and-readiness-checkpoint/70-PATTERNS.md
@.planning/phases/69-adopter-verify-spine/69-02-PLAN.md
@.planning/phases/68-example-proof-and-support-contract/68-01-PLAN.md
@README.md
@CONTRIBUTING.md
@guides/golden-path.md
@guides/support-and-compatibility.md
@examples/phoenix_meilisearch/README.md
@lib/mix/tasks/verify.adopter.ex
@test/scrypath/docs_contract_test.exs
@mix.exs
@examples/phoenix_meilisearch/mix.exs

<interfaces>
From mix.exs:
```elixir
files:
  ~w(lib .formatter.exs mix.exs README.md CONTRIBUTING.md ARCHITECTURE.md CHANGELOG.md guides docs)
```

From examples/phoenix_meilisearch/mix.exs:
```elixir
{:phoenix, "~> 1.8.5"},
{:phoenix_ecto, "~> 4.5"},
{:ecto_sql, "~> 3.13"},
{:scrypath, path: "../.."}
```

From test/scrypath/docs_contract_test.exs:
```elixir
assert_contains_all(@support_guide, [...])
assert ordered?(@readme, "guides/golden-path.md", "examples/phoenix_meilisearch/README.md")
```
</interfaces>
</context>

<source_audit>
- GOAL: Phase 70 closes no more than three bounded papercuts before the milestone becomes a readiness checkpoint; this plan owns all three papercuts and nothing broader.
- REQ: `INTG-05` is covered here via three concrete friction fixes, each protected by a bounded docs-contract assertion.
- RESEARCH: implement only the repo-vs-package example boundary, explicit Ecto/Phoenix support anchors, and maintainer verify ordering around `mix verify.adopter`.
- CONTEXT: user hard constraints require no public search/indexing surface expansion, no new command family, no more than three papercuts, and recurrence protection for each fix.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend docs contracts so all three papercuts fail loudly on recurrence</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <behavior>
    - Test 1: The docs-contract suite fails if README, the golden path, or the example README stop clarifying that the Phoenix example is a repo/GitHub proof surface rather than part of the Hex package payload.
    - Test 2: The suite fails if the support guide loses narrow Ecto and Phoenix expectation anchors backed by `mix.exs` and `examples/phoenix_meilisearch/mix.exs`.
    - Test 3: The suite fails if maintainer verification guidance stops putting `mix verify.adopter` ahead of older specialist verify tasks for integration-confidence work.
  </behavior>
  <action>Extend `Scrypath.DocsContractTest` with exactly three new bounded assertions, one per papercut. For the example-boundary papercut, read `mix.exs` and assert the package file list still excludes `examples/` while public docs explicitly say the Phoenix example lives in the repository/GitHub proof surface rather than the Hex payload. For the support-contract papercut, assert `guides/support-and-compatibility.md` names current Ecto and Phoenix expectation anchors in narrow "tested/current proof" language tied to the root and example `mix.exs` files; do not freeze large prose blocks or create a broad version matrix snapshot. For the maintainer-guidance papercut, use `ordered?/2` style assertions so CONTRIBUTING and any README verification handoff put `mix verify.adopter` before `mix verify.phase5` and `mix verify.meilisearch_smoke` when the topic is default integration confidence. Keep the additions narrow to strings, ordering, and the relevant version tokens; do not add a new helper subsystem.</action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <done>The docs-contract suite now encodes all three papercuts as bounded failures instead of relying on prose review.</done>
</task>

<task type="auto">
  <name>Task 2: Clarify the example boundary and support anchors in the canonical public docs</name>
  <files>README.md, guides/golden-path.md, guides/support-and-compatibility.md, examples/phoenix_meilisearch/README.md</files>
  <action>Apply the first two papercuts without widening scope. In README and `guides/golden-path.md`, replace any ambiguous local-path wording with explicit repository-path or GitHub-proof wording and state that `examples/phoenix_meilisearch/` is the canonical repo example surface, not part of the Hex package payload. Keep README concise and route detailed runnable proof back to the example README. In `examples/phoenix_meilisearch/README.md`, make the repo-clone assumption explicit so readers do not mistake the example for packaged payload. In `guides/support-and-compatibility.md`, add explicit but narrow Ecto/Phoenix expectation anchors using the existing defended facts: root library `{:ecto, "~> 3.13"}` and the example's Phoenix `~> 1.8.5`, `phoenix_ecto ~> 4.5`, and `ecto_sql ~> 3.13`. Phrase them as the current defended onboarding/tested anchors, not a broad support matrix or promise of all-version parity. Do not edit `mix.exs`, do not add new guides, and do not widen any public API or command surface.</action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <done>Public docs make the example boundary explicit and the support guide states narrow, defensible Ecto/Phoenix anchors without overpromising support breadth.</done>
</task>

<task type="auto">
  <name>Task 3: Reorder maintainer verification guidance around the adopter verify spine</name>
  <files>CONTRIBUTING.md, README.md</files>
  <action>Implement the third papercut by making `mix verify.adopter` the unmistakable default integration-confidence path for maintainers. In CONTRIBUTING, move the adopter verify family ahead of `mix verify.phase5` and other specialist verify tasks in the Verification section, explain that it is the default fast proof for docs/support/example parity, and keep the older tasks framed as deeper domain-specific gates rather than the first thing a maintainer should run. In README, keep the maintainer note concise but align any integration-smoke or verification wording so it does not compete with `mix verify.adopter` as the default integration-confidence entrypoint. Preserve the existing authority boundaries: README stays short, CONTRIBUTING owns maintainer workflow detail, and the example README stays canonical for the live proof path. Do not add or rename any Mix task.</action>
  <verify>
    <automated>mix verify.adopter</automated>
  </verify>
  <done>Maintainer-facing docs now teach `mix verify.adopter` first for integration-confidence work, while specialist verify tasks remain available but clearly secondary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published docs -> adopters | adopters infer packaging, compatibility, and setup expectations from these files |
| maintainer docs -> verification commands | incorrect default guidance can send maintainers to the wrong proof surface and create false confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | S | README / golden path / example README | mitigate | Make the repo-vs-package example boundary explicit and pin it with docs-contract assertions tied to `mix.exs` package files. |
| T-70-02 | I | support contract wording | mitigate | Phrase Ecto/Phoenix anchors as current defended expectations backed by repo evidence, not as an open-ended compatibility matrix. |
| T-70-03 | R | CONTRIBUTING / README verification order | mitigate | Reorder maintainer guidance around `mix verify.adopter` and assert the ordering in `docs_contract_test.exs` so future drift fails immediately. |
</threat_model>

<verification>
Run `mix test test/scrypath/docs_contract_test.exs` after each docs edit. Close the plan with `mix verify.adopter` to prove the fast maintainer path still matches the new wording and remains service-free.
</verification>

<success_criteria>
- Exactly three papercuts are closed in this plan, and each one has a bounded recurrence guard in `docs_contract_test.exs`.
- The Phoenix example is clearly presented as a repository/GitHub proof surface, not a Hex payload artifact.
- The support contract names current Ecto/Phoenix expectations narrowly and defensibly.
- CONTRIBUTING and README teach `mix verify.adopter` as the default integration-confidence path without adding a new command family.
</success_criteria>

<output>
After completion, create `.planning/phases/70-papercuts-and-readiness-checkpoint/70-papercuts-and-readiness-checkpoint-01-SUMMARY.md`
</output>
