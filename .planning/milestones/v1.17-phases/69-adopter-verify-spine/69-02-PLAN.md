---
phase: 69-adopter-verify-spine
plan: 02
type: execute
wave: 2
depends_on:
  - 69-01
files_modified:
  - test/scrypath/docs_contract_test.exs
  - README.md
  - CONTRIBUTING.md
  - guides/support-and-compatibility.md
  - .github/workflows/ci.yml
  - examples/phoenix_meilisearch/README.md
autonomous: true
requirements:
  - INTG-02
must_haves:
  truths:
    - "Repo docs teach one root adopter verify command family with an explicit fast default and explicit live depth."
    - "The example README, the task help wording, and CI all point at the same canonical live proof path."
    - "The support guide’s inline/manual/oban support claims stay aligned with the adopter verify fast path and live proof contract."
    - "CI uses the same command family or directly linked constituent commands so maintainer guidance cannot drift from automation."
  artifacts:
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Bounded contract assertions for adopter verify task/help/docs/CI parity"
    - path: "README.md"
      provides: "Root maintainer wayfinding for `mix verify.adopter`"
    - path: "CONTRIBUTING.md"
      provides: "Contributor verify matrix and explicit live prerequisites for adopter verification"
    - path: "guides/support-and-compatibility.md"
      provides: "Protected support claims for inline/manual/oban adopter paths and fast/live verification scope"
    - path: ".github/workflows/ci.yml"
      provides: "Fast and live adopter verification job wiring using the semantic command family"
  key_links:
    - from: "README.md"
      to: "lib/mix/tasks/verify.adopter.ex"
      via: "documented command family"
      pattern: "mix verify.adopter"
    - from: "CONTRIBUTING.md"
      to: ".github/workflows/ci.yml"
      via: "job-name and command mapping"
      pattern: "verify.adopter --live"
    - from: "guides/support-and-compatibility.md"
      to: "test/scrypath/docs_contract_test.exs"
      via: "bounded support-claim contract assertions"
      pattern: "inline|manual|oban"
    - from: "test/scrypath/docs_contract_test.exs"
      to: "examples/phoenix_meilisearch/README.md"
      via: "canonical live path/env/order assertions"
      pattern: "cd examples/phoenix_meilisearch"
---

<objective>
Lock docs, contributor guidance, and CI to the new adopter verify spine.

Purpose: make the semantic task from Plan 01 the single durable maintainer story while preserving the canonical example README as the proof authority and keeping CI service boundaries explicit.
Output: bounded docs-contract assertions plus README, CONTRIBUTING, example README, and CI updates that all point to the same fast/live command family.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/69-adopter-verify-spine/69-CONTEXT.md
@.planning/phases/69-adopter-verify-spine/69-RESEARCH.md
@.planning/phases/69-adopter-verify-spine/69-PATTERNS.md
@.planning/phases/69-adopter-verify-spine/69-VALIDATION.md
@.planning/phases/68-example-proof-and-support-contract/68-02-SUMMARY.md
@lib/mix/tasks/verify.adopter.ex
@test/scrypath/docs_contract_test.exs
@README.md
@CONTRIBUTING.md
@guides/support-and-compatibility.md
@examples/phoenix_meilisearch/README.md
@.github/workflows/ci.yml

<interfaces>
From test/scrypath/docs_contract_test.exs:
```elixir
@readme File.read!("README.md")
@contributing File.read!("CONTRIBUTING.md")
@support_guide File.read!("guides/support-and-compatibility.md")
@example_readme File.read!("examples/phoenix_meilisearch/README.md")
@ci_workflow File.read!(".github/workflows/ci.yml")
assert ordered?(job_head, "cd examples/phoenix_meilisearch", "mix deps.get")
```

From examples/phoenix_meilisearch/README.md:
```markdown
cd examples/phoenix_meilisearch
mix deps.get
mix test
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend bounded docs contracts to pin the adopter verify spine</name>
  <read_first>test/scrypath/docs_contract_test.exs, lib/mix/tasks/verify.adopter.ex, README.md, CONTRIBUTING.md, guides/support-and-compatibility.md, examples/phoenix_meilisearch/README.md, .github/workflows/ci.yml, .planning/phases/69-adopter-verify-spine/69-CONTEXT.md</read_first>
  <files>test/scrypath/docs_contract_test.exs</files>
  <action>Extend `Scrypath.DocsContractTest` using the existing bounded string/order style from Phase 68. Add `@verify_adopter File.read!("lib/mix/tasks/verify.adopter.ex")`, then pin the D-05 and D-17 mapping between README, CONTRIBUTING, `guides/support-and-compatibility.md`, task help, example README, and CI. Assert that README and CONTRIBUTING mention `mix verify.adopter` as the fast default, that the support guide keeps the `:inline`, `:manual`, and `:oban` support claims aligned with the adopter verify path and the documented fast-vs-live boundary, that task help text distinguishes fast versus live and names live prerequisites, that the example README still contains the canonical `cd examples/phoenix_meilisearch -> mix deps.get -> mix test` path and env prerequisites, and that `.github/workflows/ci.yml` uses `mix verify.adopter` for the fast adopter gate and `mix verify.adopter --live` for the service-backed adopter job. Keep assertions narrow to command names, support-mode atoms, env vars, job names, and ordering; do not snapshot prose blocks.</action>
  <acceptance_criteria>- `rg -n "@verify_adopter" test/scrypath/docs_contract_test.exs` finds the new fixture.
- `rg -n "mix verify\\.adopter|verify\\.adopter --live" test/scrypath/docs_contract_test.exs` shows both command depths are pinned.
- `rg -n "@support_guide|:inline|:manual|:oban|support-and-compatibility" test/scrypath/docs_contract_test.exs` shows support-guide coverage is part of the adopter contract.
- `rg -n "phoenix-example-integration|cd examples/phoenix_meilisearch|mix deps.get|mix test" test/scrypath/docs_contract_test.exs` shows CI/example ordering contracts are present.
- `rg -n "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" test/scrypath/docs_contract_test.exs` shows live prerequisite names are asserted.</acceptance_criteria>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <done>Docs-contract failures now catch drift across the task help surface, maintainer docs, the example README, and the CI adopter jobs.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire maintainer docs to the semantic fast/live adopter command family</name>
  <read_first>README.md, CONTRIBUTING.md, examples/phoenix_meilisearch/README.md, guides/support-and-compatibility.md, .planning/phases/69-adopter-verify-spine/69-CONTEXT.md, lib/mix/tasks/verify.adopter.ex</read_first>
  <files>README.md, CONTRIBUTING.md, guides/support-and-compatibility.md, examples/phoenix_meilisearch/README.md</files>
  <action>Update README, CONTRIBUTING, and `guides/support-and-compatibility.md` so the maintainer story is one obvious semantic command family per D-03, D-05, D-13, D-19, and D-20: `mix verify.adopter` from the repo root for the fast auth-free/service-free contract slice, and `mix verify.adopter --live` for the canonical Phoenix example proof with explicit env/service prerequisites. In README, keep the entrypoint concise and point to CONTRIBUTING plus the example README for deeper job/env details. In CONTRIBUTING, add the fast/live adopter gate alongside existing verify-task guidance, call out that live mode uses the example README’s exact `cd examples/phoenix_meilisearch && mix deps.get && mix test` path, and state that `./scripts/smoke.sh` remains local convenience rather than CI truth per D-10 and D-11. In the support guide, keep the support claims for `:inline`, `:manual`, and `:oban` aligned with what fast mode proves and what only live mode proves, so support posture and verifier scope cannot diverge. Update the example README only if needed to preserve that canonical path while linking the new root command family back to the repo-root maintainer flow; do not replace the example README with generic root-task prose.</action>
  <acceptance_criteria>- `rg -n "mix verify\\.adopter" README.md CONTRIBUTING.md examples/phoenix_meilisearch/README.md` finds the command family in maintainer-facing docs.
- `rg -n "mix verify\\.adopter --live" README.md CONTRIBUTING.md examples/phoenix_meilisearch/README.md` finds the live depth wherever it is intentionally surfaced.
- `rg -n ":inline|:manual|:oban|verify\\.adopter|fast|live" guides/support-and-compatibility.md` shows the support guide is aligned with the new adopter verify story.
- `rg -n "auth-free|service-free|fast" README.md CONTRIBUTING.md` shows the fast/live distinction is explicit.
- `rg -n "smoke\\.sh.*local|local DX harness|not the GitHub Actions test driver" CONTRIBUTING.md examples/phoenix_meilisearch/README.md` preserves the local-vs-canonical distinction.</acceptance_criteria>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <done>Maintainer docs consistently teach the root command family, explain what fast mode does and does not prove, and preserve the example README as the canonical live proof authority.</done>
</task>

<task type="auto">
  <name>Task 3: Align CI job commands with the adopter verify spine without moving service orchestration into Mix</name>
  <read_first>.github/workflows/ci.yml, CONTRIBUTING.md, examples/phoenix_meilisearch/README.md, lib/mix/tasks/verify.adopter.ex, .planning/phases/69-adopter-verify-spine/69-RESEARCH.md</read_first>
  <files>.github/workflows/ci.yml, CONTRIBUTING.md</files>
  <action>Update `.github/workflows/ci.yml` to follow the hybrid-parity model from D-13 through D-17. Add or rename the fast non-service adopter gate so it runs `mix verify.adopter` in a no-services job. Update the existing Phoenix example integration job, or add a clearly named adopter-live job if needed, so the service-backed path runs `mix verify.adopter --live` with GitHub Actions still owning `services:`, env setup, and any readiness wait logic. Keep Postgres and Meilisearch provisioning in the workflow, not in the task. Reflect the final job names and command mapping in CONTRIBUTING so the CI table and maintainer story stay exact.</action>
  <acceptance_criteria>- `rg -n "mix verify\\.adopter$|mix verify\\.adopter --live" .github/workflows/ci.yml` finds both CI command invocations.
- `rg -n "services:|meilisearch:|postgres:" .github/workflows/ci.yml` confirms service provisioning stays in Actions.
- `rg -n "verify\\.adopter|phoenix-example-integration|adopter" CONTRIBUTING.md` shows the CI table matches the final workflow names and commands.</acceptance_criteria>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <done>CI and maintainer docs now reference the same adopter verify command family, while GitHub Actions retains explicit service boundaries and live prerequisites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer docs -> CI workflow | human guidance can drift from automation and produce false confidence |
| docs-contract suite -> canonical example README | contract coverage must pin facts without freezing prose tone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-04 | T | `README.md` / `CONTRIBUTING.md` / `.github/workflows/ci.yml` | mitigate | Add bounded contract assertions for job names, command strings, env vars, and command ordering so any drift fails in `docs_contract_test.exs`. |
| T-69-05 | R | contributor verify guidance | mitigate | Keep CONTRIBUTING’s CI table and root-task wording synchronized to the actual workflow commands and the new semantic task help. |
| T-69-06 | I | example proof authority | mitigate | Preserve the example README as the canonical live proof path and assert its `cd -> mix deps.get -> mix test` sequence directly in contract tests. |
</threat_model>

<verification>
Run `mix test test/scrypath/docs_contract_test.exs` after each doc/workflow edit. Before closing the phase, run `mix verify.adopter` and, with services/env present, `mix verify.adopter --live` to prove the docs-contract and live branches both match the new maintainer story.
</verification>

<success_criteria>
- README and CONTRIBUTING surface `mix verify.adopter` and explain that it is the fast auth-free/service-free maintainer path.
- `guides/support-and-compatibility.md` stays inside the same protected adopter-truth contract and does not overclaim beyond what fast versus live mode actually proves.
- CI invokes `mix verify.adopter` and `mix verify.adopter --live` directly or via explicitly documented constituent commands with no hidden `smoke.sh` indirection.
- `docs_contract_test.exs` fails if task help, docs, example README, or CI job mapping drift apart.
</success_criteria>

<output>
After completion, create `.planning/phases/69-adopter-verify-spine/69-adopter-verify-spine-02-SUMMARY.md`
</output>
