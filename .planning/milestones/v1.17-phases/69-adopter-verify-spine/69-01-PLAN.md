---
phase: 69-adopter-verify-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/verify.adopter.ex
  - mix.exs
  - test/mix/tasks/verify_adopter_test.exs
autonomous: true
requirements:
  - INTG-02
must_haves:
  truths:
    - "A maintainer can run `mix verify.adopter` from the repo root without services and get an adopter-focused confidence signal."
    - "A maintainer can run `mix verify.adopter --live` and the task either executes the canonical Phoenix example proof path or fails immediately with explicit prerequisite guidance."
    - "The public Mix help surface explains fast versus live behavior without implying hidden fallback."
  artifacts:
    - path: "lib/mix/tasks/verify.adopter.ex"
      provides: "Semantic root maintainer verify command with explicit `--live` behavior"
    - path: "mix.exs"
      provides: "CLI preferred env registration for `verify.adopter`"
    - path: "test/mix/tasks/verify_adopter_test.exs"
      provides: "Regression coverage for arg parsing and loud live-prerequisite failures"
  key_links:
    - from: "mix.exs"
      to: "lib/mix/tasks/verify.adopter.ex"
      via: "preferred_envs registration"
      pattern: "\"verify.adopter\": :test"
    - from: "lib/mix/tasks/verify.adopter.ex"
      to: "examples/phoenix_meilisearch"
      via: "System.cmd bash orchestration for `mix deps.get && mix test`"
      pattern: "cd: example_dir"
    - from: "test/mix/tasks/verify_adopter_test.exs"
      to: "lib/mix/tasks/verify.adopter.ex"
      via: "Mix.Task.run assertions"
      pattern: "Mix.Task.run\\(\"verify\\.adopter\""
---

<objective>
Establish the durable maintainer command family for adopter verification.

Purpose: implement the semantic root entrypoint required by D-01 through D-20 without widening phase scope beyond the adopter-confidence spine.
Output: a public `mix verify.adopter` task, CLI registration, and task-focused regression coverage for fast/live behavior and loud failures.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/69-adopter-verify-spine/69-CONTEXT.md
@.planning/phases/69-adopter-verify-spine/69-RESEARCH.md
@.planning/phases/69-adopter-verify-spine/69-PATTERNS.md
@.planning/phases/69-adopter-verify-spine/69-VALIDATION.md
@.planning/phases/68-example-proof-and-support-contract/68-02-SUMMARY.md
@README.md
@CONTRIBUTING.md
@examples/phoenix_meilisearch/README.md
@.github/workflows/ci.yml
@lib/mix/tasks/verify.opsui.ex
@lib/mix/tasks/verify.phase5.ex
@lib/mix/tasks/verify.phase41.ex
@lib/mix/tasks/verify.phase43.ex
@mix.exs
@test/mix/tasks/verify_workspace_clean_test.exs

<interfaces>
From lib/mix/tasks/verify.opsui.ex:
```elixir
def run(args) do
  Mix.Task.run("app.start")
  ensure_no_args!(args)
  {out, status} = System.cmd("bash", ["-lc", script], cd: ops_dir, stderr_to_stdout: true)
end
```

From lib/mix/tasks/verify.phase5.ex:
```elixir
{opts, _argv, _invalid} = OptionParser.parse(args, strict: [skip_integration: :boolean])
unless System.get_env("SCRYPATH_MEILISEARCH_URL"), do: Mix.raise(...)
System.put_env("SCRYPATH_INTEGRATION", "1")
```

From mix.exs:
```elixir
def cli do
  [preferred_envs: ["verify.phase43": :test, "verify.opsui": :test, ...]]
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the `mix verify.adopter` command surface and execution branches</name>
  <read_first>lib/mix/tasks/verify.opsui.ex, lib/mix/tasks/verify.phase5.ex, lib/mix/tasks/verify.phase41.ex, lib/mix/tasks/verify.phase43.ex, .planning/phases/69-adopter-verify-spine/69-CONTEXT.md, .planning/phases/69-adopter-verify-spine/69-RESEARCH.md, examples/phoenix_meilisearch/README.md, CONTRIBUTING.md, .github/workflows/ci.yml</read_first>
  <files>lib/mix/tasks/verify.adopter.ex</files>
  <behavior>
    - No-flag invocation runs only the bounded adopter-confidence fast slice and stays auth-free/service-free per D-04 through D-07.
    - `--live` runs the exact canonical example path `cd examples/phoenix_meilisearch && mix deps.get && mix test` per D-08 through D-12.
    - Unexpected args or missing live env prerequisites raise `Mix.Error` with the required env var names and no silent fallback per D-12.
  </behavior>
  <action>Create `Mix.Tasks.Verify.Adopter` with a real `@shortdoc` and `@moduledoc` per D-01 through D-03 and D-18 through D-20. Parse flags with `OptionParser.parse/2` using a strict switch list that supports `--live` and, only if helpful, `--fast` while keeping the no-flag default fast per D-04 and D-09. For fast mode, call `Mix.Task.reenable("test")` and run only adopter-focused repo-root tests, anchored on `test/scrypath/docs_contract_test.exs` and any new task-specific file needed for this plan; do not add Credo, Dialyzer, docs builds, release gates, or service-backed example runs per D-06 and D-07. For live mode, expand `examples/phoenix_meilisearch`, assert the directory exists, require the documented env prerequisites from the example README and CI (`SCRYPATH_EXAMPLE_INTEGRATION`, `PGPORT`, `SCRYPATH_MEILISEARCH_URL`), then shell out with `System.cmd("bash", ["-lc", script], cd: example_dir, stderr_to_stdout: true)` to run the visible sequence `printf 'n\\n' | mix deps.get && mix test`; do not call `./scripts/smoke.sh`, do not boot services, and do not add readiness logic to the Mix task per D-10, D-11, and D-16.</action>
  <acceptance_criteria>- `rg -n "defmodule Mix.Tasks.Verify.Adopter" lib/mix/tasks/verify.adopter.ex` finds the task module.
- `rg -n "@shortdoc|@moduledoc" lib/mix/tasks/verify.adopter.ex` shows public help text exists per D-03.
- `rg -n "strict: \\[.*live: :boolean" lib/mix/tasks/verify.adopter.ex` shows strict flag parsing for `--live`.
- `rg -n "test/scrypath/docs_contract_test.exs" lib/mix/tasks/verify.adopter.ex` shows the fast slice stays bounded to adopter contracts.
- `rg -n "examples/phoenix_meilisearch|mix deps.get|mix test|SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" lib/mix/tasks/verify.adopter.ex` shows the exact live path and prerequisite names.
- `rg -n "smoke.sh" lib/mix/tasks/verify.adopter.ex` returns no matches.</acceptance_criteria>
  <verify>
    <automated>mix help verify.adopter</automated>
  </verify>
  <done>`mix help verify.adopter` is backed by a public task module, the default branch is fast and service-free, and the live branch is explicit, canonical, and loud on missing prerequisites.</done>
</task>

<task type="auto">
  <name>Task 2: Register the root task in Mix CLI and add regression coverage for arg handling</name>
  <read_first>mix.exs, test/mix/tasks/verify_workspace_clean_test.exs, lib/mix/tasks/verify.adopter.ex, .planning/phases/69-adopter-verify-spine/69-VALIDATION.md</read_first>
  <files>mix.exs, test/mix/tasks/verify_adopter_test.exs</files>
  <action>Add `"verify.adopter": :test` to `Scrypath.MixProject.cli/0` beside the existing `verify.*` entries so the semantic maintainer task always runs in the `:test` environment. Create `test/mix/tasks/verify_adopter_test.exs` following the repo’s non-async Mix-task test style from `verify_workspace_clean_test.exs`: cover stray/unknown arg rejection, `--live` prerequisite failures that name `SCRYPATH_EXAMPLE_INTEGRATION`, `PGPORT`, and `SCRYPATH_MEILISEARCH_URL`, and one progress-marker assertion for the fast branch by capturing output from `Mix.Task.run("verify.adopter", [])`. Keep the file focused on task semantics rather than duplicating docs-contract assertions from Plan 02.</action>
  <acceptance_criteria>- `rg -n "\"verify.adopter\": :test" mix.exs` finds the CLI registration.
- `test -f test/mix/tasks/verify_adopter_test.exs` succeeds.
- `rg -n "verify\\.adopter" test/mix/tasks/verify_adopter_test.exs` shows task-specific coverage exists.
- `rg -n "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" test/mix/tasks/verify_adopter_test.exs` shows loud-prerequisite assertions are pinned.</acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/verify_adopter_test.exs</automated>
  </verify>
  <done>The root task is discoverable in the test environment and regressions around flags or silent live downgrades fail locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer CLI args -> `Mix.Tasks.Verify.Adopter` | untrusted flags and invocation shape enter the task |
| task runtime -> shell command in example app | the task crosses into a subprocess that can mislead maintainers if the command path drifts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | T | `lib/mix/tasks/verify.adopter.ex` flag handling | mitigate | Use strict `OptionParser.parse/2`, reject stray args, and test `Mix.Error` paths so fast/live semantics cannot be silently altered by unknown flags. |
| T-69-02 | I | live prerequisite branch | mitigate | Require explicit env vars and raise before shell execution so maintainers never confuse a partial or downgraded run for live proof. |
| T-69-03 | R | maintainer verify surface | mitigate | Add task-focused regression tests and CLI preferred-env registration so the semantic command surface stays reproducible across local and CI execution. |
</threat_model>

<verification>
Run `mix test test/mix/tasks/verify_adopter_test.exs` after task and CLI edits. If the task includes the fast docs-contract slice already, also run `mix verify.adopter` to ensure the default branch stays service-free.
</verification>

<success_criteria>
- `mix verify.adopter` exists as the durable public command named in D-01 and runs in `:test`.
- The task help, default behavior, and `--live` branch visibly encode the fast/live distinction and the exact example proof path.
- Missing live prerequisites or unsupported args fail with explicit `Mix.Error` output rather than silent fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/69-adopter-verify-spine/69-adopter-verify-spine-01-SUMMARY.md`
</output>
