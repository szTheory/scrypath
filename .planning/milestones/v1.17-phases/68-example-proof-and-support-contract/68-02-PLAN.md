---
phase: 68-example-proof-and-support-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 68-01
files_modified:
  - examples/phoenix_meilisearch/README.md
  - examples/phoenix_meilisearch/test/smoke/meilisearch_manual_stack_test.exs
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - INTG-01
  - INTG-04
must_haves:
  truths:
    - "The Phoenix example proves `:inline`, `:manual`, and `:oban` in one real app without requiring readers to stitch together multiple docs."
    - "The example README is the source of truth for env vars, startup order, CI-shaped test commands, and mode-specific proof paths."
    - "Bounded docs contracts fail when README, guides, CONTRIBUTING, and the example drift on start-here links, env names, or startup/command order."
  artifacts:
    - path: "examples/phoenix_meilisearch/README.md"
      provides: "Canonical adopter proof runbook with all three sync modes"
    - path: "examples/phoenix_meilisearch/test/smoke/meilisearch_manual_stack_test.exs"
      provides: "Real `:manual` proof path in the example app"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Bounded Phase 68 drift assertions for links, env names, and command order"
  key_links:
    - from: "examples/phoenix_meilisearch/README.md"
      to: "examples/phoenix_meilisearch/test/smoke/meilisearch_manual_stack_test.exs"
      via: "Mode-specific proof path"
      pattern: "manual"
    - from: "test/scrypath/docs_contract_test.exs"
      to: "examples/phoenix_meilisearch/README.md"
      via: "Canonical example README assertions"
      pattern: "phoenix_meilisearch/README.md"
    - from: "test/scrypath/docs_contract_test.exs"
      to: "README.md"
      via: "Canonical start-here order assertions"
      pattern: "support-and-compatibility.md"
---

<objective>
Turn the Phoenix example into the canonical adopter proof for all three write paths and lock the resulting docs surface with bounded contract tests.

Purpose: Deliver the real-app proof required by INTG-01 and the drift protection half of INTG-04 after the support contract and top-level wayfinding land.
Output: A canonical example README, a real `:manual` integration proof, and Phase 68 docs-contract assertions for facts/navigation.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/68-example-proof-and-support-contract/68-CONTEXT.md
@.planning/phases/68-example-proof-and-support-contract/68-RESEARCH.md
@.planning/phases/68-example-proof-and-support-contract/68-VALIDATION.md
@.planning/phases/68-example-proof-and-support-contract/68-01-PLAN.md
@examples/phoenix_meilisearch/README.md
@examples/phoenix_meilisearch/scripts/smoke.sh
@examples/phoenix_meilisearch/test/smoke/meilisearch_stack_test.exs
@examples/phoenix_meilisearch/test/smoke/meilisearch_oban_stack_test.exs
@test/scrypath/docs_contract_test.exs

<interfaces>
From `examples/phoenix_meilisearch/test/smoke/meilisearch_stack_test.exs`:
```elixir
assert {:ok, %{mode: :inline, status: :completed}} =
         Scrypath.sync_record(Post, post,
           backend: Scrypath.Meilisearch,
           sync_mode: :inline,
           index_prefix: prefix,
           meilisearch_url: url,
           inline_poll_interval: 50
         )
```

From `examples/phoenix_meilisearch/test/smoke/meilisearch_oban_stack_test.exs`:
```elixir
assert {:ok,
        %{
          mode: :oban,
          status: :accepted,
          job: %{worker: "Scrypath.Oban.UpsertWorker", state: "completed"}
        }} =
         Scrypath.sync_record(Post, post,
           backend: Scrypath.Meilisearch,
           sync_mode: :oban,
           oban: ScrypathDemo.Oban,
           oban_queue: :scrypath,
           index_prefix: prefix,
           meilisearch_url: url
         )
```

From `examples/phoenix_meilisearch/scripts/smoke.sh`:
```bash
export SCRYPATH_MEILISEARCH_URL="${SCRYPATH_MEILISEARCH_URL:-http://127.0.0.1:7700}"
export PGPORT="${PGPORT:-5433}"
export SCRYPATH_EXAMPLE_INTEGRATION=1
docker compose up -d
mix deps.get
mix test
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the missing `:manual` proof to the Phoenix example</name>
  <files>examples/phoenix_meilisearch/test/smoke/meilisearch_manual_stack_test.exs</files>
  <behavior>
    - Test 1: `cd examples/phoenix_meilisearch && SCRYPATH_EXAMPLE_INTEGRATION=1 mix test` includes a real `:manual` smoke proof beside the existing `:inline` and `:oban` tests.
    - Test 2: The manual proof demonstrates the public `sync_mode: :manual` contract honestly per D-04, D-07, D-19, and D-24: accepted backend work is not the same as immediate search visibility, and any explicit follow-up is bounded and consumer-shaped.
    - Test 3: The proof stays near ExUnit and the existing smoke shape per D-25 and D-26; no browser/E2E harness or second example app appears.
  </behavior>
  <action>Add a focused integration smoke test for `sync_mode: :manual` in the Phoenix example. Reuse the existing live Postgres + Meilisearch setup and keep the proof minimal but concrete: assert the accepted manual return envelope first, then use a bounded explicit follow-up through `Scrypath.sync_status/2` or an equivalent helper to observe completion before asserting `Scrypath.search/3` returns the row. This must demonstrate that accepted work is not immediate visibility while still closing the loop in one real-app proof. The new test must complement rather than replace the existing inline and Oban proofs, and it must stay within the single-example boundary from D-05.</action>
  <verify>
    <automated>cd examples/phoenix_meilisearch && SCRYPATH_EXAMPLE_INTEGRATION=1 mix test</automated>
  </verify>
  <done>The example test surface now proves inline, manual, and Oban adoption on the real example app using the same integration command shape CI already uses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote the example README and bounded docs contracts to canonical proof authority</name>
  <files>examples/phoenix_meilisearch/README.md, test/scrypath/docs_contract_test.exs</files>
  <behavior>
    - Test 1: The example README becomes the source of truth for env vars, startup order, CI-vs-local command order, and explicit inline/manual/Oban proof paths per D-03, D-06, D-17, and D-25.
    - Test 2: `test/scrypath/docs_contract_test.exs` extends only the Phase 68 drift surface per D-20 through D-23: canonical example/support links, start-here order, env names, startup/command order, and mode-coverage wording where needed.
    - Test 3: The example README links outward to the support guide and deeper operational guides per D-15 and D-19 instead of duplicating sync semantics or becoming the whole onboarding system.
  </behavior>
  <action>Rewrite `examples/phoenix_meilisearch/README.md` as the canonical adopter-proof source per D-03, D-06, and D-17: what the example proves, the three sync stories it covers, which command path CI runs, what local smoke proves, and where to go next for conceptual/operational depth. Then extend `test/scrypath/docs_contract_test.exs` with bounded Phase 68 assertions only: canonical support/example link targets, README -> golden path -> example -> support ordering, example env var names, command/startup order, and wording that confirms all three modes are covered. Do not snapshot long prose or introduce a manifest/meta-system.</action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs && mix docs --warnings-as-errors && cd examples/phoenix_meilisearch && SCRYPATH_EXAMPLE_INTEGRATION=1 mix test</automated>
  </verify>
  <done>The example README is the canonical real-app proof, and the docs-contract suite fails on Phase 68 fact/navigation drift without freezing the whole prose body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example README -> adopters | Readers use the example as the source of truth for startup commands, env vars, and supported proof paths |
| tests/contracts -> maintainer confidence | False-positive proof or docs drift would let unsupported combinations look verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04 | T | `examples/phoenix_meilisearch/README.md` | mitigate | Make the example README authoritative for env names, startup order, and CI/local command shape, then lock those facts with bounded docs contracts. |
| T-68-05 | I | `examples/phoenix_meilisearch/README.md` | mitigate | Keep only placeholder/local env names, avoid secret examples, and route deeper security posture to `guides/meilisearch-operations.md`. |
| T-68-06 | S | `examples/phoenix_meilisearch/test/smoke/meilisearch_manual_stack_test.exs` | mitigate | Prove `:manual` with real integration behavior and explicit follow-up semantics so docs cannot imply immediate visibility or a broader support story than the example actually exercises. |
</threat_model>

<verification>
- After each task commit: `mix test test/scrypath/docs_contract_test.exs`
- Wave completion gate: `mix docs --warnings-as-errors && cd examples/phoenix_meilisearch && SCRYPATH_EXAMPLE_INTEGRATION=1 mix test`
- Human spot-check before phase verification: `cd examples/phoenix_meilisearch && ./scripts/smoke.sh`
</verification>

<success_criteria>
- The Phoenix example README and tests together prove `:inline`, `:manual`, and `:oban` on one real example app.
- The example README is the authority for env vars, startup order, CI/local proof paths, and next-step links.
- `test/scrypath/docs_contract_test.exs` fails when canonical start-here links, env names, or command order drift across README, guides, CONTRIBUTING, and the example.
</success_criteria>

<output>
After completion, create `.planning/phases/68-example-proof-and-support-contract/68-example-proof-and-support-contract-02-SUMMARY.md`
</output>
