---
phase: 144-root-http-client-dependency-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - mix.exs
  - mix.lock
  - scrypath_ops/mix.exs
  - scrypath_ops/mix.lock
  - examples/phoenix_meilisearch/mix.lock
  - examples/scrypath_ecommerce/mix.exs
  - examples/scrypath_ecommerce/mix.lock
autonomous: true
requirements: [SEC-01, COMPAT-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Per D-01 through D-06, root Scrypath, ScrypathOps, and ecommerce directly require Req ~> 0.6.1, root test-only Plug requires ~> 1.19.5, and all four checked-in Mix locks form one compatible, explained Req-floor handoff without graph-local framework/database remediation."
    - "Per D-07 through D-11, this tracer establishes the checked Req 0.6 dependency state consumed by Plan 02's focused root Meilisearch compatibility proof while reserving Swoosh runtime proof for Phase 146."
    - "Per D-18 through D-22, the handoff leaves production compatibility ownership to the failure-driven Plan 02 seam, keeps adopter APIs/defaults unchanged, and gives maintainers causal lock diffs plus explicit stop conditions."
    - "D-21: Security, correctness, compatibility, least surprise, reproducibility, maintainability, evidence clarity, bounded CI cost, and strict scope discipline govern this dependency-only phase; visual UI, accessibility styling, theme behavior, and brand expression are not applicable and must not enter scope."
  artifacts:
    - path: "mix.exs"
      provides: "Root Req ~> 0.6.1 and test-only Plug ~> 1.19.5 constraints"
      contains: "{:req, \"~> 0.6.1\"}"
    - path: "scrypath_ops/mix.exs"
      provides: "ScrypathOps direct Req ~> 0.6.1 compatibility constraint"
      contains: "{:req, \"~> 0.6.1\"}"
    - path: "examples/scrypath_ecommerce/mix.exs"
      provides: "Ecommerce direct Req ~> 0.6.1 compatibility constraint"
      contains: "{:req, \"~> 0.6.1\"}"
  key_links:
    - from: "mix.exs"
      to: "scrypath_ops/mix.exs, examples/scrypath_ecommerce/mix.exs, examples/phoenix_meilisearch/mix.lock"
      via: "one Req version per independently resolved path-consumer graph (D-01/D-06)"
      pattern: "~> 0.6.1"
  prohibitions:
    - statement: "[FLAGGED-UNVERIFIED] SEC-01 remediation must not silently become package-head modernization, unrelated framework/database churn, dependency overrides, or advisory suppression."
      status: unresolved
      verification: null
    - statement: "[FLAGGED-UNVERIFIED] COMPAT-02 evidence from Phase 144 must not claim that the configured ScrypathOps Swoosh Req client received runtime proof in this phase."
      status: unresolved
      verification: null
---

<objective>
Perform the production-quality tracer for the shared Req 0.6 compatibility path: reconcile the delivery wording, then land the atomic three-direct-Req/four-lock handoff and prove Scrypath's root Req-backed behavior at its existing public boundary.

Purpose: Maintainers and ordinary path consumers must resolve one patched compatible Req closure before any later graph-local advisory batch begins (D-01, D-02, D-20).
Output: Corrected planning truth, three bounded direct Req constraints, the bounded root Plug constraint, and four causal locks ready for Plan 02 compatibility proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTRIBUTING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/144-root-http-client-dependency-remediation/144-CONTEXT.md
@.planning/phases/144-root-http-client-dependency-remediation/144-RESEARCH.md
@.planning/phases/144-root-http-client-dependency-remediation/144-PATTERNS.md
@.planning/phases/144-root-http-client-dependency-remediation/144-VALIDATION.md

<interfaces>
`Scrypath.Meilisearch.Client.get_settings/2`, `tasks/2`, and the other public Client functions return `{:ok, map()} | {:error, term()}`. The private `run_request/5` builds the request through `request/1`, calls `Req.request/2`, and normalizes non-2xx responses to `{:error, {:http_error, status, body}}` and transport failures to `{:error, {:transport_error, exception}}`. `request/1` uses `Keyword.put_new(:base_url, ...)` and prepends `default_headers(config)` to caller headers. `Scrypath.Telemetry.span/3` emits start/stop/exception events and merges only the callback's response metadata.
</interfaces>
</context>

## Artifacts this phase produces

This plan creates no new public symbol, CLI flag, struct field, permanent script, Mix task, CI lane, or product file. It adds only these genuinely new test cases/artifacts:

- Updated `mix.exs`, `scrypath_ops/mix.exs`, `examples/scrypath_ecommerce/mix.exs`, and four existing lockfiles.

<flagged_planner_assumptions>
- SEC-01 edge probe: UNRESOLVED / unclassified. Phase 144 proof boundary: Plan 01 owns only the bounded three-direct-manifest/four-lock Req-floor handoff that feeds the later exact-candidate root proof; `unresolved` describes deterministic edge-probe classification only and gives the executor no discretion to broaden or reinterpret this locked boundary. Concrete execution observation: the three Req constraints, root Plug constraint, four selected lock closures, four exact `mix deps.get --check-locked` exits, and causal moved-row explanation establish this plan's bounded outcome. Excluded later-phase ownership: Phase 145 owns legacy graph-local remediation, Phase 146 owns Ops/Swoosh remediation and Swoosh runtime behavior, and Phase 147 owns ecommerce remediation plus all-four-graph advisory closure. Exact fail/unavailable condition: any out-of-bound selection, nonzero or unavailable checked-lock/solver command, unrelated or unexplained moved row, or solver inability to produce the locked scope stops this plan, blocks Phase 144 completion and Phase 145, and requires re-planning; unavailable evidence is not a pass and no generic edge backstop is inferred.
- COMPAT-02 edge probe: UNRESOLVED / unclassified. Phase 144 proof boundary: Plan 01 establishes only the atomic checked dependency state consumed by Plan 02's root Meilisearch Req.Test/Telemetry proof; `unresolved` describes deterministic edge-probe classification only and gives the executor no discretion to treat dependency resolution as broader runtime proof. Concrete execution observation: the bounded manifests and all four checked locks resolve successfully at the specified floors without changing adopter-facing APIs, defaults, or later-phase package rows. Excluded later-phase ownership: Plan 02 owns the four root causal compatibility cases, Plan 03 owns root release-train evidence, Phase 146 owns all Swoosh runtime behavior, and Phase 147 owns all-four-graph advisory closure. Exact fail/unavailable condition: any nonzero or unavailable checked-lock result, broadened source/default/API need, or inability to preserve the locked later-phase rows stops this plan, blocks Phase 144 completion and Phase 145, and requires re-planning; unavailable evidence is not a pass.
</flagged_planner_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Reconcile delivery truth and land the atomic shared Req-floor handoff</name>
  <reversibility rating="costly">Per D-01, undo requires a coordinated three-manifest/four-lock reversal and reopens the recorded Req advisory floor.</reversibility>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, mix.exs, mix.lock, scrypath_ops/mix.exs, scrypath_ops/mix.lock, examples/phoenix_meilisearch/mix.lock, examples/scrypath_ecommerce/mix.exs, examples/scrypath_ecommerce/mix.lock</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/phases/144-root-http-client-dependency-remediation/144-CONTEXT.md
    .planning/phases/144-root-http-client-dependency-remediation/144-RESEARCH.md
    .planning/todos/pending/2026-08-16-remediate-dependency-security-advisories.md
    .planning/quick/260816-tzr-triage-dependency-security-advisories-re/260816-tzr-ADVISORY-TRIAGE.md
    mix.exs
    mix.lock
    scrypath_ops/mix.exs
    scrypath_ops/mix.lock
    examples/phoenix_meilisearch/mix.exs
    examples/phoenix_meilisearch/mix.lock
    examples/scrypath_ecommerce/mix.exs
    examples/scrypath_ecommerce/mix.lock
  </read_first>
  <action>
    First satisfy D-02 before touching dependency declarations: replace only Phase 144 success criterion 4 in `.planning/ROADMAP.md` so it states that one minimal, explained shared Req compatibility handoff spans the three direct manifests and four locks, followed by graph-local legacy/Ops/ecommerce remediation; replace only EVID-02 in `.planning/REQUIREMENTS.md` with the same truthful milestone delivery shape. Do not reassign requirement traceability or later-phase ownership.

    Then execute D-01/D-03/D-04: change only the existing direct Req entries in `mix.exs`, `scrypath_ops/mix.exs`, and `examples/scrypath_ecommerce/mix.exs` to `~> 0.6.1`; change only root's test-scoped Plug entry to `~> 1.19.5`. Keep the legacy example free of a direct Req declaration per D-06.

    Refresh the causal Req closure in each independent graph using targeted Mix dependency update/unlock commands: root owns Req plus root Plug; Ops and ecommerce own their direct Req closure; the legacy example refreshes Req only through the changed root path dependency. The reviewed checked-lock result must contain Req `>= 0.6.1` and `< 0.7.0`, Mint `>= 1.9.3`, hpax `>= 1.0.4`, and root Plug `>= 1.19.5` and `< 1.20.0`. Explain every moved Req/Finch/Mint/hpax/Plug or unavoidable solver row in the task summary. Per D-05/D-06/D-16, preserve every unrelated manifest entry and graph-local Phoenix, Bandit, Ecto, Decimal, Postgrex, LiveView, and Swoosh lock row; add no direct Mint/hpax constraint, override, ignore, script, task, policy, or CI wiring.

    Stop without committing and re-plan per D-17/D-19 if the solver cannot produce that bounded state without an unrelated upgrade, a lock remains dirty after `--check-locked`, a moved row has no causal explanation, or the handoff would alter public APIs/config/default request semantics. This tracer commit is the smallest truthful cross-graph compatibility handoff and must not be split into a misleading root-only state (D-20 through D-22).
  </action>
  <verify>
    <automated>mix deps.get --check-locked &amp;&amp; (cd scrypath_ops &amp;&amp; mix deps.get --check-locked) &amp;&amp; (cd examples/phoenix_meilisearch &amp;&amp; mix deps.get --check-locked) &amp;&amp; (cd examples/scrypath_ecommerce &amp;&amp; mix deps.get --check-locked) &amp;&amp; test "$(rg -l '\{:req, "~> 0\.6\.1"\}' mix.exs scrypath_ops/mix.exs examples/scrypath_ecommerce/mix.exs | wc -l | tr -d ' ')" = 3 &amp;&amp; rg -q '\{:plug, "~> 1\.19\.5", only: :test\}' mix.exs &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    `.planning/ROADMAP.md` criterion 4 and `.planning/REQUIREMENTS.md` EVID-02 describe exactly one shared Req-floor compatibility handoff followed by graph-local batches (D-02). All three direct manifests contain Req `~> 0.6.1`; root contains test-only Plug `~> 1.19.5`; the legacy manifest has no direct Req. All four `mix deps.get --check-locked` commands exit 0. Lock review records only Req/Finch/Mint/hpax/root-Plug or an explicitly solver-required row, with target floors satisfied and later-phase package rows unchanged. No primary lock is dirty after the checks.
  </acceptance_criteria>
  <done>The repository has one reviewable, compatible three-direct-Req/four-lock state that clears the recorded root HTTP-client floors without beginning Phases 145-147.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry/solver → four checked locks | Network-sourced package metadata determines the code selected by four independent graphs. |
| Checked dependency state → compatibility plan | The atomic solver result becomes the input to Plan 02's request/telemetry proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-01 | Tampering / DoS | Mix manifests and four locks | high | mitigate | D-03/D-04 bounded direct constraints, target-floor lock review, causal-row explanation, and four `mix deps.get --check-locked` gates. |

## OWASP ASVS L1 Control Mapping

| ASVS Control | Applicable Threats | Plan Enforcement |
|---|---|---|
| V2 Authentication | N/A at the manifest/lock boundary | Preserve the dependency state required for Plan 02's additive API-key-header proof; this tracer does not alter authentication material or request construction. |
| V5 Input Validation | T-144-01 | Accept only the exact bounded Req/Plug constraints and target transitive floors; reject unrelated or unexplained solver rows. |
| V7 Error Handling and Logging | T-144-01 | Record causal solver-row explanations and explicit command failures without treating an unresolved or dirty state as success. |
| V14 Configuration | T-144-01 | Enforce three direct constraints, four checked locks, no overrides/ignores/direct transitive constraints, and unchanged graph-local package rows. |
</threat_model>

<verification>
Run the four checked-lock gates and inspect the bounded manifest/lock diff. Any unrelated solver row, dirty lock, or unverified high-severity mitigation blocks completion and Phase 145, requiring stop/re-plan; only a clean checked state may proceed to Plan 02.
</verification>

<success_criteria>
- SEC-01: the shared checked state resolves Req/Mint/hpax/root-Plug beyond the recorded floors using bounded direct constraints and causal locks.
- COMPAT-02: the exact checked dependency state needed for Plan 02's focused root Meilisearch proof exists; Swoosh runtime proof is truthfully reserved for Phase 146.
- D-01 through D-06, D-11, and D-18 through D-22 are observable in source, handoff ownership, or explicit stop conditions.
- Any failed or unverified high-severity mitigation blocks completion and Phase 145, requiring stop/re-plan.
</success_criteria>

<output>
Create `.planning/phases/144-root-http-client-dependency-remediation/144-01-SUMMARY.md` when done.
</output>
