---
phase: 144-root-http-client-dependency-remediation
plan: 02
type: execute
wave: 2
depends_on: [144-01]
files_modified:
  - test/scrypath/meilisearch/client_test.exs
  - test/scrypath/telemetry_test.exs
  - lib/scrypath/meilisearch/client.ex
autonomous: true
requirements: [COMPAT-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Per D-07/D-08, Req 0.6 preserves decoded JSON successes, tagged HTTP and transport errors, caller req_options, additive API-key headers, unique task-filter encoding, and request telemetry at Scrypath's public boundary."
    - "Per D-09/D-11, compatibility proof leaves compression, archive decoding, multipart, retry, redirect, timeout, and decompression defaults unchanged and makes no Swoosh runtime-proof claim; Phase 146 owns that focused Swoosh contract."
    - "Per D-18/D-19, production source changes only after a demonstrated Req 0.6 compile/test failure, remain private and regression-tested, and stop/re-plan rather than altering public APIs, defaults, or transport architecture."
    - "Per D-20 through D-22, the checked dependency handoff remains invisible to adopter APIs and behavior while the causal compatibility evidence uses existing Scrypath domain language and quality gates."
  artifacts:
    - path: "test/scrypath/meilisearch/client_test.exs"
      provides: "Focused Req 0.6 transport, header-merge, and task-filter regression coverage"
    - path: "test/scrypath/telemetry_test.exs"
      provides: "Error-span metadata privacy regression coverage"
    - path: "lib/scrypath/meilisearch/client.ex"
      provides: "Existing private Req request/normalization seam; changed only after a demonstrated focused failure"
  key_links:
    - from: "test/scrypath/meilisearch/client_test.exs"
      to: "lib/scrypath/meilisearch/client.ex"
      via: "public Client calls through the existing Req.Test plug seam (D-07/D-08)"
      pattern: "Req.Test"
    - from: "test/scrypath/telemetry_test.exs"
      to: "Scrypath.Telemetry.span/3"
      via: "captured [:scrypath, :meilisearch, :request] error event excludes request secrets"
      pattern: "meilisearch.*request"
  prohibitions:
    - statement: "[FLAGGED-UNVERIFIED] COMPAT-02 evidence from Phase 144 must not claim that the configured ScrypathOps Swoosh Req client received runtime proof in this phase."
      status: unresolved
      verification: null
---

<objective>
Prove the focused Req 0.6 compatibility contract against Plan 01's atomic checked state, with conditional production-source ownership limited to a demonstrated private-seam failure.

Purpose: The dependency handoff is not eligible for deterministic release-train evidence until Scrypath's root Meilisearch request and telemetry contracts have causal Req.Test coverage (D-07, D-08, D-18).
Output: Four focused Req.Test/Telemetry regression cases and, only if a red test requires it, the smallest private client compatibility fix.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTRIBUTING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/144-root-http-client-dependency-remediation/144-CONTEXT.md
@.planning/phases/144-root-http-client-dependency-remediation/144-RESEARCH.md
@.planning/phases/144-root-http-client-dependency-remediation/144-PATTERNS.md
@.planning/phases/144-root-http-client-dependency-remediation/144-VALIDATION.md
@.planning/phases/144-root-http-client-dependency-remediation/144-01-SUMMARY.md

<interfaces>
`Scrypath.Meilisearch.Client.get_settings/2`, `tasks/2`, and the other public Client functions return `{:ok, map()} | {:error, term()}`. The private `run_request/5` builds the request through `request/1`, calls `Req.request/2`, and normalizes non-2xx responses to `{:error, {:http_error, status, body}}` and transport failures to `{:error, {:transport_error, exception}}`. `request/1` uses `Keyword.put_new(:base_url, ...)` and prepends `default_headers(config)` to caller headers. `Scrypath.Telemetry.span/3` emits start/stop/exception events and merges only the callback's response metadata.
</interfaces>
</context>

## Artifacts this phase produces

This plan creates no new public symbol, CLI flag, struct field, permanent script, Mix task, CI lane, dependency policy, or product file. It adds only these genuinely new test cases/artifacts:

- `client_test.exs`: retry-disabled transport-error normalization test.
- `client_test.exs`: default API-key plus caller-header merge test.
- `client_test.exs`: unique comma-separated task-filter encoding test.
- `telemetry_test.exs`: request-error metadata privacy test.
- A private `client.ex` compatibility edit only if a focused red failure demonstrates it is required.

<flagged_planner_assumptions>
- COMPAT-02 edge probe: UNRESOLVED / unclassified. Phase 144 proof boundary: this plan owns only root Scrypath Meilisearch request construction, tagged error normalization, task-filter encoding, and request-telemetry privacy across the Req 0.6 transition; `unresolved` describes deterministic edge-probe classification only and gives the executor no discretion to broaden or reinterpret that locked boundary. Concrete execution observation: the exact focused ExUnit command passes the four new causal Req.Test/Telemetry cases together with the existing success, HTTP-error, sync, multi-search, facet-search, and success-telemetry cases, and any production edit is tied to a demonstrated red case at the existing private seam. Excluded later-phase ownership: Phase 146 owns configured Swoosh client behavior and runtime proof, while Phase 147 owns ecommerce remediation and all-four-graph advisory closure; Plan 03 separately owns release-train and network-dependent root evidence. Exact fail/unavailable condition: a nonzero or unavailable focused command, missing causal assertion, secret-bearing error metadata, altered Req defaults, or any required public API/default/transport-architecture/broad-refactor change stops this plan, blocks Phase 144 completion and Phase 145, and requires re-planning; unavailable evidence is not a pass.
</flagged_planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the causal Req.Test and telemetry compatibility gaps</name>
  <files>test/scrypath/meilisearch/client_test.exs, test/scrypath/telemetry_test.exs, lib/scrypath/meilisearch/client.ex (conditional only after a demonstrated failure)</files>
  <read_first>
    .planning/phases/144-root-http-client-dependency-remediation/144-CONTEXT.md
    .planning/phases/144-root-http-client-dependency-remediation/144-RESEARCH.md
    .planning/phases/144-root-http-client-dependency-remediation/144-01-SUMMARY.md
    lib/scrypath/meilisearch/client.ex
    lib/scrypath/telemetry.ex
    test/scrypath/meilisearch/client_test.exs
    test/scrypath/meilisearch/client_multi_search_test.exs
    test/scrypath/sync_test.exs
    test/scrypath/telemetry_test.exs
  </read_first>
  <behavior>
    - "A retry-disabled `Req.Test.transport_error/2` response returns `{:error, {:transport_error, %Req.TransportError{reason: reason}}}` through a public Client call (D-07/D-08)."
    - "A configured Meilisearch API key and a caller header are both visible once at the Req.Test plug boundary; caller req_options remain effective (D-07/D-08)."
    - "`Client.tasks/2` encodes each list filter once as one comma-separated camelCase query value, omitting nil filters (D-08)."
    - "A transport failure emits request error metadata with request identity and error information but no headers, body, API-key value, or payload field (D-07/D-08)."
  </behavior>
  <action>
    Write the four focused tests before changing production code. Extend the existing `client_test.exs` Req.Test setup for retry-disabled transport normalization, additive default/caller headers, and unique task-filter query encoding; extend the existing `async: false` telemetry module and `capture_events/2` helper for the error-span privacy assertion. Assert Scrypath's public tuples and Plug.Conn-observable request shape, not Req structs or internal step ordering. Reuse all existing decoded-JSON, HTTP-error, sync, multi-search, facet-search, and success-span coverage rather than cloning wrapper tests (D-07/D-08).

    Keep Req 0.6/0.6.1 secure defaults unchanged per D-09: do not opt into decompression, archive decoding, multipart behavior, or alter retry/redirect/timeout defaults. Do not add Mox, Bypass, a public transport seam, or Swoosh assertions; D-11 assigns the configured Swoosh client proof to Phase 146.

    Run the focused tests against Plan 01's checked lock. Edit `lib/scrypath/meilisearch/client.ex` only if a failing compile/test demonstrates a Req 0.6 incompatibility. Any permitted D-18 fix must stay inside the private request construction/normalization seam, preserve public return/header/options/telemetry meanings, and be justified by its red regression. Stop and re-plan under D-19 for any public API/default/transport abstraction/broad refactor need.
  </action>
  <verify>
    <automated>mix test test/scrypath/meilisearch/client_test.exs test/scrypath/telemetry_test.exs --exclude integration --exclude docs_contract</automated>
  </verify>
  <acceptance_criteria>
    The focused command exits 0 and reports all four new regression cases passing. The transport test uses `retry: false`; the header test observes both `x-meili-api-key` and a caller header; the tasks test observes one comma-separated value per list filter; the error-span test explicitly refutes headers, body/payload fields, and the configured secret value. `git diff -- lib/scrypath/meilisearch/client.ex` is empty unless the task summary names the demonstrated red failure and the focused regression guarding the smallest private fix. No Swoosh proof, compression/multipart behavior, public API, new dependency, or permanent infrastructure is added.
  </acceptance_criteria>
  <done>Req 0.6 compatibility is covered at the stable Scrypath client and telemetry boundaries with causal tests and no broadened transport contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller config → Req request | Caller options and API-key material cross into the internal HTTP client. |
| Upstream response/error → telemetry | Untrusted response/failure data is normalized and exposed as bounded metadata. |
| Checked Req dependency state → private client seam | Plan 01's resolved transport implementation exercises Scrypath's stable public contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-02 | Information disclosure | `Scrypath.Meilisearch.Client` request telemetry | high | mitigate | Retry-disabled transport failure test asserts error identity while excluding headers, bodies, payload fields, and API-key values. |
| T-144-03 | Denial of service | Req response decoding/decompression defaults | medium | mitigate | Preserve Req 0.6.1 JSON-only secure defaults per D-09; stop/re-plan if compatibility requires changing decompression/retry/timeout semantics. |
| T-144-04 | Spoofing | API-key plus caller-header merge | medium | mitigate | Req.Test plug-boundary regression proves the configured key and caller header coexist without replacing caller options. |

## OWASP ASVS L1 Control Mapping

| ASVS Control | Applicable Threats | Plan Enforcement |
|---|---|---|
| V2 Authentication | T-144-04 | Exercise the configured Meilisearch API key at the Req.Test plug boundary and prove it coexists once with caller headers and options. |
| V5 Input Validation | T-144-03 | Assert list filters are encoded exactly once as comma-separated camelCase query values, omit nil filters, and retain bounded JSON-only request behavior. |
| V7 Error Handling and Logging | T-144-02 | Preserve tagged HTTP/transport errors and prove request-error telemetry contains error identity but excludes headers, bodies, payloads, and API-key values. |
| V14 Configuration | T-144-03, T-144-04 | Preserve caller `req_options` plus secure Req defaults; any required default, retry, redirect, timeout, decompression, or transport-architecture change triggers stop/re-plan. |
</threat_model>

<verification>
Run the focused Req.Test/telemetry command against Plan 01's checked dependency state and inspect any production diff for a named red regression plus the smallest private fix. Any failed or unverified high-severity mitigation blocks completion and Phase 145, requiring stop/re-plan; only a green compatibility result may proceed to Plan 03.
</verification>

<success_criteria>
- COMPAT-02: root Req-backed Meilisearch behavior has focused causal coverage; Swoosh runtime proof is truthfully reserved for Phase 146.
- D-07 through D-11 and D-18 through D-22 are observable in tests, conditional private-source ownership, or explicit stop conditions.
- Any failed or unverified high-severity mitigation blocks completion and Phase 145, requiring stop/re-plan.
</success_criteria>

<output>
Create `.planning/phases/144-root-http-client-dependency-remediation/144-02-SUMMARY.md` when done.
</output>
