---
phase: 144-root-http-client-dependency-remediation
plan: 03
type: execute
wave: 3
depends_on: [144-02]
files_modified: []
autonomous: true
requirements: [SEC-01, COMPAT-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Per D-10/D-12, the candidate passes the complete service-free root release-train bundle and every touched graph passes `mix deps.get --check-locked`; deterministic results are reported separately from live network evidence."
    - "Per D-13/D-14, a disposable detached worktree at the exact candidate SHA resolves without its root lock using isolated dependency/build paths and selects Req >= 0.6.1 and < 0.7.0, Plug >= 1.19.5 and < 1.20.0, Mint >= 1.9.3, and hpax >= 1.0.4; root `mix hex.audit` exits zero without suppression."
    - "Per D-15/D-16, the compact evidence record contains only dated provenance, commands/exits, target versions/paths, audit result, evidence classification, and supplemental smoke status; no raw logs, generated trees, lock snapshots, permanent task/script/CI/policy, or security abstraction is committed."
    - "Per D-17/D-19, unavailable registry/feed/service evidence is never a pass, and any failed floor, audit, clean-lock, required gate, or broadened compatibility need blocks Phase 145 and triggers stop/re-plan."
    - "Per D-11/D-20 through D-22, evidence describes the unchanged consumer-facing Scrypath contract and does not claim Swoosh runtime proof or expose Req migration details through public APIs/docs."
  artifacts:
    - path: ".planning/phases/144-root-http-client-dependency-remediation/144-03-SUMMARY.md"
      provides: "Compact deterministic-versus-network Phase 144 audit record produced by execute-plan"
  key_links:
    - from: "candidate commit SHA"
      to: "detached lockless root resolution"
      via: "git worktree add --detach at the exact SHA with isolated MIX_DEPS_PATH/MIX_BUILD_PATH (D-13)"
    - from: "fresh resolved package set"
      to: "SEC-01"
      via: "strict Req/Plug upper and lower bounds, Mint/hpax minima, dependency paths, and unsuppressed root mix hex.audit (D-14)"
    - from: "Plan 01 checked locks and Plan 02 focused tests"
      to: "root release-train gates"
      via: "warnings-as-errors compile, fast tests, verify, phase11, and phase99 on the reviewed candidate (D-10/D-12)"
  prohibitions:
    - statement: "[FLAGGED-UNVERIFIED] SEC-01 evidence must not silently report a registry, advisory-feed, or Meilisearch-service outage as a passing result."
      status: unresolved
      verification: null
---

<objective>
Seal the candidate with two explicit proof classes: first the complete service-free deterministic gate bundle on reviewed locks, then a network-dependent detached exact-candidate fresh resolution and root advisory audit.

Purpose: Maintainers need reproducible green-main confidence and honest current-registry evidence before Phase 145 can begin (D-10, D-12, D-17).
Output: Green deterministic gates plus a compact dated exact-SHA fresh-resolution/audit record in the executor summary.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTRIBUTING.md
@docs/releasing.md
@.github/workflows/ci.yml
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/144-root-http-client-dependency-remediation/144-CONTEXT.md
@.planning/phases/144-root-http-client-dependency-remediation/144-RESEARCH.md
@.planning/phases/144-root-http-client-dependency-remediation/144-VALIDATION.md
@.planning/phases/144-root-http-client-dependency-remediation/144-01-SUMMARY.md
@.planning/phases/144-root-http-client-dependency-remediation/144-02-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates no new symbol, CLI flag, struct field, source file, permanent script, Mix task, CI lane, dependency policy, or generated dependency artifact. Its only new file is the normal execute-plan output `.planning/phases/144-root-http-client-dependency-remediation/144-03-SUMMARY.md`, containing the compact evidence fields required by D-15. Disposable worktree contents, its lock, raw logs, advisory snapshots, and generated dependency trees are explicitly not phase artifacts.

<flagged_planner_assumptions>
- SEC-01 edge probe: UNRESOLVED / unclassified. Phase 144 proof boundary: this plan owns deterministic checked-lock/root gates and one exact-candidate detached fresh root resolution plus unsuppressed root Hex audit for Req, Plug, Mint, and hpax; `unresolved` describes deterministic edge-probe classification only and gives the executor no discretion to broaden or reinterpret that locked boundary. Concrete execution observation: all named service-free commands exit zero on one candidate SHA, then the isolated lockless root resolve selects Req `>= 0.6.1 and < 0.7.0`, Plug `>= 1.19.5 and < 1.20.0`, Mint `>= 1.9.3`, and hpax `>= 1.0.4` through the expected paths and unsuppressed `mix hex.audit` exits zero. Excluded later-phase ownership: Phase 146 owns Ops/Swoosh remediation and Swoosh runtime behavior, and Phase 147 owns ecommerce remediation plus all-four-graph advisory closure. Exact fail/unavailable condition: any nonzero required gate or audit, out-of-bound or unexplained selection/path, dirty primary state, or unavailable registry/advisory feed stops Phase 144 completion, blocks Phase 145, and requires re-planning; unavailable evidence is never converted to pass or backstop.
- COMPAT-02 edge probe: UNRESOLVED / unclassified. Phase 144 proof boundary: this plan seals only the root Meilisearch compatibility outcome already made causal by Plan 02, using the complete root release-train bundle; `unresolved` describes deterministic edge-probe classification only and gives the executor no discretion to infer broader runtime or graph coverage. Concrete execution observation: Plan 02's exact focused suite is recorded green and all required checked-lock, compile, test, verify, phase11, and phase99 commands exit zero on the same reviewed candidate; an available live Meilisearch smoke is supplemental only. Excluded later-phase ownership: Phase 146 owns Swoosh behavior and runtime proof, while Phase 147 owns ecommerce remediation and all-four-graph advisory closure. Exact fail/unavailable condition: missing or failed Plan 02 evidence, any nonzero required root/checked-lock gate, dirty candidate state, or a compatibility finding that requires broadened APIs/defaults/Swoosh/transport architecture stops Phase 144 completion, blocks Phase 145, and requires re-planning; supplemental smoke unavailability is recorded but cannot replace or negate the required service-free proof.
</flagged_planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Run the deterministic checked-lock and root release-train bundle</name>
  <files>None; record compact command/exit evidence in the normal 144-03-SUMMARY.md output.</files>
  <read_first>
    AGENTS.md
    CONTRIBUTING.md
    docs/releasing.md
    .github/workflows/ci.yml
    .planning/phases/144-root-http-client-dependency-remediation/144-CONTEXT.md
    .planning/phases/144-root-http-client-dependency-remediation/144-01-SUMMARY.md
    mix.exs
    mix.lock
    scrypath_ops/mix.exs
    scrypath_ops/mix.lock
    examples/phoenix_meilisearch/mix.exs
    examples/phoenix_meilisearch/mix.lock
    examples/scrypath_ecommerce/mix.exs
    examples/scrypath_ecommerce/mix.lock
    lib/mix/tasks/verify.ex
    lib/mix/tasks/verify.phase11.ex
    lib/mix/tasks/verify.phase99.ex
  </read_first>
  <action>
    Assert the primary worktree is clean for tracked files, Plan 01's manifest/lock diff explanation accounts for every moved row, and Plan 02's focused Req.Test/telemetry compatibility suite passed. Run `mix deps.get --check-locked` in root, ScrypathOps, legacy Phoenix, and ecommerce. Then run D-10's binding root sequence on the checked candidate: `mix deps.get`, `mix compile --warnings-as-errors`, `mix test --exclude integration --exclude docs_contract`, `mix verify --exclude integration`, `mix verify.phase11`, and `mix verify.phase99`. Do not substitute a narrower alias or promote a new lane.

    Keep results classified as deterministic per D-12. If a local Meilisearch service is already available, run the existing `mix verify.meilisearch_smoke` path with the documented URL and report it separately as supplemental; if prerequisites are unavailable, record `unavailable` rather than pass and continue only because the smoke is non-required. Do not claim Swoosh runtime proof (D-11).

    Stop and block Phase 145 per D-17 if any checked-lock or required root command is nonzero, if the tracked workspace becomes dirty, or if investigation demands a D-19 change to APIs/defaults/Swoosh/schema/transport architecture/extra dependencies. Record only command names and exit statuses in the final summary, not raw output (D-15/D-16/D-20 through D-22).
  </action>
  <verify>
    <automated>mix deps.get --check-locked &amp;&amp; (cd scrypath_ops &amp;&amp; mix deps.get --check-locked) &amp;&amp; (cd examples/phoenix_meilisearch &amp;&amp; mix deps.get --check-locked) &amp;&amp; (cd examples/scrypath_ecommerce &amp;&amp; mix deps.get --check-locked) &amp;&amp; mix deps.get &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix test --exclude integration --exclude docs_contract &amp;&amp; mix verify --exclude integration &amp;&amp; mix verify.phase11 &amp;&amp; mix verify.phase99</automated>
  </verify>
  <acceptance_criteria>
    All four checked-lock commands and all six named root commands exit 0 on the same candidate. The tracked workspace is clean after the bundle. The summary classifies these results as deterministic and lists each command/exit without pasted logs. Supplemental Meilisearch smoke is separately `passed`, `failed`, or `unavailable`; a failure is reported, while unavailability is never labeled pass. No Swoosh runtime claim or new required lane appears.
  </acceptance_criteria>
  <done>The exact reviewed candidate is green on the complete deterministic root release-train contract and remains eligible for detached current-registry proof.</done>
</task>

<task type="auto">
  <name>Task 2: Prove exact-candidate fresh resolution and unsuppressed root audit</name>
  <precondition>Task 1's four checked-lock commands and complete root release-train bundle all exited 0, and the primary tracked worktree is clean.</precondition>
  <reversibility rating="reversible">The detached probe creates no persistent product state; its disposable worktree can be removed after compact evidence is captured.</reversibility>
  <files>None; record compact dated evidence in the normal 144-03-SUMMARY.md output and do not retain probe artifacts.</files>
  <read_first>
    .planning/phases/144-root-http-client-dependency-remediation/144-CONTEXT.md
    .planning/phases/144-root-http-client-dependency-remediation/144-RESEARCH.md
    .planning/quick/260816-tzr-triage-dependency-security-advisories-re/260816-tzr-ADVISORY-TRIAGE.md
    mix.exs
    mix.lock
    docs/releasing.md
  </read_first>
  <action>
    Capture the exact candidate with `git rev-parse HEAD`, the UTC timestamp, OS, Elixir/OTP, Mix, and Hex versions. Create a disposable directory with `mktemp -d /tmp/scrypath-phase144.XXXXXX`, add it as a detached worktree at that exact SHA, and verify the resolved path is inside `/tmp/scrypath-phase144.` before any removal. Inside only that detached worktree, remove only the root `mix.lock`; set `MIX_DEPS_PATH` and `MIX_BUILD_PATH` to directories inside the disposable worktree so the probe cannot reuse primary graph state. Run a fresh root `mix deps.get` with the live Hex registry.

    Inspect the freshly selected versions and `mix deps.tree` paths for Req, Mint, hpax, and Plug. Enforce D-14 exactly: Req `>= 0.6.1` and `< 0.7.0`; Plug `>= 1.19.5` and `< 1.20.0`; Mint `>= 1.9.3`; hpax `>= 1.0.4`. Run root `mix hex.audit` without ignore/suppression flags and require exit 0. Treat registry/advisory-feed outage as `unavailable`, never pass. Treat a below-floor/above-bound result, nonzero audit, or dependency path that does not match the expected root Req/Finch/Mint/hpax and root Plug closure as a D-17 blocker.

    Capture only D-15's compact fields in `144-03-SUMMARY.md`: candidate SHA, UTC timestamp, OS and Elixir/OTP/Mix/Hex versions, commands with exits, four selected versions and dependency paths, audit result, deterministic-vs-network classification, and supplemental smoke status from Task 1. Do not copy the fresh lock, raw logs, advisory feed/snapshot, or generated dependency tree into the repository. Remove the validated disposable worktree with `git worktree remove --force` and prune its registration after evidence capture; leave the primary lock byte-identical and tracked workspace clean. Per D-16/D-19/D-20 through D-22, do not turn this procedure into permanent code or public migration documentation.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain --untracked-files=no)" &amp;&amp; candidate_sha=$(git rev-parse HEAD) &amp;&amp; probe_dir=$(mktemp -d /tmp/scrypath-phase144.XXXXXX) &amp;&amp; git worktree add --detach "$probe_dir" "$candidate_sha" &amp;&amp; case "$probe_dir" in /tmp/scrypath-phase144.*) ;; *) exit 97 ;; esac &amp;&amp; rm "$probe_dir/mix.lock" &amp;&amp; (cd "$probe_dir" &amp;&amp; test "$(git rev-parse HEAD)" = "$candidate_sha" &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase144_deps" MIX_BUILD_PATH="$probe_dir/.phase144_build" mix deps.get &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase144_deps" MIX_BUILD_PATH="$probe_dir/.phase144_build" mix run --no-compile --no-deps-check -e 'lock = Mix.Dep.Lock.read(); Enum.each([{:req, ">= 0.6.1 and < 0.7.0"}, {:plug, ">= 1.19.5 and < 1.20.0"}, {:mint, ">= 1.9.3"}, {:hpax, ">= 1.0.4"}], fn {dep, requirement} -&gt; version = lock |&gt; Map.fetch!(dep) |&gt; elem(2); Version.match?(version, requirement) || raise "#{dep} #{version} violates #{requirement}" end)' &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase144_deps" MIX_BUILD_PATH="$probe_dir/.phase144_build" mix hex.audit) ; probe_status=$?; git worktree remove --force "$probe_dir"; git worktree prune; test "$probe_status" -eq 0 &amp;&amp; test -z "$(git status --porcelain --untracked-files=no)"</automated>
  </verify>
  <acceptance_criteria>
    The detached worktree HEAD equals the recorded candidate SHA; only its root lock is removed; dependency/build paths are isolated inside it. Fresh resolution selects Req in `[0.6.1, 0.7.0)`, Plug in `[1.19.5, 1.20.0)`, Mint `>= 1.9.3`, and hpax `>= 1.0.4`, with compact dependency paths recorded. Unsuppressed root `mix hex.audit` exits 0. The evidence record includes the required provenance and classifications but no raw log/tree/lock/advisory artifact. The disposable worktree is removed, the primary lock is unchanged, and tracked status is clean. Any unavailable network/feed, bad bound, nonzero audit, dirty primary lock, unexplained path, or required-gate failure is recorded as blocked and prevents Phase 145.
  </acceptance_criteria>
  <done>Maintainers have exact-SHA, current-registry proof that the root candidate resolves beyond all four recorded advisory floors and audits cleanly, without contaminating the reviewed lock state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Primary candidate → detached worktree | Evidence must exercise the exact reviewed commit without mutating its lock or build state. |
| Live Hex registry/advisory feed → evidence record | Mutable network data determines fresh versions and current audit status. |
| Command output → committed summary | Raw logs may contain excessive or environment-specific data; only compact provenance is retained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-05 | Tampering / Repudiation | Exact-candidate proof | high | mitigate | Record SHA before `git worktree add --detach`; require detached HEAD equality, isolated paths, clean primary state, and compact commands/exits. |
| T-144-06 | Tampering | Fresh dependency resolution | high | mitigate | Remove only detached root lock; enforce strict Req/Plug bounds and Mint/hpax minima; inspect dependency paths; require unsuppressed `mix hex.audit` zero. |
| T-144-07 | Denial of service | Registry/advisory availability | medium | accept | Outage cannot be controlled locally; D-17 records evidence unavailable and blocks Phase 145 rather than manufacturing a pass. |
| T-144-08 | Information disclosure | Evidence capture | low | mitigate | Commit only D-15 compact provenance; exclude raw logs, advisory snapshots, disposable locks, and generated dependency trees. |

## OWASP ASVS L1 Control Mapping

| ASVS Control | Applicable Threats | Plan Enforcement |
|---|---|---|
| V2 Authentication | N/A in this evidence-only plan | Authentication material is not exercised or retained here; Plan 02 owns the additive API-key-header contract. |
| V5 Input Validation | T-144-06 | Parse and enforce exact version bounds and dependency paths from the detached resolve; reject any out-of-range or unexplained selection. |
| V7 Error Handling and Logging | T-144-05, T-144-07, T-144-08 | Record command exits and unavailable network evidence explicitly, never as pass; retain compact provenance only and exclude raw logs or secret-bearing request data. |
| V14 Configuration | T-144-05, T-144-06 | Bind the probe to the exact candidate SHA, isolate build/dependency paths, remove only the detached root lock, prohibit ignores/overrides, and require a clean primary worktree. |
</threat_model>

<source_coverage_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Root resolves beyond Req/Mint/hpax/Plug advisories with covered Req behavior | 144-01, 144-02, 144-03 | COVERED | Atomic checked state, causal behavior proof, deterministic gates, fresh audit. |
| REQ | SEC-01 | Fixed-compatible root resolution | 144-01, 144-03 | COVERED | Bounded manifests/locks plus exact-candidate audit. |
| REQ | COMPAT-02 | Req-backed Meilisearch and Swoosh coverage after Req 0.6 | 144-02, 144-03 | COVERED | Root Meilisearch proven here; D-11 explicitly reserves focused Swoosh runtime proof for Phase 146. |
| RESEARCH | — | Atomic three-direct/four-lock handoff | 144-01 | COVERED | Leading tracer. |
| RESEARCH | — | Four focused Req.Test/telemetry gaps | 144-02 | COVERED | TDD compatibility plan. |
| RESEARCH | — | Deterministic versus network evidence | 144-03 | COVERED | Separate tasks and classifications. |
| RESEARCH | — | Exact-candidate detached fresh probe | 144-03 | COVERED | Isolated worktree with strict bounds/audit. |
| CONTEXT | D-01–D-06 | Shared Req floor and bounded lock scope | 144-01 | COVERED | Task 1. |
| CONTEXT | D-07–D-11 | Stable Req behavior and proof boundary | 144-02, 144-03 | COVERED | Focused tests, full gates, no Swoosh claim. |
| CONTEXT | D-12–D-17 | Evidence classes, detached probe, compact record, stop rules | 144-03 | COVERED | Tasks 1-2. |
| CONTEXT | D-18–D-20 | Failure-driven compatibility fixes and adopter contract | 144-02, 144-03 | COVERED | Conditional source edit and re-plan stops. |
| CONTEXT | D-21–D-22 | Quality pillars and domain language | 144-01, 144-02, 144-03 | COVERED | Actions and evidence vocabulary. |

Exclusions, not gaps: Phase 145 legacy framework/data remediation; Phase 146 Ops/Swoosh remediation and runtime proof; Phase 147 ecommerce/all-graph closure; broader modernization and permanent automation.
</source_coverage_audit>

<verification>
First require all deterministic checked-lock/root gates on the reviewed candidate. Only then run the detached fresh resolution and unsuppressed audit. Verify the recorded SHA/bounds/paths/exits, evidence classifications, disposable cleanup, primary-lock identity, and tracked workspace cleanliness.

Any failed or unverified high-severity mitigation blocks completion and Phase 145, requiring stop/re-plan.
</verification>

<success_criteria>
- SEC-01 has deterministic checked-lock proof and current-registry exact-candidate proof for all four root advisory packages.
- COMPAT-02's root Meilisearch behavior is green under the complete release-train bundle; supplemental live smoke is separately classified; Swoosh remains Phase 146.
- D-10 through D-17 and D-19 through D-22 are represented by concrete commands, evidence fields, and blocking stop conditions.
- Phase 145 cannot begin unless every required gate, bound, and audit assertion passed.
- Any failed or unverified high-severity mitigation blocks completion and Phase 145, requiring stop/re-plan.
</success_criteria>

<output>
Create `.planning/phases/144-root-http-client-dependency-remediation/144-03-SUMMARY.md` when done, containing the compact D-15 evidence record.
</output>
