---
phase: 146-scrypathops-web-client-remediation
plan: 02
type: execute
wave: 2
depends_on: [146-01]
files_modified:
  - .planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md
autonomous: true
requirements: [SEC-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Per D-10 and D-12, root mix verify.opsui is the binding standalone Phoenix, LiveView, endpoint, Repo/migration, operator-contract, accessibility, and retained UI-invariant proof; ecommerce and browser/protocol proof are absent."
    - "Per D-11, fixed selection plus the later unsuppressed audit owns upstream advisory proof; no copied vulnerability exploit tests or permanent browser/protocol lane are added."
    - "Per D-13/D-16, any required gate or Swoosh failure stops handoff; a source fix is allowed only after a demonstrated failure and must remain minimal, internal, behavior-preserving, and focused-test-covered."
    - "Per D-14, deterministic evidence follows the exact order: manifest/lock diff explanation; Ops checked lock and warnings-as-errors compile; root mix verify.opsui; then the command equivalents for main-ci, repo-hygiene, release-truth, and phase99-trust."
    - "Per D-17/D-20/D-21, evidence is compact, uses familiar Mix/Phoenix interfaces, and adds no policy language, permanent gate, or capability."
    - "The existing operator empty, loading, error, populated, partial-data, overflow, zero/one/many, and long-text states all remain unchanged; the implementation diff contains no UI-owned file and mix verify.opsui passes (D-10/D-22)."
  artifacts:
    - path: ".planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md"
      provides: "Compact deterministic gate evidence tied to the exact implementation SHA"
  key_links:
    - from: "146-01 implementation commit"
      to: "mix verify.opsui"
      via: "recorded exact SHA and unchanged tracked implementation state"
    - from: "CONTRIBUTING.md named jobs"
      to: "root commands"
      via: "main-ci, repo-hygiene, release-truth, and phase99-trust command equivalents"
  flagged_assumptions:
    - statement: "SEC-03 remains an unclassified/unresolved edge-probe assumption; this plan treats the complete standalone Ops and named root gates as sufficient application behavior coverage without ecommerce or browser proof."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "Must not silently replace a failed required deterministic gate with advisory, ecommerce, browser, protocol, or network evidence."
      status: unverified
      flagged: true
    - statement: "Must not change any HEEx/template, LiveView render, route, layout, component API, CSS, JavaScript, theme, icon, copy, schema, migration, or provider configuration to make the upgraded graph pass."
      status: unverified
      flagged: true
---

<objective>
Prove the exact atomic ScrypathOps candidate through the binding standalone Ops application gate and every named required root release-train gate.

Purpose: Demonstrate fixed dependencies preserve Phoenix, LiveView, Repo, migration, Swoosh, operator-contract, and repository trust behavior before network-dependent fresh evidence (SEC-03; D-10 through D-14).
Output: Compact deterministic evidence tied to the exact Plan 01 implementation SHA.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@scrypath_ops/AGENTS.md
@CONTRIBUTING.md
@.github/workflows/ci.yml
@.planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
@.planning/phases/146-scrypathops-web-client-remediation/146-VALIDATION.md
@.planning/phases/146-scrypathops-web-client-remediation/146-UI-SPEC.md
@.planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md
@lib/mix/tasks/verify.opsui.ex
</context>

## Artifacts this phase produces

- `.planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md` with exact candidate SHA, UTC window, Elixir/OTP/Mix versions, changed lock rows and causal explanations, ordered deterministic commands, exit statuses, and UI-scope diff result.
- No new production symbol, source file, test, Mix task, CI flag, policy, route, UI element, or schema artifact.

<tasks>

<task type="auto">
  <name>Task 1: Prove the standalone ScrypathOps application on the reviewed graph</name>
  <precondition>The Plan 146-01 summary records exactly one implementation commit whose diff is limited to scrypath_ops/mix.exs, scrypath_ops/mix.lock, scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs, plus at most one explicitly justified D-13 internal compatibility fix.</precondition>
  <files>.planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md</files>
  <read_first>
    AGENTS.md
    scrypath_ops/AGENTS.md
    CONTRIBUTING.md
    .planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
    .planning/phases/146-scrypathops-web-client-remediation/146-UI-SPEC.md
    .planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md
    scrypath_ops/mix.exs
    scrypath_ops/mix.lock
    lib/mix/tasks/verify.opsui.ex
  </read_first>
  <action>At the exact Plan 01 implementation SHA, record the manifest and lock diff and classify every moved row as a direct target or named causal transitive; stop on unexplained churn (D-03/D-04/D-14/D-16). Record a pre-gate hash of the primary Ops lock. From scrypath_ops run `mix deps.get --check-locked` followed by `mix compile --warnings-as-errors`, then from the root run `mix verify.opsui`. Confirm the post-gate lock hash equals the pre-gate hash and inspect the implementation diff for the approved no-UI/no-route/no-schema/no-provider boundary. Write compact commands/exits and classification to 146-02-SUMMARY.md, not raw logs. Do not edit implementation to bypass a failure; D-13 requires stopping and re-planning unless a smallest internal focused fix is demonstrably necessary.</action>
  <verify>
    <automated>set -e; before=$(shasum -a 256 scrypath_ops/mix.lock | awk '{print $1}'); (cd scrypath_ops && mix deps.get --check-locked && mix compile --warnings-as-errors); mix verify.opsui; after=$(shasum -a 256 scrypath_ops/mix.lock | awk '{print $1}'); test "$before" = "$after"</automated>
  </verify>
  <acceptance_criteria>
    - "The causal diff is explained row-by-row and the primary Ops lock stays byte-identical while deterministic gates run (D-04/D-14)."
    - "Checked-lock, warnings-as-errors compile, and root mix verify.opsui all exit zero on the recorded candidate SHA (D-10/D-14)."
    - "The diff has no UI/template/CSS/JS/route/layout/component-API/copy/theme/schema/migration/provider/ecommerce changes, preserving all eight approved UI considerations (D-12/D-22)."
  </acceptance_criteria>
  <done>The reviewed candidate passes the binding standalone Ops graph and application gate, with compact deterministic evidence and unchanged primary lock.</done>
</task>

<task type="auto">
  <name>Task 2: Run every named root release-train blocker</name>
  <precondition>Task 1 recorded zero exits for checked-lock, Ops compile, and root mix verify.opsui on the exact candidate SHA.</precondition>
  <files>.planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md</files>
  <read_first>
    AGENTS.md
    CONTRIBUTING.md
    .github/workflows/ci.yml
    .planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
    .planning/phases/146-scrypathops-web-client-remediation/146-VALIDATION.md
    .planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md
  </read_first>
  <action>Run the CONTRIBUTING command equivalents in named-gate order and against the unchanged implementation candidate: main-ci = `mix compile --warnings-as-errors` then `mix test --exclude integration --exclude docs_contract --include requires_clean_workspace`; repo-hygiene = `mix verify --exclude integration`; release-truth = `mix verify.phase11`; phase99-trust = `mix verify.phase99` (D-14). Record command, exact exit, and deterministic classification in the summary. Stop immediately on the first non-zero result and do not proceed to Plan 03 or classify any advisory evidence as a substitute (D-16). Confirm git diff remains limited to expected planning evidence and pre-existing user changes; do not stage or alter unrelated dirty state (D-17/D-18).</action>
  <verify>
    <automated>set -e; mix compile --warnings-as-errors; mix test --exclude integration --exclude docs_contract --include requires_clean_workspace; mix verify --exclude integration; mix verify.phase11; mix verify.phase99</automated>
  </verify>
  <acceptance_criteria>
    - "All four named required root gates exit zero after mix verify.opsui and are recorded separately by job name and command (D-14)."
    - "No advisory/browser/ecommerce result substitutes for a required gate, and any failure blocks Plan 03 (D-12/D-16)."
    - "The exact Plan 01 implementation commit remains the only production remediation commit; this task changes only compact planning evidence (D-17/D-18)."
  </acceptance_criteria>
  <done>The exact candidate is green through every required deterministic Ops and root gate and is eligible for detached fresh-resolution evidence.</done>
</task>

</tasks>

<threat_model>
## Assets and Trust Boundaries

| Boundary | Protected asset | Exposure |
|---|---|---|
| Implementation commit → local dependency/build state | Exact candidate behavior | Stale build/deps state could mask incompatibility. |
| ScrypathOps → Postgres test service | Repo/migration correctness | Service/setup failures can be misreported as product success. |
| Root gate orchestration → summary | Release-train truth | Skipped/reordered/non-zero commands can be omitted from evidence. |
| Git diff → preservation contract | Existing UI/routes/schema and user work | A passing suite could coexist with unauthorized scope changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-146-05 | Repudiation | Gate evidence | high | mitigate | Record exact SHA, command, exit, and deterministic classification; stop at first failure. |
| T-146-06 | Tampering | Primary Ops lock | high | mitigate | Hash before/after all Ops gates and require equality. |
| T-146-07 | DoS | Postgres-backed Ops gate | medium | mitigate | Treat service/setup failure as blocking and retain the canonical mix verify.opsui boundary. |
| T-146-08 | Elevation of privilege / Tampering | Scope diff | high | mitigate | Explicit no-UI/no-route/no-schema/no-provider diff review before recording success. |
| T-146-SC | Tampering | Resolved packages | high | mitigate | Checked lock and exact candidate SHA here; detached lockless resolution plus unsuppressed audit in Plan 03. |
</threat_model>

<verification>
- Ordered deterministic command sequence matches CONTRIBUTING job equivalents exactly.
- Summary contains the exact candidate SHA and per-command exit without raw terminal logs.
- Primary lock hash is unchanged and the no-UI/no-schema/no-route preservation diff is explicit.
</verification>

<success_criteria>
- SEC-03 is green through the standalone Ops and all named root required gates.
- All eight approved UI preservation considerations remain unchanged and protected by diff review plus mix verify.opsui.
- No network-dependent evidence is mislabeled as a required deterministic pass.
</success_criteria>

<output>
Create `.planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md` when done.
</output>
