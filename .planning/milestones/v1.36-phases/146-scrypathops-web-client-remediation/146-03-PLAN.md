---
phase: 146-scrypathops-web-client-remediation
plan: 03
type: execute
wave: 3
depends_on: [146-02]
files_modified:
  - .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md
autonomous: true
requirements: [SEC-03, EVID-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Per D-02/EVID-03, live Hex and EEF CNA are re-queried immediately before fresh proof and must still establish stable unretired Postgrex 0.22.4 with < 0.22.4 affected; any loss or contradiction blocks completion."
    - "Per amended D-05, a detached lockless resolution at the exact candidate selects Phoenix >= 1.8.9 < 1.9.0, LiveView >= 1.1.33 < 1.2.0, Bandit >= 1.12.1 < 1.13.0, Swoosh >= 1.26.3 < 1.27.0, Postgrex >= 0.22.4 < 0.23.0, Req >= 0.6.1 < 0.7.0, Plug >= 1.19.5 < 2.0.0, Mint >= 1.9.3, and hpax >= 1.0.4; Plug remains transitive per D-03, and the exact Plug version selected by that fresh lock must appear in the live official Hex package response with retired null."
    - "Per D-11/D-15, the detached fresh graph passes the fail-closed exact-release Plug predicate before the separate unsuppressed ScrypathOps mix hex.audit advisory control; only the disposable lock is removed, and MIX_DEPS_PATH plus MIX_BUILD_PATH are isolated beneath the disposable worktree."
    - "Per D-15/D-16, a fail-closed EXIT trap is installed immediately after the mode-restricted state directory is created and validated, before locator or receipt setup; before any path is cleanup-authorized, both the probe parent and exact worktree child must be existing owned non-symlink directories as well as exact-prefix/exact-child matches; finalization preserves every prior non-zero proof status, checks every exact-path cleanup operation and post-cleanup preservation assertion, and can record PASS only after all cleanup succeeds."
    - "Per D-16, any registry/feed outage, audit/range/gate failure, dirty primary lock, unexplained churn, Postgrex-evidence loss, or Swoosh failure is blocking and never recorded as passed."
    - "Per D-17, evidence records candidate SHA, UTC window, tool versions, commands/exits, exact target versions, audit result, causal lock rows, evidence classification, and cleanup without raw logs, locks, trees, snapshots, credentials, or service artifacts."
    - "Per D-18/D-19, the branch contains exactly one isolated ScrypathOps implementation remediation commit and no ecommerce, public API, route/UI, mail semantic, schema, provider, permanent CI/security, or modernization change."
    - "Per D-20 through D-22, maintainers receive familiar Mix/Hex operations and a truthful Phase 147 handoff with all eight approved UI preservation states unchanged."
  artifacts:
    - path: ".planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md"
      provides: "Compact exact-SHA fresh-resolution, audit, cleanup, and handoff evidence"
  key_links:
    - from: "Plan 01 implementation SHA"
      to: "detached worktree HEAD"
      via: "git worktree add --detach at the exact recorded commit"
    - from: "disposable scrypath_ops/mix.lock"
      to: "D-05 version matrix"
      via: "Mix.Dep.Lock.read plus Version.match?/2 assertions after lockless resolution"
    - from: "fresh lock-selected Plug version"
      to: "https://hex.pm/api/packages/plug"
      via: "jq --arg exact-version predicate requiring a matching releases entry with retired null before audit"
    - from: "fresh resolved graph"
      to: "unsuppressed advisory status"
      via: "mix hex.audit with no ignore flags or ignore configuration"
    - from: "disposable proof"
      to: "primary workspace"
      via: "lock-hash equality, tracked-state preservation, and validated worktree cleanup"
  flagged_assumptions:
    - statement: "EVID-03 remains an unclassified/unresolved edge-probe assumption; this plan assumes two live authorities, exact-version assertions, and unsuppressed audit are sufficient to establish a truthful Postgrex and graph remediation handoff."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "Must not report unavailable, contradictory, suppressed, or non-zero external evidence as passing, and must not substitute any prerelease, invented version, or affected fallback."
      status: unverified
      flagged: true
    - statement: "Must not retain or commit raw logs, disposable locks/worktrees, dependency trees, advisory snapshots, provider credentials, generated service artifacts, or isolated build/dependency directories."
      status: unverified
      flagged: true
---

<objective>
Seal the exact ScrypathOps candidate with detached lockless version proof, live Postgrex authority evidence, an unsuppressed advisory audit, and validated cleanup.

Purpose: Give maintainers reproducible and honest evidence that the manifest resolves safely without relying on the reviewed lock, while protecting the primary workspace and the single implementation-commit boundary (SEC-03, EVID-03; D-02, D-05, D-15 through D-22).
Output: Compact fresh-resolution/audit evidence and a green-or-blocked Phase 147 handoff.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@scrypath_ops/AGENTS.md
@CONTRIBUTING.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
@.planning/phases/146-scrypathops-web-client-remediation/146-RESEARCH.md
@.planning/phases/146-scrypathops-web-client-remediation/146-VALIDATION.md
@.planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md
@.planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md
</context>

## Artifacts this phase produces

- `.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md` with exact candidate SHA, UTC evidence window, Elixir/OTP/Mix/Hex versions, live Postgrex source predicates, D-05 selected-version matrix, compact lock-selected Plug version plus exact-release live predicate pass, unsuppressed audit exit, causal lock summary, deterministic/network classification, primary-lock hash equality, disposable cleanup, and handoff status.
- No retained lock, worktree, raw log, dependency tree, advisory snapshot, credential, service artifact, build directory, production symbol, route, UI flag, schema file, CI task, or product capability.

<tasks>

<task type="auto">
  <name>Task 1: Resolve and audit the exact candidate in a detached isolated worktree</name>
  <precondition>Plan 146-02 records zero exits for mix verify.opsui and all four named root gates on the exact Plan 01 implementation SHA; the primary scrypath_ops/mix.lock hash and tracked status are recorded before this task.</precondition>
  <files>.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md</files>
  <read_first>
    AGENTS.md
    scrypath_ops/AGENTS.md
    CONTRIBUTING.md
    .planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
    .planning/phases/146-scrypathops-web-client-remediation/146-RESEARCH.md
    .planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md
    .planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md
    scrypath_ops/mix.exs
    scrypath_ops/mix.lock
  </read_first>
  <action>Initialize `probe_parent`, `worktree_path`, and locator-created state to empty, then create a mode-0700 state directory with `mktemp -d "${TMPDIR:-/tmp}/scrypath-phase146-state.XXXXXX"` and reject symlink/non-owned/unexpected-prefix paths. Immediately after that validation—and before checking, creating, or writing the locator or any receipt—install a minimal fail-closed EXIT trap under `set -Eeo pipefail`. The bootstrap trap must capture the incoming status, disable errexit only while finalizing, preserve any prior non-zero status, append compact BLOCKED setup/cleanup status without paths or secrets, check removal of the exact validated state directory, and exit non-zero if either setup or cleanup/reporting fails; it must never emit PASS. Then require that no prior mode-0600 locator exists at `git rev-parse --git-path gsd-phase146-03.locator`, create that exact non-symlink owned locator, and persist separate single-value receipt files for the exact implementation SHA, Phase 145 handoff SHA, primary Ops lock SHA-256, task-created probe parent/worktree paths, the sorted approved candidate path list from the Plan 01 commit, and a NUL-delimited `git status --porcelain=v1 -z` baseline that excludes only this task-owned 146-03-SUMMARY.md. Do not source the receipt as shell code. Before accepting the candidate path list, prove it contains the manifest, lock, and focused test and no path other than those three plus an explicitly recorded D-13 fix.

After locator and receipt setup succeeds, replace the bootstrap trap with the full Task 1 finalizer. It must capture the original proof status before disabling errexit; never replace a prior non-zero status with zero; and validate every non-empty probe path against the mktemp prefix, ownership, exact `worktree` child relationship, existing-directory status, and non-symlink status before marking it cleanup-authorized. Task 1 does not remove or prune the successfully proven disposable worktree: on success it records probe exit zero and retains the validated worktree/probe parent plus the secure locator/state receipt for Task 2's final cleanup. On failure, the finalizer may attempt exact registered-worktree removal, `git worktree prune`, and exact probe-parent removal (including isolated deps/build state) only if the same validation gate has first proved that both `$probe_parent` and `$worktree_path` are existing directories, are not symlinks, are owned by the executing user, match the mktemp prefix, and have the exact `worktree` child relationship. If any one of those checks fails, append only `exact-path-validation` to the compact cleanup-failure list, force non-zero/BLOCKED, leave the invalid target untouched, and do not run worktree removal, prune, or recursive probe-parent deletion. In every path, check and capture primary-lock hash equality, recorded NUL-baseline equality, probe-exit receipt write, and any locator/state removal required on failure. Each failed cleanup/preservation/reporting operation must add only a compact stage name to a cleanup-failure list, force the final status non-zero, and produce `Final handoff: BLOCKED`; no cleanup failure may be hidden by `set +e`. On Task 1 failure, check removal of locator and state receipt and return non-zero after the BLOCKED record.

With the full trap active, record the exact implementation SHA, UTC time, and tool versions; re-query both D-02 authorities with the Plan 01 predicates; create the worktree as the `worktree` child of a separately validated `mktemp -d "${TMPDIR:-/tmp}/scrypath-phase146-probe.XXXXXX"` parent; detach it at the exact SHA; set `MIX_DEPS_PATH` and `MIX_BUILD_PATH` beneath that worktree; remove only its Ops lock; run fresh `mix deps.get`; then, from the detached `scrypath_ops` directory, run the lock-only matrix as `elixir -e 'Mix.start(); lock = Mix.Dep.Lock.read(); [{"Phoenix", :phoenix, ">= 1.8.9 and < 1.9.0"}, {"LiveView", :phoenix_live_view, ">= 1.1.33 and < 1.2.0"}, {"Bandit", :bandit, ">= 1.12.1 and < 1.13.0"}, {"Swoosh", :swoosh, ">= 1.26.3 and < 1.27.0"}, {"Postgrex", :postgrex, ">= 0.22.4 and < 0.23.0"}, {"Req", :req, ">= 0.6.1 and < 0.7.0"}, {"Plug", :plug, ">= 1.19.5 and < 2.0.0"}, {"Mint", :mint, ">= 1.9.3"}, {"hpax", :hpax, ">= 1.0.4"}] |> Enum.each(fn {label, dep, requirement} -> version = lock |> Map.fetch!(dep) |> elem(2); Version.match?(version, requirement) || raise "#{label} #{version} violates #{requirement}"; IO.puts("#{label} #{version} satisfies #{requirement}") end)'`. Use this `elixir -e` lock reader rather than `mix run`, because `mix run` initializes Phoenix dependency load paths and previously failed before reading the lock. This amended D-05 check preserves all eight original non-Plug ranges, accepts only compatible Plug 1.x selected transitively per D-03, and does not alter the reviewed primary Plug `1.19.5` lock. After the matrix passes, extract the exact selected Plug version from that same fresh lock with `selected_plug=$(elixir -e 'Mix.start(); version = Mix.Dep.Lock.read() |> Map.fetch!(:plug) |> elem(2); Version.match?(version, ">= 1.19.5 and < 2.0.0") || raise "selected Plug violates D-05"; IO.write(version)')`; require the value is non-empty, then live-query only the official endpoint with `curl --fail --silent --show-error --location --proto '=https' --tlsv1.2 https://hex.pm/api/packages/plug | jq -e --arg selected_plug "$selected_plug" '(.releases | type == "array") and any(.releases[]; (.version == $selected_plug and .retired == null))' >/dev/null`. Passing the lock-derived value through `jq --arg` is mandatory; do not interpolate it into jq source. Request failure, JSON parse failure, a missing/non-array releases field, no exact version match, or a non-null retired value must exit non-zero through the existing BLOCKED path and unconditional cleanup. Only after this exact-release predicate passes, run the separate unsuppressed `mix hex.audit` advisory control (D-11/D-15/D-16). For Plug, record exactly one compact evidence line shaped `Plug selected: <exact version>; Hex exact-release predicate: PASS`; do not retain the response body or release list. Otherwise record only compact versions, commands/exits, source status, advisory audit result, cleanup result, and network classification in 146-03-SUMMARY.md—never raw output, receipt values, disposable paths/locks/trees, credentials, or service artifacts (D-17).</action>
  <verify>
    <automated>set -eu; locator=$(git rev-parse --git-path gsd-phase146-03.locator); test -f "$locator"; test ! -L "$locator"; test -O "$locator"; state_dir=$(sed -n '1p' "$locator"); case "$state_dir" in "${TMPDIR:-/tmp}"/scrypath-phase146-state.*) ;; *) exit 1 ;; esac; test -d "$state_dir"; test ! -L "$state_dir"; test -O "$state_dir"; test "$(cat "$state_dir/probe-exit")" = 0; worktree_path=$(cat "$state_dir/worktree-path"); probe_parent=$(cat "$state_dir/probe-parent"); case "$probe_parent" in "${TMPDIR:-/tmp}"/scrypath-phase146-probe.*) ;; *) exit 1 ;; esac; test "$worktree_path" = "$probe_parent/worktree"; test -d "$probe_parent"; test ! -L "$probe_parent"; test -O "$probe_parent"; test -d "$worktree_path"; test ! -L "$worktree_path"; test -O "$worktree_path"; test -s .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; rg -q 'Phoenix.*>= 1\.8\.9.*< 1\.9\.0' .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; rg -q 'Postgrex.*>= 0\.22\.4.*< 0\.23\.0' .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; rg -q '^Plug selected: [^;[:space:]]+; Hex exact-release predicate: PASS$' .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; rg -q 'mix hex\.audit.*0' .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; ! rg -q '^Final handoff: PASS' .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - "Fresh proof runs in a detached worktree at exactly the recorded implementation SHA with both build and dependency paths isolated; only its disposable Ops lock is removed (D-15)."
    - "All nine amended D-05 ranges pass against actual fresh selections—including transitive Plug >= 1.19.5 and < 2.0.0 per D-03—and the official Hex Plug response contains that exact fresh-selected version with retired null before unsuppressed mix hex.audit runs and exits zero."
    - "Live Postgrex stable/unretired and CNA predicates pass again in the same evidence window; any outage or contradiction blocks EVID-03 (D-02/D-16)."
    - "The summary contains compact evidence only and contains no raw log, disposable lock/tree, credential, snapshot, or generated service artifact (D-17)."
  </acceptance_criteria>
  <done>The exact candidate freshly resolves within every approved range, its exact lock-selected Plug release is live and unretired in the official Hex response, and it passes the separate unsuppressed advisory audit under current dual-source Postgrex evidence, or the phase is explicitly blocked without fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Validate cleanup, commit topology, and Phase 147 handoff</name>
  <precondition>Task 1 exited zero after recording successful live dual-source predicates, all nine fresh version assertions, the exact lock-selected Plug release's official Hex retired-null predicate, and a zero unsuppressed audit exit for the exact implementation SHA; its secure locator/state receipt and its validated owned non-symlink probe parent plus exact worktree child remain for Task 2 cleanup.</precondition>
  <files>.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md</files>
  <read_first>
    AGENTS.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
    .planning/phases/146-scrypathops-web-client-remediation/146-UI-SPEC.md
    .planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md
    .planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md
    .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md
  </read_first>
  <action>Read, never source, the mode-0600 locator and separate receipt files left by Task 1; validate the locator/state ownership, non-symlink status, and mktemp prefix before trusting any value. Install failure handling before assertions: capture status, disable errexit only inside finalization, safely act only on validated exact paths, and append compact BLOCKED proof without raw paths or replacing a prior non-zero exit. Before setting `paths_valid=1`, require both `$probe_parent` and `$worktree_path` to pass `test -d`, `test ! -L`, and `test -O`, in addition to the exact mktemp prefix and exact `$probe_parent/worktree` child checks. Treat that as one fail-closed authorization gate completed before cleanup begins, and rerun the directory, non-symlink, ownership, and exact-child checks inside the EXIT finalizer immediately before its cleanup sequence. If either gate check fails, record only compact `exact-path-validation`, force nonzero/BLOCKED, leave the invalid target untouched, and do not execute `git worktree remove`, `git worktree prune`, or `rm -rf -- "$probe_parent"`. With the gate passed, assert the exact recorded worktree is registered and exists at the recorded path before cleanup. Re-hash the primary Ops lock and require equality with the recorded pre-probe hash. Recreate the same NUL-delimited primary status view excluding only 146-03-SUMMARY.md and require byte equality with the recorded dirty baseline, so pre-existing user dirt is preserved exactly. Resolve the Phase 145 handoff and candidate receipt SHAs, require the former is an ancestor, enumerate commits in that range touching `scrypath_ops/mix.exs`, `scrypath_ops/mix.lock`, or the focused Swoosh contract, and require exactly one unique commit equal to the recorded implementation SHA; separately inspect that commit's paths for only the three owned files plus any Plan 01-recorded D-13 fix (D-18). Record successful proof stages, equality assertions, commit topology, and no-UI/no-schema/no-route/no-provider scope, but do not write `Final handoff: PASS` in this action. The automated finalizer below owns the only PASS transition: it may start worktree removal/prune and recursive probe-parent deletion only after the authorization gate passed, must check every removal, and must then rerun post-cleanup absence, lock-hash, dirty-baseline-hash, and topology/UI/scope preservation assertions before appending PASS. Any proof, cleanup, reporting, or post-cleanup assertion failure forces BLOCKED and non-zero. Do not record raw receipt values/paths, close the four-graph todo, or claim ecommerce/all-graph evidence (D-12/D-17/D-22).</action>
  <verify>
    <automated>set -Eeu -o pipefail; summary=.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; locator=$(git rev-parse --git-path gsd-phase146-03.locator); state_dir=; worktree_path=; probe_parent=; paths_valid=0; expected_lock=; expected_status_hash=; base_sha=; candidate_sha=; approved_paths_hash=; finish() { proof_rc=$?; trap - EXIT INT TERM; set +e; final_rc=$proof_rc; cleanup_failures=; note_cleanup_failure() { final_rc=1; cleanup_failures="${cleanup_failures}${cleanup_failures:+,}$1"; }; if test "$paths_valid" = 1 && test "$worktree_path" = "$probe_parent/worktree" && test -d "$probe_parent" && test ! -L "$probe_parent" && test -O "$probe_parent" && test -d "$worktree_path" && test ! -L "$worktree_path" && test -O "$worktree_path"; then registered=$(git worktree list --porcelain); check_rc=$?; if test "$check_rc" -ne 0; then note_cleanup_failure worktree-list; elif printf '%s\n' "$registered" | awk '/^worktree / {sub(/^worktree /, ""); print}' | grep -Fxq -- "$worktree_path"; then git worktree remove --force -- "$worktree_path"; test "$?" -eq 0 || note_cleanup_failure worktree-remove; else note_cleanup_failure worktree-registration; fi; git worktree prune; test "$?" -eq 0 || note_cleanup_failure worktree-prune; if test -e "$probe_parent"; then rm -rf -- "$probe_parent"; test "$?" -eq 0 || note_cleanup_failure probe-state-remove; fi; else paths_valid=0; note_cleanup_failure exact-path-validation; fi; if test -n "$state_dir" && test -d "$state_dir" && test ! -L "$state_dir" && test -O "$state_dir"; then rm -rf -- "$state_dir"; test "$?" -eq 0 || note_cleanup_failure receipt-remove; else note_cleanup_failure receipt-validation; fi; if test -e "$locator" || test -L "$locator"; then if test ! -L "$locator" && test -O "$locator"; then rm -f -- "$locator"; test "$?" -eq 0 || note_cleanup_failure locator-remove; else note_cleanup_failure locator-validation; fi; fi; if test "$paths_valid" = 1; then test ! -e "$worktree_path" && test ! -L "$worktree_path" || note_cleanup_failure worktree-absence; test ! -e "$probe_parent" && test ! -L "$probe_parent" || note_cleanup_failure probe-state-absence; fi; test ! -e "$state_dir" && test ! -L "$state_dir" || note_cleanup_failure receipt-absence; test ! -e "$locator" && test ! -L "$locator" || note_cleanup_failure locator-absence; current_lock=$(shasum -a 256 scrypath_ops/mix.lock | awk '{print $1}'); test "$?" -eq 0 && test -n "$expected_lock" && test "$current_lock" = "$expected_lock" || note_cleanup_failure primary-lock-preservation; current_status_hash=$(git status --porcelain=v1 -z -- . ':(exclude).planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md' | shasum -a 256 | awk '{print $1}'); test "$?" -eq 0 && test -n "$expected_status_hash" && test "$current_status_hash" = "$expected_status_hash" || note_cleanup_failure dirty-baseline-preservation; git merge-base --is-ancestor "$base_sha" "$candidate_sha"; test "$?" -eq 0 || note_cleanup_failure topology-preservation; current_impl_commits=$(git rev-list "$base_sha..$candidate_sha" -- scrypath_ops/mix.exs scrypath_ops/mix.lock scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs); check_rc=$?; test "$check_rc" -eq 0 && test "$(printf '%s\n' "$current_impl_commits" | sed '/^$/d' | wc -l | tr -d ' ')" = 1 && test "$current_impl_commits" = "$candidate_sha" || note_cleanup_failure implementation-topology-preservation; current_paths_hash=$(git diff-tree --no-commit-id --name-only -r "$candidate_sha" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}'); test "$?" -eq 0 && test -n "$approved_paths_hash" && test "$current_paths_hash" = "$approved_paths_hash" || note_cleanup_failure scope-preservation; if test "$final_rc" -eq 0; then printf '\nFinal handoff: PASS\n' >> "$summary"; report_rc=$?; if test "$report_rc" -ne 0; then final_rc=1; fi; else printf '\nFinal handoff: BLOCKED — executable proof/cleanup verification failed (proof exit %s; cleanup: %s).\n' "$proof_rc" "${cleanup_failures:-none}" >> "$summary"; report_rc=$?; test "$report_rc" -eq 0 || final_rc=1; fi; exit "$final_rc"; }; trap finish EXIT; trap 'exit 130' INT TERM; test -f "$locator"; test ! -L "$locator"; test -O "$locator"; state_dir=$(sed -n '1p' "$locator"); case "$state_dir" in "${TMPDIR:-/tmp}"/scrypath-phase146-state.*) ;; *) exit 1 ;; esac; test -d "$state_dir"; test ! -L "$state_dir"; test -O "$state_dir"; worktree_path=$(cat "$state_dir/worktree-path"); probe_parent=$(cat "$state_dir/probe-parent"); case "$probe_parent" in "${TMPDIR:-/tmp}"/scrypath-phase146-probe.*) ;; *) exit 1 ;; esac; test "$worktree_path" = "$probe_parent/worktree"; test -d "$probe_parent"; test ! -L "$probe_parent"; test -O "$probe_parent"; test -d "$worktree_path"; test ! -L "$worktree_path"; test -O "$worktree_path"; paths_valid=1; expected_lock=$(cat "$state_dir/primary-lock.before"); expected_status_hash=$(shasum -a 256 "$state_dir/primary-status.before" | awk '{print $1}'); base_sha=$(cat "$state_dir/phase145-sha"); candidate_sha=$(cat "$state_dir/candidate-sha"); approved_paths_hash=$(shasum -a 256 "$state_dir/candidate-paths.approved" | awk '{print $1}'); registered_before=$(git worktree list --porcelain); printf '%s\n' "$registered_before" | awk '/^worktree / {sub(/^worktree /, ""); print}' | grep -Fxq -- "$worktree_path"; test "$(shasum -a 256 scrypath_ops/mix.lock | awk '{print $1}')" = "$expected_lock"; test "$(git status --porcelain=v1 -z -- . ':(exclude).planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md' | shasum -a 256 | awk '{print $1}')" = "$expected_status_hash"; git merge-base --is-ancestor "$base_sha" "$candidate_sha"; impl_commits=$(git rev-list "$base_sha..$candidate_sha" -- scrypath_ops/mix.exs scrypath_ops/mix.lock scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs); test "$(printf '%s\n' "$impl_commits" | sed '/^$/d' | wc -l | tr -d ' ')" = 1; test "$impl_commits" = "$candidate_sha"; test "$(git diff-tree --no-commit-id --name-only -r "$candidate_sha" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = "$approved_paths_hash"; if rg -q '^Final handoff: PASS$' "$summary"; then exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - "Before `paths_valid=1` and before any worktree removal/prune or recursive probe-parent deletion, both exact targets pass existing-directory, non-symlink, executing-user ownership, prefix, and exact-child validation; any failure records compact `exact-path-validation`, returns nonzero/BLOCKED, leaves the invalid target untouched, and runs none of those destructive commands (D-15/D-16)."
    - "After that gate passes, the disposable worktree and its isolated build/dependency state are gone, the primary Ops lock hash is unchanged, and pre-existing user changes remain untouched (D-15/D-17)."
    - "Exactly one implementation commit owns the ScrypathOps manifest, lock, focused Swoosh contract, and any explicitly justified D-13 fix; planning evidence commits are separate (D-18)."
    - "The finalizer preserves any prior non-zero proof status, checks exact worktree/probe-state/receipt/locator cleanup plus post-cleanup absence and preservation assertions, and records green handoff only after every D-16 deterministic, Swoosh, range, audit, authority, cleanliness, causal-diff, cleanup, and reporting condition passed."
    - "No ecommerce/four-graph closure, UI, public API, route, schema, provider, CI subsystem, policy language, or modernization claim is made (D-19 through D-22)."
  </acceptance_criteria>
  <done>Fresh evidence is compact and auditable; exact-path cleanup, dirty-baseline equality, primary-lock equality, and exactly-one implementation topology are executable assertions; and the summary's final outcome is PASS only when they all hold, otherwise BLOCKED with a non-zero plan result.</done>
</task>

</tasks>

<threat_model>
## Assets and Trust Boundaries

| Boundary | Protected asset | Exposure |
|---|---|---|
| Live Hex/CNA → evidence summary | Truthful fixed-release status | Outage, malformed response, contradiction, or stale assumption can be mislabeled as pass. |
| Exact Git SHA → detached worktree | Candidate identity | Wrong revision or copied dirty state invalidates proof. |
| Lockless Mix solver → external registry | Fresh dependency graph | Resolver can select out-of-range or newly vulnerable packages. |
| Disposable worktree → primary workspace | Reviewed lock and user changes | Unsafe cleanup or shared build paths can mutate/delete primary state. |
| Command output → committed summary | Credential/log safety | Raw logs or environment data can disclose secrets or create unverifiable noise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-146-09 | Spoofing / Repudiation | Candidate SHA | high | mitigate | Detach at the recorded Plan 01 SHA and record HEAD before resolution. |
| T-146-10 | Tampering | Fresh dependency selection | high | mitigate | Remove only disposable lock, isolate paths, assert all nine D-05 ranges, require the lock-selected Plug version's exact official Hex release to have retired null, and then run the unsuppressed audit. |
| T-146-11 | Repudiation | External evidence | high | mitigate | Live Postgrex dual-source and exact Plug release predicates fail closed on request/parse/missing/retired errors; record UTC/source status, zero audit exit, and no unavailable-as-pass classification. |
| T-146-12 | Information disclosure | Evidence summary | medium | mitigate | Persist compact status/version evidence only; exclude raw logs, environment secrets, provider credentials, and service artifacts. |
| T-146-13 | DoS / Tampering | Cleanup | high | mitigate | Install a bootstrap EXIT trap immediately after state-directory validation; preserve prior proof status; authorize cleanup only after both exact probe directories pass existing-directory, non-symlink, executing-user ownership, prefix, and exact-child checks; on failure record `exact-path-validation`, leave the invalid target untouched, and skip worktree removal/prune plus recursive probe deletion; otherwise check exact worktree, isolated-state, receipt, and locator removals, rerun preservation assertions, and emit PASS only after all finalization succeeds. |
| T-146-SC | Tampering | Hex supply chain | high | mitigate | Existing official package identities only, exact bounds, fresh solver assertions, checked SHA, exact lock-selected Plug release confirmed live and unretired through the official Hex endpoint, and unsuppressed audit; no new install identity. |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Independently run ScrypathOps on a fixed-compatible web, LiveView, mailer, HTTP, database, and transitive graph | 01-03 | COVERED | Atomic candidate, full deterministic gates, fresh resolve/audit. |
| REQ | SEC-03 | Independently resolve recorded Ops advisories | 01-03 | COVERED | Exact bounds, causal lock, Swoosh contract, Ops/root gates, audit. |
| REQ | EVID-03 | Block Postgrex until live advisory and Hex confirm stable fixed release | 01,03 | COVERED | Dual-source checks before edit and before final fresh proof. |
| RESEARCH | — | Real Swoosh.ApiClient.Req via Req.Test | 01 | COVERED | Direct init/post request, precedence, tuple, and error contract. |
| RESEARCH | — | Binding deterministic gates and exact-SHA detached proof | 02-03 | COVERED | Ordered gates, isolation, version assertions, audit, cleanup. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05 | Bounds, ownership, causal lock, fresh range matrix | 01,03 | COVERED | Exact strings and nine assertions. |
| CONTEXT | D-06, D-07, D-08, D-09 | Production-shaped Swoosh/Req proof and config preservation | 01 | COVERED | Service-free direct real-client test. |
| CONTEXT | D-10, D-11, D-12, D-13 | Ops application proof and minimal-fix boundary | 01-02 | COVERED | mix verify.opsui, upstream-proof ownership, hard stop. |
| CONTEXT | D-14, D-15, D-16, D-17, D-18, D-19 | Gate order, detached evidence, stops, compact record, one commit | 01-03 | COVERED | Explicit ordered tasks and cleanup. |
| CONTEXT | D-20, D-21, D-22 | Maintainer experience, no new subsystem/capability/UI | 01-03 | COVERED | Preservation truths and prohibitions. |

<verification>
- Exact-SHA detached proof selects all nine approved ranges, confirms the exact lock-selected Plug version is present and unretired at the official Hex endpoint, and only then passes the separate unsuppressed advisory audit.
- Both live Postgrex authorities pass in the same evidence window; unavailable/conflicting data blocks.
- Cleanup preserves the primary lock and dirty-worktree baseline and removes all disposable state.
- Evidence distinguishes deterministic from network proof and contains no sensitive/raw artifacts.
</verification>

<success_criteria>
- SEC-03 fresh resolution and advisory evidence are green at the exact implementation SHA.
- EVID-03 is satisfied by live dual-source stable fixed-release evidence without substitutes.
- The phase hands off exactly one isolated ScrypathOps implementation commit and no ecommerce or product/UI/schema expansion.
</success_criteria>

<output>
Create `.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md` when done.
</output>
