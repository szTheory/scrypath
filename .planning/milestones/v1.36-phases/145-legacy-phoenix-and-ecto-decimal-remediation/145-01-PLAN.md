---
phase: 145-legacy-phoenix-and-ecto-decimal-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_meilisearch/mix.exs
  - examples/phoenix_meilisearch/mix.lock
  - examples/phoenix_meilisearch/test/scrypath_demo/ecto_compatibility_test.exs
  - examples/phoenix_meilisearch/test/scrypath_demo_web/endpoint_compatibility_test.exs
autonomous: true
requirements: [SEC-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Per D-01 through D-05, the legacy manifest directly bounds Phoenix ~> 1.8.9, Bandit ~> 1.12.1, Ecto SQL ~> 3.14.0, and Postgrex ~> 0.22.4; Ecto and Decimal remain transitive, and the reviewed lock contains only causal cohort movement plus Phase 144's inherited Req floor."
    - "Per D-07 through D-12, the real Postgres model accepts and rejects representative changesets, persists and queries Author/Post rows and associations with timestamps, and proves no Decimal-valued application contract or search synchronization behavior."
    - "Per D-13 through D-15, the real Phoenix endpoint handles representative JSON and malformed-cookie requests without a crash, and one supervised loopback Bandit HTTP/1 request returns the stable JSON error shape and emits [:bandit, :request, :stop]."
    - "Per D-20 through D-22, the implementation is one isolated legacy-graph commit with no public Scrypath API, route, socket, schema field, migration, seed, UI, CI lane, permanent Mix task, override, advisory suppression, or broad modernization."
  artifacts:
    - path: "examples/phoenix_meilisearch/mix.exs"
      provides: "Four direct fixed-compatible dependency bounds"
      contains: "{:phoenix, \"~> 1.8.9\"}"
    - path: "examples/phoenix_meilisearch/mix.lock"
      provides: "Reviewed deterministic legacy application resolution"
    - path: "examples/phoenix_meilisearch/test/scrypath_demo/ecto_compatibility_test.exs"
      provides: "ScrypathDemo.EctoCompatibilityTest Postgres-backed changeset and persistence contract"
      contains: "defmodule ScrypathDemo.EctoCompatibilityTest"
    - path: "examples/phoenix_meilisearch/test/scrypath_demo_web/endpoint_compatibility_test.exs"
      provides: "ScrypathDemoWeb.EndpointCompatibilityTest ConnCase and real Bandit boundary contract"
      contains: "defmodule ScrypathDemoWeb.EndpointCompatibilityTest"
  key_links:
    - from: "examples/phoenix_meilisearch/mix.exs"
      to: "examples/phoenix_meilisearch/mix.lock"
      via: "targeted Mix resolution of the D-01 direct cohort and D-03 transitive Ecto/Decimal ownership"
      pattern: "~> (1\\.8\\.9|1\\.12\\.1|3\\.14\\.0|0\\.22\\.4)"
    - from: "ecto_compatibility_test.exs"
      to: "ScrypathDemo.Repo, Author, Post, and existing migrations"
      via: "generated DataCase and SQL Sandbox ownership (D-07/D-12)"
      pattern: "use ScrypathDemo.DataCase"
    - from: "endpoint_compatibility_test.exs"
      to: "ScrypathDemoWeb.Endpoint and Bandit request telemetry"
      via: "ConnCase plus a supervised loopback port-0 HTTP/1 request (D-13/D-14)"
      pattern: "bandit.*request.*stop"
  flagged_assumptions:
    - "[UNRESOLVED boundary probe] SEC-02 version thresholds are exactly D-01/D-05's inclusive floors and exclusive minor ceilings. No other numeric threshold is inferred; the detached exact-candidate proof in Plan 02 must confirm every selected version before this assumption can close."
    - "[UNRESOLVED precision probe] D-09 establishes that the example has no Decimal-valued domain field, so no rounding, tie-breaking, overflow, or precision-loss behavior can be truthfully asserted. Decimal 3 proof is limited to transitive selection/audit while real application tests cover only existing persisted fields."
  prohibitions: []
---

<objective>
Land the production-quality end-to-end tracer for SEC-02 in one isolated legacy-graph implementation commit: bound and resolve the coordinated dependency cohort, then prove the actual Ecto/Postgres and Phoenix/Bandit paths on that exact checked lock.

Purpose: Maintainers need one understandable remediation batch that identifies whether resolution, persistence, request processing, or the real listener regressed without expanding Scrypath or the example (D-23 through D-27).
Output: Four bounded direct requirements, one causal lock refresh, and two focused compatibility test modules.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTRIBUTING.md
@examples/phoenix_meilisearch/AGENTS.md
@examples/phoenix_meilisearch/README.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-CONTEXT.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-RESEARCH.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-PATTERNS.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-VALIDATION.md
@.planning/phases/144-root-http-client-dependency-remediation/144-01-SUMMARY.md
@.planning/phases/144-root-http-client-dependency-remediation/144-03-SUMMARY.md

<interfaces>
`ScrypathDemo.DataCase` aliases `ScrypathDemo.Repo`, imports Ecto helpers, and calls `Ecto.Adapters.SQL.Sandbox.start_owner!/2` with `stop_owner/1` cleanup. `ScrypathDemoWeb.ConnCase` sets `@endpoint ScrypathDemoWeb.Endpoint`, imports `Phoenix.ConnTest`/`Plug.Conn`, and reuses the same Sandbox setup. `Author.changeset/2` casts and requires `:name`; `Post.changeset/2` casts `:title`, `:body`, `:status`, `:author_id`, and `:author_name`, requiring the first three. `ScrypathDemoWeb.Router` has an empty `/api` scope through the JSON `:api` pipeline. `ScrypathDemoWeb.Endpoint` terminates in that router after parsers and signed cookie session handling. `Bandit.PhoenixAdapter.server_info/2` is the preferred public bound-listener introspection seam when the selected version's supported startup shape makes it available; `[:bandit, :request, :stop]` supplies `duration`, `conn`, and `plug` evidence.
</interfaces>
</context>

## Artifacts this phase produces

- Dependency bounds: Phoenix `~> 1.8.9`, Bandit `~> 1.12.1`, Ecto SQL `~> 3.14.0`, Postgrex `~> 0.22.4` in `examples/phoenix_meilisearch/mix.exs`.
- Deterministic dependency artifact: the causally refreshed `examples/phoenix_meilisearch/mix.lock`, including transitive Ecto `>= 3.14.0 and < 3.15.0`, Decimal `>= 3.0.0`, Plug `>= 1.19.5 and < 1.20.0`, and the inherited Req/Mint/hpax floors.
- Test module `ScrypathDemo.EctoCompatibilityTest` with private `author_fixture/1` and `post_fixture/2` helpers in `examples/phoenix_meilisearch/test/scrypath_demo/ecto_compatibility_test.exs`.
- Test module `ScrypathDemoWeb.EndpointCompatibilityTest` with its private telemetry/listener setup helpers in `examples/phoenix_meilisearch/test/scrypath_demo_web/endpoint_compatibility_test.exs`.
- No new public symbol, production helper, application route/socket, schema field, migration, seed, UI, CI lane, dependency-policy layer, Mix task, or external package.

<assumption_delta_decision>
Primary noun: commit. Decision: no-change. The detector's “second” signal refers to the ordered graph-local implementation commit after Phase 144, not a second provider, external integration, fallback capability, or alternate product surface; D-19/D-23 already require explicit provenance for that single legacy batch.
</assumption_delta_decision>

<flagged_planner_assumptions>
- Boundary probe remains unresolved until Plan 02's detached lockless exact-candidate resolution proves every D-05 inclusive floor and exclusive minor ceiling. D-01 and D-05 are the complete version-boundary contract; a selected patch above the floor is valid only inside its stated minor line.
- Precision probe remains unresolved but delimited by D-09: there is no Decimal-valued schema field, migration, fixture value, or public numeric contract, so inventing a rounding test would be false evidence. Decimal 3 is proven only as the transitive Ecto/Postgrex cohort while the DataCase contract verifies the strings, association, and timestamps the example actually persists.
- Two-stage prohibition recall for SEC-02 produced only routine engineering and canonical security candidates (unrelated lock churn, unsafe dependency selection, false pass reporting, listener cleanup, secret-bearing raw logs). Those are dropped from bespoke prohibitions and are instead enforced by task acceptance criteria and the ASVS L1 threat register; no values/safety/ethics prohibition survives precision, so `must_haves.prohibitions` is intentionally empty with no silent drop.
</flagged_planner_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Land the atomic fixed-compatible legacy graph and exercise persistence through real HTTP</name>
  <precondition>Phase 144 summary records candidate `23b8b834e8648eb97e76561f933556a4b6b04f96` green with legacy Req 0.6.3, Mint 1.9.3, and hpax 1.0.4 checked into the example lock; a Postgres listener matching `examples/phoenix_meilisearch/config/test.exs` can be started through the documented local/CI service path.</precondition>
  <reversibility rating="costly">D-01's coordinated direct-minor bounds and generated lock move as one graph-local cohort; undo requires a coordinated four-bound/lock reversal and reopens the recorded advisories.</reversibility>
  <files>examples/phoenix_meilisearch/mix.exs, examples/phoenix_meilisearch/mix.lock, examples/phoenix_meilisearch/test/scrypath_demo/ecto_compatibility_test.exs, examples/phoenix_meilisearch/test/scrypath_demo_web/endpoint_compatibility_test.exs</files>
  <read_first>
    AGENTS.md
    CONTRIBUTING.md
    examples/phoenix_meilisearch/AGENTS.md
    examples/phoenix_meilisearch/README.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-CONTEXT.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-RESEARCH.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-PATTERNS.md
    .planning/phases/144-root-http-client-dependency-remediation/144-01-SUMMARY.md
    examples/phoenix_meilisearch/mix.exs
    examples/phoenix_meilisearch/mix.lock
    examples/phoenix_meilisearch/config/test.exs
    examples/phoenix_meilisearch/test/test_helper.exs
    examples/phoenix_meilisearch/test/support/data_case.ex
    examples/phoenix_meilisearch/test/support/conn_case.ex
    examples/phoenix_meilisearch/lib/scrypath_demo/blog.ex
    examples/phoenix_meilisearch/lib/scrypath_demo/blog/author.ex
    examples/phoenix_meilisearch/lib/scrypath_demo/blog/post.ex
    examples/phoenix_meilisearch/lib/scrypath_demo_web/endpoint.ex
    examples/phoenix_meilisearch/lib/scrypath_demo_web/router.ex
    examples/phoenix_meilisearch/test/scrypath_demo_web/controllers/error_json_test.exs
    examples/phoenix_meilisearch/test/smoke/meilisearch_stack_test.exs
    examples/phoenix_meilisearch/priv/repo/migrations/20250418120000_create_posts.exs
    examples/phoenix_meilisearch/priv/repo/migrations/20250420000000_add_authors_and_post_author_fields.exs
    prompts/phoenix-best-practices-deep-research.md
    prompts/ecto-best-practices-deep-research.md
    prompts/elixir-best-practices-deep-research.md
    prompts/elixir-opensource-libs-best-practices-deep-research.md
  </read_first>
  <behavior>
    - "Manifest/lock: D-01/D-03 bounds resolve Phoenix in [1.8.9, 1.9.0), Bandit in [1.12.1, 1.13.0), Ecto and Ecto SQL in [3.14.0, 3.15.0), Postgrex in [0.22.4, 0.23.0), Decimal >= 3.0.0, Plug in [1.19.5, 1.20.0), Req in [0.6.1, 0.7.0), Mint >= 1.9.3, and hpax >= 1.0.4."
    - "DataCase: empty Author/Post changesets are invalid with required errors; representative valid changesets insert; Repo query plus preload returns title, body, status, author_name, matching Author association, and non-nil inserted_at/updated_at."
    - "ConnCase: a representative unmatched JSON `/api/...` request and the same boundary with a malformed signed-session cookie return the existing 404 JSON shape without crashing."
    - "Real listener: one supervised port-0 loopback Bandit HTTP/1 request made with existing Req returns status 404 and the same JSON detail, then yields a matching [:bandit, :request, :stop] event with non-negative duration, `%Plug.Conn{status: 404}`, and the endpoint Plug metadata."
  </behavior>
  <action>
    Implement D-01 through D-06's checked candidate. Change exactly four existing direct rows in `mix.exs`: Phoenix to `~> 1.8.9`, Bandit to `~> 1.12.1`, Ecto SQL to `~> 3.14.0`, and Postgrex to `~> 0.22.4`. Keep `phoenix_ecto` and every unrelated declaration unchanged per D-02. Preserve transitive ownership per D-03: add neither direct Ecto nor Decimal, and add no override. Use targeted dependency unlock/update operations limited to the direct cohort and its necessary solver closure; retain Phase 144's Req/Mint/hpax handoff. Before continuing, inspect the manifest/lock diff and explain every moved row; any package-head or unrelated movement triggers the D-22 stop rather than acceptance (D-04/D-21).

    Create `ScrypathDemo.EctoCompatibilityTest` under `use ScrypathDemo.DataCase, async: true` per D-07/D-12. Keep optional fixture helpers private in this test file per D-08. Assert `Author.changeset/2` and `Post.changeset/2` validity plus exact `errors_on/1` required keys; insert one Author and one representative Post; query the Post and preload `:author`; assert title, body, status, denormalized `author_name`, Author identity/name, and non-nil timestamps. Exercise only existing schemas and migrations. Per D-09/D-11, add no Decimal value and never call `Blog.update_author/3` or any Scrypath synchronization function.

    Create one serial `ScrypathDemoWeb.EndpointCompatibilityTest` using the existing ConnCase and endpoint (D-13/D-14). Cover an unmatched JSON path such as `/api/phase-145-compatibility` and the same request with a deliberately malformed `_scrypath_demo_key` cookie; assert status 404 and `%{"errors" => %{"detail" => "Not Found"}}` without adding a route. In the same module, attach a unique telemetry handler, start exactly one loopback Bandit listener under `start_supervised!/1` with port `0` and the real `ScrypathDemoWeb.Endpoint` Plug using the selected versions' supported public startup/introspection surface, obtain the actual port, issue a normal HTTP/1 request with existing Req, and assert status/body plus the matching stop event. Detach telemetry in `on_exit`; use message assertions and supervised teardown, never sleeps, a fixed port, `:httpc`, or global shared test state (D-12/D-14).

    Respect D-15/D-16/D-20: do not add or claim WebSocket, HTTP/2, browser, session round-trip, UI, new service, route, socket, endpoint policy, permanent harness, Mix task, CI topology, or live Meilisearch proof. Per D-25, preserve the ordinary Phoenix/Ecto/Postgres adopter journey, keep solver mechanics out of the public Scrypath API, and leave the documented operational search prerequisites explicit. Make all four source/test files one implementation commit so the dependency candidate and its causal contracts are never separated (D-17 through D-19, D-23 through D-27). If the selected public Bandit/Phoenix surface cannot supply an ephemeral listener without endpoint-policy redesign, or any compatibility failure requires a public Scrypath change, migration, direct Decimal/override, or broader dependency work, stop and re-plan per D-21/D-22.
  </action>
  <verify>
    <automated>cd examples/phoenix_meilisearch &amp;&amp; mix deps.get --check-locked &amp;&amp; mix test test/scrypath_demo/ecto_compatibility_test.exs test/scrypath_demo_web/endpoint_compatibility_test.exs &amp;&amp; mix format --check-formatted mix.exs test/scrypath_demo/ecto_compatibility_test.exs test/scrypath_demo_web/endpoint_compatibility_test.exs &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>
    The four direct requirements equal D-01's exact strings; `phoenix_ecto` and unrelated manifest rows are unchanged; no direct Ecto/Decimal or override exists. The checked lock resolves every D-05 range and every moved row has a causal path from Phoenix/Bandit/Ecto SQL/Postgrex or Phase 144's inherited Req closure. Both focused files compile and pass: the DataCase contract proves required errors, valid inserts, query/preload, association, timestamps, and actual persisted values; the endpoint contract proves ConnCase JSON/malformed-cookie behavior plus a real loopback port-0 HTTP/1 404 and matching Bandit stop telemetry. The diff contains only the four listed files and introduces none of D-15/D-20/D-22's excluded surfaces. One implementation commit contains the complete candidate.
  </acceptance_criteria>
  <done>Maintainers can run the checked legacy candidate through its real dependency, Ecto/Postgres, Phoenix endpoint, and Bandit HTTP boundaries, with failures localized and no product-scope expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hex resolver metadata → tracked manifest/lock | Network-origin package metadata determines the executable dependency cohort. |
| Test input → Phoenix endpoint/session/parser pipeline | Malformed cookies and unmatched JSON requests cross the request-processing boundary. |
| Test process → Postgres SQL Sandbox | Repo calls and any supervised process must remain within explicit owner lifetime. |
| Loopback client → ephemeral Bandit listener | A real socket is exposed briefly and must bind only to loopback with deterministic teardown. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-145-01 | Tampering / Information disclosure / DoS | Dependency cohort and lock | high | mitigate | Enforce D-01/D-05 bounds, D-03 ownership, targeted causal lock review, and `mix deps.get --check-locked`; Plan 02 adds exact-SHA fresh resolution and unsuppressed audit. |
| T-145-02 | Denial of service | Parser/session endpoint boundary | medium | mitigate | D-13 ConnCase coverage sends representative JSON and malformed cookie input through the real endpoint and requires a stable non-crashing response. |
| T-145-03 | Denial of service / Repudiation | Sandbox and listener test harness | medium | mitigate | D-12/D-14 require DataCase owner lifecycle, `start_supervised!/1`, port 0, message synchronization, telemetry detachment, and no sleeps/global shared state. |
| T-145-04 | Elevation of privilege / Tampering | Scope boundary | medium | mitigate | D-15/D-20/D-22 reject new routes, sockets, migrations, public APIs, endpoint-policy changes, overrides, services, and CI infrastructure. |
| T-145-SC | Tampering | Existing Hex package update | high | mitigate | Research's Package Legitimacy Audit confirms no new package; retain known Phoenix/Bandit/Ecto SQL/Postgrex identities, bounded requirements, checked lock, causal diff, and Plan 02 unsuppressed Hex audit. |

## OWASP ASVS L1 Control Mapping

| ASVS Control | Threats | Plan Enforcement |
|---|---|---|
| V3 Session Management | T-145-02 | Preserve signed-cookie configuration and test malformed input without inventing a session flow. |
| V5 Input Validation | T-145-01, T-145-02 | Enforce exact version ranges and test real changeset/parser error boundaries. |
| V7 Error Handling and Logging | T-145-02, T-145-03 | Require stable errors, explicit telemetry, deterministic teardown, and concise failure evidence. |
| V14 Configuration | T-145-01, T-145-04, T-145-SC | Bound direct owners, forbid overrides/scope expansion, and require a consistent tracked lock. |
</threat_model>

<verification>
Start the documented Postgres prerequisite, then run the focused tracer command. Inspect `git diff --stat` and `git diff -- examples/phoenix_meilisearch/mix.exs examples/phoenix_meilisearch/mix.lock` before commit. Any failed or unverified high-severity mitigation, unexplained lock row, direct Decimal/Ecto ownership, non-loopback listener, or excluded surface blocks completion and requires stop/re-plan.
</verification>

<success_criteria>
- SEC-02's fixed-compatible direct cohort and transitive Ecto/Decimal posture exist in one checked, causally reviewed lock.
- Existing changesets, migrations, Repo persistence, association, timestamps, JSON error handling, malformed-cookie handling, and one real Bandit HTTP/telemetry path pass.
- D-01 through D-15 and D-20 through D-27 are visible in exact source assertions, tests, scope exclusions, or stop conditions.
- The implementation is one isolated commit and all high-severity mitigations are verified before Plan 02 starts.
</success_criteria>

<output>
Create `.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-01-SUMMARY.md` when done, including the exact implementation commit and causal moved-lock-row explanation.
</output>
