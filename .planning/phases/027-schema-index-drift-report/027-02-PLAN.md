---
phase: 27
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/scrypath/operator.ex
  - lib/scrypath/operator/reconcile.ex
  - test/scrypath/operator/reconcile_test.exs
autonomous: true
requirements: [DRIFT15-01, DRIFT15-02, OPS15-01]
tags: [operator, reconcile, optional, elixir]

must_haves:
  truths:
    - "`Reconcile.run/3` without `include_index_contract_drift: true` does not call `IndexContractDrift.build/2` (027-CONTEXT D-05, D-06)"
    - "When opt-in is true, `%Reconcile{}` includes `index_contract_drift: %Report{}` from the **same builder** as `Scrypath.index_contract_drift/2` (027-CONTEXT D-04)"
    - "`@enforce_keys` on `%Reconcile{}` is unchanged — new field is optional on defstruct only (027-CONTEXT + REQUIREMENTS additive rule)"
  artifacts:
    - path: "lib/scrypath/operator/reconcile.ex"
      provides: "optional `index_contract_drift` attachment"
      contains: "index_contract_drift"
    - path: "lib/scrypath/operator.ex"
      provides: "`:include_index_contract_drift` in operator-only opts"
      contains: "include_index_contract_drift"
---

<objective>
Add the **optional** reconcile enrichment from 027-CONTEXT D-04–D-06: **`include_index_contract_drift: true`** strips from runtime config, triggers one extra contract report build, and attaches **`index_contract_drift`** on **`%Reconcile{}`** without changing default behavior or `@enforce_keys`.
</objective>

<threat_model>
**ASVS L1 — optional reconcile enrichment**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-27-OPT: Default callers pay latency | Medium | Gate behind explicit keyword; default **`nil` / false** — no extra **`get_settings`** |
| T-27-OPT: Type confusion on `%Reconcile{}` | Low | Field documented in `@moduledoc`; **`@type t`** updated to **`index_contract_drift: nil \| Report.t()`** |
| T-27-OPT: Keyword leaks into Meilisearch config | Medium | Add **`:include_index_contract_drift`** to **`@operator_only_opts`** and **`pop`** before **`Config.resolve!`** path inside **`reconcile_sync`** / **`Reconcile.run`** boundary |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Extend operator opts and `reconcile_sync` split</name>
  <files>lib/scrypath/operator.ex</files>
  <read_first>
    - lib/scrypath/operator.ex (`@operator_only_opts`, `reconcile_sync/2`)
    - .planning/phases/027-schema-index-drift-report/027-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    In **`lib/scrypath/operator.ex`**:

    1. Add **`:include_index_contract_drift`** to **`@operator_only_opts`** immediately after **`:reason_class_counts`** (preserve list readability).
    2. In **`reconcile_sync/2`**, after **`Keyword.split(opts, @operator_only_opts)`**, keep **`:include_index_contract_drift`** inside **`operator_opts`** (it must **not** reach **`Config.resolve!`**). Call **`Reconcile.run(schema_module, config, operator_opts)`** unchanged except for the new key being allowed in **`operator_opts`**.
    3. In **`Reconcile.run/3`**, **`Keyword.pop(operator_opts, :include_index_contract_drift, false)`** (or read without mutating the keyword passed downstream) so **`FailedWork.list/3`** and **`Status.fetch/3`** never receive **`:include_index_contract_drift`** — mirror how **`reason_class_counts`** is stripped in **`failed_sync_work/2`** if **`list/3`** is strict about keys.

    **Document** on **`Scrypath.reconcile_sync/2`** (or **`Operator`** `@moduledoc` if that is the public doc surface) that **`include_index_contract_drift: true`** adds a Meilisearch **`get_settings`** read.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'include_index_contract_drift' lib/scrypath/operator.ex` returns at least two lines (list + usage)
    - `grep '@operator_only_opts' lib/scrypath/operator.ex` returns at least one line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Attach report on `%Reconcile{}` when opt-in</name>
  <files>lib/scrypath/operator/reconcile.ex</files>
  <read_first>
    - lib/scrypath/operator/reconcile.ex (full file — `run/3`, struct, `@type`)
    - lib/scrypath/operator/index_contract_drift.ex
    - lib/scrypath/operator/index_contract_drift/report.ex
  </read_first>
  <action>
    1. Add **`:index_contract_drift`** to **`defstruct`** list for **`%Reconcile{}`** **after** existing optional fields, **not** in **`@enforce_keys`**.
    2. Update **`@type t`** to include **`index_contract_drift: Scrypath.Operator.IndexContractDrift.Report.t() | nil`** (adjust alias to actual report module name from Plan 01).
    3. In **`run/3`**, read **`include_index_contract_drift = Keyword.get(operator_opts, :include_index_contract_drift, false)`**.
    4. After successful assembly of the reconcile struct body (**inside** the **`with`** success branch, before **`{:ok, %__MODULE__{...}}`**):

       - If **`include_index_contract_drift`** is **`true`**, call **`IndexContractDrift.build(schema_module, config)`** (or the finalized builder name from Plan 01). On **`{:ok, report}`**, set **`index_contract_drift: report`**; on **`{:error, _}`**, decide: **either** propagate **`{:error, reason}`** for the whole reconcile (document choice in **`@moduledoc`**) **or** set **`index_contract_drift: nil`** and add an operational field — **pick one** and implement consistently; **recommended:** propagate error so operators do not see a false-green reconcile when contract read fails.

    5. When flag is **`false`**, set **`index_contract_drift: nil`** explicitly.

    **Do not** append contract atoms to **`drift_signals`**.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'index_contract_drift' lib/scrypath/operator/reconcile.ex` returns at least three lines
    - `grep '@enforce_keys' lib/scrypath/operator/reconcile.ex` — verify **`index_contract_drift` is NOT listed** in the `@enforce_keys` block (manual read): the line after edit must still list exactly **`:schema` through `:reindex`** only as before
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Tests — default vs opt-in on `reconcile_sync`</name>
  <files>test/scrypath/operator/reconcile_test.exs</files>
  <read_first>
    - test/scrypath/operator/reconcile_test.exs (existing stubs, schema, `Reconcile.run` or `Scrypath.reconcile_sync` usage)
    - lib/scrypath/operator/reconcile.ex
  </read_first>
  <action>
    Add tests under a new **`describe "index_contract_drift opt-in (Phase 27)"`**:

    1. **Default:** Call **`Scrypath.reconcile_sync(SearchablePost, opts)`** (use existing test helper opts) **without** **`include_index_contract_drift`**. Assert **`match?({:ok, %Reconcile{index_contract_drift: nil}}, result)`** OR field **absent** only if you intentionally omit **`nil` keys** — **prefer explicit **`nil`**.

    2. **Opt-in with stubbed success:** When tests can inject a fake **`get_settings`** response (reuse mechanism from Plan 01 tests or shared helper), pass **`include_index_contract_drift: true`** and assert **`{:ok, %Reconcile{index_contract_drift: %Report{}}}`** (version **`1`**).

    3. **GET budget:** If the test stack exposes a call counter for **`get_settings`**, assert default path does **not** increment beyond the reconcile’s existing Meilisearch calls baseline; if not measurable, assert **`index_contract_drift`** is **`nil`** only (acceptable fallback per plan).

    Reference **`027-CONTEXT`** D-05 in a comment above the default-path test.
  </action>
  <verify>
    <automated>mix test test/scrypath/operator/reconcile_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'index_contract_drift' test/scrypath/operator/reconcile_test.exs` returns at least one line
    - `mix test test/scrypath/operator/reconcile_test.exs` exits `0`
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <automated>mix format --check-formatted && mix test test/scrypath/operator/index_contract_drift_test.exs test/scrypath/operator/reconcile_test.exs</automated>
</verification>

<success_criteria>
  - Optional field on **`%Reconcile{}`**; default **`nil`**
  - Opt-in uses same **`IndexContractDrift`** builder as standalone API
  - Tests prove default vs opt-in behavior
</success_criteria>
