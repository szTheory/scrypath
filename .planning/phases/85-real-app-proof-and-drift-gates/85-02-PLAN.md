---
phase: 85-real-app-proof-and-drift-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scrypath/docs_contract_test.exs
  - lib/mix/tasks/verify.phase85.ex
  - mix.exs
autonomous: true
requirements: [VRFY-01, DOC-01, DOC-02]
must_haves:
  truths:
    - "Phase 85 has one focused maintainer-runnable gate that fails on real-app story drift without broad noisy reruns."
    - "Docs contracts pin canonical-guide discoverability, non-goal truth, and proof-guide boundary language."
    - "`verify.phase85` reuses only the minimal prior runtime seams needed to defend the Phase 85 public story."
  artifacts:
    - path: "lib/mix/tasks/verify.phase85.ex"
      provides: "focused phase verification task"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "bounded Phase 85 docs/example drift assertions"
    - path: "mix.exs"
      provides: "preferred CLI env registration for the new verify task"
  key_links:
    - from: "lib/mix/tasks/verify.phase85.ex"
      to: "test/scrypath/composition_test.exs"
      via: "composition precedence proof"
      pattern: "test/scrypath/composition_test.exs"
    - from: "lib/mix/tasks/verify.phase85.ex"
      to: "test/scrypath/metadata_test.exs"
      via: "metadata derivation proof"
      pattern: "test/scrypath/metadata_test.exs"
    - from: "lib/mix/tasks/verify.phase85.ex"
      to: "test/scrypath/composition_many_test.exs"
      via: "multi-search lowering proof"
      pattern: "test/scrypath/composition_many_test.exs"
---

<objective>
Build the focused verification lane that protects the Phase 85 public story.

Purpose: make composition/metadata/multi-search truth and docs/example wayfinding fail together when they drift.
Output: one `mix verify.phase85` task plus narrow docs-contract assertions for the new canonical guide and non-goal language.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/85-real-app-proof-and-drift-gates/85-CONTEXT.md
@.planning/phases/85-real-app-proof-and-drift-gates/85-RESEARCH.md
@.planning/phases/85-real-app-proof-and-drift-gates/85-PATTERNS.md
@.planning/phases/85-real-app-proof-and-drift-gates/85-VALIDATION.md
@test/scrypath/docs_contract_test.exs
@lib/mix/tasks/verify.phase83.ex
@lib/mix/tasks/verify.phase84.ex
@mix.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend bounded docs contracts for the Phase 85 authority and non-goals</name>
  <read_first>test/scrypath/docs_contract_test.exs, guides/composing-real-app-search.md, guides/overview.md, guides/request-edge-search.md, guides/faceted-search-with-phoenix-liveview.md, guides/multi-index-search.md</read_first>
  <files>test/scrypath/docs_contract_test.exs</files>
  <acceptance_criteria>
    - `rg -n "composing-real-app-search|verify\\.phase85|generated UI|schema-generated|related-data|tenant" test/scrypath/docs_contract_test.exs` finds explicit Phase 85 assertions.
    - `mix test test/scrypath/docs_contract_test.exs` passes once the docs are aligned.
  </acceptance_criteria>
  <action>Extend `Scrypath.DocsContractTest` in the existing bounded string/order style. Pin that the new canonical guide exists and is discoverable from README, overview, and root moduledoc; that request-edge hands off to it; that the faceted and multi-index guides keep their role-specific proof posture; and that the core non-goals stay explicit (`%Scrypath.Query{}` non-public, no schema-generated runtime verbs, no generated UI, no tenant/authz or related-data promises). Keep assertions narrow and structural rather than snapshotting whole prose blocks.</action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <done>Bounded docs contracts now fail when the Phase 85 story, guide order, or non-goal language drifts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `mix verify.phase85` and keep it phase-local</name>
  <read_first>lib/mix/tasks/verify.phase83.ex, lib/mix/tasks/verify.phase84.ex, mix.exs, .planning/phases/85-real-app-proof-and-drift-gates/85-VALIDATION.md</read_first>
  <files>lib/mix/tasks/verify.phase85.ex, mix.exs</files>
  <acceptance_criteria>
    - `rg -n "defmodule Mix.Tasks.Verify.Phase85" lib/mix/tasks/verify.phase85.ex` finds the task.
    - `rg -n "\"verify.phase85\": :test" mix.exs` finds preferred env wiring.
    - `rg -n "composition_test|metadata_test|composition_many_test|docs_contract_test|docs\" lib/mix/tasks/verify.phase85.ex` finds the exact focused seams.
    - `mix help verify.phase85` exits successfully.
  </acceptance_criteria>
  <action>Create `Mix.Tasks.Verify.Phase85` by following the focused pattern from `verify.phase83` and `verify.phase84`: no args, start the app, run only `test/scrypath/composition_test.exs`, `test/scrypath/metadata_test.exs`, `test/scrypath/composition_many_test.exs`, and `test/scrypath/docs_contract_test.exs`, then run `mix docs --warnings-as-errors`. Register `"verify.phase85": :test` in `mix.exs`. Do not shell out to prior verify tasks, the example app, or service-backed checks.</action>
  <verify>
    <automated>mix help verify.phase85</automated>
  </verify>
  <done>The milestone-close story now has one phase-scoped verification lane that is small enough to trust and fast enough to run routinely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer verify command -> public story | a weak or noisy gate can let docs/runtime drift survive |
| docs-contract assertions -> contributor ergonomics | over-broad assertions can create brittle maintenance cost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-04 | T | `lib/mix/tasks/verify.phase85.ex` | mitigate | Keep the task phase-local and explicit about the exact focused seams it runs. |
| T-85-05 | R | `test/scrypath/docs_contract_test.exs` | mitigate | Add narrow ordering and keyword assertions rather than prose snapshots. |
| T-85-06 | S | public docs and proof guides | mitigate | Fail the docs-contract lane if canonical authority or non-goal language disappears or moves to the wrong surfaces. |
</threat_model>

<verification>
Run `mix test test/scrypath/docs_contract_test.exs` after contract edits and `mix help verify.phase85` after task wiring. The full plan gate is `mix verify.phase85` after Plan 03 lands the docs updates.
</verification>

<success_criteria>
Phase 85 has a focused verification lane and bounded contract coverage that together fail on the exact public-story drift the milestone cares about.
</success_criteria>

<output>
After completion, create `.planning/phases/85-real-app-proof-and-drift-gates/85-02-SUMMARY.md`
</output>
