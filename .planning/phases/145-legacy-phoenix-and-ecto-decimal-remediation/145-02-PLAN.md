---
phase: 145-legacy-phoenix-and-ecto-decimal-remediation
plan: 02
type: execute
wave: 2
depends_on: [145-01]
files_modified: []
autonomous: true
requirements: [SEC-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Per D-10/D-17, the reviewed candidate passes the binding deterministic order: explained manifest/lock diff, legacy checked lock, clean-database tests, already-migrated no-op migration, legacy precommit, then root fast regression."
    - "Per D-05/D-06/D-18, a detached lockless legacy resolution at the exact implementation SHA satisfies all nine package ranges and an unsuppressed legacy `mix hex.audit` exits zero; unavailable registry or advisory evidence is never a pass."
    - "Per D-16/D-18, the existing Postgres+Meilisearch inline/Oban/fan-out lane is reported separately as supplemental passed, failed, or unavailable and never replaces causal hard gates."
    - "Per D-19 through D-27, the phase summary records compact provenance, commands/exits, selected versions, causal lock rows, audit result, and evidence class while retaining one isolated implementation commit and normal Mix/Phoenix contributor/adopter workflows."
  artifacts:
    - path: ".planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md"
      provides: "Compact deterministic, exact-candidate network, and supplemental live evidence record"
  key_links:
    - from: "145-01 implementation commit SHA"
      to: "detached lockless examples/phoenix_meilisearch resolution"
      via: "git worktree add --detach with isolated MIX_DEPS_PATH/MIX_BUILD_PATH (D-05/D-18)"
    - from: "fresh package versions"
      to: "SEC-02"
      via: "nine exact D-05 range assertions plus unsuppressed legacy mix hex.audit"
    - from: "checked candidate"
      to: "Phase 146 start"
      via: "D-17 ordered deterministic commands and D-22 stop conditions"
  flagged_assumptions:
    - "[UNRESOLVED boundary probe] D-01/D-05's inclusive floors and exclusive minor ceilings close only when the exact-candidate detached resolution selects every package inside range; any miss blocks the phase."
    - "[UNRESOLVED precision probe] D-09 supplies no Decimal-valued application contract to test; this plan may report Decimal's selected version/path but must not infer rounding or numeric behavior from dependency resolution."
  prohibitions: []
---

<objective>
Seal the exact Phase 145 candidate with deterministic database/framework regression proof first, then truthful current-registry advisory evidence and separately classified optional live-stack evidence.

Purpose: Maintainers must know the fixed legacy graph is reproducible, auditable, and safe to hand to Phase 146 without conflating a mutable service/feed result with required deterministic proof (D-16 through D-27).
Output: A compact exact-SHA Phase 145 summary containing all required evidence fields and no disposable artifacts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTRIBUTING.md
@examples/phoenix_meilisearch/AGENTS.md
@examples/phoenix_meilisearch/README.md
@.github/workflows/ci.yml
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-CONTEXT.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-RESEARCH.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-VALIDATION.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-01-SUMMARY.md
@.planning/phases/144-root-http-client-dependency-remediation/144-03-SUMMARY.md
</context>

## Artifacts this phase produces

- `.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md` with candidate SHA, UTC timestamp, host/Elixir/OTP/Mix/Hex versions, deterministic commands/exits, selected dependency versions and compact paths, causal lock rows, unsuppressed audit status, and supplemental-live classification.
- No new symbol, helper, test module, source file, manifest/lock edit, public API, route/socket, schema field/migration, UI, CI lane, permanent script/Mix task, raw log, advisory snapshot, generated dependency tree, or disposable lock/worktree artifact.

<flagged_planner_assumptions>
- The boundary probe is unresolved until Task 2 executes the detached exact-SHA version assertions; D-05 is exhaustive and no substitute floor/ceiling is permitted.
- The precision probe is deliberately not converted into a rounding assertion: D-09 says the domain has no Decimal value. Version/path evidence closes only dependency selection, while Plan 01's DataCase assertions own real persistence behavior.
- Prohibition recall has no surviving bespoke values/safety/ethics row. False passing, vulnerable selection, raw-log retention, service exposure, and flaky cleanup are routine/canonical security concerns covered by exact gates and the threat model rather than descriptor-less bespoke prohibitions.
</flagged_planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Run the binding deterministic legacy and root regression sequence</name>
  <precondition>Plan 01's one implementation commit exists, its summary explains every moved lock row, and the documented Postgres test listener is reachable; the tracked candidate contains no unrelated changes.</precondition>
  <reversibility rating="reversible">The gate run changes only disposable build/database state and records results in the normal summary.</reversibility>
  <files>None; record compact command/exit evidence in the normal 145-02-SUMMARY.md output.</files>
  <read_first>
    AGENTS.md
    CONTRIBUTING.md
    examples/phoenix_meilisearch/AGENTS.md
    examples/phoenix_meilisearch/README.md
    .github/workflows/ci.yml
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-CONTEXT.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-01-SUMMARY.md
    examples/phoenix_meilisearch/mix.exs
    examples/phoenix_meilisearch/mix.lock
    examples/phoenix_meilisearch/config/test.exs
    examples/phoenix_meilisearch/test/scrypath_demo/ecto_compatibility_test.exs
    examples/phoenix_meilisearch/test/scrypath_demo_web/endpoint_compatibility_test.exs
  </read_first>
  <action>
    Enforce D-17 in order on the exact Plan 01 candidate. First inspect the manifest/lock diff and Plan 01 causal-row explanation; confirm the diff is limited to the four planned files and the implementation is one isolated commit. Run from `examples/phoenix_meilisearch`: `mix deps.get --check-locked`, then the normal `mix test` alias against a clean test database so its existing create/migrate steps execute, then `mix ecto.migrate --quiet` again against that already-migrated database and require a zero no-op, then `mix precommit`. Return to repository root and run `mix test --exclude integration --exclude docs_contract`. Record each command and exact exit status as deterministic evidence; do not reorder, collapse, or replace these gates (D-10/D-17/D-24).

    Keep the root fast regression and legacy gates on the same candidate. Record only compact D-19 fields, not raw output. If any required command fails, if the migration does work beyond a no-op on the second invocation, if the real listener test cannot boot, if the root regresses, if the workspace acquires unexplained tracked changes, or if repair would cross D-21/D-22's compatibility/scope boundary, stop and re-plan before Task 2 or Phase 146. D-23/D-26/D-27 require failures to be named by their owning boundary: resolve, migrate/cast/persist, serve/request, or root regression.
  </action>
  <verify>
    <automated>cd examples/phoenix_meilisearch &amp;&amp; mix deps.get --check-locked &amp;&amp; mix test &amp;&amp; mix ecto.migrate --quiet &amp;&amp; mix precommit &amp;&amp; cd ../.. &amp;&amp; mix test --exclude integration --exclude docs_contract</automated>
  </verify>
  <acceptance_criteria>
    Plan 01's four-file implementation diff and causal lock explanation are reviewed first. In D-17 order, checked-lock, clean-database legacy tests, already-migrated no-op, legacy precommit, and root fast tests all exit 0 on the same candidate. The summary classifies all results as deterministic and lists commands/exits without raw logs. Any nonzero gate, dirty tracked state, unexplained lock row, or widened repair need prevents Task 2 and Phase 146.
  </acceptance_criteria>
  <done>The reviewed legacy candidate is green on every required deterministic dependency, database, endpoint, migration, precommit, and root regression gate.</done>
</task>

<task type="auto">
  <name>Task 2: Prove exact-candidate fresh resolution, audit status, and supplemental live classification</name>
  <precondition>Task 1's complete D-17 deterministic sequence exited 0 on one clean candidate SHA.</precondition>
  <reversibility rating="reversible">The fresh probe and optional smoke create no retained product state; validated disposable worktree and service artifacts are removed after compact evidence capture.</reversibility>
  <files>None; record compact dated evidence in the normal 145-02-SUMMARY.md output and retain no probe artifacts.</files>
  <read_first>
    CONTRIBUTING.md
    examples/phoenix_meilisearch/README.md
    examples/phoenix_meilisearch/scripts/smoke.sh
    .github/workflows/ci.yml
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-CONTEXT.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-RESEARCH.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-01-SUMMARY.md
    .planning/phases/144-root-http-client-dependency-remediation/144-03-SUMMARY.md
    examples/phoenix_meilisearch/mix.exs
    examples/phoenix_meilisearch/mix.lock
  </read_first>
  <action>
    Capture D-19 provenance: exact implementation commit SHA, UTC timestamp, host, Elixir/OTP, Mix, and Hex versions. Create a disposable directory with `mktemp -d /tmp/scrypath-phase145.XXXXXX`, validate that resolved path before removal, and add a detached worktree at the exact SHA. Inside only `examples/phoenix_meilisearch` in that detached worktree, remove only its disposable `mix.lock`, set `MIX_DEPS_PATH` and `MIX_BUILD_PATH` to directories inside the disposable worktree, and run live-registry `mix deps.get` (D-05/D-18). Do not reuse or mutate primary dependency/build/lock state.

    Inspect fresh lock versions and compact `mix deps.tree --format plain` paths. Require D-05 exactly: Phoenix `>= 1.8.9 and < 1.9.0`; Bandit `>= 1.12.1 and < 1.13.0`; Ecto and Ecto SQL each `>= 3.14.0 and < 3.15.0`; Postgrex `>= 0.22.4 and < 0.23.0`; Decimal `>= 3.0.0`; Plug `>= 1.19.5 and < 1.20.0`; Req `>= 0.6.1 and < 0.7.0`; Mint `>= 1.9.3`; hpax `>= 1.0.4`. Compile in the isolated worktree if required before the version assertion, as Phase 144 established. Run `mix hex.audit` without ignore/suppression flags and require exit 0 (D-06). A registry/feed outage is `unavailable` and blocks phase completion; an out-of-range version, wrong dependency path, or audit finding is a D-22 failure, never a pass.

    Separately apply D-16/D-18: when the documented Postgres and Meilisearch prerequisites are available, run the existing `examples/phoenix_meilisearch/scripts/smoke.sh` or the exact CI-shaped `SCRYPATH_EXAMPLE_INTEGRATION=1` lane and record `passed` or `failed`; when prerequisites cannot be established, record `unavailable`. This lane remains supplemental and cannot replace or change Task 1 or the audit result. Do not add live tooling, services, CI, routes, or product assertions.

    Write only the compact D-19 evidence into `145-02-SUMMARY.md`: SHA/time/environment, command/exit table, D-05 selected versions plus compact paths, reviewed causal tracked-lock rows, unsuppressed audit result, deterministic/network/supplemental classifications, and live prerequisite/status. Do not retain raw logs, disposable lock, full dependency tree, advisory snapshot, build/deps directories, or service artifacts. Remove the validated disposable worktree and prune its registration; confirm the primary lock is byte-identical and tracked worktree clean. Any D-22 stop condition blocks Phase 146 and requires re-planning.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain --untracked-files=no)" &amp;&amp; candidate_sha=$(git rev-parse HEAD) &amp;&amp; primary_lock_sha=$(shasum -a 256 examples/phoenix_meilisearch/mix.lock | awk '{print $1}') &amp;&amp; probe_dir=$(mktemp -d /tmp/scrypath-phase145.XXXXXX) &amp;&amp; git worktree add --detach "$probe_dir" "$candidate_sha" &amp;&amp; case "$probe_dir" in /tmp/scrypath-phase145.*) ;; *) exit 97 ;; esac &amp;&amp; rm "$probe_dir/examples/phoenix_meilisearch/mix.lock" &amp;&amp; (cd "$probe_dir/examples/phoenix_meilisearch" &amp;&amp; test "$(git rev-parse HEAD)" = "$candidate_sha" &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase145_deps" MIX_BUILD_PATH="$probe_dir/.phase145_build" mix deps.get &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase145_deps" MIX_BUILD_PATH="$probe_dir/.phase145_build" mix compile --warnings-as-errors &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase145_deps" MIX_BUILD_PATH="$probe_dir/.phase145_build" mix run --no-compile --no-deps-check -e 'lock = Mix.Dep.Lock.read(); Enum.each([{:phoenix, ">= 1.8.9 and < 1.9.0"}, {:bandit, ">= 1.12.1 and < 1.13.0"}, {:ecto, ">= 3.14.0 and < 3.15.0"}, {:ecto_sql, ">= 3.14.0 and < 3.15.0"}, {:postgrex, ">= 0.22.4 and < 0.23.0"}, {:decimal, ">= 3.0.0"}, {:plug, ">= 1.19.5 and < 1.20.0"}, {:req, ">= 0.6.1 and < 0.7.0"}, {:mint, ">= 1.9.3"}, {:hpax, ">= 1.0.4"}], fn {dep, requirement} -&gt; version = lock |&gt; Map.fetch!(dep) |&gt; elem(2); Version.match?(version, requirement) || raise "#{dep} #{version} violates #{requirement}" end)' &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase145_deps" MIX_BUILD_PATH="$probe_dir/.phase145_build" mix hex.audit); probe_status=$?; git worktree remove --force "$probe_dir"; git worktree prune; test "$probe_status" -eq 0 &amp;&amp; test "$(shasum -a 256 examples/phoenix_meilisearch/mix.lock | awk '{print $1}')" = "$primary_lock_sha" &amp;&amp; test -z "$(git status --porcelain --untracked-files=no)"</automated>
  </verify>
  <acceptance_criteria>
    Detached HEAD equals the recorded implementation SHA; only the detached example lock is removed; all isolated paths stay inside the validated disposable worktree. Fresh resolution satisfies all ten listed package assertions (the nine D-05 dependency targets with Ecto and Ecto SQL asserted separately), compact dependency paths are recorded, and unsuppressed legacy `mix hex.audit` exits 0. Registry/feed unavailability blocks completion rather than passing. Optional live evidence is separately `passed`, `failed`, or `unavailable` with prerequisites. The summary contains all D-19 fields and no prohibited artifact; the worktree is removed, primary lock hash unchanged, and tracked state clean.
  </acceptance_criteria>
  <done>Maintainers have compact exact-SHA proof that the legacy example freshly resolves and audits inside every fixed-compatible boundary, plus an honest separately classified live-stack result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Reviewed candidate → deterministic commands | Required gates must exercise the exact checked implementation in binding order. |
| Primary worktree → detached probe | Network resolution must not mutate or borrow the tracked lock/build/dependency state. |
| Hex registry/advisory feed → evidence record | Mutable network results must be range-checked and may be unavailable. |
| Local services → supplemental status | Postgres/Meilisearch availability must never be confused with required proof. |
| Command output → committed summary | Only compact provenance may cross into versioned evidence. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-145-05 | Repudiation / Tampering | Deterministic candidate evidence | high | mitigate | D-17 exact command order, Plan 01 SHA/commit provenance, causal diff review, exit statuses, clean tracked-state checks. |
| T-145-06 | Tampering / Information disclosure / DoS | Fresh Hex resolution and audit | high | mitigate | Exact detached SHA, isolated paths, strict D-05 bounds/paths, unsuppressed audit, and primary lock hash preservation. |
| T-145-07 | Repudiation | Network/feed/service evidence truthfulness | high | mitigate | D-06/D-16/D-18 classify registry/feed outage as blocked-unavailable and live services as supplemental passed/failed/unavailable, never inferred pass. |
| T-145-08 | Information disclosure | Evidence retention | low | mitigate | D-19 retains compact fields only and excludes raw logs, advisory snapshots, full trees, disposable locks, and service artifacts. |
| T-145-09 | Denial of service / Tampering | Disposable worktree and test harness cleanup | medium | mitigate | Validate `/tmp/scrypath-phase145.*`, isolate deps/build, remove worktree, prune registration, preserve lock hash, and require clean tracked status. |
| T-145-SC | Tampering | Existing Hex dependency supply chain | high | mitigate | No new package; known package identities, bounded fresh resolve, causal paths, checked deterministic lock, and unsuppressed `mix hex.audit` all must pass. |

## OWASP ASVS L1 Control Mapping

| ASVS Control | Threats | Plan Enforcement |
|---|---|---|
| V5 Input Validation | T-145-06, T-145-SC | Parse and enforce every explicit D-05 version boundary and dependency path. |
| V7 Error Handling and Logging | T-145-05, T-145-07, T-145-08 | Preserve exit statuses and unavailable/failure classifications while excluding raw logs. |
| V14 Configuration | T-145-05, T-145-06, T-145-09 | Bind all proof to exact SHA/lock, isolate disposable state, and verify cleanup/lock identity. |
</threat_model>

<source_coverage_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Run legacy Phoenix on coordinated fixed-compatible Phoenix/Bandit/Ecto/Ecto SQL/Decimal set | 145-01, 145-02 | COVERED | Atomic checked candidate plus deterministic and current-registry proof. |
| REQ | SEC-02 | Resolve recorded legacy advisories through coordinated web/data upgrades | 145-01, 145-02 | COVERED | Direct bounds, transitive ownership, causal compatibility tests, exact-SHA audit. |
| RESEARCH | — | Four direct bounded owners and causal lock | 145-01 | COVERED | Leading tracer implementation. |
| RESEARCH | — | DataCase changeset/persistence contract | 145-01 | COVERED | Existing Repo/schemas/migrations only. |
| RESEARCH | — | ConnCase plus one real Bandit listener/telemetry contract | 145-01 | COVERED | One serial test module, no synthetic route. |
| RESEARCH | — | Deterministic gates before detached registry/audit evidence | 145-02 | COVERED | Tasks 1 and 2 preserve proof tiers. |
| RESEARCH | — | Supplemental existing live search lane | 145-02 | COVERED | Passed/failed/unavailable only. |
| CONTEXT | D-01–D-06 | Manifest bounds, ownership, lock causality, fresh ranges, audit truth | 145-01, 145-02 | COVERED | Candidate plus exact-SHA audit. |
| CONTEXT | D-07–D-12 | Real Ecto behavior, fixtures, Decimal boundary, migrations, Sandbox | 145-01, 145-02 | COVERED | DataCase tracer and ordered migration gates. |
| CONTEXT | D-13–D-16 | Endpoint layers, real Bandit event, protocol exclusions, live evidence | 145-01, 145-02 | COVERED | Focused endpoint test and separate supplemental lane. |
| CONTEXT | D-17–D-22 | Gate order, evidence fields, conditional fixes, stop/re-plan rules | 145-01, 145-02 | COVERED | One implementation commit followed by binding proof. |
| CONTEXT | D-23–D-27 | Maintainer/contributor/adopter JTBD, domain language, quality lenses | 145-01, 145-02 | COVERED | Boundary-local diagnostics, normal Mix/Phoenix interface, no UI work. |

Exclusions, not gaps: WebSocket/HTTP2/session/browser/UI proof; new routes/sockets/schema fields/migrations/seeds; public Scrypath changes; permanent automation; package-head modernization; Phase 146 Ops/Swoosh work; Phase 147 ecommerce/all-graph closure. CONTEXT contains no deferred idea requiring a plan.
</source_coverage_audit>

<verification>
Require Task 1's full deterministic sequence before any network proof. Then verify exact detached SHA, isolated paths, all D-05 bounds/paths, unsuppressed audit exit, optional-live classification, disposable cleanup, primary-lock hash, and clean tracked state. Any failed or unverified high-severity mitigation blocks Phase 145 completion and Phase 146 and requires stop/re-plan.
</verification>

<success_criteria>
- SEC-02 has both deterministic checked-lock/compatibility proof and exact-candidate fresh-resolution/audit proof.
- D-05's complete package boundary contract passes; D-09's no-Decimal-domain precision boundary remains truthful.
- D-10 and D-17's clean/already-migrated/precommit/root order passes on one candidate.
- D-16/D-18/D-19 evidence is compact and honestly classified, with no retained disposable artifacts.
- D-01 through D-27 are covered across the two-plan set, and every high-severity threat mitigation is verified before Phase 146.
</success_criteria>

<output>
Create `.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md` when done, containing the compact D-19 evidence record.
</output>
