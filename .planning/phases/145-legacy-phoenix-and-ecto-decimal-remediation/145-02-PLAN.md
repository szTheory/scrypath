---
phase: 145-legacy-phoenix-and-ecto-decimal-remediation
plan: 02
type: execute
wave: 2
depends_on: [145-01]
files_modified:
  - examples/phoenix_meilisearch/mix.exs
  - examples/phoenix_meilisearch/mix.lock
  - .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md
autonomous: true
requirements: [SEC-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Per the user-approved recovery amendment to D-01 and the unchanged D-03/D-05 contract, the legacy manifest directly bounds Plug at ~> 1.19.5, keeps Phoenix/Bandit/Ecto SQL/Postgrex bounds unchanged, keeps Ecto and Decimal transitive, and cannot freshly resolve Plug 1.20.x."
    - "The completed Plan 145-01 commit e50fbd5694c75ff5f25c2a07046185f35c107dd9 remains immutable; one additional graph-local recovery commit records the direct Plug bound and only causal lock movement, if Mix requires any."
    - "Per D-10/D-17/D-22, the complete deterministic sequence is rerun after the graph amendment and passes on the exact recovery candidate before any live-registry audit or supplemental smoke is evaluated."
    - "Per D-05/D-06/D-18, a detached lockless resolution at the recovery SHA compiles, satisfies all ten explicit package assertions including Plug >= 1.19.5 and < 1.20.0, and passes unsuppressed mix hex.audit; unavailable registry or advisory evidence is blocking, never passing."
    - "Per D-16/D-18, the existing Postgres and Meilisearch lane is reported separately as supplemental passed, failed, or unavailable and never substitutes for deterministic or audit evidence."
    - "Per the narrow D-20 clarification and D-21 through D-27, recovery adds no override, suppression, source compatibility change, policy layer, CI topology, service, UI, public API, route, socket, schema, migration, or broad dependency modernization."
    - "Per D-24, contributors keep normal Mix/Phoenix workflows: the reviewed manifest and lock, boundary-owned failures, fast causal preflight, and one documented opt-in live path remain the complete interface; no security/testing subsystem is added."
    - "Per D-26, the plan and compact evidence consistently use the established manifest, lock, dependency cohort, Repo, migration, changeset, fixture, row, association, endpoint, request, audit, and live-stack nouns plus resolve, compile, migrate, cast, insert, query, serve, audit, smoke, bound, align, explain, verify, isolate, report, and stop verbs."
    - "Per D-19, cleanup removes the validated disposable worktree and isolated build/dependency state, preserves the primary lock byte-for-byte during probing, leaves tracked state clean, and does not disturb unrelated untracked files."
  artifacts:
    - path: "examples/phoenix_meilisearch/mix.exs"
      provides: "Direct fixed-compatible Plug 1.19.x ownership alongside the completed four-bound cohort"
      contains: "{:plug, \"~> 1.19.5\"}"
    - path: "examples/phoenix_meilisearch/mix.lock"
      provides: "Reviewed deterministic legacy resolution; unchanged by recovery unless Mix proves causal movement"
    - path: ".planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md"
      provides: "Compact recovery history, deterministic rerun, exact-SHA registry/audit, supplemental smoke, and cleanup evidence"
  key_links:
    - from: "examples/phoenix_meilisearch/mix.exs"
      to: "detached lockless Plug selection"
      via: "direct ~> 1.19.5 requirement plus D-05 Version.match? assertion"
      pattern: "\\{:plug, \"~> 1\\.19\\.5\"\\}"
    - from: "recovery commit SHA"
      to: "D-17 deterministic and D-18 network proof"
      via: "all gates and the detached worktree use the same recorded recovery candidate"
    - from: "fresh package versions"
      to: "SEC-02"
      via: "ten explicit bounds, boundary-neighbor checks, compact dependency paths, and unsuppressed mix hex.audit"
    - from: "disposable worktree"
      to: "primary workspace"
      via: "isolated MIX_DEPS_PATH/MIX_BUILD_PATH, validated cleanup, primary-lock hash equality, and tracked-only cleanliness assertion"
  flagged_assumptions:
    - "[UNRESOLVED boundary probe] D-01/D-05 floors are inclusive and ceilings exclusive. The recovery must reject one patch below each floor, accept each floor and an in-range value immediately below each finite ceiling, and reject each finite ceiling; actual selected versions must independently satisfy the same requirements."
    - "[UNRESOLVED precision probe] D-09 supplies no Decimal-valued application contract. The plan reports Decimal selection and dependency paths but deliberately makes no rounding, overflow, tie-breaking, or precision-loss claim."
  prohibitions: []
---

<objective>
Recover and seal the Phase 145 candidate after live resolution exposed that the four-bound manifest did not enforce D-05's Plug ceiling: add the approved direct Plug `~> 1.19.5` bound in its own graph-local commit, then rerun every deterministic, fresh-resolution, audit, supplemental, and cleanup gate against the recovery SHA.

Purpose: Maintainers need a reproducible fixed-compatible legacy graph and an honest execution history. Plan 145-01 remains completed evidence; the approved recovery is a second Phase 145 graph-local remediation commit, not a rewrite or amendment of that completed commit (D-19/D-23 through D-27).
Output: One narrow Plug-bound recovery commit plus a compact 145-02 summary tied to its exact SHA.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTRIBUTING.md
@examples/phoenix_meilisearch/AGENTS.md
@examples/phoenix_meilisearch/README.md
@.github/workflows/ci.yml
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-CONTEXT.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-RESEARCH.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-PATTERNS.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-VALIDATION.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-01-PLAN.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-01-SUMMARY.md
@.planning/phases/144-root-http-client-dependency-remediation/144-03-SUMMARY.md
</context>

## Artifacts this phase produces

- Direct Plug `~> 1.19.5` ownership in `examples/phoenix_meilisearch/mix.exs`, preserving the completed Phoenix, Bandit, Ecto SQL, and Postgrex bounds.
- `examples/phoenix_meilisearch/mix.lock` remains byte-identical when the direct bound is already satisfied; if Mix requires movement, only explained Plug-causal rows may change.
- One new recovery implementation commit after immutable `e50fbd5694c75ff5f25c2a07046185f35c107dd9`; normal commit hooks remain enabled and neither the completed implementation commit nor summary commit `580c5f8` is amended.
- `.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md` records both Phase 145 implementation SHAs, UTC/environment provenance, deterministic commands/exits, boundary and selected-version evidence, causal lock disposition, unsuppressed audit status, supplemental-live classification, and cleanup proof.
- No retained raw log, disposable lock/worktree, dependency tree, build/deps directory, advisory snapshot, or service artifact.

<assumption_delta_decision>
Primary noun: commit. Decision: no-change. The detector fired on “second commit,” but the noun remains a graph-local dependency-remediation commit in the already approved ordered history; it does not introduce a second provider, integration, product identity, fallback, or public surface.
</assumption_delta_decision>

<decision_amendment>
- D-01 is narrowly amended by user approval: add Plug `~> 1.19.5` as the fifth direct fixed-compatible cohort owner while leaving Phoenix `~> 1.8.9`, Bandit `~> 1.12.1`, Ecto SQL `~> 3.14.0`, and Postgrex `~> 0.22.4` unchanged.
- D-20 is clarified, not broadened: an ordinary direct requirement for an already resolved dependency is part of the graph-local manifest repair, not a permanent dependency-policy layer. All D-20 infrastructure and interface exclusions remain locked.
- D-03, D-05, and D-21 through D-27 remain locked exactly. All other CONTEXT decisions remain in force unless the live registry finding specifically changed Plug ownership.
</decision_amendment>

<flagged_planner_assumptions>
- Boundary evidence remains explicit and unresolved until Task 2 proves both the synthetic edge matrix and the recovery candidate's actual selected versions. Finite ranges test one patch below the inclusive floor, the floor, a value immediately below the exclusive ceiling, and the ceiling; minimum-only ranges test one patch below and the exact floor.
- Precision remains explicitly unresolved and delimited by D-09. The example has no Decimal-valued field, so no numeric behavior may be invented; existing Plan 145-01 persistence tests remain the application-behavior proof.
- Two-stage prohibition recall produced only routine engineering and canonical security concerns: dependency tampering, false pass classification, artifact leakage, and unsafe cleanup. Those remain concrete task gates and ASVS L1 mitigations. No bespoke values/safety/ethics prohibition survives precision, so `must_haves.prohibitions` is intentionally empty rather than silently dropped.
</flagged_planner_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Commit the direct Plug boundary as the atomic recovery candidate</name>
  <precondition>Commits e50fbd5694c75ff5f25c2a07046185f35c107dd9, 580c5f8, and recovery research commit 721d6bc exist; the tracked workspace is clean; examples/phoenix_meilisearch/mix.lock has its pre-task SHA-256 recorded; unrelated untracked .planning/v1.36-v1.36-MILESTONE-AUDIT.md remains outside task ownership.</precondition>
  <reversibility rating="costly">The direct Plug minor-line ownership changes the legacy graph's resolver contract; undo is local to one manifest/lock but reopens the reproducibility failure and requires the full graph proof again.</reversibility>
  <files>examples/phoenix_meilisearch/mix.exs, examples/phoenix_meilisearch/mix.lock</files>
  <read_first>
    AGENTS.md
    CONTRIBUTING.md
    examples/phoenix_meilisearch/AGENTS.md
    examples/phoenix_meilisearch/README.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-CONTEXT.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-RESEARCH.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-01-PLAN.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-01-SUMMARY.md
    examples/phoenix_meilisearch/mix.exs
    examples/phoenix_meilisearch/mix.lock
    prompts/elixir-plug-ecto-phoenix-system-design-best-practices-deep-research.md
    prompts/elixir-opensource-libs-best-practices-deep-research.md
    prompts/elixir-oss-lib-ci-cd-best-practices-deep-research.md
  </read_first>
  <behavior>
    - "Manifest ownership: exactly one direct Plug requirement uses ~> 1.19.5; the four completed direct bounds remain byte-for-byte the same; Ecto and Decimal remain transitive; no dependency override exists (approved D-01 amendment, D-03/D-05)."
    - "Checked graph: Phoenix, Bandit, Ecto, Ecto SQL, Postgrex, Decimal, Plug, Req, Mint, and hpax all satisfy the exact D-05 requirements before the recovery commit is created."
    - "Lock causality: because the current reviewed lock already selects Plug 1.19.5, a valid checked lock stays unchanged; if Mix requires a refresh, every changed row has an explained path from the new direct Plug requirement."
    - "History: the recovery is committed separately with normal hooks; e50fbd5694c75ff5f25c2a07046185f35c107dd9 and 580c5f8 remain immutable."
  </behavior>
  <action>
    Capture tracked status and the primary legacy lock SHA-256 before editing. Add `{:plug, "~> 1.19.5"}` as one ordinary direct dependency in `examples/phoenix_meilisearch/mix.exs` per the approved D-01 amendment, alongside the existing web cohort. Preserve the four completed direct ranges exactly and keep `phoenix_ecto` and all unrelated rows unchanged per D-02. Preserve D-03 by adding neither Ecto nor Decimal as a direct dependency and by using no override.

    Run `mix deps.get --check-locked` from the legacy example first. If it passes, retain the existing lock byte-for-byte. If it reports that the new direct requirement requires resolution, use a Plug-targeted Mix update, inspect the resulting manifest/lock diff, and accept only rows causally required by direct Plug ownership; explain each accepted lock row. Any unrelated movement, a need to alter another direct bound, or a source compatibility change triggers D-04/D-21/D-22 and stops the task.

    Prove the checked graph by reading the tracked lock through Mix and asserting all ten D-05 package requirements, with Ecto and Ecto SQL checked separately. Confirm the direct Plug path appears in the dependency tree. Run the existing legacy `mix precommit` and `git diff --check`; these are recovery-commit checks, not a substitute for Task 2's complete ordered rerun. Commit only `mix.exs` and any justified causal `mix.lock` rows as a new graph-local recovery commit using normal repository hooks. Do not amend, rewrite, or squash the completed Plan 145-01 implementation or summary commits. Preserve every unrelated untracked file.
  </action>
  <verify>
    <automated>cd examples/phoenix_meilisearch &amp;&amp; mix deps.get --check-locked &amp;&amp; mix precommit &amp;&amp; mix run --no-start --no-compile --no-deps-check -e 'lock = Mix.Dep.Lock.read(); Enum.each([{:phoenix, ">= 1.8.9 and &lt; 1.9.0"}, {:bandit, ">= 1.12.1 and &lt; 1.13.0"}, {:ecto, ">= 3.14.0 and &lt; 3.15.0"}, {:ecto_sql, ">= 3.14.0 and &lt; 3.15.0"}, {:postgrex, ">= 0.22.4 and &lt; 0.23.0"}, {:decimal, ">= 3.0.0"}, {:plug, ">= 1.19.5 and &lt; 1.20.0"}, {:req, ">= 0.6.1 and &lt; 0.7.0"}, {:mint, ">= 1.9.3"}, {:hpax, ">= 1.0.4"}], fn {dep, requirement} -&gt; version = lock |&gt; Map.fetch!(dep) |&gt; elem(2); Version.match?(version, requirement) || raise "#{dep} #{version} violates #{requirement}" end); direct_plug? = Enum.any?(Mix.Project.config()[:deps], fn {:plug, "~> 1.19.5"} -&gt; true; {:plug, "~> 1.19.5", _opts} -&gt; true; _ -&gt; false end); direct_plug? || raise "missing direct Plug ~> 1.19.5 requirement"' &amp;&amp; rg -F '{:plug, "~> 1.19.5"}' mix.exs &amp;&amp; cd ../.. &amp;&amp; git diff --check &amp;&amp; git diff --name-only | awk '$0 != "examples/phoenix_meilisearch/mix.exs" &amp;&amp; $0 != "examples/phoenix_meilisearch/mix.lock" { bad=1 } END { exit bad }'</automated>
  </verify>
  <acceptance_criteria>
    `mix.exs` has exactly the approved direct Plug requirement and retains the four completed cohort strings. The tracked lock either keeps its recorded SHA-256 or contains only explicitly explained Plug-causal movement. The checked lock passes all ten D-05 assertions, direct Plug ownership is visible, legacy precommit passes, and the task diff owns no file outside the two declared paths. A distinct normal-hook recovery commit is created without altering e50fbd5694c75ff5f25c2a07046185f35c107dd9 or 580c5f8; unrelated untracked files remain untouched.
  </acceptance_criteria>
  <stop_conditions>
    Stop without committing if checked-lock validation fails without a Plug-causal resolution, any selected package is out of D-05 range, Mix requires an override or direct Ecto/Decimal ownership, lock movement is unexplained, precommit fails, a source/public/schema/route/CI change appears necessary, completed commits would need rewriting, or any unrelated tracked/untracked file would be modified.
  </stop_conditions>
  <done>The legacy manifest itself enforces the reviewed Plug 1.19.x cohort in one auditable recovery commit, while the completed implementation and unrelated workspace state remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Rerun deterministic proof, then exact-SHA audit and supplemental smoke</name>
  <precondition>Task 1 produced a distinct recovery commit whose diff is limited to examples/phoenix_meilisearch/mix.exs and any justified mix.lock rows; Postgres at 127.0.0.1:5433 is reachable for deterministic gates; tracked state is clean while unrelated untracked files remain preserved.</precondition>
  <reversibility rating="reversible">All verification state is disposable or test-local; the summary records evidence while the validated worktree, isolated build/deps, and temporary service artifacts are removed.</reversibility>
  <files>.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md</files>
  <read_first>
    AGENTS.md
    CONTRIBUTING.md
    examples/phoenix_meilisearch/AGENTS.md
    examples/phoenix_meilisearch/README.md
    examples/phoenix_meilisearch/scripts/smoke.sh
    .github/workflows/ci.yml
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-CONTEXT.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-RESEARCH.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-VALIDATION.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-01-SUMMARY.md
    .planning/phases/144-root-http-client-dependency-remediation/144-03-SUMMARY.md
    examples/phoenix_meilisearch/mix.exs
    examples/phoenix_meilisearch/mix.lock
    examples/phoenix_meilisearch/config/test.exs
    examples/phoenix_meilisearch/test/scrypath_demo/ecto_compatibility_test.exs
    examples/phoenix_meilisearch/test/scrypath_demo_web/endpoint_compatibility_test.exs
    prompts/ecto-best-practices-deep-research.md
    prompts/elixir-best-practices-deep-research.md
    prompts/phoenix-best-practices-deep-research.md
  </read_first>
  <behavior>
    - "Deterministic order: inspect recovery history and causal diff; checked lock; clean-database legacy tests; already-migrated no-op; legacy precommit; root fast regression. Every command runs on the recovery candidate and exits zero (D-10/D-17)."
    - "Boundary matrix: Phoenix rejects 1.8.8, accepts 1.8.9 and 1.8.99, rejects 1.9.0; Bandit rejects 1.12.0, accepts 1.12.1 and 1.12.99, rejects 1.13.0; Ecto and Ecto SQL reject 3.13.99, accept 3.14.0 and 3.14.99, reject 3.15.0; Postgrex rejects 0.22.3, accepts 0.22.4 and 0.22.99, rejects 0.23.0; Plug rejects 1.19.4, accepts 1.19.5 and 1.19.99, rejects 1.20.0; Req rejects 0.6.0, accepts 0.6.1 and 0.6.99, rejects 0.7.0; Decimal rejects 2.99.99 and accepts 3.0.0; Mint rejects 1.9.2 and accepts 1.9.3; hpax rejects 1.0.3 and accepts 1.0.4."
    - "Fresh resolution: the detached recovery SHA selects all ten packages inside D-05, compiles with warnings as errors, exposes compact causal paths, and passes unsuppressed mix hex.audit; Plug 1.20.x cannot pass the retained Plug assertion."
    - "Evidence taxonomy: deterministic, network/audit, and Postgres+Meilisearch supplemental results remain separate; current healthy services cause the existing smoke lane to run, while a later prerequisite outage is recorded as unavailable rather than pass."
    - "Cleanup: the disposable path is validated before removal, worktree registration is pruned, isolated deps/build and disposable lock are gone, the primary lock hash is unchanged by probing, tracked state is clean, and unrelated untracked files remain untouched."
  </behavior>
  <action>
    Before any recovery verification or disposable probe, require `.planning/v1.36-v1.36-MILESTONE-AUDIT.md` to exist and record its SHA-256 so preservation of this named unrelated user file is explicit rather than inferred from tracked-only Git status. Record both the immutable Plan 145-01 implementation SHA and Task 1 recovery SHA, then bind all new evidence to the recovery SHA. Capture UTC time, host, Elixir/OTP, Mix, and Hex versions. Inspect `git show` for both commits and the Plan 145-01 summary so the final record states honestly that Phase 145 required the completed compatibility commit plus the execution-discovered Plug-bound recovery commit; do not restate the obsolete one-implementation-commit claim.

    Rerun D-17 in binding order after the graph change. First inspect and explain the recovery manifest/lock diff. From `examples/phoenix_meilisearch`, run `mix deps.get --check-locked`, then run a fast preflight through `Mix.Project.config()[:deps]` and `Mix.Dep.Lock.read()` that requires the exact direct `{:plug, "~> 1.19.5"}` declaration and a checked Plug version in `>= 1.19.5 and < 1.20.0`. Only after that short gate passes, run the normal `mix test` alias against a clean test database, immediately run `MIX_ENV=test mix ecto.migrate --quiet` against that same already-migrated database and require a zero no-op, then run `mix precommit`. From repository root run `mix test --exclude integration --exclude docs_contract`. Record every command and exit status as deterministic evidence. A failure stops before detached resolution.

    Exercise the explicit boundary matrix in `<behavior>` with `Version.match?/2`; this is specification-edge evidence, separate from checking actual selected versions. Create a disposable directory with `mktemp -d /tmp/scrypath-phase145.XXXXXX`, validate the resolved prefix, add a detached worktree at the recovery SHA, record the primary lock SHA-256, and remove only the detached example's lock. Keep `MIX_DEPS_PATH` and `MIX_BUILD_PATH` inside that disposable worktree. In the detached example run `mix deps.get`, `mix compile --warnings-as-errors`, all ten actual-package D-05 assertions, compact `mix deps.tree --format plain` inspection including the direct Plug path, and `mix hex.audit` with no ignore or suppression option (D-05/D-06/D-18). A registry/advisory outage is unavailable and blocking; an out-of-range version, Plug 1.20.x, wrong path, compile failure, or audit finding is a D-22 failure.

    After deterministic and audit evidence pass, check the documented Postgres and Meilisearch prerequisites. They are currently healthy at 127.0.0.1:5433 and 127.0.0.1:7700, so run the existing `scripts/smoke.sh` or its exact documented CI-shaped command and record the result separately under D-16/D-18. If prerequisites change, record `unavailable`; if the lane runs, record its real exit as `passed` or `failed`. Never use this supplemental result to replace or relabel deterministic or audit evidence. If a smoke failure demonstrates a candidate behavior regression rather than an external prerequisite failure, stop under D-22.

    Write compact D-19 evidence only: both implementation SHAs and roles, time/environment, commands/exits, unchanged-versus-causal lock disposition, boundary matrix, selected versions and compact paths, unsuppressed audit result, supplemental prerequisites/status, primary lock hash before/after, tracked cleanliness, disposable cleanup, and the before/after SHA-256 equality for `.planning/v1.36-v1.36-MILESTONE-AUDIT.md`. Retain no raw logs, generated tree, advisory snapshot, lock, worktree, build/deps directory, or service artifact. Remove and prune the validated disposable worktree even on probe failure. Require the unrelated milestone audit to still exist with its original hash after cleanup.
  </action>
  <verify>
    <automated>audit_file=.planning/v1.36-v1.36-MILESTONE-AUDIT.md &amp;&amp; test -f "$audit_file" &amp;&amp; audit_sha=$(shasum -a 256 "$audit_file" | awk '{print $1}') &amp;&amp; cd examples/phoenix_meilisearch &amp;&amp; mix deps.get --check-locked &amp;&amp; mix run --no-start --no-compile --no-deps-check -e 'deps = Mix.Project.config()[:deps]; direct_plug? = Enum.any?(deps, fn {:plug, "~> 1.19.5"} -&gt; true; {:plug, "~> 1.19.5", _opts} -&gt; true; _ -&gt; false end); direct_plug? || raise "missing direct Plug ~> 1.19.5 requirement"; plug = Mix.Dep.Lock.read() |&gt; Map.fetch!(:plug) |&gt; elem(2); Version.match?(plug, ">= 1.19.5 and &lt; 1.20.0") || raise "checked Plug #{plug} violates recovery range"' &amp;&amp; mix test &amp;&amp; MIX_ENV=test mix ecto.migrate --quiet &amp;&amp; mix precommit &amp;&amp; cd ../.. &amp;&amp; mix test --exclude integration --exclude docs_contract &amp;&amp; cd examples/phoenix_meilisearch &amp;&amp; mix run --no-start --no-compile --no-deps-check -e 'cases = [{">= 1.8.9 and &lt; 1.9.0", [{"1.8.8", false}, {"1.8.9", true}, {"1.8.99", true}, {"1.9.0", false}]}, {">= 1.12.1 and &lt; 1.13.0", [{"1.12.0", false}, {"1.12.1", true}, {"1.12.99", true}, {"1.13.0", false}]}, {">= 3.14.0 and &lt; 3.15.0", [{"3.13.99", false}, {"3.14.0", true}, {"3.14.99", true}, {"3.15.0", false}]}, {">= 0.22.4 and &lt; 0.23.0", [{"0.22.3", false}, {"0.22.4", true}, {"0.22.99", true}, {"0.23.0", false}]}, {">= 1.19.5 and &lt; 1.20.0", [{"1.19.4", false}, {"1.19.5", true}, {"1.19.99", true}, {"1.20.0", false}]}, {">= 0.6.1 and &lt; 0.7.0", [{"0.6.0", false}, {"0.6.1", true}, {"0.6.99", true}, {"0.7.0", false}]}, {">= 3.0.0", [{"2.99.99", false}, {"3.0.0", true}]}, {">= 1.9.3", [{"1.9.2", false}, {"1.9.3", true}]}, {">= 1.0.4", [{"1.0.3", false}, {"1.0.4", true}]}]; Enum.each(cases, fn {requirement, samples} -&gt; Enum.each(samples, fn {version, expected} -&gt; actual = Version.match?(version, requirement); actual == expected || raise "boundary #{version} against #{requirement}: expected #{expected}, got #{actual}" end) end)' &amp;&amp; cd ../.. &amp;&amp; recovery_sha=$(git rev-parse HEAD) &amp;&amp; primary_lock_sha=$(shasum -a 256 examples/phoenix_meilisearch/mix.lock | awk '{print $1}') &amp;&amp; probe_dir=$(mktemp -d /tmp/scrypath-phase145.XXXXXX) &amp;&amp; case "$probe_dir" in /tmp/scrypath-phase145.*) ;; *) exit 97 ;; esac &amp;&amp; cleanup_probe() { git worktree remove --force "$probe_dir" &gt;/dev/null 2&gt;&amp;1 || true; git worktree prune; } &amp;&amp; trap cleanup_probe EXIT &amp;&amp; git worktree add --detach "$probe_dir" "$recovery_sha" &amp;&amp; rm "$probe_dir/examples/phoenix_meilisearch/mix.lock" &amp;&amp; (cd "$probe_dir/examples/phoenix_meilisearch" &amp;&amp; test "$(git rev-parse HEAD)" = "$recovery_sha" &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase145_deps" MIX_BUILD_PATH="$probe_dir/.phase145_build" mix deps.get &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase145_deps" MIX_BUILD_PATH="$probe_dir/.phase145_build" mix compile --warnings-as-errors &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase145_deps" MIX_BUILD_PATH="$probe_dir/.phase145_build" mix run --no-start --no-compile --no-deps-check -e 'lock = Mix.Dep.Lock.read(); Enum.each([{:phoenix, ">= 1.8.9 and &lt; 1.9.0"}, {:bandit, ">= 1.12.1 and &lt; 1.13.0"}, {:ecto, ">= 3.14.0 and &lt; 3.15.0"}, {:ecto_sql, ">= 3.14.0 and &lt; 3.15.0"}, {:postgrex, ">= 0.22.4 and &lt; 0.23.0"}, {:decimal, ">= 3.0.0"}, {:plug, ">= 1.19.5 and &lt; 1.20.0"}, {:req, ">= 0.6.1 and &lt; 0.7.0"}, {:mint, ">= 1.9.3"}, {:hpax, ">= 1.0.4"}], fn {dep, requirement} -&gt; version = lock |&gt; Map.fetch!(dep) |&gt; elem(2); Version.match?(version, requirement) || raise "#{dep} #{version} violates #{requirement}" end)' &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase145_deps" MIX_BUILD_PATH="$probe_dir/.phase145_build" mix deps.tree --format plain &amp;&amp; MIX_DEPS_PATH="$probe_dir/.phase145_deps" MIX_BUILD_PATH="$probe_dir/.phase145_build" mix hex.audit) &amp;&amp; cleanup_probe &amp;&amp; trap - EXIT &amp;&amp; test ! -e "$probe_dir" &amp;&amp; test "$(shasum -a 256 examples/phoenix_meilisearch/mix.lock | awk '{print $1}')" = "$primary_lock_sha" &amp;&amp; test -f "$audit_file" &amp;&amp; test "$(shasum -a 256 "$audit_file" | awk '{print $1}')" = "$audit_sha" &amp;&amp; test -z "$(git status --porcelain --untracked-files=no)"</automated>
  </verify>
  <acceptance_criteria>
    The recovery SHA is the single candidate for all rerun evidence. The D-17 checked-lock, clean-database tests, already-migrated no-op, precommit, and root fast commands pass in order after the graph amendment. The explicit boundary matrix passes at every floor, ceiling, and adjacent value; all ten freshly selected packages satisfy the actual D-05 assertions, direct Plug ownership is visible, isolated compilation succeeds, and unsuppressed audit exits zero. Network/feed unavailability blocks completion. Supplemental smoke is separately classified from its observed prerequisites and exit. The summary identifies both Phase 145 implementation commits without rewriting history, contains all D-19 fields, and retains no prohibited artifact. Cleanup removes the disposable worktree, preserves the primary lock hash, leaves tracked state clean, and preserves unrelated untracked files.
  </acceptance_criteria>
  <stop_conditions>
    Stop before Phase 146 on any deterministic failure or reordering, non-no-op second migration, out-of-range selected or boundary value, Plug 1.20.x selection, wrong dependency path, compile/audit failure, blocking registry/feed outage, unexplained lock or tracked-state mutation, cleanup/primary-lock identity failure, demonstrated candidate smoke regression, need for an override/suppression/direct Decimal/source/public/schema/route/CI change, or any broader modernization. Report supplemental prerequisite unavailability truthfully without promoting it to a pass.
  </stop_conditions>
  <done>Maintainers have exact-recovery-SHA proof that the legacy example deterministically runs, freshly resolves only within the reviewed cohort, audits cleanly, reports live evidence honestly, and leaves no disposable or unrelated workspace damage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Completed Plan 145-01 history → recovery commit | The new manifest owner must extend, not rewrite, the already completed compatibility implementation. |
| Hex resolver metadata → direct manifest and tracked lock | Mutable package metadata must not bypass the retained Plug ceiling or introduce unrelated lock movement. |
| Recovery candidate → deterministic commands | Required gates must exercise the exact post-recovery graph in binding order. |
| Primary worktree → detached probe | Network resolution must not mutate or borrow the primary lock, build, dependency, or unrelated user state. |
| Hex registry/advisory feed → evidence summary | Mutable results may fail or be unavailable and must never be reported as deterministic proof. |
| Postgres/Meilisearch services → supplemental status | Service-dependent behavior evidence remains distinct from required graph/audit proof. |
| Command output → committed summary | Only compact non-secret provenance may cross into versioned evidence. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-145-10 | Tampering / Repudiation | Completed and recovery commit history | high | mitigate | Preserve e50fbd5694c75ff5f25c2a07046185f35c107dd9 and 580c5f8; create a distinct normal-hook recovery commit; record both implementation roles and exact SHAs. |
| T-145-11 | Tampering / Information disclosure / DoS | Direct Plug bound and causal lock | high | mitigate | Approved D-01 amendment, retained D-03/D-05 constraints, checked-lock proof, all ten version assertions, direct dependency path, and causal diff review. |
| T-145-12 | Repudiation / Tampering | Deterministic rerun | high | mitigate | D-17 exact order on the recovery SHA, command exits, clean-database and no-op migration distinction, root regression, and tracked cleanliness. |
| T-145-13 | Tampering / Information disclosure / DoS | Detached Hex resolution and audit | high | mitigate | Exact recovery SHA, isolated paths, explicit boundary matrix, actual D-05 assertions, warnings-as-errors compilation, unsuppressed audit, and blocking-unavailable classification. |
| T-145-14 | Repudiation | Supplemental service evidence | medium | mitigate | Record Postgres/Meilisearch prerequisites and actual passed/failed/unavailable result separately; investigate demonstrated candidate regressions under D-22. |
| T-145-15 | Information disclosure | Evidence retention | low | accept | Compact non-secret versions, paths, hashes, and statuses are necessary for D-19; raw logs, advisory snapshots, full trees, and service artifacts are excluded. |
| T-145-16 | Denial of service / Tampering | Disposable worktree cleanup | high | mitigate | Validate the temporary prefix, trap cleanup, isolate deps/build, remove/prune the worktree, compare primary lock SHA-256, check tracked cleanliness, and preserve unrelated untracked files. |
| T-145-SC | Tampering | Existing Hex package supply chain | high | mitigate | Plug is already present and the recovery introduces no new package identity; normal resolver metadata, known package provenance, direct bound, checked lock, exact-SHA fresh resolution, and unsuppressed audit remain mandatory. |

## OWASP ASVS L1 Control Mapping

| ASVS Control | Threats | Plan Enforcement |
|---|---|---|
| V5 Input Validation | T-145-11, T-145-13, T-145-SC | Parse actual versions and exercise exact inclusive/exclusive boundary neighbors before accepting the graph. |
| V7 Error Handling and Logging | T-145-10, T-145-12, T-145-14, T-145-15 | Record exact provenance/exits and truthful classifications while excluding raw output and secrets. |
| V14 Configuration | T-145-11, T-145-12, T-145-13, T-145-16 | Bind Plug directly, preserve lock causality, isolate the probe, and enforce cleanup plus primary-lock identity. |
</threat_model>

<source_coverage_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Run the legacy Phoenix example on a coordinated fixed-compatible Phoenix/Bandit/Ecto/Ecto SQL/Decimal set | 145-01, 145-02 | COVERED | Completed compatibility tracer plus direct-Plug recovery and exact-candidate proof. |
| REQ | SEC-02 | Resolve recorded legacy advisories through coordinated Phoenix/Bandit and Ecto/Ecto SQL/Decimal upgrades | 145-01, 145-02 | COVERED | Checked graph, real behavior, bounded fresh resolution, and unsuppressed audit. |
| RESEARCH | recovery | Add direct Plug ~> 1.19.5 and retain the D-05 ceiling | 145-02 Task 1 | COVERED | User-approved graph-local recovery with no override or suppression. |
| RESEARCH | recovery | Rerun deterministic gates after the graph change | 145-02 Task 2 | COVERED | Full D-17 order binds to the recovery SHA. |
| RESEARCH | recovery | Detached exact-SHA lockless compile, ten assertions, audit, cleanup | 145-02 Task 2 | COVERED | Includes boundary-neighbor evidence and primary-lock identity. |
| RESEARCH | recovery | Supplemental existing live search lane | 145-02 Task 2 | COVERED | Passed/failed/unavailable remains separate. |
| CONTEXT | D-01 | Direct fixed-compatible cohort ownership | 145-01, 145-02 Task 1 | COVERED | Narrowly amended by approval to add direct Plug; four completed bounds remain unchanged. |
| CONTEXT | D-02–D-06 | Unrelated bounds, transitive Ecto/Decimal, lock causality, exact fresh ranges, unsuppressed audit | 145-01, 145-02 | COVERED | D-05 range retained exactly; Plug ownership becomes direct. |
| CONTEXT | D-07, D-08, D-09, D-10, D-11, D-12 | Real Ecto behavior, fixtures, Decimal domain boundary, clean/no-op migrations, search isolation, Sandbox | 145-01, 145-02 Task 2 | COVERED | Completed tests plus post-recovery deterministic rerun; precision remains delimited. |
| CONTEXT | D-13, D-14, D-15, D-16 | Endpoint/listener proof, protocol exclusions, supplemental live lane | 145-01, 145-02 Task 2 | COVERED | Completed endpoint contracts rerun; no new protocol/UI claim. |
| CONTEXT | D-17, D-18, D-19 | Ordered gates, exact-candidate network proof, compact evidence | 145-02 Task 2 | COVERED | Recovery history, proof tiers, and cleanup are explicit. |
| CONTEXT | D-20 | No permanent policy/infrastructure/UI surface | 145-02 Task 1 | COVERED | Clarified that the ordinary direct bound is graph ownership, not a policy layer; all exclusions remain. |
| CONTEXT | D-21, D-22 | Conditional compatibility fix and stop/re-plan boundary | 145-02 Tasks 1–2 | COVERED | No source fix invoked; every recovery stop is explicit. |
| CONTEXT | D-23, D-24, D-25, D-26, D-27 | Maintainer/contributor/adopter JTBD, domain language, quality lenses, scope discipline | 145-01, 145-02 | COVERED | Normal Mix/Phoenix flow, exact provenance, no public or UI expansion. |

Exclusions, not gaps: Decimal rounding/overflow/tie-breaking behavior because no Decimal-valued domain field exists; WebSocket/HTTP2/session/browser/UI proof; new routes/sockets/schema fields/migrations/seeds; public Scrypath changes; permanent automation; package-head modernization; Phase 146 Ops/Swoosh work; Phase 147 ecommerce/all-graph closure. CONTEXT contains no deferred idea requiring a plan.
</source_coverage_audit>

<verification>
Verify the atomic direct-Plug recovery commit first. Then require Task 2's complete deterministic sequence before detached network proof, require boundary and actual-version assertions plus unsuppressed audit, classify supplemental smoke independently, and enforce cleanup/primary-lock/tracked-state invariants. Any failed or unverified high-severity mitigation blocks Phase 145 completion and Phase 146.
</verification>

<success_criteria>
- SEC-02 is recovered without modifying the completed Plan 145-01 artifacts or widening the reviewed Plug cohort.
- Direct Plug `~> 1.19.5` makes fresh Plug 1.20.x selection impossible while D-03 transitive Ecto/Decimal ownership remains intact.
- The recovery commit is separately auditable, uses normal hooks, and owns only the graph-local manifest plus causal lock movement if required.
- D-10/D-17 deterministic gates rerun in order on the recovery SHA before D-05/D-06 detached resolution and audit.
- Boundary-neighbor and actual selected-version evidence close the SEC-02 range probe; the Decimal precision probe remains explicitly delimited without invented behavior.
- D-16/D-18/D-19 evidence stays compact and truthfully classified, with disposable cleanup, primary-lock identity, tracked cleanliness, and unrelated untracked-file preservation proven.
- D-01 through D-27 remain covered across the immutable completed plan and this targeted recovery; every high-severity threat mitigation is verified before Phase 146.
</success_criteria>

<output>
Create `.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md` when done, containing the compact approved-recovery evidence record.
</output>
