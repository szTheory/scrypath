---
phase: 128-contrast-gate-harness-dark-seed-coverage-s-g
plan: 03
type: execute
wave: 3
depends_on:
  - 128-01
  - 128-02
files_modified:
  - examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts
  - .planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md
autonomous: false
requirements:
  - CONTRAST-HARNESS-01

must_haves:
  truths:
    - "admin_contrast_matrix.spec.ts runs the full ~13 states × 3 modes × 2 viewports axe matrix and exits non-zero on any AA violation"
    - "THEME_MODES covers exactly 3 modes — light, dark, system-dark — with no system-light row (D-03: redundant since explicit-light covers it; only dark has a prefers-color-scheme branch in app.css)"
    - "system-dark rows use colorScheme:dark context + omit phx:theme write; D-08 invariants (no data-theme, matchMedia true, data-theme-effective=dark) assert after waitForLiveConnected"
    - "AA gate keys on violations[] only — never on incomplete[] (D-04)"
    - "AAA-body advisory pass uses color-contrast-enhanced scoped to BODY_SELECTORS; findings never increment aa_fail"
    - "128-CONTRAST-REPORT.md committed in the phase dir as curated evidence for phases 129/132"
    - "admin_screenshot_matrix.spec.ts is UNCHANGED and still captures 40 shots successfully"
  artifacts:
    - path: "examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts"
      provides: "Full contrast gate: 3 theme-modes × 2 viewports × ~13 screen-states with AA gate + AAA advisory"
      contains: "AxeBuilder"
    - path: ".planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md"
      provides: "Curated committed evidence: summary + AA failure list + AAA advisory list; feeds phases 129/132"
      contains: "scrypath.contrast.v1"
  key_links:
    - from: "examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts"
      to: "@axe-core/playwright"
      via: "import { AxeBuilder } from '@axe-core/playwright'"
      pattern: "AxeBuilder"
    - from: "examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts"
      to: "examples/scrypath_ecommerce/e2e/helpers/e2e.ts"
      via: "seedScenario, waitForLiveConnected, drainSearchQueue, waitForSearchVisible"
      pattern: "waitForLiveConnected"
    - from: ".planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md"
      to: "examples/scrypath_ecommerce/test-results/contrast/contrast-report.json"
      via: "curated promotion from gitignored run artifacts"
      pattern: "summary\\.aa_fail"
---

<objective>
Create admin_contrast_matrix.spec.ts (the full axe contrast gate), run it against the
live dev stack to produce the phase's primary evidence artifact, and verify the existing
40-shot screenshot matrix is unaffected.

Purpose: This plan closes CONTRAST-HARNESS-01. The spec is the measurement instrument that
reveals the real AA failure set before any pixels change — its output (128-CONTRAST-REPORT.md)
is what phases 129/132 consume. The regression smoke on admin_screenshot_matrix.spec.ts
proves success criterion #4.

Output:
- admin_contrast_matrix.spec.ts — ~200 lines, cloned from screenshot matrix, axeCheck() replacing shoot(), discriminated union theme modes, D-08 system-dark invariants
- 128-CONTRAST-REPORT.md — committed curated markdown with summary table + AA failures + AAA advisory list
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTEXT.md
@.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-RESEARCH.md
@.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-PATTERNS.md
@.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-VALIDATION.md
@.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-01-SUMMARY.md
@.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin_contrast_matrix.spec.ts — full axe contrast gate</name>
  <files>examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts</files>

  <read_first>
  - examples/scrypath_ecommerce/e2e/admin_screenshot_matrix.spec.ts — read the ENTIRE file; this is the clone source; note exact lines for: imports (19–29), Theme type (33–42), VIEWPORTS/VIEWPORT_NAMES (36–43), ScreenCapture type (46–51), shoot() function (53–76), screenshotDir env (31), describeScenario loop (79–107), six gotoXxx helpers (111–156), runSearch (153–156), scenario groups (161–218)
  - examples/scrypath_ecommerce/e2e/helpers/e2e.ts — seedScenario, waitForLiveConnected, waitForSearchVisible, drainSearchQueue, SeedScenario type signatures; note that waitForLiveConnected is called inside each gotoXxx prepare helper, making it the correct attach point for D-08 invariants
  - PATTERNS.md §admin_contrast_matrix.spec.ts — the complete transformation map: ThemeMode discriminated union, themeSlug(), axeCheck() replacing shoot(), assertSystemDarkInvariants(), BODY_SELECTORS, appendFindings(), writeContrastReport(), ContrastFinding type; scenario groups with D-01 curated 9 states + D-02 dark-risk supplement indices 10–13
  - CONTEXT.md D-04 — gate STRICTLY on violations[], NEVER on incomplete[]
  - CONTEXT.md D-05 — BOTH viewports (mobile 390, desktop 1440) hard-gate AA
  - CONTEXT.md D-06/D-07/D-08/D-09 — system-dark drive mechanism: colorScheme:"dark" context + OMIT phx:theme write; three D-08 runtime invariants after waitForLiveConnected
  - CONTEXT.md D-17/D-18/D-19/D-20/D-21 — unified scrypath.contrast.v1 schema; finding fields; scope=systemic tag; AAA advisory pass; write report before exit; exit non-zero iff summary.aa_fail > 0
  - RESEARCH.md §Pattern 1 (AxeBuilder Two-Pass Per Page) — AA pass: withRules(['color-contrast']); AAA pass: withRules(['color-contrast-enhanced']).include(BODY_SELECTORS)
  - RESEARCH.md §Pattern 4 (describeScenario Loop Extension) — exact replacement of THEMES with THEME_MODES
  - RESEARCH.md §Clone Target Structure (Exact) — scenario groups D-01 indices 00–09 + D-02 supplement indices 10–13
  </read_first>

  <action>
  Create `examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` by cloning
  `admin_screenshot_matrix.spec.ts` wholesale, then applying exactly three categories of
  surgical changes per PATTERNS.md:

  CHANGE 1 — Theme type replacement (D-09):
  Remove `type Theme = "light" | "dark"` and `THEMES: Theme[]`. Replace with the
  discriminated union:
    `type ThemeMode = { kind: "explicit"; theme: "light" | "dark" } | { kind: "system"; colorScheme: "dark" }`
  Add `THEME_MODES: ThemeMode[]` with 3 entries (explicit light, explicit dark, system dark).
  Add `themeSlug(mode: ThemeMode): string` returning `system-${mode.colorScheme}` for
  system rows and `mode.theme` for explicit rows (D-09 flat slug).

  CHANGE 2 — shoot() → axeCheck() replacement (D-04/D-06/D-08/D-09/D-20):
  Replace the `screenshotDir` env var, `mkdir`, and `shoot()` function with:
  - `contrastReportDir`: from `process.env.CONTRAST_REPORT_DIR || "test-results/contrast"`
  - `BODY_SELECTORS` constant: `["main p", "main li", "main dd", "main dt",
    ".ops-text-body", ".ops-preflight__hint", ".ops-handoff__hint",
    ".ops-intent-card__summary"]` — covers body/long-form text per the brand book's
    AAA-body targets and RESEARCH.md §Open Questions A3
  - `assertSystemDarkInvariants(page)` function with three expect assertions per D-08:
    (1) `expect(page.locator("html")).not.toHaveAttribute("data-theme")`
    (2) `expect(await page.evaluate(() => window.matchMedia("(prefers-color-scheme: dark)").matches)).toBe(true)`
    (3) `expect(page.locator("html")).toHaveAttribute("data-theme-effective", "dark")`
  - `ContrastFinding` type with all D-18 schema fields: id, producer, severity, screen,
    theme, viewport, state, shot, element_role, selector, token_pair, fg, bg, actual_ratio,
    required_ratio, aaa_required, pass_aa, aaa_body_status, axe_rule, impact, fix_class,
    scope, evidence
  - `axeCheck(browser, capture, mode, viewport, findings)` function:
    - Creates context: system-dark → `{viewport, colorScheme: "dark"}`; explicit → `{viewport}`
    - Explicit rows: `context.addInitScript(...)` writes `["phx:theme", mode.theme]`
    - System-dark rows: NO addInitScript (D-07 prohibition)
    - After `capture.prepare(page)` (which internally calls waitForLiveConnected):
      if system-dark, call `assertSystemDarkInvariants(page)`
    - AA pass: `new AxeBuilder({page}).withRules(["color-contrast"]).analyze()`
    - Gate: `violations.length > 0` → accumulate aa-fail findings; `incomplete` is surfaced
      as advisory only (NEVER gates — D-04)
    - AAA pass: `new AxeBuilder({page}).withRules(["color-contrast-enhanced"])`
      with `.include(sel)` for each BODY_SELECTORS entry → advisory findings only
      (severity: "aaa-body-advisory", never increments aa_fail — D-20)
    - Map axe violation nodes to ContrastFinding schema; auto-tag `scope: "systemic"` when
      same selector fails on ≥3 distinct screens per D-19
    - Close context in finally block
  - `writeContrastReport(findings, scenario)`: builds scrypath.contrast.v1 JSON with
    `summary.aa_fail` count (aa-fail severity only) and `summary.aaa_advisory` count; writes
    JSON + MD to contrastReportDir per D-21 (write BEFORE returning)

  CHANGE 3 — Scenario groups (D-01 + D-02):
  Keep the 9 curated D-01 states (indices "00"–"09") VERBATIM from the screenshot spec.
  Add the 4 D-02 dark-risk supplement states:
  - index "10", screen "sync-drift", state "drift-detail": `prepare: gotoSyncDrift` (same
    page as index "03" but in a separate capture object for D-02's drift-chip muted-metadata
    target — the dark-risk is present on any sync-drift page)
  - index "11", screen "posture", state "healthy-detail": `prepare: gotoPosture` (all_green
    scenario provides the healthy/populated posture state)
  - index "12", screen "playbooks", state "populated": `prepare: gotoPlaybooks` (empty
    scenario seeds playbooks screen; if no saved items exist, the screen still shows muted
    metadata/UI — valid D-02 target)
  - index "13", screen "search", state "results-with-facets": `prepare: async (page) =>
    { await gotoSearch(page); await runSearch(page, "quantum"); await
    expect(page.getByRole("heading", { name: /results/i })).toBeVisible(); }` (same search
    result state as index "06" but explicitly captures facet/secondary text in the results)

  Distribute D-02 supplement states across scenarios matching their seed requirement:
  index "10" goes in incident scenario, index "11" + "13" go in all_green scenario,
  index "12" goes in empty scenario.

  describeScenario() loop: change `for (const theme of THEMES)` to
  `for (const mode of THEME_MODES)` and `await shoot(...)` to
  `await axeCheck(browser, capture, mode, viewport, findings)`. After all loops:
  call `await writeContrastReport(findings, scenario)` then assert
  `const aaFails = findings.filter(f => f.severity === "aa-fail").length; expect(aaFails).toBe(0)`.

  Keep UNCHANGED from the clone source: all six gotoXxx helpers + runSearch, ScreenCapture
  type, VIEWPORTS constant, VIEWPORT_NAMES constant, test.setTimeout(180_000), the
  drainSearchQueue + waitForSearchVisible guard in the all_green branch.

  Scope guard: admin_screenshot_matrix.spec.ts must NOT be touched. Zero CSS token changes.
  </action>

  <verify>
    <automated>cd examples/scrypath_ecommerce && npx playwright test e2e/admin_contrast_matrix.spec.ts --list 2>&1 | grep -c "contrast matrix"</automated>
  </verify>

  <acceptance_criteria>
  - File exists at `examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts`
  - `grep -q 'AxeBuilder' examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` exits 0
  - `grep -q 'assertSystemDarkInvariants' examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` exits 0
  - `grep -q 'colorScheme.*dark' examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` exits 0 (D-06 system-dark context)
  - `grep -q 'not.toHaveAttribute.*data-theme' examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` exits 0 (D-08 invariant 1)
  - `grep -q 'violations' examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` exits 0 (D-04 gate on violations[])
  - `grep -q 'incomplete' examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` exits 0 (incomplete surfaced as advisory)
  - `grep -q 'color-contrast-enhanced' examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` exits 0 (D-20 AAA pass)
  - `grep -q 'aaa-body-advisory' examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` exits 0 (D-20 severity)
  - `grep -q 'system-dark' examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` exits 0 (D-03/D-09 slug)
  - `grep -q '"10"' examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts` exits 0 (D-02 dark-risk supplement index)
  - `cd examples/scrypath_ecommerce && npx playwright test e2e/admin_contrast_matrix.spec.ts --list` lists the 3 scenario tests without error
  - `git diff --name-only examples/scrypath_ecommerce/e2e/admin_screenshot_matrix.spec.ts` returns empty (screenshot spec untouched — success criterion #4)
  </acceptance_criteria>

  <done>admin_contrast_matrix.spec.ts created with full D-09 discriminated union, D-08 system-dark invariants, D-04/D-20 two-pass axe, D-02 dark-risk supplement; screenshot spec untouched</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  The full contrast gate harness is assembled. All four Wave 0 deliverables are in place:
  @axe-core/playwright installed, contrast-pairs.mjs manifest, contrast-checker.mjs with
  --self-test, admin_contrast_matrix.spec.ts. The Makefile has contrast + contrast-matrix
  targets. DESIGN-TOKENS.md has the registry pointer.

  Before running the live full matrix, the dev server must be running and seeded. This
  checkpoint confirms the gate infrastructure is working and captures the first run's
  contrast report for committing as 128-CONTRAST-REPORT.md.
  </what-built>

  <how-to-verify>
  **Pre-flight (fast, under 10s):**
  1. `cd examples/scrypath_ecommerce && node contrast-checker.mjs --self-test`
     Expected: prints "self-test passed" and exits 0.
  2. `make contrast`
     Expected: runs under 1 second, prints verdict table, exits 0 or 1 (both valid — if
     token pairs fail AA it exits 1; that is correct and expected behavior showing the
     gate is live and measuring real values).

  **Full matrix run (requires running dev server, ~2–4 min):**
  3. Ensure the ecommerce dev server is running with the ops path-dep built:
     `cd examples/scrypath_ecommerce && make up` (containerized) OR
     `make dev` + `make infra` (source-level) in a separate terminal.
  4. `make contrast-matrix` (or `npm run test:e2e:admin-contrast`)
     Expected: runs ~78 axe passes across 3 scenarios; exits 0 or 1. If it exits 1,
     that means real AA failures were found — this is the phase's goal ("learn the real
     failure set"). Record the result.
  5. Confirm the JSON report was written:
     `cat test-results/contrast/contrast-report.json | head -20`
     Expected: valid JSON with `{"schema": "scrypath.contrast.v1", "summary": {...}}`

  **Regression smoke (success criterion #4):**
  6. `npm run test:e2e:admin-matrix`
     Expected: 40 screenshots captured, all pass. If this fails, something in the new
     spec file accidentally imported or modified the screenshot spec — investigate.

  **After verification, commit the curated report:**
  Copy `test-results/contrast/contrast-report.md` to
  `.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md`
  (edit for readability if desired — this is the committed evidence phases 129/132 consume).
  </how-to-verify>

  <resume-signal>
  Type "approved" once pre-flight self-test and make contrast pass, and the full matrix
  has run (even if it exits 1 — AA failures are expected and correct). Include:
  - Self-test result: passed/failed
  - make contrast exit code: 0 or 1, and what the verdict table showed
  - Full matrix exit code: 0 or 1, and summary.aa_fail count from the JSON report
  - Screenshot matrix result: pass/fail, screenshot count

  OR describe any issues found.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Commit 128-CONTRAST-REPORT.md as curated phase evidence</name>
  <files>.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md</files>

  <read_first>
  - test-results/contrast/contrast-report.md — the generated markdown from the full matrix run (produced in the checkpoint); this is the source to promote
  - test-results/contrast/contrast-report.json — the machine-readable source of truth; extract summary stats
  - .planning/milestones/v1.33-phases/120-per-touchpoint-audit/120-AUDIT-BACKLOG.md — the format precedent; the committed report should have a similar summary table structure (ID, touchpoint, dim, score/ratio, severity, selector, fix_class, scope columns)
  - CONTEXT.md D-17/D-18/D-19 — schema fields to include in the curated report; fix_class and scope pre-seeding for phases 129/132; the `#1B2230` surface-2 gap should surface as the dark aa-fail cluster (becomes DARKAUDIT-01 finding #1 in phase 129)
  </read_first>

  <action>
  Create `.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md`
  as a curated markdown promotion of the gitignored run artifact per D-17.

  The file structure:
  - Header: Phase 128 contrast report, date, schema version `scrypath.contrast.v1`, run summary
  - Summary section: `summary.aa_fail` count, `summary.aaa_advisory` count, themes tested
    (light/dark/system-dark), viewports tested (390/1440), screen-states count
  - AA Failures table: columns — ID, screen, theme, viewport, state, selector, fg, bg,
    actual_ratio, required_ratio, fix_class, scope; sorted by scope (systemic first) then
    by screen. Include `incomplete` entries in a separate "Incomplete (unresolvable)" section
    for manual triage per D-04.
  - AAA Advisory table: columns — selector, screen, theme, actual_ratio; note these are
    advisory only and do not affect the gate
  - Token checker findings: any aa_fail findings from `make contrast` (fast checker output)
  - D-19 systemic cluster annotations: flag any (selector|token_pair) that fails on ≥3
    screens with `scope: systemic`; note "Likely #1B2230 surface-2 gap" for raised .ops-*
    surfaces in dark per CONTEXT.md D-19
  - Footer: note that raw artifacts are at `test-results/contrast/` (gitignored); this
    committed file is the single source for phases 129/132

  If the full matrix run from the checkpoint produced no AA failures (exit 0), note this
  explicitly: "No AA violations found at time of Phase 128 run. Gate is operational and
  ready for phases 130–132 to trigger it as tokens change." Still include the AAA advisory
  section if any advisory findings were found.

  Scope guard: this file is committed documentation. Zero changes to CSS, tokens, or
  component files.
  </action>

  <verify>
    <automated>test -f .planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md && grep -q "scrypath.contrast.v1" .planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md && echo "report exists and has schema tag"</automated>
  </verify>

  <acceptance_criteria>
  - File exists at `.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md`
  - File contains `scrypath.contrast.v1` (schema version tag)
  - File contains `summary.aa_fail` or an equivalent summary line showing the AA failure count
  - File contains a section for AA failures (even if the table is empty with "No AA violations found")
  - File contains a section for AAA advisory findings (even if empty)
  - File contains a note that raw artifacts are gitignored under `test-results/contrast/`
  - If any systemic findings exist: `grep -c "systemic" .planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md` returns >= 1
  - `git status .planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md` shows the file as a new tracked file (not gitignored)
  - `test-results/contrast/contrast-report.json` is gitignored (already covered by existing `test-results/` gitignore entry)
  </acceptance_criteria>

  <done>128-CONTRAST-REPORT.md committed in phase dir as the diffable evidence artifact phases 129/132 will consume; raw run artifacts remain gitignored.

  Final step — update 128-VALIDATION.md frontmatter: set `nyquist_compliant: true` and
  `wave_0_complete: true` once all tasks in this plan are confirmed green (self-test exits 0,
  make contrast runs, full matrix has run, 128-CONTRAST-REPORT.md is committed). Edit the
  frontmatter block at the top of
  `.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-VALIDATION.md`
  to replace both `false` values with `true`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright spec → local dev server | Browser automation drives http://127.0.0.1:$(WEB_PORT) (local only) |
| axe-core → rendered DOM | axe injects into the browser page to read computed styles; local dev only |
| contrast-report.json → phase dir | Curated promotion copies local file into .planning tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-04 | Information Disclosure | contrast-report.json written to test-results/ | accept | Local filesystem only; gitignored; no secrets or user data in contrast results (only CSS selector names and color ratios); dev-only tool |
| T-128-05 | Spoofing | D-08 invariants for system-dark | mitigate | Three explicit expect() assertions in assertSystemDarkInvariants() catch any future regression that stamps data-theme in system mode or where Playwright colorScheme emulation silently fails |
| T-128-06 | Tampering | admin_screenshot_matrix.spec.ts regression | mitigate | admin_screenshot_matrix.spec.ts must remain UNCHANGED (verified by git diff gate in acceptance criteria); contrast spec is additive only |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages in this plan; @axe-core/playwright was installed in Plan 01; no additional dependencies |

**No applicable ASVS categories:** This plan creates a Playwright test spec and commits a markdown report. No authentication, session management, user input handling, or data persistence pathways are introduced. The spec drives a local dev server only.
</threat_model>

<verification>
Phase-level gate (all three plans complete):

**Fast gate (no server needed, under 1 minute):**
- `cd examples/scrypath_ecommerce && node contrast-checker.mjs --self-test` → exits 0, prints "self-test passed"
- `make contrast` → runs, prints verdict table, report written to test-results/contrast/
- `make help` → shows both `contrast` and `contrast-matrix` targets

**Full gate (server required, ~5 minutes total):**
- `npm run test:e2e:admin-contrast` → runs ~78 axe passes; exits 0 or 1 (both valid)
- `npm run test:e2e:admin-matrix` → 40 screenshots captured, exits 0 (regression smoke)

**Evidence:**
- `.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md` exists and is committed
- `scrypath_ops/assets/css/contrast-pairs.mjs` exists with 13 entries
- `scrypath_ops/assets/css/DESIGN-TOKENS.md` contains "contrast-pairs.mjs" and "sRGB"
- `git diff --name-only scrypath_ops/assets/css/app.css` → empty (no CSS tokens modified)
- `git diff --name-only examples/scrypath_ecommerce/e2e/admin_screenshot_matrix.spec.ts` → empty (success criterion #4)
</verification>

<success_criteria>
1. `npm run test:e2e:admin-contrast` runs the full screen × {light, dark, system-dark} × scenario matrix and exits non-zero on any AA color-contrast violation (ROADMAP success criterion #1)
2. AAA (7:1) status for body/long-form text is reported without failing the build (ROADMAP success criterion #2)
3. `make contrast` grades every declared `--color-*` pair + documented muted alphas at AA/AAA with no browser (ROADMAP success criterion #3)
4. The existing 40-shot screenshot matrix still captures both themes cleanly — admin_screenshot_matrix.spec.ts is UNCHANGED (ROADMAP success criterion #4)
5. 128-CONTRAST-REPORT.md is committed in the phase dir as diffable evidence for phases 129/132
6. All six VALIDATION.md load-bearing proofs are satisfied: gate is live (exits 1 on #767676/#fff), compliant pair exits 0, golden math = 21.00, system-dark = media-query path (D-08 invariants pass), AAA advisory never affects exit code, 40-shot matrix intact
</success_criteria>

<output>
Create `.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-03-SUMMARY.md` when done
</output>
