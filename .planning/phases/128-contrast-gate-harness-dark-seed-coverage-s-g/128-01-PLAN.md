---
phase: 128-contrast-gate-harness-dark-seed-coverage-s-g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/scrypath_ecommerce/package.json
  - scrypath_ops/assets/css/contrast-pairs.mjs
autonomous: true
requirements:
  - CONTRAST-HARNESS-01

must_haves:
  truths:
    - "@axe-core/playwright 4.11.3 is installed as a devDependency in examples/scrypath_ecommerce"
    - "contrast-pairs.mjs exists beside DESIGN-TOKENS.md with 13 muted-alpha manifest entries (12 contrast-gated + 1 decorative)"
    - "test:e2e:admin-contrast npm script is wired to admin_contrast_matrix.spec.ts"
    - "The D-15 lockstep guard has a complete manifest to validate against"
  artifacts:
    - path: "scrypath_ops/assets/css/contrast-pairs.mjs"
      provides: "Muted-alpha text manifest (D-11); input to contrast-checker.mjs lockstep guards"
      exports: ["MUTED_PAIRS"]
    - path: "examples/scrypath_ecommerce/package.json"
      provides: "axe devDependency + test:e2e:admin-contrast script"
      contains: "@axe-core/playwright"
  key_links:
    - from: "examples/scrypath_ecommerce/contrast-checker.mjs"
      to: "scrypath_ops/assets/css/contrast-pairs.mjs"
      via: "dynamic import (path.resolve(__dirname, ../../scrypath_ops/assets/css/contrast-pairs.mjs))"
      pattern: "contrast-pairs\\.mjs"
    - from: "examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts"
      to: "@axe-core/playwright"
      via: "import { AxeBuilder } from '@axe-core/playwright'"
      pattern: "AxeBuilder"
---

<objective>
Install the @axe-core/playwright devDependency, create the muted-alpha manifest
(contrast-pairs.mjs), and wire the test:e2e:admin-contrast npm script. These three
artifacts are the Wave 1 foundation that Plans 02 and 03 consume.

Purpose: All downstream work — the token checker's D-15 lockstep guard (Plan 02) and
the axe contrast spec (Plan 03) — depends on the manifest existing and the axe package
being installed. This plan has no runtime dependencies and can execute immediately.

Output:
- @axe-core/playwright 4.11.3 in package.json devDependencies + node_modules installed
- scrypath_ops/assets/css/contrast-pairs.mjs — 13-entry muted manifest (D-11)
- package.json test:e2e:admin-contrast script pointing to e2e/admin_contrast_matrix.spec.ts
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTEXT.md
@.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-RESEARCH.md
@.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @axe-core/playwright and add test:e2e:admin-contrast npm script</name>
  <files>examples/scrypath_ecommerce/package.json</files>

  <read_first>
  - examples/scrypath_ecommerce/package.json — current scripts block (lines 5–11) and devDependencies block; follow the exact existing `test:e2e:admin-matrix` script shape
  - RESEARCH.md §Standard Stack — @axe-core/playwright 4.11.3, no postinstall script, verified on npm registry
  - PATTERNS.md §package.json (config, modify) — exact diff: add `test:e2e:admin-contrast` script + `@axe-core/playwright` devDependency
  </read_first>

  <action>
  Run `npm install --save-dev @axe-core/playwright@4.11.3` from `examples/scrypath_ecommerce`
  to install the package and update package.json devDependencies. The package is approved
  per RESEARCH.md Package Legitimacy Audit (official Dequelabs monorepo, no postinstall script).

  Then verify or add the `test:e2e:admin-contrast` script entry to the `scripts` block in
  `examples/scrypath_ecommerce/package.json`:
    `"test:e2e:admin-contrast": "playwright test e2e/admin_contrast_matrix.spec.ts"`

  Follow the existing sibling pattern exactly (per D-16, PATTERNS.md §package.json):
  existing `"test:e2e:admin-matrix": "playwright test e2e/admin_screenshot_matrix.spec.ts"`.
  The new script differs only in the spec file name.

  Do NOT change any other scripts or dependencies. The @playwright/test version already
  present must remain unchanged.

  Scope guard (this phase is measurement-only): this task changes ZERO CSS tokens, .ops-*
  classes, or design-system files.
  </action>

  <verify>
    <automated>cd examples/scrypath_ecommerce && node -e "require('./package.json').devDependencies['@axe-core/playwright']" && echo "dep present" && grep -q '"test:e2e:admin-contrast"' package.json && echo "script present"</automated>
  </verify>

  <acceptance_criteria>
  - `package.json` devDependencies contains `"@axe-core/playwright": "^4.11.3"` (or exact version pinned by npm install)
  - `node_modules/@axe-core/playwright` directory exists
  - `package.json` scripts block contains `"test:e2e:admin-contrast": "playwright test e2e/admin_contrast_matrix.spec.ts"`
  - No other scripts, devDependencies, or dependencies changed
  - `node -e "const {AxeBuilder} = require('@axe-core/playwright'); console.log(typeof AxeBuilder)"` prints `function`
  </acceptance_criteria>

  <done>@axe-core/playwright installed, test:e2e:admin-contrast script wired; downstream plans can import AxeBuilder</done>
</task>

<task type="auto">
  <name>Task 2: Create contrast-pairs.mjs muted-alpha manifest (D-11)</name>
  <files>scrypath_ops/assets/css/contrast-pairs.mjs</files>

  <read_first>
  - scrypath_ops/assets/css/app.css — lines 431, 479, 537, 691, 711, 727, 746, 833, 898, 985, 1115, 1120, 1144 (the exact `color: color-mix(in oklch, var(--color-base-content) NN%, transparent)` lines); confirms selector, alpha%, and CSS property type (color vs border vs background)
  - PATTERNS.md §contrast-pairs.mjs (config, transform) — the complete module shape, all 13 entries with selector/alpha/fg_token/bg_token/role/note; the D-11 spec including the `MUTED_PAIRS` export name; the decorative exclusion for `.ops-trail__sep`
  - CONTEXT.md D-11 — manifest references TOKEN NAMES not hex; alpha cases only; the surface each token composites over
  - CONTEXT.md D-15 — the checker's lockstep guard will grep app.css for `color:` property uses of base-content alpha mixes and assert every (alpha%, selector) appears in this manifest
  - scrypath_ops/assets/css/DESIGN-TOKENS.md — sibling file; confirm directory; do NOT modify in this plan (that is Plan 02 Task 3)
  </read_first>

  <action>
  Create `scrypath_ops/assets/css/contrast-pairs.mjs` as a new file. This file has no code
  analog — it is the first .mjs in that directory. It lives beside DESIGN-TOKENS.md.

  The file exports a single named export `MUTED_PAIRS` — an array of manifest entries. Each
  entry has fields: `selector` (string, exact CSS selector from app.css), `alpha` (decimal
  0–1, e.g. 0.55 for 55%), `fg_token` (string, token name without `--color-` prefix),
  `bg_token` (string, token name without `--color-` prefix), `role` (one of: "text" → AA
  4.5/AAA 7.0; "large" → AA 3.0; "ui" → AA 3.0; "decorative" → skipped by checker),
  and `note` (human-readable description).

  Include ALL 13 entries from PATTERNS.md §contrast-pairs.mjs:
  1. `.ops-text-meta` — alpha 0.55, bg base-100, role text
  2. `.ops-trail__crumb` — alpha 0.60, bg base-100, role text
  3. `.ops-handoff__eyebrow` — alpha 0.50, bg base-100, role large (uppercase, font-weight 700)
  4. `.ops-handoff__hint` — alpha 0.60, bg base-100, role text
  5. `.ops-preflight__hint` — alpha 0.60, bg base-100, role text
  6. `.ops-intent-card__summary` — alpha 0.75, bg base-100, role text
  7. `.ops-tone-chip__value` — alpha 0.75, bg base-100, role text
  8. `.ops-badge-neutral` — alpha 0.82, bg base-200, role text
  9. `.ops-signal-table th[scope="row"]` — alpha 0.78, bg base-100, role text
  10. `.ops-cmdk__item-hint` — alpha 0.55, bg base-100, role text
  11. `.ops-cmdk__empty` — alpha 0.55, bg base-100, role text
  12. `.ops-cheatsheet__row dd` — alpha 0.70, bg base-100, role text
  13. `.ops-trail__sep` — alpha 0.35, bg base-100, role "decorative" (NOT contrast-gated)

  Add a file-level comment block explaining: (1) references token names not hex (hex lives
  in app.css per D-10); (2) alpha compositing is sRGB: out = fg·α + bg·(1−α) per channel
  per D-12; (3) D-15 lockstep guard in contrast-checker.mjs validates that every
  `color: color-mix(...)` text occurrence in app.css appears in this manifest.

  Scope guard: this file is a data manifest only. Zero changes to app.css, DESIGN-TOKENS.md,
  or any --color-* token values.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "import('./scrypath_ops/assets/css/contrast-pairs.mjs').then(m => { const p = m.MUTED_PAIRS; console.assert(p.length === 13, 'expected 13 entries, got ' + p.length); console.assert(p.some(e => e.selector === '.ops-text-meta' && e.alpha === 0.55), 'missing ops-text-meta'); console.assert(p.some(e => e.selector === '.ops-trail__sep' && e.role === 'decorative'), 'missing decorative sep'); console.log('contrast-pairs OK, entries:', p.length); })"</automated>
  </verify>

  <acceptance_criteria>
  - File exists at `scrypath_ops/assets/css/contrast-pairs.mjs`
  - Exports `MUTED_PAIRS` as a named export (ESM)
  - Array has exactly 13 entries
  - Entry for `.ops-trail__sep` has `role: "decorative"`
  - Entry for `.ops-badge-neutral` has `bg_token: "base-200"` (not base-100 — it composites over the badge background)
  - Entry for `.ops-handoff__eyebrow` has `role: "large"` (uppercase bold qualifies as large text)
  - All fg_token values are `"base-content"`; all alpha values are decimals in range (0, 1)
  - No hex color values appear in this file (token names only — D-10)
  - File contains no `--color-*` CSS declarations, no import of app.css, no Node built-in imports
  - `grep -c "\"decorative\"" scrypath_ops/assets/css/contrast-pairs.mjs` returns 1
  </acceptance_criteria>

  <done>contrast-pairs.mjs manifest exists with all 13 muted-alpha entries; D-15 lockstep guard has complete input; contrast-checker.mjs (Plan 02) can import it</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | New devDependency (@axe-core/playwright) fetched from npm registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering | npm install of @axe-core/playwright | accept | Package is [VERIFIED: npm registry] — official Dequelabs monorepo (dequelabs/axe-core-npm), 5 years on registry, no postinstall script; devDependency only (not in production bundle); no elevated risk beyond standard npm supply chain |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | @axe-core/playwright is the only new package; verified as official Dequelabs package with no postinstall script; no [ASSUMED] or [SUS] packages in this plan |

**No applicable ASVS categories:** This plan installs a devDependency test tool and creates a static data manifest (`.mjs` with no I/O, no network, no user input). No authentication, session management, user input handling, or data persistence pathways are introduced or modified. The contrast-pairs.mjs manifest is read-only structured data with no executable logic.
</threat_model>

<verification>
After both tasks complete:
- `cd examples/scrypath_ecommerce && node -e "const {AxeBuilder} = require('@axe-core/playwright'); console.log('AxeBuilder:', typeof AxeBuilder)"` prints `AxeBuilder: function`
- `grep '"@axe-core/playwright"' examples/scrypath_ecommerce/package.json` returns a match
- `grep '"test:e2e:admin-contrast"' examples/scrypath_ecommerce/package.json` returns a match
- `node --input-type=module -e "import { MUTED_PAIRS } from './scrypath_ops/assets/css/contrast-pairs.mjs'; console.log(MUTED_PAIRS.length)"` prints 13
- `git diff --name-only` shows exactly `examples/scrypath_ecommerce/package.json`, `examples/scrypath_ecommerce/package-lock.json`, and `scrypath_ops/assets/css/contrast-pairs.mjs` (no CSS token files, no app.css, no component files)
</verification>

<success_criteria>
- @axe-core/playwright 4.11.3 installed as devDependency, importable as `{ AxeBuilder }`
- `test:e2e:admin-contrast` npm script present and pointing to the correct spec path
- `contrast-pairs.mjs` exists with 13 entries, correct token names (not hex), correct roles including `decorative` for `.ops-trail__sep` and `large` for `.ops-handoff__eyebrow`
- Zero changes to scrypath_ops CSS tokens, app.css, or any design-system files
</success_criteria>

<output>
Create `.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-01-SUMMARY.md` when done
</output>
