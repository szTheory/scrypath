---
phase: 49-visual-hierarchy-theming-and-phoenix-ergonomics
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/assets/css/app.css
  - scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
  - scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex
autonomous: true
requirements:
  - OPSUX-04
user_setup: []
must_haves:
  truths:
    - `system` / `light` / `dark` storage and `data-theme` contract unchanged (`phx:theme` key only).
    - Tailwind `dark:` behavior under `system` OS dark is documented in-repo via `app.css` comment + variant definition.
  artifacts:
    - `theme_toggle` visual knob matches effective theme including `system` + OS preference.
  key_links:
    - `live_title` suffix is not stock Phoenix marketing on operator-facing tabs.
---

<objective>
Close **OPSUX-04** gaps from **49-CONTEXT D-04–D-09** and **D-16**: (1) document and implement **one coherent strategy** for **`@custom-variant dark`** vs **system** dark (**D-07**); (2) fix **`theme_toggle`** so the sliding indicator reflects **effective** appearance in **`system`** mode (**D-08**); (3) replace **`live_title`** suffix **` · Phoenix Framework`** with **` · ScrypathOps`** in **`root.html.heex`** (or equivalent product string) so browser tabs match operator product (**D-16**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-CONTEXT.md
@.planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-UI-SPEC.md
@scrypath_ops/assets/css/app.css
@scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
@scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex
</context>

<threat_model>
**Trust boundaries:** Static layout + CSS; inline first-party script in **`root.html.heex`**.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | XSS | Inline theme script | Mitigated | Script only reads **`localStorage`** / **`dataset`**; no **`innerHTML`**; keep CSP note in CONTEXT **D-10** as doc-only unless CSP lands. |
| T-49-04 | Privacy | `matchMedia` | Accepted (low) | Browser API for theme UI only; no exfiltration. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend or document @custom-variant dark for system coherence</name>
  <files>scrypath_ops/assets/css/app.css</files>
  <read_first>scrypath_ops/assets/css/app.css, .planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-CONTEXT.md</read_first>
  <action>Immediately above **`@custom-variant dark`**, add a **5–15 line comment block** explaining: when **`data-theme` is absent** (`system`), daisyUI uses **`prefers-color-scheme`** via vendored theme plugins; **`dark:`** utilities apply only when the **`@custom-variant dark`** selector matches. **Either** (A) extend **`@custom-variant dark`** so Tailwind **`dark:`** matches **(`[data-theme=dark]` OR (`prefers-color-scheme: dark` AND NOT `[data-theme=light]`))** using valid **Tailwind CSS v4** variant syntax per upstream docs, **or** (B) keep the variant as-only-`data-theme=dark` and state explicitly that operator chrome must use **daisyUI semantic tokens** (`base-*`, `alert-*`) for **system** dark — pick **one** and implement. If (A), update the **`@custom-variant dark`** line accordingly. If (B), do **not** change the variant line; instead comment **`# dark: utilities` are intentionally explicit-dark only**.</action>
  <verify>cd scrypath_ops && mix assets.build 2>/dev/null || (cd scrypath_ops && mix compile)</verify>
  <acceptance_criteria>
    - `grep -q 'custom-variant dark' scrypath_ops/assets/css/app.css` exits 0.
    - `grep -q 'system' scrypath_ops/assets/css/app.css` exits 0 OR `grep -q 'prefers-color-scheme' scrypath_ops/assets/css/app.css` exits 0.
  </acceptance_criteria>
  <done>CSS builds; strategy is documented in `app.css` comments.</done>
</task>

<task type="auto">
  <name>Task 2: Fix theme_toggle indicator for system + light + dark</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/layouts.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/components/layouts.ex, scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex</read_first>
  <action>Update **`theme_toggle/1`** so the sliding pill position reflects **effective** theme: when **`document.documentElement` has no `data-theme`**, treat as **system** and use **`window.matchMedia('(prefers-color-scheme: dark)')`** to choose **light vs dark visual bucket** for the knob. **Minimal implementation acceptable:** add a tiny inline **`phx:hook`**-free approach by extending the **existing** **`root.html.heex`** head script to set a **`data-theme-effective`** attribute on **`<html>`** (or a class on a wrapper inside toggle only — prefer not to fork theme contract) **OR** use **`phx-update="ignore"`** wrapper with **`id="theme-toggle"`** and DOM patched from the head script on `storage` + `change` media events. Preserve **`phx:set-theme`** dispatch contract and **`phx:theme`** localStorage key names (**D-04**).</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'theme_toggle' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` exits 0.
    - `grep -q 'matchMedia' scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex` exits 0 OR `grep -q 'matchMedia' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` exits 0.
  </acceptance_criteria>
  <done>Toggle reflects system-dark vs system-light in manual smoke.</done>
</task>

<task type="auto">
  <name>Task 3: live_title suffix for ScrypathOps</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex</read_first>
  <action>In **`<.live_title>`**, change **`suffix=" · Phoenix Framework"`** to **`suffix=" · ScrypathOps"`** (exact ASCII middle dot character as today).</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep 'ScrypathOps' scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex` exits 0.
    - `grep 'Phoenix Framework' scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex` exits 1.
  </acceptance_criteria>
  <done>Tab titles use ScrypathOps suffix.</done>
</task>

</tasks>

<verification>
- [ ] `cd scrypath_ops && mix compile` exits 0
- [ ] `grep 'Phoenix Framework' scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex` exits 1
</verification>

<success_criteria>

- **D-07** strategy is explicit in **`app.css`**.
- **D-08** effective theme is reflected in **`theme_toggle`** UI.
- **D-16** stock Phoenix **`live_title`** suffix removed from **`root.html.heex`**.

</success_criteria>

---

## PLANNING COMPLETE
