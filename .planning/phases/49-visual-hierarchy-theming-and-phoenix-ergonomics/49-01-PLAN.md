---
phase: 49-visual-hierarchy-theming-and-phoenix-ergonomics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex
  - scrypath_ops/lib/scrypath_ops_web.ex
  - scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
autonomous: true
requirements:
  - OPSUX-03
user_setup: []
must_haves:
  truths:
    - `/ops` pages can compose a shared header/panel scaffold from `OpsUi` function components.
    - `Layouts.app` `:ops` supports a documented main width variant without breaking default `max-w-3xl`.
  artifacts:
    - `scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` exists and compiles.
  key_links:
    - LiveViews import `OpsUi` helpers via `ScrypathOpsWeb` `:live_view` html helpers.
---

<objective>
Introduce **`ScrypathOpsWeb.OpsUi`** function components (**`ops_page_header`**, **`ops_panel`**, optional **`ops_scaffold`**) per **49-CONTEXT D-01** and **`49-UI-SPEC`**, wire **`import ScrypathOpsWeb.OpsUi`** into **`html_helpers/0`**, and extend **`Layouts.app/1` (`shell: :ops`)** with **`attr :ops_main_width, :atom, default: :default`** where **`:default`** keeps **`max-w-3xl`** and **`:wide`** uses **`max-w-7xl mx-auto w-full`** (or equivalent from UI-SPEC) on the inner `main` wrapper (**D-02**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-CONTEXT.md
@.planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-UI-SPEC.md
@.planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-RESEARCH.md
@scrypath_ops/lib/scrypath_ops_web.ex
@scrypath_ops/lib/scrypath_ops_web/components/core_components.ex
@scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
</context>

<threat_model>
**Trust boundaries:** Maintainer-authored HEEx and layout code only; read-only **`/ops`** UI.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | `layouts.ex` / `OpsUi` | Mitigated | No new user-controlled HTML attributes; keep **`protect_from_forgery`** on **`/ops`** unchanged; code review + **`mix test`**. |
| T-49-02 | XSS | HEEx templates | Mitigated | Use HEEx `{}` interpolation only; no **`raw/1`** on user input (none introduced in this plan). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add OpsUi module with scaffold function components</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/components/core_components.ex, .planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-UI-SPEC.md, .planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-CONTEXT.md</read_first>
  <action>Create **`scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex`** with **`use ScrypathOpsWeb, :html`**. Define **`def ops_page_header/1`** accepting **`assigns` with `:title`** (string) and optional **`:subtitle`**, rendering a single **`<h1 class="text-2xl font-semibold leading-8 tracking-tight">`** for the title (per UI-SPEC typography) and optional subtitle in **`text-sm text-base-content/70`**. Define **`def ops_panel/1`** with **`slot :inner_block, required: true`** wrapping children in **`border border-base-300 rounded-lg bg-base-100 p-4`** (flat panel per **D-12**). Optionally define **`ops_scaffold/1`** that composes **`ops_page_header`** + **`ops_panel`** for the primary stack, or document that LiveViews compose the two — either is acceptable if **one visible `<h1>`** pattern is preserved. Export only function components (no LiveView).</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - Path `scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` exists.
    - `grep -q 'def ops_page_header' scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` exits 0.
    - `grep -q 'def ops_panel' scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` exits 0.
    - `grep -q 'border-base-300' scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` exits 0.
  </acceptance_criteria>
  <done>`mix compile` succeeds with new module.</done>
</task>

<task type="auto">
  <name>Task 2: Import OpsUi in html_helpers</name>
  <files>scrypath_ops/lib/scrypath_ops_web.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web.ex</read_first>
  <action>In **`html_helpers/0`** quoted block, after **`import ScrypathOpsWeb.CoreComponents`**, add **`import ScrypathOpsWeb.OpsUi`** so all **`use ScrypathOpsWeb, :live_view`** templates can call **`<.ops_page_header>`**, **`<.ops_panel>`**, etc., without per-file imports.</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'import ScrypathOpsWeb.OpsUi' scrypath_ops/lib/scrypath_ops_web.ex` exits 0.
  </acceptance_criteria>
  <done>OpsUi functions resolve in LiveView modules.</done>
</task>

<task type="auto">
  <name>Task 3: ops_main_width attr on :ops Layouts.app</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/layouts.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/components/layouts.ex, .planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-CONTEXT.md</read_first>
  <action>Add **`attr :ops_main_width, :atom, default: :default`** to **`def app(%{shell: :ops} = assigns)`** (Phoenix **1.7+ `attr` on function components**). Replace the fixed inner **`div`** classes **`mx-auto max-w-3xl`** with a **`cond`** or **`case @ops_main_width`** branch: **`:default`** → **`mx-auto max-w-3xl w-full min-w-0`**, **`:wide`** → **`mx-auto max-w-7xl w-full min-w-0`**. Document in **`@doc`** that LiveViews pass **`ops_main_width={:wide}`** for table-first surfaces (**Search** in Plan 03).</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'ops_main_width' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` exits 0.
    - `grep -q 'max-w-7xl' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` exits 0.
    - `grep -q 'max-w-3xl' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` exits 0.
  </acceptance_criteria>
  <done>Ops shell compiles; width variant is opt-in via assign.</done>
</task>

</tasks>

<verification>
- [ ] `cd scrypath_ops && mix compile` exits 0
- [ ] `grep -q 'OpsUi' scrypath_ops/lib/scrypath_ops_web.ex` exits 0
</verification>

<success_criteria>

- **`OpsUi`** provides **`ops_page_header`** and **`ops_panel`** (and optional **`ops_scaffold`**) aligned with **49-UI-SPEC** hierarchy.
- **`:ops`** layout supports **`:default`** and **`:wide`** inner main widths.

</success_criteria>

---

## PLANNING COMPLETE
