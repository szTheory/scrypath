---
phase: 49-visual-hierarchy-theming-and-phoenix-ergonomics
plan: "04"
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs
  - scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
  - scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
  - scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex
  - scrypath_ops/lib/scrypath_ops_web/live/search_live.ex
autonomous: true
requirements:
  - OPSUX-05
user_setup: []
must_haves:
  truths:
    - Structural tests prove `/ops` LiveViews use `shell={@shell}` and assign `page_title`.
    - No `operator_ia_contract_test` duplication for nav labels (Phase 48 owns IA).
  artifacts:
    - `ops_shell_contract_test.exs` exists and passes.
  key_links:
    - `mix test` green for scrypath_ops app.
---

<objective>
Deliver **OPSUX-05** verification and Phoenix polish: add **`scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs`** asserting **structural** contracts only (**D-19**) — each ops LiveView module’s **`render/1`** template (or compiled module source) contains **`shell={@shell}`**, assigns **`page_title`** in **`mount/3`**, and uses **`<.link navigate={~p`** for internal **`/ops`** links (sample with **`grep`-style assertions in ExUnit** by reading **`@template`** is not available; instead **`mix test`** using **`Phoenix.LiveViewTest`** **`live/3`** + **`element?`** or **string contains** on rendered HTML for **`data-phx-session`** pages). Pragmatic approach: **live_conn** tests that **`GET /ops/posture`** etc. return **200** and HTML contains **`ops_page_header`** marker class or **`flash-group`** id once; **`grep` forbidden inside ExUnit** — use **`assert html =~`** patterns. Additionally **`grep`** the codebase in a **manual verification** checklist task: ensure no **`live_redirect`** / **`live_patch`** in touched LiveViews (**D-18**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-CONTEXT.md
@scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs
@scrypath_ops/lib/scrypath_ops_web/router.ex
</context>

<threat_model>
**Trust boundaries:** Test-only additions; no auth bypass.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-06 | False assurance | Tests | Mitigated | Tests assert presence of structural markers, not security properties; keep **`OPSUI_AUTH_MODE`** tests untouched. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add ops_shell_contract_test.exs</name>
  <files>scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs</files>
  <read_first>scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs, scrypath_ops/test/support/conn_case.ex, scrypath_ops/lib/scrypath_ops_web/router.ex</read_first>
  <action>Create **`scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs`** using **`ScrypathOpsWeb.ConnCase`**, **`import Phoenix.LiveViewTest`**. For each path **`~p"/ops/posture"`**, **`~p"/ops/failed-sync"`**, **`~p"/ops/sync-drift"`**, **`~p"/ops/search"`**, **`{:ok, view, html} = live(conn, path)`** (adjust to your **`ConnCase`** setup), then assert **`html =~ "flash-group"`** OR **`html =~ "id=\"flash-group\""`** (exactly **one** flash region per **49-CONTEXT D-17** — if duplicate appears, test should fail). Assert **`html =~ "data-phx-session"`** or LiveView root marker. Assert page-specific marker after Plan 03: e.g. **`html =~ "ops_page_header"`** is **not** required if component emits no literal string — instead assert **`html =~ "Posture / health"`** text content for posture route, **`Search & federation`** for search, etc. **Do not** assert **`Nav.primary`** label ordering. Add **`@moduletag :integration`** only if existing suite uses tags.</action>
  <verify>cd scrypath_ops && mix test test/scrypath_ops_web/ops_shell_contract_test.exs</verify>
  <acceptance_criteria>
    - File `scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs` exists.
    - `cd scrypath_ops && mix test test/scrypath_ops_web/ops_shell_contract_test.exs` exits 0.
    - Test file contains **`/ops/posture"`** and **`/ops/search"`** substrings at minimum.
  </acceptance_criteria>
  <done>New contract tests pass locally.</done>
</task>

<task type="auto">
  <name>Task 2: Navigation + deprecated LiveView API grep</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/*.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex, scrypath_ops/lib/scrypath_ops_web/live/search_live.ex</read_first>
  <action>Run ripgrep from repo root: **`rg 'live_redirect|live_patch' scrypath_ops/lib/scrypath_ops_web/live/`** — if matches exist in ops LiveViews, replace with **`push_navigate`** / **`push_patch`** per **D-18**. For internal **`href={`** to **`/ops/`**, prefer **`~p`** links — fix only if introduced in Phase 49 edits or obvious one-line fixes in touched files.</action>
  <verify>cd scrypath_ops && mix test</verify>
  <acceptance_criteria>
    - `rg 'live_redirect|live_patch' scrypath_ops/lib/scrypath_ops_web/live/` exits 1 (no matches) OR documented exception comment with follow-up only if upstream blocks.
  </acceptance_criteria>
  <done>No deprecated LiveView navigation in ops LiveViews.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite + flash_group count sanity</name>
  <files>scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/components/layouts.ex</read_first>
  <action>Extend **`ops_shell_contract_test`** (or fold into Task 1) so rendered HTML for **`/ops/posture`** contains the substring **`id=\"flash-group\"`** exactly once: e.g. **`assert Enum.count(String.split(html, "id=\\\"flash-group\\\""), &(&1 != \"\")) == 2`** after splitting, or normalize with a small helper — avoid adding **`floki`** unless the project already depends on it. Run **`cd scrypath_ops && mix test`** full.</action>
  <verify>cd scrypath_ops && mix test</verify>
  <acceptance_criteria>
    - `cd scrypath_ops && mix test` exits 0.
  </acceptance_criteria>
  <done>CI-local full test green.</done>
</task>

</tasks>

<verification>
- [ ] `cd scrypath_ops && mix test` exits 0
- [ ] Manual: toggle **system/light/dark** on **`/ops/search`** after Plans 02–03 (record in SUMMARY during execute-phase)
</verification>

<success_criteria>

- Structural **`/ops`** tests exist and pass without duplicating Phase **48** IA assertions.
- Deprecated **`live_redirect` / `live_patch`** removed from ops LiveViews where present.

</success_criteria>

---

## PLANNING COMPLETE
