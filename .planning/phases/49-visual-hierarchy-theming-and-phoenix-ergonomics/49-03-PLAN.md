---
phase: 49-visual-hierarchy-theming-and-phoenix-ergonomics
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
  - scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
  - scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex
  - scrypath_ops/lib/scrypath_ops_web/live/search_live.ex
autonomous: true
requirements:
  - OPSUX-03
user_setup: []
must_haves:
  truths:
    - All four `/ops` LiveViews compose `ops_page_header` + `ops_panel` (or equivalent scaffold) with one primary `<h1>` per page.
    - Search uses `ops_main_width={:wide}` on `Layouts.app`.
  artifacts:
    - Primary CTAs and empty-state copy follow **49-UI-SPEC** verb+object pattern where applicable.
  key_links:
    - No changes to `Nav.primary/0` order or labels (Phase 48).
---

<objective>
Refactor **`PostureLive`**, **`FailedSyncLive`**, **`SyncDriftLive`**, and **`SearchLive`** to use **`OpsUi`** scaffold components (**OPSUX-03**), apply **density** patterns (**`text-sm`**, **`tabular-nums`**, **`overflow-x-auto`**, **`min-w-0`**) per **D-13**, align **severity → `alert-*`** mapping per **D-11**, update **primary button labels** and **empty-state** headings/bodies per **`49-UI-SPEC` Copywriting Contract**, and pass **`ops_main_width={:wide}`** from **`SearchLive`** **`Layouts.app`** open tag (**D-02**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-CONTEXT.md
@.planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-UI-SPEC.md
@scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
@scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
@scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex
@scrypath_ops/lib/scrypath_ops_web/live/search_live.ex
</context>

<threat_model>
**Trust boundaries:** Read-only operator UI; no new server endpoints.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-05 | Information disclosure | Error copy | Accepted (low) | Keep messages bounded to existing assign data; no stack traces in HEEx. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: PostureLive scaffold + copy alignment</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex, .planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-UI-SPEC.md, scrypath_ops/lib/scrypath_ops_web/components/layouts.ex</read_first>
  <action>Inside **`<Layouts.app …>`** `inner_block`, wrap the page body with **`<.ops_page_header title={…} />`** using the same string as **`assign(:page_title, …)`** (**`"Posture / health"`**). Remove any **extra** page-level **`<h1>`** in this template so the visible document title comes from **`ops_page_header`** (single focal **`<h1>`** in the rendered tree — verify in Plan 04 LiveView tests if needed). Wrap primary rollup / JTBD panel in **`<.ops_panel>`**. Rename the refresh **`button`** visible text to **`Refresh posture`** (exact substring). Apply **`alert-warning` / `alert-error` / `alert-info`** classes per **D-11** for posture banners where severity is already implied by assigns. Add **`text-sm tabular-nums`** to numeric / timestamp table cells if a posture table exists.</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'ops_page_header' scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex` exits 0.
    - `grep -q 'ops_panel' scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex` exits 0.
    - `grep -q 'Refresh posture' scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex` exits 0.
    - `grep '<h1' scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex` exits 1 (no raw `<h1` left in this module’s HEEx after header extraction).
  </acceptance_criteria>
  <done>Posture view compiles; single h1; CTA label updated.</done>
</task>

<task type="auto">
  <name>Task 2: FailedSyncLive + SyncDriftLive scaffold + tables</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex, scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex, scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex, .planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-UI-SPEC.md</read_first>
  <action>For each file: add **`<.ops_page_header>`** with title matching **`page_title`** assign (**`"Failed sync work"`** / **`"Sync / drift"`**). Remove stray duplicate titles. Wrap main tables in **`overflow-x-auto min-w-0`** container **`div`**. Set primary refresh buttons to **`Refresh failed sync jobs`** and **`Load / refresh contract drift`** respectively (exact strings from UI-SPEC). Use **`<.ops_panel>`** for the primary list or drift summary block. Apply **`text-sm leading-snug tabular-nums`** on **`<tbody>`** rows per **D-13**.</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'ops_page_header' scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex` exits 0.
    - `grep -q 'Refresh failed sync jobs' scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex` exits 0.
    - `grep -q 'ops_page_header' scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex` exits 0.
    - `grep -q 'Load / refresh contract drift' scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex` exits 0.
  </acceptance_criteria>
  <done>Both views compile; CTA strings match UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: SearchLive wide layout + playground card + empty copy</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/search_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/search_live.ex, scrypath_ops/lib/scrypath_ops_web/components/layouts.ex, .planning/phases/49-visual-hierarchy-theming-and-phoenix-ergonomics/49-UI-SPEC.md</read_first>
  <action>Change the root **`<Layouts.app`** invocation to **`Layouts.app flash={@flash} shell={@shell} ops_main_width={:wide}`**. Compose **`<.ops_page_header title="Search & federation" />`** (must match **`page_title`** assign). Wrap the playground controls + results in **`card bg-base-100 border border-base-300`** (or **`<.ops_panel>`** inside a **`card`** per **D-12** self-contained task surface). Update **`empty_or_hits_single/1`** empty copy: heading **`No hits for this query.`** and body mentioning widening filters and honesty panel per **49-UI-SPEC** (exact heading string required). Rename submit control to **`Run sample searches`** (exact substring visible to user). Ensure multi-index mode toggle labels remain clear.</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'ops_main_width={:wide}' scrypath_ops/lib/scrypath_ops_web/live/search_live.ex` exits 0.
    - `grep -q 'No hits for this query.' scrypath_ops/lib/scrypath_ops_web/live/search_live.ex` exits 0.
    - `grep -q 'Run sample searches' scrypath_ops/lib/scrypath_ops_web/live/search_live.ex` exits 0.
  </acceptance_criteria>
  <done>Search view compiles; wide layout opt-in; UI-SPEC strings present.</done>
</task>

</tasks>

<verification>
- [ ] `cd scrypath_ops && mix compile` exits 0
- [ ] `cd scrypath_ops && mix test test/scrypath_ops_web/live/search_live_test.exs` exits 0 if that file exists; otherwise `mix test test/scrypath_ops_web/` exits 0
</verification>

<success_criteria>

- Four **`/ops`** surfaces share scaffold vocabulary and readable density.
- **Search** uses **wide** main width; CTAs / empty states match **49-UI-SPEC** anchors.

</success_criteria>

---

## PLANNING COMPLETE
