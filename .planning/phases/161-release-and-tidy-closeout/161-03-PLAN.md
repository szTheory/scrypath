---
phase: 161-release-and-tidy-closeout
plan: 03
type: execute
wave: 2
depends_on: [161-01, 161-02]
files_modified:
  - .planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md
  - .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md
autonomous: false
requirements: [REL-01, CLOSE-01]
user_setup:
  - service: github
    why: "A maintainer must authorize a new milestone PR submission and any milestone or Release Please PR merge; GitHub Actions must be available for exact-SHA and release evidence."
    dashboard_config:
      - task: "Authorize the prepared milestone PR submission when absent, then review and merge the ready milestone and Release Please PRs under repository policy."
        location: "GitHub pull requests for szTheory/scrypath"
  - service: hex
    why: "The existing Release Please publish job requires its publisher-scoped credential and live Hex/HexDocs services."
    env_vars:
      - name: HEX_API_KEY
        source: "Existing GitHub Actions publisher secret; check presence/status only, never reveal its value"
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "An absent milestone PR has an exact title, body, and source SHA prepared locally, then is submitted only after explicit maintainer authorization; existing PRs and authorization refusals have recorded dispositions."
    - "Milestone and Release Please PRs have recorded review, triage, check, and merge dispositions, including an explicit empty result if no release PR exists."
    - "The changed candidate commit has a successful exact-SHA closeout with five required jobs and SHA-bound coverage and attestation artifacts."
    - "A release is called shipped only after the Release Please tag's Hex publish, versioned HexDocs/consumer check, and package-to-tag parity pass; release-ready requires a specific external blocker and resume action, never PR absence alone."
  artifacts:
    - path: .planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md
      provides: Candidate SHA, run, gate, audit, and advisory evidence
    - path: .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md
      provides: PR triage and published or release-ready disposition
  key_links:
    - from: candidate commit
      to: GitHub Actions ci.yml
      via: Exact full head SHA, required jobs, and digest-bearing SHA-bound artifacts
    - from: locally prepared milestone PR title/body/source SHA
      to: GitHub milestone PR
      via: Blocking maintainer authorization before gh pr create and renewed exact-SHA checks if the head moves
    - from: Release Please tag
      to: Hex and HexDocs
      via: Existing publish-hex job followed by verify.release_publish and verify.release_parity
---

<objective>
Prepare and attest the exact milestone candidate, obtain authorization before submitting a missing PR, then follow review and the existing maintainer-controlled Release Please path as far as external authorization and services allow.

Purpose: REL-01 requires review, green exact-commit checks, and real post-publish parity before a shipped claim. D-03 and D-04 define the published and release-ready endpoints.
Output: Updated validation record and an explicit release evidence record.
</objective>

## Artifacts this phase produces

This plan updates `161-VALIDATION.md` and creates `161-RELEASE-EVIDENCE.md` with PR, CI, and publication evidence. Plan 04 adds ownership cleanup and final-SHA disposition to these records.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/161-release-and-tidy-closeout/161-CONTEXT.md
@.planning/phases/161-release-and-tidy-closeout/161-RESEARCH.md
@.planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md
@.planning/phases/161-release-and-tidy-closeout/161-01-SUMMARY.md
@.planning/phases/161-release-and-tidy-closeout/161-02-SUMMARY.md
@CONTRIBUTING.md
@docs/releasing.md
@.github/workflows/ci.yml
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
@scripts/ci_monitor.cjs

REL-01 and CLOSE-01 spec-less probes left adjacency, empty, and ordering cases unresolved. Cover them concretely: require adjacent SHA/run/artifact IDs; record empty PR or release-run results; and enforce candidate checks before merge, Release Please version/tag before publish, publish before post-publish checks, and final-SHA checks after tracked evidence. No schema push or new product capability is in scope.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attest the candidate and prepare a missing milestone PR</name>
  <files>.planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md, .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md</files>
  <read_first>.planning/REQUIREMENTS.md; docs/releasing.md; CONTRIBUTING.md; .github/workflows/release-please.yml; scripts/ci_monitor.cjs; .planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md; .planning/phases/161-release-and-tidy-closeout/161-01-SUMMARY.md; .planning/phases/161-release-and-tidy-closeout/161-02-SUMMARY.md; .planning/phases/160-package-backed-phoenix-proof/160-03-SUMMARY.md</read_first>
  <action>For D-01, D-03, REL-01, and CLOSE-01, query open and recently closed milestone PRs and the Release Please PR with `gh`, including head/base refs, review decisions, required checks, merge state, and links. Inspect actual comments and unresolved actionable findings; record every matching PR's number and disposition, distinguish completed from pending review, and record explicit empty results without inventing review or approval. Finish and commit the docs and Mint candidate with a reviewed diff excluding pre-existing unrelated status entries. Capture its full CANDIDATE_SHA and run `scripts/ci_monitor.cjs closeout --push` on that exact branch/SHA; require five named required jobs, advisory coverage, closeout attestation, immutable artifact digests, and exact SHA. Inspect hosted deep-quality separately and require the three Mint advisories cleared, while keeping that lane advisory. Put commands, SHA, run URL/ID, job conclusions, artifact IDs/digests, and audit result in `161-VALIDATION.md`; reference the candidate in `161-RELEASE-EVIDENCE.md`. If no matching milestone PR exists, prepare a conventional `fix:` milestone PR title and the complete PR body locally in a temporary file, with base `main`, current head branch, full PR_SOURCE_SHA equal to CANDIDATE_SHA, scope, exact checks, audit evidence, and review request; record the exact proposed title, body, source SHA, and local body-file path in the release evidence for the authorization checkpoint. Do not submit the PR in this task. If a matching milestone PR exists, record its number and actual head SHA instead. Before any release PR merge, compare `mix.exs` version/source ref, `.release-please-manifest.json`, and top `CHANGELOG.md` entry to the proposed tag; record parity or the precise mismatch. Create release-evidence fields for disposition, PR inventory, candidate/final SHA, main SHA, release version/tag, workflow run, published verification, blocker, resume action, and cleanup; leave unknown values pending. If GitHub authentication or hosted CI blocks the candidate gate, record that exact external prerequisite and all local results; do not assert green required checks without SHA-bound evidence.</action>
  <verify>
    <automated>gh pr list --state all --json number,title,headRefName,baseRefName,reviewDecision,mergedAt,url | jq -e 'type == "array"'</automated>
    <fails_when>GitHub authentication/query fails, output is not a PR array, or matching PRs are omitted from the evidence inventory without an explicit empty disposition.</fails_when>
    <automated>node scripts/ci_monitor.cjs closeout --push --branch "$(git branch --show-current)" --sha "$(git rev-parse HEAD)"</automated>
    <fails_when>The helper exits nonzero, remote or run SHA differs from HEAD, any of the five required jobs, coverage, or attestation fails, or either SHA-bound artifact lacks an immutable digest.</fails_when>
    <automated>if [ "$MILESTONE_PR_ABSENT" = true ]; then test -n "$PR_TITLE" &amp;&amp; test -s "$PR_BODY_FILE" &amp;&amp; test "$(git rev-parse HEAD)" = "$PR_SOURCE_SHA"; else test -n "$MILESTONE_PR_NUMBER"; fi</automated>
    <fails_when>An absent PR lacks prepared exact title/body/source SHA, or an existing PR lacks its actual number.</fails_when>
  </verify>
  <acceptance_criteria>Every matching PR has a link, review/triage state, and version parity where applicable; the fixed Mint candidate has exact-SHA hosted evidence, and an absent milestone PR has complete local submission material tied to that SHA.</acceptance_criteria>
  <done>The candidate and PR state are concrete and reviewable before any external submission decision.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize submission of the prepared milestone PR if absent</name>
  <files>.planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md</files>
  <read_first>.planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md; .planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md</read_first>
  <action>For D-03 and D-04, apply this checkpoint only when Task 1 found no matching milestone PR. Present the maintainer with the exact proposed title, full body, base/head refs, PR_SOURCE_SHA, and candidate CI/audit links already prepared locally. Stop in every execution mode for an explicit authorization decision; no PR may be submitted from a default or inferred answer. If the matching PR already exists, record its link and skip the authorization branch. If authorization is withheld, record that specific external authorization blocker and the exact resume action in `161-RELEASE-EVIDENCE.md`, then proceed to Task 3 without creating a PR.</action>
  <decision>May the executor submit the displayed milestone PR from the recorded PR_SOURCE_SHA to main?</decision>
  <context>The PR is an external message and starts the reviewed milestone path. The maintainer must approve its exact title, body, and source before `gh pr create`.</context>
  <options>
    <option id="authorize-pr"><name>Authorize submission</name><pros>Task 3 creates the exact prepared PR and pursues review.</pros><cons>Creates a public GitHub PR.</cons></option>
    <option id="withhold-pr"><name>Withhold authorization</name><pros>Keeps the prepared candidate local and records the exact resume action.</pros><cons>Release publication remains externally blocked.</cons></option>
  </options>
  <resume-signal>Reply `authorize-pr` for this displayed title/body/SHA or `withhold-pr`; silence does not authorize submission.</resume-signal>
  <verify><automated>if [ "$MILESTONE_PR_ABSENT" = true ]; then test "$(git rev-parse HEAD)" = "$PR_SOURCE_SHA" &amp;&amp; test -s "$PR_BODY_FILE"; else test -n "$MILESTONE_PR_NUMBER"; fi</automated></verify>
  <done>An existing PR is identified, or the maintainer explicitly authorizes or withholds submission of the fully prepared candidate.</done>
</task>

<task type="auto">
  <name>Task 3: Submit the authorized PR and resolve the Release Please endpoint</name>
  <files>.planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md, .planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md</files>
  <read_first>docs/releasing.md; .github/workflows/release-please.yml; .github/workflows/publish-hex.yml; .github/workflows/verify-published-release.yml; .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md; .planning/phases/161-release-and-tidy-closeout/161-VALIDATION.md</read_first>
  <action>For D-03, D-04, and REL-01, if Task 1 found no milestone PR and Task 2 explicitly returned `authorize-pr`, confirm the current local and remote head still equal the approved PR_SOURCE_SHA and the prepared title/body are unchanged, then submit the prepared PR with `gh pr create --base main --head "$HEAD_BRANCH" --title "$PR_TITLE" --body-file "$PR_BODY_FILE"`. Capture the resulting PR number, URL, and GitHub head SHA as PR_CREATED_SHA in `161-RELEASE-EVIDENCE.md`. If the source, title, or body changed, prepare and attest the new exact SHA and obtain fresh authorization before submitting. If authorization was withheld, or an actual GitHub permission/service error prevents the authorized submission, record the specific external blocker, observed error/decision, and exact resume action; mere absence of a PR or an unattempted submission never qualifies as `release-ready`. For an existing or newly created milestone PR, inspect real review comments, unresolved actionable findings, and required checks on its latest head as PR_REVIEW_SHA, triage them, and record review, check, and merge disposition. Commit and push any pre-merge tracked corrections, rerun closeout for a moved head, and require actual completed review and exact-head green checks before an authorized maintainer merges; do not invent a reviewer or approval. Keep milestone and Release Please merges and publisher credential use behind actual maintainer authorization; do not force a merge, read the Hex secret, hand-create a tag, or run a separate publish. If the authorized merges occurred and `release_created` is true, inspect the existing Release Please run's `tag_name`/version and `publish-hex` job. Require successful tagged checkout, `mix verify.package`, dry-run, `mix hex.publish --yes`, then `mix verify.release_publish X.Y.Z` and `mix verify.release_parity X.Y.Z` in that order. Correlate package version, top changelog, manifest, GitHub tag, release source SHA, Hex version, and versioned HexDocs; record exact URLs, run/job/step conclusions, and tag/source SHA. Verify green required CI on the exact merged main SHA. Mark `shipped` only if every publication and parity check passes. If explicit PR authorization, real review/merge authorization, repository permission, publisher secret, GitHub workflow, Hex, or HexDocs blocks publication, finish all attainable checks and mark `release-ready` with `Published: no`, the named external blocker, exact resume action, and still-pending check; preserve a separate unresolved status if candidate required checks are unavailable. A Release Please PR absent from the inventory is a pending prerequisite to investigate through the existing workflow, never sole grounds for `release-ready`. Set DISPOSITION, VERSION, and RELEASE_RUN_ID from observed evidence before running conditional verifiers; never supply guessed values.</action>
  <verify>
    <automated>if [ "$MILESTONE_PR_ABSENT" = true ] &amp;&amp; [ "$PR_AUTHORIZATION" = "authorize-pr" ] &amp;&amp; [ "$PR_CREATION_BLOCKED" != true ]; then test "$PR_CREATED_SHA" = "$PR_SOURCE_SHA" &amp;&amp; gh pr view "$MILESTONE_PR_NUMBER" --json url,headRefOid | jq -e --arg sha "$PR_REVIEW_SHA" '.headRefOid == $sha and (.url | length &gt; 0)'; else test "$MILESTONE_PR_ABSENT" != true || [ "$PR_AUTHORIZATION" = "withhold-pr" ] || [ "$PR_CREATION_BLOCKED" = true ]; fi</automated>
    <fails_when>An authorized, unblocked submission did not create the prepared PR at its approved SHA, the current PR head differs from the reviewed SHA, or a missing PR has neither an explicit authorization refusal nor an observed GitHub blocker.</fails_when>
    <automated>if [ "$DISPOSITION" = "shipped" ]; then gh run view "$RELEASE_RUN_ID" --json jobs | jq -e '[.jobs[] | select(.name == "publish-hex" and .conclusion == "success") | .steps | map(select(.name == "Publish package to Hex" or .name == "Verify the live published release" or .name == "Verify release parity (tarball vs tag)") | .conclusion)] == [["success", "success", "success"]]'; else test "$DISPOSITION" = "release-ready" &amp;&amp; rg -q '^\*\*Published:\*\* no$' .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md &amp;&amp; rg -q '^\*\*Blocker:\*\* .+' .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md &amp;&amp; rg -q '^\*\*Resume action:\*\* .+' .planning/phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md; fi</automated>
    <fails_when>Published disposition lacks a successful publish/post-publish/parity sequence, or release-ready lacks the explicit unpublished state, specific blocker, and resume action.</fails_when>
    <automated>if [ "$DISPOSITION" = "shipped" ]; then mix verify.release_publish "$VERSION" &amp;&amp; mix verify.release_parity "$VERSION"; else test "$DISPOSITION" = "release-ready"; fi</automated>
    <fails_when>A shipped package fails Hex visibility, clean-consumer/HexDocs verification, or tag-to-tarball parity; a blocked release is mislabeled shipped.</fails_when>
  </verify>
  <acceptance_criteria>A missing milestone PR is submitted only after approval of its exact title/body/SHA, then its real review, checks, and merge disposition are recorded. Either the actual Release Please package and docs publish with tag/source parity and green merged main is evidenced, or the artifact says release-ready with one genuine external blocker, resume action, pending checks, and no shipped claim. Required candidate checks are never silently waived.</acceptance_criteria>
  <done>The release endpoint is evidence-backed and unambiguous for D-03/D-04.</done>
</task>

</tasks>

<threat_model>
ASVS level 1; block on unresolved high-severity findings.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local candidate to GitHub Actions | Hosted results must bind to the exact committed SHA. |
| Maintainer review to release automation | Merge permission and publisher credentials control an irreversible external publish. |
| Release Please tag to Hex/HexDocs | Published artifacts must match reviewed version, changelog, tag, and source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-05 | Repudiation | Candidate and main CI evidence | high | mitigate | Use `scripts/ci_monitor.cjs closeout` on the full candidate SHA and record run, job, and digest-bearing artifact IDs; separately inspect exact merged main SHA. |
| T-161-06 | Elevation of privilege | PR submission/merge and Hex publisher | high | mitigate | Require a blocking-human authorization checkpoint for exact PR title/body/SHA before `gh pr create`, real maintainer authorization for merges, and `HEX_API_KEY` only in the existing `publish-hex` job. |
| T-161-07 | Tampering | Hex package/tag parity | high | mitigate | Require Release Please `tag_name`, successful `verify.release_publish`, and successful `verify.release_parity` before a shipped disposition. |
| T-161-08 | Information disclosure | Release evidence logs | medium | mitigate | Record run and step metadata, never publisher secret contents or unredacted service credentials. |
</threat_model>

<verification>
The candidate helper is the required exact-SHA gate. On the published branch, the existing release workflow and live post-publish tasks provide artifact proof. On the blocked branch, the evidence record must name the precise external dependency and next action; absent PR inventory alone is pending, not a release-ready blocker. Plan 04 repeats final-SHA authority after all tracked evidence edits.
</verification>

<success_criteria>
PR review, required checks, Mint audit, and either a real published release or an explicit release-ready external blocker are evidenced without simulated approval.
</success_criteria>

<output>
Create `.planning/phases/161-release-and-tidy-closeout/161-03-SUMMARY.md` when done.
</output>
