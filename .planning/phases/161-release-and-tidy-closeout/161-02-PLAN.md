---
phase: 161-release-and-tidy-closeout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.lock
autonomous: true
requirements: [REL-01]
estimate:
  tokens: 7000
  raw_tokens: 7000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The resolved root dependency graph contains Mint 1.10.1 or later and the three Phase 160 Mint advisories no longer appear in the Hex audit."
    - "The dependency change is limited to the smallest resolver-supported graph delta and does not alter Scrypath's runtime or public API."
    - "The package and deep-quality gates pass on the updated dependency graph."
  artifacts:
    - path: mix.lock
      provides: Resolver-produced fixed Mint graph
  key_links:
    - from: mix.lock
      to: mix verify.deep_quality
      via: Hex audit reads the resolved dependency graph
---

<objective>
Remediate the three Mint advisories exposed by Phase 160 before a release is considered.

Purpose: D-01 requires Mint 1.10.1 or later and cleared audit findings; D-02 bounds this to dependency remediation and its evidence.
Output: A reviewed, resolver-produced lock graph and passing package/security checks.
</objective>

## Artifacts this phase produces

This plan updates `mix.lock`. Plans 03 and 04 record its audit result and exact-SHA release evidence.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/161-release-and-tidy-closeout/161-CONTEXT.md
@.planning/phases/161-release-and-tidy-closeout/161-RESEARCH.md
@.planning/phases/161-release-and-tidy-closeout/161-PATTERNS.md
@.planning/phases/160-package-backed-phoenix-proof/160-VERIFICATION.md
@mix.exs
@mix.lock

Mint is already transitive through Finch's `~> 1.8` edge. The user approved the fixed graph, not a new product capability. No new package is introduced, so the package-legitimacy install checkpoint does not apply.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the smallest compatible Mint security update</name>
  <files>mix.lock</files>
  <read_first>mix.exs; mix.lock; .planning/phases/161-release-and-tidy-closeout/161-CONTEXT.md; .planning/phases/161-release-and-tidy-closeout/161-RESEARCH.md; .planning/phases/160-package-backed-phoenix-proof/160-VERIFICATION.md</read_first>
  <action>For D-01 and D-02, select the installed Elixir/OTP project toolchain and refresh Hex metadata. Ask Mix to update the existing transitive `mint` resolution through Finch, inspect the resulting dependency tree and `git diff -- mix.lock`, and retain only resolver-produced graph changes required to reach Mint 1.10.1 or later. Do not hand-edit Hex checksums or add Mint as a direct dependency unless the resolver proves the current Finch edge cannot satisfy the floor. If another package changes, explain why its graph edge must move. Keep runtime modules, public API, and release workflow out of this security patch. Run the Hex audit and cite the three previously reported EEF advisory IDs in the summary with their cleared result.</action>
  <verify>
    <automated>mix deps.tree mint</automated>
    <fails_when>Mix exits nonzero or Mint is absent from the resolved dependency tree.</fails_when>
    <automated>mix run -e 'case Mix.Dep.Lock.read()[:mint] do {:hex, :mint, version, _, _, _, _, _} -&gt; if Version.compare(version, "1.10.1") == :lt, do: System.halt(1); IO.puts(version); _ -&gt; System.halt(1) end'</automated>
    <fails_when>The lock entry is absent or resolves Mint below 1.10.1.</fails_when>
    <automated>mix hex.audit</automated>
    <fails_when>Hex audit exits nonzero or reports EEF-CVE-2026-82672, EEF-CVE-2026-82729, or EEF-CVE-2026-82728 against the resolved graph.</fails_when>
  </verify>
  <acceptance_criteria>`mix.lock` is resolver-produced, Mint resolves at or above 1.10.1, the three named advisory findings are absent, and any companion lockfile change has a stated dependency reason.</acceptance_criteria>
  <done>D-01's fixed Mint floor is present in the lock graph and audit-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the remediated graph remains publishable</name>
  <files>mix.lock</files>
  <read_first>mix.lock; CONTRIBUTING.md; docs/releasing.md; .github/workflows/ci.yml</read_first>
  <action>For D-01 and D-02, run the canonical deep-quality and auth-free package gates on the updated lock graph. Review `git diff -- mix.lock` once more after the gates, since Mix must not silently introduce a wider dependency change. Report deep-quality separately from the five required CI gates: its advisory CI status does not waive the three known security findings. If local Mix is unavailable, select the installed project toolchain or use the existing hosted deep-quality lane and record the exact run/SHA; do not mark audit remediation complete from lockfile text alone.</action>
  <verify>
    <automated>mix verify.deep_quality</automated>
    <fails_when>Nonzero exit, a Hex audit advisory remains, Dialyzer or namespace checks fail, or the command cannot run without an exact-SHA hosted substitute.</fails_when>
    <automated>mix verify.package</automated>
    <fails_when>Nonzero exit or package, consumer, docs, or release-contract proof fails on the updated lock graph.</fails_when>
  </verify>
  <acceptance_criteria>The full deep-quality gate reports no Mint advisories and passes; the package gate passes; no runtime or public API files were changed for the security remediation.</acceptance_criteria>
  <done>The fixed dependency graph has local or exact-SHA hosted audit evidence and passes the canonical package contract.</done>
</task>

</tasks>

<threat_model>
ASVS level 1; block on unresolved high-severity findings.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry to dependency lock | Resolver metadata determines the dependency code included in builds. |
| Lock graph to package artifact | The remediated version must be what package verification and later release jobs consume. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-03 | Tampering | Transitive Mint package | high | mitigate | Resolve Mint at or above 1.10.1 with Mix, review the `mix.lock` graph delta, and require `mix hex.audit` and `mix verify.deep_quality` to clear all three named findings. |
| T-161-04 | Denial of service | Package dependency graph | medium | mitigate | Run `mix verify.package` on the resulting graph before candidate-SHA CI. |
</threat_model>

<verification>
Use the existing Hex audit, deep-quality, and package commands. Plan 03 binds these changes to candidate exact-SHA CI; Plan 04 binds final status to final-SHA evidence.
</verification>

<success_criteria>
REL-01's known Mint blocker is resolved with an audit-clean, publishable dependency graph and no source API expansion.
</success_criteria>

<output>
Create `.planning/phases/161-release-and-tidy-closeout/161-02-SUMMARY.md` when done.
</output>
