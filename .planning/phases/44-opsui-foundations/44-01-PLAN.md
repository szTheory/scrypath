---
phase: 44
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scrypath_ops/
  - mix.exs
  - README.md
autonomous: true
requirements:
  - OPSUI-09
---

<objective>

Create the **`scrypath_ops/`** standalone Phoenix (+ LiveView) application at the repository root with **`{:scrypath, path: ".."}`**, clone-and-run **README** instructions, and explicit **Hex boundary** documentation so `scrypath_ops` never ships inside the **`scrypath`** package.

</objective>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-44-01 | OPSUI code accidentally added to Hex `files` list | high | Document whitelist-only `package.files`; add grep-verifiable note in root `mix.exs` comment or `docs/releasing.md` pointer—**do not** add `scrypath_ops` to `files`. |
| T-44-01b | Path dependency resolves wrong when cloning shallow layouts | medium | README documents clone from repo root; `mix.exs` uses `path: ".."` from `scrypath_ops/mix.exs`. |

</threat_model>

<tasks>

<task id="44-01-01" type="execute">
<read_first>
- `mix.exs` (library `package.files` whitelist)
- `examples/phoenix_meilisearch/mix.exs` (Phoenix + path dep pattern)
- `.planning/phases/44-opsui-foundations/44-CONTEXT.md` (D-01–D-04)
</read_first>
<action>

1. From the **repository root**, generate **`scrypath_ops/`** with Phoenix LiveView enabled, e.g. run:

   `mix phx.new scrypath_ops --module ScrypathOps --app scrypath_ops`

   Accept defaults that include HTML and LiveView; **do not** nest under `examples/`.

2. In **`scrypath_ops/mix.exs`**, add **`{:scrypath, path: ".."}`** to `deps/0` (same major constraints style as the demo app, adjusting version/path as needed).

3. Write **`scrypath_ops/README.md`** with: prerequisites (Elixir ~> 1.17, Erlang/OTP per AGENTS), **`mix setup`** / **`mix deps.get`**, **`mix phx.server`**, default URL, and a one-line note that publishing **`scrypath`** Hex package is **only** from the **parent** library directory.

4. Add a **short** subsection or bullet to **`docs/releasing.md`** (or root **`README.md`** if releasing doc is too heavy) stating explicitly: **`package.files` must never include `scrypath_ops/`**—the Hex artifact is library-only.

5. Run **`cd scrypath_ops && mix deps.get && mix compile`** and fix any dependency conflicts against the library.

</action>
<acceptance_criteria>

- `test -f scrypath_ops/mix.exs` exits 0.
- `grep -F '{:scrypath, path: ".."}' scrypath_ops/mix.exs` returns a match line (exact path string).
- `grep -A12 'defp package' mix.exs | grep -F 'scrypath_ops' | wc -l | tr -d ' '` outputs **`0`** (the string **`scrypath_ops`** must not appear inside the **`package`** function’s **`files:`** block).
- `scrypath_ops/README.md` contains the substring **`mix phx.server`**.
- `cd scrypath_ops && mix compile` exits 0.

</acceptance_criteria>
</task>

</tasks>

<verification>

1. `mix compile` from repository root (library) exits 0.
2. `cd scrypath_ops && mix compile` exits 0.

</verification>

<success_criteria>

- OPSUI-09 satisfied: dedicated app directory with documented bootstrap and correct path dependency; Hex surface remains library-only by documentation + unchanged safe whitelist.

</success_criteria>

<must_haves>

- [ ] `scrypath_ops/` exists and compiles
- [ ] Path dep to `scrypath` is `".."` from ops app
- [ ] Maintainer-facing run instructions exist
- [ ] Hex packaging boundary explicitly documented

</must_haves>
