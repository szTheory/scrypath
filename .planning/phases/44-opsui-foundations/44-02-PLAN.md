---
phase: 44
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/docs/operator-ia.md
  - README.md
  - guides/operator-mix-tasks.md
autonomous: true
requirements:
  - OPSUI-06
---

<objective>

Author **`scrypath_ops/docs/operator-ia.md`** as the canonical persona/JTBD + navigation contract (2–3 roles, 5–7 ranked jobs, single mapping table) and add **short discoverability pointers** from the library **`README.md`** and/or **`guides/`** without duplicating long prose in guides.

</objective>

<threat_model>

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-44-02 | Nav order drifts from documented JTBD | medium | Table explicitly orders jobs 1..N matching nav priority from CONTEXT D-07; acceptance grep for job order headings. |
| T-44-02b | Operators never find OPSUI | low | Root `README.md` contains full path string **`scrypath_ops/README.md`** and **`scrypath_ops/docs/operator-ia.md`**. |

</threat_model>

<tasks>

<task id="44-02-01" type="execute">
<read_first>
- `.planning/phases/44-opsui-foundations/44-CONTEXT.md` (D-05–D-09)
- `scrypath_ops/README.md` (from plan 01)
</read_first>
<action>

1. Create **`scrypath_ops/docs/operator-ia.md`** containing:
   - **Personas** section with **2–3** role-based personas (e.g. on-call engineer, library maintainer, search owner)—plain ops language, not marketing.
   - **Jobs-to-be-done** section with **5–7** numbered jobs using **trigger → outcome → done when** phrasing for each.
   - **Single markdown table** with columns exactly: **`Job`**, **`Primary persona`**, **`Top nav label`**, **`Route`**, **`Scrypath / doc / Mix follow-up`** (header row must match these labels for grep stability).
   - Map job **(1)** to posture/health, **(2)** failed sync work, **(3)** sync/drift read-only + doc/task links, **(4)** bounded search / federation honesty—matching CONTEXT D-07 order.

2. In **root `README.md`**, add **one short paragraph or bullet list** (≤ 6 lines total new content) linking to **`scrypath_ops/README.md`** and **`scrypath_ops/docs/operator-ia.md`** using **repo-relative** paths as written above.

3. Optionally add **one sentence** pointer in **`guides/operator-mix-tasks.md`** (or another existing ops-facing guide if more appropriate) referencing **`../scrypath_ops/docs/operator-ia.md`** or repo-relative `scrypath_ops/docs/operator-ia.md` from repo root—**no** full JTBD paste.

</action>
<acceptance_criteria>

- `test -f scrypath_ops/docs/operator-ia.md` exits 0.
- `grep -E '^##+ (Personas|Jobs-to-be-done|Navigation)' scrypath_ops/docs/operator-ia.md` returns at least 3 lines (section headings exist).
- `grep -F '| Job |' scrypath_ops/docs/operator-ia.md` returns 1 line (table header includes `Job`).
- `grep -F 'scrypath_ops/docs/operator-ia.md' README.md` returns ≥1 line.
- `grep -F 'scrypath_ops/README.md' README.md` returns ≥1 line.

</acceptance_criteria>
</task>

</tasks>

<verification>

- `mix compile` from repository root exits 0.

</verification>

<success_criteria>

- OPSUI-06 satisfied: canonical IA doc exists, nav priority encoded, discoverability pointers in library docs.

</success_criteria>

<must_haves>

- [ ] `operator-ia.md` with personas, JTBD, and mapping table
- [ ] README pointers use exact path strings requested
- [ ] Nav priority matches CONTEXT D-07

</must_haves>
