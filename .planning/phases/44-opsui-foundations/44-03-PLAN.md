---
phase: 44
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/router.ex
  - scrypath_ops/lib/scrypath_ops_web/endpoint.ex
  - scrypath_ops/lib/scrypath_ops_web/components/layouts/*.heex
  - scrypath_ops/lib/scrypath_ops_web/live/**/*.ex
autonomous: true
requirements:
  - OPSUI-07
  - OPSUI-06
---

<objective>

Implement the **boring Phoenix LiveView shell** for operators: **`scope "/ops"`**, dedicated `*Web` namespace, **`live_session :ops, on_mount: [...]`** wrapping all operator LiveViews, stock layouts with **`<.flash_group>`**, stub LiveViews whose **titles match** `operator-ia.md` nav labels, and **real routes** (no placeholder `href="#"` for primary nav).

</objective>

<threat_model>

| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-44-03 | HTTP and LiveSocket auth boundaries diverge | high | Single `live_session :ops` wraps **all** `/ops` LiveViews; `on_mount` hook module stub present for future auth halting (CONTEXT D-11–D-12). |
| T-44-03b | CSRF/session defaults broken by custom hacks | medium | Use generated Phoenix plugs/pipelines unchanged except for scoped routes. |

</threat_model>

<tasks>

<task id="44-03-01" type="execute">
<read_first>
- `scrypath_ops/lib/scrypath_ops_web/router.ex`
- `examples/phoenix_meilisearch/lib/scrypath_demo_web/router.ex`
- `scrypath_ops/docs/operator-ia.md`
- `.planning/phases/44-opsui-foundations/44-UI-SPEC.md`
</read_first>
<action>

1. Define **`live_session :ops`** in **`router.ex`** wrapping a **`scope "/ops", ScrypathOpsWeb`** (adjust module prefix to match generated app name, e.g. `ScrypathOpsWeb`) so **every** operator LiveView is in that session.

2. Add **`on_mount`** tuple list including at minimum a stub module **`ScrypathOpsWeb.Live.OnMount`** (name may vary if generator conflict—pick one module and use consistently) that implements `on_mount(:default, _params, _session, socket)` returning `{:cont, socket}` today.

3. Create **one LiveView module per primary nav item** from `operator-ia.md` (minimum 4 routes matching the four priority buckets). Each module renders:
   - `<Layouts.app flash={@flash} ...>` (or generator-equivalent) including **`<.flash_group flash={@flash} id="flash-group" />`** in the layout template if not already present.
   - Stub heading using **exact nav label string** copied from the IA table’s **`Top nav label`** column for that route.

4. Wire routes using **`~p`** sigils for internal links in the **app layout** (header nav) between stub screens.

5. Ensure **`lib/scrypath_ops_web/controllers/error_html.ex`** / JSON error modules remain generator defaults unless broken.

</action>
<acceptance_criteria>

- `grep -n 'live_session :ops' scrypath_ops/lib/scrypath_ops_web/router.ex` returns ≥1 line.
- `grep -n 'on_mount' scrypath_ops/lib/scrypath_ops_web/router.ex` returns ≥1 line referencing the same hook module used in `lib/scrypath_ops_web/live/` or `lib/scrypath_ops_web/`.
- `grep -r 'flash_group' scrypath_ops/lib/scrypath_ops_web/components/layouts` returns ≥1 match.
- `grep -rE 'href=\"#\"' scrypath_ops/lib/scrypath_ops_web` returns **empty** (no primary `href="#"` in web tree).
- `cd scrypath_ops && mix compile` exits 0.

</acceptance_criteria>
</task>

<task id="44-03-02" type="execute">
<read_first>
- `scrypath_ops/docs/operator-ia.md`
- `scrypath_ops/lib/scrypath_ops_web/router.ex`
</read_first>
<action>

Update **`scrypath_ops/docs/operator-ia.md`** **Route** column cells so each row’s route path **exactly matches** the `path` segment wired in `router.ex` (e.g. `/ops/posture`) for the corresponding LiveView—same PR per CONTEXT D-08.

</action>
<acceptance_criteria>

- Every **`Route`** cell in the mapping table contains a string starting with **`/ops/`**.
- `grep -c 'live "/' scrypath_ops/lib/scrypath_ops_web/router.ex` is **≥** the number of primary nav LiveViews declared in the same file (at least 4).

</acceptance_criteria>
</task>

</tasks>

<verification>

- `cd scrypath_ops && mix compile`
- Manual spot-check optional: `mix phx.server` and visit `/ops/...` first route.

</verification>

<success_criteria>

- OPSUI-07 satisfied: conventional Phoenix/LiveView structure with shared session and flash.
- OPSUI-06 nav alignment: routes and IA table consistent.

</success_criteria>

<must_haves>

- [ ] `live_session :ops` + `on_mount` stub
- [ ] ≥4 stub LiveViews with honest phase-boundary copy
- [ ] `operator-ia.md` route column matches router

</must_haves>
