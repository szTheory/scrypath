---
phase: 48-ia-and-jtbd-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/nav.ex
  - scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
  - scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs
autonomous: true
requirements:
  - OPSUX-01
user_setup: []
must_haves:
  truths:
    - Primary ops nav order is posture → failed-sync → sync-drift → search.
    - Nav labels match operator-ia.md Top nav label intent for those four routes.
  artifacts:
    - scrypath_ops/lib/scrypath_ops_web/nav.ex exists and exports primary/0.
  key_links:
    - Nav.primary/0 is the only source of path+label pairs rendered in layouts.ex :ops shell.
---

<objective>
Introduce **`ScrypathOpsWeb.Nav`** as the single Elixir source of truth for ordered **`/ops`** primary navigation (paths + labels), wire **`layouts.ex`** (`shell: :ops`) to render from it, and extend **`operator_ia_contract_test`** so CI fails on label/order drift or missing router↔nav coverage (**OPSUX-01**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-ia-and-jtbd-alignment/48-CONTEXT.md
@.planning/phases/48-ia-and-jtbd-alignment/48-RESEARCH.md
@scrypath_ops/docs/operator-ia.md
@scrypath_ops/lib/scrypath_ops_web/router.ex
@scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
@scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs
</context>

<threat_model>
**Trust boundaries:** Browser → read-only **`/ops`** LiveViews; maintainer edits to **`router.ex`**, **`Nav`**, and **`operator-ia.md`**.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | Primary nav | Mitigated | Contract tests assert **`Nav.primary/0`** order and labels; router **`live`** routes under **`:ops`** must stay documented and aligned per tests. |
| T-48-02 | Information disclosure | Nav hyperlinks | Accepted (low) | Links only point to in-app **`/ops`** routes or existing public docs paths already used in IA. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Nav module with ordered primary/0</name>
  <files>scrypath_ops/lib/scrypath_ops_web/nav.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/router.ex, scrypath_ops/docs/operator-ia.md, scrypath_ops/lib/scrypath_ops_web/components/layouts.ex</read_first>
  <action>Create **`scrypath_ops/lib/scrypath_ops_web/nav.ex`** defining **`def primary/0`** returning a list of **exactly four** maps **`%{path: sigil_p_string, label: binary}`** in this order: (1) **`~p"/ops/posture"`** + label **`Posture / health`**, (2) **`~p"/ops/failed-sync"`** + **`Failed sync work`**, (3) **`~p"/ops/sync-drift"`** + **`Sync / drift`**, (4) **`~p"/ops/search"`** + **`Search & federation`**. Use **`use ScrypathOpsWeb, :verified_routes`** (or the same verified-routes mechanism other **`ScrypathOpsWeb`** modules use for **`~p`**) so paths compile-check against the router. Add **`@moduledoc`** stating router owns route existence and **`Nav`** is the curated chrome subset.</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - File `scrypath_ops/lib/scrypath_ops_web/nav.ex` exists.
    - `grep -q 'def primary' scrypath_ops/lib/scrypath_ops_web/nav.ex` exits 0.
    - `grep -q 'Posture / health' scrypath_ops/lib/scrypath_ops_web/nav.ex` exits 0.
    - `grep -q 'Search & federation' scrypath_ops/lib/scrypath_ops_web/nav.ex` exits 0.
  </acceptance_criteria>
  <done>`mix compile` succeeds; four routes present in order with exact labels above.</done>
</task>

<task type="auto">
  <name>Task 2: Render ops shell nav from Nav.primary/0</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/layouts.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/nav.ex, scrypath_ops/lib/scrypath_ops_web/components/layouts.ex</read_first>
  <action>In **`def app(%{shell: :ops}`**, replace the four hard-coded **`<li><.link navigate={~p"..."`>…** items under **`aria-label="Operator primary"`** with a single **`<%= for item <- ScrypathOpsWeb.Nav.primary() do %>`** loop: each item renders **`<.link navigate={item.path} class="link link-hover">{item.label}</.link>`** inside **`<li>`**. Remove duplicate string literals for those four labels/paths from this function so they exist only in **`Nav`**.</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'Nav.primary' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` exits 0.
    - `grep -c 'link link-hover' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` is ≥ 1 (loop still applies link-hover class).
    - `grep 'Posture / health' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` exits 1 (label not duplicated in layouts — must not appear as literal in `:ops` clause).
  </acceptance_criteria>
  <done>Ops shell compiles; labels for the four items are not string literals in `layouts.ex`.</done>
</task>

<task type="auto">
  <name>Task 3: Extend operator IA contract tests for Nav</name>
  <files>scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs</files>
  <read_first>scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs, scrypath_ops/lib/scrypath_ops_web/nav.ex, scrypath_ops/lib/scrypath_ops_web/router.ex</read_first>
  <action>Add tests that: (1) **`ScrypathOpsWeb.Nav.primary/0`** returns **4** entries; (2) path values match the ordered list **`[~p"/ops/posture", ~p"/ops/failed-sync", ~p"/ops/sync-drift", ~p"/ops/search"]`** (e.g. `Enum.map(Nav.primary(), fn e -> e.path end)` in test); (3) labels match **`Posture / health`**, **`Failed sync work`**, **`Sync / drift`**, **`Search & federation`** respectively; (4) every **`live("` path inside **`live_session :ops`** in **`router.ex`** has a corresponding **`~p"/ops/..."`** in **`Nav.primary/0`** (parse **`@router`** string or assert the four known paths — if a new **`live`** is added, test fails until **`Nav`** and doc are updated).</action>
  <verify>cd scrypath_ops && mix test test/scrypath_ops_web/operator_ia_contract_test.exs</verify>
  <acceptance_criteria>
    - `mix test test/scrypath_ops_web/operator_ia_contract_test.exs` exits 0.
    - `grep -q 'Nav.primary' scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs` exits 0.
  </acceptance_criteria>
  <done>Contract test file covers Nav order, labels, and router live_session parity.</done>
</task>

</tasks>

<verification>
- [ ] `cd scrypath_ops && mix test test/scrypath_ops_web/operator_ia_contract_test.exs` exits 0
- [ ] `cd scrypath_ops && mix test` exits 0
</verification>

<success_criteria>

- **`Nav.primary/0`** is canonical for ops primary nav path+label pairs.
- **`layouts.ex`** `:ops` shell contains no duplicated four-route label strings.
- Tests fail if **`Nav`** order/labels drift or **`live_session :ops`** gains an undocumented route.

</success_criteria>

---

## PLANNING COMPLETE
