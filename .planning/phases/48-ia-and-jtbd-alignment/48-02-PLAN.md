---
phase: 48-ia-and-jtbd-alignment
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/docs/operator-ia.md
  - scrypath_ops/lib/mix/tasks/scrypath_ops/check_nav_contract.ex
  - scrypath_ops/mix.exs
autonomous: true
requirements:
  - OPSUX-01
user_setup: []
must_haves:
  truths:
    - CI fails when the machine-readable nav block in operator-ia.md drifts from Nav.primary/0.
  artifacts:
    - Mix task `mix scrypath_ops.check_nav_contract` exists and exits non-zero on mismatch.
  key_links:
    - Doc fence is the only second copy of nav data; Nav remains Elixir SOT.
---

<objective>
Add a **machine-verifiable** delimited block in **`operator-ia.md`** and a **`mix scrypath_ops.check_nav_contract`** task that compares it to **`ScrypathOpsWeb.Nav.primary/0`**, then wire the check into **`scrypath_ops`** test/CI flow so nav/doc drift fails the pipeline (**OPSUX-01**, CONTEXT **D-04**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-ia-and-jtbd-alignment/48-CONTEXT.md
@.planning/phases/48-ia-and-jtbd-alignment/48-RESEARCH.md
@scrypath_ops/lib/scrypath_ops_web/nav.ex
@scrypath_ops/docs/operator-ia.md
@scrypath_ops/mix.exs
</context>

<threat_model>
**Trust boundaries:** Maintainer workstation / CI reads repo files; no runtime user input to the Mix task.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | operator-ia.md fence | Mitigated | **`mix scrypath_ops.check_nav_contract`** compares fence to compiled **`Nav`**; CI runs after **`mix test`** or inside test alias. |
| T-48-03 | Repudiation | Doc vs code | Mitigated | Non-zero exit documents drift; no silent overwrite in default **`mix`** invocation (use optional **`--write`** only if explicitly implemented and documented). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Delimited nav-contract block in operator-ia.md</name>
  <files>scrypath_ops/docs/operator-ia.md</files>
  <read_first>scrypath_ops/docs/operator-ia.md, scrypath_ops/lib/scrypath_ops_web/nav.ex</read_first>
  <action>Immediately after the **`| Search & federation |`** table row (end of the markdown nav table), insert two HTML comment markers **`<!-- scrypath:nav-contract-begin -->`** and **`<!-- scrypath:nav-contract-end -->`** with **only** a JSON array between them (no extra prose). JSON format: array of objects **`{"route":"/ops/...","label":"..."}`** in **exactly** the same order and strings as **`Nav.primary/0`** (routes as plain strings **`"/ops/posture"`** etc., labels matching **`Nav`**). The JSON must be valid for **`Jason.decode!/1`**. Preserve all existing **Personas**, **JTBD**, and hand-written table rows above the fence.</action>
  <verify>grep -q 'scrypath:nav-contract-begin' scrypath_ops/docs/operator-ia.md</verify>
  <acceptance_criteria>
    - `grep -q 'scrypath:nav-contract-begin' scrypath_ops/docs/operator-ia.md` exits 0.
    - `grep -q 'scrypath:nav-contract-end' scrypath_ops/docs/operator-ia.md` exits 0.
    - `grep -q '/ops/posture' scrypath_ops/docs/operator-ia.md` exits 0 (still present in doc).
  </acceptance_criteria>
  <done>Fence exists with valid JSON between markers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement mix scrypath_ops.check_nav_contract</name>
  <files>scrypath_ops/lib/mix/tasks/scrypath_ops/check_nav_contract.ex</files>
  <read_first>scrypath_ops/mix.exs, scrypath_ops/lib/scrypath_ops_web/nav.ex</read_first>
  <action>Create **`scrypath_ops/lib/mix/tasks/scrypath_ops/check_nav_contract.ex`** as **`Mix.Tasks.ScrypathOps.CheckNavContract`** with **`@shortdoc`** describing the check. In **`run/1`**: (1) start **`ScrypathOps.Application`** if needed via **`Mix.Task.run("app.start")`** only when required for **`Nav`** — prefer approach that works in **`Mix`** without full server (if **`Nav`** has no app deps, **`Mix.Task.run("compile")`** may suffice); (2) read **`docs/operator-ia.md`** relative to **`File.cwd!()`** expecting **`cd scrypath_ops`**; (3) extract substring between **`<!-- scrypath:nav-contract-begin -->`** and **`<!-- scrypath:nav-contract-end -->`**; (4) **`Jason.decode!`** to list of maps; (5) build expected list from **`ScrypathOpsWeb.Nav.primary/0`** by mapping each entry to **`%{"route" => path_to_binary, "label" => label}`** where **`path_to_binary`** is **`path`** from **`Nav`** converted with **`to_string/1`** or **`URI.append_path`** pattern Phoenix uses (must match **`"/ops/..."`** form); (6) compare with **`==`** or ordered diff; (7) **`Mix.raise/1`** with clear message on mismatch, otherwise **`Mix.shell().info`** success. Support optional **`--write`** flag: when passed, rewrite the fenced JSON from **`Nav.primary/0`** and **`Mix.shell().info`** confirmation — when absent, read-only check only.</action>
  <verify>cd scrypath_ops && mix scrypath_ops.check_nav_contract</verify>
  <acceptance_criteria>
    - `mix help scrypath_ops.check_nav_contract` shows shortdoc (task discoverable).
    - `cd scrypath_ops && mix scrypath_ops.check_nav_contract` exits 0 when doc matches **`Nav`**.
    - File path matches `scrypath_ops/lib/mix/tasks/scrypath_ops/check_nav_contract.ex`.
  </acceptance_criteria>
  <done>Mix task enforces parity between fence and **`Nav`**.</done>
</task>

<task type="auto">
  <name>Task 3: Wire check into mix test alias</name>
  <files>scrypath_ops/mix.exs</files>
  <read_first>scrypath_ops/mix.exs</read_first>
  <action>In **`scrypath_ops/mix.exs`** **`aliases/0`**, prepend **`"scrypath_ops.check_nav_contract"`** to the existing **`test`** alias list **before** **`ecto.create`** so **`cd scrypath_ops && mix test`** (including CI) fails fast when the doc fence drifts from **`Nav.primary/0`**.</action>
  <verify>cd scrypath_ops && mix test</verify>
  <acceptance_criteria>
    - `grep -q 'check_nav_contract' scrypath_ops/mix.exs` exits 0.
    - `cd scrypath_ops && mix test` runs the nav contract check (observable: temporarily break JSON in doc locally and confirm **`mix test`** fails — optional manual smoke; not automated in criteria).
  </acceptance_criteria>
  <done>`mix test` from **`scrypath_ops`** invokes nav contract verification.</done>
</task>

</tasks>

<verification>
- [ ] `cd scrypath_ops && mix scrypath_ops.check_nav_contract` exits 0
- [ ] `cd scrypath_ops && mix test` exits 0
</verification>

<success_criteria>

- Machine fence in **`operator-ia.md`** stays in sync with **`Nav.primary/0`** via Mix.
- Drift fails **`mix test`** via the **`test`** alias.

</success_criteria>

---

## PLANNING COMPLETE
