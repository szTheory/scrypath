---
phase: 48-ia-and-jtbd-alignment
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
  - scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs
autonomous: true
requirements:
  - OPSUX-02
user_setup: []
must_haves:
  truths:
    - Posture view exposes a headline healthy/degraded/broken (or equivalent) plus ≤5 ordered next checks with single primary egress each.
  artifacts:
    - `data-testid="posture-next-checks"` present in rendered HTML when posture assigns support it.
  key_links:
    - Next-check internal links use routes consistent with **`Nav.primary/0`** (reuse **`Nav`** or **`~p`** literals matching those paths).
---

<objective>
Upgrade **`PostureLive`** so on-call operators see a **single headline posture state**, a **short evidence line**, and a **fixed ordered “Next checks”** list (≤5) with imperative copy and one egress each (ops routes, repo guide paths, or **`mix …`** pointers), aligned with **JTBD 1** in **`operator-ia.md`** — read-only, no new recovery verbs (**OPSUX-02**, CONTEXT **D-06**–**D-09**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-ia-and-jtbd-alignment/48-CONTEXT.md
@scrypath_ops/docs/operator-ia.md
@scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
@scrypath_ops/lib/scrypath_ops_web/nav.ex
@scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs
</context>

<threat_model>
**Trust boundaries:** Authenticated or local-only **`/ops`** operator; read-only UI.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-02 | Information disclosure | “Next checks” copy | Mitigated | Links only to in-repo **`guides/`**, **`docs/`**, **`/ops/*`**, or documented **`mix scrypath.*`** strings from **`guides/operator-mix-tasks.md`** — no credentials or env values. |
| T-48-04 | Elevation of privilege | Implied recovery | Mitigated | No new **Fix/Sync now** buttons; **`phx-click`** limited to existing **Refresh** only; next checks are **`navigate`** / **`<a href>`** to read-only surfaces. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Compute headline and next_checks assigns</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex, scrypath_ops/lib/scrypath_ops_web/nav.ex, scrypath_ops/docs/operator-ia.md</read_first>
  <action>In **`PostureLive`**, add private helpers that derive from existing assigns only: **`:empty_allowlist`** → headline **Broken** (or **Not configured**) + one-line evidence about **`schema_allowlist`** / **`SCRYPATH_OPS_SCHEMAS`**; **`:missing_backend`** → **Broken** + missing **`:backend`** evidence; **`{:ok, rows}`** with **`aggregate_error_count > 0`** → **Degraded** + error count; **`{:ok, _}`** with zero errors → **Healthy**. Assign **`@next_checks`** as a list of at most **5** items renderable in HEEx (map with **`text`** plus optional **`navigate`** using **`~p"/ops/..."`** paths consistent with **`ScrypathOpsWeb.Nav.primary/0`**, and/or **`href`** for **`https://github.com/szTheory/scrypath/blob/main/guides/meilisearch-operations.md`** per root **`mix.exs`** **`@source_url`**). When fetch errors exist, include a triage item pointing to **`~p"/ops/failed-sync"`**. Optionally include a monospace **`mix scrypath.*`** line only if that string exists in **`guides/operator-mix-tasks.md`**. Merge these assigns in **`mount`** after **`load_posture`** and inside **`load_posture/1`** so **Refresh** updates them.</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'next_checks' scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex` exits 0.
    - `grep -Eq 'headline|Healthy|Degraded|Broken' scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex` exits 0.
  </acceptance_criteria>
  <done>Assigns available in **`socket`** for render; **`mix compile`** OK.</done>
</task>

<task type="auto">
  <name>Task 2: Render next-checks region in HEEx</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex</read_first>
  <action>In **`render/1`**, above the existing conditional table block (below H1 / Refresh), add a section **`<section data-testid="posture-next-checks" aria-label="Next checks">`** containing: (1) a **`p`** or **`div`** with the **headline** text and **evidence** line; (2) **`<ol class="list-decimal ...">`** iterating **`@next_checks`** with at most **5** **`<li>`** items; each item renders **`text`** and optionally **`<.link navigate={...}>`** or **`<a href=...>`** per assign shape. Optional **`<details>`** below with “More links” containing anchor-only runbook links (**CONTEXT D-08**) — if omitted, ensure main list still ≤5. Do not duplicate long guide prose.</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'data-testid=\"posture-next-checks\"' scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex` exits 0.
    - `grep -q '<ol' scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex` exits 0.
  </acceptance_criteria>
  <done>Rendered HTML includes next-checks landmark for tests.</done>
</task>

<task type="auto">
  <name>Task 3: LiveView tests for next checks</name>
  <files>scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs</files>
  <read_first>scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs, scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex</read_first>
  <action>Add **`test`** **`"posture shows next checks block with ordered items"`** (name may vary): **`live(conn, ~p"/ops/posture")`**, then assert **`has_element?(lv, "[data-testid='posture-next-checks']")`** (or **`~s`**) is true; assert rendered HTML contains **`/ops/failed-sync`** when **`fetch error`** scenario from existing setup (errors present); assert **`<ol>`** has **≤5** **`<li>`** inside the section (count via **`render(lv)`** string split or **`Floki`** if already a dep — **prefer** simple **`assert html =~ "data-testid"`** and **`refute`** excessive markers). Keep **`async: false`** and existing **`setup`** env.</action>
  <verify>cd scrypath_ops && mix test test/scrypath_ops_web/live/posture_live_test.exs</verify>
  <acceptance_criteria>
    - `cd scrypath_ops && mix test test/scrypath_ops_web/live/posture_live_test.exs` exits 0.
    - `grep -q 'posture-next-checks' scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs` exits 0.
  </acceptance_criteria>
  <done>Automated coverage for next-checks presence and key egress.</done>
</task>

</tasks>

<verification>
- [ ] `cd scrypath_ops && mix test test/scrypath_ops_web/live/posture_live_test.exs` exits 0
- [ ] `cd scrypath_ops && mix test` exits 0
</verification>

<success_criteria>

- On-call **JTBD 1** satisfied in UI: headline state + explicit ordered next checks without new write paths.
- Tests guard **`data-testid="posture-next-checks"`** and degraded path content.

</success_criteria>

---

## PLANNING COMPLETE
