---
phase: 42-per-query-tuning-pipeline-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/per-query-tuning-pipeline.md
autonomous: true
requirements:
  - TUNE-PIPE-01
  - TUNE-PIPE-02
  - TUNE-PIPE-03
  - TUNE-PIPE-04
tags:
  - docs
  - spec
must_haves:
  truths:
    - "`guides/per-query-tuning-pipeline.md` exists and is the single normative spec for precedence, Meilisearch mapping categories, non-goals, errors/telemetry expectations, and an implementation checklist authorizing Phase 43 runtime work."
    - "Published prose passes `docs_contract_test` hygiene (no forbidden internal tokens per existing regexes)."
  artifacts:
    - path: "guides/per-query-tuning-pipeline.md"
      provides: "TUNE-PIPE authoritative specification"
      contains: "## Scope and non-goals"
---

<objective>
Author **`guides/per-query-tuning-pipeline.md`** so **TUNE-PIPE-01** … **TUNE-PIPE-04** are satisfied in one discoverable artifact: precedence and ordering, Meilisearch request mapping (v1.9 slice + deferrals), non-goals plus error/telemetry contracts, and a maintainer **implementation readiness checklist** that explicitly gates **`TUNE-PQ-*`**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-per-query-tuning-pipeline-spec/42-CONTEXT.md
@.planning/phases/42-per-query-tuning-pipeline-spec/42-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author canonical per-query tuning pipeline guide</name>
  <files>guides/per-query-tuning-pipeline.md</files>
  <read_first>
    - .planning/phases/42-per-query-tuning-pipeline-spec/42-CONTEXT.md (decisions D-01 through D-42)
    - guides/relevance-tuning.md (Plane A vocabulary; do not duplicate index-time narrative)
    - guides/multi-index-search.md (federation and `search_many` ownership; appendix-level pointer only)
    - lib/scrypath/config.ex (Config.resolve!/1 cascade — cite accurately)
    - lib/scrypath/multi_search/entries.ex (entry vs shared_opts merge story)
    - lib/scrypath/search.ex (telemetry names such as [:scrypath, :search_many] and [:scrypath, :search_many, :partial] if documented)
  </read_first>
  <action>
    Create **`guides/per-query-tuning-pipeline.md`** with **exactly these H2 section titles in order** (H2 lines must match verbatim for doc-contract anchors in Plan 02):

    1. `## Scope and non-goals`
    2. `## Two-plane model and precedence`
    3. `## Pipeline stages`
    4. `## Meilisearch mapping and version stance`
    5. `## Error taxonomy`
    6. `## Telemetry catalog`
    7. `## Federation and search_many`
    8. `## Recipes (Phoenix and LiveView)`
    9. `## Implementation readiness checklist`

    Content requirements (map to **TUNE-PIPE-**\*):

    - **TUNE-PIPE-01 / D-10–D-12, D-14:** Explain Plane A vs Plane B; document the **right-biased** stack for search-time keywords: `Application.get_env(:scrypath, :defaults)` &lt; per-repo `:scrypath` &lt; per-call runtime opts &lt; per-entry tuple opts &lt; future per-query tuning map (state explicitly which layers exist **today** vs **Phase 43**). Include nested map merge default (**shallow replace**) and **`:deep` opt-in** alignment with Phase 19.
    - **TUNE-PIPE-02 / D-20–D-25:** Principle-based mapping categories; **in-scope exemplars** for the first runtime slice: **`rankingScoreThreshold`**, **`showRankingScore`**, optional **`showRankingScoreDetails`** with operational honesty (hits, totals, facet distributions, pagination); **deferred** items with rationale (vector/hybrid, per-query synonym/rankingRules mutation, etc.); **minimum Meilisearch version** called out with **links** to vendor docs (no copying of vendor prose blocks).
    - **TUNE-PIPE-03 / D-30–D-35, D-16:** Non-goals in **Scope and non-goals**; error layers (options vs transport vs engine); normative vs non-normative (tagged tuples and telemetry vs human strings); strict allowlist posture for library-owned keys.
    - **TUNE-PIPE-04:** **Implementation readiness checklist** must end with explicit bullets that **authorize starting `TUNE-PQ-*` implementation** only when each gate is satisfied (checkbox list).

    Hygiene: do **not** include substrings that match **`docs_contract_test`** forbidden patterns (e.g. bare `TUNE-` + two digits pattern, `(D-`, `Per decision D-`, `DocsContractTest`, `UI-SPEC`, `FACET-` + digits, etc.).

    Add a short opening paragraph after the title line stating the document is the **canonical** request-time tuning pipeline spec for Scrypath v1.9.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File **`guides/per-query-tuning-pipeline.md`** exists.
    - **`grep -n '^## Scope and non-goals$' guides/per-query-tuning-pipeline.md`** returns ≥ 1 line; same for **`^## Implementation readiness checklist$`**.
    - All nine H2 titles listed in **action** appear as lines matching **`^## `** in order (verify with **`grep -n '^##' guides/per-query-tuning-pipeline.md`** showing nine sections in listed order).
    - **`grep -n 'rankingScoreThreshold' guides/per-query-tuning-pipeline.md`** returns ≥ 1 line.
    - **`grep -n 'showRankingScore' guides/per-query-tuning-pipeline.md`** returns ≥ 1 line.
    - **`grep -n 'TUNE-PQ' guides/per-query-tuning-pipeline.md`** returns ≥ 1 line inside the checklist section (authorization phrase).
    - **`mix test test/scrypath/docs_contract_test.exs`** exits 0 (new guide is not scanned for hygiene until Plan 02 adds it to `@guide_paths`; if the suite fails for unrelated reasons, fix before completing the phase).
  </acceptance_criteria>
  <done>
    Canonical guide file exists with locked H2 spine and TUNE-PIPE coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Publisher → adopters | Markdown is public documentation; must not leak internal planning artifacts. |
| Spec → implementers | Checklist gates runtime behavior in Phase 43 — incorrect gates could mis-route engineering effort (integrity, not confidentiality). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-01 | Tampering | Spec text introduces insecure defaults (e.g. encouraging silent credential handling) | mitigate | Doc describes library behavior only; no env examples with real secrets; link to existing operator guides for transport. |
| T-42-01-02 | Info disclosure | Accidental paste of internal tokens into published guide | mitigate | Run **`docs_contract_test`** after registration in Plan 02; follow hygiene rules in **action**. |

## Security Notes

Doc-only deliverable; no new executable paths.

## Security Verification Commands (ASVS L1)

```bash
grep -nE 'TUNE-[0-9]{2}\\b|\\(D-[0-9]|DocsContractTest|UI-SPEC' guides/per-query-tuning-pipeline.md || true
mix test test/scrypath/docs_contract_test.exs
```
</threat_model>

<verification>
- `guides/per-query-tuning-pipeline.md` contains the nine locked H2 sections in order.
- `mix test test/scrypath/docs_contract_test.exs` exits 0 after Plan 02 registers the guide and completes wiring tasks.
</verification>

<success_criteria>
- [ ] TUNE-PIPE-01 precedence and ordering documented (two-plane + right-biased stack).
- [ ] TUNE-PIPE-02 mapping categories + v1.9 exemplars + version stance.
- [ ] TUNE-PIPE-03 non-goals, errors, telemetry expectations documented.
- [ ] TUNE-PIPE-04 checklist authorizes TUNE-PQ implementation when complete.
</success_criteria>
