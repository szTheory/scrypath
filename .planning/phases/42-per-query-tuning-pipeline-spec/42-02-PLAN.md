---
phase: 42-per-query-tuning-pipeline-spec
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01-PLAN.md
files_modified:
  - mix.exs
  - test/scrypath/docs_contract_test.exs
  - README.md
  - guides/overview.md
  - guides/golden-path.md
  - guides/relevance-tuning.md
  - guides/multi-index-search.md
  - lib/scrypath.ex
autonomous: true
requirements: []
tags:
  - docs
  - exdoc
  - discoverability
must_haves:
  truths:
    - "HexDocs `extras` and Operations group list include `guides/per-query-tuning-pipeline.md` immediately after `guides/relevance-tuning.md`."
    - "`docs_contract_test` includes the new guide path and asserts stable spec anchors."
    - "`Scrypath.search/3` and `Scrypath.search_many/2` carry a compact pipeline paragraph linking to the canonical guide."
  artifacts:
    - path: "mix.exs"
      provides: "ExDoc extras registration"
      contains: "guides/per-query-tuning-pipeline.md"
---

<objective>
Make the Phase 42 spec **discoverable** (README, guides index, golden path, cross-links) and **API-documented** on **`Scrypath`**, matching **42-CONTEXT.md** D-02–D-07 and D-24; extend **`docs_contract_test`** so the new published surface cannot regress silently.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-per-query-tuning-pipeline-spec/42-CONTEXT.md
@guides/per-query-tuning-pipeline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register guide in mix.exs ExDoc extras</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (function `docs/0`, `:extras` list and `groups_for_extras` → `Operations`)
    - .planning/phases/42-per-query-tuning-pipeline-spec/42-CONTEXT.md (D-02)
  </read_first>
  <action>
    1. In **`mix.exs`** **`:extras`** list, insert **`"guides/per-query-tuning-pipeline.md"`** **immediately after** **`"guides/relevance-tuning.md"`**.
    2. In **`groups_for_extras`** under **`Operations:`**, insert the same path **immediately after** **`"guides/relevance-tuning.md"`**.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'guides/per-query-tuning-pipeline.md' mix.exs`** returns ≥ 2 lines (extras + Operations group).
    - In **`mix.exs`**, under **`"Operations:"`**, the line containing **`relevance-tuning.md`** appears **above** the line containing **`per-query-tuning-pipeline.md`** with **no other guide path lines between them** (verify by reading the **`Operations:`** list slice).
    - **`mix compile --warnings-as-errors`** exits 0.
  </acceptance_criteria>
  <done>
    ExDoc ships the new guide under Operations adjacent to relevance tuning.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend docs_contract_test for new guide</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (`@guide_paths`, `@published_markdown_for_hygiene`, existing guide-focused tests)
    - guides/per-query-tuning-pipeline.md (nine H2 spine)
  </read_first>
  <action>
    1. Append **`"guides/per-query-tuning-pipeline.md"`** to **`@guide_paths`** in the same relative path string style as siblings (immediately after **`"guides/relevance-tuning.md"`**).
    2. The **`@published_markdown_for_hygiene`** list is built from **`@guide_paths`** — ensure the new path is included (update list construction if the module duplicates paths manually).
    3. Add **`@per_query_tuning_pipeline File.read!("guides/per-query-tuning-pipeline.md")`** and one **`test`** that asserts **`@per_query_tuning_pipeline`** contains these **exact substrings**:
       - **`## Two-plane model and precedence`**
       - **`## Implementation readiness checklist`**
       - **`## Telemetry catalog`**
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'guides/per-query-tuning-pipeline.md' test/scrypath/docs_contract_test.exs`** returns ≥ 1 line in **`@guide_paths`**.
    - **`mix test test/scrypath/docs_contract_test.exs`** exits 0.
  </acceptance_criteria>
  <done>
    Doc contract suite pins the new guide path and spine anchors.
  </done>
</task>

<task type="auto">
  <name>Task 3: README billboard link</name>
  <files>README.md</files>
  <read_first>
    - README.md (Phoenix Wayfinding or Public Surface sections)
    - .planning/phases/42-per-query-tuning-pipeline-spec/42-CONTEXT.md (D-03)
  </read_first>
  <action>
    Add **one sentence** plus a Markdown link to **`guides/per-query-tuning-pipeline.md`** in **`README.md`**, placed in **`## Phoenix Wayfinding`** as a new bullet **after** the **[Multi-index search](guides/multi-index-search.md)** bullet. The sentence must mention **request-time** search options vs **index-time** settings (plain language) and use relative path **`guides/per-query-tuning-pipeline.md`** inside the link target.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'guides/per-query-tuning-pipeline.md' README.md`** returns ≥ 1 line.
    - **`mix test test/scrypath/docs_contract_test.exs`** exits 0.
  </acceptance_criteria>
  <done>
    README points adopters at the canonical pipeline spec.
  </done>
</task>

<task type="auto">
  <name>Task 4: Guides overview table row</name>
  <files>guides/overview.md</files>
  <read_first>
    - guides/overview.md (markdown table of guides)
    - .planning/phases/42-per-query-tuning-pipeline-spec/42-CONTEXT.md (D-05)
  </read_first>
  <action>
    Add a **table row** to the guides map in **`guides/overview.md`** for **`[Per-query tuning pipeline](per-query-tuning-pipeline.md)`** with purpose text describing **request-time** Meilisearch search parameters vs **index-time** settings (one short phrase).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'per-query-tuning-pipeline.md' guides/overview.md`** returns ≥ 1 line.
    - **`mix test test/scrypath/docs_contract_test.exs`** exits 0.
  </acceptance_criteria>
  <done>
    Overview TOC lists the new guide.
  </done>
</task>

<task type="auto">
  <name>Task 5: Golden path pointer after first search</name>
  <files>guides/golden-path.md</files>
  <read_first>
    - guides/golden-path.md (section around first `search_posts` / `result.records` demo)
    - .planning/phases/42-per-query-tuning-pipeline-spec/42-CONTEXT.md (D-04)
  </read_first>
  <action>
    Immediately **after** the fenced code block that ends with **`result.records`** (first successful **`Scrypath.search/3`** demo), insert **one** Markdown line (pointer-only): link **`[Per-query tuning pipeline](per-query-tuning-pipeline.md)`** and state that index-time relevance lives in **`relevance-tuning.md`** while request-time options are specified in the pipeline guide. Do **not** add a second tutorial — single line (or two short lines max).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'per-query-tuning-pipeline.md' guides/golden-path.md`** returns ≥ 1 line **before** the **`## Integration smoke`** heading (verify with **`awk '/per-query-tuning-pipeline.md/{print NR; exit}' guides/golden-path.md'`** and **`grep -n '^## Integration smoke' guides/golden-path.md'`** — pipeline link line number must be **less** than Integration smoke line number).
    - **`mix test test/scrypath/docs_contract_test.exs`** exits 0.
  </acceptance_criteria>
  <done>
    Golden path deep-links without duplicating the spec.
  </done>
</task>

<task type="auto">
  <name>Task 6: relevance-tuning.md cross-link</name>
  <files>guides/relevance-tuning.md</files>
  <read_first>
    - guides/relevance-tuning.md (introduction / first sections)
    - guides/per-query-tuning-pipeline.md (title line for consistent naming)
  </read_first>
  <action>
    In **`guides/relevance-tuning.md`**, add a short **“Request-time vs index-time”** note near the top (after the opening paragraph and before **`## The five declarative settings knobs`**): link to **`per-query-tuning-pipeline.md`** and state that **this** guide is **index settings (Plane A)**; the pipeline guide is **search request parameters (Plane B)**.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'per-query-tuning-pipeline.md' guides/relevance-tuning.md`** returns ≥ 1 line.
    - **`mix test test/scrypath/docs_contract_test.exs`** exits 0.
  </acceptance_criteria>
  <done>
    Index-time guide points at request-time spec.
  </done>
</task>

<task type="auto">
  <name>Task 7: multi-index-search.md appendix pointer</name>
  <files>guides/multi-index-search.md</files>
  <read_first>
    - guides/multi-index-search.md (intro or “See also” area)
    - .planning/phases/42-per-query-tuning-pipeline-spec/42-CONTEXT.md (D-24)
  </read_first>
  <action>
    Add a **short paragraph** (≤ 4 sentences) linking to **`per-query-tuning-pipeline.md`** for how **per-entry** / **shared** keyword precedence interacts with future **per-query tuning** keys — explicitly defer full federation narrative duplication to this file’s existing sections; the paragraph is only a **pointer** + **compatibility** framing per **D-24**.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'per-query-tuning-pipeline.md' guides/multi-index-search.md`** returns ≥ 1 line.
    - **`mix test test/scrypath/docs_contract_test.exs`** exits 0.
  </acceptance_criteria>
  <done>
    Multi-search guide acknowledges pipeline spec without forking merge semantics.
  </done>
</task>

<task type="auto">
  <name>Task 8: Scrypath @doc for search/3 and search_many/2</name>
  <files>lib/scrypath.ex</files>
  <read_first>
    - lib/scrypath.ex (`def search/3`, `def search_many/2`, neighboring `@doc` style)
    - .planning/phases/42-per-query-tuning-pipeline-spec/42-CONTEXT.md (D-06)
  </read_first>
  <action>
    1. Add **`@doc`** immediately **above** **`def search/3`** (currently undocumented). Include: one paragraph describing **`Scrypath.search/3`** as the primary hydrated search entry; mention options validation and telemetry span **`[:scrypath, :search]`** only if accurate per **`lib/scrypath/search.ex`**; include literal line **`Full pipeline:`** followed by a Markdown link to **`guides/per-query-tuning-pipeline.md`** (relative path string as used in other `@doc` blocks: **`guides/per-query-tuning-pipeline.md`**).
    2. **Prepend** (or append) to the existing **`@doc` for `search_many/2`** a second paragraph containing the same **`Full pipeline:`** sentence + link to **`guides/per-query-tuning-pipeline.md`**, and one sentence stating **`search_many/2`** merge rules remain canonical in **`guides/multi-index-search.md`**.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -B30 'def search(schema_module' lib/scrypath.ex | grep -c '@doc'`** returns **≥ 1** (a **`@doc`** precedes **`search/3`**).
    - **`grep -n 'Full pipeline:' lib/scrypath.ex`** returns **≥ 2** lines (one in **`search/3`** doc, one in **`search_many/2`** doc).
    - **`grep -n 'guides/per-query-tuning-pipeline.md' lib/scrypath.ex`** returns ≥ 2 lines.
    - **`mix compile --warnings-as-errors`** exits 0.
  </acceptance_criteria>
  <done>
    Public API docs compress to the canonical guide per D-06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer → ExDoc | Published `@doc` and guides are security-adjacent only via encouraging correct transport/config patterns. |
| Doc contract CI | Fails closed on forbidden internal tokens in published markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02-01 | Tampering | `@doc` suggests embedding secrets in search options | mitigate | Do not add examples with real API keys; follow existing doc tone in **`lib/scrypath.ex`**. |

## Security Notes

No new network endpoints; documentation and compile-time checks only.

## Security Verification Commands (ASVS L1)

```bash
mix compile --warnings-as-errors
mix test test/scrypath/docs_contract_test.exs
```
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/scrypath/docs_contract_test.exs` exits 0.
- `grep -RIn 'guides/per-query-tuning-pipeline.md' mix.exs README.md guides/overview.md guides/golden-path.md guides/relevance-tuning.md guides/multi-index-search.md lib/scrypath.ex test/scrypath/docs_contract_test.exs`
</verification>

<success_criteria>
- [ ] ExDoc extras + Operations group register the guide after relevance tuning.
- [ ] README, overview, golden path, relevance tuning, and multi-index guides cross-link per CONTEXT.
- [ ] `Scrypath.search/3` and `Scrypath.search_many/2` include `Full pipeline:` links.
- [ ] Doc contract suite green with spine anchors.
</success_criteria>
