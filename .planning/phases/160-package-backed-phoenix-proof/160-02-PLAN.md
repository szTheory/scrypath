---
phase: 160-package-backed-phoenix-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 160-01
files_modified:
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
  - examples/phoenix_meilisearch/README.md
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - PROOF-01
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The existing advisory Phoenix service job runs the path-backed proof first and package-backed proof second with its current Postgres and Meilisearch services."
    - "Maintainer documentation names the package command and the existing service prerequisites while preserving the no-argument path workflow."
    - "Automated documentation contracts fail if command behavior, CI order/advisory status, service requirements, or the example path dependency drift."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Ordered path then package proof steps within the existing advisory service job"
    - path: "CONTRIBUTING.md"
      provides: "Maintainer-facing package proof command and advisory lane classification"
    - path: "examples/phoenix_meilisearch/README.md"
      provides: "Canonical command and service-prerequisite runbook for path and package proof modes"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Machine-checked command, CI order, advisory status, prerequisite, and path-workflow alignment"
  key_links:
    - from: "phoenix-example advisory job"
      to: "mix verify.phoenix_example --package"
      via: "the second command step follows the preserved path-backed command in the same service job"
      pattern: "run: mix verify.phoenix_example --package"
    - from: "CONTRIBUTING.md and example README"
      to: "Mix capability and service preflight"
      via: "documented command and required environment names match executable behavior"
      pattern: "SCRYPATH_EXAMPLE_INTEGRATION"
    - from: "docs contract test"
      to: "CI job and example dependency declaration"
      via: "scoped job assertions verify order/status while example mix.exs remains path-backed"
      pattern: "continue-on-error: true"
---

<objective>
Make the package-backed Phoenix proof part of the existing advisory lane and keep maintainer guidance machine-checked against its executable contract.

Purpose: Ensure the documented command, CI run order, service environment, and unchanged example workflow remain a single deterministic proof path.
Output: Sequential advisory CI wiring, concise contributor/example instructions, and docs contract assertions that detect drift.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/160-package-backed-phoenix-proof/160-CONTEXT.md
@.planning/phases/160-package-backed-phoenix-proof/160-RESEARCH.md
@.github/workflows/ci.yml
@CONTRIBUTING.md
@examples/phoenix_meilisearch/README.md
@examples/phoenix_meilisearch/mix.exs
@test/scrypath/docs_contract_test.exs

The Wave 1 plan establishes --package dispatch and its live prerequisites. This plan owns only CI and documentation contracts, so its files do not overlap with Wave 1 implementation files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run both Phoenix proofs in order in the current advisory service lane</name>
  <files>.github/workflows/ci.yml</files>
  <action>In the existing phoenix-example job, retain the current service declarations, environment, advisory continue-on-error setting, and mix verify.phoenix_example step. Add a following step that runs mix verify.phoenix_example --package, so both runs share the job's Postgres and Meilisearch services but start independent Mix processes. Do not create another service job or alter required merge gates. Implement this exact in-job sequence per D-03.</action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
    <fails_when>Non-zero exit, or Mix reports zero tests executed.</fails_when>
  </verify>
  <done>The existing advisory job runs the no-argument path proof and then the package proof against the same service block, with continue-on-error still set on that job.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align maintainer instructions and guard command, service, and CI behavior against drift</name>
  <files>CONTRIBUTING.md, examples/phoenix_meilisearch/README.md, test/scrypath/docs_contract_test.exs</files>
  <behavior>
    - Test 1: Maintainer and example runbooks name mix verify.phoenix_example --package, preserve the no-argument path-backed command, and identify the existing SCRYPATH_EXAMPLE_INTEGRATION, PGPORT, and SCRYPATH_MEILISEARCH_URL prerequisites.
    - Test 2: A job-scoped contract finds the path command before the package command inside phoenix-example and asserts the job remains advisory and provisions Postgres 16 and Meilisearch v1.15.
    - Test 3: The example dependency declaration remains path-backed for its ordinary workflow.
  </behavior>
  <action>Update the Phoenix capability row and live-proof prose in CONTRIBUTING.md to show both existing path mode and --package mode while keeping the Phoenix lane labeled advisory. Update examples/phoenix_meilisearch/README.md, the authoritative detailed runbook, with the package command beside the existing command and the same three environment names and Postgres/Meilisearch prerequisites; keep its path: dependency declaration and script workflow intact. Extend DocsContractTest with job-scoped assertions that check command order inside phoenix-example, its continue-on-error setting and Postgres 16/Meilisearch v1.15 services, plus documentation coverage for both command modes and the environment names. Assert examples/phoenix_meilisearch/mix.exs still declares its normal path dependency. Cover D-01/D-02 command behavior, D-03 advisory topology, and D-04/D-05 diagnostics/prerequisite truth without publishing internal decision IDs.</action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs test/mix/tasks/verify_capability_test.exs test/mix/tasks/verify_phoenix_example_package_test.exs</automated>
    <fails_when>Non-zero exit, or Mix reports zero tests executed.</fails_when>
  </verify>
  <done>The contributor and example runbooks describe the same package command and service inputs as the implementation; the docs contract catches regressions in command order, advisory status, service versions, and the normal path dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository CI config → GitHub Actions | Workflow edits control whether live service proof stays advisory and which commands share service instances. |
| Maintainer documentation → command invocation | Readers rely on the documented modes and prerequisite names to run service-backed verification correctly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-160-04 | Tampering | phoenix-example job topology | medium | mitigate | Keep both commands inside the existing job and assert order, service block, and advisory status in the docs contract test. |
| T-160-05 | Information disclosure | Maintainer docs and example runbook | low | mitigate | Document variable names and safe local defaults only; do not add credential values or captured environment output. |
</threat_model>

<verification>
Run the docs contract and focused capability/package tests. Confirm the CI assertion scopes both commands to the phoenix-example job and that the job retains its current service definitions and advisory status.
</verification>

<success_criteria>
The command, CI wiring, and maintainer documentation agree under an automated contract; the existing path proof and advisory service lane remain intact.
</success_criteria>

<output>
Create .planning/phases/160-package-backed-phoenix-proof/160-02-SUMMARY.md when done.
</output>
