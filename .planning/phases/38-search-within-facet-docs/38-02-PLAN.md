---
phase: 38-search-within-facet-docs
plan: 02
type: execute
wave: 2
depends_on:
  - "38-01"
files_modified:
  - guides/faceted-search-with-phoenix-liveview.md
  - test/scrypath/docs_contract_test.exs
  - lib/mix/tasks/verify.phase38.ex
  - mix.exs
  - README.md
  - lib/scrypath.ex
autonomous: true
requirements:
  - FACET-04
tags:
  - documentation
  - liveview-guide
  - verify-task
  - readme
must_haves:
  truths:
    - "Guide contains two new **`##`** sections with exact titles **`## Searching within a facet selection`** and **`## Composing facet filters with scoped search`** describing AND composition, duplicate-attribute rejection, and LiveView footgun (URL assigns vs duplicate `facet_filter:`) without internal requirement IDs in prose."
    - "`docs_contract_test.exs` locks **≥2** heading strings plus **≥2** short explanatory phrases from those sections."
    - "`mix verify.phase38` runs focused tests including **`search_within_facet_test.exs`** and **`docs_contract_test.exs`**; **`mix.exs`** registers **`verify.phase38`** in **`preferred_cli_env`**."
  artifacts:
    - path: "guides/faceted-search-with-phoenix-liveview.md"
      provides: "FACET-04 prose for scoped facet search"
      contains: "Searching within a facet selection"
    - path: "lib/mix/tasks/verify.phase38.ex"
      provides: "Focused verify entrypoint"
      contains: "Phase 38"
---

<objective>
Deliver **FACET-04**: add the two locked **`##`** sections to **`guides/faceted-search-with-phoenix-liveview.md`**, extend **`test/scrypath/docs_contract_test.exs`** with stable anchors (headings + short phrases; **no** `FACET-NN` strings in published markdown), add **`lib/mix/tasks/verify.phase38.ex`** plus **`mix.exs`** **`preferred_cli_env`**, and add **thin** discoverability in **`README.md`** (one line + link to guide) and **`Scrypath`** **`@moduledoc`** **see also** pointer to **`search_within_facet/4`** — per **`38-CONTEXT.md`** D-13–D-16, D-15.
</objective>

<threat_model>
**ASVS V5.1 L1 — documentation / social engineering**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| **T-38-03:** Docs imply multi-search or disjunctive counts ship with **`search_within_facet`** | Medium | Explicit prose: single POST `/search`; cross-link to **Disjunctive facet counts** section only as “different feature” |
| **T-38-04:** Hygiene regression — internal REQ IDs in user docs | Medium | `docs_contract_test.exs` **`published markdown`** patterns already forbid **`FACET-NN`**; reviewer greps changed markdown before commit |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Guide sections in `guides/faceted-search-with-phoenix-liveview.md`</name>
  <files>guides/faceted-search-with-phoenix-liveview.md</files>
  <read_first>
    - guides/faceted-search-with-phoenix-liveview.md (full file — place after **Disjunctive facet counts** section, before **Primary path** OR after **Hierarchical facets** per reading flow; must not break existing contract anchors)
    - .planning/phases/38-search-within-facet-docs/38-CONTEXT.md (D-13, D-05–D-07)
  </read_first>
  <action>
    1. Insert **`## Searching within a facet selection`** with **≥3** short paragraphs explaining: **`Scrypath.search_within_facet/4`**, **`{facet_attribute, value}`** bucket, AND semantics with the rest of the query, link to **`Scrypath.search/3`** as the general path.
    2. Insert **`## Composing facet filters with scoped search`** covering: **`filter:`** AND **`facet_filter:`** on **other** attributes compose with the bucket; **duplicate** facet attribute in **`facet_filter:`** raises (**library rejects — do not duplicate URL param + assigns**); mention dotted hierarchical facet atoms follow the same **one-attribute** rule.
    3. Include the exact sentence **`search_within_facet:`** does **not** change disjunctive count mechanics (so **`docs_contract_test`** can anchor without REQ IDs).
    4. Do **not** insert strings matching **`~r/\bFACET-\d{2}\b/`** or other forbidden hygiene patterns from **`docs_contract_test.exs`**.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^## Searching within a facet selection$' guides/faceted-search-with-phoenix-liveview.md` returns **≥1** line.
    - `grep -n '^## Composing facet filters with scoped search$' guides/faceted-search-with-phoenix-liveview.md` returns **≥1** line.
    - `grep -n 'Scrypath.search_within_facet' guides/faceted-search-with-phoenix-liveview.md` returns **≥1** line.
    - `grep -nE '\bFACET-[0-9]{2}\b' guides/faceted-search-with-phoenix-liveview.md` returns **exit code 1** (no matches).
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: `docs_contract_test.exs` anchors + `README` + `@moduledoc`</name>
  <files>test/scrypath/docs_contract_test.exs, README.md, lib/scrypath.ex</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (phase 37 tests pattern — `@guides` map and `assert_contains_all/2` if present)
    - README.md (facet / LiveView discoverability — find **`faceted-search-with-phoenix-liveview.md`** or facet mentions)
    - lib/scrypath.ex (`@moduledoc` opening — minimal **see also** line)
  </read_first>
  <action>
    1. Add **`test`** block **`"phase 38 scoped facet search guide headings and phrases"`** (name can vary) asserting **`@guides["guides/faceted-search-with-phoenix-liveview.md"]`** contains exact headings **`## Searching within a facet selection`** and **`## Composing facet filters with scoped search`** and contains **≥2** of these phrases (pick stable lines you actually authored in Task 1): **`search_within_facet`**, **`search_within_facet:`**, **`duplicate`**, **`AND`** (use longest unique substring from Task 1 prose to avoid brittle tests — minimum **8 characters** per substring).
    2. Add **`@verify_phase38 File.read!("lib/mix/tasks/verify.phase38.ex")`** near other **`@verify_phase`** reads and one test asserting file contains **`search_within_facet_test.exs`** string in the focused test list array.
    3. **`README.md`:** add **exactly one** short bullet or sentence under an existing facet / search section that contains substring **`search_within_facet`** and a **Markdown link** to **`guides/faceted-search-with-phoenix-liveview.md`** (relative path as used elsewhere in README).
    4. **`lib/scrypath.ex`:** append to **`@moduledoc`** a **single** sentence **see also** line containing **`search_within_facet/4`** and the guide path **`guides/faceted-search-with-phoenix-liveview.md`**.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'phase 38' test/scrypath/docs_contract_test.exs` returns **≥1** line (case-insensitive acceptable).
    - `grep -n 'verify.phase38' test/scrypath/docs_contract_test.exs` returns **≥1** line.
    - `grep -n 'search_within_facet' README.md` returns **≥1** line.
    - `grep -n 'search_within_facet/4' lib/scrypath.ex` returns **≥1** line inside **`@moduledoc`** block (or entire file if moduledoc is contiguous).
    - `mix test test/scrypath/docs_contract_test.exs` exits **0** after Tasks 1–2 complete.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: `Mix.Tasks.Verify.Phase38` + `mix.exs` registration</name>
  <files>lib/mix/tasks/verify.phase38.ex, mix.exs</files>
  <read_first>
    - lib/mix/tasks/verify.phase37.ex (full file — copy structure)
    - mix.exs (`preferred_cli_env` section)
  </read_first>
  <action>
    1. Create **`lib/mix/tasks/verify.phase38.ex`** as **`Mix.Tasks.Verify.Phase38`** with **`@shortdoc`** mentioning **scoped facet search** + docs contracts; **`@focused_tests`** list must include **`"test/scrypath/search_within_facet_test.exs"`** and **`"test/scrypath/docs_contract_test.exs"`**; add **≥1** more existing high-signal test path if needed for confidence (e.g. **`test/scrypath/meilisearch/query_test.exs`**).
    2. **`@impl true def run(args)`** calls **`Mix.Task.run("app.start")`**, **`ensure_no_args!(args)`**, then **`run_test!(@focused_tests, "Phase 38 search within facet + docs verification")`** — mirror phase 37 exactly.
    3. Add **`"verify.phase38": :test`** to **`mix.exs`** **`preferred_cli_env`** next to **`verify.phase37`**.
  </action>
  <verify>
    <automated>mix verify.phase38</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/mix/tasks/verify.phase38.ex` exits **0**.
    - `grep -n 'verify.phase38' mix.exs` returns **≥1** line.
    - `grep -n 'search_within_facet_test.exs' lib/mix/tasks/verify.phase38.ex` returns **≥1** line.
    - `mix verify.phase38` exits **0** after plan 01 tests exist and pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix verify.phase38`
- `mix test test/scrypath/docs_contract_test.exs`
</verification>

<success_criteria>
- [ ] Guide headings and prose match **`38-CONTEXT.md`** composition story; contracts locked in ExUnit.
- [ ] **`mix verify.phase38`** is the documented local slice for this phase.
</success_criteria>
