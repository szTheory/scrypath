---
phase: "95"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/facet_search_result.ex
  - test/scrypath/facet_search_result_test.exs
  - lib/scrypath/backend.ex
  - test/support/fake_backend.ex
  - scrypath_ops/lib/scrypath_ops/search_playground/adapter.ex
  - lib/scrypath/meilisearch/client.ex
  - test/scrypath/meilisearch/client_test.exs
  - lib/scrypath/meilisearch.ex
  - test/scrypath/meilisearch_test.exs
autonomous: true
requirements:
  - FACET-UX-01
  - FACET-UX-02
must_haves:
  truths:
    - "Backend interface defines search_facet_values callback"
    - "Test environments compile without warnings because mocks implement the new callback"
    - "Meilisearch client posts to facet-search endpoint with correct payload"
  artifacts:
    - path: "lib/scrypath/facet_search_result.ex"
      provides: "Struct definition and parsing for facet results"
    - path: "lib/scrypath/backend.ex"
      provides: "Callback contract"
    - path: "lib/scrypath/meilisearch/client.ex"
      provides: "HTTP client integration"
  key_links:
    - from: "lib/scrypath/meilisearch.ex"
      to: "lib/scrypath/meilisearch/client.ex"
      via: "facet_search call"
---

<objective>
Introduce the backend contract and provider implementation for facet value searches.

Purpose: Facet value search targets a different Meilisearch endpoint (`/facet-search`) and returns a different payload shape than standard search. This plan lays the foundation by defining the interfaces, structs, and backend integrations.
Output: Contracts, `FacetSearchResult` struct, updated test fakes, and Meilisearch provider implementation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-DISCUSS.md
@.planning/phases/95-PATTERNS.md
@.planning/phases/95-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interface contracts and test fakes</name>
  <files>lib/scrypath/facet_search_result.ex, test/scrypath/facet_search_result_test.exs, lib/scrypath/backend.ex, test/support/fake_backend.ex, scrypath_ops/lib/scrypath_ops/search_playground/adapter.ex</files>
  <behavior>
    - `FacetSearchResult.new/1` parses `facetHits` into a list of `%Scrypath.SearchResult.Facets.Bucket{}` structs.
    - Test fakes return a dummy `{:ok, %{}}` or delegate appropriately to satisfy compilation.
  </behavior>
  <action>
    Create `Scrypath.FacetSearchResult` struct with keys `[:facet_query, :hits, :raw]`. Explicitly reference the analog file `lib/scrypath/search_result.ex` as defined in 95-PATTERNS.md for struct enforcement and parsing conventions.
    Add `@callback search_facet_values(module(), String.t(), String.t(), keyword(), keyword()) :: {:ok, map()} | {:error, term()}` to `Scrypath.Backend` (referencing its existing `@callback` analogs).
    Update `FakeBackend` to implement `search_facet_values/5` returning `{:ok, %{"facetHits" => []}}` (referencing `test/support/fake_backend.ex` analogs).
    Update `ScrypathOps.SearchPlayground.Adapter` (which delegates to Scrypath) to implement the callback if it requires a matching definition (referencing `scrypath_ops/lib/scrypath_ops/search_playground/adapter.ex` analogs).
  </action>
  <verify>
    <automated>mix test test/scrypath/facet_search_result_test.exs</automated>
  </verify>
  <done>Contracts defined, struct parses raw hits, and tests pass for struct construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Meilisearch provider implementation</name>
  <files>lib/scrypath/meilisearch/client.ex, test/scrypath/meilisearch/client_test.exs, lib/scrypath/meilisearch.ex, test/scrypath/meilisearch_test.exs</files>
  <behavior>
    - `Client.facet_search/5` issues a POST to `/indexes/:index_name/facet-search`.
    - Payload JSON maps `facetName` and `facetQuery`.
  </behavior>
  <action>
    In `Scrypath.Meilisearch.Client`, add `facet_search(index_name, facet_name, facet_query, opts, config)` mirroring `search/3` telemetry and HTTP setup. Reference the `lib/scrypath/meilisearch/client.ex` analog from 95-PATTERNS.md for HTTP payload and telemetry handling.
    In `Scrypath.Meilisearch`, add `search_facet_values(schema_module, facet_name, search_string, opts, config)` that determines `index_name` and calls `client(config).facet_search/5`. Reference the `lib/scrypath/meilisearch.ex` analog for alias resolution and client delegation.
  </action>
  <verify>
    <automated>mix test test/scrypath/meilisearch_test.exs test/scrypath/meilisearch/client_test.exs</automated>
  </verify>
  <done>Backend correctly formats and dispatches the payload to Meilisearch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Facet search inputs (`facet_name`, `facet_query`) cross into the backend provider. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Tampering | `Client` payload | mitigate | Inputs are encoded into JSON HTTP payload, preventing injection at the transport layer. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test test/scrypath/facet_search_result_test.exs test/scrypath/meilisearch_test.exs test/scrypath/meilisearch/client_test.exs
</verification>

<success_criteria>
Backend interfaces, structs, and implementations compile cleanly without warnings, and specific tests for facet search and meilisearch client pass.
</success_criteria>

<output>
After completion, create `.planning/phases/95-01-SUMMARY.md`
</output>
