---
phase: 47
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs
  - scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs
  - scrypath_ops/test/scrypath_ops_web/live/failed_sync_live_test.exs
  - scrypath_ops/test/scrypath_ops_web/live/sync_drift_live_test.exs
  - scrypath_ops/test/scrypath_ops_web/live/search_live_test.exs
  - scrypath_ops/docs/operator-ia.md
autonomous: true
requirements:
  - OPSUI-10
---

<objective>

Add a **narrow** **`operator_ia_contract_test.exs`** under **`scrypath_ops/test/`** pinning **`operator-ia.md`** spine and **`/ops/*`** routes against **`router.ex`**, and extend **existing** LiveView tests only where **47-CONTEXT** D-07–D-17 gaps remain (auth fail-closed, allowlist, posture honesty, failed-sync counts, sync/drift separation, search semantics) — **no** new product UI.

</objective>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-47-04 | Tests pass while **`/ops`** is reachable without auth in production config | high | Add or extend tests that compile **`runtime.exs` / auth config** paths or HTTP/LiveView assertions proving unknown **`SCRYPATH_OPS_AUTH_MODE`** (or documented sentinel) yields **404** or **halt** consistent with **44-CONTEXT** D-14 — use the **exact** env key names from **`scrypath_ops/docs/SECURITY.md`**. |
| T-47-05 | Contract test couples to decorative copy | low | Restrict **`operator_ia_contract_test.exs`** to heading markers, nav order tokens, and route path substrings listed in **47-CONTEXT** D-07 — not full prose paragraphs. |

</threat_model>

<tasks>

<task id="47-03-01" type="execute">
<read_first>
- `.planning/phases/47-verification-hardening/47-CONTEXT.md` (D-07, D-17)
- `.planning/phases/47-verification-hardening/47-UI-SPEC.md`
- `test/scrypath/docs_contract_test.exs` (first 120 lines: patterns)
- `scrypath_ops/docs/operator-ia.md`
- `scrypath_ops/lib/scrypath_ops_web/router.ex`
</read_first>
<action>

1. Create **`scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs`** as **`ExUnit.Case`**, **`async: true`** if it only reads files at compile time (match **`DocsContractTest`** style).

2. At module attribute compile time, **`File.read!/1`** for **`scrypath_ops/docs/operator-ia.md`** and **`scrypath_ops/lib/scrypath_ops_web/router.ex`** using paths relative to project root OR `Application.app_dir` — pick one approach used elsewhere in **`scrypath_ops` tests** and stay consistent.

3. Add **≥3** tests that together prove:
   - **Ordered** presence of **≥2** major **`##`** headings you select from **`operator-ia.md`** (stable spine — list the exact heading strings in the test module as literals).
   - **Every** **`live "/ops/..."`** path segment in **`router.ex`** appears as a substring in **`operator-ia.md`** **or** in a small explicit allowlist documented in a comment (routes: **`/posture`**, **`/failed-sync`**, **`/sync-drift`**, **`/search`** under **`/ops`**).

4. If **`operator-ia.md`** lacks a route row for a **`router.ex`** live path, **update `operator-ia.md` only** to restore parity (minimal table row — no product redesign).

</action>
<acceptance_criteria>

- `test -f scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs` exits **0**.
- `grep -n "operator-ia.md" scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs` returns **≥1** line.
- `cd scrypath_ops && mix test test/scrypath_ops_web/operator_ia_contract_test.exs` exits **0**.

</acceptance_criteria>
</task>

<task id="47-03-02" type="execute">
<read_first>
- `.planning/phases/47-verification-hardening/47-CONTEXT.md` (D-10–D-16)
- `scrypath_ops/docs/SECURITY.md`
- `scrypath_ops/test/support/conn_case.ex` (auth / session helpers if any)
- `scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs`
- `scrypath_ops/test/scrypath_ops_web/live/failed_sync_live_test.exs`
- `scrypath_ops/test/scrypath_ops_web/live/sync_drift_live_test.exs`
- `scrypath_ops/test/scrypath_ops_web/live/search_live_test.exs`
</read_first>
<action>

1. **`grep -r "auth\\|SCRYPATH_OPS" scrypath_ops/test/scrypath_ops_web/live`** — for each **D-10** requirement clause in **47-CONTEXT**, either reference an **existing** test name that satisfies it in a short comment in the most relevant test file, **or** add a **new** `test "..."` in the appropriate `*_live_test.exs` proving fail-closed behavior using the **same env keys** documented in **`SECURITY.md`**.

2. **D-11 (allowlist):** Add one test (in **`search_live_test.exs`** or shared module) that temporarily sets **`Application.put_env(:scrypath_ops, :schema_allowlist, [])`** (or minimal list) and asserts **`/ops/search`** does **not** widen targets beyond config (exact assertion: **`html =~`** a substring proving empty schema list / disabled controls — pick a stable string from the LiveView today).

3. **D-12 / D-13:** Extend **`posture_live_test.exs`** / **`failed_sync_live_test.exs`** with injected **`{:ok, %Scrypath.Operator.Status{}}` / `{:error, _}`** rows or **`reason_class_counts: true`** snapshot data **only if** current tests do not already cover; use **`Scrypath.Operator`** structs as in phase-45 tests if present in repo.

4. **D-14:** In **`sync_drift_live_test.exs`**, assert default reconcile path does **not** require drift section success when drift is forced to error (match **45-CONTEXT** semantics — use existing stub levers first).

5. **D-15–D-16:** Confirm **`search_live_test.exs`** covers **no auto-run on mount**, **page size ceiling copy includes `50`**, and **partial vs hard error** — add missing assertions with **`data-testid`** or exact copy substrings from **`46-UI-SPEC.md`**.

All new tests keep **`async: false`** when mutating **`Application` env** and use **`on_exit`** restore like sibling files.

</action>
<acceptance_criteria>

- `cd scrypath_ops && mix test test/scrypath_ops_web/live/` exits **0**.
- `cd scrypath_ops && mix test` exits **0** after all LiveView / contract edits.
- `grep -r "schema_allowlist" scrypath_ops/test/scrypath_ops_web/live/*.exs` returns **≥1** line after work (D-11 allowlist traceability).

</acceptance_criteria>
</task>

</tasks>

<verification>

1. `cd scrypath_ops && mix test` exits **0**.
2. `mix verify.opsui` from repo root exits **0**.

</verification>

<success_criteria>

- Doc/router parity test exists; LiveView suite encodes critical OPSUI honesty + security wiring per **47-CONTEXT**.

</success_criteria>

<must_haves>

- [ ] **`operator_ia_contract_test.exs`** green
- [ ] Live tests extended only where gaps proven
- [ ] Full **`scrypath_ops`** test suite green

</must_haves>
