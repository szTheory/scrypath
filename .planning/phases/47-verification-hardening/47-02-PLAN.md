---
phase: 47
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .formatter.exs
  - lib/mix/tasks/verify.opsui.ex
  - CONTRIBUTING.md
autonomous: true
requirements:
  - OPSUI-10
---

<objective>

Add **`mix verify.opsui`** at the repo root (with **`preferred_envs`**) that delegates to the same commands as CI (**`cd scrypath_ops && mix test`**), extend **`.formatter.exs`** so root **`mix format --check-formatted`** covers **`scrypath_ops`**, and document the maintainer entry in **`CONTRIBUTING.md`** per **47-CONTEXT** D-04–D-05.

</objective>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-47-02 | **`verify.opsui`** drifts from CI (different cwd or skips **`ecto.migrate`**) | high | Task implementation must invoke the **`scrypath_ops`** **`test` alias** path exactly: **`cd scrypath_ops && mix test`** (which already runs **`ecto.create`** / **`ecto.migrate`** per **`mix.exs`**) — no bare **`mix test`** from repo root without `cd`. |
| T-47-03 | Formatter change accidentally includes secrets paths | low | Only add glob **`scrypath_ops/{config,lib,test}/**/*.{ex,exs}`** and **`scrypath_ops/mix.exs`** to **`.formatter.exs`** `inputs` — do not widen beyond ops app sources. |

</threat_model>

<tasks>

<task id="47-02-01" type="execute">
<read_first>
- `.planning/phases/47-verification-hardening/47-CONTEXT.md` (D-04–D-05)
- `mix.exs` (top of file: **`cli/0`**, **`preferred_envs`** list)
- `lib/mix/tasks/verify.phase14.ex` (task structure)
- `.formatter.exs`
</read_first>
<action>

1. Add **`lib/mix/tasks/verify.opsui.ex`** defining **`Mix.Tasks.Verify.Opsui`** with **`@shortdoc`** describing OPSUI verification. **`run/1`** must:
   - Reject unexpected args the same style as **`Mix.Tasks.Verify.Phase14`** (`ensure_no_args!` or equivalent).
   - Log a clear banner, then run **from repo root**: shell command **`cd scrypath_ops && mix deps.get && mix test`** using **`Mix.shell().cmd/2`** with **`into: IO.stream(:stdio, :line)`** or documented equivalent so failures propagate non-zero exit.

2. In root **`mix.exs`**, add **`"verify.opsui": :test`** to the **`preferred_envs`** keyword list inside **`cli/0`** alongside existing **`verify.phase*`** entries.

3. Update **`.formatter.exs`** `inputs` to include **`"scrypath_ops/{config,lib,test}/**/*.{ex,exs}"`** and **`"scrypath_ops/mix.exs"`** (exact strings) in addition to existing patterns so **`mix format --check-formatted`** at root validates ops sources.

4. In **`CONTRIBUTING.md`**, add a short subsection or bullet under the verification / testing area that states: run **`mix verify.opsui`** from the repository root for the OPSUI app slice (include the exact command string **`mix verify.opsui`** verbatim).

</action>
<acceptance_criteria>

- `test -f lib/mix/tasks/verify.opsui.ex` exits **0**.
- `grep -n '"verify.opsui": :test' mix.exs` returns **≥1** line.
- `grep -n "scrypath_ops" .formatter.exs` returns **≥1** line.
- `grep -n "mix verify.opsui" CONTRIBUTING.md` returns **≥1** line.
- From repo root after implementation: `mix verify.opsui` exits **0** (executor runs this once).

</acceptance_criteria>
</task>

</tasks>

<verification>

1. `mix format --check-formatted` from repo root exits **0**.
2. `mix verify.opsui` from repo root exits **0**.

</verification>

<success_criteria>

- Single documented alias matches CI behavior; formatting covers **`scrypath_ops`**.

</success_criteria>

<must_haves>

- [ ] **`Mix.Tasks.Verify.Opsui`** task file
- [ ] **`preferred_envs`** entry for **`verify.opsui`**
- [ ] **`.formatter.exs`** includes **`scrypath_ops`**
- [ ] **`CONTRIBUTING.md`** mentions **`mix verify.opsui`**

</must_haves>
