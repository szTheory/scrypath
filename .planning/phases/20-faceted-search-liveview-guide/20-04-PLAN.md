---
phase: 20-faceted-search-liveview-guide
plan: 04
type: execute
wave: 4
depends_on:
  - "20-01"
  - "20-02"
  - "20-03"
files_modified:
  - guides/faceted-search-with-phoenix-liveview.md
  - mix.exs
  - test/scrypath/docs_contract_test.exs
  - test/support/docs/phoenix_examples_test.exs
  - guides/phoenix-liveview.md
  - CHANGELOG.md
autonomous: true
requirements:
  - FACET-08
tags:
  - documentation
  - liveview
  - exdoc
must_haves:
  truths:
    - "`guides/faceted-search-with-phoenix-liveview.md` exists, is listed in `mix.exs` `docs/0` `:extras` under the Phoenix group, and satisfies `20-UI-SPEC.md` for the four canonical patterns + copy strings where locked."
    - "Anti-pattern appendix contains **≥7** entries across labeled bands `API`, `Meilisearch`, `UI` per `20-CONTEXT.md` D-04."
    - "`DocsContractTest` asserts presence of stable section headers and cross-links (`Scrypath.search/3`, guide filename string)."
    - "`PhoenixExamplesTest` (or equivalent) compiles critical HEEx + LiveView module snippets extracted from the guide."
  artifacts:
    - path: "guides/faceted-search-with-phoenix-liveview.md"
      provides: "FACET-08 narrative + worked example + appendix"
      contains: "handle_params"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "contract strings for new guide"
      contains: "faceted-search-with-phoenix-liveview"
  key_links:
    - from: "mix docs"
      to: "guides/faceted-search-with-phoenix-liveview.md"
      via: ":extras"
      pattern: "faceted-search"
---

<objective>
Ship FACET-08: the faceted LiveView guide as the primary documentation artifact for Phase 20, wired into ExDoc, enforced by docs contract tests, and compile-checked via the Phoenix examples harness. Add minimal cross-links from `guides/phoenix-liveview.md` and a CHANGELOG entry under **Unreleased** for the facet feature set.

Output: New guide markdown, updated `mix.exs` extras/groups, extended tests, optional one-line wayfinding in an existing Phoenix guide — no runnable `examples/` app (explicit non-goal per CONTEXT D-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-faceted-search-liveview-guide/20-UI-SPEC.md
@.planning/phases/20-faceted-search-liveview-guide/20-CONTEXT.md
@guides/phoenix-liveview.md
@test/scrypath/docs_contract_test.exs
@test/support/docs/phoenix_examples_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `guides/faceted-search-with-phoenix-liveview.md`</name>
  <files>guides/faceted-search-with-phoenix-liveview.md</files>
  <read_first>
    - .planning/phases/20-faceted-search-liveview-guide/20-UI-SPEC.md (full file)
    - .planning/phases/20-faceted-search-liveview-guide/20-CONTEXT.md (D-02 URL sync, D-04 appendix)
    - guides/phoenix-liveview.md (tone, heading depth)
    - guides/relevance-tuning.md (ExDoc-style depth reference)
  </read_first>
  <action>
    1. Create `guides/faceted-search-with-phoenix-liveview.md` with sections (minimum):
       - Overview + prerequisites (link Phase 19 relevance guide for settings context).
       - Schema snippet: `FacetableMovie`-style `faceting:` + `filterable:` alignment.
       - **Primary path:** LiveView module using `handle_params/3` + `push_patch/2` or `<.link patch={~p\"...\"}>` with normalized query params for text + facets (per D-02).
       - Sidebar checklist, chip row, numeric range, search-within-facet sections matching UI-SPEC ordering; state explicitly that search-within-facet is assign-filter only (not Meilisearch facet-search API).
       - **Progressive disclosure** subsection: short `handle_event`-only variant with disclaimer about refresh/deep links.
       - Anti-pattern appendix: ≥7 entries, grep-friendly titles, bands `API` | `Meilisearch` | `UI`, per-entry template from D-04.
    2. Use UI-SPEC copy verbatim for: primary CTA **Search catalog**, empty state strings, error pattern, chip `aria-label` pattern, unknown facet dev string.
  </action>
  <verify>
    <automated>wc -l guides/faceted-search-with-phoenix-liveview.md | awk '$1 >= 200 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `guides/faceted-search-with-phoenix-liveview.md` exists with line count ≥ 200.
    - `grep -c '^## '` on the file returns ≥ 8 (multiple major sections).
    - `grep -F 'Search catalog' guides/faceted-search-with-phoenix-liveview.md` returns exit 0.
    - `grep -F 'handle_params' guides/faceted-search-with-phoenix-liveview.md` returns exit 0.
    - Appendix contains the literal substring `### API` or `## API` and `Meilisearch` and `UI` band headers (choose one heading style and grep accordingly).
  </acceptance_criteria>
  <done>
    Guide content meets FACET-08 narrative scope and UI-SPEC locks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ExDoc + docs contract + Phoenix compile-check snippets</name>
  <files>mix.exs, test/scrypath/docs_contract_test.exs, test/support/docs/phoenix_examples_test.exs, guides/phoenix-liveview.md</files>
  <read_first>
    - mix.exs `defp docs` from L71 onward
    - test/scrypath/docs_contract_test.exs `@guide_paths` and a representative `assert_contains_all` test
    - test/support/docs/phoenix_examples_test.exs (how snippets are embedded and compiled)
  </read_first>
  <action>
    1. Append `"guides/faceted-search-with-phoenix-liveview.md"` to `:extras` and add it to the existing `Phoenix:` group in `groups_for_extras`.
    2. Add the path to `@guide_paths` (or equivalent) in `DocsContractTest` and assert stable anchors such as: literal substring `faceted-search-with-phoenix-liveview`, `Scrypath.search/3`, `facet_filter`, and `Anti-pattern` (pick set ≥ 5 strings total including guide path).
    3. Extend `phoenix_examples_test.exs` with a **minimal** quoted snippet block from the guide's LiveView module + template that compiles (follow existing pattern for `.ex` / `.exs` fragment files — mirror exactly how `phoenix-liveview.md` snippets are tested).
    4. Add a "See also" bullet in `guides/phoenix-liveview.md` pointing to the new guide (one short paragraph).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs test/support/docs/phoenix_examples_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'faceted-search-with-phoenix-liveview' mix.exs` returns ≥ 1 line.
    - `mix test test/scrypath/docs_contract_test.exs test/support/docs/phoenix_examples_test.exs` exits 0.
    - `grep -n 'faceted-search-with-phoenix-liveview' guides/phoenix-liveview.md` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    ExDoc and CI contracts reference the new guide.
  </done>
</task>

<task type="auto">
  <name>Task 3: CHANGELOG Unreleased entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md top of file (Keep a Changelog format)
  </read_first>
  <action>
    1. Under `## [Unreleased]`, add a `### Added` bullet summarizing faceted search + guide (one to three bullets, no marketing fluff).
  </action>
  <verify>
    <automated>grep -n 'faceted-search-with-phoenix-liveview\\|faceting' CHANGELOG.md | head -n 5</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` contains the substring `faceted` or `facet` in the Unreleased section.
  </acceptance_criteria>
  <done>
    Release notes acknowledge the shipped surface.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide → reader | Copy-paste HEEx — must not instruct storing secrets in query params or assigns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Information Disclosure | example URLs | accept | Use `example.com` / fictional hosts only; never real API keys. |
| T-20-04-02 | Social Engineering | copy-paste ops | accept | Guide stays within library search scope; no admin credentials. |

Documentation-only plan — no new executable attack surface in production code paths beyond Plans 01–03.
</threat_model>

<verification>
- `mix test test/scrypath/docs_contract_test.exs test/support/docs/phoenix_examples_test.exs` exits 0.
- `MIX_ENV=dev mix docs` exits 0 (optional if CI skips dev; if unavailable in sandbox, substitute `mix compile --warnings-as-errors` only and note in SUMMARY).
- `mix test --exclude external_meilisearch` exits 0.
</verification>

<success_criteria>
- FACET-08 satisfied: guide + contracts + ExDoc wiring + changelog note.
</success_criteria>
