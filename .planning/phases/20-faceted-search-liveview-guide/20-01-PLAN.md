---
phase: 20-faceted-search-liveview-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/options.ex
  - lib/scrypath/schema.ex
  - lib/scrypath.ex
  - test/scrypath/options_test.exs
  - test/support/facetable_movie.ex
autonomous: true
requirements:
  - FACET-01
  - FACET-02
  - FACET-10
tags:
  - faceting
  - schema
  - compile-time
must_haves:
  truths:
    - "`use Scrypath` accepts optional `faceting:` with `attributes`, `max_values_per_facet`, and `sort_facet_values_by` per FACET-01; `__scrypath__(:faceting)` returns normalized keyword or empty default."
    - "Every atom in `faceting.attributes` is a member of `filterable:`; otherwise compile-time `ArgumentError` message contains the literal substring `not in filterable` and names the offending attribute (FACET-02)."
    - "FACET-10 non-goals are enforced: wildcard `:*` in attributes list rejected; hierarchical dotted facet names rejected; tests or compile errors prove each."
  artifacts:
    - path: "lib/scrypath/options.ex"
      provides: "faceting NimbleOptions schema + post-validation hooks"
      contains: "faceting"
    - path: "lib/scrypath/schema.ex"
      provides: "__scrypath__(:faceting)"
      contains: ":faceting"
    - path: "lib/scrypath.ex"
      provides: "schema_faceting/1"
      contains: "schema_faceting"
  key_links:
    - from: "validate_schema_options!"
      to: "__scrypath__(:filterable)"
      via: "subset check"
      pattern: "filterable"
---

<objective>
Implement declarative `faceting:` on `use Scrypath` schemas (FACET-01), compile-time alignment with `filterable:` (FACET-02), and explicit rejection of FACET-10 non-goals (wildcards, hierarchical declarations, raw string DSL in schema layer where applicable). Add `Scrypath.schema_faceting/1` for runtime reflection matching `schema_settings/1` style.

Output: Extended options validation, schema macro metadata, public reflection helper, dedicated test schema module `FacetableMovie` (or equivalent) under `test/support/`, and tests proving happy path + FACET-02/FACET-10 failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-faceted-search-liveview-guide/20-CONTEXT.md
@.planning/phases/20-faceted-search-liveview-guide/20-RESEARCH.md
@.planning/phases/20-faceted-search-liveview-guide/20-PATTERNS.md
@.planning/research/deep/FACETING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: NimbleOptions `faceting:` on `@schema_options` + persist on config struct</name>
  <files>lib/scrypath/options.ex, lib/scrypath/schema.ex, lib/scrypath.ex</files>
  <read_first>
    - lib/scrypath/options.ex (entire `@schema_options` block through `validate_schema_options!/1` and where `@scrypath_config` keys are assembled for Schema)
    - lib/scrypath/schema.ex (full file)
    - lib/scrypath.ex (moduledoc list + `schema_settings/1`)
    - test/support/searchable_post.ex (reference `use Scrypath` shape)
  </read_first>
  <action>
    1. In `lib/scrypath/options.ex`, extend `@schema_options` with a `faceting:` entry typed as a keyword list with keys:
       - `attributes:` required non-empty list of atoms **when** `faceting:` is present; when `faceting:` is omitted entirely, default to `[]` or a sentinel meaning "no faceting" per existing option style (must not break schemas that omit `faceting:`).
       - `max_values_per_facet:` optional positive integer with sensible default (e.g. 100) when faceting enabled.
       - `sort_facet_values_by:` optional map `%{}` of facet attribute → `:alpha` | `:count` (or documented atoms matching RESEARCH.md / FACETING.md — lock to NimbleOptions `:map` or keyword-shaped map).
    2. Ensure the normalized faceting keyword list is stored on the same persisted config map as `:filterable` (inspect how `Options.validate_schema_options!` returns config consumed by `Schema` quote).
    3. In `lib/scrypath/schema.ex`, add `def __scrypath__(:faceting), do: @scrypath_config.faceting` (or accessor matching actual key name on config struct).
    4. Extend `def __scrypath__(key)` error clause unchanged for unknown keys.
    5. In `lib/scrypath.ex`, add `@doc` + `@spec schema_faceting(module()) :: keyword() | map()` (match real shape) + `def schema_faceting(m), do: m.__scrypath__(:faceting)` and mention it in the module doc bullet list.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'faceting' lib/scrypath/options.ex` returns at least 3 lines.
    - `grep -n '__scrypath__(:faceting)' lib/scrypath/schema.ex` returns exactly 1 line.
    - `grep -n 'def schema_faceting' lib/scrypath.ex` returns exactly 1 line.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    `faceting:` is a validated schema option; `__scrypath__(:faceting)` works; `Scrypath.schema_faceting/1` exists with spec and doc.
  </done>
</task>

<task type="auto">
  <name>Task 2: FACET-02 subset enforcement + FACET-10 rejections at schema validation time</name>
  <files>lib/scrypath/options.ex, test/scrypath/options_test.exs, test/support/facetable_movie.ex</files>
  <read_first>
    - lib/scrypath/options.ex (after Task 1 edits — locate `validate_schema_options!`)
    - .planning/REQUIREMENTS.md (FACET-02, FACET-10 verbatim)
    - test/scrypath/options_test.exs (describe block style for compile-time tests — use `Code.compile_string/1` or isolated fixture modules per existing repo pattern; grep `compile_string` in test/)
  </read_first>
  <action>
    1. After NimbleOptions validation in `validate_schema_options!/1`, compute `faceting_attrs = MapSet.new(Keyword.get(faceting, :attributes, []))` and `filterable_set = MapSet.new(filterable)`. If `faceting_attrs` is non-empty and **not** a subset of `filterable_set`, `raise ArgumentError` with message including the first offending attribute **name as string** and the literal substring `not in filterable`.
    2. If `:attributes` contains `:*` or any element equal to `:*`, `raise ArgumentError` with substring `wildcard`.
    3. If any attribute atom contains a `.` character in its name (inspect `Atom.to_string(attr)` for `"."`) representing hierarchical facets, `raise ArgumentError` with substring `hierarchical` or `nested` (pick one and use consistently in tests).
    4. Add `test/support/facetable_movie.ex` declaring `filterable: [:genre, :year, :rating, :director]`, `faceting: [attributes: [:genre, :year, :rating, :director], max_values_per_facet: 100]` (adjust to match validated shape), minimal `embedded_schema` fields for compile.
    5. Add tests in `test/scrypath/options_test.exs` (or new file if cleaner) proving: (a) `FacetableMovie` module compiles; (b) a `Code.compile_string/2` fragment with `faceting: [attributes: [:orphan]]` and `filterable: [:genre]` raises `ArgumentError` with `not in filterable` and `orphan`; (c) wildcard attributes list raises with `wildcard`; (d) hierarchical atom raises per rule 3.
  </action>
  <verify>
    <automated>mix test test/scrypath/options_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File `test/support/facetable_movie.ex` exists and defines a module using `Scrypath` with non-empty `faceting:`.
    - `mix test test/scrypath/options_test.exs` exits 0.
    - `grep -n 'not in filterable' lib/scrypath/options.ex` returns ≥ 1 match.
    - `grep -n 'wildcard' test/scrypath/options_test.exs` returns ≥ 1 match.
  </acceptance_criteria>
  <done>
    FACET-02 and FACET-10 rules are enforced at schema declaration time with tests; `FacetableMovie` is the canonical test schema for later plans.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → `use Scrypath` opts | Compile-time only; malicious strings only if developer pastes them — same trust as existing schema options. |
| validator → stderr | Must not print user-controlled facet names from runtime (compile-time errors use attribute literals from source). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Denial of Service | `String.to_atom/1` on facet names | mitigate | Use only literals from compile-time quoted options; never call `String.to_atom/1` on runtime input in this plan. |
| T-20-01-02 | Information Disclosure | `ArgumentError` messages | accept | Messages name schema-declared atoms only (developer's own code). |

No high-severity threats; schema compile surface only.
</threat_model>

<verification>
- `mix test test/scrypath/options_test.exs` exits 0.
- `mix test --exclude external_meilisearch` exits 0.
- `mix compile --warnings-as-errors` exits 0.
</verification>

<success_criteria>
- FACET-01 satisfied: `__scrypath__(:faceting)` + `Scrypath.schema_faceting/1` reflect declared options.
- FACET-02 satisfied: offending attribute surfaced when not filterable.
- FACET-10 satisfied: wildcard and hierarchical declarations rejected with tests.
</success_criteria>
