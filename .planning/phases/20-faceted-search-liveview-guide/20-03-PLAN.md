---
phase: 20-faceted-search-liveview-guide
plan: 03
type: execute
wave: 3
depends_on:
  - "20-01"
  - "20-02"
files_modified:
  - lib/scrypath/meilisearch/settings.ex
  - test/scrypath/meilisearch/settings_test.exs
  - test/scrypath/reindex_test.exs
autonomous: true
requirements:
  - FACET-07
tags:
  - meilisearch-settings
  - reindex
must_haves:
  truths:
    - "For schemas with non-empty `faceting.attributes`, `Scrypath.Meilisearch.Settings.resolve/2` → `translate_settings/1` produces Meilisearch `filterableAttributes` entries in **object** form including `features: [\"facetSearch\"]` for each facet attribute (per Meilisearch faceting docs), with **zero** new user-facing config keys beyond the schema declaration."
    - "`verify_applied/3` continues to compare declared wire subset to applied settings using the augmented declared map (no false drift from missing facet features when schema declares faceting)."
  artifacts:
    - path: "lib/scrypath/meilisearch/settings.ex"
      provides: "facet-driven filterableAttributes merge in resolve/translate path"
      contains: "facetSearch|schema_faceting|__scrypath__(:faceting)"
  key_links:
    - from: "Settings.resolve/2"
      to: "Scrypath.schema_faceting/1"
      via: "facet attribute list"
      pattern: "faceting"
---

<objective>
Implement FACET-07: when a schema declares `faceting:`, managed settings resolution merges Meilisearch `filterableAttributes` object entries so facet attributes are filterable with `facetSearch` enabled, reusing the Phase 19 translation pipeline (no second translator module).

Output: Extended `Settings.resolve/2` (or a single private helper called from it), tests asserting wire shape from `translate_settings(resolve(...))`, and at least one reindex/settings integration assertion if an existing test harness already applies settings for a schema fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/20-faceted-search-liveview-guide/20-CONTEXT.md
@.planning/phases/19-relevance-tuning/19-CONTEXT.md
@lib/scrypath/meilisearch/settings.ex
@test/scrypath/meilisearch/settings_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge facet-derived `filterableAttributes` into resolved settings map</name>
  <files>lib/scrypath/meilisearch/settings.ex, test/scrypath/meilisearch/settings_test.exs</files>
  <read_first>
    - lib/scrypath/meilisearch/settings.ex (`resolve/2`, `translate_settings/1`, `verify_applied/3`)
    - .planning/research/deep/FACETING.md (filterableAttributes object form section if present)
    - test/scrypath/meilisearch/settings_test.exs (existing `translate_settings` / `resolve` tests)
  </read_first>
  <action>
    1. In `resolve/2`, after merging declared schema settings from `Scrypath.schema_settings(schema_module)` and overrides, read `Scrypath.schema_faceting(schema_module)` (or `__scrypath__(:faceting)` if helper not landed yet — align with Plan 01).
    2. For each attribute in `:attributes` list, ensure the canonical **snake** field name participates in `filterable_attributes` / `:filterable_attributes` value that `translate_settings/1` turns into Meilisearch `filterableAttributes` **array-of-objects** shape required by FACET-07. If schema already declares string or object entries for the same attribute, apply deterministic merge rules documented in a **two-line** moduledoc note (prefer explicit schema `settings:` entry for conflicts vs facet-derived — pick one and test it).
    3. Do **not** add new public Mix tasks in this plan.
    4. Add tests that call `resolve(FacetableMovie, [])` (or stub module) and `translate_settings/1` on the result, asserting the JSON-ready map contains a `filterableAttributes` list where the entry for at least one facet attribute is a map with `"features" => ["facetSearch"]` (exact string casing per Meilisearch wire).
  </action>
  <verify>
    <automated>mix test test/scrypath/meilisearch/settings_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'facetSearch' lib/scrypath/meilisearch/settings.ex` returns ≥ 1 line OR the literal appears in a test fixture string adjacent to Settings code path (prefer source).
    - `mix test test/scrypath/meilisearch/settings_test.exs` exits 0.
    - `grep -n 'schema_faceting\\|__scrypath__(:faceting)' lib/scrypath/meilisearch/settings.ex` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    FACET-07 wire augmentation is implemented with automated proof in settings tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regression — `verify_applied/3` declared subset includes facet-derived keys</name>
  <files>test/scrypath/meilisearch/settings_test.exs, test/scrypath/reindex_test.exs</files>
  <read_first>
    - test/scrypath/meilisearch/settings_test.exs (tests touching `verify_applied`)
    - test/scrypath/reindex_test.exs (if settings verify is exercised)
  </read_first>
  <action>
    1. Add or extend a test where `verify_applied` is called with a stub `Client` that returns applied settings including the facet object entries; expect `:ok` when declared includes the same structure from `resolve` + `translate_settings`.
    2. If no stub exists, add minimal `Req.Test` or existing client double pattern already in file.
  </action>
  <verify>
    <automated>mix test test/scrypath/meilisearch/settings_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - At least one test name contains `facet` or `FACET`.
    - `mix test test/scrypath/meilisearch/settings_test.exs` exits 0.
  </acceptance_criteria>
  <done>
    Verify path understands facet-augmented declared settings.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema → settings map | Only compile-declared attributes; no runtime user strings. |
| settings → Meilisearch | Same transport trust as existing `apply_settings/3`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Tampering | merge with overrides | mitigate | Document merge precedence; tests cover override vs facet-derived. |

Low severity — extends existing settings pipeline only.
</threat_model>

<verification>
- `mix test test/scrypath/meilisearch/settings_test.exs` exits 0.
- `mix test --exclude external_meilisearch` exits 0.
</verification>

<success_criteria>
- FACET-07 satisfied with tests and no new public configuration surface.
</success_criteria>
