---
phase: "03-search-query-api-and-hydration"
plan: "03-04"
type: "execute"
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - README.md
  - ARCHITECTURE.md
  - lib/scrypath/meilisearch.ex
  - test/scrypath/search_test.exs
  - test/scrypath/meilisearch_test.exs
autonomous: true
requirements:
  - SRCH-01
  - SRCH-05
  - SRCH-06
must_haves:
  truths:
    - "Docs explain the common search happy path, explicit hydration inputs, and the raw-hit result contract."
    - "The explicit `Scrypath.Meilisearch.*` namespace remains the escape hatch for backend-native query power."
    - "Search docs and tests make the common-path versus native-path distinction unmistakable."
  artifacts:
    - "README.md"
    - "ARCHITECTURE.md"
    - "lib/scrypath/meilisearch.ex"
    - "test/scrypath/search_test.exs"
    - "test/scrypath/meilisearch_test.exs"
  key_links:
    - "README.md -> common search examples with structured filters, sorts, pagination, and explicit repo hydration"
    - "ARCHITECTURE.md -> common-path search, hydration, and native escape hatch boundaries"
    - "lib/scrypath/meilisearch.ex -> module docs frame the native search namespace honestly"
---

<objective>
Finish the Phase 3 contract by documenting the common search path, the stable result envelope, and the explicit Meilisearch escape hatch, while rounding out the highest-risk behavior tests.

Purpose: make the Phase 3 API understandable and adoption-ready without reopening the design surface.
Output: updated docs, reinforced native-namespace framing, and tests that pin the common-vs-native path boundary.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-search-query-api-and-hydration/03-CONTEXT.md
@.planning/phases/03-search-query-api-and-hydration/03-RESEARCH.md
@README.md
@ARCHITECTURE.md
@lib/scrypath/meilisearch.ex
@prompts/elixir-search-lib-deep-research.md
@prompts/search-lib-use-cases-deep-research.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the common search contract and hydration behavior</name>
  <files>README.md, ARCHITECTURE.md</files>
  <read_first>
    - README.md
    - ARCHITECTURE.md
    - .planning/phases/03-search-query-api-and-hydration/03-CONTEXT.md
    - .planning/phases/03-search-query-api-and-hydration/03-RESEARCH.md
  </read_first>
  <action>Update the README and architecture guide with the common Phase 3 happy path: `Scrypath.search/3`, structured filters/sorts/pagination, explicit `repo:` and optional `preload:`, stable result envelope behavior, and missing-id visibility. Keep examples concrete and Ecto-native. Do not blur the common path with backend-native options.</action>
  <acceptance_criteria>
    - `README.md` contains a common search example using `Scrypath.search`
    - `README.md` mentions raw hits and hydrated records together
    - `ARCHITECTURE.md` describes common search execution and explicit hydration
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scrypath/search_test.exs</automated>
  </verify>
  <done>Public docs explain how Scrypath search works in normal Ecto and Phoenix code and what guarantees it does and does not make.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reinforce the explicit Meilisearch native-path boundary</name>
  <files>lib/scrypath/meilisearch.ex, test/scrypath/meilisearch_test.exs, test/scrypath/search_test.exs</files>
  <read_first>
    - lib/scrypath/meilisearch.ex
    - test/scrypath/meilisearch_test.exs
    - .planning/phases/03-search-query-api-and-hydration/03-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: native Meilisearch search remains available without flowing through common-path passthrough options.
    - Test 2: common-path tests prove backend-native strings or payloads are rejected from `Scrypath.search/3`.
  </behavior>
  <action>Finalize the `Scrypath.Meilisearch.*` module docs and targeted tests so the boundary is explicit: use the common path for stable happy-path search, and use the native namespace when Meilisearch-only power is needed. Ensure the common path does not quietly accept backend-native payloads or raw filter strings.</action>
  <acceptance_criteria>
    - `lib/scrypath/meilisearch.ex` module docs mention the native search escape hatch
    - `test/scrypath/search_test.exs` rejects backend-native common-path inputs
    - `test/scrypath/meilisearch_test.exs` covers the explicit native search path
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scrypath/search_test.exs test/scrypath/meilisearch_test.exs</automated>
  </verify>
  <done>The common and native search paths are both documented and enforced clearly enough that execution should not drift into a fake abstraction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| common-path docs -> user expectations | Public docs establish what behavior callers should rely on. |
| native namespace -> backend-specific capabilities | The Meilisearch namespace exposes richer power without redefining the common contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Repudiation | public docs | medium | mitigate | Document the common search contract and the native escape hatch clearly so callers understand the supported boundaries. |
| T-03-11 | Elevation of Privilege | common-path inputs | medium | mitigate | Add tests that reject backend-native payloads or raw filter strings on the common path. |
</threat_model>

<verification>
Run `mix test test/scrypath/search_test.exs test/scrypath/meilisearch_test.exs`.
</verification>

<success_criteria>
Phase 3 is documented and tested as one explicit common search contract plus one explicit Meilisearch-native escape hatch, with no ambiguity between the two.
</success_criteria>

<output>
After completion, create `.planning/phases/03-search-query-api-and-hydration/03-04-SUMMARY.md`
</output>
