---
phase: 35
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/sync-modes-and-visibility.md
  - README.md
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements: [ADPT-02, ADPT-03]
tags: [docs, sync-modes, readme, contract-test, adoption, milestone-audit]

must_haves:
  truths:
    - "**`guides/sync-modes-and-visibility.md`** contains heading **`## Operator lifecycle`** and the monospace lifecycle line **`requested -> enqueued -> processing -> backend_accepted -> completed | retrying | discarded`** (same characters as README) so README’s authority sentence is true for lifecycle vocabulary (**`INT-SYNC-GUIDE-AUTHORITY`**)"
    - "**`README.md`** states that on conflict the **sync guide wins**, and ties the README lifecycle line to the guide’s **Operator lifecycle** section (per **035-CONTEXT.md** D-01, D-02)"
  artifacts:
    - path: "guides/sync-modes-and-visibility.md"
      provides: "Canonical operator lifecycle subsection"
      contains: "## Operator lifecycle"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Parity lock for lifecycle string in guide"
      contains: "requested -> enqueued -> processing -> backend_accepted -> completed | retrying | discarded"
---

<objective>
Close **`v1.6-MILESTONE-AUDIT.md`** integration gap **`INT-SYNC-GUIDE-AUTHORITY`**: align **`README.md`** Sync Modes authority claims with **`guides/sync-modes-and-visibility.md`** by making the **operator lifecycle chain** first-class in the guide, tightening README **precision** (guide wins on disagreement), and extending **`docs_contract_test.exs`** with minimal stable locks — **ADPT-02**, **ADPT-03**.
</objective>

<threat_model>
**ASVS V14 / documentation integrity (L1)**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| **T-35-DOC-01:** Guide edit **weakens** or **removes** existing **The Contract** table or per-mode **`## :inline` / `:oban` / `:manual`** sections | Medium | **Insert only** the new subsection **between** **The Contract** and **`## :inline`**; do not delete existing tables or headings |
| **T-35-DOC-02:** README precision edit **removes** **`guides/sync-modes-and-visibility.md`** CTA, **Choosing a mode**, or the **Sync Modes** table | Medium | **Append** at most **two** sentences **immediately after** the existing authority paragraph (the two sentences ending with *compact decision surface.*); do not move or delete the lifecycle line block |
| **T-35-DOC-03:** Contract test uses **regex** that breaks on `\|` in the lifecycle string | Low | Use **`String.contains?/2`** or **`assert_contains_all`** with **literal** substrings only (no regex over the pipe) |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: sync guide — Operator lifecycle subsection</name>
  <files>guides/sync-modes-and-visibility.md</files>
  <read_first>
    - guides/sync-modes-and-visibility.md (full file)
    - README.md (`## Sync Modes` authority paragraph + lifecycle line ~L106–L128)
    - .planning/phases/035-sync-guide-lifecycle-parity/035-CONTEXT.md (D-02, D-03, D-08, D-09)
  </read_first>
  <action>
    1. **Immediately after** the **The Contract** markdown table and the paragraph **Accepted work is not the same thing as search visibility.** (and before the first existing **`## :inline`** heading), insert a new section:
       - **Heading (exact):** `## Operator lifecycle`
       - **First monospace line (exact backtick line):** `` `requested -> enqueued -> processing -> backend_accepted -> completed | retrying | discarded` ``
       - **Body:** Short gloss (prose **or** a small table — executor choice) so each state name in the chain is **defined once** in plain language: what **is** / **is not** implied for **search visibility** and **caller-return** semantics at a high level.
       - **Closing paragraph:** Explain how **`:inline`**, **`:oban`**, and **`:manual`** relate to **where** an app usually observes progress along this **shared** chain, **without** copying the full per-mode sections below.
    2. Do **not** duplicate the **The Contract** table or rewrite **`## :inline` / `:oban` / `:manual`** bodies except if a **single** cross-reference sentence is needed.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '## Operator lifecycle' guides/sync-modes-and-visibility.md` exits **0**
    - `grep -F 'requested -> enqueued -> processing -> backend_accepted -> completed | retrying | discarded' guides/sync-modes-and-visibility.md` exits **0**
    - `grep -n '## The Contract' guides/sync-modes-and-visibility.md` prints a line number **less than** the line number printed by `grep -n '## Operator lifecycle' guides/sync-modes-and-visibility.md`
    - `grep -n '## Operator lifecycle' guides/sync-modes-and-visibility.md` prints a line number **less than** the line number printed by `grep -n '## :inline' guides/sync-modes-and-visibility.md`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: README — precision lines after authority paragraph</name>
  <files>README.md</files>
  <read_first>
    - README.md (`## Sync Modes` ~L106–L130)
    - guides/sync-modes-and-visibility.md (post Task 1; confirm **`## Operator lifecycle`** exists)
    - .planning/phases/035-sync-guide-lifecycle-parity/035-CONTEXT.md (D-01, D-06)
  </read_first>
  <action>
    1. Locate the Sync Modes paragraph ending with **keep README as the compact decision surface.**
    2. **Immediately after that paragraph**, insert **exactly these two sentences** (markdown links optional but if used keep **relative** path `guides/sync-modes-and-visibility.md`):
       - `If this README and the sync guide disagree, treat **guides/sync-modes-and-visibility.md** as the source of truth for semantics.`
       - `The monospace lifecycle line below matches the **Operator lifecycle** section in that guide.`
       (Adjust only bold vs **`**path**`** formatting to match surrounding README style — **do not** change the meaning.)
    3. Leave the existing **All three modes share one operator-facing lifecycle:** paragraph and the following **backtick lifecycle line** unchanged **unless** a typo fix is required for **byte-identical** match to the guide line from Task 1.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'If this README and the sync guide disagree' README.md` exits **0**
    - `grep -F 'Operator lifecycle' README.md` exits **0**
    - `grep -F 'requested -> enqueued -> processing -> backend_accepted -> completed | retrying | discarded' README.md` exits **0**
    - `grep -F '## Sync Modes' README.md` exits **0** and `grep -F 'Choosing a mode:' README.md` exits **0**
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: docs_contract_test — lifecycle parity locks</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (`assert_contains_all` for sync guide ~L228–235; helpers at top of file)
    - guides/sync-modes-and-visibility.md (post Task 1)
    - README.md (post Task 2)
    - .planning/phases/035-sync-guide-lifecycle-parity/035-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    1. Extend the existing list passed to **`assert_contains_all(@guides["guides/sync-modes-and-visibility.md"], [...])`** (inside **`test "phase 6 guide shell exists..."`**) with:
       - **`## Operator lifecycle`**
       - **`requested -> enqueued -> processing -> backend_accepted -> completed | retrying | discarded`**
    2. Add a **new** test named **`phase 35 readme and sync guide share operator lifecycle chain`** that asserts **`String.contains?(@readme, "requested -> enqueued -> processing -> backend_accepted -> completed | retrying | discarded")`** and the same **`String.contains?`** for **`@guides["guides/sync-modes-and-visibility.md"]`**.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/docs_contract_test.exs` exits **0**
    - `grep -F 'phase 35 readme and sync guide share operator lifecycle chain' test/scrypath/docs_contract_test.exs` exits **0**
    - `grep -F '## Operator lifecycle' test/scrypath/docs_contract_test.exs` exits **0**
  </acceptance_criteria>
</task>

</tasks>

<verification>
From repository root: **`mix test test/scrypath/docs_contract_test.exs`**, then **`mix test`** if any non-doc files were touched (none expected).
</verification>

<success_criteria>
- Guide carries **`## Operator lifecycle`** and the **same** lifecycle monospace line as README
- README states **guide wins** on disagreement and links lifecycle to **Operator lifecycle**
- Doc contract tests lock the new vocabulary; **`mix test test/scrypath/docs_contract_test.exs`** passes
</success_criteria>

## PLANNING COMPLETE
