---
phase: 90-async-execution
plan: 02
type: execute
wave: 2
depends_on: ["90-01"]
files_modified:
  - test/scrypath/sync/related_worker_test.exs
autonomous: true
requirements:
  - DATA-03
  - EXEC-01
must_haves:
  truths:
    - "Test suite validates Oban retry behavior for transient errors."
    - "Test suite validates Oban cancellation behavior for unrecoverable errors."
  artifacts:
    - path: "test/scrypath/sync/related_worker_test.exs"
      provides: "Error handling test coverage"
      min_lines: 100
  key_links:
    - from: "test/scrypath/sync/related_worker_test.exs"
      to: "lib/scrypath/sync/related_worker.ex"
      via: "Asserts return values of perform/1"
      pattern: "assert \\{:cancel, _\\} = RelatedWorker\\.perform"
---

<objective>
Provide comprehensive test coverage for `RelatedWorker` error propagation behavior.
Purpose: Prove that midway failures during fan-out yield actionable errors that Oban uses to either retry or cancel the job.
Output: Extended test suite in `related_worker_test.exs` with tests for failure scenarios.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@test/scrypath/sync/related_worker_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test graceful cancellation on invalid arguments</name>
  <files>test/scrypath/sync/related_worker_test.exs</files>
  <behavior>
    - Test: `perform/1` returns `{:cancel, {:invalid_job, :invalid_schema}}` when the schema argument points to a non-existent module.
    - Test: `perform/1` returns `{:cancel, {:invalid_job, :invalid_fan_out}}` when the fan_out argument does not exist in the schema.
  </behavior>
  <action>
    Add test cases in `test/scrypath/sync/related_worker_test.exs` asserting that `RelatedWorker.perform/1` returns `{:cancel, _}` for invalid args instead of raising exceptions.
  </action>
  <verify>
    <automated>mix test test/scrypath/sync/related_worker_test.exs</automated>
  </verify>
  <done>Invalid argument tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test Oban cancellation and retry flows</name>
  <files>test/scrypath/sync/related_worker_test.exs</files>
  <behavior>
    - Test: `perform/1` returns `{:cancel, "HTTP 400: ... "}` when `sync_records` encounters a 400 Bad Request error.
    - Test: `perform/1` returns `{:error, reason}` when `sync_records` encounters a 500 Internal Server Error or other transient reason.
  </behavior>
  <action>
    Extend the mock `RecordingBackend` in `related_worker_test.exs` (or create a new specialized one, e.g. `ErrorBackend`) to simulate failures.
    Add a test where the backend's `upsert_documents` returns `{:error, {:http_error, 400, "Bad Request"}}`, and assert `perform/1` returns `{:cancel, _}`.
    Add another test where the backend returns `{:error, {:http_error, 500, "Internal Server Error"}}` (or just `{:error, :timeout}`), and assert `perform/1` returns `{:error, _}` to trigger retries.
  </action>
  <verify>
    <automated>mix test test/scrypath/sync/related_worker_test.exs</automated>
  </verify>
  <done>HTTP error handling tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing -> Code | Ensure test verification accurately models production constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-02 | Denial of Service | Tests | accept | Tests verify that the mitigation built in 90-01 correctly stops infinite retry loops |
</threat_model>

<verification>
Run `mix test test/scrypath/sync/related_worker_test.exs` to ensure complete coverage.
</verification>

<success_criteria>
The test suite explicitly proves `RelatedWorker` will cancel on 40x and bubble up 50x errors.
</success_criteria>

<output>
After completion, create `.planning/phases/90-async-execution/90-02-SUMMARY.md`
</output>
