---
phase: 90-async-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/sync/related_worker.ex
autonomous: true
requirements:
  - DATA-03
  - EXEC-01
must_haves:
  truths:
    - "Unrecoverable client errors (HTTP 4xx) halt the Oban job immediately."
    - "Transient network or server errors (HTTP 5xx) bubble up to trigger Oban retries."
    - "Invalid job arguments (missing schema or fan_out) cancel the job instead of crashing."
  artifacts:
    - path: "lib/scrypath/sync/related_worker.ex"
      provides: "Oban worker perform/1 implementation"
      min_lines: 50
  key_links:
    - from: "lib/scrypath/sync/related_worker.ex"
      to: "Oban"
      via: "Return values of perform/1"
      pattern: "{:cancel, .*}\\s*|\\s*{:error, .*}"
---

<objective>
Update the internal `Scrypath.Sync.RelatedWorker` to explicitly handle error propagation. 
Purpose: Ensure midway failures yield actionable errors for Oban to retry (transient) or cancel (unrecoverable) rather than failing silently with partial drops.
Output: A resilient Oban worker that correctly bubbles up or cancels jobs based on `sync_records` results.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/scrypath/sync/related_worker.ex
@lib/scrypath/oban/upsert_worker.ex
</context>

<interfaces>
<!-- Extracting relevant analog logic for graceful argument resolution from UpsertWorker -->
From `lib/scrypath/oban/upsert_worker.ex`:
```elixir
    defp resolve_schema(%{"schema" => name}) when is_binary(name) do
      try do
        {:ok, String.to_existing_atom(name)}
      rescue
        ArgumentError -> {:error, :invalid_schema}
      end
    end
    defp resolve_schema(_args), do: {:error, :invalid_schema}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add graceful argument resolution</name>
  <files>lib/scrypath/sync/related_worker.ex</files>
  <action>
    Introduce private helpers `resolve_schema/1` and `resolve_fan_out/2` in `RelatedWorker` to safely parse string arguments into existing atoms. 
    Use a `with` statement in `perform/1` to call these helpers.
    If schema resolution fails, or if `fan_out_key` doesn't exist on the schema's `__scrypath__(:fan_outs)`, the `else` block should return `{:cancel, {:invalid_job, reason}}` (e.g. `{:cancel, {:invalid_job, :invalid_schema}}` or `{:cancel, {:invalid_job, :invalid_fan_out}}`).
    This replaces the unsafe `String.to_existing_atom/1` calls that previously caused crash loops on invalid arguments.
  </action>
  <verify>
    <automated>mix test test/scrypath/sync/related_worker_test.exs</automated>
  </verify>
  <done>Invalid arguments safely cancel the job instead of crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Implement robust error bubbling</name>
  <files>lib/scrypath/sync/related_worker.ex</files>
  <action>
    Modify `perform/1` to pattern match on the return value of `Scrypath.Telemetry.span`.
    Remove the unconditional `:ok` at the end of the function.
    Instead, evaluate the `result` tuple:
    - If `{:ok, _}` or `:ok`, return `:ok`.
    - If `{:error, {:http_error, status, body}}` when `status in 400..499`, return `{:cancel, "HTTP #{status}: #{inspect(body)}"}` (this prevents retry storms on unrecoverable client errors per D-XX decisions).
    - If `{:error, reason}`, return `{:error, reason}` (this bubbles up transient errors to Oban for exponential backoff).
  </action>
  <verify>
    <automated>mix test test/scrypath/sync/related_worker_test.exs</automated>
  </verify>
  <done>Worker correctly translates `sync_records` outcomes into Oban job states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oban Worker -> Meilisearch | Async job sending data to external search engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | Denial of Service | RelatedWorker | mitigate | Cancel jobs explicitly on 40x HTTP errors to prevent retry storms against the target search cluster |
</threat_model>

<verification>
Check that `lib/scrypath/sync/related_worker.ex` correctly cancels on `4xx` and retries on `5xx`.
</verification>

<success_criteria>
`RelatedWorker.perform/1` no longer swallows errors and properly instructs Oban on retry vs cancel behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/90-async-execution/90-01-SUMMARY.md`
</output>
