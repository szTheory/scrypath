---
phase: 107
plan: 1
type: execute
wave: 1
depends_on:
  - 106-01
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
  - examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs
  - lib/mix/tasks/verify.phase107.ex
  - test/mix/tasks/verify.phase107_test.exs
  - mix.exs
autonomous: true
requirements:
  - E2E-01
must_haves:
  truths:
    - "D-01/E2E-01: `/dev/e2e/search-visible` calls `Scrypath.search/3` with both `tenant_id` and `category_id` present in `%Scrypath.Query.filter` when category readiness filtering is requested."
    - "D-02/D-03/D-06/D-07: Phase 107 does not add broader Playwright cross-tenant fixtures, promote `phase105-e2e`, or change required/advisory CI topology."
    - "D-04/D-05/D-13: `mix verify.phase107` provides a fast, deterministic, service-free contributor gate with readable local ExUnit failures."
    - "D-08/D-09: The readiness probe preserves explicit `filter` composition and does not switch this proof to `tenant_scope:`."
    - "D-10/D-11: The repair stays in the existing controller/test/gate surfaces without extracting a shared helper or adding public Scrypath API breadth."
  artifacts:
    - path: "examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex"
      provides: "Readiness probe tenant/category filter composition"
      contains: "Keyword.get(:filter, []) |> Keyword.put(:category_id, category_id)"
    - path: "examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs"
      provides: "Controller regression proof for tenant scope plus category filtering"
      contains: "category readiness filter preserves tenant scope"
    - path: "lib/mix/tasks/verify.phase107.ex"
      provides: "Focused Phase 107 verification gate"
      contains: "defmodule Mix.Tasks.Verify.Phase107"
  key_links:
    - from: "GET /dev/e2e/search-visible"
      to: "Scrypath.search(Product, query, search_opts)"
      via: "explicit `filter: [tenant_id: tenant_id]` plus merged `category_id`"
      pattern: "backend-stubbed query assertion proves tenant filter is not overwritten"
---

<objective>
Prove and guard that the e-commerce example app's `/dev/e2e/search-visible` readiness probe preserves tenant scope when category readiness filtering is present, using a narrow controller-level regression and a focused `mix verify.phase107` gate without broadening browser E2E, CI topology, or public Scrypath API surface.
</objective>

<context>
Phase 107 is a bounded E2E-01 regression guard. The historical risk is category readiness filtering replacing the existing tenant filter before the probe calls `Scrypath.search/3`. The executor should keep the proof at the Phoenix controller/backend-stub boundary, preserve explicit filter composition, and avoid deferred ecommerce proof breadth.
</context>

<tasks>
### Task 107-01-01 - Lock tenant/category filter composition in the readiness probe
<read_first>
- `examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex`
- `examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/live/search_live.ex`
- `examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex`
- `.planning/phases/107-ecommerce-readiness-regression-guard/107-CONTEXT.md`
- `.planning/phases/107-ecommerce-readiness-regression-guard/107-RESEARCH.md`
- `.planning/phases/107-ecommerce-readiness-regression-guard/107-PATTERNS.md`
</read_first>
<action>
- In `examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex`, keep `search_visible/2` building base options as `[filter: [tenant_id: tenant_id]]`.
- Ensure `maybe_put_category_filter/2` parses `"category_id"` with the existing `parse_integer/1` helper and merges into the existing `:filter` keyword list by reading `Keyword.get(opts, :filter, [])` and applying `Keyword.put(:category_id, category_id)`.
- Preserve the no-category branch as `{:ok, opts}` so category filtering remains optional.
- Preserve deterministic invalid integer handling through `invalid_integer/1`.
- Do not replace `filter` with a category-only list, do not switch this probe to `tenant_scope:`, and do not extract a shared storefront/probe helper in this phase.
</action>
<acceptance_criteria>
- `rg "filter: \\[tenant_id: tenant_id\\]" examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex` returns at least 1 match.
- `rg "Keyword\\.get\\(:filter, \\[\\]\\)|Keyword\\.put\\(:category_id, category_id\\)" examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex` returns at least 2 matches.
- `rg "tenant_scope:" examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex` exits non-zero.
- `mix test examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs` exits 0.
</acceptance_criteria>
<verify>
- `rg "filter: \\[tenant_id: tenant_id\\]" examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex`
- `rg "Keyword\\.get\\(:filter, \\[\\]\\)|Keyword\\.put\\(:category_id, category_id\\)" examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex`
- `! rg "tenant_scope:" examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex`
- `mix test examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`
</verify>

### Task 107-01-02 - Prove the readiness query keeps tenant scope with category filtering
<read_first>
- `examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`
- `examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex`
- `lib/scrypath/query.ex`
- `lib/scrypath/backend.ex`
- `.planning/phases/107-ecommerce-readiness-regression-guard/107-VALIDATION.md`
</read_first>
<action>
- In `examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`, keep or add a `GET /dev/e2e/search-visible` test named `category readiness filter preserves tenant scope`.
- Use a local `SearchVisibleBackend` implementing `Scrypath.Backend` so the test remains service-free.
- In the stubbed `search/3`, send `{:search_visible_query, schema_module, query}` to the registered test process when `Process.whereis(:e2e_controller_test)` exists.
- In the test, temporarily set `Application.put_env(:scrypath, :defaults, Keyword.put(defaults, :backend, SearchVisibleBackend))` and restore the original defaults in `on_exit/1`.
- Request `/dev/e2e/search-visible` with `tenant_id: "101"`, `category_id: "202"`, and `query: "quantum"`.
- Assert the response is `%{"hits" => ["Quantum CyberPhone X"]}` and assert the captured `%Scrypath.Query{text: "quantum", filter: filter}` sorts to exactly `[category_id: 202, tenant_id: 101]`.
- Keep this as a controller/query-boundary regression; do not add Playwright fixtures, cross-tenant seed expansion, fixed sleeps, or live Meilisearch dependencies.
</action>
<acceptance_criteria>
- `rg "category readiness filter preserves tenant scope|SearchVisibleBackend|:search_visible_query" examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs` returns at least 3 matches.
- `rg "Enum\\.sort\\(filter\\) == \\[category_id: 202, tenant_id: 101\\]" examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs` returns 1 match.
- `mix test examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs` exits 0.
- `git diff -- examples/scrypath_ecommerce/e2e/storefront.spec.ts examples/scrypath_ecommerce/e2e/helpers/e2e.ts .github/workflows/ci.yml` shows no Phase 107 changes unless execution discovered a direct bug in an existing helper callsite.
</acceptance_criteria>
<verify>
- `rg "category readiness filter preserves tenant scope|SearchVisibleBackend|:search_visible_query" examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`
- `rg "Enum\\.sort\\(filter\\) == \\[category_id: 202, tenant_id: 101\\]" examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`
- `mix test examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`
- `git diff -- examples/scrypath_ecommerce/e2e/storefront.spec.ts examples/scrypath_ecommerce/e2e/helpers/e2e.ts .github/workflows/ci.yml`
</verify>

### Task 107-01-03 - Add focused Phase 107 verification gate
<read_first>
- `lib/mix/tasks/verify.phase106.ex`
- `lib/mix/tasks/verify.phase94.ex`
- `test/mix/tasks/verify.phase106_test.exs`
- `mix.exs`
- `.planning/phases/107-ecommerce-readiness-regression-guard/107-VALIDATION.md`
</read_first>
<action>
- Create `lib/mix/tasks/verify.phase107.ex` with module `Mix.Tasks.Verify.Phase107`.
- Follow the existing focused phase task style: `use Mix.Task`, `@shortdoc`, `ensure_no_args!/1`, `Mix.Task.run("app.start")`, `Mix.Task.reenable("test")`, and `Mix.Task.run("test", focused_tests)`.
- Set the focused test list to exactly:
  - `examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`
  - `test/mix/tasks/verify.phase107_test.exs`
- Include readable shell output containing `verify.phase107` and `Phase 107 ecommerce readiness regression guard`.
- Create `test/mix/tasks/verify.phase107_test.exs` with argument rejection coverage for stray args, help output coverage for `verify.phase107`, and source assertions for both focused test paths.
- Register `"verify.phase107": :test` in `mix.exs` under `preferred_envs`, near the adjacent phase verify entries.
- Do not add `verify.phase107` to GitHub Actions or promote any advisory browser lane.
</action>
<acceptance_criteria>
- `test -f lib/mix/tasks/verify.phase107.ex` exits 0.
- `test -f test/mix/tasks/verify.phase107_test.exs` exits 0.
- `rg "defmodule Mix.Tasks.Verify.Phase107|verify.phase107 does not accept arguments|Phase 107 ecommerce readiness regression guard" lib/mix/tasks/verify.phase107.ex` returns at least 3 matches.
- `rg "examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs|test/mix/tasks/verify.phase107_test.exs" lib/mix/tasks/verify.phase107.ex test/mix/tasks/verify.phase107_test.exs` returns at least 4 matches.
- `rg "\"verify.phase107\": :test" mix.exs` returns 1 match.
- `mix test test/mix/tasks/verify.phase107_test.exs` exits 0.
- `mix verify.phase107` exits 0.
</acceptance_criteria>
<verify>
- `test -f lib/mix/tasks/verify.phase107.ex`
- `test -f test/mix/tasks/verify.phase107_test.exs`
- `rg "defmodule Mix.Tasks.Verify.Phase107|verify.phase107 does not accept arguments|Phase 107 ecommerce readiness regression guard" lib/mix/tasks/verify.phase107.ex`
- `rg "examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs|test/mix/tasks/verify.phase107_test.exs" lib/mix/tasks/verify.phase107.ex test/mix/tasks/verify.phase107_test.exs`
- `rg "\"verify.phase107\": :test" mix.exs`
- `mix test test/mix/tasks/verify.phase107_test.exs`
- `mix verify.phase107`
</verify>
</tasks>

<threat_model>
- **T-107-001 (Information Disclosure, High):** Category readiness filtering overwrites `filter: [tenant_id: tenant_id]`, letting the readiness probe query across tenants.  
  **Mitigation:** preserve explicit filter merge in `maybe_put_category_filter/2` and assert captured `%Scrypath.Query.filter` contains both `tenant_id` and `category_id`.  
  **Detection:** `mix test examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`.
- **T-107-002 (Integrity, Medium):** A future contributor changes the phase gate to a broad or live-service suite, making the regression guard slow, flaky, or hard to run locally.  
  **Mitigation:** `verify.phase107` uses an explicit focused test list and a self-test that asserts the task source includes only the intended paths.  
  **Detection:** `mix test test/mix/tasks/verify.phase107_test.exs` and `mix verify.phase107`.
- **T-107-003 (Scope Creep, Medium):** The repair switches to `tenant_scope:`, adds shared helper abstractions, expands Playwright fixtures, or promotes CI lanes, proving a different behavior than the known explicit-filter bug.  
  **Mitigation:** plan-level negative assertions and no edits to Playwright helper/spec or GitHub Actions unless a direct pre-existing helper bug is discovered.  
  **Detection:** `! rg "tenant_scope:" examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex` plus `git diff -- examples/scrypath_ecommerce/e2e/storefront.spec.ts examples/scrypath_ecommerce/e2e/helpers/e2e.ts .github/workflows/ci.yml`.
</threat_model>

<verification>
- `mix test examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`
- `mix test test/mix/tasks/verify.phase107_test.exs`
- `mix verify.phase107`
- `rg "Keyword\\.get\\(:filter, \\[\\]\\)|Keyword\\.put\\(:category_id, category_id\\)" examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex`
- `rg "Enum\\.sort\\(filter\\) == \\[category_id: 202, tenant_id: 101\\]" examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`
- `rg "\"verify.phase107\": :test" mix.exs`
- `! rg "tenant_scope:" examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex`
</verification>

<success_criteria>
- `/dev/e2e/search-visible` preserves `tenant_id` while applying `category_id` filtering.
- Regression coverage proves the explicit filter-merge bug class by inspecting the `%Scrypath.Query{}` sent to the backend.
- `mix verify.phase107` runs a focused, deterministic, service-free local gate.
- Phase 107 does not expand browser E2E fixtures, live-service dependencies, required CI topology, public API surface, or helper abstractions.
</success_criteria>

<output>
- Updated `examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex`.
- Updated `examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs`.
- New `lib/mix/tasks/verify.phase107.ex`.
- New `test/mix/tasks/verify.phase107_test.exs`.
- Updated `mix.exs`.
</output>
