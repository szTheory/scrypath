---
phase: 45
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
  - scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs
autonomous: true
requirements:
  - OPSUI-01
---

<objective>

Replace **`PostureLive`** stub with a **read-only** fleet table over **`Scrypath.sync_status/2`** for each allowlisted schema, honoring **backend vs queue** columns, **`queue.observed?`**, per-row **`{:ok, %Scrypath.Operator.Status{}}` / `{:error, term}`**, **manual refresh**, and bounded concurrent fetch per CONTEXT D-04–D-08.

</objective>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-45-02 | High-cardinality telemetry (per-schema module in metric labels) | medium | Any new `:telemetry` execute only on refresh aggregate; event metadata keys documented in code comment — **no** schema module in label set. |
| T-45-02b | UI implies global health from one row | low | Show aggregate line (“N fetch errors”) plus per-row state; no single green/red badge for fleet. |

</threat_model>

<tasks>

<task id="45-02-01" type="execute">
<read_first>
- `.planning/phases/45-posture-failure-triage/45-CONTEXT.md` (D-04–D-08, D-20)
- `lib/scrypath/operator/status.ex`
- `lib/scrypath.ex` (`sync_status/2` doc)
- `scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex`
- `scrypath_ops/lib/scrypath_ops/schemas.ex` (from plan 01)
</read_first>
<action>

1. In **`mount/3`**, load **`ScrypathOps.Schemas.allowlist/0`** into assigns; default shared **`operator_opts`** / runtime opts from **`ScrypathOps.Schemas.default_operator_opts/0`** merged with any LiveView-specific overrides.

2. Implement **`load_posture/1`** (or similar) that maps each schema module with **`Task.async_stream(..., max_concurrency: 3, timeout: 15_000, on_timeout: :kill_task)`** (adjust numbers if needed) to **`Scrypath.sync_status(mod, opts)`**, producing a list of **`{:ok, schema, result}`** / **`{:error, schema, reason}`** tuples sorted with **errors first** (CONTEXT D-06).

3. Replace stub HEEx with a **dense `<table>`**: columns at minimum **schema**, **index**, **`sync_mode`**, backend pending/failed counts, backend **last_succeeded** (render `"—"` when nil), queue **observed** flag, queue pending/retrying/failed counts, queue **last_succeeded**. Add a **“Refresh”** button calling **`phx-click="refresh"`** / **`handle_event/3`**.

4. Add optional **auto-refresh** assign default **`false`**; when `true`, use **`Phoenix.LiveView.schedule_tick/3`** or attach **`hook="VisibilityRefresh"`** only if implemented — acceptable for phase 45 to ship **manual-only** if the assign and README note future work; if auto-refresh is implemented, gate on **tab visible** per CONTEXT D-07.

5. Add **`test/scrypath_ops_web/live/posture_live_test.exs`** using **`Phoenix.LiveViewTest`** with **`Scrypath.sync_status/2`** stubbed via **`:meilisearch_tasks`** / **`:oban_jobs`** fakes (return canned successes and one `{:error, :boom}` row) — assert HTML contains **`data-testid="posture-row"`** (or stable substring **`queue not observed`** when `observed?` is false in stub).

</action>
<acceptance_criteria>

- `grep -q "Full posture dashboards" scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex && echo found || echo notfound` — the legacy stub sentence **must not** remain (expect **`notfound`**).
- `grep -q "sync_status" scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex && echo ok` outputs **`ok`**.
- `grep -q "Task.async_stream" scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex && echo ok` outputs **`ok`**.
- `cd scrypath_ops && mix test test/scrypath_ops_web/live/posture_live_test.exs` exits 0.

</acceptance_criteria>
</task>

</tasks>

<verification>

1. `cd scrypath_ops && mix test test/scrypath_ops_web/live/posture_live_test.exs` exits 0.

</verification>

<success_criteria>

- OPSUI-01: landing shows real posture API results per allowlisted schema with honest error rows and refresh.

</success_criteria>

<must_haves>

- [ ] Table wired to **`Scrypath.sync_status/2`**
- [ ] Manual refresh
- [ ] LiveViewTest with injected operator opts

</must_haves>
