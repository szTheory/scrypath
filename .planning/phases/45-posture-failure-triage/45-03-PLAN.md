---
phase: 45
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
  - scrypath_ops/test/scrypath_ops_web/live/failed_sync_live_test.exs
autonomous: true
requirements:
  - OPSUI-02
---

<objective>

Replace **`FailedSyncLive`** stub with **`Scrypath.failed_sync_work/2`** using **`reason_class_counts: true`**, displaying **`%Scrypath.Operator.FailedSyncWorkInspection{}`** rollups from **`counts`** and a table over **`entries`** with CLI-aligned columns and default sort **newest first**, read-only row expand and doc links only (CONTEXT D-09–D-14).

</objective>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-45-03 | Rollups recomputed from truncated `entries` | high | Never derive header totals from sliced `entries`; always from **`inspection.counts`**; pagination only mutates displayed subset with copy if subset. |
| T-45-03b | Operators trigger retries from UI | high | No `phx-click` calling **`Scrypath.retry_sync_work/2`**; template contains **no** substring **`retry_sync_work`**. |

</threat_model>

<tasks>

<task id="45-03-01" type="execute">
<read_first>
- `.planning/phases/45-posture-failure-triage/45-CONTEXT.md` (D-09–D-14)
- `lib/scrypath/operator/failed_work.ex`
- `lib/scrypath/operator/failed_sync_work_inspection.ex`
- `guides/operator-mix-tasks.md` (failed task section)
- `scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex`
</read_first>
<action>

1. On **`mount`** and **refresh**, for each allowlisted schema (same list as posture) call **`Scrypath.failed_sync_work(mod, [{:reason_class_counts, true} | operator_opts])`**. If multiple schemas, default UI is **per-schema tabs** or a **schema `<select>`** at top (pick one; document in module doc).

2. Assign **`%FailedSyncWorkInspection{entries: e, counts: c}`** on success. Render a **rollup strip** from **`c`** (use inspect of struct keys in **`Scrypath.Operator.ReasonClassCounts`** or print `_struct_` in IEx if needed — plan executor must read the struct module).

3. HEEx table: include **`reason_class`** per row; map **`:unknown`** to the visible text **`unknown`** (never blank). Sort **`entries`** by **`last_attempt_at`** (fallback **`failed_at`**) descending for “newest first” — see **`FailedWork`** struct fields in **`failed_work.ex`**.

4. Row detail: **`<details>`** or toggle for long **`reason`** / **`metadata`**; include plain-text pointers to **`guides/drift-recovery.md`** and **`guides/operator-mix-tasks.md`** in the template (same style as **`operator-ia.md`** repo-relative paths).

5. Optional **compact mode** toggle: when on, hide rollup strip in CSS (`hidden` class) but **`assigns[:inspection]`** must still hold full **`counts`**.

6. Add **`failed_sync_live_test.exs`** with fake operator rows + counts; assert rendered rollup includes the substring **`total`** adjacent to the numeric total from **`inspection.counts.total`** (e.g. stub with **`total: 2`**) and table cells include **`reason_class`** text for stub rows.

</action>
<acceptance_criteria>

- `grep -q "retry_sync_work" scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex; test $? -eq 1` exits 0 (grep **finds nothing**).
- `grep -q "reason_class_counts" scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex && echo ok` outputs **`ok`**.
- `grep -q "FailedSyncWorkInspection" scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex && echo ok` outputs **`ok`**.
- `cd scrypath_ops && mix test test/scrypath_ops_web/live/failed_sync_live_test.exs` exits 0.

</acceptance_criteria>
</task>

</tasks>

<verification>

1. `cd scrypath_ops && mix test test/scrypath_ops_web/live/failed_sync_live_test.exs` exits 0.

</verification>

<success_criteria>

- OPSUI-02: failed sync view matches **`failed_sync_work/2`** inspection semantics including **`reason_class`** rollups.

</success_criteria>

<must_haves>

- [ ] `reason_class_counts: true` path
- [ ] No recovery buttons
- [ ] LiveViewTest proves rollups + rows coherent for stub data

</must_haves>
