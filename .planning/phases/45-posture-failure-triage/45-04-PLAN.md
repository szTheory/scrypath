---
phase: 45
plan: "04"
type: execute
wave: 3
depends_on:
  - "01"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex
  - scrypath_ops/test/scrypath_ops_web/live/sync_drift_live_test.exs
  - scrypath_ops/docs/operator-ia.md
autonomous: true
requirements:
  - OPSUI-03
---

<objective>

Replace **`SyncDriftLive`** stub with **two labeled sections**: (A) **`Scrypath.reconcile_sync/2`** without **`include_index_contract_drift: true`** on default load/refresh; (B) **`Scrypath.index_contract_drift/2`** only after an explicit control — with scoped errors, timestamps, and read-only links to Mix tasks and guides (CONTEXT D-15–D-19).

</objective>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-45-04 | Default load calls **`include_index_contract_drift: true`** | high | Default **`handle_event`** / **`mount`** path must **not** pass that flag; grep verification. |
| T-45-04b | Copy conflates reconcile drift with index contract | medium | Section headings use distinct strings **`Sync & queue posture`** and **`Index contract (declared vs live)`** verbatim in HEEx. |

</threat_model>

<tasks>

<task id="45-04-01" type="execute">
<read_first>
- `.planning/phases/45-posture-failure-triage/45-CONTEXT.md` (D-15–D-19)
- `lib/scrypath.ex` (`reconcile_sync/2`, `index_contract_drift/2` docs)
- `lib/scrypath/operator/reconcile.ex`
- `scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex`
- `scrypath_ops/docs/operator-ia.md`
</read_first>
<action>

1. Add assigns: **`:selected_schema`**, **`:reconcile_result`**, **`:reconcile_loaded_at`**, **`:drift_result`**, **`:drift_loaded_at`**, **`:drift_error`**. Default **`mount`** may leave reconcile/drift nil until user action or provide **“Load reconcile”** button as primary — choose **one explicit refresh** for reconcile on first visit **or** auto-load reconcile only (no drift) per CONTEXT D-15; document choice in `@moduledoc`.

2. Implement **`"refresh_reconcile"`** → **`Scrypath.reconcile_sync(selected, opts)`** with **no** **`include_index_contract_drift: true`**. On success store **`%Scrypath.Operator.Reconcile{}`** in assigns; render **headings** **`Sync & queue posture`** as a `<h2>` (or same visible text).

3. Implement **`"load_drift"`** → **`Scrypath.index_contract_drift(selected, opts)`** separately; failures go to **`:drift_error`** only; **do not** clear successful reconcile assigns.

4. HEEx second section heading must include the substring **`Index contract (declared vs live)`** exactly.

5. Body copy for links must mention at least these substrings somewhere in the template or adjacent doc panel: **`mix scrypath.reconcile`**, **`mix scrypath.index.contract_drift`**, **`guides/drift-recovery.md`**, **`guides/sync-modes-and-visibility.md`**.

6. Update **`scrypath_ops/docs/operator-ia.md`** JTBD rows for sync/drift to say **shipped (phase 45)** where appropriate (grep-verifiable phrase **`phase 45`** in the row for job 3).

7. Add **`sync_drift_live_test.exs`** proving: after reconcile event, assigns contain reconcile struct; drift section shows error when fake returns **`{:error, :settings}`** without breaking reconcile HTML.

</action>
<acceptance_criteria>

- `grep "include_index_contract_drift: true" scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex | wc -l | tr -d ' '` outputs **`0`**.
- `grep -F "Sync & queue posture" scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex >/dev/null && echo ok` outputs **`ok`**.
- `grep -F "Index contract (declared vs live)" scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex >/dev/null && echo ok` outputs **`ok`**.
- `grep -F "phase 45" scrypath_ops/docs/operator-ia.md >/dev/null && echo ok` outputs **`ok`**.
- `cd scrypath_ops && mix test test/scrypath_ops_web/live/sync_drift_live_test.exs` exits 0.

</acceptance_criteria>
</task>

</tasks>

<verification>

1. `cd scrypath_ops && mix test test/scrypath_ops_web/live/sync_drift_live_test.exs` exits 0.
2. `cd scrypath_ops && mix test` exits 0.

</verification>

<success_criteria>

- OPSUI-03: read-only reconcile + lazy contract drift with doc/Mix linkage and honest error scoping.

</success_criteria>

<must_haves>

- [ ] No default **`include_index_contract_drift: true`**
- [ ] Two labeled sections in UI
- [ ] `operator-ia.md` traceability updated

</must_haves>
