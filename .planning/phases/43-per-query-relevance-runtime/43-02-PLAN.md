---
phase: 43-per-query-relevance-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - lib/scrypath/multi_search/entries.ex
  - test/scrypath/search_many_test.exs
autonomous: true
requirements:
  - TUNE-PQ-01
  - TUNE-PQ-02
tags:
  - search_many
  - merge
must_haves:
  truths:
    - "When both shared and entry supply `:per_query`, inner keys merge with **`Map.merge(shared_map, entry_map)`** (entry wins on duplicate inner keys) after top-level entry-wins merge — per **43-CONTEXT** D-11."
    - "`search_many/2` tests cover shared-only, entry-only, and both-sides `:per_query` matrix."
  artifacts:
    - path: "lib/scrypath/multi_search/entries.ex"
      provides: "per_query shallow inner merge"
      contains: "per_query"
---

<objective>
Implement **43-CONTEXT D-11** **`:per_query`** merge semantics in **`MultiSearch.Entries`** and lock behavior with **`search_many`** tests (**TUNE-PQ-01**, **TUNE-PQ-02**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-per-query-relevance-runtime/43-CONTEXT.md
@.planning/phases/43-per-query-relevance-runtime/43-RESEARCH.md
@.planning/phases/43-per-query-relevance-runtime/43-PATTERNS.md
@guides/per-query-tuning-pipeline.md (Federation and search_many section)
</context>

<tasks>

<task type="auto">
  <name>Task 1 (43-02-01): Inner Map.merge for :per_query in Entries.normalize</name>
  <files>lib/scrypath/multi_search/entries.ex</files>
  <read_first>
    - lib/scrypath/multi_search/entries.ex (full `normalize_one/2` and `Keyword.merge/3` on shared vs entry)
    - .planning/phases/43-per-query-relevance-runtime/43-CONTEXT.md (D-10 through D-13)
  </read_first>
  <action>
    After computing **`merged`** with **`Keyword.merge(shared, entry_core, fn _k, _s, e -> e end)`**, normalize **`:per_query`** as follows:
    - Let **`s`** = **`Keyword.get(shared, :per_query)`** (may be nil).
    - Let **`e`** = **`Keyword.get(entry_core, :per_query)`** (may be nil).
    - If **both** **`s`** and **`e`** are present and are **maps** (if lists appear, normalize to map with **`Map.new/1`** only if already validated upstream — otherwise assume maps per Phase 43 validation order), set **`merged`** to **`Keyword.put(merged, :per_query, Map.merge(Map.new(s), Map.new(e)))`** so **entry** inner keys win.
    - If **only one** side is present, ensure **`merged`** retains that side’s **`:per_query`** map unchanged (the top-level **`Keyword.merge`** already does this when the other side omits **`:per_query`** — **verify** and only special-case the **both-present** branch).
    - If **neither** side has **`:per_query`**, do not inject the key.
  </action>
  <verify>
    <automated>mix test test/scrypath/search_many_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n ':per_query' lib/scrypath/multi_search/entries.ex`** returns ≥ 1 line.
    - **`grep -n 'Map.merge' lib/scrypath/multi_search/entries.ex`** returns ≥ 1 line in **`normalize_one`** path (or a dedicated helper **called from** **`normalize_one`**).
    - **`mix test test/scrypath/search_many_test.exs`** exits 0.
  </acceptance_criteria>
  <done>
    Inner merge semantics implemented for multi-search normalization.
  </done>
</task>

<task type="auto">
  <name>Task 2 (43-02-02): search_many tests for :per_query merge matrix</name>
  <files>test/scrypath/search_many_test.exs</files>
  <read_first>
    - test/scrypath/search_many_test.exs (existing `@base_opts` and success path tests)
    - lib/scrypath/search.ex (`validate_search_quads/1` — queries built from merged opts)
  </read_first>
  <action>
    Add **ExUnit** tests under **`Scrypath.SearchManyTest`** that assert the **Meilisearch query** or **`%Scrypath.Query{}`** passed to the backend contains merged **`:per_query`** outcomes. Preferred approach (pick **one** that stays **daemon-free**):
    - **Option A:** Extend **`SequentialOnlyBackend.search/3`** (in this test file) to **`send(self(), {:query, query})`** and assert **`query.per_query`** map for three scenarios: **(1)** shared only, **(2)** entry only, **(3)** shared **`%{show_ranking_score: true}`** + entry **`%{ranking_score_threshold: 0.42}`** yields **`%{show_ranking_score: true, ranking_score_threshold: 0.42}`** with **float** threshold preserved.
    - Document in test comments that **(3)** proves **D-11** entry wins on overlapping inner keys if you add a fourth case with conflicting booleans.
  </action>
  <verify>
    <automated>mix test test/scrypath/search_many_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'per_query' test/scrypath/search_many_test.exs`** returns ≥ 3 lines (tests or opts).
    - File **`test/scrypath/search_many_test.exs`** contains the substring **`per_query`** in at least **three** distinct non-empty lines (**`grep -c per_query test/scrypath/search_many_test.exs`** returns a number **≥ 3**).
    - **`mix test test/scrypath/search_many_test.exs`** exits 0.
  </acceptance_criteria>
  <done>
    Multi-search merge matrix covered by automated tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-entry opts | Must not bypass federation shared-only key rejection; `:per_query` is not a federation rail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-43-02-01 | Elevation of privilege | N/A for merge-only | accept | No auth surface. |

## Security Notes

Merge logic only; validation remains in **`validate_search_options`**.

## Security Verification Commands (ASVS L1)

```bash
mix test test/scrypath/search_many_test.exs
```
</threat_model>

<verification>
- `mix test test/scrypath/search_many_test.exs` exits 0.
- Conflicting inner keys favor entry map (documented test assertion).
</verification>

<success_criteria>
- [ ] **TUNE-PQ-01** `search_many/2` honors `:per_query` merge rules from the locked spec.
- [ ] **TUNE-PQ-02** automated regression tests for merge matrix.
</success_criteria>
