---
phase: 43-per-query-relevance-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - lib/mix/tasks/verify.phase43.ex
  - mix.exs
  - test/scrypath/docs_contract_test.exs
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
  - lib/scrypath.ex
autonomous: true
requirements:
  - TUNE-PQ-02
  - TUNE-PQ-03
tags:
  - verify
  - docs_contract
  - ci
must_haves:
  truths:
    - "`mix verify.phase43` exists, is registered in **`mix.exs`**, runs a thin **`mix test`** slice, and is invoked from CI **`quality`** job."
    - "`docs_contract_test.exs` pins **`verify.phase43`** source via **`@verify_phase43`** (same hygiene as phase 41)."
    - "Public **`Scrypath.search/3`** (and **`search_many/2`** if not already sufficient) **`@doc`** mentions the **`:per_query`** keyword with a pointer to **`guides/per-query-tuning-pipeline.md`**."
  artifacts:
    - path: "lib/mix/tasks/verify.phase43.ex"
      provides: "thin PR gate for per-query runtime + contracts"
      contains: "Verify.Phase43"
---

<objective>
Add **`mix verify.phase43`**, wire **CI + CONTRIBUTING**, extend **`docs_contract_test.exs`** pins, and anchor public **`@doc`** for **`:per_query`** so **TUNE-PQ-02** and **TUNE-PQ-03** cannot drift silently.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-per-query-relevance-runtime/43-CONTEXT.md (D-30, D-31)
@lib/mix/tasks/verify.phase41.ex
@test/scrypath/docs_contract_test.exs (existing `@verify_phase41` patterns)
@.github/workflows/ci.yml
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (43-03-01): Add Mix.Tasks.Verify.Phase43 thin composer</name>
  <files>lib/mix/tasks/verify.phase43.ex, mix.exs</files>
  <read_first>
    - lib/mix/tasks/verify.phase41.ex (entire file — template)
    - mix.exs (`aliases/0` around **`verify.phase41`**)
  </read_first>
  <action>
    1. Create **`lib/mix/tasks/verify.phase43.ex`** as **`Mix.Tasks.Verify.Phase43`** mirroring **`Phase41`**: **`@moduledoc false`**, **`use Mix.Task`**, **`@shortdoc`** text **exact substring** **`Per-query`** (for doc grep), **`@focused_tests`** list containing **`"test/scrypath/docs_contract_test.exs"`**, **`"test/scrypath/per_query_tuning_test.exs"`**, **`"test/scrypath/search_test.exs"`**, and **`"test/scrypath/search_many_test.exs"`** (doc contracts first, then per-query tests).
    2. Add **`"verify.phase43": :test`** to **`mix.exs`** **`aliases/0`** **immediately after** **`"verify.phase41": :test`**.
  </action>
  <verify>
    <automated>mix verify.phase43</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'defmodule Mix.Tasks.Verify.Phase43' lib/mix/tasks/verify.phase43.ex`** returns 1 line.
    - **`grep -n 'per_query_tuning_test' lib/mix/tasks/verify.phase43.ex`** returns ≥ 1 line.
    - **`grep -n 'verify.phase43' mix.exs`** returns ≥ 1 line.
    - **`mix verify.phase43`** exits 0.
  </acceptance_criteria>
  <done>
    Thin verify task runs focused tests.
  </done>
</task>

<task type="auto">
  <name>Task 2 (43-03-02): docs_contract_test pins + CI + CONTRIBUTING</name>
  <files>test/scrypath/docs_contract_test.exs, .github/workflows/ci.yml, CONTRIBUTING.md</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (`@verify_phase41` and **`test \"verify.phase41 pins`** block)
    - .github/workflows/ci.yml (step **`Federation docs gate`**)
    - CONTRIBUTING.md (paragraph mentioning **`mix verify.phase41`**)
  </read_first>
  <action>
    1. Add **`@verify_phase43 File.read!(\"lib/mix/tasks/verify.phase43.ex\")`** next to **`@verify_phase41`**.
    2. Add **`ExUnit`** test **`test \"verify.phase43 pins per-query verify task and focused paths\"`** asserting **`@verify_phase43`** contains all of: **`\"Mix.Tasks.Verify.Phase43\"`**, **`\"docs_contract_test.exs\"`**, **`\"per_query_tuning_test.exs\"`**, **`\"search_test.exs\"`**, **`\"search_many_test.exs\"`**, and refutes **`\"HEX_API_KEY\"`** (same style as phase 41 test).
    3. In **`.github/workflows/ci.yml`**, add a **`quality`** job step **immediately after** **`Federation docs gate (`mix verify.phase41`)`** named **`Per-query runtime gate (`mix verify.phase43`)`** with **`run: mix verify.phase43`**.
    4. In **`CONTRIBUTING.md`**, extend the **`quality`** row and add one short paragraph: **`mix verify.phase43`** is the fast gate for **per-query Plane B** tests + doc contracts; must mention **`verify.phase43`** literal **twice** (table + prose) mirroring **`verify.phase41`** pattern.
  </action>
  <verify>
    <automated>mix verify.phase43</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n '@verify_phase43' test/scrypath/docs_contract_test.exs`** returns ≥ 1 line.
    - **`grep -n 'verify.phase43' .github/workflows/ci.yml`** returns ≥ 1 line.
    - **`grep -n 'verify.phase43' CONTRIBUTING.md`** returns ≥ 2 lines.
    - **`mix verify.phase43`** exits 0.
  </acceptance_criteria>
  <done>
    Doc contracts and contributor docs reference the new verify slice; CI enforces it.
  </done>
</task>

<task type="auto">
  <name>Task 3 (43-03-03): Public @doc for :per_query keyword</name>
  <files>lib/scrypath.ex</files>
  <read_first>
    - lib/scrypath.ex (`@doc` for **`search/3`**, **`search_many/2`**, **`search_within_facet/3`**)
    - guides/per-query-tuning-pipeline.md (link target)
  </read_first>
  <action>
    Extend **`@doc`** on **`Scrypath.search/3`** with a bullet or short paragraph that:
    - Names the keyword **`:per_query`** literally (grep target).
    - States it is an **allowlisted map** for Plane B Meilisearch search parameters (v1.9 slice: ranking score threshold / show flags).
    - Links **`guides/per-query-tuning-pipeline.md`** (relative path already used in existing **`@doc`** — keep consistent).
    Add an equivalent **one-line** mention on **`search_many/2`** **`@doc`** that **`:per_query`** may appear in **shared** and **per-entry** keywords and follows the guide’s merge rules.
  </action>
  <verify>
    <automated>mix verify.phase43</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n ':per_query' lib/scrypath.ex`** returns ≥ 2 lines (search + search_many mentions).
    - **`grep -n 'per-query-tuning-pipeline' lib/scrypath.ex`** returns ≥ 2 lines.
    - **`mix verify.phase43`** exits 0.
  </acceptance_criteria>
  <done>
    ExDoc-facing entry documents the new keyword.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verify task source | Must not embed CI secrets (HEX keys). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-43-03-01 | Tampering | CONTRIBUTING / CI drift | mitigate | `docs_contract_test` reads verify source; CI runs **`mix verify.phase43`**. |

## Security Notes

Doc and CI wiring only in this plan.

## Security Verification Commands (ASVS L1)

```bash
! grep -q HEX_API_KEY lib/mix/tasks/verify.phase43.ex
mix verify.phase43
```
</threat_model>

<verification>
- `mix verify.phase43` exits 0 in a clean tree.
- `@verify_phase43` assertions pass.
</verification>

<success_criteria>
- [ ] **TUNE-PQ-02**: Focused verify slice covers per-query tests + contracts.
- [ ] **TUNE-PQ-03**: Doc contracts + public `@doc` mention **`:per_query`** and the canonical guide.
</success_criteria>
