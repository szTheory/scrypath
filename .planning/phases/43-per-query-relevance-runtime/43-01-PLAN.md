---
phase: 43-per-query-relevance-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/options.ex
  - lib/scrypath/query.ex
  - lib/scrypath/meilisearch/query.ex
  - lib/scrypath/search.ex
  - test/scrypath/per_query_tuning_test.exs
autonomous: true
requirements:
  - TUNE-PQ-01
tags:
  - options
  - query
  - meilisearch
  - telemetry
must_haves:
  truths:
    - "`:per_query` is validated on `search/3` and `search_within_facet/4` with a strict allowlist matching `guides/per-query-tuning-pipeline.md` v1.9 exemplars."
    - "`Scrypath.Meilisearch.Query.to_payload/1` emits `rankingScoreThreshold`, `showRankingScore`, and `showRankingScoreDetails` when present (camelCase JSON keys)."
    - "Telemetry adds documented low-cardinality metadata when `show_ranking_score_details` is truthy in effective per-query options — without `Mix.env/0`."
  artifacts:
    - path: "lib/scrypath/options.ex"
      provides: "NimbleOptions :per_query nested map"
      contains: ":per_query"
    - path: "test/scrypath/per_query_tuning_test.exs"
      provides: "validation + payload regression tests"
      contains: "PerQueryTuningTest"
---

<objective>
Implement **Plane B** **`:per_query`** validation, carry it on **`%Scrypath.Query{}`**, project it into Meilisearch search JSON, and emit **telemetry** when expensive debug details are enabled — satisfying **TUNE-PQ-01** for the single-index path (`search/3`, `search_within_facet/4` reuse validation).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-per-query-relevance-runtime/43-CONTEXT.md
@.planning/phases/43-per-query-relevance-runtime/43-RESEARCH.md
@.planning/phases/43-per-query-relevance-runtime/43-PATTERNS.md
@guides/per-query-tuning-pipeline.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (43-01-01): Options, Query, Meilisearch payload, and unit tests</name>
  <files>lib/scrypath/options.ex, lib/scrypath/query.ex, lib/scrypath/meilisearch/query.ex, test/scrypath/per_query_tuning_test.exs</files>
  <read_first>
    - lib/scrypath/options.ex (entire `@search_options` and `validate_search_options/2`)
    - lib/scrypath/query.ex (entire module — struct and `new/2`)
    - lib/scrypath/meilisearch/query.ex (entire `to_payload/1`)
    - guides/per-query-tuning-pipeline.md (v1.9 exemplars section)
    - test/support/searchable_post.ex (`SearchablePost` schema used across search tests)
    - test/scrypath/search_test.exs (patterns for `Scrypath.search/3` with `FakeBackend`)
  </read_first>
  <action>
    1. In **`lib/scrypath/options.ex`**, add **`per_query:`** to **`@search_options`** with a **custom validator** (e.g. `{:custom, __MODULE__, :validate_per_query_map, []}`) that:
       - Accepts **`%{}`** or **`[]`** (normalize empty to **`%{}`** in the returned keyword).
       - Allowlists **exactly** these **atom keys**: **`:ranking_score_threshold`**, **`:show_ranking_score`**, **`:show_ranking_score_details`**.
       - Types: **`:ranking_score_threshold`** — non-negative number (integer or float); **`:show_ranking_score`** and **`:show_ranking_score_details`** — strict booleans.
       - Any other key → **`{:error, {:validation, _}}`** via NimbleOptions (same shape family as existing invalid search options).
    2. In **`lib/scrypath/query.ex`**: add field **`per_query`** default **`%{}`** to **`defstruct`** and **`@type t`**. In **`new/2`**, set **`per_query:`** from **`Keyword.get(opts, :per_query, %{})`** (must be a map after validation).
    3. In **`lib/scrypath/meilisearch/query.ex`**: extend **`to_payload/1`** so that for each present allowlisted value in **`query.per_query`**, the JSON map includes:
       - **`"rankingScoreThreshold"`** when `:ranking_score_threshold` is present (numeric encoding per existing **`Jason`** / Meilisearch expectations used elsewhere in the project).
       - **`"showRankingScore"`** when `:show_ranking_score` is **`true`** (omit key when **`false`** unless the team standard is to always send booleans — pick **one** behavior and document in **`@moduledoc`** here; default **omit when false** to minimize payload).
       - **`"showRankingScoreDetails"`** when `:show_ranking_score_details` is **`true`** (same omit-when-false rule).
    4. Create **`test/scrypath/per_query_tuning_test.exs`** with **`use ExUnit.Case, async: true`** containing at minimum:
       - One test: **`Scrypath.Options.validate_search_options(SearchablePost, per_query: [bad: :key])`** returns **`{:error, _}`** (assert `elem(result, 0) == :error`).
       - One test: valid map passes validation and **`Scrypath.Meilisearch.Query.to_payload(Scrypath.Query.new("hi", validated_kw))`** includes **`"rankingScoreThreshold"`** when threshold set (use **`SearchablePost`** and the same **`FakeBackend` / opts patterns** as **`search_test.exs`** if you need a schema; for **`Query.new`** + **`to_payload`**, schema may be unnecessary).
    5. Update **`test/scrypath/search_test.exs`** (and any other tests pattern-matching **`%Query{facet_filter: ...}`** exhaustively) so they include the new **`per_query:`** field — default **`%{}`** in expected structs.
  </action>
  <verify>
    <automated>mix test test/scrypath/per_query_tuning_test.exs test/scrypath/search_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n ':per_query' lib/scrypath/options.ex`** returns ≥ 1 line inside **`@search_options`**.
    - **`grep -n 'per_query' lib/scrypath/query.ex`** returns ≥ 2 lines (struct field + **`new/2`**).
    - **`grep -n 'rankingScoreThreshold' lib/scrypath/meilisearch/query.ex`** returns ≥ 1 line.
    - File **`test/scrypath/per_query_tuning_test.exs`** exists and **`grep -n 'defmodule Scrypath.PerQueryTuningTest' test/scrypath/per_query_tuning_test.exs`** returns 1 line.
    - **`mix test test/scrypath/per_query_tuning_test.exs test/scrypath/search_test.exs`** exits 0.
  </acceptance_criteria>
  <done>
    `:per_query` validates, flows through **`Query`**, projects to Meilisearch JSON, and has dedicated unit tests.
  </done>
</task>

<task type="auto">
  <name>Task 2 (43-01-02): Telemetry metadata for ranking-score details</name>
  <files>lib/scrypath/search.ex</files>
  <read_first>
    - lib/scrypath/search.ex (`do_search/5`, `Telemetry.span` call sites)
    - lib/scrypath/telemetry.ex (`common_metadata/3`, `stop_metadata/1` if present)
    - .planning/phases/43-per-query-relevance-runtime/43-CONTEXT.md (D-20, D-22)
  </read_first>
  <action>
    In **`do_search/5`**, when the **effective** search opts (the **`search_opts`** argument already validated — **not** raw caller opts) contain **`per_query: %{show_ranking_score_details: true}`** or **`[show_ranking_score_details: true]`** normalized to **truthy** in the **map** form, add **one** documented **atom** key to the **span start metadata** (e.g. **`ranking_score_details: true`**) so operators can sample it. **Do not** call **`Mix.env/0`**. Keep cardinality fixed (boolean only).
  </action>
  <verify>
    <automated>mix test test/scrypath/per_query_tuning_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'ranking_score_details' lib/scrypath/search.ex`** returns ≥ 1 line **or** the chosen metadata key appears with **`grep -n 'show_ranking_score_details' lib/scrypath/search.ex`** in telemetry assembly (executor must pick **one** grep-able key and keep it stable).
    - **`rg 'Mix\.env' lib/scrypath/search.ex`** exits with **no matches** (shell exit code **1**, meaning forbidden gating is absent).
    - **`mix test test/scrypath/per_query_tuning_test.exs`** exits 0 after adding a minimal test that attaches to **`[:scrypath, :search, :start]`** or **`:stop`** and asserts the metadata key when **`per_query: [show_ranking_score_details: true]`** is passed through **`Scrypath.search/3`** with **`FakeBackend`** — mirror patterns from **`test/scrypath/search_within_facet_test.exs`** (telemetry attach in test).
  </acceptance_criteria>
  <done>
    Expensive debug knob surfaces in telemetry without compile-time env hacks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller opts → library | Untrusted controllers must not pass raw params; library validates allowlist. |
| Library → Meilisearch | JSON body must only contain intended search fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01-01 | Tampering | Extra keys in `:per_query` map | mitigate | NimbleOptions allowlist; unknown keys error. |
| T-43-01-02 | Info disclosure | `show_ranking_score_details` in prod | mitigate | Document-only discouragement + telemetry sampling metadata — no secret data in metadata values. |

## Security Notes

No new secret handling; ranking options affect response shape only.

## Security Verification Commands (ASVS L1)

```bash
grep -n "Mix.env" lib/scrypath/search.ex lib/scrypath/options.ex || test $? = 1
mix test test/scrypath/per_query_tuning_test.exs
```
</threat_model>

<verification>
- `mix test test/scrypath/per_query_tuning_test.exs test/scrypath/search_test.exs` exits 0.
- `Scrypath.Meilisearch.Query.to_payload/1` includes camelCase ranking fields when `:per_query` supplies them.
</verification>

<success_criteria>
- [ ] **TUNE-PQ-01**: `search/3` and `search_within_facet/4` accept validated `:per_query` per locked spec exemplars.
- [ ] Wire projection matches Meilisearch field names for the v1.9 slice.
- [ ] Telemetry documents expensive-debug usage without `Mix.env/0`.
</success_criteria>
