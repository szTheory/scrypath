---
phase: "05-reindexing-and-operational-workflows"
plan: "05-04"
type: "execute"
wave: 4
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - lib/scrypath.ex
  - lib/scrypath/reindex.ex
  - README.md
  - ARCHITECTURE.md
  - test/scrypath/reindex_test.exs
autonomous: true
requirements:
  - OPER-02
  - OPER-03
  - OPER-05
must_haves:
  truths:
    - "A developer can run a managed reindex workflow instead of composing index creation, settings application, backfill, and cutover manually."
    - "Managed reindex applies settings to the target index before backfill and records whether cutover occurred."
    - "Official docs explain drift, cutover, backfill, eventual consistency, and recovery semantics plainly."
  artifacts:
    - "lib/scrypath.ex"
    - "lib/scrypath/reindex.ex"
    - "README.md"
    - "ARCHITECTURE.md"
    - "test/scrypath/reindex_test.exs"
  key_links:
    - "lib/scrypath.ex -> public reindex entrypoint keeps the operator workflow under Scrypath.*"
    - "lib/scrypath/reindex.ex -> workflow order is create target -> apply settings -> backfill -> optional cutover"
    - "README.md + ARCHITECTURE.md -> docs define what cutover and recovery do and do not guarantee"
    - "test/scrypath/reindex_test.exs -> proves operation order and cutover targeting"
---

<objective>
Implement managed reindex orchestration and the operator-facing documentation that makes Phase 5 usable in real systems.

Purpose: satisfy OPER-02, complete OPER-03 in workflow form, and deliver the official recovery guidance required by OPER-05.
Output: `Scrypath.reindex/2`, a step-ordered orchestration module, focused workflow tests, and docs that explain drift and recovery without euphemism.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-reindexing-and-operational-workflows/05-RESEARCH.md
@.planning/phases/05-reindexing-and-operational-workflows/05-VALIDATION.md
@.planning/phases/02-meilisearch-core-sync/02-04-SUMMARY.md
@.planning/phases/03-search-query-api-and-hydration/03-04-SUMMARY.md
@.planning/phases/04-oban-and-observability/04-03-SUMMARY.md
@lib/scrypath/backfill.ex
@lib/scrypath/meilisearch.ex
@README.md
@ARCHITECTURE.md
@prompts/elixir-opensource-libs-best-practices-deep-research.md
@prompts/search-lib-use-cases-deep-research.md

<interfaces>
Expected from Phase 5 prior plans:
```elixir
def backfill(schema_module, opts \\ [])
def Scrypath.Meilisearch.create_index(schema_module, index_name, opts)
def Scrypath.Meilisearch.apply_settings(schema_module, index_name, opts)
def Scrypath.Meilisearch.swap_indexes(index_pairs, opts)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build managed reindex orchestration per OPER-02 and OPER-03</name>
  <read_first>lib/scrypath.ex, lib/scrypath/backfill.ex, lib/scrypath/meilisearch.ex, test/scrypath/reindex_test.exs, .planning/phases/05-reindexing-and-operational-workflows/05-RESEARCH.md, .planning/phases/05-reindexing-and-operational-workflows/05-VALIDATION.md</read_first>
  <files>lib/scrypath.ex, lib/scrypath/reindex.ex, test/scrypath/reindex_test.exs</files>
  <behavior>
    - Test 1: `Scrypath.reindex/2` creates a target index, applies settings, backfills, and performs cutover in that exact order.
    - Test 2: `cutover?: false` leaves the live index untouched while still returning the target index and batch counts.
    - Test 3: the result map exposes `live_index`, `target_index`, `settings_applied`, `batches`, `documents`, and `cutover` so operators can see what happened.
  </behavior>
  <acceptance_criteria>`rg -n "def reindex|cutover\\?|settings_applied|target_index" lib/scrypath.ex lib/scrypath/reindex.ex test/scrypath/reindex_test.exs` shows the public API and workflow result keys, and `mix test test/scrypath/reindex_test.exs` passes with ordered-operation assertions.</acceptance_criteria>
  <action>Create `Scrypath.Reindex` and expose `Scrypath.reindex/2` as the Phase 5 managed workflow. Validate options with `Scrypath.Options.validate_reindex_options!/1`, compute `live_index` from the backend and `target_index` from either caller override or a deterministic rebuild suffix, call the Meilisearch lifecycle helpers in this order: create target index, apply settings, invoke `Scrypath.Backfill.run/2` against the target index, and conditionally swap indexes when `cutover?: true`. Return an explicit workflow result map; do not hide cutover, do not delete old indexes in the same step, and do not promise search visibility before backend acceptance semantics allow it.</action>
  <verify>
    <automated>mix test test/scrypath/reindex_test.exs</automated>
  </verify>
  <done>Scrypath provides one intentional managed reindex workflow that operators can invoke without hand-rolling index creation, settings, batch writes, and cutover ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Document drift, backfill, cutover, and recovery semantics per OPER-05</name>
  <read_first>README.md, ARCHITECTURE.md, .planning/phases/02-meilisearch-core-sync/02-04-SUMMARY.md, .planning/phases/03-search-query-api-and-hydration/03-04-SUMMARY.md, .planning/phases/04-oban-and-observability/04-03-SUMMARY.md, .planning/phases/05-reindexing-and-operational-workflows/05-RESEARCH.md</read_first>
  <files>README.md, ARCHITECTURE.md</files>
  <acceptance_criteria>`rg -n "backfill|reindex|cutover|drift|recovery|eventual consistency|detect" README.md ARCHITECTURE.md` returns dedicated operational sections, and the docs state that accepted work is not equivalent to search visibility or full rebuild completion while also describing how operators can detect drift before deciding to backfill or rebuild.</acceptance_criteria>
  <action>Update `README.md` and `ARCHITECTURE.md` with concrete operator guidance for backfill and managed reindex. Include when to backfill into the live index versus rebuild into a target index, how schema setting changes and projection changes create drift, how operators detect drift in practice such as stale search hits, missing hydrated records, document-count mismatches, or failed/discarded async work, why `accepted` and durable enqueue do not imply search visibility, what `cutover?: false` is for, and how to recover from failed backfills, stale deletes, or incomplete rebuilds. Keep the language aligned with Phases 2 through 4: explicit, operational, and blunt about eventual consistency.</action>
  <verify>
    <automated>rg -n "backfill|reindex|cutover|drift|recovery|eventual consistency|detect" README.md ARCHITECTURE.md</automated>
    <manual>Read `README.md` and `ARCHITECTURE.md` after the update and confirm they explicitly distinguish backfill versus managed reindex, `accepted` versus search-visible completion, `cutover?: false` versus real cutover, how operators detect drift before rebuilding, and recovery guidance for drift, stale deletes, and failed rebuilds.</manual>
  </verify>
  <done>The official docs teach users how to reason about drift, rebuilds, cutovers, and recovery using the same runtime semantics the code now implements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| managed workflow -> live and target indexes | Reindex orchestration can create new indexes and cut traffic over to them. |
| docs -> operator decisions | README and architecture guidance influence how developers recover from drift and failed rebuilds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | reindex operation order | mitigate | Hard-code the workflow order in `Scrypath.Reindex` and cover it with ordered-call tests. |
| T-05-11 | Tampering | live-index cutover | mitigate | Require explicit `cutover?: true` behavior and expose `cutover` plus target/live index names in the result map. |
| T-05-12 | Repudiation | operator documentation | mitigate | Document accepted-versus-visible semantics, drift sources, and recovery steps in README and ARCHITECTURE using the exact workflow vocabulary from earlier phases. |
</threat_model>

<verification>
Run `mix test test/scrypath/reindex_test.exs`, run `rg -n "backfill|reindex|cutover|drift|recovery|eventual consistency|detect" README.md ARCHITECTURE.md`, and manually review `README.md` plus `ARCHITECTURE.md` for accepted-versus-visible semantics, drift detection guidance, cutover behavior, drift causes, and recovery guidance.
</verification>

<success_criteria>
Phase 5 finishes with one explicit managed reindex workflow plus docs that make rebuilds, cutovers, and recovery understandable enough for real operators to trust.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reindexing-and-operational-workflows/05-04-SUMMARY.md`
</output>
