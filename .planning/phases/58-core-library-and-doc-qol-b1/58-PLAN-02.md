---
phase: 58
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/query.ex
  - lib/scrypath.ex
  - lib/scrypath/options.ex
  - .planning/EVID-01-b1-v1.14.md
  - CHANGELOG.md
autonomous: true
requirements:
  - LIB-02
---

# Plan 02 — LIB-02: Non-macro clarity (Query + entry discoverability)

<objective>
Deliver **LIB-02** per **58-CONTEXT.md** **D-08–D-12**: improve **`Scrypath.Query`** and top-level discoverability **without** new public macros—honest **`@moduledoc`** on internal query shape, richer **`@typedoc`** on **`t`** / nested types (**`page_t`**, etc.); tighten **`NimbleOptions`** nested **`:doc`** where it reduces confusion; extend **`Scrypath`** `@moduledoc` with a **grouped map of entry points** (sync, search, multi-search) linking canonical guides. After merge, append a **short before/after** note for **EVID-57-02** per ledger append-only rules (**D-12**). PR: **`Evidence: EVID-57-02`**, **`Implements: LIB-02`**.
</objective>

<threat_model>
| ID | Threat | Severity | Control |
|----|--------|----------|---------|
| TM-58-03 | `@opaque` blocks legitimate adapter code | Low | Explicitly **no** `@opaque` on **`Scrypath.Query`** this phase (**D-09**) |

**ASVS-style scope:** documentation and typespec clarity only unless a pure private helper is required.
</threat_model>

<tasks>

<task id="58-02-01" type="execute">
  <read_first>
    - `lib/scrypath/query.ex`
    - `.planning/phases/58-core-library-and-doc-qol-b1/58-CONTEXT.md`
  </read_first>
  <action>
    Update **`lib/scrypath/query.ex`**: expand **`@moduledoc`** to state the struct is **internal normalized query state**, **not** a semver-stable pattern-match target for adopters. Add or expand **`@typedoc`** on **`@type t`** and on nested types (**pagination**, **filters**, etc.—use the actual type names in the file). Do **not** add **`@opaque`**. Add **`@spec`** on public functions in this module if any are missing and types are known.
  </action>
  <acceptance_criteria>
    - `rg -n 'internal' lib/scrypath/query.ex` exits **0** (word **internal** appears in **`@moduledoc`** body).
    - `rg -n '@typedoc' lib/scrypath/query.ex` prints **≥2** lines.
    - `rg -n '@opaque' lib/scrypath/query.ex` exits **1** (no opaque).
  </acceptance_criteria>
</task>

<task id="58-02-02" type="execute">
  <read_first>
    - `lib/scrypath.ex`
    - `guides/golden-path.md`
  </read_first>
  <action>
    In **`lib/scrypath.ex`** **`@moduledoc`**, after **`## Read next`**, add a section **`## Entry points`** (exact heading) listing **bulleted** lines that each contain **one** of the function names **`sync_record`**, **`search`**, **`search_many`** (literal substrings) and link to the guide file **`guides/sync-modes-and-visibility.md`** or **`guides/golden-path.md`** using relative markdown paths.
  </action>
  <acceptance_criteria>
    - `rg -n '## Entry points' lib/scrypath.ex` exits **0**.
    - `rg -n 'sync_record' lib/scrypath.ex` exits **0** (inside **`@moduledoc`** block — verify by reading context: line occurs before first **`defmacro`** or **`def `** after moduledoc if ambiguous, else accept any **`sync_record`** in file).
    - `rg -n 'guides/golden-path.md' lib/scrypath.ex` exits **0**.
  </acceptance_criteria>
</task>

<task id="58-02-03" type="execute">
  <read_first>
    - `lib/scrypath/options.ex`
  </read_first>
  <action>
    Improve **at least three** **`NimbleOptions`** field definitions (existing keyword entries in **`@schema_options`** / **`@runtime_options`**) by adding or sharpening **`doc:`** string values where dogfood showed confusion—**no** API shape change to public functions.
  </action>
  <acceptance_criteria>
    - `rg -n 'sync_mode:' lib/scrypath/options.ex` exits **0** and the **`doc:`** string on the **`sync_mode`** entry contains the **verbatim** substring **`visibility`** (ASCII lowercase).
    - `rg -n 'inline_timeout:' lib/scrypath/options.ex` exits **0** and that entry’s **`doc:`** string contains **`millisecond`**.
    - `rg -n 'inline_poll_interval:' lib/scrypath/options.ex` exits **0** and that entry’s **`doc:`** string contains **`poll`**.
    - `mix test test/scrypath/` exits **0**.
  </acceptance_criteria>
</task>

<task id="58-02-04" type="execute">
  <read_first>
    - `.planning/EVID-01-b1-v1.14.md`
    - `.planning/phases/57-evidence-triage-and-b1-scope-lock/57-CONTEXT.md`
  </read_first>
  <action>
    Append to **`.planning/EVID-01-b1-v1.14.md`** (end of file or designated errata section) **one** new paragraph starting with **`Errata (LIB-02):`** containing **before:** and **after:** substrings (literal ASCII) describing which surface was clarified (**Query** moduledoc, **`Scrypath`** entry map, or **options** docs).
  </action>
  <acceptance_criteria>
    - `rg -n 'Errata \(LIB-02\):' .planning/EVID-01-b1-v1.14.md` exits **0**.
    - `rg -n 'before:' .planning/EVID-01-b1-v1.14.md` exits **0**.
    - `rg -n 'after:' .planning/EVID-01-b1-v1.14.md` exits **0**.
  </acceptance_criteria>
</task>

<task id="58-02-05" type="execute">
  <read_first>
    - `CHANGELOG.md`
  </read_first>
  <action>
    Add **`CHANGELOG.md`** bullet prefixed **`LIB-02:`** describing documentation/typespec clarity (no macro DSL).
  </action>
  <acceptance_criteria>
    - `rg -n 'LIB-02' CHANGELOG.md` exits **0**.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `mix format --check-formatted`.
2. `mix test`.
3. All plan **`<acceptance_criteria>`** **`rg`** / **`mix test`** commands exit **0**.
</verification>

<must_haves>
- **`Scrypath.Query`** documents internal vs stable boundary (**D-09**)
- **`## Entry points`** in **`Scrypath`** `@moduledoc`
- Ledger errata for **EVID-57-02** (**D-12**)
</must_haves>

<success_criteria>
- **LIB-02** met with evidence note and no new public macros
</success_criteria>

---

## PLANNING COMPLETE
