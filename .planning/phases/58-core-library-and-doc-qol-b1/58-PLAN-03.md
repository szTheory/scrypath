---
phase: 58
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scrypath/docs_contract_test.exs
  - README.md
  - CONTRIBUTING.md
  - lib/mix/tasks/verify.opsui.ex
  - CHANGELOG.md
autonomous: true
requirements:
  - LIB-03
---

# Plan 03 — LIB-03: Doc-contract + verify matrix anchors

<objective>
Close **D-13–D-17** from **58-CONTEXT.md**: reconcile **`mix.exs` `extras:`** with **`test/scrypath/docs_contract_test.exs`** so **`guides/overview.md`** is either in **`@guide_paths`** (and thus hygiene) **or** explicitly excluded with rationale in the test module; add **spine-level** contract assertions for any **new stable strings** introduced by **LIB-01** (e.g. hop paths, `:accepted` wording in README if mirrored); keep **Phase 59+** playbook UI strings **out** of this contract (**D-16**). Update **README** / **CONTRIBUTING** / **`verify.opsui`** triad if new contributor tokens appear (**D-15**). PR: **`Evidence: EVID-57-01`** (shared row allowed) **and** **`Implements: LIB-03`** per **D-18**.
</objective>

<threat_model>
| ID | Threat | Severity | Control |
|----|--------|----------|---------|
| TM-58-04 | Contract test locks prose churn | Low | Use **spine tokens** only per **D-13**—paths, env keys, arities, **one** invariant sentence max |

**ASVS-style scope:** test and markdown consistency; no runtime auth changes.
</threat_model>

<tasks>

<task id="58-03-01" type="execute">
  <read_first>
    - `test/scrypath/docs_contract_test.exs`
    - `mix.exs`
  </read_first>
  <action>
    Insert **`"guides/overview.md"`** into **`@guide_paths`** in **`test/scrypath/docs_contract_test.exs`** **immediately before** **`"guides/per-query-tuning-pipeline.md"`** (lexically **overview** sorts before **per-query**).
  </action>
  <acceptance_criteria>
    - `rg -n '"guides/overview.md"' test/scrypath/docs_contract_test.exs` exits **0**.
    - `mix test test/scrypath/docs_contract_test.exs` exits **0**.
  </acceptance_criteria>
</task>

<task id="58-03-02" type="execute">
  <read_first>
    - `README.md`
    - `test/scrypath/docs_contract_test.exs`
    - `.planning/phases/58-core-library-and-doc-qol-b1/58-CONTEXT.md`
  </read_first>
  <action>
    Add **one** **`test`** block (or extend an existing README contract test) that **`assert_contains_all`** on **`@readme`** for **exactly these** strings if **LIB-01** merged them into README (skip strings not present yet—**executor**: if string missing, **first** add the sentence to **`README.md`** under an existing sync/visibility section, **then** assert): **`guides/sync-modes-and-visibility.md`**, **`Accepted work is not the same thing as search visibility`** (substring already required elsewhere—if duplicate assertion is redundant, instead assert **`:status` `:accepted`** appears in README sync section). **Minimum:** **`README.md`** contains the **verbatim** path **`guides/sync-modes-and-visibility.md`** after this task.
  </action>
  <acceptance_criteria>
    - `rg -n 'guides/sync-modes-and-visibility.md' README.md` exits **0**.
    - `mix test test/scrypath/docs_contract_test.exs` exits **0**.
  </acceptance_criteria>
</task>

<task id="58-03-03" type="execute">
  <read_first>
    - `CONTRIBUTING.md`
    - `lib/mix/tasks/verify.opsui.ex`
  </read_first>
  <action>
    If **`README.md`** gained a new **mix verify** or **doc hop** token in **58-03-02**, mirror it in **`CONTRIBUTING.md`** (verify section) **and** in **`lib/mix/tasks/verify.opsui.ex`** moduledoc bullet list **if** the token is a **mix task** name. If **no** new task: add **one** cross-reference line in **`CONTRIBUTING.md`** pointing to **`test/scrypath/docs_contract_test.exs`** for **published markdown hygiene** (verbatim filename substring).
  </action>
  <acceptance_criteria>
    - `rg -n 'docs_contract_test.exs' CONTRIBUTING.md` exits **0**.
  </acceptance_criteria>
</task>

<task id="58-03-04" type="execute">
  <read_first>
    - `CHANGELOG.md`
  </read_first>
  <action>
    Append **`CHANGELOG.md`** bullet prefixed **`LIB-03:`** describing doc-contract / verify matrix update.
  </action>
  <acceptance_criteria>
    - `rg -n 'LIB-03' CHANGELOG.md` exits **0**.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `mix format --check-formatted`.
2. `mix test`.
3. `mix test test/scrypath/docs_contract_test.exs`.
</verification>

<must_haves>
- **`guides/overview.md`** in **`@guide_paths`** OR documented exclusion (**D-14** — this plan chooses **inclusion**)
- README + CONTRIBUTING + contract test alignment (**D-15** minimum: CONTRIBUTING cites **`docs_contract_test.exs`**)
</must_haves>

<success_criteria>
- **`docs_contract_test`** extended per **LIB-03** without locking **scrypath_ops** UI prose (**D-16**)
</success_criteria>

---

## PLANNING COMPLETE
