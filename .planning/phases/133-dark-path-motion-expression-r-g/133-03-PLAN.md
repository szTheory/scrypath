---
phase: 133-dark-path-motion-expression-r-g
plan: 03
type: execute
wave: 2
depends_on:
  - 133-01-dark-path-motion-expression-r-g
files_modified:
  - examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts
autonomous: true
requirements:
  - DARKMOTION-01
must_haves:
  truths:
    - "Each shipped DARKMOTION-01 anchor is exercised in BOTH dark and light: hover code-shimmer, active-path trace/node glow, recommended intent card"
    - "Under prefers-reduced-motion: reduce, each new anchor computes ~0.01ms transition/animation duration AND its active state is still visually present (functional integrity)"
    - "A LiveView patch/re-run (Search single↔multi mode switch; Playbook A→B selection) does NOT re-fire/flicker the path motion (no running-animation count increment, no opacity flash)"
    - "A small targeted screenshot set captures the affected surfaces at rest/interaction endpoints"
  artifacts:
    - path: "examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts"
      provides: "Focused Playwright proof: reduced-motion + hover-shimmer + active-path + patch-refire, dark AND light, plus targeted screenshots"
      contains: "reducedMotion"
  key_links:
    - from: "examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts"
      to: "examples/scrypath_ecommerce/e2e/helpers/e2e.ts"
      via: "imports waitForLiveConnected + seedScenario for live-ready/seed gates"
      pattern: "waitForLiveConnected|seedScenario"
---

<objective>
Ship the locked D-05/D-05a focused Playwright proof for DARKMOTION-01: exercise every shipped path-
motion anchor in dark AND light, prove reduced-motion neutralization, prove NO LiveView patch-refire
flicker (the Phase 123 A3 risk), and capture a small targeted screenshot set. This is the second
Wave-0 net-new gap (no reduced-motion / patch-refire helper exists in the harness).

Purpose: Browser-prove the transient motion + the patch-safety that static asserts cannot cover.
Output: `examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts` (+ its screenshot artifacts).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-dark-path-motion-expression-r-g/133-RESEARCH.md
@.planning/phases/133-dark-path-motion-expression-r-g/133-PATTERNS.md
@.planning/phases/133-dark-path-motion-expression-r-g/133-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin_path_motion.spec.ts — reduced-motion + hover-shimmer + active-path + patch-refire, dark AND light, with targeted screenshots</name>
  <files>examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts</files>
  <read_first>
    - examples/scrypath_ecommerce/e2e/admin_screenshot_matrix.spec.ts:19-29 (imports + helper reuse) and :59-67 (theme-before-first-paint via addInitScript phx:theme; system-dark = newContext colorScheme:'dark' with NO phx:theme write)
    - examples/scrypath_ecommerce/e2e/helpers/e2e.ts:11 (waitForLiveConnected), :99 (seedScenario), :40 (SeedScenario type), :118 (drainSearchQueue)
    - examples/scrypath_ecommerce/playwright.config.ts:9 (baseURL = PLAYWRIGHT_BASE_URL || http://127.0.0.1:4002; NO webServer — server booted manually)
    - .planning/phases/133-dark-path-motion-expression-r-g/133-PATTERNS.md ("admin_path_motion.spec.ts" section: NEW inline helpers + coverage list)
    - .planning/phases/133-dark-path-motion-expression-r-g/133-RESEARCH.md ("Proving no patch-refire concretely" + "Proving reduced-motion neutralization")
  </read_first>
  <action>
    Create `admin_path_motion.spec.ts` reusing the harness patterns. Import
    `waitForLiveConnected`, `seedScenario`, `drainSearchQueue` from `./helpers/e2e`. Set theme before
    first paint via `context.addInitScript([... localStorage.setItem("phx:theme", theme)])` for
    `light`/`dark`; cover system-dark via `browser.newContext({ colorScheme: 'dark' })` with NO phx:theme
    write. Gate every flow on `waitForLiveConnected(page)` and seed with `seedScenario(request, "all_green")`.

    Add the two NEW inline helpers (no existing analog):
    - Reduced-motion: build the context/page with `reducedMotion: 'reduce'` (or
      `page.emulateMedia({ reducedMotion: 'reduce' })`), then for each shipped anchor assert
      `getComputedStyle(...).transitionDuration`/`animationDuration` ≈ `0.01ms` (use a `toHaveCSS`/
      `evaluate` probe) AND assert the active state is still visually present (the mark/glow element is
      visible) — functional integrity under reduced motion (D-09).
    - Patch-refire probe: before triggering a `push_patch`, capture a running-animation baseline on the
      anchor via `page.evaluate(() => el.getAnimations().length)` (or an `animationstart` counter); trigger
      the patch (toggle Search single↔multi mode; select Playbook A then B); assert the running-animation
      count does NOT increment and no opacity flash on the merge-trace region. If the path motion is pure
      `transition` (recommended), `getAnimations()` returns 0 before AND after — that zero IS the proof.

    Coverage (D-05/D-05a) — exercise in BOTH dark and light:
    - Control Room: recommended intent card carries the path glow (seed a posture that flags recommended).
    - Search: hover the merge-trace `.ops-path-trace` affordance → line-draw visible; run a bounded search
      (`all_green` + a known term, e.g. "quantum"), then toggle single↔multi mode → patch-refire probe.
    - Playbooks: select playbook A then B → active marker moves via `.ops-object-item-active` with no
      replayed reveal (patch-refire probe).
    - Code shimmer: hover a NON-evidence code block with `shimmer={true}` → glint visible; assert an
      evidence code block (search payload / failed-sync) has NO `.ops-code-block--shimmer` class.

    Targeted screenshots (D-05, small set — NOT the 40-shot recapture deferred to Phase 136): capture
    rest/interaction endpoints only for the affected surfaces (recommended card, merge-trace hover,
    active playbook item) in dark + light via `page.screenshot`/`toHaveScreenshot` into the spec's
    artifact dir. Keep the set minimal.

    Command shape (manual server per config — no webServer): document in a top-of-file comment that the
    spec runs against a booted seeded ops server (`:4002` or `PLAYWRIGHT_BASE_URL`), using the compose
    dev lane so it hits CURRENT source, not the stale baked image (per project Docker DX note).
  </action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && npx playwright test e2e/admin_path_motion.spec.ts --reporter=line 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test e2e/admin_path_motion.spec.ts` passes against a booted seeded ops server (dark + light + system-dark contexts)
    - The spec imports `waitForLiveConnected` and `seedScenario` from `./helpers/e2e` and gates every flow on live-connected + seeded state
    - Reduced-motion assertion: each shipped anchor computes `transition-duration`/`animation-duration` ≈ `0.01ms` AND its active state remains visible
    - Patch-refire assertion: Search single↔multi toggle and Playbook A→B selection do NOT increment the anchor's running-animation count and produce no opacity flash on the merge-trace region
    - Hover code-shimmer renders only on a `shimmer={true}` block; an evidence code block is asserted to NOT carry `.ops-code-block--shimmer`
    - A small targeted screenshot set (recommended card, merge-trace hover, active playbook item; dark + light) is produced — NOT a 40-shot recapture
  </acceptance_criteria>
  <done>`admin_path_motion.spec.ts` green: reduced-motion neutralization, hover-shimmer, active-path trace/node glow, and no-patch-refire all proven in dark AND light, with a small targeted screenshot set; evidence code blocks confirmed shimmer-off.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

| Symbol | Kind | File | Notes |
|--------|------|------|-------|
| `admin_path_motion.spec.ts` | Playwright e2e spec | examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts | reduced-motion + hover-shimmer + active-path + patch-refire, dark/light/system-dark |
| inline `reducedMotion` probe | test helper (inline) | (same spec) | no existing analog; asserts ~0.01ms + active-state-present |
| inline patch-refire probe | test helper (inline) | (same spec) | `getAnimations()` count baseline across a push_patch |
| targeted screenshot set | test artifacts | (spec artifact dir) | small rest/interaction set; NOT the 40-shot recapture (deferred to Phase 136) |

Conditional (D-05b): wire `admin_contrast_matrix.spec.ts` / `light-pixel-diff.mjs` ONLY if Plan 01 changed color-bearing glow/shimmer end states. The contrast/light gate is NOT the primary motion gate — skip unless end-state colors changed.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A read-only e2e spec exercising the existing seeded ops UI over the existing `/dev/e2e/seed` test endpoint. No new network/data/auth surface; no production code changes. |

## STRIDE Threat Register

No `high`+ threats. Reasoning: this plan adds a Playwright spec that drives the already-existing seeded
admin UI and asserts visual/motion properties. It introduces no new route, input handler, or data flow;
`seedScenario` uses the pre-existing dev-only `/dev/e2e/seed` endpoint (SEED-01, already threat-modeled).
ASVS L1 not materially engaged. Explicit non-applicability per `security_enforcement`.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-133-03 | Tampering | patch-refire regression (A3) | mitigate | the patch-refire probe IS the mitigation — fails if a mount-keyframe reveal re-fires on LiveView patch |
| T-133-SC | Tampering | npm/pip/cargo installs | accept | no external packages installed (reuses existing `@playwright/test` harness) |
</threat_model>

<verification>
- `cd examples/scrypath_ecommerce && npx playwright test e2e/admin_path_motion.spec.ts` green against a booted seeded ops server (compose dev lane → current source).
- Reduced-motion + patch-refire + hover-shimmer + active-path all asserted, dark AND light.
- Targeted screenshots reviewed before `/gsd-verify-work` (full recapture/gallery deferred to Phase 136).
</verification>

<success_criteria>
- Focused Playwright proof exercises each shipped anchor in dark AND light.
- Reduced-motion neutralization and functional integrity confirmed.
- No LiveView patch-refire flicker (A3 regression caught automatically).
- Small targeted screenshot set captured; evidence code blocks shimmer-off.
</success_criteria>

<output>
Create `.planning/phases/133-dark-path-motion-expression-r-g/133-03-SUMMARY.md` when done
</output>