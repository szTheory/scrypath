---
phase: 59
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/docs/playbook-schema-v1.md
  - scrypath_ops/docs/operator-ia.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - OPS-PB-01
  - OPS-PB-03
---

# Plan 02 — Normative playbook spec, IA link, persistence record

<objective>
Ship human-readable **playbook_format v1** documentation (**59-CONTEXT D-16**), link it from **`scrypath_ops/docs/operator-ia.md`**, and record the **OPS-PB-03** persistence choice (**portable JSON export/import**, **no** team DB in v1.14) plus **security limits** (**no secrets in exports**, path hygiene pointer) in **`.planning/REQUIREMENTS.md`** adjacent to **OPS-PB-03** (**roadmap success criterion 2**).
</objective>

<threat_model>
| ID | Threat | Severity | Control |
|----|--------|----------|---------|
| TM-59-04 | Operators store API keys in playbook JSON | High | Doc section **“Banned fields”** lists **`meilisearch_api_key`**, **`req_options`**, etc., matching codec |
| TM-59-05 | Wrong doc location implies Hex-level support | Low | Path stays under **`scrypath_ops/docs/`** per **D-18** |

**ASVS-style scope:** documentation and traceability only — no new runtime surface.
</threat_model>

<tasks>

<task id="59-02-01" type="execute">
  <read_first>
    - `.planning/phases/59-playbook-schema-and-persistence-mvp/59-CONTEXT.md`
    - `.planning/phases/59-playbook-schema-and-persistence-mvp/59-RESEARCH.md`
    - `scrypath_ops/lib/scrypath_ops/playbook/v1.ex`
  </read_first>
  <action>
    Create **`scrypath_ops/docs/playbook-schema-v1.md`** with sections: **Versioning** (**`playbook_format` integer**), **Modes** (**`search` / `search_many`** with field tables mirroring **59-CONTEXT D-08**), **Caps** (cite **`SearchPlayground.max_page_size_allowed/0`** and **`max_schemas_allowed/0`** numerically **1..50** and **≤10** defaults), **Federation notes** (**D-13**, **D-15** stub honesty), **Banned / secret keys** (**D-04** verbatim examples), **Persistence** paragraph stating **JSON files on disk**, **UTF-8**, **no durable OPSUI DB** in v1.14, and **“no secrets in artifacts.”** Include **≥1** complete minimal JSON example for **`search`** and **≥1** for **`search_many`** consistent with **`ScrypathOps.Playbook.V1`** tests.
  </action>
  <acceptance_criteria>
    - `test -f scrypath_ops/docs/playbook-schema-v1.md` exits **0**.
    - `rg -n 'playbook_format' scrypath_ops/docs/playbook-schema-v1.md` exits **0**.
    - `rg -n 'meilisearch_api_key' scrypath_ops/docs/playbook-schema-v1.md` exits **0**.
    - `rg -n 'search_many' scrypath_ops/docs/playbook-schema-v1.md` exits **0**.
  </acceptance_criteria>
</task>

<task id="59-02-02" type="execute">
  <read_first>
    - `scrypath_ops/docs/operator-ia.md`
    - `scrypath_ops/docs/playbook-schema-v1.md`
  </read_first>
  <action>
    In **`scrypath_ops/docs/operator-ia.md`**, add a short subsection under **Jobs-to-be-done** or **Navigation** (planner picks best fit) with heading text **`Playbook`** or **`Saved playbooks`** containing a **markdown link** to **`playbook-schema-v1.md`** using a **relative** path from **`scrypath_ops/docs/`** (e.g. **`[playbook-schema-v1.md](playbook-schema-v1.md)`**). One sentence must state that **v1** interchange is **JSON** and **ops-local**.
  </action>
  <acceptance_criteria>
    - `rg -n 'playbook-schema-v1.md' scrypath_ops/docs/operator-ia.md` exits **0**.
  </acceptance_criteria>
</task>

<task id="59-02-03" type="execute">
  <read_first>
    - `.planning/REQUIREMENTS.md`
    - `.planning/phases/59-playbook-schema-and-persistence-mvp/59-CONTEXT.md`
  </read_first>
  <action>
    Under the **OPS-PB-03** bullet in **`.planning/REQUIREMENTS.md`**, append an **implementation note** (single blockquote or sub-bullet) that states explicitly: **Phase 59 chose portable JSON file export/import**; **no** new **Ecto** persistence in **`scrypath_ops`** for this milestone; **limitations**: single-operator / **git+attachments** team bus, not a live shared catalog; **security**: **no secrets** in exported JSON — reference **`scrypath_ops/docs/playbook-schema-v1.md`**. Do **not** remove the normative **OPS-PB-03** checkbox text — **extend** it.
  </action>
  <acceptance_criteria>
    - `rg -n 'Phase 59' .planning/REQUIREMENTS.md` exits **0**.
    - `rg -n 'playbook-schema-v1' .planning/REQUIREMENTS.md` exits **0**.
    - `rg -n 'OPS-PB-03' .planning/REQUIREMENTS.md` exits **0**.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `mix format --check-formatted` if any `*.ex` was touched (expected **no** for this plan); otherwise skip.
2. `cd scrypath_ops && mix test test/scrypath_ops/playbook/v1_test.exs` — must remain green (regression guard).
3. All **`<acceptance_criteria>`** checks exit **0**.
</verification>

<must_haves>
- **`playbook-schema-v1.md`** exists and matches locked decisions
- **IA doc** links to the spec
- **REQUIREMENTS** records persistence choice + security pointer
</must_haves>

<success_criteria>
- Roadmap **(1)** payload schema v1 documented in **`scrypath_ops`**
- Roadmap **(2)** persistence choice recorded in **REQUIREMENTS** with security notes
</success_criteria>

---

## PLANNING COMPLETE
