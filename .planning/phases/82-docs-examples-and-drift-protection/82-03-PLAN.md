---
phase: 82-docs-examples-and-drift-protection
plan: 03
type: execute
wave: 3
depends_on:
  - 82-01
  - 82-02
files_modified:
  - lib/mix/tasks/verify.phase82.ex
  - mix.exs
  - CONTRIBUTING.md
  - .github/workflows/ci.yml
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - VRFY-01
must_haves:
  truths:
    - "Phase 82 drift protection stays narrow, contract-shaped, and contributor-friendly."
    - "A dedicated `mix verify.phase82` gate proves the request-edge public story without broadening the default fast test path."
    - "Docs contracts fail on helper/runtime boundary drift and example/CI parity drift."
  artifacts:
    - path: "lib/mix/tasks/verify.phase82.ex"
      provides: "Focused phase verify alias for docs, fixtures, and request-edge drift"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Narrow v1.21 public-spine assertions"
    - path: ".github/workflows/ci.yml"
      provides: "Quality job wiring for the new verify gate"
    - path: "CONTRIBUTING.md"
      provides: "Contributor-facing documentation for the new verify path"
  key_links:
    - from: "lib/mix/tasks/verify.phase82.ex"
      to: "test/scrypath/docs_contract_test.exs"
      via: "phase verify task runs focused request-edge docs contracts"
      pattern: "docs_contract_test.exs"
    - from: "lib/mix/tasks/verify.phase82.ex"
      to: ".github/workflows/ci.yml"
      via: "quality job invokes the same phase verify alias contributors run locally"
      pattern: "verify.phase82"
    - from: "CONTRIBUTING.md"
      to: "lib/mix/tasks/verify.phase82.ex"
      via: "maintainer docs describe when and how to run the new focused gate"
      pattern: "mix verify.phase82"
---

<objective>
Add a narrow phase-specific drift gate for the v1.21 public story, then wire it into contributor docs and CI without broadening the default fast test path.

Purpose: make public-story drift fail deterministically while keeping the verification posture specific, maintainable, and aligned with existing phase-verify conventions.
Output: `mix verify.phase82`, targeted docs-contract additions, and CI / CONTRIBUTING wiring.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/82-docs-examples-and-drift-protection/82-CONTEXT.md
@.planning/phases/82-docs-examples-and-drift-protection/82-RESEARCH.md
@.planning/phases/82-docs-examples-and-drift-protection/82-VALIDATION.md
@.planning/phases/82-docs-examples-and-drift-protection/82-01-PLAN.md
@.planning/phases/82-docs-examples-and-drift-protection/82-02-PLAN.md
@CONTRIBUTING.md
@.github/workflows/ci.yml
@mix.exs
@lib/mix/tasks/verify.phase14.ex
@lib/mix/tasks/verify.phase41.ex
@lib/mix/tasks/verify.phase43.ex
@test/scrypath/docs_contract_test.exs
@test/scrypath/query_params_test.exs
@test/scrypath/phoenix_test.exs
@test/support/docs/phoenix_examples_test.exs
@test/support/docs/phoenix_request_shape_smoke_test.exs

<interfaces>
Phase 82 verification must fail on:
- helper APIs drifting into a second runtime
- public docs implying Phoenix is required or `%Scrypath.Query{}` is public
- example README / CI / local smoke command drift
- request-edge guide discoverability and root-doc drift

Phase 82 verification must not:
- re-enable the entire broad optional docs-contract suite in the default `mix test` fast path
- freeze ordinary editorial prose
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a focused `mix verify.phase82` gate following existing phase-verify conventions</name>
  <files>lib/mix/tasks/verify.phase82.ex, mix.exs</files>
  <read_first>
    - lib/mix/tasks/verify.phase14.ex
    - lib/mix/tasks/verify.phase41.ex
    - lib/mix/tasks/verify.phase43.ex
    - mix.exs
    - .planning/phases/82-docs-examples-and-drift-protection/82-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `mix verify.phase82` accepts no args and fails on unexpected arguments like other phase verify tasks.
    - Test 2: it runs the focused docs/request-edge suite only, not the broad default test path.
    - Test 3: it includes a strict docs build step with `--warnings-as-errors`.
  </behavior>
  <action>Create `lib/mix/tasks/verify.phase82.ex` using the same task style as `verify.phase41` and `verify.phase43`: no arguments, `Mix.Task.run(\"app.start\")`, focused `Mix.Task.run(\"test\", [...])`, and a strict docs build using `Mix.Task.run(\"docs\", [\"--warnings-as-errors\"])`. Update `mix.exs` `preferred_envs` so `verify.phase82` runs in `:test`. The focused test list must cover `test/scrypath/query_params_test.exs`, `test/scrypath/phoenix_test.exs`, `test/support/docs/phoenix_examples_test.exs`, `test/support/docs/phoenix_request_shape_smoke_test.exs`, and `test/scrypath/docs_contract_test.exs`.</action>
  <acceptance_criteria>
    - `lib/mix/tasks/verify.phase82.ex` contains `defmodule Mix.Tasks.Verify.Phase82`
    - `lib/mix/tasks/verify.phase82.ex` contains `Mix.Task.run("docs", ["--warnings-as-errors"])`
    - `lib/mix/tasks/verify.phase82.ex` contains all five focused test file paths
    - `mix.exs` contains `"verify.phase82": :test`
  </acceptance_criteria>
  <verify>
    <automated>mix verify.phase82</automated>
  </verify>
  <done>The repo has one contributor-friendly phase verify task that exercises the narrow public-story drift surface end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add narrow v1.21 docs-contract assertions and wire the gate into CI/docs</name>
  <files>test/scrypath/docs_contract_test.exs, .github/workflows/ci.yml, CONTRIBUTING.md</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs
    - .github/workflows/ci.yml
    - CONTRIBUTING.md
    - guides/request-edge-search.md
    - README.md
    - lib/scrypath.ex
    - examples/phoenix_meilisearch/README.md
  </read_first>
  <behavior>
    - Test 1: docs contracts assert discoverability of the canonical request-edge guide and the helper-only/core-first public boundary.
    - Test 2: docs contracts assert example README / CI / local smoke parity on job name, env vars, and command order.
    - Test 3: CI `quality` job and CONTRIBUTING both reference `mix verify.phase82`.
  </behavior>
  <action>Add focused assertions to `test/scrypath/docs_contract_test.exs` for the v1.21 public spine only: root docs and root moduledoc route to the canonical request-edge guide, `Scrypath.Phoenix` is optional and does not execute search, contexts remain canonical, `%Scrypath.Query{}` is not public API, and the example README / CI / local smoke instructions remain aligned. Update `.github/workflows/ci.yml` `quality` job to run `mix verify.phase82`. Update `CONTRIBUTING.md` so contributors know when to run `mix verify.phase82` and how it differs from the fast default `mix test` path. Keep the assertions contract-shaped; do not snapshot ordinary prose.</action>
  <acceptance_criteria>
    - `test/scrypath/docs_contract_test.exs` contains `request-edge-search`
    - `test/scrypath/docs_contract_test.exs` contains `It does not execute search.` or an equivalent helper-only assertion target
    - `.github/workflows/ci.yml` contains `mix verify.phase82`
    - `CONTRIBUTING.md` contains `mix verify.phase82`
    - `.github/workflows/ci.yml` still contains `mix test --exclude integration --exclude docs_contract` or the existing fast-test exclusion posture
  </acceptance_criteria>
  <verify>
    <automated>mix verify.phase82</automated>
  </verify>
  <done>The docs contracts and automation now fail on v1.21 public-story drift without broadening the default fast path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs -> contributors | broad or vague docs assertions can create maintainer-hostile friction |
| verify task -> CI | a local verify alias that differs from CI is not a trustworthy drift gate |
| docs contracts -> public API story | the assertions must target public promises, not internal prose or implementation trivia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-07 | R | `test/scrypath/docs_contract_test.exs` | mitigate | Add only narrow public-spine assertions so maintainers can understand exactly what contract failed |
| T-82-08 | D | `mix verify.phase82` | mitigate | Keep the gate focused and argument-free so contributors can run it cheaply and CI can mirror it exactly |
| T-82-09 | T | `.github/workflows/ci.yml` and `CONTRIBUTING.md` | mitigate | Wire the same verify alias into docs and automation so local and CI verification stay aligned |
</threat_model>

<verification>
- `mix verify.phase82`
- Confirm `.github/workflows/ci.yml` and `CONTRIBUTING.md` both reference the new verify alias
- Confirm the fast default CI test path is not broadened to re-enable the full optional docs-contract suite
</verification>

<success_criteria>
- The repo has a dedicated `mix verify.phase82` gate for request-edge docs/examples drift.
- Docs contracts fail on the v1.21 public spine and example/CI parity, not on ordinary editorial changes.
- CI and contributor docs both point to the same focused verify path.
</success_criteria>

<output>
After completion, create `.planning/phases/82-docs-examples-and-drift-protection/82-03-SUMMARY.md`
</output>
