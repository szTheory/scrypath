---
phase: 82-docs-examples-and-drift-protection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/request-edge-search.md
  - guides/overview.md
  - guides/getting-started.md
  - guides/golden-path.md
  - README.md
  - lib/scrypath.ex
  - mix.exs
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "The v1.21 request-edge story has one canonical guide instead of being duplicated across root docs and Phoenix guides."
    - "`Scrypath.QueryParams` is presented as the framework-light edge contract, `Scrypath.Phoenix` as optional glue, and `Scrypath.search/3` as the canonical runtime."
    - "README and the root `Scrypath` moduledoc become wayfinding surfaces, not the canonical explanation."
  artifacts:
    - path: "guides/request-edge-search.md"
      provides: "Canonical v1.21 request-edge guide"
    - path: "README.md"
      provides: "Root wayfinding into the canonical guide"
    - path: "lib/scrypath.ex"
      provides: "ExDoc lobby wording aligned with the shipped Phase 81 surface"
    - path: "mix.exs"
      provides: "ExDoc extras/grouping entry for the new canonical guide"
  key_links:
    - from: "README.md"
      to: "guides/request-edge-search.md"
      via: "root wayfinding points to the canonical request-edge guide"
      pattern: "request-edge"
    - from: "lib/scrypath.ex"
      to: "guides/request-edge-search.md"
      via: "moduledoc read-next path references the canonical guide"
      pattern: "guides/request-edge-search.md"
    - from: "mix.exs"
      to: "guides/request-edge-search.md"
      via: "ExDoc extras and group list include the new guide"
      pattern: "guides/request-edge-search.md"
---

<objective>
Publish one canonical request-edge guide for the v1.21 story, then rewire root docs and ExDoc wayfinding around it without broadening the public product surface.

Purpose: make the shipped `QueryParams` / optional Phoenix helper boundary obvious to adopters while keeping contexts and `Scrypath.search/3` canonical.
Output: one new guide, root wayfinding updates, and ExDoc extras/grouping wired to the guide.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/82-docs-examples-and-drift-protection/82-CONTEXT.md
@.planning/phases/82-docs-examples-and-drift-protection/82-RESEARCH.md
@.planning/phases/82-docs-examples-and-drift-protection/82-PATTERNS.md
@.planning/phases/82-docs-examples-and-drift-protection/82-VALIDATION.md
@README.md
@lib/scrypath.ex
@mix.exs
@guides/overview.md
@guides/getting-started.md
@guides/golden-path.md
@guides/phoenix-contexts.md
@guides/phoenix-controllers-and-json.md
@guides/phoenix-liveview.md

<interfaces>
Locked Phase 82 decisions to preserve:
- Add one short canonical request-edge guide and point other docs into it.
- Keep the guide narrow: params enter at the web edge, `Scrypath.QueryParams` normalizes, `Scrypath.Phoenix` is optional glue, `QueryParams.to_search_args/1` feeds app contexts, `Scrypath.search/3` remains canonical.
- Root docs and start-here flows should present the core toolkit and context boundary first, with Phoenix wrappers clearly second and optional.
- Do not expose `%Scrypath.Query{}` as public API.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical request-edge guide and wire it into ExDoc</name>
  <files>guides/request-edge-search.md, mix.exs, guides/overview.md</files>
  <read_first>
    - guides/overview.md
    - mix.exs
    - guides/phoenix-contexts.md
    - guides/phoenix-controllers-and-json.md
    - guides/phoenix-liveview.md
    - .planning/phases/82-docs-examples-and-drift-protection/82-CONTEXT.md
  </read_first>
  <action>Create `guides/request-edge-search.md` as the one canonical v1.21 guide. It must explain the boundary in this order: browser params enter at the request edge, `Scrypath.QueryParams` owns framework-light normalization, `Scrypath.Phoenix` is optional helper glue for params/forms/URLs, `QueryParams.to_search_args/1` feeds app contexts, and `Scrypath.search/3` remains the only runtime path. Add the new guide to `mix.exs` `docs.extras` and place it in the appropriate ExDoc group near onboarding / Phoenix discovery. Update `guides/overview.md` so the new guide appears as the obvious route for the shared request-edge mental model.</action>
  <acceptance_criteria>
    - `guides/request-edge-search.md` contains `Scrypath.QueryParams`, `Scrypath.Phoenix`, `QueryParams.to_search_args/1`, and `Scrypath.search/3`
    - `guides/request-edge-search.md` contains `%Scrypath.Query{}` and `not public API`
    - `mix.exs` contains `guides/request-edge-search.md` in both `extras:` and `groups_for_extras:`
    - `guides/overview.md` links to `request-edge-search.md`
  </acceptance_criteria>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <done>The repo has one canonical request-edge guide, and ExDoc exposes it in the published guide map.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire root wayfinding and start-here surfaces around the canonical guide</name>
  <files>README.md, lib/scrypath.ex, guides/getting-started.md, guides/golden-path.md</files>
  <read_first>
    - README.md
    - lib/scrypath.ex
    - guides/getting-started.md
    - guides/golden-path.md
    - guides/request-edge-search.md
    - .planning/phases/82-docs-examples-and-drift-protection/82-CONTEXT.md
  </read_first>
  <action>Update README, the root `Scrypath` moduledoc, `guides/getting-started.md`, and `guides/golden-path.md` so they route readers into the new canonical request-edge guide instead of repeating the whole explanation. Replace stale phase-80 wording in `lib/scrypath.ex` that says nested values must already match runtime shapes. Keep the ordering core-first: `Scrypath.QueryParams` and context-owned `Scrypath.search/3` first, optional Phoenix wrappers second. Keep README and moduledoc compact; they should summarize the contract and point to the guide, not become the guide.</action>
  <acceptance_criteria>
    - `README.md` contains `guides/request-edge-search.md`
    - `lib/scrypath.ex` contains `guides/request-edge-search.md`
    - `lib/scrypath.ex` no longer contains `top-level request envelope`
    - `lib/scrypath.ex` no longer contains `nested values must already match`
    - `guides/getting-started.md` or `guides/golden-path.md` references `guides/request-edge-search.md`
  </acceptance_criteria>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <done>Root docs and start-here flows point to one canonical request-edge story and no longer restate stale or contradictory boundary language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root docs -> public adopters | README and ExDoc lobby wording can accidentally overstate helper authority or expose stale contract language |
| canonical guide -> Phoenix docs | Shared contract details must flow through one source of truth instead of diverging across role-specific guides |
| ExDoc extras -> published docs site | Missing extras/group entries can hide the canonical guide even if the file exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | T | `lib/scrypath.ex` | mitigate | Remove stale phase-80 wording so the root moduledoc cannot contradict the shipped Phase 81 surface |
| T-82-02 | I | `README.md` and `guides/request-edge-search.md` | mitigate | Keep `%Scrypath.Query{}` mentioned only as a non-public boundary reminder |
| T-82-03 | E | root wayfinding | mitigate | Keep `Scrypath.search/3` and contexts visually canonical so helpers do not become a second runtime by implication |
</threat_model>

<verification>
- `mix docs --warnings-as-errors`
- Confirm the new canonical guide is included in `mix.exs` extras/groups and referenced from README and `lib/scrypath.ex`
- Confirm stale phase-80 wording is removed from the root moduledoc
</verification>

<success_criteria>
- The v1.21 request-edge story lives in one canonical guide.
- Root docs and ExDoc lobby surfaces route readers into that guide without duplicating it.
- Root docs keep `Scrypath.search/3` and contexts canonical, with `Scrypath.Phoenix` clearly optional.
</success_criteria>

<output>
After completion, create `.planning/phases/82-docs-examples-and-drift-protection/82-01-SUMMARY.md`
</output>
