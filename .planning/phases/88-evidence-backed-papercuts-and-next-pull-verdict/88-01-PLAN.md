---
phase: "88"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/related-data-and-reindexing.md
  - test/scrypath/docs_contract_test.exs
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FIX-01
  - FIX-02
must_haves:
  truths:
    - "Documentation explicitly mentions custom Oban jobs as a temporary workaround for related-data propagation."
    - "Automated tests verify that the temporary workaround documentation is not accidentally removed."
    - "Milestone v1.23 is officially frozen and archived."
    - "Next-pull verdict is explicitly set to 'related-data propagation'."
  artifacts:
    - path: "guides/related-data-and-reindexing.md"
      provides: "Temporary workaround instructions for related-data updates"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Docs contract test for related-data workaround"
    - path: ".planning/ROADMAP.md"
      provides: "Frozen v1.23 milestone with next-pull verdict"
    - path: ".planning/STATE.md"
      provides: "Updated phase 88 completion and verdict state"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Checked off FIX-01 and FIX-02 requirements"
  key_links:
    - from: "test/scrypath/docs_contract_test.exs"
      to: "guides/related-data-and-reindexing.md"
      via: "@guide_paths and test assertion"
      pattern: "guides/related-data-and-reindexing\\.md"
---

<objective>
Fix the evidence-backed papercut regarding related-data updates, provide regression protection, and officially freeze milestone v1.23 with the explicit verdict to open "related-data propagation" next.

Purpose: Provide a clear temporary workaround for adopters handling related-data changes, prevent this documentation from regressing, and transition the project state to explicitly declare "related-data propagation" as the next major focus.
Output: Updated documentation with custom Oban job examples, an ExUnit doc contract test, and finalized milestone files.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Oban workaround to related-data guide</name>
  <files>guides/related-data-and-reindexing.md</files>
  <action>
    Per decision D-01 and D-02:
    Update `guides/related-data-and-reindexing.md`. Under the section "2. One changed row affects many other documents", explicitly add instructions showing how to manually handle related-data updates using custom Oban jobs for child relations. 
    Ensure the documentation explicitly states that this is a "temporary workaround" until "related-data propagation" becomes a "first-class feature".
    *Pattern Analog:* Apply the "Context-based orchestration with concrete Elixir blocks" pattern seen in `guides/getting-started.md`. Ensure any added Elixir code block adheres to the "Elixir Code Block Validation" shared pattern.
  </action>
  <verify>
    <automated>grep -i "temporary workaround" guides/related-data-and-reindexing.md && grep -i "first-class feature" guides/related-data-and-reindexing.md</automated>
  </verify>
  <done>Guide contains custom Oban job instructions and the specific phrases "temporary workaround" and "first-class feature".</done>
</task>

<task type="auto">
  <name>Task 2: Add contract test for related-data workaround</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <action>
    Per decision D-03:
    Update `test/scrypath/docs_contract_test.exs`. 
    1. Add `"guides/related-data-and-reindexing.md"` to the `@guide_paths` list at the top of the file so it's loaded into the `@guides` map.
    2. Add a new test block (e.g., `test "related-data guide explicitly mentions temporary Oban workaround"`) that asserts `String.contains?` for "temporary workaround", "first-class feature", and "Oban" against the `@guides["guides/related-data-and-reindexing.md"]` content.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <done>The test suite passes and verifies the presence of the required strings in the guide.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze milestone and record verdict</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <action>
    Per decisions D-04 and D-05:
    1. In `.planning/ROADMAP.md`, update the `v1.23` milestone status from "active" to "shipped + archived in-repo". Add the explicit verdict "related-data propagation" to its progress summary. Tick the box for Phase 88 plan. Update "Current Milestone" to reflect there is none active or it's completed.
    2. In `.planning/STATE.md`, update the status to `shipped` (or `completed`), add a closing entry in the "Decisions" list stating "Phase 88 complete: Papercuts fixed and milestone frozen. Next-pull verdict is related-data propagation." Update `Next Command` to indicate completion.
    3. In `.planning/REQUIREMENTS.md`, tick off `FIX-01` and `FIX-02`. Ensure Traceability shows them as "Complete" for Phase 88.
  </action>
  <verify>
    <automated>grep -i "related-data propagation" .planning/STATE.md && grep -i "FIX-01" .planning/REQUIREMENTS.md | grep -i "Complete"</automated>
  </verify>
  <done>All three planning files reflect the v1.23 milestone closure and explicit verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal / Planning | Changes are restricted to documentation and planning metadata; no runtime data flows are altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 | Tampering | test/scrypath/docs_contract_test.exs | mitigate | Implemented a dedicated contract test asserting the presence of workaround instructions so they cannot be accidentally altered or removed. |
| T-88-02 | Information Disclosure | guides/related-data-and-reindexing.md | accept | Documentation is public and intended for developers; no sensitive secrets or PII are exposed. |
</threat_model>

<verification>
1. Run `mix test test/scrypath/docs_contract_test.exs` to ensure tests pass.
2. Verify `.planning/ROADMAP.md` is frozen and phase 88 plans are checked off.
</verification>

<success_criteria>
Documentation is updated with the Oban workaround, tests pass protecting this workaround, and the v1.23 milestone is closed with the correct next-pull verdict in all planning files.
</success_criteria>

<output>
After completion, create `.planning/phases/88-evidence-backed-papercuts-and-next-pull-verdict/88-01-SUMMARY.md`
</output>