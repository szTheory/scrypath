---
phase: 162-whole-product-evidence-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/162-whole-product-evidence-baseline/162-BASELINE.md
  - .planning/phases/162-whole-product-evidence-baseline/check_baseline.py
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]
estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A maintainer can follow one first-hour package-to-search claim from adopter job and integration seam to a dated canonical result, exact proof boundary, independent support status, and evidence freshness."
    - "Each assessed claim has a stable ID, one of supported/insufficiently supported/unknown, and an independent current/reusable within its stated boundary/stale after a relevant invalidator/unknown freshness value; an absent receipt cannot yield supported."
    - "A claim with no evidence link remains an explicit unknown or insufficiently supported entry, not an empty supported row (BASE-01/BASE-02 empty-input edge)."
    - "Two claims citing the same receipt remain separate when their jobs or boundaries differ; the receipt never merges their conclusions (BASE-01/BASE-02 adjacency edge)."
    - "Rows have stable IDs and a documented dimension-then-lifecycle ordering, including when claims share a status or evidence item (BASE-01/BASE-02 ordering edge)."
  artifacts:
    - .planning/phases/162-whole-product-evidence-baseline/162-BASELINE.md
    - .planning/phases/162-whole-product-evidence-baseline/check_baseline.py
  key_links:
    - "The tracer row links Phase 160 package coverage and exact-SHA verification to only the package consumer and named service tuple it exercised."
    - "The baseline links the approved readiness program's seven dimensions to claim rows; it does not copy archived evidence tables."
  prohibitions:
    - statement: "The baseline must not present missing proof or a passing bounded milestone as whole-product support."
      status: unresolved
    - statement: "The baseline must not imply that Scrypath owns host authorization, business policy, or presentation."
      status: unresolved
---

<objective>
Create the canonical claim-level evidence index and prove its row contract through a real first-hour package-backed search journey, then cover the integrator and feature-owner entry paths.

Purpose: BASE-01, BASE-02, and BASE-03 require a maintainer to see what a claim means, what evidence supports it, and where the proof stops. D-01, D-02, D-03, D-04, D-05, and D-06 define that contract.
Output: `.planning/phases/162-whole-product-evidence-baseline/162-BASELINE.md` with claim IDs, definitions, a production-quality tracer row, and first-hour/indexing/search rows, plus a local structural artifact checker.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/162-whole-product-evidence-baseline/162-CONTEXT.md
@.planning/phases/162-whole-product-evidence-baseline/162-RESEARCH.md
@.planning/phases/162-whole-product-evidence-baseline/162-PATTERNS.md
@.planning/reference/PRE-OPERATOR-UI-READINESS.md
@.planning/milestones/v1.38-phases/160-package-backed-phoenix-proof/COVERAGE.md
@.planning/milestones/v1.38-phases/160-package-backed-phoenix-proof/160-VERIFICATION.md
@.planning/milestones/v1.38-phases/161-release-and-tidy-closeout/161-RELEASE-EVIDENCE.md
@guides/golden-path.md
@guides/request-edge-search.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace one first-hour package-backed search claim end to end</name>
  <files>.planning/phases/162-whole-product-evidence-baseline/162-BASELINE.md, .planning/phases/162-whole-product-evidence-baseline/check_baseline.py</files>
  <action>Create the sole canonical Phase 162 baseline index, with scope and downstream ownership at the top. State the four D-01 roles in user-job terms; list the seven approved dimensions verbatim from the readiness program. Use a Markdown claim matrix with one data row per stable claim ID and exact columns `ID`, `Dimension`, `Role`, `User job`, `Stage`, `Seam`, `Claim`, `Source`, `Observed result`, `Date`, `Receipt/SHA`, `Environment/versions`, `Evidence layer/CI posture`, `What it proves`, `Proof boundary`, `Limitation`, `Assessment`, `Freshness`, and `Invalidator/next question`. Use dimension tokens `1` through `7`, role tokens `integrator`, `feature owner`, `operator`, `maintainer`, and stage tokens `first-hour setup`, `indexing`, `search`, `failure diagnosis`, `recovery`, `upgrade`, `release`; define these in the index. Keep assessment and freshness separate with the exact D-06 vocabularies and explain D-07 event-triggered revalidation. Create `check_baseline.py` as a local documentation-artifact check, not a product test or CI lane. Its `--through N` mode must parse actual matrix rows (including suffix IDs), require each base ID C-01 through C-N exactly once, reject malformed or duplicate IDs, wrong column counts, blank/placeholder cells, invalid dimension/role/stage/status/freshness values, unresolved local Source links, duplicate claim assertions, and a `supported` row with absent direct proof or observed result. Treat absent proof as an explicit reason-bearing `No direct proof` Source and `No observed result` value; require reason-bearing `unknown`/`not applicable` values for genuinely unavailable date, receipt, or environment metadata. Require a dated direct result and source link for `supported`. Its `--full-coverage` mode must derive all seven dimensions, four roles, and seven stages from row values, and fail on a missing category, independently of heading prose. Error output must name the claim ID and field. Put C-01 in the matrix as a fully assessed first-hour integrator journey from package/toolchain through Ecto schema/context/Repo, Scrypath inline indexing, Meilisearch task and visible search/hydration. Use Phase 160 scenario coverage and verification plus Phase 161 final candidate/release receipts; bind each result to its actual SHA, Postgres/Meilisearch service tuple, package version, and scenario, distinguish observed pass from advisory CI posture, and state that Repo success, backend task acceptance, and searchable visibility are different claims. Per D-02/D-03, identify host-owned authorization, business policy, and presentation; cite canonical proof rather than copying it. Per D-05, never invent a date, command result, or environment value absent from the receipt.</action>
  <verify><automated>python3 .planning/phases/162-whole-product-evidence-baseline/check_baseline.py --through 1</automated></verify>
  <done>C-01 is an independently traceable first-hour row with an exact source/result, status, freshness, seam, boundary, and limitation; the baseline declares one canonical index and D-01 through D-07 field meanings.</done>
</task>

<task type="auto">
  <name>Task 2: Expand first-hour, indexing, and search entry claims</name>
  <files>.planning/phases/162-whole-product-evidence-baseline/162-BASELINE.md</files>
  <action>Extend the same matrix with bounded claim IDs C-02 through C-06: public API and error/compatibility contract, Ecto schema projection and inline/manual synchronization, search and hydration with host-context ownership, optional Phoenix request-edge parsing, and first-hour documentation/diagnostics. For each, read current code/tests/guides and the appropriate canonical receipt; record the observed result and proof layer, or an explicit insufficient/unknown status with the smallest unanswered question. Include package/toolchain, Ecto schema/context/Repo, Scrypath, Meilisearch, and optional Phoenix seams per D-02. Treat downstream search as the application feature owner's outcome per D-03. Preserve the Phase 160 package scenario opt-outs and selected-tuple limits per D-04/D-05. Keep a distinct row for each materially different job or proof boundary even when the same receipt is reused, and order IDs consistently. No product changes or new CI proof are authorized by these rows.</action>
  <verify><automated>python3 .planning/phases/162-whole-product-evidence-baseline/check_baseline.py --through 6</automated></verify>
  <done>A maintainer can inspect C-01 through C-06 to distinguish package first use, core write/search, optional request-edge, and documentation claims with independent evidence and explicit limits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical receipt to baseline assertion | Archived source and hosted run describe a bounded SHA, tuple, and scenario; the new row may overstate them. |
| Host app to Scrypath | Authorization, policy, and rendering belong to the application even when a search result passes through Scrypath. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-01 | Tampering | C-01 receipt mapping | high | mitigate | Link the exact Phase 160 source/result and state SHA, service tuple, scenario opt-outs, and advisory posture in Task 1. |
| T-162-02 | Information disclosure | Evidence snippets | medium | mitigate | Link canonical receipts and cite sanitized results; Task 1 and Task 2 avoid copying raw credentials, queries, or payloads. |
| T-162-03 | Elevation of privilege | Host policy claim | medium | mitigate | Task 1 and Task 2 explicitly name application-owned authorization and presentation at the Phoenix/context seam. |
</threat_model>

<verification>
Run each task's local row-level artifact check and `git diff --check -- .planning/phases/162-whole-product-evidence-baseline/162-BASELINE.md .planning/phases/162-whole-product-evidence-baseline/check_baseline.py`. Inspect linked C-01 receipt semantics against the cited Phase 160 artifact; structural completeness does not establish source support.
</verification>

<success_criteria>
The baseline exists with a production-quality C-01 tracer, a stable row contract, and C-02 through C-06 entry claims; claim assessment and evidence freshness are separate, and the first-hour proof stays within its recorded boundary.
</success_criteria>

<output>
Create `.planning/phases/162-whole-product-evidence-baseline/162-01-SUMMARY.md` when done.
</output>
