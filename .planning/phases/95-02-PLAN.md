---
phase: "95"
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/scrypath/search.ex
  - test/scrypath/search_test.exs
  - lib/scrypath.ex
  - test/scrypath_test.exs
autonomous: true
requirements:
  - FACET-UX-03
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "Developers can call Scrypath.search_facet_values/4"
    - "Options are validated before hitting backend"
    - "Raw map responses are mapped to FacetSearchResult structs"
  artifacts:
    - path: "lib/scrypath.ex"
      provides: "Facade functions and documentation"
    - path: "lib/scrypath/search.ex"
      provides: "Options validation, routing, and struct conversion"
  key_links:
    - from: "lib/scrypath.ex"
      to: "lib/scrypath/search.ex"
      via: "delegation"
    - from: "lib/scrypath/search.ex"
      to: "lib/scrypath/backend.ex"
      via: "schema.__scrypath__(:backend).search_facet_values callback"
---

<objective>
Wire up the `Scrypath` facade and `Scrypath.Search` logic to expose facet value search to developers.

Purpose: Provide the clean, idiomatic Elixir API that abstracts away the raw map response into a developer-friendly struct.
Output: `search_facet_values/4` and bang variant available on the main `Scrypath` facade.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-DISCUSS.md
@.planning/phases/95-PATTERNS.md
@.planning/phases/95-RESEARCH.md
</context>

<interfaces>
<!-- Key types from prior plan -->
From lib/scrypath/facet_search_result.ex:
```elixir
defmodule Scrypath.FacetSearchResult do
  def new(raw)
end
```
From lib/scrypath/backend.ex:
```elixir
  @callback search_facet_values(module(), String.t(), String.t(), keyword(), keyword()) :: {:ok, map()} | {:error, term()}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core routing and struct conversion</name>
  <files>lib/scrypath/search.ex, test/scrypath/search_test.exs</files>
  <behavior>
    - Validates options using `Scrypath.Options.validate_search_options`.
    - Invokes `schema.__scrypath__(:backend).search_facet_values`.
    - Maps `{:ok, map}` to `{:ok, Scrypath.FacetSearchResult.new(map)}`.
  </behavior>
  <action>
    Implement `search_facet_values(schema_module, facet_name, search_string, opts \\ [])`.
    Explicitly reference and use the "Options Validation" shared pattern defined in 95-PATTERNS.md to handle option validation errors consistently with `search/3`.
  </action>
  <verify>
    <automated>mix test test/scrypath/search_test.exs</automated>
  </verify>
  <done>Search routing handles validation, backend dispatch, and struct parsing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Facade API and documentation</name>
  <files>lib/scrypath.ex, test/scrypath_test.exs</files>
  <behavior>
    - `Scrypath.search_facet_values/4` delegates to `Scrypath.Search`.
    - `Scrypath.search_facet_values!/4` delegates and unwraps or raises `Scrypath.Search.Error`.
  </behavior>
  <action>
    Add `search_facet_values/4` and `search_facet_values!/4` with comprehensive `@doc` strings explaining their use for high-cardinality/type-ahead facets.
    Explicitly reference and use the "Error Wrapping" shared pattern defined in 95-PATTERNS.md to mirror the exception unwrapping behavior of `search!/3`.
  </action>
  <verify>
    <automated>mix test test/scrypath_test.exs</automated>
  </verify>
  <done>API is exposed with idiomatic bang and non-bang variants and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public API | User inputs arrive at `Scrypath.search_facet_values` and are validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02 | Denial of Service | `Scrypath.Search` | accept | High frequency searches are expected for type-ahead; application layer must rate limit. |
</threat_model>

<verification>
mix compile --warnings-as-errors
mix test test/scrypath/search_test.exs test/scrypath_test.exs
</verification>

<success_criteria>
`Scrypath.search_facet_values/4` and bang variant are fully functional, return `FacetSearchResult` structs, and have complete documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/95-02-SUMMARY.md`
</output>
