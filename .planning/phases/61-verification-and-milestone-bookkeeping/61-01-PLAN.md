---
phase: 61
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs
autonomous: true
requirements:
  - OPS-PB-05
---

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|---------------------------|
| Injection / XSS via playbook JSON in tests | N/A | Tests use fixed JSON literals; no new user-controlled strings in production code |
| Path traversal or workspace escape | Low | Tests use **`Store.safe_basename?/1`**-compliant names only; workspace root is **`test.exs`** **`tmp/playbooks_test*`** |
| Leaking secrets in CI logs | N/A | No secrets; stub adapter has no network I/O |
| Breaking contributor verify contracts | Low | **`mix verify.opsui`** unchanged unless a follow-up task explicitly updates **README** / **CONTRIBUTING** / **`docs_contract_test`** in lockstep |
</threat_model>

<objective>
Close **OPS-PB-05** by proving **`PlaybookLive`** **save → list refresh → load → run** on **`ScrypathOps.Test.SearchPlaygroundStubAdapter`** (no Meilisearch), optionally adding one **`search_many`** paste/run case; keep **`mix verify.opsui`** the documented contributor mirror without silent contract drift.
</objective>

<tasks>

<task>
<type>execute</type>
<id>61-01-01</id>
<title>LiveView integration: save → listed file → load → run (stub)</title>
<read_first>
- scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
- scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs
- scrypath_ops/config/test.exs
- scrypath_ops/lib/scrypath_ops/playbook/store.ex
</read_first>
<action>
1. In **`scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs`**, reuse the existing **`setup`** (stub adapter + allowlist + backend) so **`Application.get_env(:scrypath_ops, :playbook_workspace_dir)`** remains the **`test.exs`** partitioned **`tmp/playbooks_test*`** root.
2. Add one test that **`live(conn, ~p"/ops/playbooks")`**, pastes a minimal valid **`mode: "search"`** playbook via **`form(..., phx-submit: 'import_paste')`** / **`render_submit()`** (same shape as existing tests: **`OpsPostA`**, **`"q" => "x"`**).
3. Submit **`form(..., phx-submit: 'save')`** with a unique basename matching **`Store.safe_basename?/1`** (e.g. **`"ops-pb-05-#{:erlang.unique_integer([:positive])}.json"`** captured in a variable).
4. Assert the saved basename appears in **`render(view)`** (workspace **`ul.menu`** lists **`phx-click="load"`** rows — match the **`font-mono`** filename text or **`phx-value-name`** via rendered HTML).
5. Call **`render_click(view, "load", %{"name" => basename})`** (event **`handle_event("load", ...)`** in **`PlaybookLive`**).
6. Call **`render_click(view, "run", %{})`** and assert the success path: rendered HTML contains **`Run finished`** (from **`run_result_summary/1`**) **or** flash copy **`Playbook run completed.`** from **`handle_event("run", ...)`**.
7. Optionally call **`render_click(view, "refresh_list", %{})`** once and assert the basename still listed — only if needed to stabilize ordering; default is list updated on **`save`** assign.
</action>
<acceptance_criteria>
- **`cd scrypath_ops && mix test test/scrypath_ops_web/live/playbook_live_test.exs`** exits **0**
- **`grep -n 'phx-submit=.save' scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex`** and **`grep -n 'handle_event("save"' scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex`** each return at least one line (sanity: events under test match production module)
- New test name or comment references **OPS-PB-05** / stub path so future triage finds the requirement
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>61-01-02</id>
<title>Optional: search_many paste → run on stub (federation-shaped)</title>
<read_first>
- scrypath_ops/test/scrypath_ops/playbook/runner_test.exs
- scrypath_ops/lib/scrypath_ops/playbook/v1.ex
- scrypath_ops/test/support/search_playground_stub_adapter.ex
</read_first>
<action>
1. Only if **`Playbook.V1.validate/1`** accepts a minimal **`"mode" => "search_many"`** fixture with **`entries`** referencing **`ScrypathOps.Test.OpsPostA`** / **`OpsPostB`** (mirror **`runner_test.exs`** shapes and caps).
2. In **`playbook_live_test.exs`**, add a second test: paste JSON → **`render_submit()`** → **`render_click(view, "run", %{})`** with **`Application.put_env(:scrypath_ops, :search_stub_variant, :ok)`** (already default in **`setup`**); assert **`Run finished`** substring appropriate for **`%MultiSearchResult{}`** (e.g. **`schema(s)`** in **`run_result_summary/1`**) or success flash.
3. Skip this task entirely if a valid minimal **`search_many`** playbook for the LiveView pipeline is non-obvious after reading **`V1`** — then leave a one-line comment in **`61-01-01`** test block pointing to **`runner_test.exs`** coverage instead (document gap honestly in commit message, not in published docs).
</action>
<acceptance_criteria>
- Either **`cd scrypath_ops && mix test test/scrypath_ops_web/live/playbook_live_test.exs`** still exits **0** with an added **`search_many`** case, **or** task explicitly skipped with no failing test and **`runner_test.exs`** still covers **`search_many`** (grep proves: **`grep -n 'search_many' scrypath_ops/test/scrypath_ops/playbook/runner_test.exs`** returns ≥1 line)
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>61-01-03</id>
<title>Confirm verify.opsui + doc contracts (change only if task changes)</title>
<read_first>
- lib/mix/tasks/verify.opsui.ex
- CONTRIBUTING.md
- README.md
- test/scrypath/docs_contract_test.exs
- .planning/phases/61-verification-and-milestone-bookkeeping/61-RESEARCH.md
</read_first>
<action>
1. If **`lib/mix/tasks/verify.opsui.ex`** is **unchanged** after **61-01-01**/**61-01-02**: verify **README** / **CONTRIBUTING** still mention **`mix verify.opsui`** with the same **`cd scrypath_ops` → `mix deps.get` → `mix test`** story as **`.github/workflows/ci.yml`** **`scrypath-ops`** job (**61-RESEARCH.md** §2.1). No edits required.
2. If **`verify.opsui`** gains env flags, extra steps, or different cwd: update **README.md**, **CONTRIBUTING.md** ( **`scrypath-ops`** row ordering must stay aligned with **`ci.yml`** per **`docs_contract_test`**), and extend **`test/scrypath/docs_contract_test.exs`** only for **stable** new phrases worth pinning (Phase 53 pattern).
3. Run **`mix verify.opsui`** from repo root after test changes to prove **OPSUI** suite green.
</action>
<acceptance_criteria>
- **`mix verify.opsui`** from repository root exits **0**
- If any of **README.md**, **CONTRIBUTING.md**, **`lib/mix/tasks/verify.opsui.ex`**, **`.github/workflows/ci.yml`**, or **`test/scrypath/docs_contract_test.exs`** were edited: **`mix test test/scrypath/docs_contract_test.exs`** exits **0** from repo root
- If none of those files were edited: **`grep -n 'mix verify.opsui' README.md CONTRIBUTING.md`** shows both files still reference the command
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd scrypath_ops && mix test test/scrypath_ops_web/live/playbook_live_test.exs
mix verify.opsui
```
</verification>

<success_criteria>
- At least one **LiveView** test exercises **`save`**, **`load`**, and **`run`** with **`SearchPlaygroundStubAdapter`** and no live Meilisearch dependency.
- **`mix verify.opsui`** remains green and contributor-facing docs stay contract-true with **CI** when verify text changes.
</success_criteria>

<must_haves>
- **OPS-PB-05** satisfied: automated proof of operator **save/list/load/run** on stub path in default **`scrypath_ops`** test config.
- No weakening of **Phase 53** doc-contract ordering between **CONTRIBUTING**, **`verify.opsui`**, and **`ci.yml`**.
</must_haves>
