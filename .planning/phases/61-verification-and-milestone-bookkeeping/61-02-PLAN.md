---
phase: 61
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SHIP-01
---

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|---------------------------|
| OWASP ASVS L1 injection via planning markdown | N/A | No runtime code paths; edits are maintainer-facing text only |
| Mis-stating shipped phases / Hex line | Medium | Cross-check **`.planning/STATE.md`**, root **`mix.exs`** **`@version`**, **README** “current Hex” claims, and **`.planning/phases/61-verification-and-milestone-bookkeeping/61-RESEARCH.md`** before marking **Complete** |
| Breaking **AUDT-01** / Nyquist doc-contract rows | Medium | Preserve **`| AUDT-01 |`**, **Phase 32**, and **`gap closure 33`** strings in **REQUIREMENTS** historical table; run **`docs_contract_test`** after edits |
| Accidental checkbox flip before verification | Low | Executor marks **ROADMAP** Phase **61** **`[x]`** only after **REQUIREMENTS** / **PROJECT** / **MILESTONES** edits and **`docs_contract_test`** pass |
</threat_model>

<objective>
Satisfy **SHIP-01**: align **`.planning/REQUIREMENTS.md`** body checkboxes and **v1.14** traceability table with shipped work (**LIB-*** → Phase **58**, **OPS-PB-02** / **OPS-PB-04** → Phase **60**, **OPS-PB-05** → Phase **61** once **61-01** is verified), fix **`.planning/PROJECT.md`** *Planning window* / next-phase pointer vs **STATE**, extend **`.planning/MILESTONES.md`** summary for **v1.9–v1.14** per existing narrative + table house style, and document **ROADMAP** Phase **61** checkbox completion rule for the executor. **Execution order:** land **61-01** (stub LiveView tests) before flipping **OPS-PB-05** to **Complete** in **REQUIREMENTS**; **`depends_on`** remains empty — coordination is procedural, not a graph edge.
</objective>

<tasks>

<task>
<type>execute</type>
<id>61-02-01</id>
<title>REQUIREMENTS.md — v1.14 body + traceability table closure</title>
<read_first>
- .planning/REQUIREMENTS.md
- .planning/STATE.md
- .planning/phases/61-verification-and-milestone-bookkeeping/61-RESEARCH.md
</read_first>
<action>
1. For each **v1.14** requirement row in the **Traceability** table (**LIB-01**..**LIB-03**, **OPS-PB-02**, **OPS-PB-04**, **OPS-PB-05**, **SHIP-01**): set **Status** to **Complete** when implementation is merged in-repo per **STATE** (Phases **58–60** executed **2026-04-22**; **OPS-PB-05** completes with **61-01** execution).
2. Set **Phase** column to: **LIB-01..LIB-03** → **Phase 58**; **OPS-PB-02**, **OPS-PB-04** → **Phase 60**; **OPS-PB-05** → **Phase 61**; **SHIP-01** → **Phase 61** (this plan).
3. Align section body checkboxes (`- [ ]` / `- [x]`) with the table for **LIB-*** , **OPS-PB-02**, **OPS-PB-04** so contributors are not misled.
4. Leave **Historical traceability (Nyquist invariants)** **AUDT-01** row unchanged (still **Phase 32; gap closure 33** / **Complete**).
5. Update the **Coverage** footer “Last updated” line if your process records dates there.
</action>
<acceptance_criteria>
- **`grep -n '| LIB-01 |' .planning/REQUIREMENTS.md`** shows **Complete** and **Phase 58** (repeat spot-check for **LIB-02**, **LIB-03**, **OPS-PB-02** Phase **60**, **OPS-PB-04** Phase **60**)
- **`grep -n 'OPS-PB-05' .planning/REQUIREMENTS.md`** shows traceability row **Phase 61** / **Complete** only after **61-01** is merged and verified
- **`grep -n 'gap closure 33' .planning/REQUIREMENTS.md`** returns ≥1 line
- **`grep -n '| AUDT-01 |' .planning/REQUIREMENTS.md`** returns ≥1 line
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>61-02-02</id>
<title>PROJECT.md — Planning window vs STATE.md</title>
<read_first>
- .planning/PROJECT.md
- .planning/STATE.md
</read_first>
<action>
1. In **`.planning/PROJECT.md`** § **Planning window**, replace stale **“next execution: Phase 60”** (or equivalent) with truth matching **STATE**: **Phase 61** verification / milestone bookkeeping, or **“v1.14 closure”** wording consistent with **STATE** **Current focus** line.
2. Ensure **Active** bullet for **v1.14** still points to **`.planning/REQUIREMENTS.md`** / **ROADMAP** as appropriate after **61** completes (executor may move **v1.14** to **Validated** only if that matches project habit when **SHIP-01** is fully done — default per **SHIP-01**: align *Current State* text with **STATE**, not necessarily flipping **Active** until **`/gsd-complete-milestone`**).
3. Confirm **“Current public line on Hex”** / **README**-aligned **`scrypath 0.3.4`** sentence still matches root **`mix.exs`** **`@version`** after any edit.
</action>
<acceptance_criteria>
- **`grep -nE 'next execution:.*Phase 60' .planning/PROJECT.md`** returns **no** matches (stale **Planning window** pointer removed)
- **Planning window** paragraph references **Phase 61** and/or **verification and milestone bookkeeping** consistent with **`.planning/STATE.md`** **Current focus** (spot-read **§ Planning window**)
- **`grep -n '@version' mix.exs`** shows **`0.3.4`** in the same line as **`@version`** — must match **PROJECT** sentence claiming current Hex line **`scrypath 0.3.4`**
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>61-02-03</id>
<title>MILESTONES.md — v1.9–v1.14 summary table / narrative gap</title>
<read_first>
- .planning/MILESTONES.md
- .planning/ROADMAP.md
- .planning/phases/61-verification-and-milestone-bookkeeping/61-RESEARCH.md
</read_first>
<action>
1. Extend the tail **summary table** (currently ends **`v1.8`**) with rows **`v1.9`** … **`v1.14`** using the same column style (**Version**, **Date**, **Phases**, **Plans** or **Requirements** count where known, **Status**, **Notes**).
2. Pull phase spans, ship dates, and theme one-liners from **`.planning/ROADMAP.md`** milestone list bullets (**v1.9** phases **42–43** through **v1.14** phases **57–61**).
3. For **`v1.14`**, use **In progress** (or **Shipped in-repo** + date when executor actually closes) — **SHIP-01** allows honest **in progress** until **`/gsd-complete-milestone`** archives; narrative should mention **B1** + **operator playbooks** and **Hex `0.3.4`** line if claiming current published package (match **PROJECT** / **README**).
4. Optionally add a short **`## v1.14 …`** section above the table mirroring **v1.13** section style — only if it reduces duplication without fighting the table.
</action>
<acceptance_criteria>
- **`grep -n '| `v1.9` |' .planning/MILESTONES.md`** returns ≥1 line in the summary table
- **`grep -n '| `v1.14` |' .planning/MILESTONES.md`** returns ≥1 line
- Table remains monotonic **v1.0** → **v1.14** with no broken markdown pipe rows
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>61-02-04</id>
<title>ROADMAP.md — Phase 61 checkbox rule + milestone list consistency</title>
<read_first>
- .planning/ROADMAP.md
- .planning/STATE.md
</read_first>
<action>
1. Review **Phase 61** bullet under **v1.14** — wording should still list **OPS-PB-05** + **SHIP-01** and success criteria; do not check **`[x]`** until **61-01** tests and **61-02** doc edits are verified.
2. When the executor confirms **OPS-PB-05** and **SHIP-01** complete: change **`- [ ]` Phase 61** to **`- [x]`** with completion date parenthetical matching **STATE** style (e.g. **2026-04-22** or actual close date).
3. If **v1.14** top-level roadmap row remains **`[ ]` in progress`**, leave it until milestone archive per project habit — **SHIP-01** only requires *agreement* across files; executor documents choice in commit message.
4. Optionally bump **ROADMAP** footer “Last updated” metadata line.
</action>
<acceptance_criteria>
- When this task runs (executor has verified **61-01** + other **61-02** tasks): **`grep -nE '^- \[x\].*Phase 61: Verification' .planning/ROADMAP.md`** returns **≥1** line and that line matches **`\([0-9]{4}-[0-9]{2}-[0-9]{2}\)`** via human spot-check of the grep output (completion date parenthetical).
- **`grep -n 'v1.14' .planning/ROADMAP.md`** still references **`.planning/REQUIREMENTS.md`**
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>61-02-05</id>
<title>Doc-contract gate after planning edits</title>
<read_first>
- test/scrypath/docs_contract_test.exs
- .planning/STATE.md
</read_first>
<action>
1. From repository root, run **`mix test test/scrypath/docs_contract_test.exs`** after all edits to **`.planning/REQUIREMENTS.md`**, **`.planning/PROJECT.md`**, **`.planning/MILESTONES.md`**, and **`.planning/ROADMAP.md`**.
2. If **`STATE.md`** is updated in the same change set (optional **Plan** pointer), re-run the same test file.
</action>
<acceptance_criteria>
- **`mix test test/scrypath/docs_contract_test.exs`** exits **0** from repo root
- Test output includes **`Scrypath.DocsContractTest`** and **0 failures**
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
mix test test/scrypath/docs_contract_test.exs
```
</verification>

<success_criteria>
- **REQUIREMENTS** **v1.14** traceability rows and body checkboxes match shipped phases (**58** / **60** / **61**) per **STATE** and **61-RESEARCH.md**.
- **PROJECT** *Planning window* / next-phase pointer matches **STATE** (no stale “next Phase 60”).
- **MILESTONES** summary table covers **v1.9–v1.14** with consistent house style.
- **ROADMAP** Phase **61** **`[x]`** only after verification; **Nyquist** doc-contract tests pass.
</success_criteria>

<must_haves>
- **SHIP-01** satisfied: planning artifacts agree on **v1.14** outcomes and Hex narrative where stated; **Complete** traceability for shipped requirement IDs.
- **AUDT-01** / **gap closure 33** invariant strings preserved in **REQUIREMENTS** historical section.
</must_haves>
