---
phase: 99
plan: 3
type: execute
wave: 3
depends_on:
  - 99-01
  - 99-02
files_modified:
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
  - test/mix/tasks/workflow_wiring_test.exs
  - test/scrypath/phase99_contract_test.exs
autonomous: true
requirements:
  - TEST-03
  - GATE-02
must_haves:
  - "Add one stable required Phase 99 trust job token in CI (recommended `phase99-trust`) that runs `mix verify.phase99`."
  - "Document explicit required-check contract tokens in CONTRIBUTING and keep heavy/live checks advisory by default."
  - "Lock workflow-docs-test parity for required check names and phase99 gate references with deterministic assertions."
---

<objective>
Finalize Phase 99 required-check policy enforcement by wiring one stable trust job token in CI and locking check-name parity across workflow, docs, and tests.
</objective>

<context>
This is policy/wiring hardening, not runtime feature work. The required-check contract must be explicit, deterministic, and low-noise, while keeping heavy/live jobs advisory unless maintainers intentionally promote them.
</context>

<tasks>
### Task 99-03-01 - Add stable Phase 99 required trust job to CI workflow
<read_first>
- `.github/workflows/ci.yml`
- `lib/mix/tasks/verify.phase99.ex`
- `.planning/ROADMAP.md`
- `.planning/phases/99-drift-gates-and-ci-enforcement/99-CONTEXT.md`
</read_first>
<action>
- Update `.github/workflows/ci.yml` to add one stable service-free required-check job token: `phase99-trust`.
- Ensure the job executes `mix verify.phase99` after standard setup steps (checkout, setup-beam, cache, deps install).
- Keep job semantics deterministic and prerequisite-light (no external service startup, no path-conditional skip logic).
- Preserve existing core required jobs (`main-ci`, `repo-hygiene`, `release-truth`) and do not reclassify heavy/live jobs as required in this plan.
</action>
<acceptance_criteria>
- `rg "^  phase99-trust:" ".github/workflows/ci.yml"` returns 1 match.
- `rg "phase99-trust|mix verify\\.phase99" ".github/workflows/ci.yml"` returns at least 2 matches.
- `rg -U "(?s)^  phase99-trust:.*uses: actions/checkout@.*uses: erlef/setup-beam@.*uses: actions/cache@.*mix deps.get.*mix verify\\.phase99" ".github/workflows/ci.yml"` returns at least 1 match.
- `! rg "if:.*phase99-trust" ".github/workflows/ci.yml"` exits 0 (no path-conditional required-check ambiguity for this job).
</acceptance_criteria>
<verify>
- `rg "^  phase99-trust:" ".github/workflows/ci.yml"`
- `rg "phase99-trust|mix verify\\.phase99" ".github/workflows/ci.yml"`
- `rg -U "(?s)^  phase99-trust:.*uses: actions/checkout@.*uses: erlef/setup-beam@.*uses: actions/cache@.*mix deps.get.*mix verify\\.phase99" ".github/workflows/ci.yml"`
- `! rg "if:.*phase99-trust" ".github/workflows/ci.yml"`
</verify>

### Task 99-03-02 - Publish explicit required-check contract tokens in CONTRIBUTING
<read_first>
- `CONTRIBUTING.md`
- `.github/workflows/ci.yml`
- `.planning/ROADMAP.md`
- `.planning/REQUIREMENTS.md`
</read_first>
<action>
- Update `CONTRIBUTING.md` CI section to explicitly list required merge blockers for this milestone:
  - `main-ci`,
  - `repo-hygiene`,
  - `release-truth`,
  - `phase99-trust`.
- Keep wording explicit that heavy/live checks remain advisory by default unless maintainers intentionally promote them.
- Ensure `mix verify.phase99` is described as the local reproduction command for the Phase 99 trust check.
- Keep edits scoped to trust-hardening policy clarity; do not add runtime capability guidance.
</action>
<acceptance_criteria>
- `rg "main-ci|repo-hygiene|release-truth|phase99-trust" "CONTRIBUTING.md"` returns at least 4 matches.
- `rg "mix verify\\.phase99" "CONTRIBUTING.md"` returns at least 1 match.
- `rg "required merge blockers|advisory|heavy|live" "CONTRIBUTING.md"` returns at least 2 matches.
- `! rg "phase5-verification.*Required merge gate|phoenix-example-integration.*Required merge gate" "CONTRIBUTING.md"` exits 0.
</acceptance_criteria>
<verify>
- `rg "main-ci|repo-hygiene|release-truth|phase99-trust" "CONTRIBUTING.md"`
- `rg "mix verify\\.phase99" "CONTRIBUTING.md"`
- `rg "required merge blockers|advisory|heavy|live" "CONTRIBUTING.md"`
- `! rg "phase5-verification.*Required merge gate|phoenix-example-integration.*Required merge gate" "CONTRIBUTING.md"`
</verify>

### Task 99-03-03 - Lock cross-surface parity tests for TEST-03 and GATE-02
<read_first>
- `test/mix/tasks/workflow_wiring_test.exs`
- `test/scrypath/phase99_contract_test.exs`
- `.github/workflows/ci.yml`
- `CONTRIBUTING.md`
- `mix.exs`
</read_first>
<action>
- Extend `test/mix/tasks/workflow_wiring_test.exs` with deterministic assertions that:
  - CI contains `phase99-trust`,
  - that job routes to `mix verify.phase99`,
  - required-check tokens in docs/workflow remain aligned.
- Extend `test/scrypath/phase99_contract_test.exs` with TEST-03 checks that assert cross-surface parity for:
  - required-check names (`main-ci`, `repo-hygiene`, `release-truth`, `phase99-trust`),
  - verify alias token (`verify.phase99`) across `mix.exs`, workflow, and docs.
- Add ordering assertion for required-check token list in CONTRIBUTING to minimize rename/reorder drift noise.
- Keep assertions token/anchor/order-based only; no paragraph snapshots.
</action>
<acceptance_criteria>
- `rg "phase99-trust|mix verify\\.phase99|main-ci|repo-hygiene|release-truth" "test/mix/tasks/workflow_wiring_test.exs"` returns at least 5 matches.
- `rg "TEST-03|phase99-trust|main-ci|repo-hygiene|release-truth|verify\\.phase99|ordered\\?" "test/scrypath/phase99_contract_test.exs"` returns at least 7 matches.
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/mix/tasks/workflow_wiring_test.exs" "test/scrypath/phase99_contract_test.exs"` exits 0.
- `mix test test/mix/tasks/workflow_wiring_test.exs test/scrypath/phase99_contract_test.exs` exits 0.
- `mix verify.phase99` exits 0.
</acceptance_criteria>
<verify>
- `rg "phase99-trust|mix verify\\.phase99|main-ci|repo-hygiene|release-truth" "test/mix/tasks/workflow_wiring_test.exs"`
- `rg "TEST-03|phase99-trust|main-ci|repo-hygiene|release-truth|verify\\.phase99|ordered\\?" "test/scrypath/phase99_contract_test.exs"`
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/mix/tasks/workflow_wiring_test.exs" "test/scrypath/phase99_contract_test.exs"`
- `mix test test/mix/tasks/workflow_wiring_test.exs test/scrypath/phase99_contract_test.exs`
- `mix verify.phase99`
</verify>
</tasks>

<threat_model>
- **T99-P3-001 (Required-Check Rename Drift, High):** workflow job token changes without matching docs/tests updates, breaking branch-protection expectations.  
  **Mitigation:** lock one stable token (`phase99-trust`) and assert parity across workflow, CONTRIBUTING, and phase99 test suites.  
  **Detection:** `rg "phase99-trust" ".github/workflows/ci.yml" "CONTRIBUTING.md" "test/mix/tasks/workflow_wiring_test.exs" "test/scrypath/phase99_contract_test.exs"`.
- **T99-P3-002 (Policy Ambiguity, Medium):** required vs advisory boundaries become unclear, creating merge confusion and check-noise disputes.  
  **Mitigation:** explicit required-check list plus advisory boundary language in CONTRIBUTING.  
  **Detection:** `rg "required merge blockers|main-ci|repo-hygiene|release-truth|phase99-trust|advisory" "CONTRIBUTING.md"`.
- **T99-P3-003 (Scope Expansion Through CI Policy, High):** live/service-heavy jobs are silently promoted to routine required blockers.  
  **Mitigation:** keep Phase 99 required contract limited to lean deterministic checks and assert absence of heavy-job required wording.  
  **Detection:** `! rg "phase5-verification.*Required merge gate|phase13-verification.*Required merge gate|phoenix-example-integration.*Required merge gate" "CONTRIBUTING.md"`.
</threat_model>

<verification>
- `mix test test/mix/tasks/workflow_wiring_test.exs`
- `mix test test/scrypath/phase99_contract_test.exs`
- `mix verify.phase99`
- `rg "phase99-trust|mix verify\\.phase99" ".github/workflows/ci.yml" "CONTRIBUTING.md" "test/mix/tasks/workflow_wiring_test.exs" "test/scrypath/phase99_contract_test.exs"`
</verification>

<success_criteria>
- `GATE-02` is satisfied with explicit required-check token contract and one stable Phase 99 trust job in CI.
- `TEST-03` is fully locked through cross-surface parity assertions for required-check names and verify alias references.
- Required/advisory boundaries are explicit and deterministic, with no path-conditional required-check ambiguity.
- Trust-hardening policy is enforced without runtime feature expansion.
</success_criteria>

<output>
- Updated `.github/workflows/ci.yml`
- Updated `CONTRIBUTING.md`
- Updated `test/mix/tasks/workflow_wiring_test.exs`
- Updated `test/scrypath/phase99_contract_test.exs`
</output>
