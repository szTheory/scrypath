---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/golden-path.md
  - guides/getting-started.md
  - mix.exs
  - README.md
autonomous: true
requirements: [ADPT-01]
tags: [docs, adoption, golden-path, exdoc]

must_haves:
  truths:
    - "A single linear `guides/golden-path.md` exists and is registered in `mix.exs` `:extras` and appears under HexDocs Getting Started (or equivalent group) per 029-CONTEXT D-01"
    - "README points to that guide as the primary first-hour path without pasting the full tutorial (029-CONTEXT D-01)"
  artifacts:
    - path: "guides/golden-path.md"
      provides: "Canonical ADPT-01 narrative"
      contains: "Scrypath.search"
    - path: "mix.exs"
      provides: "ExDoc extra for golden path"
      contains: "guides/golden-path.md"
---

<objective>
Deliver **ADPT-01**: one **linear golden-path document** from dependency install through **working** `Scrypath.search/3` with **`sync_mode: :inline`**, plus README wayfinding and ExDoc registration.
</objective>

<threat_model>
**ASVS L1 — documentation**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-29-DOC-01: Copy-paste deps or env vars that encourage committing secrets | Low | Use placeholders only; reference example README for real env names without embedding keys |
| T-29-DOC-01: Broken primary link blocks onboarding | Medium | Every new relative link must be grep-verifiable against existing paths |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Author `guides/golden-path.md`</name>
  <files>guides/golden-path.md</files>
  <read_first>
    - .planning/phases/029-golden-path-adoption-documentation/029-CONTEXT.md
    - guides/getting-started.md
    - guides/sync-modes-and-visibility.md
    - examples/phoenix_meilisearch/README.md
    - examples/phoenix_meilisearch/compose.yaml
    - README.md
  </read_first>
  <action>
    Create **`guides/golden-path.md`** as an ordered checklist covering, in this order:

    1. **Goal** — one paragraph: first indexed document + first `Scrypath.search/3` using **inline** sync only (defer Oban/manual to sync guide).
    2. **Dependencies** — `mix.exs` snippet using **`{:scrypath, "~> 0.3"}`** (minor-flexible range matching current **`mix.exs @version "0.3.3"`**; do not leave `~> 0.3.0` as the only example).
    3. **Bring up Meilisearch** — copy-paste **`docker compose up -d`** (or equivalent) aligned with **`examples/phoenix_meilisearch/README.md`**: note **image tag v1.15**, default **`http://127.0.0.1:7700`**, env var **`SCRYPATH_MEILISEARCH_URL`**; link to **`examples/phoenix_meilisearch/README.md`** for full Phoenix+Postgres smoke.
    4. **Application config** — minimal `config/runtime.exs` or `config/dev.exs` pattern pointing Scrypath at Meilisearch (reuse wording from existing guides; no invented module names beyond `MyApp` style).
    5. **Schema** — `use Scrypath` block with at least **`fields:`** and **`filterable:`** (can mirror README Quick Path).
    6. **Context** — one module showing **`Scrypath.sync_record(..., sync_mode: :inline)`** after a successful repo write **and** **`Scrypath.search/3`** with **`backend: Scrypath.Meilisearch`** and **`repo:`**.
    7. **Proof of search** — **`IEx`** snippet invoking the context search function (preferred for brevity per 029-CONTEXT discretion); link **`guides/phoenix-walkthrough.md`** for UI depth.
    8. **`## Ecto without Phoenix (API-only)`** — short subsection: same context-owned pattern, no `MyAppWeb`, link example app as reference (029-CONTEXT D-05).
    9. **`## What is next`** — link **`guides/sync-modes-and-visibility.md`** and Oban docs; state explicitly that production often uses **`:oban`** but the first hour stays **inline** only (029-CONTEXT D-03).

    Include a backward link near the top: readers who want theory first → **`guides/getting-started.md`**.
  </action>
  <verify>
    <automated>mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - `test -f guides/golden-path.md` exits 0
    - `grep -F 'Scrypath.search' guides/golden-path.md` returns ≥1 line
    - `grep -F 'sync_mode: :inline' guides/golden-path.md` returns ≥1 line
    - `grep -F 'guides/sync-modes-and-visibility.md' guides/golden-path.md` returns ≥1 line
    - `grep -F 'examples/phoenix_meilisearch/README.md' guides/golden-path.md` returns ≥1 line
    - `grep -F 'SCRYPATH_MEILISEARCH_URL' guides/golden-path.md` returns ≥1 line
    - `grep -Fi 'ecto without phoenix' guides/golden-path.md` returns ≥1 line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Register guide in ExDoc</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (`defp docs`)
    - guides/golden-path.md
  </read_first>
  <action>
    In **`mix.exs`** inside **`defp docs` → `:extras`**, add **`"guides/golden-path.md"`** immediately after **`"guides/getting-started.md"`** (keep alphabetical or reading-order grouping consistent with file).

    In **`groups_for_extras` → "Getting Started"**, append **`"guides/golden-path.md"`** after **`"guides/getting-started.md"`** so HexDocs shows the golden path beside getting started.
  </action>
  <verify>
    <automated>mix format --check-formatted && MIX_ENV=test mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'guides/golden-path.md' mix.exs` returns ≥2 lines (extras + group)
    - `MIX_ENV=test mix docs --warnings-as-errors` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Cross-link from `guides/getting-started.md`</name>
  <files>guides/getting-started.md</files>
  <read_first>
    - guides/getting-started.md
    - guides/golden-path.md
  </read_first>
  <action>
    Near the top (after the first H1 or first introductory paragraph), add a short callout block or paragraph with a Markdown link **`[Golden path](golden-path.md)`** (or relative path matching existing guide link style) stating that **hands-on linear onboarding** lives there; keep existing content intact.
  </action>
  <verify>
    <automated>mix format --check-formatted && MIX_ENV=test mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'golden-path.md' guides/getting-started.md` returns ≥1 line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: README primary entry for golden path</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - guides/golden-path.md
  </read_first>
  <action>
    In **`README.md`**, immediately after **`## Installation`** (or before **`## Quick Path`**), add a concise **“Start here”** line or bullet that links **`guides/golden-path.md`** as the **canonical first-hour path** (use repo-relative path consistent with other README guide links, e.g. **`guides/golden-path.md`**).

    Optionally trim **Quick Path** intro by **one short sentence** if needed to avoid duplication — do **not** remove the Quick Path code blocks entirely (029-CONTEXT: README stays scannable).
  </action>
  <verify>
    <automated>mix format --check-formatted && MIX_ENV=test mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'guides/golden-path.md' README.md` returns ≥1 line
    - `grep -F '## Quick Path' README.md` returns ≥1 line
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <automated>mix format --check-formatted && MIX_ENV=test mix docs --warnings-as-errors</automated>
</verification>

<success_criteria>
  - Golden path guide exists, is linked from README and getting-started, and builds under ExDoc without warnings
  - A reader can follow the new doc from install/search proof without hunting unrelated files first
</success_criteria>
