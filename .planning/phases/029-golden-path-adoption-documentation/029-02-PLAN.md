---
phase: 29
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - README.md
  - CHANGELOG.md
  - docs/releasing.md
autonomous: true
requirements: [ADPT-02, ADPT-03]
tags: [docs, adoption, sync-modes, versioning]

must_haves:
  truths:
    - "README dependency line uses a `~>` range compatible with `mix.exs @version` (029-CONTEXT D-04)"
    - "README sync-mode table remains compact and defers depth to `guides/sync-modes-and-visibility.md` (029-CONTEXT D-03)"
    - "`docs/releasing.md` stays canonical for verify gates; README does not duplicate the full verify matrix (029-CONTEXT D-04)"
  artifacts:
    - path: "README.md"
      provides: "Adopter-facing sync + versioning wayfinding"
      contains: "docs/releasing.md"
    - path: "docs/releasing.md"
      provides: "Maintainer gates unchanged; optional adopter pointer"
      contains: "verify.phase11"
---

<objective>
Deliver **ADPT-02** and **ADPT-03**: README stays **decision-ready** for sync modes without forking the sync guide; **versioning / verify / changelog** messaging is consistent and points at **`docs/releasing.md`** as canonical.
</objective>

<threat_model>
**ASVS L1 — documentation**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-29-DOC-02: README implies Oban is required for first search | Medium | Keep golden path + table wording aligned with “inline first”; link sync guide for production |
| T-29-DOC-03: Conflicting semver or verify claims across README vs releasing | Medium | Single sentence in README: “details in docs/releasing.md”; no second verify matrix |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: README — sync modes and production pointers (ADPT-02)</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - guides/sync-modes-and-visibility.md
    - .planning/phases/029-golden-path-adoption-documentation/029-CONTEXT.md
  </read_first>
  <action>
    Under **`## Sync Modes`** in **`README.md`**:

    1. Keep the existing **three-row table** (inline / manual / oban) intact.
    2. Add **3–5 lines** immediately below the table (or after the existing “Accepted work…” paragraph) that answer: **local dev vs production** heuristics — when **`:inline`** is enough, when to reach for **`:oban`**, when **`:manual`** fits migrations/imports — without duplicating full semantics.
    3. Add an explicit sentence that **`guides/sync-modes-and-visibility.md`** is the **authority** for lifecycle states, Phoenix implications, and recovery language.

    Do **not** paste large sections from the sync guide into README.
  </action>
  <verify>
    <automated>mix format --check-formatted && MIX_ENV=test mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '## Sync Modes' README.md` returns ≥1 line
    - `sed -n '/## Sync Modes/,/## Search/p' README.md | grep -F 'guides/sync-modes-and-visibility.md'` exits 0 (link appears in Sync Modes section, not only in Wayfinding)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: README — versioning, verify, and dependency snippet (ADPT-03)</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - mix.exs (first 15 lines for `@version`)
    - docs/releasing.md
    - CHANGELOG.md
  </read_first>
  <action>
    1. In **`## Installation`**, change the dependency snippet from **`{:scrypath, "~> 0.3.0"}`** to **`{:scrypath, "~> 0.3"}`** (or another form explicitly justified in a one-line comment in the plan execution if Elixir dep syntax requires different — goal is **compatibility with `@version "0.3.3"`** without pinning patch floor incorrectly).

    2. Add a short section **`## Versioning and upgrades`** (or equivalent H2) that states in plain language:
       - **Semver posture** (public API / breaking vs additive — keep consistent with CHANGELOG tone; if unsure, mirror language from **`CHANGELOG.md`** header conventions).
       - **What protects quality**: point to **`mix verify.phase11`** as the always-on gate **without** copying the full task table from **`docs/releasing.md`**.
       - **Where releases are announced**: link **`CHANGELOG.md`** and **`docs/releasing.md`** with one sentence each on audience (adopters vs maintainers).
  </action>
  <verify>
    <automated>mix format --check-formatted && MIX_ENV=test mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '{:scrypath, "~> 0.3"}' README.md` returns ≥1 line OR if using `~> 0.3.3` document in task notes — prefer `grep -E '\{:scrypath, "~> 0\.3' README.md` shows updated line not `0.3.0` floor
    - `grep -F 'docs/releasing.md' README.md` returns ≥1 line
    - `grep -F 'CHANGELOG.md' README.md` returns ≥1 line
    - `grep -F 'verify.phase11' README.md` returns ≥1 line
    - `grep -F '## Versioning' README.md` returns ≥1 line OR `grep -F '## Versioning and upgrades' README.md` returns ≥1 line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: `docs/releasing.md` — adopter pointer without forking gates</name>
  <files>docs/releasing.md</files>
  <read_first>
    - docs/releasing.md
    - README.md
  </read_first>
  <action>
    At the top of **`docs/releasing.md`**, after the first H1 or before **`## Automated Release Gate`**, insert a short **“For library adopters”** (or similar) note: README summarizes upgrades; **this document** is the maintainer source for Release Please + **`mix verify.phase11`** + publish checks — **≤ 4 sentences**.
  </action>
  <verify>
    <automated>mix format --check-formatted && mix verify.phase11</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fi 'adopter' docs/releasing.md` returns ≥1 line OR `grep -Fi 'README' docs/releasing.md` returns ≥1 line in first 40 lines of file (`head -n 40 docs/releasing.md | grep -i readme` exits 0)
    - `mix verify.phase11` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: `CHANGELOG.md` — cross-link hygiene</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - docs/releasing.md
  </read_first>
  <action>
    Under **`## Unreleased`**, add **one new bullet** (prefer **`### Notes`**; create that subsection if absent) that states explicitly:
    - adopters should read **`README.md`** **versioning** section for upgrade expectations, and
    - maintainers should follow **`docs/releasing.md`** for verify gates and Release Please.

    Do **not** duplicate the full **`mix verify.phase11`** task list in the changelog.
  </action>
  <verify>
    <automated>mix format --check-formatted && mix verify.phase11</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'docs/releasing.md' CHANGELOG.md` returns ≥1 line
    - `sed -n '/## Unreleased/,/## \[/p' CHANGELOG.md | grep -Fi 'readme' | grep -Fi 'version'` exits 0 OR `sed -n '/## Unreleased/,/## \[/p' CHANGELOG.md | grep -Fi 'adopter'` exits 0 (Unreleased links README versioning to maintainer doc)
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <automated>mix format --check-formatted && MIX_ENV=test mix docs --warnings-as-errors && mix verify.phase11</automated>
</verification>

<success_criteria>
  - Sync-mode README guidance is decision-ready and defers semantics to the sync guide
  - Versioning and verify expectations are consistent across README, releasing doc pointer, and CHANGELOG cross-link
  - `mix verify.phase11` passes after doc edits
</success_criteria>
