---
phase: 46
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - scrypath_ops/test/scrypath_ops_web/live/search_live_test.exs
  - scrypath_ops/docs/operator-ia.md
autonomous: true
requirements:
  - OPSUI-04
  - OPSUI-05
---

<objective>

Pin **OPSUI-04** / **OPSUI-05** behavior with **`SearchLiveTest`** using a **fake `SearchPlayground` adapter** (no network), and align **`operator-ia.md`** nav copy with shipped search (remove “phase 46 — future” phrasing if still stale).

</objective>

<threat_model>

| ID | Threat | Severity | Mitigation in this plan |
|----|--------|----------|-------------------------|
| T-46-03 | Fake adapter leaks into prod release | medium | Fake module lives only under **`test/support/`**; tests **`on_exit`** restore **`:search_playground_adapter`** to previous value (same pattern as **`posture_live_test.exs`**). |

</threat_model>

<tasks>

<task id="46-03-01" type="execute">
<read_first>
- `scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs` (setup / `on_exit`)
- `scrypath_ops/test/support/conn_case.ex`
- `scrypath_ops/lib/scrypath_ops_web/live/search_live.ex` (events + assigns)
- `lib/scrypath/multi_search_result.ex` (struct fields for building stubs)
</read_first>
<action>

1. Add **`scrypath_ops/test/support/search_playground_stub_adapter.ex`** (name at executor discretion) implementing the **`SearchPlayground.Adapter`** behaviour from plan **01**:
   - **`search/3`** returns **`{:ok, Scrypath.SearchResult.new(Scrypath.Query.new(text, opts), %{"hits" => [], "estimatedTotalHits" => 0}, [], [])}`** (adjust **`opts`** arity if **`Query.new/2`** needs keyword from third arg — match **`Scrypath.Query`** API).
   - **`search_many/2`** returns **`{:ok, Scrypath.MultiSearchResult.new(ordered: [...], by_schema: map, failures: [])}`** with **at least two** ordered entries when entries list length ≥ 2. Provide **two** stub modules or function clauses: one with **`merge_hit_order: nil`**, one with **non-empty** **`merge_hit_order`** list, selected by a **compile-time** or **`Application.get_env`** test-only flag (no **`Agent`**).

2. Add **`scrypath_ops/test/scrypath_ops_web/live/search_live_test.exs`** with **`async: false`**:
   - Setup: allowlist **`[ScrypathOps.Test.OpsPostA, ScrypathOps.Test.OpsPostB]`** (or modules that exist under **`test/support`** — grep **`defmodule ScrypathOps.Test`**); minimal **`Application.put_env`** for **`:meilisearch_url`**, **`:backend`**, etc. mirroring **`posture_live_test`** so **`Schemas.scrypath_opts/0`** is coherent.
   - Test A: **`live(conn, ~p"/ops/search")`** — HTML contains **`Non-production search playground`** and **`Run sample searches`** (UI-SPEC CTA).
   - Test B: **`live(conn, ~p"/ops/search?mode=multi")`** — assert **`mode`** multi UI path (e.g. substring **`multi`** in a **`data-testid="search-mode-multi"`** attribute you add in **plan 02**, or assert URL patch — prefer **one** stable **`data-testid`** on the multi toggle for deterministic tests).
   - Test C: stub adapter returns partial failures — **`failures: [%{schema: Mod, reason: :stub}]`** with **`ordered` non-empty** — assert **`Some indexes did not return results.`** appears after **`render_click`** on search button.
   - Test D: **`page_size` 99** in params/event → assert error path contains **`50`** (library ceiling messaging) **or** the custom **`{:page_size_out_of_range, ...}`** tuple string from plan **01** — must match actual implementation.

3. Update **`scrypath_ops/docs/operator-ia.md`** table row for **`/ops/search`**: replace future-tense “**Phase 46 —** …” with **past-tense shipped** wording (e.g. “**Shipped in phase 46** — …”) so the nav table matches **`router.ex`** for shipped OPSUI.

</action>
<acceptance_criteria>

- `test -f scrypath_ops/test/scrypath_ops_web/live/search_live_test.exs` exits **0**.
- `cd scrypath_ops && mix test test/scrypath_ops_web/live/search_live_test.exs` exits **0**.
- `cd scrypath_ops && mix test` exits **0** (full ops app).
- `rg "Phase 46 —" scrypath_ops/docs/operator-ia.md` returns **exit code 1** and prints nothing (substring **`Phase 46 —`** must not appear in that file).

</acceptance_criteria>
</task>

</tasks>

<verification>

1. `cd scrypath_ops && mix test test/scrypath_ops_web/live/search_live_test.exs` exits **0**.
2. `cd scrypath_ops && mix test` exits **0**.
3. `rg "Phase 46 —" scrypath_ops/docs/operator-ia.md` produces **no output** and exit code **1**.

</verification>

<success_criteria>

- Search UI regressions caught by LiveView tests; docs no longer imply search is unshipped future work.

</success_criteria>

<must_haves>

- [ ] Stub adapter under **`test/support`**
- [ ] **≥4** test cases covering banner, mode URL, partial failures, page bound
- [ ] **`operator-ia.md`** past-tense alignment for **`/ops/search`**

</must_haves>
