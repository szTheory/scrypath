---
phase: "102-admin-ui-router-engine-refactor"
plan: "03"
type: execute
wave: 3
depends_on: ["102-02"]
files_modified:
  - "scrypath_ops/lib/scrypath_ops_web.ex"
  - "scrypath_ops/lib/scrypath_ops_web/components/layouts.ex"
  - "scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex"
  - "scrypath_ops/lib/scrypath_ops_web/nav.ex"
  - "scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex"
  - "scrypath_ops/lib/scrypath_ops_web/live/search_live.ex"
  - "scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex"
autonomous: true
requirements:
  - "OPS-01"

must_haves:
  truths:
    - "Internal navigation works dynamically regardless of where the engine is mounted"
    - "The engine does not depend on a statically compiled verified routes helper tied to a specific router"
  artifacts:
    - path: "scrypath_ops/lib/scrypath_ops_web/live/search_live.ex"
      provides: "Dynamic internal navigation"
      contains: "@mount_path"
  key_links:
    - from: "scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex"
      to: "scrypath_ops/lib/scrypath_ops_web/live/search_live.ex"
      via: "Dynamic path concatenation"
      pattern: "\#{@mount_path}"
---

<objective>
Remove static verified routes and fix internal engine navigation.
Purpose: Since `scrypath_ops` is now a mountable engine, it cannot rely on `~p` tied to a specific `ScrypathOpsWeb.Router`. All navigation must be dynamic based on the mount path injected by the macro.
Output: LiveViews and layouts that use the dynamic `@mount_path` for all links and redirects.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-admin-ui-router-engine-refactor/102-CONTEXT.md
@.planning/phases/102-admin-ui-router-engine-refactor/102-RESEARCH.md
@.planning/phases/102-admin-ui-router-engine-refactor/102-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Verified Routes and Fix Layouts</name>
  <files>scrypath_ops/lib/scrypath_ops_web.ex, scrypath_ops/lib/scrypath_ops_web/components/layouts.ex, scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex</files>
  <action>
    In `scrypath_ops/lib/scrypath_ops_web.ex`, remove `unquote(verified_routes())` from the HTML and LiveView helpers so `~p` is no longer imported (which would fail without a router).
    In `layouts.ex` and `root.html.heex`, replace all usages of `~p` (e.g., `~p"/"`, `~p"/images/logo.svg"`, `~p"/assets/css/app.css"`) with dynamic paths.
    Assuming `@mount_path` is assigned to the socket (from Plan 02), use string interpolation like `"#{@mount_path}/images/logo.svg"` or `"#{@mount_path}"`.
  </action>
  <verify>
    <automated>cd scrypath_ops &amp;&amp; mix compile</automated>
  </verify>
  <done>Layouts compile without relying on `~p`.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic LiveView Navigation</name>
  <files>scrypath_ops/lib/scrypath_ops_web/nav.ex, scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex, scrypath_ops/lib/scrypath_ops_web/live/search_live.ex, scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex</files>
  <action>
    Refactor navigation to remove `~p"/ops/..."`.
    In `nav.ex`, update the helper to accept `mount_path` as an argument or modify it to build paths dynamically.
    In the LiveViews (`posture_live.ex`, `search_live.ex`, `playbook_live.ex`), replace `push_patch(socket, to: ~p"/ops/...")` and `<.link navigate={~p"/ops/..."}>` with `#{@mount_path}/...` using the `mount_path` stored in `socket.assigns`.
  </action>
  <verify>
    <automated>cd scrypath_ops &amp;&amp; mix test</automated>
  </verify>
  <done>LiveViews compile and tests pass without verified routes errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| None | This is a purely internal navigation refactor. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-03 | Spoofing | Navigation | accept | Path is derived from the macro `mount_path` securely set by the host router. |
</threat_model>

<verification>
Full compilation succeeds with zero `~p` route errors and `mix test` passes.
</verification>

<success_criteria>
Navigation within the engine is agnostic to the host path.
</success_criteria>

<output>
Create `.planning/phases/102-admin-ui-router-engine-refactor/102-03-SUMMARY.md` when done
</output>
