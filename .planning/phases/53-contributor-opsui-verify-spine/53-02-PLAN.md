---
phase: 53-contributor-opsui-verify-spine
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - VRFY-04
user_setup: []

must_haves:
  truths:
    - "README contains the literal monospace substring **`mix verify.opsui`** in contributor-facing context."
    - "README states the command exercises the optional operator app under **`scrypath_ops/`** and links to CONTRIBUTING for the verify matrix / job names — without pasting the CONTRIBUTING table."
  artifacts:
    - README.md
  key_links:
    - "Link target is CONTRIBUTING.md (verify / CI section anchor slug is implementer choice)."
---

<objective>
Add README wayfinding (**D-01**–**D-03**) so default contributor docs visibly surface **`mix verify.opsui`** next to the existing operator UI pointers, with a single link into **`CONTRIBUTING.md`** for the full matrix — no duplicated tables.
</objective>

<threat_model>
**Trust boundary:** Public README.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-02 | Information disclosure | README | MITIGATE | No secrets; only repo-relative CONTRIBUTING link. |
| T-53-02b | Tampering | Links | MITIGATE | Use `./CONTRIBUTING.md` or `CONTRIBUTING.md` style already used elsewhere in README. |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-contributor-opsui-verify-spine/53-CONTEXT.md
@README.md
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>README — verify.opsui sentence + CONTRIBUTING link</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/53-contributor-opsui-verify-spine/53-CONTEXT.md, CONTRIBUTING.md</read_first>
  <action>
    In **`README.md`**, extend the **Operator UI (maintainers)** bullet/paragraph (near the existing **`scrypath_ops/README.md`** and **`operator-ia.md`** links). Add **one** clear sentence (or two short sentences) that includes the **verbatim** substring **`mix verify.opsui`**, states it runs verification for the optional Phoenix operator app under **`scrypath_ops/`** from the **repository root**, and links to **`CONTRIBUTING.md`** for the CI ↔ verify matrix and job names (use the same link style as the Integration smoke paragraph: ``[`CONTRIBUTING.md`](CONTRIBUTING.md)`` with an optional fragment such as `#ci` **only if** that heading exists — otherwise link without fragment). Do **not** paste the CI markdown table from CONTRIBUTING into README. Do **not** introduce requirement-style tokens **`VRFY-*`** into README (published-doc hygiene test).
  </action>
  <verify>grep -n "mix verify.opsui" README.md</verify>
  <acceptance_criteria>
    - `grep -F 'mix verify.opsui' README.md` exits 0.
    - `README.md` contains `CONTRIBUTING.md` as part of a markdown link (pattern `](CONTRIBUTING.md` ).
    - `README.md` contains substring `scrypath_ops` in the same paragraph or within 3 lines after the sentence containing `mix verify.opsui` (grep -n context acceptable).
    - `README.md` does not contain the substring `VRFY-` (hygiene).
    - `grep -F 'scrypath-ops-path-check' README.md` exits 1 (README must not paste the CONTRIBUTING CI matrix slug for the path-check job).
  </acceptance_criteria>
  <done>Acceptance criteria satisfied.</done>
</task>

</tasks>

<verification>
- [ ] `mix test test/scrypath/docs_contract_test.exs` exits 0 (may fail until Plan 03 adds assertions — if so, run again after Plan 03 or run full `mix test` only if README-only change and suite already green)
</verification>

<success_criteria>
- README satisfies must_haves and D-01–D-03
</success_criteria>

<output>
After completion, create `.planning/phases/53-contributor-opsui-verify-spine/53-02-SUMMARY.md`
</output>
