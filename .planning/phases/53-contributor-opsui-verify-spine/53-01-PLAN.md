---
phase: 53-contributor-opsui-verify-spine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/verify.opsui.ex
autonomous: true
requirements:
  - VRFY-04
user_setup: []

must_haves:
  truths:
    - "`Mix.Tasks.Verify.Opsui` exposes a non-empty `@moduledoc` so the task appears in `mix help` and `mix help verify.opsui` shows documentation."
    - Moduledoc stays short (roughly 10–25 lines); prerequisites and matrix detail defer to CONTRIBUTING via one markdown link to `CONTRIBUTING.md`.
  artifacts:
    - lib/mix/tasks/verify.opsui.ex
  key_links:
    - "@moduledoc` includes a relative link to CONTRIBUTING.md for services, env, and job names."
---

<objective>
Replace `@moduledoc false` on **`Mix.Tasks.Verify.Opsui`** with a contributor-facing **`@moduledoc`** (**D-04**–**D-06**, **D-14**) so **`mix help`** lists **`verify.opsui`** and **`mix help verify.opsui`** prints a summary plus a single handoff link to **`CONTRIBUTING.md`** — without duplicating the verify matrix.
</objective>

<threat_model>
**Trust boundary:** Public repo source and Mix CLI help text.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information disclosure | @moduledoc | MITIGATE | No Hex keys, hostnames, or operator credentials; link only to in-repo CONTRIBUTING. |
| T-53-01b | Tampering | Help text | MITIGATE | Describe behavior that matches existing `run/1` (`cd scrypath_ops`, `mix deps.get`, `mix test`, `CI=true`); do not claim Meilisearch or services the job does not use. |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-contributor-opsui-verify-spine/53-CONTEXT.md
@.planning/REQUIREMENTS.md
@lib/mix/tasks/verify.phase11.ex
@lib/mix/tasks/verify.opsui.ex
@CONTRIBUTING.md
</context>

<tasks>

<task type="auto">
  <name>Add @moduledoc to verify.opsui</name>
  <files>lib/mix/tasks/verify.opsui.ex</files>
  <read_first>lib/mix/tasks/verify.opsui.ex, lib/mix/tasks/verify.phase11.ex, .planning/phases/53-contributor-opsui-verify-spine/53-CONTEXT.md, CONTRIBUTING.md</read_first>
  <action>
    Remove **`@moduledoc false`**. Insert **`@moduledoc """ ... """`** immediately after **`use Mix.Task`** (same placement style as **`Mix.Tasks.Verify.Phase11`**). The body must state, in plain sentences: (1) purpose — run **`scrypath_ops`** tests the same way the **`scrypath-ops`** GitHub Actions job does; (2) when to run — changes under **`scrypath_ops/`**, **`lib/`**, root **`mix.exs`**, **`mix.lock`**, or **`scrypath_ops/mix.lock`** (mirror CONTRIBUTING path-gate language); (3) what it runs — from repo root, non-interactive **`cd scrypath_ops`**, **`mix deps.get`**, **`mix test`** with **`CI=true`** (mention **`printf`** / Hex prompt guard only if you keep parity with the existing script comment); (4) prerequisites summary — Postgres-backed tests, no Meilisearch (aligned with CONTRIBUTING row for **`scrypath-ops`**); (5) no arguments; (6) one markdown link **`[CONTRIBUTING.md](CONTRIBUTING.md)`** (or **`../CONTRIBUTING.md`** if you choose repo-root-relative from `lib/` — pick one form that renders in ExDoc and reads correctly in `mix help`). Keep existing **`@shortdoc`** line unchanged unless a typo fix is required. Do not change **`run/1`** behavior in this task.
  </action>
  <verify>mix help verify.opsui | head -n 40</verify>
  <acceptance_criteria>
    - `grep -n '@moduledoc false' lib/mix/tasks/verify.opsui.ex` exits 1 (no matches).
    - `lib/mix/tasks/verify.opsui.ex` matches `grep -E '@moduledoc\s+"""' lib/mix/tasks/verify.opsui.ex` exit 0.
    - `lib/mix/tasks/verify.opsui.ex` contains substring `CONTRIBUTING.md` inside the `"""` moduledoc block.
    - `lib/mix/tasks/verify.opsui.ex` contains substring `scrypath-ops` OR `scrypath_ops` in the moduledoc (CI job naming).
    - `lib/mix/tasks/verify.opsui.ex` still contains `@shortdoc "Runs ScrypathOps` (prefix match for the existing shortdoc).
    - From repo root after `mix compile`, `mix help verify.opsui 2>&1 | grep -F 'scrypath_ops'` exits 0 (summary mentions the operator app path or name).
  </acceptance_criteria>
  <done>All acceptance criteria pass from repo root with deps compiled.</done>
</task>

</tasks>

<verification>
- [ ] `mix format --check-formatted` exits 0
- [ ] `mix compile --warnings-as-errors` exits 0
- [ ] `mix help verify.opsui` prints the new moduledoc (non-zero length body)
</verification>

<success_criteria>
- Task complete per acceptance criteria
- No change to `run/1` semantics unless a separate explicitly scoped task says otherwise
</success_criteria>

<output>
After completion, create `.planning/phases/53-contributor-opsui-verify-spine/53-01-SUMMARY.md`
</output>
