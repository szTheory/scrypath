---
phase: 53-contributor-opsui-verify-spine
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - VRFY-03
  - VRFY-04
user_setup: []

must_haves:
  truths:
    - "Doc-contract tests lock README + CONTRIBUTING presence of **`mix verify.opsui`** and CONTRIBUTING ↔ **`ci.yml`** step ordering for **`scrypath-ops`**."
    - "Optional stable markers on **`verify.opsui`** Mix module remain grep-verifiable without snapshotting log strings."
  artifacts:
    - test/scrypath/docs_contract_test.exs
  key_links:
    - "Tests read `@readme`, `@contributing`, `@ci_workflow` already loaded in the module."
---

<objective>
Extend **`test/scrypath/docs_contract_test.exs`** (**D-07**–**D-11**) with bounded **`phoenix-example-integration`-style** assertions for **`scrypath-ops`**, README presence of **`mix verify.opsui`**, and optional **`lib/mix/tasks/verify.opsui.ex`** structural markers — completing **VRFY-03** / **VRFY-04** mechanical honesty without a substring museum.
</objective>

<threat_model>
**Trust boundary:** Maintainer test suite reading tracked files only.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03 | Information disclosure | Tests | MITIGATE | No secrets in new tests; only assert public file paths and commands. |
| T-53-03b | Repudiation | CI parity | MITIGATE | Split anchors must be unique substrings so tests fail if YAML renames jobs without updating docs. |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-contributor-opsui-verify-spine/53-CONTEXT.md
@test/scrypath/docs_contract_test.exs
@.github/workflows/ci.yml
@lib/mix/tasks/verify.opsui.ex
</context>

<tasks>

<task type="auto">
  <name>docs_contract_test — OPSUI verify spine contracts</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>test/scrypath/docs_contract_test.exs, .planning/phases/53-contributor-opsui-verify-spine/53-CONTEXT.md, .github/workflows/ci.yml, lib/mix/tasks/verify.opsui.ex, README.md, CONTRIBUTING.md</read_first>
  <action>
    1. Near existing **`@verify_phase*`** module attributes, add **`@verify_opsui File.read!("lib/mix/tasks/verify.opsui.ex")`**.
    2. Add test **`README surfaces mix verify.opsui for optional scrypath_ops (Phase 53)`** asserting **`String.contains?(@readme, "mix verify.opsui")`**.
    3. Add test **`CONTRIBUTING scrypath-ops row matches ci.yml mix ordering (Phase 53)`** mirroring the Phase 51 phoenix test: assert **`String.contains?(@contributing, "scrypath-ops")`**; split **`@contributing`** on the literal substring **`"`scrypath-ops-path-check` / `scrypath-ops`"`** (including backticks — this is the spine inside the CONTRIBUTING table cell) with **`parts: 2`**; on the tail **`row_tail`**, assert **`ordered?(row_tail, "cd scrypath_ops", "mix deps.get")`** and **`ordered?(row_tail, "mix deps.get", "mix test")`**. Split **`@ci_workflow`** on **`"\n  scrypath-ops:\n"`** with **`parts: 2`** (YAML job id for **`scrypath-ops`** only). Let **`[_before, from_job] = ...`**, set **`job_window = String.slice(from_job, 0, 4000)`**, then assert the same **`ordered?/3`** pairs on **`job_window`**.
    4. Add test **`verify.opsui Mix task keeps orchestration markers (Phase 53)`** asserting **`String.contains?(@verify_opsui, "cd: ops_dir")`**, **`String.contains?(@verify_opsui, "mix test")`**, and **`String.contains?(@verify_opsui, "ensure_no_args!")`**.
    Keep **`mix format`** compliance. Do not add **`VRFY-`** tokens to any **`@published_markdown_for_hygiene`** path.
  </action>
  <verify>mix test test/scrypath/docs_contract_test.exs --trace</verify>
  <acceptance_criteria>
    - `mix test test/scrypath/docs_contract_test.exs` exits 0.
    - `mix format --check-formatted` exits 0.
    - `grep -n "README surfaces mix verify.opsui" test/scrypath/docs_contract_test.exs` exits 0.
    - `grep -n "CONTRIBUTING scrypath-ops row matches ci.yml" test/scrypath/docs_contract_test.exs` exits 0.
    - `grep -n "@verify_opsui" test/scrypath/docs_contract_test.exs` exits 0.
    - `grep -n "verify.opsui Mix task keeps orchestration markers" test/scrypath/docs_contract_test.exs` exits 0.
  </acceptance_criteria>
  <done>All acceptance criteria pass.</done>
</task>

</tasks>

<verification>
- [ ] `mix test test/scrypath/docs_contract_test.exs` exits 0
- [ ] `mix format --check-formatted` exits 0
</verification>

<success_criteria>
- VRFY-03 / VRFY-04 mechanical coverage in docs_contract_test per CONTEXT D-07–D-11
</success_criteria>

<output>
After completion, create `.planning/phases/53-contributor-opsui-verify-spine/53-03-SUMMARY.md`
</output>
