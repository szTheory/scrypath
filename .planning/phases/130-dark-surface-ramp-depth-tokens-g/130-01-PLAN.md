---
phase: 130
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/scrypath_ecommerce/package.json
  - examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs
  - scrypath_ops/mix.exs
autonomous: true
requirements:
  - DARKTOKEN-01

must_haves:
  truths:
    - "node e2e/light-pixel-diff.mjs exits non-zero when any light PNG differs, exits 0 when all 20 match baseline"
    - "pixelmatch and pngjs are listed as devDependencies in examples/scrypath_ecommerce/package.json"
    - "mix verify.opsui is a valid alias that runs mix test and mix opsui.test_a11y"
  artifacts:
    - path: "examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs"
      provides: "Disposable Wave 0 light pixel-identity gate script (20 PNGs, pixelmatch diff, exit-code contract)"
    - path: "examples/scrypath_ecommerce/package.json"
      provides: "devDependencies with pixelmatch + pngjs added"
    - path: "scrypath_ops/mix.exs"
      provides: "verify.opsui alias that runs [test, opsui.test_a11y]"
  key_links:
    - from: "examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs"
      to: ".tmp/admin-screenshots/*--light--*.png"
      via: "BASELINE_DIR path resolution using path.resolve(__dirname, ../../.tmp/admin-screenshots)"
      pattern: "BASELINE_DIR.*admin-screenshots"
    - from: "examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs"
      to: "pixelmatch"
      via: "npm import"
      pattern: "import.*pixelmatch"
---

<objective>
Scaffold the Phase 130 light pixel-identity gate before any CSS moves. This Wave 0 plan:
(a) adds `pixelmatch` and `pngjs` to `examples/scrypath_ecommerce/package.json` devDependencies,
(b) writes `light-pixel-diff.mjs` — the disposable Node script that re-shoots 20 light-only admin
    PNGs and pixelmatch-diffs them against the `.tmp/admin-screenshots/*--light--*` Jun-3 baseline,
(c) adds the `verify.opsui` alias to `scrypath_ops/mix.exs` so subsequent tasks and milestone docs
    can use the documented shorthand.

Purpose: VALIDATION.md Wave 0 requirements are unblocked — no subsequent task can satisfy
DARKTOKEN-01-c without this script. The alias fixes the D-13 doc-drift by making `mix verify.opsui`
literal rather than aspirational.

Output:
- `examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs` (new file)
- `examples/scrypath_ecommerce/package.json` (pixelmatch ^5.3.0 + pngjs ^7.0.0 added to devDependencies)
- `scrypath_ops/mix.exs` (verify.opsui alias added)
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/scrypath/.planning/PROJECT.md
@/Users/jon/projects/scrypath/.planning/ROADMAP.md
@/Users/jon/projects/scrypath/.planning/STATE.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-CONTEXT.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-RESEARCH.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-PATTERNS.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From examples/scrypath_ecommerce/package.json (current devDependencies — add to this):
```json
{
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.54.2"
  }
}
```

From examples/scrypath_ecommerce/e2e/admin_screenshot_matrix.spec.ts (light PNG naming convention):
  Files follow pattern: `NN-screen--light--viewport--state.png`
  Light-only files: those whose name includes `--light--`
  Baseline directory: `../../.tmp/admin-screenshots/` relative to `e2e/`
  20 light-only PNG files confirmed present in `.tmp/admin-screenshots/`

From examples/scrypath_ecommerce/contrast-checker.mjs (structural analog for the new script):
  - Module-level ESM with `import { readFile, readdir, mkdir, writeFile } from "node:fs/promises"`
  - `import path from "node:path"` and `import { fileURLToPath } from "node:url"`
  - `const __dirname = path.dirname(fileURLToPath(import.meta.url))`
  - Paths resolved with `path.resolve(__dirname, "../../...")`
  - `async function main()` entry, called via `main().catch(err => { console.error(...); process.exit(2); })`
  - Exit-code is the gate verdict: 0 = pass, 1 = any failure, 2 = script error

From scrypath_ops/mix.exs (current aliases block, L75-94):
  test: ["scrypath_ops.check_nav_contract", "ecto.create --quiet", "ecto.migrate --quiet", "test"]
  "opsui.test_a11y": &opsui_test_a11y/1
  (no verify.opsui alias — D-13 drift)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pixelmatch + pngjs to package.json and write light-pixel-diff.mjs</name>
  <files>examples/scrypath_ecommerce/package.json, examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs</files>

  <read_first>
    - examples/scrypath_ecommerce/package.json — read current devDependencies before editing (must preserve @axe-core/playwright and @playwright/test versions exactly)
    - examples/scrypath_ecommerce/contrast-checker.mjs — read the header block (L1-40), path-resolution pattern, and exit-code pattern (L807-815) to mirror the structural style
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-PATTERNS.md — Pattern 7 (light-pixel-diff.mjs section) has the complete structural template with imports, BASELINE_DIR, FRESH_DIR, filter pattern, pass/fail accumulation, and exit-code
    - .tmp/admin-screenshots/ — run `ls` to confirm the 20 light PNG filenames and the exact naming convention before writing the filter regex
  </read_first>

  <action>
    Add pixelmatch and pngjs to package.json: in the `devDependencies` object add `"pixelmatch": "^5.3.0"` and `"pngjs": "^7.0.0"` alongside the existing entries. Do not alter script entries or existing dep versions.

    Write `examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs` following the structural analog in PATTERNS.md Pattern 7:

    Module header comment: identify it as the disposable Phase 130 DARKTOKEN-01-c gate; document usage as `node e2e/light-pixel-diff.mjs` from `examples/scrypath_ecommerce/`; note it requires pixelmatch and pngjs; note Phase 136 owns the full 40-shot re-capture and this script is light-only.

    Imports: `pixelmatch` (default import), `{ PNG }` from `pngjs`, `{ readFile, readdir, writeFile, mkdir }` from `node:fs/promises`, `path` from `node:path`, `{ fileURLToPath }` from `node:url`, `{ execSync }` from `node:child_process`.

    Path setup: derive `__dirname` from `fileURLToPath(import.meta.url)`. Set `BASELINE_DIR = path.resolve(__dirname, "../../.tmp/admin-screenshots")`. Set `FRESH_DIR = process.env.PIXEL_DIFF_FRESH_DIR ?? path.resolve(__dirname, "../../.tmp/pixel-diff-fresh")`. Set `DIFF_DIR = process.env.PIXEL_DIFF_DIFF_DIR ?? path.resolve(__dirname, "../../.tmp/pixel-diff-out")`.

    Light PNG filter: list `BASELINE_DIR`, filter for files matching `--light--` and ending `.png`. This produces the 20 baseline light files. If the list is empty, log an error and exit 2.

    Re-shoot step: for each light filename, re-shoot the same screen by invoking Playwright CLI via `execSync` with `--project=chromium` and `--grep` matching the test title derived from the filename, saving to `FRESH_DIR`. Alternatively — simpler path — accept an env var `PIXEL_DIFF_FRESH_DIR` pointing to pre-shot fresh PNGs (allowing the caller to run the Playwright matrix first). If `FRESH_DIR` does not contain the expected file, log `SKIP: filename (fresh PNG not found)` and increment `totalFail` for that file.

    pixelmatch loop: for each baseline light PNG, read baseline PNG with `pngjs.PNG.sync.read`, read the corresponding fresh PNG from `FRESH_DIR`, assert both have equal width/height (if not, count as fail), create an output diff PNG buffer, call `pixelmatch(baseline.data, fresh.data, diff.data, width, height, { threshold: 0 })`, write diff to `DIFF_DIR/filename` if diffCount > 0, accumulate `totalFail`.

    Summary output and exit: print `OK: filename` for 0-diff pairs, `DIFF: filename — N px differ` for failures. Print final `Light pixel-diff: PASS` or `FAIL` with counts. `process.exit(totalFail > 0 ? 1 : 0)`.

    Wrap in `async function main()` called via `main().catch(err => { console.error("light-pixel-diff error:", err.message); process.exit(2); })`.

    Run `npm install` from `examples/scrypath_ecommerce/` after writing package.json to install the new devDeps (this updates package-lock.json).
  </action>

  <verify>
    <automated>cd /Users/jon/projects/scrypath/examples/scrypath_ecommerce && node -e "import('./e2e/light-pixel-diff.mjs').catch(e => { if (e.code === 'ERR_IMPORT_ASSERTION_TYPE_MISSING' || e.message.includes('Cannot find module')) { process.exit(1); } })" 2>&1; node --input-type=module --eval "import { createRequire } from 'module'; const r = createRequire(import.meta.url); r('pixelmatch'); r('pngjs'); console.log('deps OK')" 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `examples/scrypath_ecommerce/package.json` devDependencies contains `"pixelmatch": "^5.3.0"` and `"pngjs": "^7.0.0"` alongside the existing entries
    - `examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs` exists and is a valid ESM module (node --check passes)
    - The script imports `pixelmatch` and `{ PNG }` from `pngjs`
    - `BASELINE_DIR` resolves to `../../.tmp/admin-screenshots` relative to the script file
    - The light-PNG filter uses `--light--` inclusion and `.png` suffix
    - `process.exit(0)` on 0 total failures; `process.exit(1)` on any diffCount > 0; `process.exit(2)` on script error
    - `npm install` ran successfully (package-lock.json updated)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add verify.opsui alias to scrypath_ops/mix.exs (D-13)</name>
  <files>scrypath_ops/mix.exs</files>

  <read_first>
    - scrypath_ops/mix.exs — read the entire `defp aliases do` block (L75-94) to confirm the exact current structure before editing; confirm `"opsui.test_a11y"` is present and `"verify.opsui"` is absent
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-RESEARCH.md — Open Questions #1 (verify.opsui alias section) for the exact alias string to add
  </read_first>

  <action>
    In `scrypath_ops/mix.exs`, in the `defp aliases do` block, add one entry after `"opsui.test_a11y"`:
    `"verify.opsui": ["test", "opsui.test_a11y"]`

    This makes `mix verify.opsui` a real runnable target that executes `mix test` followed by `mix opsui.test_a11y` — matching the D-11 proof bundle step 1. Keeps the existing alias entries byte-identical; only inserts the new line. Run `mix compile` from `scrypath_ops/` to confirm no syntax errors.

    Then run `mix test` from `scrypath_ops/` to confirm the suite stays green after the mix.exs edit.
  </action>

  <verify>
    <automated>cd /Users/jon/projects/scrypath/scrypath_ops && grep -c '"verify.opsui"' mix.exs</automated>
  </verify>

  <acceptance_criteria>
    - `grep '"verify.opsui"' scrypath_ops/mix.exs` returns exactly 1 match
    - The alias value is `["test", "opsui.test_a11y"]`
    - `mix compile` from `scrypath_ops/` exits 0 with no warnings about the aliases block
    - `mix test` from `scrypath_ops/` exits 0 (all tests pass)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node.js script → local filesystem | `light-pixel-diff.mjs` reads PNG files from `.tmp/admin-screenshots/`; path resolution is local-only |
| npm install → devDependencies | Adding pixelmatch/pngjs to devDependencies; these are build-time tools, not runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-01 | Tampering | `light-pixel-diff.mjs` file paths | accept | Script reads from `.tmp/admin-screenshots/` (local gitignored dir, no user input); all paths are resolved from `__dirname` with `path.resolve` — no path traversal possible from caller |
| T-130-02 | Spoofing | pixelmatch/pngjs npm packages | mitigate | Packages are widely-used, well-known — pixelmatch `^5.3.0` (800k+ weekly downloads), pngjs `^7.0.0` (5M+ weekly downloads); no auth tokens or sensitive data involved in this dev-only gate script |
| T-130-03 | Information Disclosure | Admin screenshots in `.tmp/` | accept | `.tmp/` is gitignored; screenshots are local admin UI dev artifacts, not production user data |
| T-130-SC | Tampering | npm install (pixelmatch, pngjs) | accept | Both packages are established, high-download npm packages with stable histories; slopcheck: pixelmatch (5.3.0) and pngjs (7.0.0) are the canonical versions at their stated semver |
| T-130-04 | Elevation of Privilege | mix.exs alias addition | accept | Adding `verify.opsui` alias only runs existing `mix test` + `mix opsui.test_a11y` targets; no new code execution surface; alias is read-only composition |
</threat_model>

<verification>
After both tasks complete:

1. `node --check examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs` — syntax valid
2. `grep '"pixelmatch"' examples/scrypath_ecommerce/package.json` — present
3. `grep '"pngjs"' examples/scrypath_ecommerce/package.json` — present
4. `grep '"verify.opsui"' scrypath_ops/mix.exs` — present
5. `cd scrypath_ops && mix test` — green (no regressions from mix.exs edit)
</verification>

<success_criteria>
- `light-pixel-diff.mjs` exists, passes `node --check`, imports pixelmatch and pngjs
- `pixelmatch` and `pngjs` are installed in `examples/scrypath_ecommerce/node_modules/`
- `mix verify.opsui` is a runnable alias in `scrypath_ops/mix.exs`
- `mix test` from `scrypath_ops/` exits 0
- VALIDATION.md Wave 0 requirements are satisfied
</success_criteria>

<output>
Create `.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-01-SUMMARY.md` when done.
</output>
