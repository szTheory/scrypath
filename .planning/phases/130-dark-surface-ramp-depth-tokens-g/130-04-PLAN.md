---
phase: 130
plan: 04
type: execute
wave: 4
depends_on:
  - 130-03
files_modified:
  - scrypath_ops/assets/css/DESIGN-TOKENS.md
autonomous: false
requirements:
  - DARKTOKEN-01

must_haves:
  truths:
    - "mix verify.opsui (from scrypath_ops/) exits 0 — both mix test and mix opsui.test_a11y green"
    - "node contrast-checker.mjs light AA/AAA counts are unchanged vs Phase 128 baseline (DARKTOKEN-01-f)"
    - "npm run test:e2e:admin-contrast from examples/scrypath_ecommerce/ exits 0; dark cluster 1 (.leading-4 1.08:1 ramp collapse) violations drop to 0 (DARKTOKEN-01-a/b)"
    - "node e2e/light-pixel-diff.mjs from examples/scrypath_ecommerce/ exits 0 with 0 diff pixels (DARKTOKEN-01-c)"
    - "DESIGN-TOKENS.md has an elevation-surfaces subsection with the 4-step dark ramp table (DARKTOKEN-01-g)"
  artifacts:
    - path: "scrypath_ops/assets/css/DESIGN-TOKENS.md"
      provides: "Elevation surfaces subsection recording --ops-bg/surface-1/surface-2 with dark/light values and ramp narrative"
      contains: "--ops-surface-2"
  key_links:
    - from: "scrypath_ops/assets/css/DESIGN-TOKENS.md"
      to: "scrypath_ops/assets/css/app.css @plugin blocks"
      via: "Lockstep documentation: values in DESIGN-TOKENS.md must match app.css declarations"
      pattern: "--ops-surface-2.*#1b2230"
---

<objective>
Run the full D-11 proof bundle in order and update `DESIGN-TOKENS.md` with the elevation-surface
subsection recording the 4-step dark ramp. This plan is the Phase 130 `[G]` gate close.

D-11 proof bundle (run in this order):
  1. `mix verify.opsui` — Elixir unit suite + a11y tag pass (DARKTOKEN-01-d/e)
  2. `node contrast-checker.mjs` — light AA/AAA counts unchanged vs Phase 128 baseline (DARKTOKEN-01-f)
  3. `npm run test:e2e:admin-contrast` — dark cluster 1 resolves; 0 AA failures for the ramp-collapse class (DARKTOKEN-01-a/b)
  4. `node e2e/light-pixel-diff.mjs` — 0 diff pixels across 20 light PNGs (DARKTOKEN-01-c)
  5. DESIGN-TOKENS.md elevation-surface subsection added (DARKTOKEN-01-g)

Purpose: Prove the ramp via contrast clusters resolving + light pixel-diff, not a full 40-shot
re-capture (D-12: full matrix deferred to Phase 136). Each gate step runs first and must pass
before proceeding to the next.

Output: `scrypath_ops/assets/css/DESIGN-TOKENS.md` updated with elevation-surface subsection.
        Gate evidence logged in 130-04-SUMMARY.md.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/scrypath/.planning/ROADMAP.md
@/Users/jon/projects/scrypath/.planning/STATE.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-CONTEXT.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-RESEARCH.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-PATTERNS.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-03-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From scrypath_ops/mix.exs (aliases added in Plan 01):
  "verify.opsui": ["test", "opsui.test_a11y"]
  Run as: cd scrypath_ops && mix verify.opsui

From examples/scrypath_ecommerce/package.json:
  "test:e2e:admin-contrast": "playwright test e2e/admin_contrast_matrix.spec.ts"
  Run as: cd examples/scrypath_ecommerce && npm run test:e2e:admin-contrast

From examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs (created in Plan 01):
  Run as: cd examples/scrypath_ecommerce && node e2e/light-pixel-diff.mjs
  Pre-condition: the fresh light PNGs must exist in PIXEL_DIFF_FRESH_DIR or .tmp/pixel-diff-fresh/
  The script needs re-shot light PNGs — if PIXEL_DIFF_FRESH_DIR is not pre-populated, run the
  light-only Playwright matrix first:
    cd examples/scrypath_ecommerce && npm run test:e2e:admin-matrix -- --project=chromium --grep "light"
  (or set PIXEL_DIFF_FRESH_DIR to point to a directory of freshly shot light PNGs)

From scrypath_ops/assets/css/DESIGN-TOKENS.md (structure to mirror):
  Existing pattern (L72-78): ## Shadow — with token table
  Existing pattern (L28-41): brand-colors table with | Token | Light | Dark | Use | columns
  New subsection goes after brand-colors table, before ## Spacing (or before ## Shadow)
  Subsection header: ## Elevation surfaces — --ops-bg, --ops-surface-1, --ops-surface-2

From .planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md:
  Phase 128 baseline: 108 total AA violations (incident 22 / all_green 60 / empty 26)
  Cluster 1 (.leading-4 ramp collapse): this must drop to 0 after Phase 130 lands
  Light AA count from contrast-checker.mjs: read from the Phase 128 baseline to compare
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run D-11 proof bundle steps 1-4 in order; document results</name>
  <files></files>

  <read_first>
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-CONTEXT.md — D-11 (proof bundle order and success criteria per step) and D-12 (defer full 40-shot to Phase 136)
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-VALIDATION.md — Per-Task Verification Map (the five criteria DARKTOKEN-01-a through DARKTOKEN-01-g)
    - .planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md — the 108-violation baseline; read the light AA count from the contrast-checker.mjs fast check section to establish the comparison number
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-03-SUMMARY.md — confirm the CSS changes from Plan 03 are committed before running gates
  </read_first>

  <action>
    Run each gate step in order. STOP and report if any step fails — do not proceed to the next step with a failing gate.

    STEP 1 — Code green:
    Run `mix verify.opsui` from `scrypath_ops/`. Expect: exits 0. Record pass/fail + any failure output.

    STEP 2 — Light token proof:
    Run `node contrast-checker.mjs` from `examples/scrypath_ecommerce/`. Record the light AA count and AAA count from the output. Compare against the Phase 128 baseline from `128-CONTRAST-REPORT.md`. The counts MUST be identical. Record pass/fail + actual vs expected counts.

    STEP 3 — Contrast gate in both themes:
    The contrast gate requires a running dev server. Boot the ecommerce dev server on an available port (e.g. WEB_PORT=4012 per the Docker DX memory note). Run `npm run test:e2e:admin-contrast` from `examples/scrypath_ecommerce/` with `CONTRAST_REPORT_DIR` set to a phase-130-specific path (e.g. `.tmp/contrast-report-130/`) to avoid overwriting Phase 128 reports. Record pass/fail + violation count. Dark cluster 1 (`.leading-4` ramp collapse) must drop to 0 failures. Light AA failures should remain at or below the Phase 128 light baseline.

    STEP 4 — Light pixel-identity:
    Re-shoot the 20 light-only PNGs. The light pixel-diff script reads from `.tmp/pixel-diff-fresh/` by default. Either: (a) run a light-only Playwright matrix shot and save to that directory, or (b) set `PIXEL_DIFF_FRESH_DIR` to point to fresh shots. Then run `node e2e/light-pixel-diff.mjs` from `examples/scrypath_ecommerce/`. Expect: 0 diff pixels across all 20 light PNGs, exit 0. Record pass/fail + any pixel-diff count > 0.

    If any step fails: diagnose and fix the regression in the relevant CSS/Elixir file, then re-run from that step. Document what failed and what was fixed.
  </action>

  <verify>
    <automated>cd /Users/jon/projects/scrypath/scrypath_ops && mix verify.opsui 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `mix verify.opsui` from `scrypath_ops/` exits 0 (all Elixir unit tests + a11y tag pass)
    - `node contrast-checker.mjs` light AA count = Phase 128 baseline (unchanged — tokens only moved dark rendering, not light)
    - `npm run test:e2e:admin-contrast` exits 0; dark cluster 1 `.leading-4` AA violations = 0 (ramp collapse resolved by surface-2)
    - `node e2e/light-pixel-diff.mjs` exits 0 with "Light pixel-diff: PASS" and "Failed pairs: 0 / 20"
    - Gate results recorded in 130-04-SUMMARY.md (pass/fail per step + violation count deltas)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The D-11 proof bundle ran — Elixir green, light contrast counts unchanged, dark contrast gate
    passing (cluster 1 resolved), and light pixel-diff confirmed 0 diff pixels. The dark surface
    ramp is now rendering with the #1B2230 surface-2 step. Dark panels (`.ops-muted-panel`,
    `.ops-data-card`, `.ops-verdict-neutral`, `.ops-disclosure`, etc.) are stepping UP in elevation.
    Shadows seat panels with dark-inward `rgba(0,0,0,α)` depth.
  </what-built>
  <how-to-verify>
    1. Boot the admin UI in dark mode (visit http://localhost:4002/admin/search in dark mode, or use the data-theme=dark toggle).
    2. Confirm: `.ops-muted-panel` surfaces (e.g. Sync/Drift section headers, disclosure panels) are visibly LIGHTER than the dark page background (#0c0f14) — they should appear as #1b2230 midnight blue.
    3. Confirm: `.ops-data-card` workspace cards on Playbooks screen are raised above the page background in dark mode.
    4. Confirm: `.ops-panel` (Control Room / Posture intent cards) step up from the page bg as Ink (#141923) over Night (#0c0f14).
    5. Confirm: shadow depth is perceptible on raised panels (dark-inward rgba shadow, subtle).
    6. Toggle to LIGHT mode and confirm: all surfaces look identical to pre-Phase-130 (pixel-identical — the contrast-checker and pixel-diff already proved this, visual double-check).
    7. If anything looks wrong, describe the specific surface and its observed vs expected color.
  </how-to-verify>
  <resume-signal>Type "approved" if visual ramp is correct in dark and light is unchanged, or describe any visual regression.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Add elevation-surface subsection to DESIGN-TOKENS.md</name>
  <files>scrypath_ops/assets/css/DESIGN-TOKENS.md</files>

  <read_first>
    - scrypath_ops/assets/css/DESIGN-TOKENS.md — read the full file to identify the current structure: brand-colors table location, ## Shadow subsection location, and where to insert the new subsection; confirm the table column format (| Token | Light | Dark | Use |)
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-PATTERNS.md — Pattern 6 (DESIGN-TOKENS.md elevation-surface subsection) for the exact prose, table, and ramp sentence to add
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-CONTEXT.md — D-11 step 5 (DESIGN-TOKENS.md update is criterion 3 of the phase success criteria)
  </read_first>

  <action>
    Read `scrypath_ops/assets/css/DESIGN-TOKENS.md` in full. Identify the position after the brand-colors table (`## Brand colors` or equivalent section) and before `## Spacing` or `## Shadow` (whichever comes first).

    Insert a new subsection at that position:

    Section header: `## Elevation surfaces — --ops-bg, --ops-surface-1, --ops-surface-2`

    Introductory prose paragraph: describe that these are named elevation tokens declared in the daisyUI `@plugin` blocks (dark + light), that the plugin spreads them to both `[data-theme="dark"]` and `@media (prefers-color-scheme: dark)` for free, and that the ramp direction in dark is bg (floor) → surface-1 (resting panel) → surface-2 (raised).

    Token table (same column format as existing brand-colors table):
    | Token | Light value | Dark value | Use |
    | `--ops-bg` | `#faf7f2` | `#0c0f14` | Page floor / Night — app background |
    | `--ops-surface-1` | `#fffdf8` | `#141923` | Resting panel / Ink — `.ops-panel`, `.ops-surface-flat`, `.ops-preflight__card` |
    | `--ops-surface-2` | `#faf7f2` | `#1b2230` | Raised / muted step — `.ops-muted-panel`, `.ops-disclosure`, `.ops-nav-list`, `.ops-kbd`, `.ops-verdict-neutral`, `.ops-preflight__card--locked` |

    Ramp sentence (after the table): "Dark 4-step midnight ramp: `#0C0F14` (bg) → `#141923` (surface-1) → `#1B2230` (surface-2) → `#2A3446` (base-300 / borders). Light values are byte-identical to the prior `base-200`/`base-100` references — zero light regression."

    Do not alter any other section of DESIGN-TOKENS.md.
  </action>

  <verify>
    <automated>grep -v "^[[:space:]]*$" /Users/jon/projects/scrypath/scrypath_ops/assets/css/DESIGN-TOKENS.md | grep -c "ops-surface-2"</automated>
  </verify>

  <acceptance_criteria>
    - `grep 'ops-surface-2' scrypath_ops/assets/css/DESIGN-TOKENS.md` returns at least 2 matches (table row + ramp sentence)
    - The elevation-surfaces subsection header `## Elevation surfaces` appears in DESIGN-TOKENS.md
    - The table contains `#1b2230` (dark value for surface-2) and `#fffdf8` (light value for surface-1)
    - The ramp sentence contains all four hex values: `#0C0F14`, `#141923`, `#1B2230`, `#2A3446`
    - The existing sections (brand-colors, shadow, spacing, etc.) are unchanged
    - `mix test` from `scrypath_ops/` exits 0 (confirming no accidental edits to test-adjacent files)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test runner → running dev server | Contrast gate requires a live server at WEB_PORT; server must be seeded with incident scenario |
| light-pixel-diff.mjs → .tmp/ PNG files | Reads local gitignored screenshot files; no network, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-12 | Tampering | DESIGN-TOKENS.md documentation values | accept | Documentation values are read from app.css source of truth; the lockstep rule ("change a value here only by changing it in app.css") is the established contract; no executable code |
| T-130-13 | Denial of Service | contrast gate server not running | mitigate | Plan documents that a dev server must be booted before running `npm run test:e2e:admin-contrast`; plan uses WEB_PORT per Docker DX memory note |
| T-130-14 | Information Disclosure | contrast report written to .tmp/ | accept | .tmp/ is gitignored; contrast reports contain only CSS selector and contrast ratio data from the admin UI, not production user data |
| T-130-15 | Tampering | pixel-diff fresh PNGs captured under wrong theme | mitigate | The re-shoot step must explicitly set the light theme before capturing; `light-pixel-diff.mjs` filters for `--light--` in the baseline names; mismatch between baseline and fresh theme would produce diffCount > 0, which is the gate signal |
</threat_model>

<verification>
Phase 130 `[G]` gate is closed when ALL of the following hold:

1. `mix verify.opsui` from `scrypath_ops/` — 0 failures
2. `node contrast-checker.mjs` from `examples/scrypath_ecommerce/` — light AA count = Phase 128 baseline
3. `npm run test:e2e:admin-contrast` — exits 0; dark cluster 1 violations = 0
4. `node e2e/light-pixel-diff.mjs` — exits 0; "Failed pairs: 0 / 20"
5. `grep 'Elevation surfaces' scrypath_ops/assets/css/DESIGN-TOKENS.md` — subsection present
6. Human visual verification: dark ramp visible in admin UI; light theme unchanged
</verification>

<success_criteria>
1. Dark renders a true four-step ramp: bg #0C0F14 → panel #141923 → raised/muted #1B2230 → border #2A3446 (ROADMAP criterion 1)
2. .ops-muted-panel, .ops-data-card, .ops-surface-flat, .ops-nav-list, .ops-disclosure, .ops-kbd, .ops-result-row, .ops-preflight__card--locked step up (not down) in dark; confirmed by dark cluster 1 AA pass (ROADMAP criterion 2)
3. Light theme is pixel-identical: contrast-checker light counts unchanged + light-pixel-diff exits 0 with 0 diff pixels; DESIGN-TOKENS.md records the dark ramp (ROADMAP criterion 3)
4. DARKTOKEN-01 closed
</success_criteria>

<output>
Create `.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-04-SUMMARY.md` when done.
Update `.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-VALIDATION.md`: set nyquist_compliant: true and wave_0_complete: true; mark DARKTOKEN-01-a through DARKTOKEN-01-g as green.
</output>
