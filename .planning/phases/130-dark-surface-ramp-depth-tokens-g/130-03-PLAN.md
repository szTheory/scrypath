---
phase: 130
plan: 03
type: execute
wave: 3
depends_on:
  - 130-02
files_modified:
  - scrypath_ops/assets/css/app.css
  - scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex
autonomous: true
requirements:
  - DARKTOKEN-01

must_haves:
  truths:
    - "9 token-swappable recipes reference --ops-surface-1 or --ops-surface-2 instead of --color-base-100/--color-base-200 inside their color-mix wrapper"
    - ".ops-data-card and .ops-result-row still use var(--color-base-100) in their shared recipe AND have dark-scoped overrides setting background: var(--ops-surface-2) under both dark paths"
    - ".bg-ops-surface-2 { background: var(--ops-surface-2); } helper class exists in app.css"
    - "ops_code_block :default at ops_ui.ex:994 uses bg-ops-surface-2 instead of bg-base-200"
    - "--shadow-ops-surface/mid/raised/overlay are overridden with rgba(0,0,0,α) values under both [data-theme=dark] and @media(prefers-color-scheme:dark) html:not([data-theme=light])"
    - "mix test from scrypath_ops/ exits 0"
    - "node contrast-checker.mjs light AA/AAA counts unchanged vs Phase 128 baseline"
  artifacts:
    - path: "scrypath_ops/assets/css/app.css"
      provides: "9 token-swapped recipes + .bg-ops-surface-2 helper + D-05 dark-scoped overrides for data-card/result-row + D-10 shadow dual-path override"
      contains: "--ops-surface-2"
    - path: "scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex"
      provides: "ops_code_block :default using bg-ops-surface-2"
  key_links:
    - from: "scrypath_ops/assets/css/app.css (.ops-muted-panel L256)"
      to: "var(--ops-surface-2)"
      via: "token-swap of inner var(--color-base-200) inside color-mix wrapper"
      pattern: "ops-muted-panel.*ops-surface-2"
    - from: "scrypath_ops/assets/css/app.css (.ops-kbd L1155)"
      to: "var(--ops-surface-2)"
      via: "token-swap of inner var(--color-base-200) 70% inside color-mix wrapper"
      pattern: "ops-kbd.*ops-surface-2"
    - from: "scrypath_ops/assets/css/app.css ([data-theme=dark] .ops-data-card)"
      to: "var(--ops-surface-2)"
      via: "D-05 dark-scoped override — solid value no color-mix wrapper"
      pattern: "ops-data-card.*ops-surface-2"
    - from: "scrypath_ops/assets/css/app.css ([data-theme=dark])"
      to: "--shadow-ops-surface: 0 1px 2px rgba(0,0,0,0.40)"
      via: "D-10 shadow dual-path override"
      pattern: "shadow-ops-surface.*rgba"
    - from: "scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex:994"
      to: "bg-ops-surface-2"
      via: "Tailwind utility class backed by .bg-ops-surface-2 CSS helper"
      pattern: "bg-ops-surface-2"
---

<objective>
Apply the recipe routing layer — the bulk of the Phase 130 CSS work. This plan contains three
groups of changes to `app.css` and one to `ops_ui.ex`:

GROUP A — 9 shared token-swaps (D-03): swap only the inner `var(--color-base-1xx)` to
`var(--ops-surface-1)` or `var(--ops-surface-2)` in the `color-mix(in oklch, … N%, transparent)`
wrapper. The `color-mix` wrapper and its alpha percentage are preserved verbatim.

GROUP B — D-05 carve-outs + `.bg-ops-surface-2` helper:
  - Add `.bg-ops-surface-2 { background: var(--ops-surface-2); }` helper class (Wave 2 prerequisite
    for `ops_ui.ex` DK-09 fix — Tailwind v4 does not auto-generate bg-* utilities for @plugin tokens).
  - Add dark-scoped dual-path overrides for `.ops-data-card` and `.ops-result-row` only
    (background: var(--ops-surface-2), solid, no color-mix wrapper) following the L525-533 precedent.

GROUP C — D-10 shadow dual-path override: hand-author the four `--shadow-ops-*` values under
`[data-theme="dark"]` AND `@media (prefers-color-scheme: dark) { html:not([data-theme="light"]) }`
with the dark-inward `rgba(0,0,0,α)` ladder from D-08.

GROUP D — ops_ui.ex DK-09: change `bg-base-200` → `bg-ops-surface-2` at L994 in `ops_code_block`.

Purpose: After this commit the 4-step dark ramp is rendered; dark surfaces step up; light is
byte-identical (color-mix inputs unchanged; dark-only overrides do not fire in light).

Output: `scrypath_ops/assets/css/app.css` (all groups A–C) and
        `scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` (group D).
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/scrypath/.planning/ROADMAP.md
@/Users/jon/projects/scrypath/.planning/STATE.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-CONTEXT.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-RESEARCH.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-PATTERNS.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

RECIPE SWAP MAP (from RESEARCH.md — verified against live file):

  Token-swap recipes (GROUP A — change inner token only, preserve wrapper + alpha):
  | app.css line | Class | Current inner | New inner |
  | L243 | .ops-panel | var(--color-base-100) | var(--ops-surface-1) |
  | L250 | .ops-surface-flat | var(--color-base-100) | var(--ops-surface-1) |
  | L256 | .ops-muted-panel | var(--color-base-200) | var(--ops-surface-2) |
  | L357 | .ops-verdict-neutral | var(--color-base-200) | var(--ops-surface-2) |
  | L557 | .ops-nav-list | var(--color-base-200) | var(--ops-surface-2) |
  | L594 | .ops-disclosure | var(--color-base-200) | var(--ops-surface-2) |
  | L792 | .ops-preflight__card (base) | var(--color-base-100) | var(--ops-surface-1) |
  | L801 | .ops-preflight__card--locked | var(--color-base-200) | var(--ops-surface-2) |
  | L1155 | .ops-kbd | var(--color-base-200) | var(--ops-surface-2) |

  D-05 carve-out recipes (GROUP B — leave shared recipe unchanged; add dark-scoped override):
  | L262 | .ops-data-card | var(--color-base-100) stays | dark-scoped override: background: var(--ops-surface-2) |
  | L912 | .ops-result-row | var(--color-base-100) stays | dark-scoped override: background: var(--ops-surface-2) |

  SKIP: .ops-notice-surface (L270-277) — NO background: property; fill from .ops-tone-* classes. Do NOT swap.

  Light parity note for .ops-kbd: the current inner token is var(--color-base-200) = light #faf7f2 =
  light --ops-surface-2 = #faf7f2. The swap is byte-identical in light — no D-05 carve-out needed.

D-10 SHADOW LADDER VALUES:
  --shadow-ops-surface: 0 1px 2px rgba(0,0,0,0.40)
  --shadow-ops-mid:     0 1px 4px rgba(0,0,0,0.45)
  --shadow-ops-raised:  0 2px 10px rgba(0,0,0,0.50)
  --shadow-ops-overlay: 0 8px 24px rgba(0,0,0,0.55)

DUAL-PATH PRECEDENT (app.css L525-533 — the select.ops-form-control block):
  [data-theme="dark"] .TARGET { ... }
  @media (prefers-color-scheme: dark) {
    html:not([data-theme="light"]) .TARGET { ... }
  }
  Note: always html:not([data-theme="light"]) — NEVER html:not([data-theme="dark"])

ops_ui.ex LINE:
  L994: @variant == :default && "max-h-96 bg-base-200 p-ops-3"
  Change to: @variant == :default && "max-h-96 bg-ops-surface-2 p-ops-3"
  (L995 and L996 stay unchanged — bg-base-100 and bg-base-100/70)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply GROUP A token-swaps and GROUP B helper+overrides in app.css</name>
  <files>scrypath_ops/assets/css/app.css</files>

  <read_first>
    - scrypath_ops/assets/css/app.css — read EACH target line before editing; use targeted reads at L240-265 (panel/flat/muted/data-card), L350-360 (verdict-neutral), L550-560 (nav-list), L590-598 (disclosure), L788-808 (preflight cards), L905-920 (result-row), L1152-1159 (.ops-kbd) to confirm the EXACT current background: value before swapping
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-PATTERNS.md — Pattern 2 (the swap rule table, includes .ops-kbd L1155) and Pattern 3 (D-05 dark-scoped override pattern + .bg-ops-surface-2 helper)
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-RESEARCH.md — D-05 carve-out detail section (the exact CSS to write for data-card and result-row dark-scoped overrides)
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-RESEARCH.md — Pitfall 2 (color-mix wrapper must be preserved on shared recipes) and Pitfall 5 (.ops-notice-surface must be skipped)
  </read_first>

  <action>
    Perform ALL of the following edits in one pass on `app.css`:

    GROUP A — 9 token-swaps (swap only the inner var(), keep color-mix wrapper + alpha verbatim):
      L243: `color-mix(in oklch, var(--color-base-100) 96%, transparent)` → `color-mix(in oklch, var(--ops-surface-1) 96%, transparent)`
      L250: `color-mix(in oklch, var(--color-base-100) 94%, transparent)` → `color-mix(in oklch, var(--ops-surface-1) 94%, transparent)`
      L256: `color-mix(in oklch, var(--color-base-200) 64%, transparent)` → `color-mix(in oklch, var(--ops-surface-2) 64%, transparent)`
      L357: `color-mix(in oklch, var(--color-base-200) 64%, transparent)` → `color-mix(in oklch, var(--ops-surface-2) 64%, transparent)`
      L557: `color-mix(in oklch, var(--color-base-200) 72%, transparent)` → `color-mix(in oklch, var(--ops-surface-2) 72%, transparent)`
      L594: `color-mix(in oklch, var(--color-base-200) 58%, transparent)` → `color-mix(in oklch, var(--ops-surface-2) 58%, transparent)`
      L792: `color-mix(in oklch, var(--color-base-100) 94%, transparent)` → `color-mix(in oklch, var(--ops-surface-1) 94%, transparent)`
      L801: `color-mix(in oklch, var(--color-base-200) 60%, transparent)` → `color-mix(in oklch, var(--ops-surface-2) 60%, transparent)`
      L1155: `color-mix(in oklch, var(--color-base-200) 70%, transparent)` → `color-mix(in oklch, var(--ops-surface-2) 70%, transparent)`
      DO NOT touch L262 (.ops-data-card) or L912 (.ops-result-row) shared recipes.
      DO NOT touch L270-277 (.ops-notice-surface) — no background: property to swap.

    GROUP B-1 — `.bg-ops-surface-2` CSS helper class: add to the @layer components section (after the existing recipe blocks, before the shadow overrides section). The new rule: `.bg-ops-surface-2 { background: var(--ops-surface-2); }` with a brief comment: `/* Helper utility for ops_code_block — --ops-surface-2 is an @plugin token, not @theme, so Tailwind v4 cannot auto-generate bg-ops-surface-2. */`

    GROUP B-2 — D-05 dark-scoped dual-path overrides for `.ops-data-card` and `.ops-result-row`:
    Add immediately after the `.bg-ops-surface-2` helper (or grouped near the D-10 shadow overrides at end of file):
      `[data-theme="dark"] .ops-data-card { background: var(--ops-surface-2); }`
      `@media (prefers-color-scheme: dark) { html:not([data-theme="light"]) .ops-data-card { background: var(--ops-surface-2); } }`
      `[data-theme="dark"] .ops-result-row { background: var(--ops-surface-2); }`
      `@media (prefers-color-scheme: dark) { html:not([data-theme="light"]) .ops-result-row { background: var(--ops-surface-2); } }`

    After editing, verify no token-swap was accidentally applied to .ops-data-card (L262) or .ops-result-row (L912) shared recipes — those must still contain `var(--color-base-100)`.

    Run `mix tailwind scrypath_ops` from `scrypath_ops/` to confirm CSS builds clean. Then run `mix test` from `scrypath_ops/`.

    Run `node contrast-checker.mjs` from `examples/scrypath_ecommerce/` to confirm light AA/AAA counts are unchanged (light `color-mix` inputs are identical — `--ops-surface-1=#fffdf8` = prior `base-100`, `--ops-surface-2=#faf7f2` = prior `base-200`; .ops-kbd swap is also byte-identical since base-200 light value equals ops-surface-2 light value).
  </action>

  <verify>
    <automated>grep -v "^[[:space:]]*//" /Users/jon/projects/scrypath/scrypath_ops/assets/css/app.css | grep -c "ops-surface-2"</automated>
  </verify>

  <acceptance_criteria>
    - `grep 'ops-muted-panel' -A3 app.css | grep 'background'` contains `var(--ops-surface-2)` not `var(--color-base-200)`
    - `grep 'ops-nav-list' -A3 app.css | grep 'background'` contains `var(--ops-surface-2)` not `var(--color-base-200)`
    - `grep 'ops-disclosure' -A3 app.css | grep 'background'` contains `var(--ops-surface-2)` not `var(--color-base-200)`
    - `grep 'ops-panel' -A3 app.css | grep 'background'` (the base .ops-panel rule at L243) contains `var(--ops-surface-1)` not `var(--color-base-100)`
    - `grep 'ops-kbd' -A3 app.css | grep 'background'` contains `var(--ops-surface-2)` not `var(--color-base-200)`
    - L262 (.ops-data-card shared recipe) still contains `var(--color-base-100)` — not swapped
    - L912 (.ops-result-row shared recipe) still contains `var(--color-base-100)` — not swapped
    - `[data-theme="dark"] .ops-data-card { background: var(--ops-surface-2); }` appears in app.css
    - `[data-theme="dark"] .ops-result-row { background: var(--ops-surface-2); }` appears in app.css
    - Both dark-scoped overrides use `html:not([data-theme="light"])` in their media-query counterpart (NOT `html:not([data-theme="dark"])`)
    - `.bg-ops-surface-2 { background: var(--ops-surface-2); }` appears in app.css
    - `mix tailwind scrypath_ops` from `scrypath_ops/` exits 0 with no errors
    - `mix test` from `scrypath_ops/` exits 0
    - `node contrast-checker.mjs` light AA count matches Phase 128 baseline
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply D-10 shadow dual-path override and ops_code_block DK-09 reroute</name>
  <files>scrypath_ops/assets/css/app.css, scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex</files>

  <read_first>
    - scrypath_ops/assets/css/app.css — read L525-533 (the select.ops-form-control dual-path precedent) to confirm the exact dual-path pattern structure (explicit dark selector, then media query with html:not([data-theme="light"])); also read L135-138 (the @theme shadow ladder) to see what values are being overridden
    - scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex — read L989-1002 (ops_code_block component) to confirm L994 currently reads `bg-base-200` and L995-996 are unaffected variants before editing
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-PATTERNS.md — Pattern 4 (shadow dual-path override block) for the exact CSS to write; Pattern 5 (ops_code_block) for the one-line edit
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-RESEARCH.md — D-10 shadow dual-path override detail section (the exact 8-line CSS block); also Pitfall 3 (shadow tokens are in box-shadow, D-15 guard is not triggered)
  </read_first>

  <action>
    SHADOW OVERRIDE (app.css):
    Add the D-10 shadow dual-path override block at the end of app.css (after the last rule, or logically grouped near the D-05 dark-scoped overrides added in Task 1). Hand-author both dark selector paths:
      `[data-theme="dark"] {` with the 4 shadow token overrides (D-08 values), then `}`
      `@media (prefers-color-scheme: dark) { html:not([data-theme="light"]) {` with the same 4 shadow token overrides, then `} }`

    The four shadow token override values per D-08:
      `--shadow-ops-surface: 0 1px 2px rgba(0,0,0,0.40);`
      `--shadow-ops-mid:     0 1px 4px rgba(0,0,0,0.45);`
      `--shadow-ops-raised:  0 2px 10px rgba(0,0,0,0.50);`
      `--shadow-ops-overlay: 0 8px 24px rgba(0,0,0,0.55);`

    These override the @theme values (L135-138 — same offsets/blur, dark-inward rgba color/alpha). Zero per-recipe changes required (D-09 cascade to all 14 shadow consumers).

    Do NOT place these inside the @plugin block. Do NOT add new tokens — override the existing four only.

    OPS_CODE_BLOCK (ops_ui.ex):
    At L994 change `"max-h-96 bg-base-200 p-ops-3"` → `"max-h-96 bg-ops-surface-2 p-ops-3"`.
    Do not alter L995 (`bg-base-100`) or L996 (`bg-base-100/70`). The `.bg-ops-surface-2` class was added to app.css in Task 1.

    After both edits: run `mix tailwind scrypath_ops` to confirm CSS builds. Run `mix test` from `scrypath_ops/` to confirm no regressions.
  </action>

  <verify>
    <automated>grep -c "shadow-ops-surface.*rgba" /Users/jon/projects/scrypath/scrypath_ops/assets/css/app.css</automated>
  </verify>

  <acceptance_criteria>
    - `grep 'shadow-ops-surface.*rgba' app.css` returns exactly 2 matches (one per dark selector path)
    - `grep 'shadow-ops-overlay.*rgba' app.css` returns exactly 2 matches
    - Both dark shadow overrides use `rgba(0,0,0,0.40)` for surface, `rgba(0,0,0,0.45)` for mid, `rgba(0,0,0,0.50)` for raised, `rgba(0,0,0,0.55)` for overlay
    - The `@media (prefers-color-scheme: dark)` shadow block contains `html:not([data-theme="light"])` (not `html:not([data-theme="dark"])`)
    - Shadow overrides are NOT inside the `@plugin` block
    - `grep 'bg-ops-surface-2' scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` returns 1 match (L994)
    - `grep 'bg-base-200' scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` returns 0 matches (the swap removed it)
    - `mix tailwind scrypath_ops` from `scrypath_ops/` exits 0
    - `mix test` from `scrypath_ops/` exits 0
    - `node contrast-checker.mjs` light AA count still matches Phase 128 baseline (shadow overrides are dark-scoped only; ops_code_block change is light-pixel-identical since bg-ops-surface-2 = faf7f2 = prior base-200 in light)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS dark-scoped overrides → browser rendering | All dark-scoped overrides fire only under [data-theme="dark"] or prefers-color-scheme:dark; light rendering path is unchanged |
| Elixir component attribute → Tailwind CSS class | bg-ops-surface-2 class must exist in app.css before the component uses it; enforced by Wave 3 build verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-08 | Tampering | app.css recipe token swaps | accept | Static CSS token references; no user input; all edits are deterministic inner-token swaps of known recipe lines |
| T-130-09 | Tampering | ops_ui.ex bg-ops-surface-2 attribute | accept | Tailwind class name change in a render function; no behavior change, no user-controlled input path; class backed by verified CSS helper added in same plan |
| T-130-10 | Denial of Service | CSS build failure from malformed token swap | mitigate | `mix tailwind scrypath_ops` run after every task catches parse errors before commit; plan requires 0 build errors |
| T-130-11 | Tampering | D-05 dark-scoped overrides specificity clash | accept | Dark-scoped `[data-theme="dark"] .ops-data-card { background: ... }` takes priority over the shared `.ops-data-card` recipe in dark; in light neither dark selector fires; verified by contrast-checker light count |
</threat_model>

<verification>
After both tasks commit:

1. `grep 'ops-surface-2' scrypath_ops/assets/css/app.css | wc -l` — count of occurrences (expect: 2 from @plugin blocks + ~11 from recipe swaps + overrides)
2. `grep 'color-base-200' scrypath_ops/assets/css/app.css` — should NOT appear in any of the 9 swapped recipe background: lines (only in non-recipe contexts)
3. `grep 'shadow-ops-surface.*rgba' scrypath_ops/assets/css/app.css` — exactly 2 matches
4. `grep 'bg-ops-surface-2' scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` — exactly 1 match (L994)
5. `cd scrypath_ops && mix test` — green
6. `cd examples/scrypath_ecommerce && node contrast-checker.mjs` — light AA count matches Phase 128 baseline
</verification>

<success_criteria>
- 9 shared recipes route to --ops-surface-1 or --ops-surface-2 inside their preserved color-mix wrappers
- .ops-data-card and .ops-result-row shared recipes keep var(--color-base-100); dark-scoped overrides lift them to surface-2
- .bg-ops-surface-2 helper class exists in app.css
- ops_code_block :default uses bg-ops-surface-2 (not bg-base-200)
- --shadow-ops-* are overridden under both dark selector paths
- Light contrast-checker counts unchanged (light token inputs byte-identical)
- Elixir test suite green
</success_criteria>

<output>
Create `.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-03-SUMMARY.md` when done.
</output>
