---
phase: 130
plan: 02
type: execute
wave: 2
depends_on:
  - 130-01
files_modified:
  - scrypath_ops/assets/css/app.css
autonomous: true
requirements:
  - DARKTOKEN-01

must_haves:
  truths:
    - "The dark @plugin block in app.css contains --ops-bg:#0c0f14, --ops-surface-1:#141923, --ops-surface-2:#1b2230"
    - "The light @plugin block in app.css contains --ops-bg:#faf7f2, --ops-surface-1:#fffdf8, --ops-surface-2:#faf7f2"
    - "node contrast-checker.mjs light AA/AAA counts are unchanged vs Phase 128 baseline after this edit"
    - "mix test from scrypath_ops/ exits 0"
  artifacts:
    - path: "scrypath_ops/assets/css/app.css"
      provides: "Three elevation tokens declared in both @plugin blocks"
      contains: "--ops-surface-2"
  key_links:
    - from: "dark @plugin block (app.css L23-56)"
      to: "--ops-surface-2: #1b2230"
      via: "custom property in daisyUI plugin block (spreads to both dark paths)"
      pattern: "--ops-surface-2:#1b2230"
    - from: "light @plugin block (app.css L58-91)"
      to: "--ops-surface-2: #faf7f2"
      via: "custom property in daisyUI plugin block"
      pattern: "--ops-surface-2:#faf7f2"
---

<objective>
Declare the three elevation tokens (`--ops-bg`, `--ops-surface-1`, `--ops-surface-2`) inside
BOTH daisyUI `@plugin "../vendor/daisyui-theme"` blocks (dark block at L23-56, light block at
L58-91). The plugin's custom-property pass-through (confirmed in `daisyui-theme.js` lines 59-96)
automatically emits them to both `[data-theme="dark"]` and `@media (prefers-color-scheme: dark)`
— no manual dual-selector authoring required for these tokens (D-02). This is a token-declaration-
only commit; no recipe references change yet. After this commit, `node contrast-checker.mjs` must
confirm the light token graph is unchanged.

Purpose: Establish the named surface ramp before recipe routing so every downstream task can
reference stable token names. Light pixel-identity is unchanged because token declarations with
no consumers are inert to rendered output.

Output: `scrypath_ops/assets/css/app.css` with three tokens added to each `@plugin` block.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/scrypath/.planning/ROADMAP.md
@/Users/jon/projects/scrypath/.planning/STATE.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-CONTEXT.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-RESEARCH.md
@/Users/jon/projects/scrypath/.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From scrypath_ops/assets/css/app.css (live — read before editing):

Dark @plugin block (L23-56), ending lines include:
  --depth: 1;
  --noise: 0;
}

Light @plugin block (L58-91), ending lines include:
  --depth: 1;
  --noise: 0;
}

Insert the three tokens BEFORE the closing `}` of each block:

Dark values (D-01):
  --ops-bg: #0c0f14;
  --ops-surface-1: #141923;
  --ops-surface-2: #1b2230;

Light values (D-01):
  --ops-bg: #faf7f2;
  --ops-surface-1: #fffdf8;
  --ops-surface-2: #faf7f2;

From scrypath_ops/assets/vendor/daisyui-theme.js (D-02 mechanism — DO NOT EDIT):
  The plugin spreads all non-reserved custom props via ...customThemeTokens into BOTH
  [data-theme="dark"] AND @media (prefers-color-scheme: dark) { :root }.
  Reserved keys: name, default, prefersdark, color-scheme, root.
  --ops-bg, --ops-surface-1, --ops-surface-2 are NOT reserved → they get both-path coverage.

Anti-patterns (from PATTERNS.md):
  - Do NOT add these tokens to @theme — would bypass plugin pass-through
  - Do NOT add them to only one @plugin block — light block must also have all three
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare elevation tokens in both @plugin blocks</name>
  <files>scrypath_ops/assets/css/app.css</files>

  <read_first>
    - scrypath_ops/assets/css/app.css — read L23-91 (the two @plugin blocks) in full before editing; confirm the exact last tokens before each closing brace (`--depth: 1; --noise: 0;`); confirm neither block already contains `--ops-surface-2`
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-PATTERNS.md — Pattern 1 (the @plugin block structure) for the exact placement: tokens go after `--noise: 0;` and before the closing `}`
    - .planning/phases/130-dark-surface-ramp-depth-tokens-g/130-RESEARCH.md — D-01 section for the canonical hex values; D-02 section for the confirmation that both blocks must receive tokens
  </read_first>

  <action>
    Read app.css L23-91. Confirm dark block ends with `--noise: 0;` then `}`. Confirm light block ends similarly.

    In the dark `@plugin "../vendor/daisyui-theme"` block (L23-56), insert after `--noise: 0;` and before the block's closing `}`:
      `--ops-bg: #0c0f14;`
      `--ops-surface-1: #141923;`
      `--ops-surface-2: #1b2230;`

    In the light `@plugin "../vendor/daisyui-theme"` block (L58-91), insert after `--noise: 0;` and before the block's closing `}`:
      `--ops-bg: #faf7f2;`
      `--ops-surface-1: #fffdf8;`
      `--ops-surface-2: #faf7f2;`

    Do not alter any other property in either block. Do not touch any @theme or @layer section. These are the ONLY two edits in this task.

    After editing: run `mix assets.build` from `scrypath_ops/` (or `cd scrypath_ops && mix tailwind scrypath_ops`) to recompile CSS and confirm no build errors. Then run `mix test` from `scrypath_ops/` to confirm no regressions.

    Then run `node contrast-checker.mjs` from `examples/scrypath_ecommerce/` to confirm the light AA/AAA counts match the Phase 128 baseline (the token declarations are inert to rendered output since no recipe references them yet, so counts must be identical).
  </action>

  <verify>
    <automated>grep -c "\-\-ops-surface-2" /Users/jon/projects/scrypath/scrypath_ops/assets/css/app.css</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '\-\-ops-surface-2' scrypath_ops/assets/css/app.css` returns exactly 2 (one in dark block, one in light block)
    - `grep '\-\-ops-surface-2' scrypath_ops/assets/css/app.css` shows `#1b2230` and `#faf7f2` on separate lines
    - `grep -c '\-\-ops-surface-1' scrypath_ops/assets/css/app.css` returns exactly 2
    - `grep -c '\-\-ops-bg' scrypath_ops/assets/css/app.css` returns at least 2 (the two @plugin declarations; existing base-200 references are separate)
    - `grep '\-\-ops-bg' scrypath_ops/assets/css/app.css` shows both `#0c0f14` (dark block) and `#faf7f2` (light block)
    - The tokens are inside `@plugin "../vendor/daisyui-theme"` blocks, NOT inside `@theme {}` or any selector
    - `mix tailwind scrypath_ops` (or `mix assets.build`) from `scrypath_ops/` exits 0 with no CSS build errors
    - `mix test` from `scrypath_ops/` exits 0
    - `node contrast-checker.mjs` from `examples/scrypath_ecommerce/` light AA count equals Phase 128 baseline (token declarations with no consumers are rendering-inert)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS authoring → browser rendering | Token declarations in @plugin blocks; no user input touches these values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-130-05 | Tampering | app.css @plugin block edits | accept | Edits are static hex color values with no dynamic/interpolated content; no user-controlled input paths; the values are brand-defined constants from prompts/scrypath-brand-book.md |
| T-130-06 | Information Disclosure | Admin CSS color scheme | accept | The admin UI is an operator tool (not public-facing); dark/light ramp values are brand colors, not secrets |
| T-130-07 | Denial of Service | CSS build failure | mitigate | `mix tailwind scrypath_ops` run after edits catches any CSS parse errors before commit; plan requires 0 build errors |
</threat_model>

<verification>
After the task commits:

1. `grep '\-\-ops-surface-2' scrypath_ops/assets/css/app.css` — shows both `#1b2230` and `#faf7f2`
2. `grep '\-\-ops-surface-1' scrypath_ops/assets/css/app.css` — shows both `#141923` and `#fffdf8`
3. `mix test` from `scrypath_ops/` — green
4. `node contrast-checker.mjs` from `examples/scrypath_ecommerce/` — light AA count unchanged vs Phase 128 baseline
</verification>

<success_criteria>
- Both @plugin blocks contain all three elevation tokens with the exact D-01 hex values
- No @theme entry for --ops-surface-* (plugin block only, per D-02)
- CSS builds without errors
- Elixir test suite green
- Light contrast-checker counts unchanged (token declarations are rendering-inert before routing)
</success_criteria>

<output>
Create `.planning/phases/130-dark-surface-ramp-depth-tokens-g/130-02-SUMMARY.md` when done.
</output>
