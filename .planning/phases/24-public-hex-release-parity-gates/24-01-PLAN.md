---
phase: 24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - release-please-config.json
  - test/mix/tasks/workflow_wiring_test.exs
autonomous: true
requirements: [SHIP-01]
tags: [release-please, semver, elixir, ci-contract]

must_haves:
  truths:
    - "release-please-config.json contains bump-minor-pre-major and bump-patch-for-minor-pre-major both true at the root object (pre-1.0 feat→patch policy per 24-CONTEXT D-02)"
    - "workflow_wiring_test UAT-09 asserts the new keys and no longer documents feat→0.4.0 as the default expectation for this repo"
  artifacts:
    - path: "release-please-config.json"
      provides: "JSON schema-valid Release Please root options"
      contains: "bump-patch-for-minor-pre-major"
    - path: "test/mix/tasks/workflow_wiring_test.exs"
      provides: "Regression lock on pre-1.0 bump policy"
      contains: "bump-minor-pre-major"
---

<objective>
Encode maintainer policy for the **next** Hex cut (`0.3.1` target): Release Please must not silently jump to **`0.4.0`** for ordinary `feat:` landings on `0.3.x`. Add the standard pre-1.0 bump pair to `release-please-config.json` and rewrite **UAT-09** so CI encodes the new policy instead of the old “feat → minor on 0.x” story.
</objective>

<threat_model>
**ASVS L1 — maintainer-controlled config only**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-24-01: Invalid JSON breaks Release Please | Medium | Preserve trailing commas off; validate with `jq empty release-please-config.json` in verify |
| T-24-01: Typos in bump keys silently ignored | Low | UAT-09 asserts exact substrings from Release Please docs |

No new secrets, credentials, or runtime user input in this plan.
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add pre-1.0 bump policy to release-please-config.json</name>
  <files>release-please-config.json</files>
  <read_first>
    - release-please-config.json
    - .planning/phases/24-public-hex-release-parity-gates/24-CONTEXT.md (§ D-01, D-02)
    - .planning/phases/24-public-hex-release-parity-gates/24-RESEARCH.md
  </read_first>
  <action>
    At the **root** JSON object (same level as `"$schema"`, `"bootstrap-sha"`, `"release-type"`, `"packages"`), insert two new properties after `"bootstrap-sha"` (order flexible but keep valid JSON):

    ```json
    "bump-minor-pre-major": true,
    "bump-patch-for-minor-pre-major": true,
    ```

    Do **not** remove `"$schema"`, `"release-type": "elixir"`, or the `"packages"` block. Do not add trailing commas.
  </action>
  <verify>
    <automated>jq empty release-please-config.json && mix test test/mix/tasks/workflow_wiring_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"bump-minor-pre-major": true' release-please-config.json` outputs `1`
    - `grep -c '"bump-patch-for-minor-pre-major": true' release-please-config.json` outputs `1`
    - `jq empty release-please-config.json` exits `0`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Rewrite UAT-09 for pre-1.0 patch policy</name>
  <files>test/mix/tasks/workflow_wiring_test.exs</files>
  <read_first>
    - test/mix/tasks/workflow_wiring_test.exs (entire `describe "UAT shift-left"` / UAT-09 block)
    - release-please-config.json (after Task 1)
  </read_first>
  <action>
    Replace the body of test `"UAT-09: release-please preconditions for cutting 0.4.0 hold on HEAD"` (rename the test string to something like `"UAT-09: release-please pre-1.0 bump policy + manifest pin hold on HEAD"`).

    New assertions (concrete):
    1. `cfg = File.read!("release-please-config.json")` contains both literal substrings: `"bump-minor-pre-major": true` and `"bump-patch-for-minor-pre-major": true`.
    2. Keep assertion that `"release-type": "elixir"` is present in `cfg`.
    3. Keep manifest read: `File.read!(".release-please-manifest.json") =~ ~s("0.3.0")` **until** the release PR merges (do not bump manifest in this task — only policy wiring).
    4. Keep `mix.exs @version "0.3.0"` assertion for the same reason.
    5. Keep the `git log` assertion for `feat(18):` subject line unchanged (historical anchor).

    Remove or rewrite any comment/test text that claims **`feat:` against `0.3.0` necessarily produces `0.4.0`** — replace with text that states **`feat:` bumps patch toward `0.3.1`** under the configured pre-1.0 policy, and that **`Release-As:`** remains the override path (24-CONTEXT D-02, D-03).
  </action>
  <verify>
    <automated>mix test test/mix/tasks/workflow_wiring_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'bump-patch-for-minor-pre-major' test/mix/tasks/workflow_wiring_test.exs` returns at least one line
    - `grep -n 'UAT-09' test/mix/tasks/workflow_wiring_test.exs` matches a test line containing `pre-1.0` OR `bump policy`
    - `mix test test/mix/tasks/workflow_wiring_test.exs` exits `0`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix format --check-formatted`
- `mix test test/mix/tasks/workflow_wiring_test.exs`
- `jq empty release-please-config.json`
</verification>

<success_criteria>
- SHIP-01 policy is expressed in machine-readable Release Please config and locked by test.
- No manifest or `mix.exs` version bump in this plan (those ship with the Release Please release PR after this machinery merges).
</success_criteria>
