---
phase: 24
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - docs/releasing.md
  - README.md
  - test/mix/tasks/workflow_wiring_test.exs
autonomous: true
requirements: [SHIP-02, SHIP-03]
tags: [documentation, readme, hex-adoption]

must_haves:
  truths:
    - "docs/releasing.md states that release_parity runs post-publish on release-please.yml and publish-hex.yml, not only on verify-published-release.yml"
    - "README install snippet uses a ~> constraint consistent with current 0.3.x line (not 0.1.0)"
  artifacts:
    - path: "docs/releasing.md"
      provides: "Canonical flow documents post-publish release_parity on both publish workflows"
      contains: "publish-hex.yml"
---

<objective>
Finish **SHIP-02** and **SHIP-03** documentation: align maintainer docs with the new post-publish **`release_parity`** steps, and fix the **README** dependency snippet so adopters are not pointed at `~> 0.1.0` when the library ships **`0.3.x`**. Extend **UAT-07** (or add a sibling test) so HexDocs-shipped releasing prose cannot drift from workflow reality.
</objective>

<threat_model>
**ASVS L1**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-24-03: Misleading install range | Low | Use `~> 0.3.0` (or `~> 0.3`) per team preference — pick one and grep-lock in tests |

Docs-only changes; no secrets.
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Update docs/releasing.md for post-publish parity</name>
  <files>docs/releasing.md</files>
  <read_first>
    - docs/releasing.md (full file)
    - .github/workflows/release-please.yml
    - .planning/phases/24-public-hex-release-parity-gates/24-CONTEXT.md (D-11, D-12)
  </read_first>
  <action>
    1. In **Canonical Release Flow** (merge / publish section), add an explicit bullet or numbered step stating that after `mix verify.release_publish`, the **`publish-hex` job** runs **`mix verify.release_parity <version>`** with the same retry environment variables.

    2. In **§ Release parity gate**, extend the narrative: **`verify-published-release.yml`** remains the **scheduled** monitor; **`release-please.yml`** and **`publish-hex.yml`** now also invoke **`release_parity`** immediately after a successful publish verification (CONTEXT D-12).

    3. In **Recovering a failed publish** / **Manual Recovery Workflow** bullets, mention that recovery repeats **`release_parity`** symmetrically to the primary path (mirror language only — do not duplicate full YAML).
  </action>
  <verify>
    <automated>mix test test/mix/tasks/workflow_wiring_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'post-publish' docs/releasing.md` returns ≥ `1` (adds maintainer-visible keyword tying parity to publish jobs)
    - `grep -c 'mix verify.release_parity' docs/releasing.md` returns ≥ `2` (today only the § heading matches once — executor must add at least one `run:`-style mention in prose)
    - `grep -n 'verify-published-release' docs/releasing.md` returns ≥ `1` (scheduled monitor section preserved)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: README install snippet → 0.3.x line</name>
  <files>README.md</files>
  <read_first>
    - README.md (deps section near top)
    - mix.exs (`@version` line)
  </read_first>
  <action>
    Replace the example dependency line:

    From: `{:scrypath, "~> 0.1.0"}`  
    To: `{:scrypath, "~> 0.3.0"}`

    If the line uses different whitespace, normalize to the above tuple format used elsewhere in the file.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/workflow_wiring_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '{:scrypath, "~> 0.3.0"}' README.md` returns exactly one match
    - `grep '{:scrypath, "~> 0.1.0"}' README.md` returns exit code `1` (no match)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Extend UAT-07 for new releasing.md claims</name>
  <files>test/mix/tasks/workflow_wiring_test.exs</files>
  <read_first>
    - test/mix/tasks/workflow_wiring_test.exs (`UAT-07` test)
    - docs/releasing.md (after Task 1)
  </read_first>
  <action>
    Inside test `"UAT-07: docs/releasing.md ships §Release parity gate section to HexDocs"`, after existing `assert rel =~` lines, add **both**:

    - `assert rel =~ "release-please.yml"` (or a distinctive phrase you added in Task 1 that uniquely ties parity to that workflow name)
    - `assert rel =~ "publish-hex.yml"` in conjunction with `release_parity` context — e.g. assert `rel` contains the substring `publish-hex.yml` and also `mix verify.release_parity` (can be two asserts).

    Goal: docs cannot claim only-scheduled parity without mentioning both publish workflows.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/workflow_wiring_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mix/tasks/workflow_wiring_test.exs` exits `0`
    - `grep -n 'release-please.yml' test/mix/tasks/workflow_wiring_test.exs` matches inside `UAT-07` describe block (line number between the `describe \"UAT shift-left\"` and `end` of that describe — manual spot OK if grep is ambiguous)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/mix/tasks/workflow_wiring_test.exs`
- `mix docs` optional if changing mix.exs extras (not in this plan)
</verification>

<success_criteria>
- Adopter-facing README matches the current release line; maintainer docs match automated publish + parity behavior.
</success_criteria>
