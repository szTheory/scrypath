---
phase: 24
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
  - test/mix/tasks/workflow_wiring_test.exs
autonomous: true
requirements: [SHIP-03]
tags: [github-actions, release-parity, hex-publish]

must_haves:
  truths:
    - "Both credential-bearing publish workflows invoke mix verify.release_parity with the same version string variable used for release_publish, after release_publish succeeds"
    - "Retry env vars SCRYPATH_RELEASE_VERIFY_ATTEMPTS and SCRYPATH_RELEASE_VERIFY_SLEEP_MS match the existing release_publish step (20 / 15000)"
  artifacts:
    - path: ".github/workflows/release-please.yml"
      contains: "mix verify.release_parity"
    - path: ".github/workflows/publish-hex.yml"
      contains: "mix verify.release_parity"
---

<objective>
Close the gap between roadmap success criterion #3 / **SHIP-03** and today’s automation: after a successful **`mix verify.release_publish`**, both **Release Please publish** and **manual recovery publish** must run **`mix verify.release_parity`** with the same retry knobs, so tarball–tag drift is caught on the release pipeline — not only on the scheduled monitor.
</objective>

<threat_model>
**ASVS L1**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-24-02: Longer job window / Hex rate limits | Low | Reuse existing attempt/sleep env; parity task is read-only vs Hex + git |
| T-24-02: Wrong version passed to parity | Medium | Use identical interpolation as `release_publish` step (`outputs.version` vs `inputs.release_version`) |

No new secrets; `HEX_API_KEY` usage unchanged.
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add release_parity to release-please.yml publish job</name>
  <files>.github/workflows/release-please.yml</files>
  <read_first>
    - .github/workflows/release-please.yml
    - .planning/phases/24-public-hex-release-parity-gates/24-PATTERNS.md
    - lib/mix/tasks/verify.release_parity.ex (moduledoc for args + env)
  </read_first>
  <action>
    In job `publish-hex`, **immediately after** the step named `Verify the live published release` (the `mix verify.release_publish "${{ needs.release-please.outputs.version }}"` step), insert a new step:

    ```yaml
      - name: Verify release parity (tarball vs tag)
        env:
          SCRYPATH_RELEASE_VERIFY_ATTEMPTS: "20"
          SCRYPATH_RELEASE_VERIFY_SLEEP_MS: "15000"
        run: mix verify.release_parity "${{ needs.release-please.outputs.version }}"
    ```

    Use the same `env` numeric strings as the preceding `release_publish` step. Step `name` may vary slightly but must be distinct from the prior step title.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/workflow_wiring_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'mix verify.release_parity' .github/workflows/release-please.yml` returns at least one line containing `needs.release-please.outputs.version`
    - The file contains the substring `SCRYPATH_RELEASE_VERIFY_ATTEMPTS: "20"` in two consecutive-related steps (release_publish + release_parity) OR grep counts `SCRYPATH_RELEASE_VERIFY_ATTEMPTS` at least twice in that job — verify manually in PR that both steps set env
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Add release_parity to publish-hex.yml recovery job</name>
  <files>.github/workflows/publish-hex.yml</files>
  <read_first>
    - .github/workflows/publish-hex.yml
    - .github/workflows/release-please.yml (after Task 1 for parity of pattern)
  </read_first>
  <action>
    In job `publish-scrypath`, after the `mix verify.release_publish "${{ inputs.release_version }}"` step, add:

    ```yaml
      - name: Verify release parity (tarball vs tag)
        env:
          SCRYPATH_RELEASE_VERIFY_ATTEMPTS: "20"
          SCRYPATH_RELEASE_VERIFY_SLEEP_MS: "15000"
        run: mix verify.release_parity "${{ inputs.release_version }}"
    ```
  </action>
  <verify>
    <automated>mix test test/mix/tasks/workflow_wiring_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'mix verify.release_parity' .github/workflows/publish-hex.yml` references `inputs.release_version`
    - YAML still has `workflow_dispatch` inputs unchanged at top of file
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Contract-test both workflows for parity step ordering</name>
  <files>test/mix/tasks/workflow_wiring_test.exs</files>
  <read_first>
    - test/mix/tasks/workflow_wiring_test.exs
    - .github/workflows/release-please.yml
    - .github/workflows/publish-hex.yml
  </read_first>
  <action>
    Add a new `describe` block, e.g. `"SHIP-03: post-publish release_parity on publish workflows"`, containing **two tests**:

    1. Read `@release_please_yml`; assert it contains `mix verify.release_parity` and that the **first** occurrence index of `mix verify.release_publish` is **less than** the **first** occurrence index of `mix verify.release_parity` (use `:binary.match/2` or `String.split/2` — avoid false positives from comments by matching the `run:` lines if needed).

    2. Same for `@publish_hex_yml` with `mix verify.release_publish` then `mix verify.release_parity`.

    Reuse module attributes `@release_please_yml` / `@publish_hex_yml` already defined at the top of the test module.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/workflow_wiring_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'SHIP-03' test/mix/tasks/workflow_wiring_test.exs` returns ≥ 1 line
    - `mix test test/mix/tasks/workflow_wiring_test.exs` exits `0`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/mix/tasks/workflow_wiring_test.exs`
- Optional: run `actionlint` locally if installed (not required by repo today)
</verification>

<success_criteria>
- SHIP-03: `release_parity` is part of the same mechanical publish path as `phase11`, `workspace_clean`, and `release_publish`.
</success_criteria>
