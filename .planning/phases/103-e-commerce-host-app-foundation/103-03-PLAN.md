---
phase: 103-e-commerce-host-app-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 103-02
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/endpoint.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
  - examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs
  - examples/scrypath_ecommerce/config/test.exs
  - examples/scrypath_ecommerce/config/dev.exs
autonomous: true
requirements:
  - APP-03
must_haves:
  truths:
    - E2E tools can seed specific fixture scenarios via POST /dev/e2e/seed
    - API endpoints correctly utilize Ecto Sandbox for parallel testing
    - Seed endpoints are strictly unavailable in production
  artifacts:
    - path: examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
      provides: Test seeding API
  key_links:
    - from: examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/endpoint.ex
      to: Phoenix.Ecto.SQL.Sandbox
      via: endpoint plug
      pattern: plug Phoenix\.Ecto\.SQL\.Sandbox
---

<objective>
Configure the Phoenix Ecto SQL Sandbox and implement the E2E seed endpoint for parallel Playwright tests.

Purpose: Implement APP-03 by enabling safe, concurrent test runs via shared sandbox mode and providing a hidden API for generating deterministic test states (D-09).
Output: Configured endpoint sandbox, a secure `/dev/e2e/seed` route, and tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-e-commerce-host-app-foundation/103-CONTEXT.md
@.planning/phases/103-e-commerce-host-app-foundation/103-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sandbox Configuration (APP-03)</name>
  <files>
    examples/scrypath_ecommerce/config/test.exs,
    examples/scrypath_ecommerce/config/dev.exs,
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/endpoint.ex
  </files>
  <behavior>
    - Test 1: Compile checks pass with Ecto Sandbox plug present.
  </behavior>
  <action>
    Update `config/test.exs` and `config/dev.exs` to include `config :scrypath_ecommerce, sandbox: true` (or equivalent structure expected by endpoint).
    Update `lib/scrypath_ecommerce_web/endpoint.ex` to unconditionally compile the plug (if configured) high up in the endpoint:
    ```elixir
    if sandbox = Application.compile_env(:scrypath_ecommerce, :sandbox) do
      plug Phoenix.Ecto.SQL.Sandbox, sandbox: sandbox
    end
    ```
    Ensure this is placed before the router or any custom plugs that touch the DB.
  </action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix compile</automated>
  </verify>
  <done>Phoenix endpoint supports Ecto Sandbox headers for isolated E2E tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E Seeding Endpoint (D-09)</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex,
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex,
    examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs
  </files>
  <behavior>
    - Test 1: `POST /dev/e2e/seed` with `scenario=standard_catalog` returns 200 OK and JSON of seeded data.
    - Test 2: Invalid scenarios return 400 Bad Request.
  </behavior>
  <action>
    Create `ScrypathEcommerceWeb.E2EController` with a `seed/2` action.
    The action should pattern match on `scenario` name, calling `CatalogFixtures.scenario_standard_catalog()` for the `"standard_catalog"` string.
    Update `lib/scrypath_ecommerce_web/router.ex` to define the `POST /dev/e2e/seed` route.
    **CRITICAL SECURITY**: Wrap the route definition in `if Mix.env() in [:dev, :test] do` to guarantee it never ships to production.
    Implement `e2e_controller_test.exs` verifying the route returns successful test data state.
  </action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs</automated>
  </verify>
  <done>E2E tests can dynamically provision tenant catalog fixtures safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network | External clients (Playwright) calling the dev/test endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-03 | Tampering | E2EController / Router | mitigate | Ensure `/dev/e2e/seed` is strictly gated by environment config (`Mix.env() in [:dev, :test]`) to prevent production data corruption. |
</threat_model>

<verification>
E2E controller test confirms successful execution of seed fixtures via HTTP POST.
</verification>

<success_criteria>
Ecto Sandbox is enabled for external testing and the E2E seed route works under dev/test only.
</success_criteria>

<output>
Create `.planning/phases/103-e-commerce-host-app-foundation/103-03-SUMMARY.md` when done
</output>
