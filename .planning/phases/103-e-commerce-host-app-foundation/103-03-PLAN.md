---
phase: 103-e-commerce-host-app-foundation
plan: 03
type: execute
wave: 3
depends_on: ["103-02"]
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/endpoint.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
  - examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex
  - examples/scrypath_ecommerce/mix.exs
requirements: [APP-03]
autonomous: true
must_haves:
  truths:
    - The Phoenix Endpoint maps HTTP headers to Ecto Sandbox connections for E2E tests.
    - A dedicated endpoint exists to securely seed test data within the sandbox transaction.
  artifacts:
    - path: examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
      provides: Dynamic fixture invocation endpoint
      min_lines: 10
    - path: examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex
      provides: Local catalog population task
      min_lines: 15
  key_links:
    - from: examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/endpoint.ex
      to: Phoenix.Ecto.SQL.Sandbox
      via: conditional plug
      pattern: plug Phoenix\.Ecto\.SQL\.Sandbox
---

<objective>
Configure the test environment for Playwright E2E browser tests and provide a local Dev seed task.

Purpose: Allow Playwright workers to run in parallel using shared Ecto transactions without cross-polluting data, and provide a convenient way for developers to populate a dev database to verify the UI.
Output: Phoenix Endpoint Sandbox configuration, a secure seeding API for E2E tests, and a `mix scrypath.seed` task.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-e-commerce-host-app-foundation/103-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sandbox Config & E2E Route (D-09)</name>
  <files>examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/endpoint.ex, examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex</files>
  <read_first>
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/endpoint.ex
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex
  </read_first>
  <action>
    In `endpoint.ex`, add the `Phoenix.Ecto.SQL.Sandbox` plug BEFORE the router is called, guarded by checking `Application.compile_env(:scrypath_ecommerce, :sandbox)`.
    In `router.ex`, add a conditional routing block: `if Mix.env() in [:dev, :test] do scope "/dev" do ... post "/e2e/seed", E2EController, :seed ... end end` to expose the endpoint securely.
  </action>
  <acceptance_criteria>
    - `endpoint.ex` contains `if sandbox = Application.compile_env(:scrypath_ecommerce, :sandbox) do plug Phoenix.Ecto.SQL.Sandbox, sandbox: sandbox end`.
    - `router.ex` contains a route for `POST /dev/e2e/seed` gated by `Mix.env() in [:dev, :test]`.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs</automated>
  </verify>
  <done>Sandbox infrastructure is exposed but secured from production use.</done>
</task>

<task type="auto">
  <name>Task 2: E2E Seed Controller (D-09)</name>
  <files>examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex</files>
  <read_first>
    - .planning/phases/103-e-commerce-host-app-foundation/103-RESEARCH.md (for E2E controller example)
    - examples/scrypath_ecommerce/test/support/fixtures/catalog_fixtures.ex
  </read_first>
  <action>
    Create the `E2EController` mapped in the previous task. Implement the `seed(conn, %{"scenario" => scenario})` action. It should use `apply(ScrypathEcommerce.CatalogFixtures, String.to_existing_atom(scenario), [])` to dynamically execute fixture functions based on E2E requests. Return a JSON success response. Note: Ensure `String.to_existing_atom` is safe by creating a whitelist or ensuring fixture functions are loaded. For simplicity, just wrap it in a try-rescue to catch missing functions.
  </action>
  <acceptance_criteria>
    - `e2e_controller.ex` defines `ScrypathEcommerceWeb.E2EController`.
    - `seed/2` safely resolves the scenario name to a function on `CatalogFixtures` and executes it.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs</automated>
  </verify>
  <done>External E2E drivers can trigger DB seeding over HTTP.</done>
</task>

<task type="auto">
  <name>Task 3: Local Dev Seeding Task (D-08)</name>
  <files>examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex, examples/scrypath_ecommerce/mix.exs</files>
  <read_first>
    - examples/scrypath_ecommerce/test/support/fixtures/catalog_fixtures.ex
    - examples/scrypath_ecommerce/mix.exs
  </read_first>
  <action>
    First, modify `mix.exs` so that `elixirc_paths` includes `"test/support"` for BOTH `:dev` and `:test` environments (e.g., `defp elixirc_paths(env) when env in [:dev, :test], do: ["lib", "test/support"]`). This prevents `UndefinedFunctionError` when the seed task runs in `:dev`.
    Then, create a standard Mix task `Mix.Tasks.Scrypath.Seed` that calls `Mix.Task.run("app.start")` and invokes functions from `ScrypathEcommerce.CatalogFixtures` to generate a rich set of dummy data (e.g., 2 Tenants, 5 Categories each, 10 Products each with Variants). End the task with a simple `IO.puts("Seeding complete.")`. (In Phase 104, this will be updated to trigger Scrypath indexing).
  </action>
  <acceptance_criteria>
    - `mix.exs` `elixirc_paths` explicitly includes `test/support` for the `:dev` environment.
    - `scrypath.seed.ex` defines `Mix.Tasks.Scrypath.Seed`.
    - The `run/1` function starts the application and generates seed data using the fixtures.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix scrypath.seed</automated>
  </verify>
  <done>The `mix scrypath.seed` task successfully provisions a functional local database without compilation errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Network -> App | The E2E seed endpoint exposes arbitrary data insertion capabilities. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-03 | Elevation of Privilege | E2EController | mitigate | Restrict the /dev/e2e/seed route via router macro `if Mix.env() in [:dev, :test] do` so it cannot be compiled into production. |
</threat_model>

<verification>
Ensure `mix test` passes across the ecommerce app and no warnings are generated regarding the router.
</verification>

<success_criteria>
E2E browser tests and developers have reliable mechanisms for interacting with realistic, sandboxed catalog data.
</success_criteria>

<output>
Create `.planning/phases/103-e-commerce-host-app-foundation/103-03-SUMMARY.md` when done
</output>
