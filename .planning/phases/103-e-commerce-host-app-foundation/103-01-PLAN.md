---
phase: 103-e-commerce-host-app-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/scrypath_ecommerce/priv/repo/migrations/
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/tenant.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/category.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/variant.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/repo.ex
  - examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_test.exs
autonomous: true
requirements:
  - APP-02
must_haves:
  truths:
    - Queries without tenant_id or skip_tenant_id raise an error
    - Developer can run migrations without tenant_id errors
    - Domain models reflect B2B E-commerce marketplace structure
  artifacts:
    - path: examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/variant.ex
      provides: Variant schema
    - path: examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/tenant.ex
      provides: Tenant schema
  key_links:
    - from: examples/scrypath_ecommerce/lib/scrypath_ecommerce/repo.ex
      to: Ecto.Query
      via: prepare_query/3
      pattern: opts\[:tenant_id\]
---

<objective>
Scaffold the multi-tenant E-Commerce catalog models and enforce strict tenant isolation via Ecto.

Purpose: Implement APP-02 (Tenant, Category, Product, Variant) and D-01/D-02 (tenant_id on every model, enforced by prepare_query).
Output: A fully functional, tenant-isolated Ecto domain model with tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-e-commerce-host-app-foundation/103-CONTEXT.md
@.planning/phases/103-e-commerce-host-app-foundation/103-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Catalog Domain Models (APP-02)</name>
  <files>
    examples/scrypath_ecommerce/priv/repo/migrations/*,
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/tenant.ex,
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/category.ex,
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex,
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/variant.ex,
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/brand.ex
  </files>
  <behavior>
    - Test 1: Tenant schema exists and has valid changeset
    - Test 2: Category belongs to Tenant
    - Test 3: Product belongs to Tenant and Category
    - Test 4: Variant belongs to Tenant and Product
  </behavior>
  <action>
    Delete existing migrations for brands, categories, products (`priv/repo/migrations/20260530*_create_*.exs`, ignore oban migration).
    Delete `brand.ex`.
    Create a new sequential migration (`priv/repo/migrations/<timestamp>_create_catalog.exs`) that creates tables in order: `tenants`, `categories`, `products`, `variants`.
    Ensure `tenant_id` is on every table (D-01).
    Update/Create the schema files in `lib/scrypath_ecommerce/catalog/` matching D-04 and D-05 structure.
    For Product: id, tenant_id, name, description, category_id.
    For Variant: id, tenant_id, product_id, sku, price_cents, currency, inventory_count, options (map).
  </action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix ecto.reset --quiet && echo "Migrations succeed"</automated>
  </verify>
  <done>Clean slate catalog structure exists in db with all required models.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tenancy Enforcement (D-02)</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/repo.ex,
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex,
    examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_test.exs
  </files>
  <behavior>
    - Test 1: Repo operations without `:tenant_id` or `:skip_tenant_id` raise ArgumentError.
    - Test 2: Catalog functions (e.g., `list_products(tenant)`) pass `:tenant_id` and successfully scope results.
  </behavior>
  <action>
    Implement `prepare_query/3` in `ScrypathEcommerce.Repo` to enforce `:tenant_id` globally. Allow `:schema_migration` and `:skip_tenant_id` to bypass.
    Update `ScrypathEcommerce.Catalog` to accept a `tenant` struct or `tenant_id` in its CRUD APIs and pass `[tenant_id: tenant_id]` to `Repo` calls.
    Implement comprehensive unit tests in `catalog_test.exs` verifying cross-tenant leakage is prevented.
  </action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce/catalog_test.exs</automated>
  </verify>
  <done>Global tenancy enforcement works natively via Repo options.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB Query Layer | API requests must not read or modify cross-tenant data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Information Disclosure | ScrypathEcommerce.Repo | mitigate | `prepare_query/3` explicitly enforces tenant boundaries on all requests |
</threat_model>

<verification>
Automated tests confirm cross-tenant leakage is prevented by Ecto.
</verification>

<success_criteria>
Migrations execute successfully and the domain model tests pass.
</success_criteria>

<output>
Create `.planning/phases/103-e-commerce-host-app-foundation/103-01-SUMMARY.md` when done
</output>
