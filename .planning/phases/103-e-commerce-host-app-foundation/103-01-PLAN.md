---
phase: 103-e-commerce-host-app-foundation
plan: 01
type: execute
wave: 1
depends_on: ["103-00"]
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/repo.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/tenant.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/category.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/variant.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/brand.ex
requirements: [APP-01, APP-02]
autonomous: true
must_haves:
  truths:
    - The Ecto Repo enforces tenancy checks via prepare_query/3 on all read queries.
    - The E-commerce application has schemas for Tenant, Category, Product, and Variant.
  artifacts:
    - path: examples/scrypath_ecommerce/lib/scrypath_ecommerce/repo.ex
      provides: Tenancy enforcement hook
      min_lines: 10
    - path: examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/variant.ex
      provides: Sellable unit schema definition
      min_lines: 15
  key_links:
    - from: examples/scrypath_ecommerce/lib/scrypath_ecommerce/repo.ex
      to: Ecto.Query
      via: prepare_query/3
      pattern: opts\[:tenant_id\]
---

<objective>
Establish the foundational multi-tenant data model and strict database-level tenancy enforcement for the E-commerce host app.

Purpose: Guarantee zero cross-tenant leakage at the lowest layer, avoiding the pitfalls of context-level `where` clauses. Set up the schema models (Tenant, Category, Product, Variant) for the domain.
Output: An updated Ecto Repo with `prepare_query/3`, four complete Ecto schemas, and a migration dropping the old `brands` table while creating the new multi-tenant structure.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo Tenancy Enforcement (D-02)</name>
  <files>examples/scrypath_ecommerce/lib/scrypath_ecommerce/repo.ex</files>
  <read_first>
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce/repo.ex
    - .planning/phases/103-e-commerce-host-app-foundation/103-RESEARCH.md (for prepare_query/3 pattern)
  </read_first>
  <action>
    Modify `ScrypathEcommerce.Repo` to add the `prepare_query/3` callback. It should intercept queries and enforce that `opts[:tenant_id]` is present. If `opts[:skip_tenant_id]` or `opts[:schema_migration]` is set to true, it should bypass the filter. Otherwise, it should append an `Ecto.Query.where(query, tenant_id: ^tenant_id)` clause. If no tenant options are provided, raise an `ArgumentError`.
  </action>
  <acceptance_criteria>
    - `examples/scrypath_ecommerce/lib/scrypath_ecommerce/repo.ex` contains the `def prepare_query` function definition.
    - `prepare_query` correctly checks for `:tenant_id`, `:skip_tenant_id`, and `:schema_migration` in the options.
    - `prepare_query` raises `ArgumentError` if tenancy options are missing.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce &amp;&amp; mix test test/scrypath_ecommerce/catalog_test.exs</automated>
  </verify>
  <done>Repo compiles with the new `prepare_query/3` hook without syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Define Multi-Tenant Schemas (D-01, D-04, D-05)</name>
  <files>examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/tenant.ex, examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/category.ex, examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex, examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/variant.ex, examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/brand.ex</files>
  <read_first>
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/category.ex
  </read_first>
  <action>
    Delete `brand.ex`.
    Create `tenant.ex` with schema "tenants" containing a string `:name` field.
    Update `category.ex` to remove `brand_id` and add `tenant_id` (`belongs_to :tenant, ScrypathEcommerce.Catalog.Tenant`).
    Update `product.ex` to match D-04: `name`, `description`, `category_id`, `tenant_id`. Remove `use Scrypath` and the `search_document` function for now, as Scrypath indexing is reserved for Phase 104. Remove `price_cents`, `sku`, and `brand_id`.
    Create `variant.ex` matching D-05: `tenant_id`, `product_id`, `sku`, `price_cents`, `currency`, `inventory_count`, and `options` (map). Add appropriate `belongs_to` associations for `tenant` and `product`. Update all changesets to require the new association fields.
  </action>
  <acceptance_criteria>
    - `examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/brand.ex` no longer exists.
    - `examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/tenant.ex` defines `ScrypathEcommerce.Catalog.Tenant` and `schema "tenants"`.
    - `examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/variant.ex` defines `ScrypathEcommerce.Catalog.Variant` with an `options` map field.
    - `Product` and `Category` schemas declare `belongs_to :tenant, ScrypathEcommerce.Catalog.Tenant`.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce &amp;&amp; mix test test/scrypath_ecommerce/catalog_test.exs</automated>
  </verify>
  <done>All schemas are defined and compile successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Ecto Migrations</name>
  <files>examples/scrypath_ecommerce/priv/repo/migrations/*</files>
  <read_first>
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/variant.ex
  </read_first>
  <action>
    Run `cd examples/scrypath_ecommerce &amp;&amp; mix ecto.gen.migration restructure_for_tenancy` (do not rely on Bash heredoc to create the file; use Write tool). Edit the newly generated migration file to:
    1. Drop the `brands` table.
    2. Create `tenants` table (name).
    3. Alter `categories` table (remove brand_id, add tenant_id referencing tenants).
    4. Alter `products` table (remove brand_id, price_cents, sku; add tenant_id referencing tenants).
    5. Create `variants` table (tenant_id, product_id, sku, price_cents, currency, inventory_count, options as JSONB).
    Finally, run `cd examples/scrypath_ecommerce &amp;&amp; MIX_ENV=test mix ecto.reset &amp;&amp; mix ecto.reset` to apply the new DB schema locally and in test environment.
  </action>
  <acceptance_criteria>
    - A new migration file exists in `priv/repo/migrations/` ending in `_restructure_for_tenancy.exs`.
    - The migration file contains `drop table(:brands)`.
    - `mix ecto.reset` executes successfully without errors in the test and dev databases.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce &amp;&amp; MIX_ENV=test mix ecto.reset &amp;&amp; mix ecto.reset</automated>
  </verify>
  <done>The database schema accurately reflects the application's multi-tenant domain models.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Database -> App | Tenant isolation is enforced at the DB boundary via prepare_query/3 to prevent cross-tenant data leakage. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Information Disclosure | ScrypathEcommerce.Repo | mitigate | Enforce tenant_id check in prepare_query/3, raising ArgumentError if absent. |
| T-103-02 | Tampering | Ecto Migrations | mitigate | Explicitly allow skip_tenant_id or schema_migration opts in prepare_query/3 to allow admin bypass. |
</threat_model>

<verification>
`mix test` might fail until Plan 2 is complete, but `mix compile` and `mix ecto.reset` must succeed.
</verification>

<success_criteria>
The database has a tenanted structure, and the repo will actively reject queries lacking a tenant definition.
</success_criteria>

<output>
Create `.planning/phases/103-e-commerce-host-app-foundation/103-01-SUMMARY.md` when done
</output>
