---
phase: 103-e-commerce-host-app-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 103-01
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog_fixtures.ex
  - examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex
autonomous: true
requirements:
  - APP-02
must_haves:
  truths:
    - Developer can seed a multi-tenant catalog via a Mix task
    - Fixtures act as the single source of truth for test data
  artifacts:
    - path: examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog_fixtures.ex
      provides: Fixture functions
    - path: examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex
      provides: CLI seed runner
  key_links:
    - from: examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex
      to: examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog_fixtures.ex
      via: function calls
      pattern: CatalogFixtures\.
---

<objective>
Implement pure Elixir function fixtures and a mix task to generate a multi-tenant test catalog.

Purpose: Implement APP-02 requirements for local developer tooling and test isolation via fixtures (D-07, D-08).
Output: Reusable `CatalogFixtures` module and a `mix scrypath.seed` task.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-e-commerce-host-app-foundation/103-CONTEXT.md
@.planning/phases/103-e-commerce-host-app-foundation/103-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Catalog Fixtures (D-07)</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog_fixtures.ex,
    examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_fixtures_test.exs
  </files>
  <behavior>
    - Test 1: `tenant_fixture/1` creates a Tenant.
    - Test 2: `scenario_standard_catalog/1` creates a tenant, 2 categories, 3 products, and 2 variants per product, returning a map of created entities.
  </behavior>
  <action>
    Create `ScrypathEcommerce.CatalogFixtures`.
    Implement basic generator functions for tenants, categories, products, and variants (using the `Catalog` context CRUD functions to ensure defaults and `tenant_id` scopes are applied correctly).
    Implement a `scenario_standard_catalog()` function that chains these together to generate a realistic small catalog for a new tenant.
  </action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce/catalog_fixtures_test.exs</automated>
  </verify>
  <done>Fixtures module can reliably construct multi-tenant domain graphs.</done>
</task>

<task type="auto">
  <name>Task 2: Mix Seed Task (D-08)</name>
  <files>
    examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex
  </files>
  <action>
    Create `Mix.Tasks.Scrypath.Seed`.
    It should start the required applications (`Mix.Task.run("app.start")`), then call `CatalogFixtures.scenario_standard_catalog()` to generate sample data in the DB.
    Optionally, emit an `IO.puts` summarizing the created data (e.g., "Created 1 Tenant, 2 Categories, 3 Products, 6 Variants").
    Ensure it gracefully handles duplicate runs if needed or is explicitly documented as additive.
  </action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix scrypath.seed</automated>
  </verify>
  <done>Developer can run `mix scrypath.seed` to populate their local DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing Layer | Fixtures run in test/dev context and shouldn't impact production DBs inadvertently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-02 | Tampering | Mix Seed Task | mitigate | Mix tasks naturally don't run automatically in prod, but ensure standard Ecto safeties |
</threat_model>

<verification>
`mix scrypath.seed` completes without error.
</verification>

<success_criteria>
CatalogFixtures provides standard testing graphs and `mix scrypath.seed` populates the DB.
</success_criteria>

<output>
Create `.planning/phases/103-e-commerce-host-app-foundation/103-02-SUMMARY.md` when done
</output>
