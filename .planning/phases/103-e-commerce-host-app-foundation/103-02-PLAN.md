---
phase: 103-e-commerce-host-app-foundation
plan: 02
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
  - examples/scrypath_ecommerce/test/support/fixtures/catalog_fixtures.ex
requirements: [APP-02]
autonomous: true
must_haves:
  truths:
    - Catalog context API strictly requires a Tenant argument to list or modify resources.
    - Test fixtures naturally generate a Tenant boundary before generating other entities.
  artifacts:
    - path: examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
      provides: Scoped CRUD operations
      min_lines: 50
    - path: examples/scrypath_ecommerce/test/support/fixtures/catalog_fixtures.ex
      provides: Pure function data generation
      min_lines: 20
  key_links:
    - from: examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
      to: ScrypathEcommerce.Repo
      via: Option passing
      pattern: tenant_id:
---

<objective>
Update the Catalog context and test fixtures to natively operate within the multi-tenant constraints introduced in Plan 1.

Purpose: Provide the correct API surface (Context layer) that developers will use, ensuring it enforces the strict tenancy boundary without boilerplate in the UI layer.
Output: A modernized `Catalog` context handling Tenants, Categories, Products, and Variants securely, alongside corresponding test fixtures.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-e-commerce-host-app-foundation/103-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog Context Tenancy Refactor (D-02)</name>
  <files>examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex</files>
  <read_first>
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/tenant.ex
  </read_first>
  <action>
    Refactor `ScrypathEcommerce.Catalog`. Remove all `Brand`-related functions. Create core `Tenant` management functions (`list_tenants/0`, `get_tenant!/1`, `create_tenant/1` using `skip_tenant_id: true` for Repo operations since tenants are top-level). Update `Category` and `Product` functions to ALWAYS require a `%Tenant{}` struct or a `tenant_id` string/integer, and pass `[tenant_id: tenant_id]` to `Repo.all`, `Repo.get`, etc. Add full CRUD for `Variant` scoped to a Tenant. Also ensure `create_` functions assign the `tenant_id` to the struct being created before inserting.
  </action>
  <acceptance_criteria>
    - `list_brands` and `create_brand` no longer exist in `catalog.ex`.
    - `list_categories/1` accepts a `Tenant` and passes `tenant_id:` as an option to `Repo.all`.
    - `create_product/2` accepts a `Tenant` and attributes, inserting the tenant's ID into the attrs or changeset.
    - `list_variants/1` exists and is tenant-scoped.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce &amp;&amp; mix test test/scrypath_ecommerce/catalog_test.exs</automated>
  </verify>
  <done>The Context layer strictly enforces tenant scopes via explicit arguments.</done>
</task>

<task type="auto">
  <name>Task 2: Catalog Fixtures Refactor (D-07)</name>
  <files>examples/scrypath_ecommerce/test/support/fixtures/catalog_fixtures.ex</files>
  <read_first>
    - examples/scrypath_ecommerce/test/support/fixtures/catalog_fixtures.ex
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
  </read_first>
  <action>
    Rewrite `CatalogFixtures`. Start with a `tenant_fixture/1`. Update `category_fixture/1` to either accept a `%Tenant{}` or implicitly generate one if absent. Similarly, update `product_fixture/1` and `variant_fixture/1` so they construct the full hierarchy (Tenant -> Category -> Product -> Variant) naturally. Ensure they call the updated `Catalog` functions.
  </action>
  <acceptance_criteria>
    - `tenant_fixture/1` is defined and utilizes `Catalog.create_tenant`.
    - `product_fixture/1` is updated to remove brand references and supply `tenant`.
    - `variant_fixture/1` is implemented.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce &amp;&amp; mix test test/scrypath_ecommerce/catalog_test.exs</automated>
  </verify>
  <done>Test fixtures generate valid hierarchical test data for the multi-tenant system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Context -> Repo | Context functions must faithfully pass the tenant context to the database. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-04 | Information Disclosure | Catalog Context | mitigate | Context functions must unpack the Tenant struct and pass `tenant_id` to Repo options to satisfy prepare_query/3. |
</threat_model>

<verification>
At this point, you may run `cd examples/scrypath_ecommerce &amp;&amp; mix test test/scrypath_ecommerce/catalog_test.exs` to fix any remaining unit test regressions from the schema changes.
</verification>

<success_criteria>
The catalog context acts as an impenetrable API wrapper requiring tenant context for all domain interactions.
</success_criteria>

<output>
Create `.planning/phases/103-e-commerce-host-app-foundation/103-02-SUMMARY.md` when done
</output>
