---
phase: 41-federation-docs-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01-PLAN.md
files_modified:
  - guides/multi-index-search.md
  - guides/golden-path.md
  - README.md
  - lib/scrypath/search.ex
  - test/scrypath/docs_contract_test.exs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FED-03
tags:
  - documentation
  - federation
  - doc_contract
must_haves:
  truths:
    - "`guides/multi-index-search.md` documents **`:all` / `global_schemas:` / `:scrypath_global_search_schemas`** resolution at a level adopters can implement, without contradicting Phase 40 shipped behavior."
    - "Public docs state the **two-layer score story**: per-index relevance stays local; **`federation_weight:`** affects **merge ordering**, not a single global relevance score (aligned with **D-15–D-18** in CONTEXT)."
    - "`search_many/2` `@doc` includes the canonical score/merge invariant paragraph; README points readers to **`guides/multi-index-search.md`** for depth."
  artifacts:
    - path: "guides/multi-index-search.md"
      provides: "Canonical federation + multi-index narrative"
      contains: ":all"
    - path: "lib/scrypath/search.ex"
      provides: "API-doc invariant for scores vs merge"
      contains: "federation_weight"
---

<objective>
Align **README**, **`guides/multi-index-search.md`**, **`guides/golden-path.md`**, **`search_many/2` `@doc`**, **`docs_contract_test.exs`**, and **requirements traceability** with **v1.8 federation** (**FED-03**) per phase CONTEXT **D-10–D-18**, including **`:all`** narrative missing from the guide today.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-federation-docs-contracts/41-CONTEXT.md
@.planning/phases/21-multi-index-search/21-CONTEXT.md
@.planning/phases/39-federation-scoring-weights/39-CONTEXT.md
@.planning/phases/40-all-expansion/40-CONTEXT.md
@.planning/phases/41-federation-docs-contracts/41-RESEARCH.md
@.planning/phases/41-federation-docs-contracts/41-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `:all` expansion section in multi-index guide</name>
  <files>guides/multi-index-search.md</files>
  <read_first>
    - guides/multi-index-search.md (full file — find best insertion point before or after `## Federation weights`)
    - lib/scrypath/search.ex (`@doc` for `search_many/2` — `:all` bullets)
    - .planning/phases/40-all-expansion/40-CONTEXT.md
  </read_first>
  <action>
    1. Add a new top-level section **`## :all expansion`** (or equivalent H2) that explains **`{:all, text}`**, **`{:all, text, keyword}`**, **`global_schemas:`** override vs **`otp_app:`** + **`:scrypath_global_search_schemas`**, and points to **`search_many/2`** docs for exact error tuple names.
    2. Keep prose **implementation-accurate** with Phase 40 code (empty registry, missing `otp_app`, post-expansion **`max_schemas`**).
    3. Do **not** duplicate the entire `@doc` — the guide is narrative; the module doc remains API-canonical.
  </action>
  <verify>
    <automated>mix verify.phase41</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n '^## :all expansion' guides/multi-index-search.md`** OR **`grep -n '^## .*[Aa]ll expansion' guides/multi-index-search.md`** returns ≥ 1 line.
    - **`grep -n 'global_schemas' guides/multi-index-search.md`** returns ≥ 1 line.
    - **`grep -n ':scrypath_global_search_schemas' guides/multi-index-search.md`** returns ≥ 1 line.
    - **`mix verify.phase41`** exits 0.
  </acceptance_criteria>
  <done>
    Canonical guide documents `:all` resolution for adopters.
  </done>
</task>

<task type="auto">
  <name>Task 2: Federation weights — two-layer + bold invariant</name>
  <files>guides/multi-index-search.md</files>
  <read_first>
    - guides/multi-index-search.md (`## Federation weights` section)
    - .planning/phases/41-federation-docs-contracts/41-CONTEXT.md (D-15, D-16, D-18)
  </read_first>
  <action>
    1. Immediately before or within the first paragraph under **`## Federation weights`**, add a **Markdown bold line** (single sentence) that states the invariant: **per-index scores stay local**; **`federation_weight:`** adjusts **merged ordering** under engine policy — **not** a claim that raw scores are commensurable across indexes.
    2. Add a short **“Two layers”** framing (1–3 sentences): layer 1 = per-index retrieval/ranking; layer 2 = explicit merge policy — consistent with CONTEXT **D-15** without contradicting existing Meilisearch-aligned wording.
    3. Prefer vocabulary **“merged ordering under federation settings”** over **“best match globally”** where CONTEXT flags hype (**D-18**).
  </action>
  <verify>
    <automated>mix verify.phase41</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n '\*\*' guides/multi-index-search.md`** matches show ≥ 1 bold line in the **`## Federation weights`** section area (lines 78–95 approximate — executor verifies by reading the section).
    - Guide file contains the substring **`merged ordering`** (case-sensitive grep): **`grep -n 'merged ordering' guides/multi-index-search.md`** returns ≥ 1 line.
    - **`mix verify.phase41`** exits 0.
  </acceptance_criteria>
  <done>
    Score vs merge story is explicit in the canonical guide.
  </done>
</task>

<task type="auto">
  <name>Task 3: Golden path + README pointers</name>
  <files>guides/golden-path.md</files>
  <read_first>
    - guides/golden-path.md (multi-index or search-related sections if any)
    - README.md (`## Quick Path` and federation-related mentions)
    - .planning/phases/41-federation-docs-contracts/41-CONTEXT.md (D-10, D-12)
  </read_first>
  <action>
    1. In **`guides/golden-path.md`**, add **one short “read next” block** (bullet or sentence) pointing to **`guides/multi-index-search.md`** for **`search_many/2`**, federation weights, and **`:all`** — **no** second full tutorial (**D-12**).
    2. In **`README.md`**, ensure **exactly one** clear sentence (or sentence + link) in an appropriate section (e.g. near multi-index / `search_many` mention) that sends readers to **`guides/multi-index-search.md`** for federation merge semantics — README stays a billboard (**D-10**).
  </action>
  <verify>
    <automated>mix verify.phase41</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'guides/multi-index-search.md' guides/golden-path.md`** returns ≥ 1 line.
    - **`grep -n 'guides/multi-index-search.md' README.md`** returns ≥ 1 line.
    - **`mix verify.phase41`** exits 0.
  </acceptance_criteria>
  <done>
    Wayfinding points adopters at the canonical guide.
  </done>
</task>

<task type="auto">
  <name>Task 4: `search_many/2` `@doc` — score comparability invariant</name>
  <files>lib/scrypath/search.ex</files>
  <read_first>
    - lib/scrypath/search.ex (`@doc` immediately above `search_many/2`, lines ~109–146)
    - .planning/phases/41-federation-docs-contracts/41-CONTEXT.md (D-17)
  </read_first>
  <action>
    In the existing **`@doc`**, add a compact subsection (e.g. **`## Scores vs merge ordering`**) that states: merged stream ordering; **scores are comparable within one index only**; cross-index **`federation_weight:`** values tune **merge ordering** under engine policy — **not** normalization into one global relevance score. Keep consistent with the guide’s bold invariant (**D-17**).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'within one index' lib/scrypath/search.ex`** returns ≥ 1 line between the **`@doc """`** that precedes **`def search_many(`** and that **`def search_many(`** line (executor confirms line numbers: doc string occurs before the function).
    - **`mix compile --warnings-as-errors`** exits 0.
  </acceptance_criteria>
  <done>
    API docs encode the score invariant for ExDoc readers.
  </done>
</task>

<task type="auto">
  <name>Task 5: Doc contract anchors for federation + `:all`</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (patterns for guide contracts)
    - guides/multi-index-search.md (post-edit)
    - lib/scrypath/search.ex (`@doc` post-edit)
  </read_first>
  <action>
    1. Add **`@moduletag :docs`** to **`Scrypath.DocsContractTest`** **only if** the project already uses that convention elsewhere — if absent, skip adding the tag (**D-09** optional).
    2. Add focused tests that **`@guides["guides/multi-index-search.md"]`** contains stable anchor substrings introduced by Tasks 1–2 (e.g. **`:all`**, **`global_schemas:`**, and the **bold invariant** sentence fragment such as **`per-index`** or the exact bold phrase chosen).
    3. Add a test that **`lib/scrypath/search.ex`** read as string (or via **`Code.fetch_docs`** if already used — prefer simple **`File.read!`**) contains **`within one index`** (or the exact invariant phrase written in Task 4) so `@doc` cannot regress silently.
    4. Keep **no** forbidden internal tokens in published markdown (**REQ-**, **`(D-`**, etc.) — do not weaken existing hygiene tests.
  </action>
  <verify>
    <automated>mix verify.phase41</automated>
  </verify>
  <acceptance_criteria>
    - **`mix verify.phase41`** exits 0.
    - **`grep -n 'multi-index-search' test/scrypath/docs_contract_test.exs`** shows ≥ 1 new test referencing federation or **`:all`** anchors (executor-added test name should include **`phase_41`** or **`federation`**).
  </acceptance_criteria>
  <done>
    CI doc contracts lock critical federation wording.
  </done>
</task>

<task type="auto">
  <name>Task 6: REQUIREMENTS traceability for FED-02</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (FED checklist + traceability table)
    - .planning/ROADMAP.md (Phase 40 complete line)
    - .planning/phases/41-federation-docs-contracts/41-CONTEXT.md (deferred: FED-02 row hygiene)
  </read_first>
  <action>
    1. Update the **FED-02** checklist line to **`[x]`** and description alignment only if it matches shipped Phase 40 reality (Phase 40 is marked complete in **ROADMAP.md**).
    2. In the **Traceability** table, set **FED-02** status to **Complete** (or **`Complete`** matching column style of **FED-01** row).
    3. Do **not** add **`FED-`** tokens to **`@published_markdown_for_hygiene`** consumer docs — this file is under **`.planning/`**, not published Hex docs.
  </action>
  <verify>
    <automated>mix verify.phase41</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'FED-02' .planning/REQUIREMENTS.md`** shows a **`[x]`** checkbox line for **FED-02**.
    - Traceability table line for **FED-02** contains **`Complete`** (case-sensitive) in the Status column cell.
    - **`mix verify.phase41`** exits 0.
  </acceptance_criteria>
  <done>
    Requirements reflect shipped `:all` expansion work.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adopter → docs | Readers rely on honest semantics statements — no new executable trust boundary. |
| `.planning/` edits | Internal traceability only; not shipped to Hex package tarball. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Tampering | Docs over-promise merge vs scores | mitigate | Doc contract tests + `@doc` invariant enforce key phrases. |

## Security Notes

Markdown and `@doc` changes only.

## Security Verification Commands (ASVS L1)

```bash
mix verify.phase41
grep -n 'within one index' lib/scrypath/search.ex
```
</threat_model>

<verification>
- `mix verify.phase41` exits 0.
- `mix compile --warnings-as-errors` exits 0.
- Spot-check: `guides/multi-index-search.md` contains **`:all`** section and federation bold invariant.
</verification>

<success_criteria>
- [ ] Canonical guide covers `:all` + federation score story.
- [ ] README and golden path link correctly.
- [ ] `search_many/2` `@doc` states per-index score scope.
- [ ] Doc contracts enforce anchors.
- [ ] REQUIREMENTS FED-02 reflects Phase 40 completion.
</success_criteria>
