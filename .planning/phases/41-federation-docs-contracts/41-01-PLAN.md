---
phase: 41-federation-docs-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/verify.phase41.ex
  - mix.exs
  - test/scrypath/docs_contract_test.exs
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
autonomous: true
requirements:
  - FED-03
tags:
  - verify
  - docs_contract
  - ci
must_haves:
  truths:
    - "`mix verify.phase41` exists, is registered in `mix.exs`, and runs a **thin** `mix test` slice with **no** Meilisearch integration requirement."
    - "Default PR CI (`quality` job) invokes `mix verify.phase41` so federation doc regressions fail the build."
    - "`docs_contract_test.exs` reads `lib/mix/tasks/verify.phase41.ex` via a module attribute matching other phase verify tasks."
  artifacts:
    - path: "lib/mix/tasks/verify.phase41.ex"
      provides: "Phase 41 verify orchestration"
      contains: "defmodule Mix.Tasks.Verify.Phase41"
    - path: "CONTRIBUTING.md"
      provides: "Contributor-facing verify matrix update"
      contains: "verify.phase41"
---

<objective>
Add **`mix verify.phase41`**, wire it into **`mix.exs`**, **`docs_contract_test.exs`**, the **`quality`** CI job, and **CONTRIBUTING** so **FED-03** has the agreed **fast PR verify slice** (doc contracts, no daemon) per **D-01–D-04** in phase CONTEXT.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-federation-docs-contracts/41-CONTEXT.md
@.planning/phases/41-federation-docs-contracts/41-RESEARCH.md
@.planning/phases/41-federation-docs-contracts/41-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `Mix.Tasks.Verify.Phase41`</name>
  <files>lib/mix/tasks/verify.phase41.ex</files>
  <read_first>
    - lib/mix/tasks/verify.phase38.ex
    - .planning/phases/41-federation-docs-contracts/41-CONTEXT.md (D-01, D-02, D-09)
  </read_first>
  <action>
    1. Create **`lib/mix/tasks/verify.phase41.ex`** as **`Mix.Tasks.Verify.Phase41`** with **`@moduledoc false`**, **`use Mix.Task`**, **`@shortdoc`** describing federation docs + doc contract verification.
    2. Set **`@focused_tests`** to **`["test/scrypath/docs_contract_test.exs"]`** only (or the exact list CONTEXT locks if it names additional **non-integration** files — default **doc contract only**).
    3. Copy **`run/1`**, **`run_test!/2`**, **`ensure_no_args!/1`** structure from **`verify.phase38.ex`** verbatim in behavior: call **`Mix.Task.run("app.start")`**, reject args, run **`mix test`** on **`@focused_tests`** with an informative **`Mix.shell().info/1`** label containing the substring **`Phase 41`**.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - File **`lib/mix/tasks/verify.phase41.ex`** exists and **`grep -n 'defmodule Mix.Tasks.Verify.Phase41' lib/mix/tasks/verify.phase41.ex`** returns ≥ 1 line.
    - **`grep -n 'test/scrypath/docs_contract_test.exs' lib/mix/tasks/verify.phase41.ex`** returns ≥ 1 line inside **`@focused_tests`**.
    - **`grep -n 'Phase 41' lib/mix/tasks/verify.phase41.ex`** returns ≥ 1 line.
    - **`mix compile --warnings-as-errors`** exits 0.
  </acceptance_criteria>
  <done>
    Phase verify task module compiles and targets doc contracts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register `verify.phase41` alias</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (aliases map near other `verify.phase*` entries)
    - lib/mix/tasks/verify.phase41.ex
  </read_first>
  <action>
    Add **`"verify.phase41": :test`** to **`mix.exs`** **`aliases/0`** immediately after **`"verify.phase38": :test`** (preserve ordering with adjacent phase verifies).
  </action>
  <verify>
    <automated>mix verify.phase41</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'verify.phase41' mix.exs`** returns ≥ 1 line inside the aliases keyword list.
    - **`mix verify.phase41`** exits 0.
  </acceptance_criteria>
  <done>
    Alias invokes new verify task successfully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Doc contract reads verify.phase41 source</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (module attributes `@verify_phase*`, existing phase contract tests)
    - lib/mix/tasks/verify.phase41.ex
  </read_first>
  <action>
    1. Add **`@verify_phase41 File.read!("lib/mix/tasks/verify.phase41.ex")`** next to other **`@verify_phase*`** reads.
    2. Add an **`ExUnit.Case`** test that asserts **`@verify_phase41`** contains **`"Mix.Tasks.Verify.Phase41"`** and **`"docs_contract_test.exs"`** (or the exact focused path string used in **`@focused_tests`**) so refactors cannot drop the doc slice silently.
  </action>
  <verify>
    <automated>mix verify.phase41</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n '@verify_phase41' test/scrypath/docs_contract_test.exs`** returns ≥ 1 line.
    - **`grep -n 'verify.phase41' test/scrypath/docs_contract_test.exs`** returns ≥ 1 line in a test body or attribute.
    - **`mix verify.phase41`** exits 0.
  </acceptance_criteria>
  <done>
    Doc contract suite pins the new verify task.
  </done>
</task>

<task type="auto">
  <name>Task 4: CI `quality` job step</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (job `quality`, steps after `verify.phase28`)
    - .planning/phases/41-federation-docs-contracts/41-CONTEXT.md (D-03)
  </read_first>
  <action>
    Insert a step after **`Index contract drift CLI gate (`mix verify.phase28`)`**:

    ```yaml
    - name: Federation docs gate (`mix verify.phase41`)
      run: mix verify.phase41
    ```

    Use the same **`run: mix verify.phase41`** pattern as neighboring verify steps (no extra services).
  </action>
  <verify>
    <automated>mix verify.phase41</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'verify.phase41' .github/workflows/ci.yml`** returns ≥ 1 line under **`quality:`**.
    - YAML remains valid (balanced indentation under **`steps:`**).
  </acceptance_criteria>
  <done>
    Default PR pipeline runs federation doc verify slice.
  </done>
</task>

<task type="auto">
  <name>Task 5: CONTRIBUTING verify matrix + two-tier note</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (`## CI` table and `## Verification` sections)
    - .planning/phases/41-federation-docs-contracts/41-CONTEXT.md (D-03, D-04)
  </read_first>
  <action>
    1. Extend the **CI table** row for **`quality`** to include **`verify.phase41`** alongside the listed phase verifies (comma-separated list in the same table cell style as existing text).
    2. Under **`## Verification`**, add **one short paragraph** (2–4 sentences) stating: **`mix verify.phase41`** is the **fast** federation docs + doc-contract gate for PRs; **integration / Meilisearch** work remains on **`mix verify.phase5`** and related jobs — mirror CONTEXT **two-tier** language without naming internal decision IDs.
  </action>
  <verify>
    <automated>mix verify.phase41</automated>
  </verify>
  <acceptance_criteria>
    - **`grep -n 'verify.phase41' CONTRIBUTING.md`** returns ≥ 2 lines (table + prose).
    - **`mix verify.phase41`** exits 0.
  </acceptance_criteria>
  <done>
    Contributors can discover the new gate from CONTRIBUTING.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → repo | CI runs compiled Mix tasks from the repo — same trust model as existing `verify.phase*` steps. |
| Doc contracts → adopters | Tests only assert published markdown and task sources; no user-data paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | Malicious PR changes doc contracts to hide ops guidance | accept | Normal code review + CI still runs full `mix test` matrix on PRs. |
| T-41-01-02 | Info disclosure | CONTRIBUTING references integration env | mitigate | Keep CONTRIBUTING aligned with existing `phase5` wording — no new secrets. |

## Security Notes

No new secrets, network listeners, or auth paths. Doc-only verification slice.

## Security Verification Commands (ASVS L1)

```bash
grep -n 'verify.phase41' mix.exs .github/workflows/ci.yml CONTRIBUTING.md
mix verify.phase41
```
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix verify.phase41` exits 0.
- `grep -RIn 'verify.phase41' mix.exs lib/mix/tasks/verify.phase41.ex test/scrypath/docs_contract_test.exs .github/workflows/ci.yml CONTRIBUTING.md` lists all expected wiring locations.
</verification>

<success_criteria>
- [ ] `lib/mix/tasks/verify.phase41.ex` exists and matches thin-verify pattern.
- [ ] `mix verify.phase41` is green locally.
- [ ] CI quality job includes `mix verify.phase41`.
- [ ] CONTRIBUTING documents the gate.
</success_criteria>
