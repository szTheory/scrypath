---
phase: 50-accessibility-and-verification-hardening
plan: "04"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - scrypath_ops/test/scrypath_ops_web/ops_a11y_contract_test.exs
  - scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs
  - scrypath_ops/mix.exs
  - docs/releasing.md
  - .planning/phases/50-accessibility-and-verification-hardening/50-VERIFICATION.md
autonomous: true
requirements:
  - OPSUX-06
  - OPSUX-07
user_setup: []
must_haves:
  truths:
    - CI-guarded DOM semantics contracts cover `/ops` posture, failed-sync, sync/drift, and search routes (**D-18**).
    - **`mix verify.opsui`** remains the canonical full gate; docs state optional tagged quick loop (**D-19**).
  artifacts:
    - `ops_a11y_contract_test.exs` (name may be adjusted by executor if module naming conflicts) with **`@moduletag :opsui_a11y`** or per-test **`@tag :opsui_a11y`**.
    - `50-VERIFICATION.md` records manual SR spot-check checklist (**D-22**).
  key_links:
    - `lib/mix/tasks/verify.opsui.ex`, **50-RESEARCH** tag table.
---

<objective>
Deliver **OPSUX-07** plus verification of **OPSUX-06**: add **`LiveViewTest`** (and Floki if needed) **DOM semantics contracts** for the **four** critical **`/ops`** routes (**D-18**); apply **`@tag :opsui_a11y`** for local **`mix test --only opsui_a11y`** iteration while **`mix verify.opsui`** stays **full suite** (**D-19**); extend **`docs/releasing.md`** (and **`scrypath_ops/README.md`** only if already the ops doc hub) to name **`mix verify.opsui`** as canonical; create **`.planning/phases/50-accessibility-and-verification-hardening/50-VERIFICATION.md`** manual SR checklist outcome stub (**D-22**). Adjust **`ops_shell_contract_test.exs`** if shell markup assertions require updates after Plan **01**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md
@.planning/phases/50-accessibility-and-verification-hardening/50-VALIDATION.md
@.planning/phases/50-accessibility-and-verification-hardening/50-RESEARCH.md
@scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs
@scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs
@lib/mix/tasks/verify.opsui.ex
</context>

<threat_model>
**Trust boundaries:** Test-only code paths; no production credentials.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04-01 | Secret leakage | Tests | Mitigated | Do not log env secrets; reuse existing test config fixtures. |
| T-50-04-02 | Flaky CI | LiveView tests | Mitigated | Avoid timing sleeps; use **`LiveViewTest`** render assertions only (**D-21** no snapshots). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: ops_a11y_contract_test.exs scaffold</name>
  <files>scrypath_ops/test/scrypath_ops_web/ops_a11y_contract_test.exs</files>
  <read_first>scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs, scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs, .planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md</read_first>
  <action>Create **`scrypath_ops/test/scrypath_ops_web/ops_a11y_contract_test.exs`** using the same **`ConnCase` / `LiveViewTest`** setup as existing ops web tests. For each route **`~p"/ops/posture"`**, **`~p"/ops/failed-sync"`**, **`~p"/ops/sync-drift"`**, **`~p"/ops/search"`** (see **`router.ex`**), **`live/3`** then assert: **exactly one `h1` inside `main#ops-main`**, **`main` has `id="ops-main"`**, **`header` + `nav[aria-label]`** present, skip link **`href="#ops-main"`** in rendered HTML, and **≥1 `fieldset`/`legend` pair** on **`/ops/search`** after Plan **03**. Use **`@moduletag :opsui_a11y`**.</action>
  <verify>cd scrypath_ops && mix test test/scrypath_ops_web/ops_a11y_contract_test.exs</verify>
  <acceptance_criteria>
    - Test file path `scrypath_ops/test/scrypath_ops_web/ops_a11y_contract_test.exs` exists.
    - `grep -q ':opsui_a11y' scrypath_ops/test/scrypath_ops_web/ops_a11y_contract_test.exs` exits 0.
    - `grep -q 'ops-main' scrypath_ops/test/scrypath_ops_web/ops_a11y_contract_test.exs` exits 0.
  </acceptance_criteria>
  <done>New contract module runs green in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: ops_shell_contract_test updates</name>
  <files>scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs</files>
  <read_first>scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs, scrypath_ops/lib/scrypath_ops_web/components/layouts.ex</read_first>
  <action>Update string/HTML assertions to expect **`id="ops-main"`**, skip link text **"Skip to operator content"**, and optional **`aria-labelledby`** on **`main`** if Plan **01** shipped it. Keep assertions **grep-stable** (substring checks), not full-page snapshots (**D-21**).</action>
  <verify>cd scrypath_ops && mix test test/scrypath_ops_web/ops_shell_contract_test.exs</verify>
  <acceptance_criteria>
    - `mix test test/scrypath_ops_web/ops_shell_contract_test.exs` exits 0.
  </acceptance_criteria>
  <done>Shell contracts match post–Plan 01 markup.</done>
</task>

<task type="auto">
  <name>Task 3: mix alias / docs for verify.opsui + optional quick target</name>
  <files>docs/releasing.md</files>
  <read_first>docs/releasing.md, lib/mix/tasks/verify.opsui.ex, scrypath_ops/mix.exs, CONTRIBUTING.md</read_first>
  <action>Ensure **`docs/releasing.md`** explicitly lists **`mix verify.opsui`** as the operator UI verification gate and mentions **`cd scrypath_ops && mix test --only opsui_a11y`** as an optional fast slice after tags exist (**50-RESEARCH**). If **`scrypath_ops/mix.exs`** lacks a discoverable **`opsui.test_a11y`** alias or similar, add **`"opsui.test_a11y"`** alias expanding to **`["test", "--only", "opsui_a11y"]`** **only if** it does not break default **`mix test`** (document interaction with **`test` alias** — may require **`mix ecto.create`** pre-steps note).</action>
  <verify>grep -q 'verify.opsui' docs/releasing.md && (cd scrypath_ops && mix compile)</verify>
  <acceptance_criteria>
    - `grep -q 'verify.opsui' docs/releasing.md` exits 0.
    - `grep -q 'opsui_a11y' docs/releasing.md` exits 0.
  </acceptance_criteria>
  <done>Contributor docs match **D-19**.</done>
</task>

<task type="auto">
  <name>Task 4: 50-VERIFICATION.md manual checklist stub</name>
  <files>.planning/phases/50-accessibility-and-verification-hardening/50-VERIFICATION.md</files>
  <read_first>.planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md</read_first>
  <action>Create **`50-VERIFICATION.md`** with dated sections: **Environment**, **Keyboard pass** (posture → failed-sync → sync/drift → search), **Screen reader pass** (VoiceOver or NVDA notes), **Outcome** (**PASS / FAIL / BLOCKED**) with signer initials placeholder. This is the **D-22** artifact — automation does not fill outcomes.</action>
  <verify>test -f .planning/phases/50-accessibility-and-verification-hardening/50-VERIFICATION.md</verify>
  <acceptance_criteria>
    - `grep -q 'posture' .planning/phases/50-accessibility-and-verification-hardening/50-VERIFICATION.md` exits 0.
    - `grep -q 'Screen reader' .planning/phases/50-accessibility-and-verification-hardening/50-VERIFICATION.md` exits 0.
  </acceptance_criteria>
  <done>Manual verification template exists.</done>
</task>

</tasks>

<verification>
- [ ] `mix verify.opsui` from repo root exits 0
- [ ] `cd scrypath_ops && mix test --only opsui_a11y` exits 0 after tags land
</verification>

<success_criteria>

- **OPSUX-07**: extended automated contracts + documented verify path; **OPSUX-06** guarded by new tests.

</success_criteria>

---

## PLANNING COMPLETE
