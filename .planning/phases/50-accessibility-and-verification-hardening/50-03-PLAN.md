---
phase: 50-accessibility-and-verification-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/live/search_live.ex
autonomous: true
requirements:
  - OPSUX-06
user_setup: []
must_haves:
  truths:
    - Search playground uses one outer form model with concern-based **`fieldset` / `legend`** chapters and disciplined **`aria-describedby`** + polite **status** region for aggregate federation chatter (**D-06**–**D-11**).
  artifacts:
    - `search_live.html.heex` compiles; no nested `<form>` elements.
  key_links:
    - **50-CONTEXT** playground + live region decisions.
---

<objective>
Normalize **`SearchLive`** for **OPSUX-06**: **`<fieldset>` + `<legend>`** for **Query**, **Federation / merge**, **Limits / safety**, and **Actions** inside the existing single **`<.form>`** submit model (**D-06**–**D-08**); wire **`aria-describedby`** only to controls constrained by honesty copy with **stable `id`s** on honesty panels (**D-09**); consolidate dynamic federation/backend messages into **one primary `role="status"`** (polite) region (**D-10**); fix **icon-only** naming and **`aria-hidden`** on decorative icons (**D-11**). Align empty-state headings with **`h2`** (not skipping levels from page **`h1`**) per **D-03**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md
@.planning/phases/50-accessibility-and-verification-hardening/50-UI-SPEC.md
@.planning/phases/50-accessibility-and-verification-hardening/50-PATTERNS.md
@scrypath_ops/lib/scrypath_ops_web/live/search_live.ex
</context>

<threat_model>
**Trust boundaries:** Playground dispatches read-only **`Scrypath`** calls; form fields are operator-tunable but not arbitrary HTML hosts.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-03-01 | Injection | Query / schema params | Mitigated | Preserve existing **`SearchPlayground`** validation; no new string-concat to SQL/HTTP beyond existing adapter paths. |
| T-50-03-02 | XSS | Honesty panel markup | Mitigated | Keep static HEEx + assigns; no `raw/1` on backend messages. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Fieldset chapters for playground form</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/search_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/search_live.ex, .planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md</read_first>
  <action>In **`render/1`** HEEx (bottom of **`search_live.ex`**), within the single outer **`<.form>`**, group controls into **`<fieldset class="…">`** blocks with **`<legend class="…">`** text: at minimum **Query**, **Federation / merge**, **Limits / safety**, **Actions** (exact legend strings may match **50-UI-SPEC** / **operator-ia.md** tone). For **radio** / **checkbox** clusters answering one question, ensure **nested `fieldset`/`legend`** per **D-07**. Verify template contains **no second `<.form>`** / nested HTML **`<form>`**.</action>
  <verify>cd scrypath_ops && mix test test/scrypath_ops_web/live/search_live_test.exs</verify>
  <acceptance_criteria>
    - `grep -c '<fieldset' scrypath_ops/lib/scrypath_ops_web/live/search_live.ex` outputs a number **≥ 2** when run via `sh -c`.
    - `grep -c '<legend' scrypath_ops/lib/scrypath_ops_web/live/search_live.ex` outputs a number **≥ 2** when run via `sh -c`.
    - `grep -c 'phx-submit="search"' scrypath_ops/lib/scrypath_ops_web/live/search_live.ex` outputs **1**.
  </acceptance_criteria>
  <done>Playground form matches **D-06**–**D-08**.</done>
</task>

<task type="auto">
  <name>Task 2: aria-describedby + polite status region</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/search_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/search_live.ex, .planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md</read_first>
  <action>Add **stable `id`** attributes to honesty / constraint panels (e.g. **`id="search-honesty-panel"`**). Set **`aria-describedby`** on **only** inputs those panels constrain (per **D-09**). Replace broad **`aria-live`** on large containers with **one small `role="status"`** (or **`div` with `role="status"`**) node updated for **aggregate** federation/backend status (**D-10**); reserve **`role="alert"`** only for rare unreachable-backend paths if already semantically justified.</action>
  <verify>cd scrypath_ops && mix test test/scrypath_ops_web/live/search_live_test.exs</verify>
  <acceptance_criteria>
    - `grep -q 'role="status"' scrypath_ops/lib/scrypath_ops_web/live/search_live.ex` exits 0.
    - `grep -q 'aria-describedby' scrypath_ops/lib/scrypath_ops_web/live/search_live.ex` exits 0 when honesty copy exists; if not applicable, document **N/A** in commit message.
  </acceptance_criteria>
  <done>Live region discipline matches **D-09** / **D-10**.</done>
</task>

<task type="auto">
  <name>Task 3: Heading ladder in function components + icons</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/search_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/search_live.ex, .planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md</read_first>
  <action>In **`empty_or_hits_single/1`** (and similar inner components), ensure prominent headings use **`h2`** (not **`h4`**) under the page **`h1`** (**D-03**). Mark decorative icons **`aria-hidden="true"`**; ensure icon-only buttons have **`aria-label`** (**D-11**).</action>
  <verify>cd scrypath_ops && mix compile && mix test test/scrypath_ops_web/live/search_live_test.exs</verify>
  <acceptance_criteria>
    - `grep -q 'h2 class' scrypath_ops/lib/scrypath_ops_web/live/search_live.ex` exits 0 in **`empty_or_hits_single`** region (or **`h2`** equivalent without `h4` as first subsection heading in that component).
  </acceptance_criteria>
  <done>Component-level headings/icons align with **D-03** / **D-11**.</done>
</task>

</tasks>

<verification>
- [ ] `cd scrypath_ops && mix test test/scrypath_ops_web/live/search_live_test.exs` exits 0
</verification>

<success_criteria>

- Playground is operable with **keyboard + SR-oriented** grouping and **low-verbosity** live updates.

</success_criteria>

---

## PLANNING COMPLETE
