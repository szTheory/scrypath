---
phase: 50-accessibility-and-verification-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
  - scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex
  - scrypath_ops/lib/scrypath_ops_web/components/core_components.ex
autonomous: true
requirements:
  - OPSUX-06
user_setup: []
must_haves:
  truths:
    - `/ops` shell exposes `id="ops-main"`, a skip link to `#ops-main`, and the page `h1` carries a stable id usable for `aria-labelledby` on `main`.
  artifacts:
    - `Layouts.app` `:ops` branch updated in `layouts.ex`.
    - `ops_page_header` emits default `id="ops-page-title"` on the visible `h1`.
  key_links:
    - Skip link copy matches **50-UI-SPEC** — **Skip to operator content**.
---

<objective>
Harden the **`:ops`** shell (**50-CONTEXT D-02**, **D-05**, **50-UI-SPEC**): first-focus **skip link** to **`#ops-main`**, stable **`main`** landmark (**`id="ops-main"`**), optional **`aria-labelledby="ops-page-title"`** on **`main`**, and a default **`id="ops-page-title"`** on **`ops_page_header`**’s **`h1`** so rotor naming is coherent without duplicate hidden titles.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md
@.planning/phases/50-accessibility-and-verification-hardening/50-UI-SPEC.md
@.planning/phases/50-accessibility-and-verification-hardening/50-PATTERNS.md
@scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
@scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex
</context>

<threat_model>
**Trust boundaries:** Maintainer-authored layout/components; read-only `/ops`.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01-01 | XSS | New skip link / main attributes | Mitigated | Skip link uses **`~p` / `href="#ops-main"`** only; no `raw/1`; no user-controlled `href`. |
| T-50-01-02 | Spoofing / misleading navigation | Skip link target | Mitigated | **`#ops-main`** must exist on same document; pair with compile-time template check + **`mix test`**. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Skip link + main landmark in Layouts.app (:ops)</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/layouts.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/components/layouts.ex, .planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md, .planning/phases/50-accessibility-and-verification-hardening/50-UI-SPEC.md</read_first>
  <action>In **`def app(%{shell: :ops} = assigns)`**, before **`<header>`**, render a **skip link**: **`<a href="#ops-main" class="…">Skip to operator content</a>`** using Tailwind **`sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-50 …`** (or equivalent daisyUI-safe utility stack) so it is visible on keyboard focus per **50-UI-SPEC**. Change the **`<main>`** opening tag to **`&lt;main id="ops-main" aria-labelledby="ops-page-title" class="px-4 py-10 sm:px-6 lg:px-8"&gt;`** — if **D-05** is deemed too aggressive for a first slice, **`aria-labelledby` may be omitted** but **`id="ops-main"`** is mandatory (**D-02**).</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'id="ops-main"' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` exits 0.
    - `grep -q 'Skip to operator content' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` exits 0.
    - `grep -q '#ops-main' scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` exits 0.
  </acceptance_criteria>
  <done>`mix compile` succeeds; skip link is first focusable in `:ops` template order before header.</done>
</task>

<task type="auto">
  <name>Task 2: Stable h1 id on ops_page_header</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex, .planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md</read_first>
  <action>On **`ops_page_header`**, add **`attr :title_id, :string, default: "ops-page-title"`** (or hard-code **`id="ops-page-title"`** on the **`<h1>`** if attrs are undesirable). The visible **`h1`** MUST expose **`id={@title_id}`** so **`main aria-labelledby`** resolves. Document in **`@moduledoc`** that LiveViews must not duplicate this id (single **`h1`** per route — **D-01**).</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - `grep -q 'ops-page-title' scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` exits 0.
    - `grep -q 'def ops_page_header' scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` exits 0.
  </acceptance_criteria>
  <done>Page title `h1` is referenceable from `main`.</done>
</task>

<task type="auto">
  <name>Task 3 (optional): ops_skip_link helper in CoreComponents</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/core_components.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/components/core_components.ex, scrypath_ops/lib/scrypath_ops_web/components/layouts.ex</read_first>
  <action>If inline skip markup in **`layouts.ex`** is hard to maintain, extract **`def ops_skip_link/1`** in **`CoreComponents`** (or **`OpsUi`**) with **`assigns :href` default `#ops-main`** and visible text **"Skip to operator content"**, then call **`<.ops_skip_link />`** from **`layouts.ex`**. Skip this task if Task 1 already keeps markup clear and DRY.</action>
  <verify>cd scrypath_ops && mix compile</verify>
  <acceptance_criteria>
    - Either `grep -q 'ops_skip_link' scrypath_ops/lib/scrypath_ops_web/components/core_components.ex` exits 0, OR file unchanged and Task 1 acceptance still passes.
  </acceptance_criteria>
  <done>Skip link implementation is maintainable.</done>
</task>

</tasks>

<verification>
- [ ] `cd scrypath_ops && mix compile` exits 0
- [ ] `cd scrypath_ops && mix test test/scrypath_ops_web/ops_shell_contract_test.exs` exits 0 (update assertions in Plan 04 if headings/landmarks change expected HTML)
</verification>

<success_criteria>

- **`:ops`** documents expose **`#ops-main`**, skip link, and coherent **`h1`** / **`main`** naming per **50-CONTEXT**.

</success_criteria>

---

## PLANNING COMPLETE
