---
phase: 50-accessibility-and-verification-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
  - scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
  - scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex
autonomous: true
requirements:
  - OPSUX-06
user_setup: []
must_haves:
  truths:
    - Posture, failed-sync, and sync/drift routes use semantic heading ladder (`h1` → `h2` → `h3`), named `section`s where appropriate, and real tables with sort affordances per **D-12**–**D-17**.
  artifacts:
    - Updated HEEx for the three LiveViews compiles under `mix test`.
  key_links:
    - **50-CONTEXT** dense triage table decisions (**D-12**–**D-17**).
---

<objective>
Apply **OPSUX-06** semantics to **Posture**, **Failed sync**, and **Sync / drift** LiveViews: **2–4** **`section aria-labelledby`** blocks (**D-04**), correct **heading levels** (**D-03**), **semantic tables** with **`th scope`**, **sort buttons** + single **`aria-sort`** authority (**D-13**), **row action** **`role="group"`** + task **`aria-label`s** (**D-15**), and **expandable row** **`aria-expanded` / `aria-controls`** where detail rows exist (**D-14**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md
@.planning/phases/50-accessibility-and-verification-hardening/50-UI-SPEC.md
@.planning/phases/50-accessibility-and-verification-hardening/50-PATTERNS.md
@scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
@scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
@scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex
</context>

<threat_model>
**Trust boundaries:** Read-only triage data rendered as text/table cells.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-02-01 | XSS | Table cell content | Mitigated | Keep HEEx `{}` escaping; do not introduce `raw/1` for row payloads. |
| T-50-02-02 | Clickjacking / confused deputy | Sort / action buttons | Mitigated | **`type="button"`** on in-table controls; no new cross-origin embeds. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: PostureLive — sections + headings</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex, .planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md</read_first>
  <action>Edit the **`render/1`** **`~H"""`** template in **`posture_live.ex`**. Wrap first-class JTBD blocks (e.g. **Next checks**, posture summary) in **`<section aria-labelledby="…">`** where **50-CONTEXT D-04** applies; ensure each section’s visible heading is **`h2`** (or **`h3`** only for true nesting under an **`h2`**) — **no `h1` → `h4` jump**. Replace anonymous **`aria-label` regions** with **`section` + visible heading + stable `id`** on the heading for **`aria-labelledby`**, unless a region is truly redundant.</action>
  <verify>cd scrypath_ops && mix test test/scrypath_ops_web/live/posture_live_test.exs</verify>
  <acceptance_criteria>
    - `grep -q '<section' scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex` exits 0 **OR** a **`# a11y:`** comment in **`posture_live.ex`** explains omission (max 3 lines) — prefer sections for **≥2** first-class blocks.
    - `grep -q 'def render' scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex` exits 0.
  </acceptance_criteria>
  <done>Posture route template aligns with **D-03** / **D-04**.</done>
</task>

<task type="auto">
  <name>Task 2: FailedSyncLive — table + sort semantics</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex, .planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md</read_first>
  <action>In **`failed_sync_live.ex`** **`render/1`** HEEx, ensure primary triage grid is a **semantic `<table>`** with **`<thead>` / `<tbody>`** and **`th scope="col"`** (**D-12**). For each **sortable** column (if any), use **`<button type="button">` inside `<th>`** and set **`aria-sort="none" | "ascending" | "descending"`** on exactly **one** element per column (**D-13**). If the view has **no column sort UI**, skip **`aria-sort`** and note **N/A** in **`done`**. Add **`role="group"`** + concise **`aria-label`** on per-row icon toolbars naming the row subject (**D-15**) where icon clusters exist.</action>
  <verify>cd scrypath_ops && mix test test/scrypath_ops_web/live/failed_sync_live_test.exs</verify>
  <acceptance_criteria>
    - `grep -q '<table' scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex` exits 0.
    - `grep -q 'scope="col"' scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex` exits 0.
  </acceptance_criteria>
  <done>Failed sync triage matches **D-12**–**D-15**.</done>
</task>

<task type="auto">
  <name>Task 3: SyncDriftLive — table + expandable rows</name>
  <files>scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex</files>
  <read_first>scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex, .planning/phases/50-accessibility-and-verification-hardening/50-CONTEXT.md</read_first>
  <action>Apply the same **table** / **sort** patterns as Task 2 where applicable. If the view implements **expandable row details**, use **sibling `<tr>`** pattern with **`aria-expanded` / `aria-controls`** on the toggle and sync **`hidden`** with expanded state (**D-14**). If no expandable rows exist, skip **`aria-expanded`** wiring and note **N/A** in **`done`**.</action>
  <verify>cd scrypath_ops && mix test test/scrypath_ops_web/live/sync_drift_live_test.exs</verify>
  <acceptance_criteria>
    - `grep -q '<table' scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex` exits 0.
    - `grep -q 'scope="col"' scrypath_ops/lib/scrypath_ops_web/live/sync_drift_live.ex` exits 0.
  </acceptance_criteria>
  <done>Sync/drift triage table semantics implemented.</done>
</task>

</tasks>

<verification>
- [ ] `cd scrypath_ops && mix test test/scrypath_ops_web/live/posture_live_test.exs test/scrypath_ops_web/live/failed_sync_live_test.exs test/scrypath_ops_web/live/sync_drift_live_test.exs` exits 0
</verification>

<success_criteria>

- Triage paths expose **boring, navigable HTML** for AT without **landmark soup**.

</success_criteria>

---

## PLANNING COMPLETE
