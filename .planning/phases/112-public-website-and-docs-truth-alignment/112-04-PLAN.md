---
phase: 112-public-website-and-docs-truth-alignment
plan: "04"
type: execute
wave: 3
depends_on:
  - 112-01
  - 112-02
  - 112-03
files_modified:
  - test/scrypath/phase112_contract_test.exs
  - lib/mix/tasks/verify.phase112.ex
  - test/mix/tasks/verify.phase112_test.exs
  - mix.exs
  - CONTRIBUTING.md
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - SCOPE-01
must_haves:
  truths:
    - Maintainers can run one service-free command that proves the Phase 112 claim envelope, route-map boundaries, and scope-policy links.
    - Focused file-read tests guard public truth drift without resorting to a noisy repo-wide scanner.
    - The new `verify.phase112` command is registered for test env use and documented for public-truth changes without widening existing CI lanes.
    - "D-16: Contract proof asserts positive route and claim tokens across targeted public surfaces and refutes specific misleading claim families on those same surfaces."
  artifacts:
    - path: test/scrypath/phase112_contract_test.exs
      provides: Focused Phase 112 contract assertions for public claim envelope, route-map boundaries, and scope-policy drift.
    - path: lib/mix/tasks/verify.phase112.ex
      provides: Standalone service-free verification command for Phase 112.
    - path: test/mix/tasks/verify.phase112_test.exs
      provides: Self-tests for `verify.phase112` arg guards, source/help text, and preferred-env registration.
    - path: mix.exs
      provides: `verify.phase112` preferred-env registration.
    - path: CONTRIBUTING.md
      provides: Maintainer-facing discovery of the new `mix verify.phase112` command.
  key_links:
    - from: lib/mix/tasks/verify.phase112.ex
      to: test/scrypath/phase112_contract_test.exs
      via: `@focused_tests` phase verification wiring
      pattern: phase112_contract_test.exs
    - from: lib/mix/tasks/verify.phase112.ex
      to: test/mix/tasks/verify.phase112_test.exs
      via: `@focused_tests` task self-test wiring
      pattern: verify.phase112_test.exs
    - from: mix.exs
      to: lib/mix/tasks/verify.phase112.ex
      via: `preferred_envs` registration
      pattern: "verify.phase112"
    - from: CONTRIBUTING.md
      to: lib/mix/tasks/verify.phase112.ex
      via: scope table entry telling maintainers when to run the new truth command
      pattern: mix verify.phase112
---

<objective>
Add focused, service-free Phase 112 contract proof and expose it through one standalone Mix task.

Purpose: Finish WEB-01, WEB-02, and SCOPE-01 with deterministic proof that guards public truth drift without widening existing lean trust gates or adding browser/external-service requirements.
Output: A new `phase112_contract_test.exs`, a standalone `mix verify.phase112` command, task wiring tests, preferred-env registration, and maintainer docs for when to run it.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-public-website-and-docs-truth-alignment/112-CONTEXT.md
@.planning/phases/112-public-website-and-docs-truth-alignment/112-RESEARCH.md
@.planning/phases/112-public-website-and-docs-truth-alignment/112-PATTERNS.md
@.planning/phases/112-public-website-and-docs-truth-alignment/112-VALIDATION.md
@.planning/phases/110-support-intake-and-evidence-routing/110-02-SUMMARY.md
@.planning/phases/111-advisory-proof-stability-decision/111-02-SUMMARY.md
@README.md
@CONTRIBUTING.md
@mix.exs
@website/src/layout.html
@website/src/pages/index.html
@website/src/pages/docs.html
@website/src/pages/evaluate.html
@website/src/pages/operators.html
@guides/scope-and-reopen-policy.md
@guides/overview.md
@guides/support-and-compatibility.md
@guides/outside-adopter-intake.md
@guides/sync-modes-and-visibility.md
@docs/operator-support.md
@docs/jtbd-gap-map.md
@lib/mix/tasks/verify.phase108.ex
@test/mix/tasks/verify.phase108_test.exs
@test/scrypath/phase110_contract_test.exs
@test/scrypath/phase111_contract_test.exs

<interfaces>
From `lib/mix/tasks/verify.phase108.ex`:
```elixir
@focused_tests [
  "test/scrypath/phase108_contract_test.exs",
  "test/mix/tasks/verify.phase108_test.exs"
]
```

From `test/scrypath/phase110_contract_test.exs`:
```elixir
defp assert_contains_all(content, snippets) do
  Enum.each(snippets, fn snippet -> assert String.contains?(content, snippet) end)
end

defp assert_absent_all(content, snippets, path) do
  Enum.each(snippets, fn snippet -> refute String.contains?(content, snippet) end)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused Phase 112 contract tests for public claim, routing, and scope drift</name>
  <files>test/scrypath/phase112_contract_test.exs</files>
  <read_first>
.planning/phases/112-public-website-and-docs-truth-alignment/112-CONTEXT.md
.planning/phases/112-public-website-and-docs-truth-alignment/112-RESEARCH.md
.planning/phases/112-public-website-and-docs-truth-alignment/112-VALIDATION.md
README.md
website/src/layout.html
website/src/pages/index.html
website/src/pages/docs.html
website/src/pages/evaluate.html
website/src/pages/operators.html
guides/scope-and-reopen-policy.md
guides/overview.md
guides/support-and-compatibility.md
guides/outside-adopter-intake.md
guides/sync-modes-and-visibility.md
docs/operator-support.md
docs/jtbd-gap-map.md
test/scrypath/phase110_contract_test.exs
test/scrypath/phase111_contract_test.exs
  </read_first>
  <behavior>
    - Test 1: canonical public surfaces contain `Scrypath, the Ecto-native search indexing library`, scope-policy routes, and the exact three reopen triggers where policy truth is expected.
    - Test 2: website route-map surfaces point to `README.md`, guides, examples, `https://hex.pm/packages/scrypath`, `https://hexdocs.pm/scrypath`, and GitHub through page or shared-layout links.
    - Test 3: misleading claim families such as `AI-powered`, `AI search`, `vector search`, `hybrid search`, `automatic callbacks`, `always current`, `immediately searchable`, and positive `public multi-backend support` language are absent from the targeted public surfaces.
    - Test 4: website pages remain summary-depth route maps by staying free of runbook tokens `docker compose up -d`, `mix deps.get`, `SCRYPATH_MEILISEARCH_URL`, and `mix test`.
  </behavior>
  <action>Create `test/scrypath/phase112_contract_test.exs` per D-14 through D-17, including D-16, using the direct-file-read helper pattern from `test/scrypath/phase110_contract_test.exs` and the policy-consistency style from `test/scrypath/phase111_contract_test.exs`. Read these exact surfaces: `README.md`, `website/src/layout.html`, `website/src/pages/index.html`, `website/src/pages/docs.html`, `website/src/pages/evaluate.html`, `website/src/pages/operators.html`, `guides/scope-and-reopen-policy.md`, `guides/overview.md`, `guides/support-and-compatibility.md`, `guides/outside-adopter-intake.md`, `guides/sync-modes-and-visibility.md`, `docs/operator-support.md`, and `docs/jtbd-gap-map.md`. Assert positive route/claim tokens including `Scrypath, the Ecto-native search indexing library`, `scope-and-reopen-policy.md`, `{{REPO_URL}}/blob/main/README.md`, the example README links, `https://hex.pm/packages/scrypath`, `https://hexdocs.pm/scrypath`, and `{{REPO_URL}}`. Assert the exact three-trigger phrases `concrete production bug`, `reviewed outside-adopter evidence`, and `deliberate strategic product decision` on the policy-bearing surfaces. For negative checks, use surface-scoped phrase lists so explicit negations on the evaluate page remain legal: assert the exact negated fit/non-fit sentences separately, and forbid only positive-implying tokens such as `AI-powered`, `AI search`, `vector search`, `hybrid search`, `automatic callbacks`, `always current`, `immediately searchable`, and `public multi-backend support`. Add website-only checks that the HTML pages do not contain `docker compose up -d`, `mix deps.get`, `SCRYPATH_MEILISEARCH_URL`, or `mix test`.</action>
  <verify>
    <automated>mix test test/scrypath/phase112_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/scrypath/phase112_contract_test.exs` exists and reads the exact public surfaces listed in this task.
    - The test file asserts `Scrypath, the Ecto-native search indexing library`, `scope-and-reopen-policy.md`, `{{REPO_URL}}/blob/main/README.md`, `https://hex.pm/packages/scrypath`, and `https://hexdocs.pm/scrypath`.
    - The test file asserts `concrete production bug`, `reviewed outside-adopter evidence`, and `deliberate strategic product decision`.
    - The test file forbids `AI-powered`, `AI search`, `vector search`, `hybrid search`, `automatic callbacks`, `always current`, `immediately searchable`, and `public multi-backend support` on the targeted public surfaces.
    - The test file checks that website HTML pages do not contain `docker compose up -d`, `mix deps.get`, `SCRYPATH_MEILISEARCH_URL`, or `mix test`.
    - `mix test test/scrypath/phase112_contract_test.exs` passes.
  </acceptance_criteria>
  <done>Phase 112 has focused, deterministic contract coverage for claim-envelope drift, route-map boundaries, and scope-policy consistency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the contract through a standalone `mix verify.phase112` task</name>
  <files>lib/mix/tasks/verify.phase112.ex, test/mix/tasks/verify.phase112_test.exs, mix.exs, CONTRIBUTING.md</files>
  <read_first>
.planning/phases/112-public-website-and-docs-truth-alignment/112-CONTEXT.md
.planning/phases/112-public-website-and-docs-truth-alignment/112-RESEARCH.md
.planning/phases/112-public-website-and-docs-truth-alignment/112-VALIDATION.md
lib/mix/tasks/verify.phase108.ex
test/mix/tasks/verify.phase108_test.exs
test/scrypath/phase112_contract_test.exs
mix.exs
CONTRIBUTING.md
  </read_first>
  <behavior>
    - Test 1: `mix verify.phase112` rejects stray args, advertises itself in help output, and runs only the Phase 112 contract suite plus its task self-test.
    - Test 2: `mix.exs` registers `"verify.phase112": :test` in `preferred_envs`.
    - Test 3: `CONTRIBUTING.md` tells maintainers to run `mix verify.phase112` when README, website, scope-guide, or route-map public-truth copy changes.
  </behavior>
  <action>Implement `lib/mix/tasks/verify.phase112.ex` as a standalone service-free task per D-14, D-15, and D-17, using `lib/mix/tasks/verify.phase108.ex` as the skeleton. `@focused_tests` must list exactly `test/scrypath/phase112_contract_test.exs` and `test/mix/tasks/verify.phase112_test.exs`. Keep the task no-args, orchestration-only, and free of docs builds, browser automation, CI wiring, or external credentials. Create `test/mix/tasks/verify.phase112_test.exs` mirroring `test/mix/tasks/verify.phase108_test.exs`, but also assert `Scrypath.MixProject.cli()[:preferred_envs][:"verify.phase112"] == :test` and that the source/help text contains the progress marker `verify.phase112: public website and docs truth alignment checks`. Update `mix.exs` to add `"verify.phase112": :test` to `preferred_envs`. Update `CONTRIBUTING.md` with one scope-table row for `mix verify.phase112` covering README, `website/`, `guides/scope-and-reopen-policy.md`, and other public truth-copy changes. Keep `verify.phase112` standalone per D-15: do not add it to `verify.phase99`, `verify.adopter`, or CI jobs.</action>
  <verify>
    <automated>mix test test/mix/tasks/verify.phase112_test.exs && mix verify.phase112</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/verify.phase112.ex` exists and contains `test/scrypath/phase112_contract_test.exs` and `test/mix/tasks/verify.phase112_test.exs` in `@focused_tests`.
    - `lib/mix/tasks/verify.phase112.ex` contains `verify.phase112: public website and docs truth alignment checks`.
    - `test/mix/tasks/verify.phase112_test.exs` asserts no-arg failure, help output, focused file list, and `preferred_envs` registration.
    - `mix.exs` contains `"verify.phase112": :test`.
    - `CONTRIBUTING.md` contains `mix verify.phase112`.
    - `mix test test/mix/tasks/verify.phase112_test.exs && mix verify.phase112` passes.
  </acceptance_criteria>
  <done>Maintainers can run one standalone, service-free Phase 112 verification command and discover it from normal contributor guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public truth surfaces -> focused contract suite | Drift in claim envelope or route-map boundaries must be translated into deterministic assertions without over-scanning the repo. |
| task source/help -> maintainer behavior | If `verify.phase112` help, source, registration, and contributor docs drift apart, maintainers may assume proof exists when it does not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-09 | Tampering | `test/scrypath/phase112_contract_test.exs` | mitigate | Use explicit positive tokens, scoped negative phrase lists, and website-only runbook-token checks instead of a noisy repo-wide scanner. |
| T-112-10 | Repudiation | `lib/mix/tasks/verify.phase112.ex` and `test/mix/tasks/verify.phase112_test.exs` | mitigate | Self-test the source/help text, focused file list, and no-arg behavior so the command contract stays auditable. |
| T-112-11 | Denial of Service | `mix.exs` and `CONTRIBUTING.md` discoverability | mitigate | Register `verify.phase112` in `preferred_envs` and document when to run it in `CONTRIBUTING.md`. |
| T-112-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs are introduced in this plan. |
</threat_model>

<verification>
- `mix test test/scrypath/phase112_contract_test.exs`
- `mix test test/mix/tasks/verify.phase112_test.exs && mix verify.phase112`
</verification>

<success_criteria>
- `mix verify.phase112` exists, runs only focused Phase 112 truth checks, and stays service-free.
- Contract tests catch claim-envelope, route-map, and scope-policy drift on the targeted public surfaces.
- Maintainers can discover and run the new verify command without changing existing CI or trust-lane topology.
</success_criteria>

<output>
Create `.planning/phases/112-public-website-and-docs-truth-alignment/112-04-SUMMARY.md` when done
</output>
