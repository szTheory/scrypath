---
phase: 52-actionable-errors-and-onboarding-pitfalls
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/scrypath/search.ex
  - lib/scrypath/search/error.ex
  - lib/scrypath/options.ex
  - test/scrypath/search_test.exs
  - test/scrypath/search_many_test.exs
autonomous: true
requirements:
  - ONBD-04
must_haves:
  truths:
    - Tagged tuple shapes for documented public errors in scope are unchanged (same atoms/tuples); only messages, metadata, docs, or exception wrapping for bang APIs improve.
    - Bang search functions raise a named Scrypath.Search exception type, not RuntimeError, with messages that include guide pointers where CONTEXT requires.
    - Public @doc for search/3 and search_many/2 states which failures are {:error, _} vs ArgumentError per D-05.
  artifacts:
    - lib/scrypath/search/error.ex
    - lib/scrypath/search.ex
---

<objective>
Implement **ONBD-04** for the bounded paths in **52-CONTEXT** **D-01**, **D-03**–**D-06**: clearer **`{:error, _}`** legibility and doc anchors for config/backend, **`search_many/2`** preflight failures, and **`{:transport_failed, _}`**; replace **`RuntimeError`** in bang search wrappers with **`Scrypath.Search.Error`** (or the exact name chosen under **D-06**); document **tuple vs `ArgumentError`** split on **`search/3`** / **`search_within_facet/4`**.
</objective>

<threat_model>
**Trust boundary:** Library return values and exception messages visible to applications and logs.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02 | Information disclosure | Error messages | MITIGATE | Do not echo API keys or raw HTTP bodies; keep `inspect` depth bounded for nested terms. |
| T-52-03 | Unexpected API change | Exception type | MITIGATE | Document new exception in `@doc` for each bang function; keep `{:error, tag, ...}` tags stable. |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-actionable-errors-and-onboarding-pitfalls/52-CONTEXT.md
@.planning/REQUIREMENTS.md
@lib/scrypath/search.ex
@guides/meilisearch-operations.md
@guides/multi-index-search.md
@guides/drift-recovery.md
</context>

<tasks>

<task type="auto">
  <name>Add Scrypath.Search.Error exception module</name>
  <files>lib/scrypath/search/error.ex</files>
  <read_first>lib/scrypath/search.ex, .planning/phases/52-actionable-errors-and-onboarding-pitfalls/52-CONTEXT.md</read_first>
  <action>
    Add **`lib/scrypath/search/error.ex`** defining **`defexception`** with field **`:reason`** (the original `{:error, term()}` reason or transport term). Implement **`message/1`** so strings include: (1) a factual one-line classification; (2) a literal pointer substring **`guides/meilisearch-operations.md`** when the reason is **`{:transport_failed, _}`**; (3) for federation / multi-index preflight reasons (`:invalid_options` variants tied to federation or **`:all`** expansion), include **`guides/multi-index-search.md`**. Do not claim automatic recovery steps. Export only what ExDoc should show if public (`@moduledoc` optional one-liner).
  </action>
  <verify>mix compile --warnings-as-errors 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - File `lib/scrypath/search/error.ex` exists and defines `defexception`.
    - `grep -E 'defexception' lib/scrypath/search/error.ex` exits **0**.
    - `grep -F 'guides/meilisearch-operations.md' lib/scrypath/search/error.ex` exits **0** (transport path mentions guide).
    - `grep -F 'guides/multi-index-search.md' lib/scrypath/search/error.ex` exits **0** OR exception delegates to a shared formatter function in `search.ex` that contains that substring — in the latter case `grep -F 'guides/multi-index-search.md' lib/scrypath/search.ex` exits **0**.
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

<task type="auto">
  <name>Wire bang functions and enrich stable {:error, _} paths</name>
  <files>lib/scrypath/search.ex</files>
  <read_first>lib/scrypath/search.ex, lib/scrypath/search/error.ex, lib/scrypath/config.ex</read_first>
  <action>
    Replace **`raise RuntimeError, ...`** in **`search!/3`**, **`search_many!/2`**, and **`search_within_facet!/4`** with **`raise Scrypath.Search.Error, reason: ...`** passing the same structured reason the non-bang API would return. Where **`{:error, {:transport_failed, reason}}`** is built in **`run_native_search_many`**, attach operator-facing hint text (e.g. second element becomes tuple **`{:transport_failed, %{reason: reason, hint: "..."}}`** **only if** you preserve a **backward-compatible** pattern for existing **`match?({:transport_failed, _}, elem(err, 1))`** callers — **preferred:** keep **`{:transport_failed, reason}`** tuple shape unchanged and improve messaging only inside **`Scrypath.Search.Error`** and **`@doc`** examples, not the tuple payload. Update **`@doc`** on **`search/3`**, **`search_within_facet/4`**, and **`search_many/2`** with a short **“Errors vs raises”** bullet: **`ArgumentError`** when NimbleOptions validation fails for misuse; **`{:error, _}`** for operational failures. Ensure **`@spec`** lines for bang functions say **`no_return()`** on error path if appropriate or keep accurate return types.
  </action>
  <verify>grep -n "RuntimeError" lib/scrypath/search.ex</verify>
  <acceptance_criteria>
    - `grep 'raise RuntimeError' lib/scrypath/search.ex` exits **1** (no RuntimeError raises remain in this module).
    - `grep 'Scrypath.Search.Error' lib/scrypath/search.ex` exits **0**.
    - `grep -i 'ArgumentError' lib/scrypath/search.ex` exits **0** (module still documents or uses ArgumentError in validation path).
    - `grep -F 'Errors vs raises' lib/scrypath/search.ex` OR `grep -F 'raises `ArgumentError`' lib/scrypath/search.ex` exits **0** (explicit doc split).
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

<task type="auto">
  <name>Regression tests for exception and messages</name>
  <files>test/scrypath/search_test.exs, test/scrypath/search_many_test.exs</files>
  <read_first>test/scrypath/search_test.exs, test/scrypath/search_many_test.exs, lib/scrypath/search.ex</read_first>
  <action>
    Add or extend tests so that: (1) **`search!/3`** (or the narrowest existing case) raises **`Scrypath.Search.Error`** and **not** **`RuntimeError`** when the underlying **`search/3`** returns **`{:error, _}`**; (2) at least one assertion on **`Exception.message/1`** containing **`guides/meilisearch-operations.md`** for a **`{:transport_failed, _}`**-like path **if** tests can reach it with a stub backend; if not feasible without heavy setup, assert **`Scrypath.Search.Error`** message contains **`guides/multi-index-search.md`** for a lightweight **`{:invalid_options, _}`** from **`search_many/2`** instead. Do not assert full HTTP payloads.
  </action>
  <verify>mix test test/scrypath/search_many_test.exs --trace 2>&1 | tail -20</verify>
  <acceptance_criteria>
    - `mix test test/scrypath/search_many_test.exs test/scrypath/search_test.exs` exits **0**.
    - At least one test file contains the substring **`Scrypath.Search.Error`**.
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

<task type="auto">
  <name>Options @doc — federation / search_many preflight pointers</name>
  <files>lib/scrypath/options.ex</files>
  <read_first>lib/scrypath/options.ex, guides/multi-index-search.md, .planning/phases/52-actionable-errors-and-onboarding-pitfalls/52-CONTEXT.md</read_first>
  <action>
    On **`validate_search_options/2`** (or the primary **`@doc`** block that lists **`{:error, _}`** shapes for search options), add **one** short paragraph stating that **`{:invalid_options, _}`** and related federation / **`:all`** failures are explained in **`guides/multi-index-search.md`** — no new tuple tags, only documentation and optional error reason strings that already exist if NimbleOptions returns human text.
  </action>
  <verify>grep -n "validate_search_options" lib/scrypath/options.ex | head -3</verify>
  <acceptance_criteria>
    - `grep -F 'guides/multi-index-search.md' lib/scrypath/options.ex` exits **0**.
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

</tasks>

<verification>
- [ ] `mix test test/scrypath/search_many_test.exs test/scrypath/search_test.exs` exits 0
- [ ] `mix test` exits 0
</verification>

<success_criteria>
- ONBD-04 bounded error surfaces improved without inventing recovery fiction
- Bang APIs use named exception; tuple tags semver-stable
</success_criteria>

<output>
After completion, create `.planning/phases/52-actionable-errors-and-onboarding-pitfalls/52-02-SUMMARY.md`
</output>
