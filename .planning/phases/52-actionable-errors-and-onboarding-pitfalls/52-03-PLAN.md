---
phase: 52-actionable-errors-and-onboarding-pitfalls
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/scrypath.ex
  - lib/mix/tasks/scrypath.status.ex
  - lib/mix/tasks/scrypath.reconcile.ex
  - lib/mix/tasks/scrypath.retry.ex
  - lib/mix/tasks/scrypath.failed.ex
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - ONBD-06
must_haves:
  truths:
    - lib/scrypath.ex @moduledoc contains markdown link to guides/golden-path.md before guides/sync-modes-and-visibility.md (order check via file positions).
    - Primary operator Mix tasks (status, reconcile, retry, failed) @moduledoc blocks include the same two-hop links in one contiguous "Read next" style subsection.
    - docs_contract_test asserts lobby or task doc substrings if Phase 52 adds contract anchors for ONBD-06.
  artifacts:
    - lib/scrypath.ex
    - lib/mix/tasks/scrypath.status.ex
---

<objective>
Deliver **ONBD-06**: reframe **`lib/scrypath.ex`** **`@moduledoc`** as the library **lobby** (product narrative + two-hop reading: **`guides/golden-path.md`** then **`guides/sync-modes-and-visibility.md`**, optional third link to **`guides/overview.md`** and link to **`guides/common-mistakes.md`**) per **D-12**–**D-15**; align primary **`mix scrypath.*`** task **`@moduledoc`** blocks (**`scrypath.status`**, **`scrypath.reconcile`**, **`scrypath.retry`**, **`scrypath.failed`**) with the same link block.
</objective>

<threat_model>
**Trust boundary:** HexDocs module pages and `mix help` text.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03 | Information disclosure | Examples in moduledoc | MITIGATE | No live hosts or secrets in new examples. |
| T-52-04 | Tampering | External URLs | MITIGATE | Use only `guides/...` and in-repo paths in new paragraphs. |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-actionable-errors-and-onboarding-pitfalls/52-CONTEXT.md
@lib/scrypath.ex
@lib/mix/tasks/scrypath.status.ex
@lib/mix/tasks/scrypath.reconcile.ex
@lib/mix/tasks/scrypath.retry.ex
@lib/mix/tasks/scrypath.failed.ex
@guides/golden-path.md
@guides/sync-modes-and-visibility.md
</context>

<tasks>

<task type="auto">
  <name>Scrypath lobby @moduledoc</name>
  <files>lib/scrypath.ex</files>
  <read_first>lib/scrypath.ex, .planning/phases/52-actionable-errors-and-onboarding-pitfalls/52-CONTEXT.md, guides/golden-path.md, guides/sync-modes-and-visibility.md, guides/common-mistakes.md</read_first>
  <action>
    Rewrite **`lib/scrypath.ex`** **`@moduledoc`** to: (1) one short product paragraph (schema → sync → search); (2) explicit “Read next” (or equivalent) subsection where the **first** markdown link target string is **`guides/golden-path.md`** and the **second** is **`guides/sync-modes-and-visibility.md`** (markdown link or backtick path accepted by contract test); (3) link **`Scrypath.Schema`** for declaration grammar instead of duplicating option tables; (4) add a single line pointing to **`guides/common-mistakes.md`**; (5) optional bullet linking **`guides/overview.md`** as TOC only. Preserve existing **`__using__`** macro and function APIs unchanged.
  </action>
  <verify>grep -n "moduledoc" -n lib/scrypath.ex | head -1</verify>
  <acceptance_criteria>
    - `grep -F 'guides/golden-path.md' lib/scrypath.ex` exits **0**.
    - `grep -F 'guides/sync-modes-and-visibility.md' lib/scrypath.ex` exits **0**.
    - `grep -F 'guides/common-mistakes.md' lib/scrypath.ex` exits **0**.
    - `grep -F 'Scrypath.Schema' lib/scrypath.ex` exits **0**.
    - Order **golden-path before sync-modes** inside **`@moduledoc` only** is enforced by the new **`docs_contract_test`** added in the next task (not by loose `grep` on the whole file).
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

<task type="auto">
  <name>Operator Mix task @moduledoc blocks</name>
  <files>lib/mix/tasks/scrypath.status.ex, lib/mix/tasks/scrypath.reconcile.ex, lib/mix/tasks/scrypath.retry.ex, lib/mix/tasks/scrypath.failed.ex</files>
  <read_first>lib/mix/tasks/scrypath.status.ex, lib/mix/tasks/scrypath.reconcile.ex, lib/mix/tasks/scrypath.retry.ex, lib/mix/tasks/scrypath.failed.ex, lib/scrypath.ex</read_first>
  <action>
    Expand **`@moduledoc`** for **`Mix.Tasks.Scrypath.Status`**, **`Mix.Tasks.Scrypath.Reconcile`**, **`Mix.Tasks.Scrypath.Retry`**, and **`Mix.Tasks.Scrypath.Failed`** so each contains a shared short block (can be near-duplicated 4×) with markdown links to **`guides/golden-path.md`** and **`guides/sync-modes-and-visibility.md`** explaining first-hour vs sync authority. Tighten **`@shortdoc`** only if a string is vague (single-line). Do not change CLI argv parsing or **`run/1`** behavior.
  </action>
  <verify>grep -l "golden-path" lib/mix/tasks/scrypath.status.ex lib/mix/tasks/scrypath.reconcile.ex lib/mix/tasks/scrypath.retry.ex lib/mix/tasks/scrypath.failed.ex | wc -l</verify>
  <acceptance_criteria>
    - `grep -F 'guides/golden-path.md' lib/mix/tasks/scrypath.status.ex lib/mix/tasks/scrypath.reconcile.ex lib/mix/tasks/scrypath.retry.ex lib/mix/tasks/scrypath.failed.ex | wc -l` prints **4** (each file has at least one occurrence).
    - `grep -F 'guides/sync-modes-and-visibility.md' lib/mix/tasks/scrypath.status.ex lib/mix/tasks/scrypath.reconcile.ex lib/mix/tasks/scrypath.retry.ex lib/mix/tasks/scrypath.failed.ex | wc -l` prints **4**.
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

<task type="auto">
  <name>Docs contract anchors for lobby + tasks</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>test/scrypath/docs_contract_test.exs, lib/scrypath.ex, lib/mix/tasks/scrypath.status.ex</read_first>
  <action>
    Add **one** focused **`test`** in **`Scrypath.DocsContractTest`** (name containing **`lobby`**, **`two_hop`**, or **`golden_path_first`**) that extracts the **`@moduledoc """ ... """`** block from **`lib/scrypath.ex`** (first module attribute only) and asserts **`guides/golden-path.md`** appears at a **lower character index** than **`guides/sync-modes-and-visibility.md`** inside that block. Optionally repeat the same index check for **`File.read!("lib/mix/tasks/scrypath.status.ex")`** `@moduledoc` substring. Do not embed internal planning tokens.
  </action>
  <verify>mix test test/scrypath/docs_contract_test.exs 2>&1 | tail -8</verify>
  <acceptance_criteria>
    - `mix test test/scrypath/docs_contract_test.exs` exits **0**.
    - New test name includes substring **`lobby`** OR **`two_hop`** OR **`golden_path_first`** (grep `test/scrypath/docs_contract_test.exs` for one of these).
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

</tasks>

<verification>
- [ ] `mix test test/scrypath/docs_contract_test.exs` exits 0
- [ ] Manual: `mix help scrypath.status` shows golden-path and sync-modes links in the moduledoc body
</verification>

<success_criteria>
- Newcomers hitting `Scrypath` and primary operator tasks see golden path + sync authority within two reading hops
- Contract test prevents accidental reordering or deletion of those pointers
</success_criteria>

<output>
After completion, create `.planning/phases/52-actionable-errors-and-onboarding-pitfalls/52-03-SUMMARY.md`
</output>
