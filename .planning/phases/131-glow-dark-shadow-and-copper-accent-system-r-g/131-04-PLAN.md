---
phase: 131-glow-dark-shadow-and-copper-accent-system-r-g
plan: 04
type: execute
wave: 3
depends_on: ["131-02", "131-03"]
files_modified:
  - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md
autonomous: false
requirements: [GLOW-01, COPPER-01]

must_haves:
  truths:
    - "The full D-11 proof bundle is green: mix verify.opsui exit 0; contrast-checker 3 AA / 12 AAA; light-pixel-diff 0/20; test:e2e:admin-contrast Cluster 1 = 0"
    - "Cluster 3 primary-violet is recorded as out-of-scope-known-fail deferred to Phase 132 (NOT gated here)"
    - "The copper 9-pairing AA table is manually re-confirmed (static spec assertion, F8)"
    - "A human confirms dark seated-depth + quiet-glow restraint and the copper eyebrow read correctly"
  artifacts:
    - path: ".planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md"
      provides: "Filled per-task verification map with green/red statuses + recorded gate outputs"
      contains: "Failed pairs"
  key_links:
    - from: "131-VALIDATION.md Per-Task Verification Map"
      to: "D-11 bundle command outputs"
      via: "recorded exit codes + counts"
      pattern: "0 / 20"
---

<objective>
Run the D-11 proof bundle as the phase gate and record results — no new tooling (Phase 130
bundle re-run). Confirm: `mix verify.opsui` exit 0; `node contrast-checker.mjs` 3 AA / 12 AAA
(Phase 128 baseline); `node e2e/light-pixel-diff.mjs` Failed pairs 0/20; `npm run
test:e2e:admin-contrast` Cluster 1 = 0 (Cluster 3 primary-violet recorded as deferred to Phase
132, NOT gated). Manually re-confirm the copper 9-pairing AA table (static assertion, F8). Then a
human verifies dark seated-depth, quiet-glow restraint, and the copper eyebrow.

This plan blocks on Plan 02 (app.css application) and Plan 03 (eyebrow + docs) — all CSS is
landed before the gate runs.

Purpose: prove GLOW-01 + COPPER-01 met and light is pixel-identical (the `[G]` cross-AI gate).
Output: filled `131-VALIDATION.md` with recorded gate outputs + statuses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-UI-SPEC.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-01-SUMMARY.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-02-SUMMARY.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the D-11 proof bundle and record results</name>
  <files>.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md</files>
  <read_first>
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md (Per-Task Verification Map + Sampling Rate + Wave 0)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md (Validation Architecture: Phase Gate + Cluster scoping + Findings F4, F8, F9; Pitfall 5)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-02-SUMMARY.md (prior pixel-diff/contrast results)
  </read_first>
  <action>
    Rebuild assets first (`mix assets.build` in scrypath_ops) so all four gate scripts see the final
    app.css (Pitfall 5 — diffing stale assets gives false results). Then run the bundle and capture
    each output verbatim into 131-VALIDATION.md's Per-Task Verification Map (mark each ✅/❌):
    (1) `mix verify.opsui` (from scrypath_ops; needs Postgres — runs ecto.create/migrate + 129 tests +
    4 a11y) → expect exit 0.
    (2) `node contrast-checker.mjs` (from examples/scrypath_ecommerce/) → expect 3 AA / 12 AAA (Phase
    128 baseline; the new copper refs bypass the harness per F8, so the count MUST be unchanged — a
    changed count is a regression, not copper detection).
    (3) `node e2e/light-pixel-diff.mjs` (from examples/scrypath_ecommerce/; no npm script — invoke
    directly, F9) → expect "Failed pairs: 0 / 20", exit 0. Trust this only if Plan 01 recorded the
    baseline FRESH/RE-CAPTURED.
    (4) `npm run test:e2e:admin-contrast` (needs a running dev server) → the 131 gate is **Cluster 1 = 0**,
    NOT matrix exit 0. Record Cluster 1 = 0; record Cluster 3 (primary-violet `#6c5ce7` @ 4.3:1 on
    cream — `.ops-nav-item-active` / `.bg-primary`) as OUT-OF-SCOPE KNOWN-FAIL deferred to Phase 132
    (A11Y-TOKEN-01), exactly as 130-VERIFICATION did. Do NOT gate on Cluster 3.
    If the dev server / Postgres / Playwright are unavailable in this environment, fall back to the
    static-grep substitutes per the 130-VERIFICATION pattern (grep that glow=none light default, the
    compose layers, panel-dark dark-only) and record the fallback explicitly. Set
    `nyquist_compliant: true` in the VALIDATION.md frontmatter once gates are green.
  </action>
  <verify>
    <automated>grep -q 'Failed pairs' .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md && echo RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.opsui` result recorded as exit 0 (or fallback documented with reason)
    - `node contrast-checker.mjs` recorded as 3 AA / 12 AAA (unchanged from Phase 128 baseline)
    - `node e2e/light-pixel-diff.mjs` recorded as "Failed pairs: 0 / 20"
    - `test:e2e:admin-contrast` Cluster 1 recorded = 0; Cluster 3 recorded as deferred-to-132 known-fail (not gated)
    - When dev server + Postgres are available, the REAL `Cluster 1 = 0` count and the REAL `mix verify.opsui` exit code are recorded verbatim (not a grep-fallback substitute); any check that could not be run live is flagged `FALLBACK: <reason>` in VALIDATION.md so the Task 3 human checkpoint scrutinizes it
    - VALIDATION.md statuses filled (✅/❌); `nyquist_compliant: true` set when green
  </acceptance_criteria>
  <done>The D-11 bundle is recorded green in VALIDATION.md; Cluster 3 documented as out-of-scope-known-fail for Phase 132.</done>
</task>

<task type="auto">
  <name>Task 2: Manually re-confirm the copper 9-pairing AA table (static assertion, F8)</name>
  <files>.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md</files>
  <read_first>
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-UI-SPEC.md (## Color — Phase 131 color pairings complete AA evidence table; the verified AA pairings list)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md (Finding F8 — copper AA is NOT machine-checked)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>
    The contrast harness does NOT track the copper classes' raw `var(--color-secondary)` /
    `var(--color-base-content)` / `var(--color-secondary-content)` refs (F8), so the 9-pairing copper
    AA table is a STATIC design assertion the planner/executor must manually re-confirm. Re-compute (or
    re-read the UI-SPEC's computed values) each pairing using the WCAG 2.1 sRGB relative-luminance
    formula (D-12 compliant) against the live hex values from the app.css `@plugin` blocks
    (dark `--color-secondary` #c17a3e / `--color-base-content` #f4f1ea / `--color-secondary-content`
    #0c0f14; light #a85d2e / #141923 / #fffdf8; surface-1 dark #141923→ wait, surface-1 dark is the Ink
    panel #141923; surface-2 dark #1b2230 — use the values from RESEARCH F2). Confirm every row reads AA
    PASS: base-content on copper-badge tinted bg (dark 12.07:1 / light 14.86:1); copper-eyebrow secondary
    text on surface-1 (dark 5.13:1 / light 4.84:1); secondary-content on solid copper (dark 5.59:1);
    copper text on surface-2 dark (4.64:1). Record the verdict (all PASS / any FAIL) in VALIDATION.md's
    Manual-Only Verifications row. If any pairing fails, flag it — but per the spec all 9 were verified
    PASS upstream, so this is a re-confirmation, not a re-derivation of the design.
  </action>
  <verify>
    <automated>grep -qi 'copper.*AA' .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md && echo COPPER_AA_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 9 copper AA pairings is re-confirmed against live `@plugin` hex values and recorded
    - The verdict (all PASS / any FAIL) is written into VALIDATION.md's Manual-Only Verifications row
    - The badge-text rule (base-content, not secondary) is reflected — the copper-badge pairing uses base-content text
  </acceptance_criteria>
  <done>The copper 9-pairing AA table is manually re-confirmed all-PASS (static F8 assertion) and recorded in VALIDATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify dark seated-depth, quiet-glow restraint, copper eyebrow, and light non-regression</name>
  <what-built>
    Three brand-signature dark layers, CSS/token-only, light pixel-identical:
    (1) dark seated-depth ambient shadow on `.ops-panel` / `.ops-intent-card` / `.ops-cmdk__panel` /
    `#flash-group`; (2) a quiet violet glow on the route mark, active nav pill, and recommended
    intent-card; (3) the copper eyebrow re-style (all 6 screens) + the shell-wash mobile tune.
    Automated gates (D-11 bundle) are recorded green and the copper AA table re-confirmed.
  </what-built>
  <how-to-verify>
    1. Boot the admin UI in DARK mode (toggle or OS dark) and visit Control Room, Posture, Sync/Drift,
       Search, Playbooks.
    2. Confirm `.ops-panel` / intent-card / command palette (⌘K) / flash messages read as SEATED depth
       (pressed into the surface with the border), not floating.
    3. Confirm the violet glow is QUIET (not loud) and appears ONLY on the route mark, the active nav
       pill, and the recommended intent-card — NEVER on text, resting panels, or background floods.
    4. Confirm the page eyebrow ("Operator workspace") renders in COPPER (not white) on every screen,
       at roughly the brand's 5% accent ratio.
    5. Switch to LIGHT mode and confirm nothing changed vs before this phase (the 0/20 pixel-diff backs
       this; eyeball the recommended card keeps its violet-ring light shadow).
    6. At a mobile width (≤640px) confirm the shell radial wash is subtler (10% / 28rem) and still clean.
  </how-to-verify>
  <action>PAUSE for human verification. Present the what-built summary and the how-to-verify steps below, then wait for the resume-signal. Do not proceed until the human approves. This is a blocking checkpoint — automated gates (Tasks 1-2) are already green; this is the perceptual sign-off on dark seated-depth, glow restraint, copper read, and light non-regression.</action>
  <resume-signal>Type "approved" or describe any seated-depth / glow-restraint / copper / light-regression issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan: runs existing gate scripts and records results in a planning doc. No production code changes, no input/auth/network/data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-04 | Tampering | gate scripts (contrast-checker / pixel-diff / mix verify) | accept | Pre-existing Phase-128/130 scripts; this plan only executes and records. CSS parsed as text only (T-128-03). Near-zero attack surface. |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH audit N/A; pixelmatch/pngjs already present). No supply-chain surface. |
</threat_model>

<verification>
- D-11 bundle recorded green: `mix verify.opsui` exit 0; `contrast-checker` 3 AA / 12 AAA;
  `light-pixel-diff` 0/20; `test:e2e:admin-contrast` Cluster 1 = 0 (Cluster 3 deferred to 132).
- Copper 9-pairing AA table re-confirmed all PASS.
- Human checkpoint approved (seated depth, glow restraint, copper eyebrow, light non-regression).
</verification>

<success_criteria>
- GLOW-01 + COPPER-01 proven met; light pixel-identical (0/20).
- Cluster 3 documented as out-of-scope-known-fail deferred to Phase 132 (not gated).
- Human confirms the dark signature + copper read correctly and light is unchanged.
</success_criteria>

<output>
Create `.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-04-SUMMARY.md` when done.
Record the full D-11 bundle outputs, the copper AA re-confirmation verdict, and the human-verify result.
</output>
