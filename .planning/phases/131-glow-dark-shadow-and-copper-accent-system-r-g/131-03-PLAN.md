---
phase: 131-glow-dark-shadow-and-copper-accent-system-r-g
plan: 03
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex
  - scrypath_ops/assets/css/DESIGN-TOKENS.md
autonomous: true
requirements: [COPPER-01, GLOW-01]

must_haves:
  truths:
    - "The shared ops_page_header eyebrow renders via .ops-copper-eyebrow (copper) on all 6 screens; the string 'Operator workspace' is unchanged"
    - "DESIGN-TOKENS.md documents the three new shadow tokens and the copper vocabulary in lockstep with app.css"
  artifacts:
    - path: "scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex"
      provides: "Eyebrow slot re-styled from inline text-secondary utilities to .ops-copper-eyebrow"
      contains: "ops-copper-eyebrow"
    - path: "scrypath_ops/assets/css/DESIGN-TOKENS.md"
      provides: "Two new sections documenting glow/dark-depth tokens and the copper accent vocabulary"
      contains: "Copper accent vocabulary"
  key_links:
    - from: "ops_ui.ex eyebrow <p> (~L23)"
      to: ".ops-copper-eyebrow class (declared in Plan 01)"
      via: "utility-class swap"
      pattern: "ops-copper-eyebrow"
    - from: "DESIGN-TOKENS.md"
      to: "app.css new tokens/classes"
      via: "lockstep mirror sections"
      pattern: "shadow-ops-panel-dark"
---

<objective>
Land COPPER-01's in-situ proof and the lockstep documentation. Swap the four inline utility
classes on the shared `ops_page_header` eyebrow slot (`ops_ui.ex:23`) for the single
`.ops-copper-eyebrow` class declared in Plan 01 — one edit, propagates to all 6 screens, the
phase's only in-situ copper application (D-01). Then add the two mandated `DESIGN-TOKENS.md`
sections so the design doc stays in lockstep with app.css (project convention).

This plan touches `ops_ui.ex` + `DESIGN-TOKENS.md` only — zero overlap with Plan 02's `app.css`
edits — so it runs in the same wave, in parallel.

Purpose: COPPER-01 in-situ proof (eyebrow) + the lockstep mirror obligation for BOTH GLOW-01 and
COPPER-01 tokens/classes.
Output: edited `ops_ui.ex` (1-line re-style) + `DESIGN-TOKENS.md` (+2 sections).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-UI-SPEC.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-PATTERNS.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-01-SUMMARY.md

<interfaces>
<!-- Class declared by Plan 01 (consume directly): .ops-copper-eyebrow -->

Eyebrow slot — ops_ui.ex:22-25 (path uses scrypath_ops_web — verified):
  current:
    `<div class="space-y-1">`
    `  <p class="text-ops-sm font-semibold uppercase tracking-wide text-secondary">`
    `    Operator workspace`
  → replace the four utility classes on the <p> with the single class `ops-copper-eyebrow`.
    String "Operator workspace" UNCHANGED. The `space-y-1` wrapper div is untouched.

DESIGN-TOKENS.md precedent section — DESIGN-TOKENS.md:87-93 (`## Shadow — --shadow-ops-* → shadow-ops-*`):
  header + prose + ladder description. Follow the file's `## Token — usage` header convention.

New tokens to document (from UI-SPEC Lockstep Obligations):
  | --shadow-ops-panel-dark | (not declared in light) | 0 0 0 1px rgba(0,0,0,0.30), 0 1px 3px rgba(0,0,0,0.45) | ambient seated depth on dark panels |
  | --shadow-ops-glow       | none | 0 0 8px 2px rgba(108,92,231,0.30) | quiet violet glow — route mark / active nav / key-callout hover only |
  | --shadow-ops-glow-copper| none | 0 0 6px 1px rgba(193,122,62,0.25) | quiet copper glow — key-node hover only (no consumer yet → Phase 133/134) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-style the shared eyebrow slot to .ops-copper-eyebrow</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex</files>
  <read_first>
    - scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex (lines 20-35 — the ops_page_header eyebrow slot; confirm L23 still holds the text-secondary <p>)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-PATTERNS.md (section 6 — eyebrow re-style)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md (Finding F5 — eyebrow slot verbatim)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-01-SUMMARY.md (confirm .ops-copper-eyebrow was declared in app.css)
  </read_first>
  <action>
    Re-grep `text-secondary` in ops_ui.ex to locate the eyebrow <p> (it should be ~L23). On that one
    `<p>` element, replace the four utility classes
    `text-ops-sm font-semibold uppercase tracking-wide text-secondary` with the single class
    `ops-copper-eyebrow`. Leave the string "Operator workspace" and the enclosing `space-y-1` div
    untouched. This is a RE-STYLE of an existing slot, NOT new markup — one shared component slot that
    propagates to all 6 screens. Do not edit any other `<p>` or any other use of `text-secondary` in
    the file (this is the lone in-situ copper application for Phase 131; per-screen copper badges/nodes
    are deferred to Phase 134, D-01a). Confirm `.ops-copper-eyebrow` exists in app.css (Plan 01) before
    relying on it.
  </action>
  <verify>
    <automated>grep -c 'ops-copper-eyebrow' scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex | grep -qx 1 && echo EYEBROW_OK</automated>
  </verify>
  <acceptance_criteria>
    - `ops-copper-eyebrow` appears exactly once in ops_ui.ex (on the eyebrow <p>)
    - The eyebrow <p> no longer carries the inline `text-ops-sm font-semibold uppercase tracking-wide text-secondary` utilities
    - The literal string "Operator workspace" is unchanged (grep confirms it still present)
    - No other `text-secondary` usage in the file was touched
    - `mix compile` (scrypath_ops) succeeds with no new warnings
  </acceptance_criteria>
  <done>The shared eyebrow renders copper via .ops-copper-eyebrow across all 6 screens; string and surrounding markup unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two DESIGN-TOKENS.md lockstep sections</name>
  <files>scrypath_ops/assets/css/DESIGN-TOKENS.md</files>
  <read_first>
    - scrypath_ops/assets/css/DESIGN-TOKENS.md (lines 87-93 — the existing ## Shadow section, to mirror format; scan the file's header convention at L43/87/110)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-PATTERNS.md (section 7 — DESIGN-TOKENS.md lockstep)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-UI-SPEC.md (## DESIGN-TOKENS.md Lockstep Obligations — the exact 3-row token table + copper allowed-use rules + AA pairing table)
  </read_first>
  <action>
    Add two new sections mirroring the existing `## Shadow` section format (header + prose + table).
    Section 1 `## Glow + dark ambient depth — Phase 131`: the 3-row token table
    (`--shadow-ops-panel-dark` dark-only, `--shadow-ops-glow` none→violet, `--shadow-ops-glow-copper`
    none→copper) plus a note that `--shadow-ops-panel-dark` is a dark-only augmentation and light panels
    keep `--shadow-ops-surface` vertical lift; note `--shadow-ops-glow-copper` has no consumer in 131
    (reserved for Phase 133/134 hover). Section 2 `## Copper accent vocabulary — Phase 131`: allowed-use
    rules for `.ops-copper-eyebrow` / `.ops-copper-badge` / `.ops-copper-node[--fill]`, the 9-pairing AA
    evidence table from UI-SPEC's color section, the badge-text rule (always `var(--color-base-content)`,
    never `var(--color-secondary)` as badge label), the eyebrow-text rule (`var(--color-secondary)` is
    AA-safe only as eyebrow on surface-1), and the hard rule "copper is a brand accent, NEVER a status
    tone — not in `tone_class/1` / `badge_class/1`". Also append a one-line note to the existing
    `## Shadow` section: `--shadow-ops-panel-dark` is a dark-only augmentation; light keeps the
    `--shadow-ops-surface` vertical lift. Wording is Discretion; values must match app.css exactly
    (lockstep). This is documentation only — no code.
  </action>
  <verify>
    <automated>grep -q 'Glow + dark ambient depth — Phase 131' scrypath_ops/assets/css/DESIGN-TOKENS.md && grep -q 'Copper accent vocabulary — Phase 131' scrypath_ops/assets/css/DESIGN-TOKENS.md && grep -q 'shadow-ops-panel-dark' scrypath_ops/assets/css/DESIGN-TOKENS.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both new section headers exist: `## Glow + dark ambient depth — Phase 131` and `## Copper accent vocabulary — Phase 131`
    - The glow section table documents all three tokens with values matching app.css (rgba literals identical)
    - The copper section documents the badge-text rule (base-content), eyebrow-text rule, and the "never a status tone" rule
    - The AA pairing table is present in the copper section
    - The existing `## Shadow` section gains the dark-only-augmentation note
  </acceptance_criteria>
  <done>DESIGN-TOKENS.md documents the glow/dark-depth tokens and copper vocabulary in lockstep with app.css; values match exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A single HEEx class-name swap (server-rendered static attribute) + Markdown documentation. No input, auth, network, or data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-03 | Tampering | ops_ui.ex eyebrow class attribute | accept | Static class literal in a server-rendered template; no interpolation, no user data. Near-zero attack surface. |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH audit N/A). No supply-chain surface. |
</threat_model>

<verification>
- `mix compile` clean (scrypath_ops).
- `mix verify.opsui` exits 0 (the eyebrow re-style changes no behavior; nav-contract + a11y tests pass).
- `grep ops-copper-eyebrow ops_ui.ex` == 1; "Operator workspace" string intact.
- DESIGN-TOKENS.md: both sections present, token values match app.css.
</verification>

<success_criteria>
- SC-3 (in-situ proof): the shared eyebrow renders copper via `.ops-copper-eyebrow` on all 6 screens.
- Lockstep: DESIGN-TOKENS.md mirrors the new tokens + copper vocabulary with exact values.
- No string/behavior change; copper remains a brand accent, never a status tone.
</success_criteria>

<output>
Create `.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-03-SUMMARY.md` when done.
</output>
