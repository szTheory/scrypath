---
phase: 131-glow-dark-shadow-and-copper-accent-system-r-g
plan: 02
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - scrypath_ops/assets/css/app.css
autonomous: true
requirements: [GLOW-01]

must_haves:
  truths:
    - "Dark .ops-panel / .ops-intent-card / .ops-cmdk__panel / #flash-group > * read as seated depth via --shadow-ops-panel-dark + existing border"
    - "Dark .ops-route-mark and .ops-nav-item-active carry --shadow-ops-glow WITHOUT losing their existing box-shadow layer"
    - ".ops-intent-card--recommended composes ring+seat+glow in dark; light stays byte-unchanged"
    - "Shell radial wash is tuned to 10% alpha / 28rem at max-width:640px"
    - "Light render is pixel-identical (Failed pairs: 0 / 20)"
  artifacts:
    - path: "scrypath_ops/assets/css/app.css"
      provides: "Dark-scoped Precedent-B application rules for panel-dark + glow; recommended-card 3-layer dark override; shell wash mobile @media tune"
      contains: "var(--shadow-ops-panel-dark)"
  key_links:
    - from: "dark-scoped blocks (both [data-theme=dark] and @media html:not([data-theme=light]))"
      to: ".ops-panel / .ops-intent-card / #flash-group > * / .ops-cmdk__panel"
      via: "Precedent-B dark override applying panel-dark"
      pattern: "shadow-ops-panel-dark"
    - from: "dark-scoped blocks"
      to: ".ops-route-mark / .ops-nav-item-active"
      via: "composed box-shadow: <existing>, var(--shadow-ops-glow)"
      pattern: "shadow-ops-glow"
---

<objective>
Apply the tokens declared in Plan 01 to their allowed sites via the D-10 dual-path Precedent-B
dark-scoped rule blocks — pure CSS, zero markup. Panel-dark seated depth to the four target
panels; the quiet violet glow to the route mark and active nav pill; the recommended intent-card
3-layer dark composition (D-02); and the shell-wash mobile tune (DK-10). Every already-shadowed
target is COMPOSED (existing layer first), never replaced (RESEARCH F7). Light stays
pixel-identical — the `[G]` gate.

Purpose: this is the visible half of GLOW-01 — it makes the dark surfaces read as seated depth
and lights the route/active path with a quiet glow, while proving light is byte-unchanged.
Output: edited `scrypath_ops/assets/css/app.css` (dark-scoped application blocks + shell @media).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-UI-SPEC.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-PATTERNS.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-01-SUMMARY.md

<interfaces>
<!-- Tokens declared by Plan 01 (consume directly): --shadow-ops-panel-dark, --shadow-ops-glow.
     Live current box-shadow values + line numbers (re-verified). RE-GREP before editing. -->

Precedent B (dark-scoped RULE block, inside @layer components) — two shapes:
- single rule: app.css:531-539 (`select.ops-form-control`)
- grouped/back-to-back: app.css:1279-1295 (`.ops-data-card` / `.ops-result-row`)
- Shape: `[data-theme="dark"] <sel> { ... }` THEN `@media (prefers-color-scheme: dark) { html:not([data-theme="light"]) <sel> { ...identical... } }`

Compose targets (existing box-shadow that MUST be preserved — RESEARCH F7):
- `.ops-panel`            app.css:250  current `var(--shadow-ops-surface)`           → dark: `var(--shadow-ops-panel-dark)` (seated-depth swap)
- `.ops-intent-card`      app.css:851  current `var(--shadow-ops-surface)`           → dark: `var(--shadow-ops-panel-dark)`
- `#flash-group > *`      app.css:1012 current `var(--shadow-ops-overlay)`           → dark: `var(--shadow-ops-overlay), var(--shadow-ops-panel-dark)`  KEEP overlay
- `.ops-cmdk__panel`      app.css:1067 current `var(--shadow-ops-overlay)`           → dark: `var(--shadow-ops-overlay), var(--shadow-ops-panel-dark)`  KEEP overlay
- `.ops-route-mark`       app.css:996  current `0 0 0 1px color-mix(...primary-content 30%...) inset` → dark: `<that inset ring>, var(--shadow-ops-glow)`  KEEP ring
- `.ops-nav-item-active`  app.css:588  current `var(--shadow-ops-surface)`           → dark: `var(--shadow-ops-surface), var(--shadow-ops-glow)`  KEEP surface

Recommended card — app.css:866-870 (LIGHT byte-unchanged, D-02a):
  current: `box-shadow: var(--shadow-ops-surface), inset 0 0 0 1px color-mix(in oklch, var(--color-primary) 45%, transparent);`
  DARK override (D-02, ring→seat→glow order):
    `inset 0 0 0 1px color-mix(in oklch, var(--color-primary) 45%, transparent),`  /* ring on top */
    `var(--shadow-ops-panel-dark),`   /* ambient seat */
    `var(--shadow-ops-glow);`         /* aura behind */

Shell wash — app.css:240-244 base; existing @media (max-width:640px) opens ~app.css:1015:
  base: `radial-gradient(circle at top left, color-mix(in oklch, var(--color-primary) 14%, transparent), transparent 34rem), linear-gradient(180deg, var(--color-base-200), var(--color-base-100))`
  mobile tune: 14%→10%, 34rem→28rem (both-theme change — re-run pixel-diff).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply panel-dark seated depth to the four target panels (Precedent B, both dark paths)</name>
  <files>scrypath_ops/assets/css/app.css</files>
  <read_first>
    - scrypath_ops/assets/css/app.css (lines 246-251 .ops-panel; 842-856 .ops-intent-card; 1010-1013 #flash-group > *; 1057-1069 .ops-cmdk__panel; 531-539 + 1279-1295 Precedent B shapes — read all verbatim before editing)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-PATTERNS.md (section 3 + the compose table + Open Q2 grouping recommendation)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md (Findings F3, F7; Pitfalls 2-3; Code Examples "Applying panel-dark + composing")
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-01-SUMMARY.md (confirm --shadow-ops-panel-dark was declared)
  </read_first>
  <action>
    Re-grep all four target line numbers before editing (they may drift after Plan 01's appends to
    @theme — Plan 01 only touched ~L141-148 and ~L1298-1313, so the 246/851/1012/1067 region anchors
    should be intact, but confirm). Author dark-scoped Precedent-B blocks (do NOT edit the base
    `.ops-panel`/`.ops-intent-card`/`#flash-group`/`.ops-cmdk__panel` definitions — editing the base
    regresses light). Use the Discretion-recommended TWO grouped blocks by base layer:
    GROUP 1 (surface base) — `.ops-panel, .ops-intent-card` → `box-shadow: var(--shadow-ops-panel-dark);`
    (this is the seated-depth swap; in dark they otherwise inherit `--shadow-ops-surface` via Plan 01's
    Precedent-A redefinition, so overriding to panel-dark is the intended swap).
    GROUP 2 (overlay base) — `#flash-group > *, .ops-cmdk__panel` →
    `box-shadow: var(--shadow-ops-overlay), var(--shadow-ops-panel-dark);` (COMPOSE — keep overlay first,
    RESEARCH F7). Each group is written TWICE: once under `[data-theme="dark"]` and once under
    `@media (prefers-color-scheme: dark) { html:not([data-theme="light"]) ... }` (D-10 dual-path —
    forgetting one breaks system-dark, Pitfall 3). Place these blocks near the Phase-130 dark-scoped
    blocks at ~app.css:1279-1295 (same authoring neighborhood). `--shadow-ops-panel-dark` must NEVER
    appear in any light/`@theme` context.
  </action>
  <verify>
    <automated>grep -c 'shadow-ops-panel-dark' scrypath_ops/assets/css/app.css | awk '$1>=6{print "PANEL_OK"; exit} {print "FAIL:"$1}'</automated>
  </verify>
  <acceptance_criteria>
    - `shadow-ops-panel-dark` referenced in dark-scoped application rules for all four selectors across BOTH dark paths
    - `#flash-group > *` and `.ops-cmdk__panel` dark rules list `var(--shadow-ops-overlay)` BEFORE `var(--shadow-ops-panel-dark)` (compose, not replace)
    - Base `.ops-panel`/`.ops-intent-card`/`#flash-group`/`.ops-cmdk__panel` definitions are UNCHANGED (grep the base lines still show original `var(--shadow-ops-surface)`/`var(--shadow-ops-overlay)`)
    - No `--shadow-ops-panel-dark` reference appears outside a `[data-theme="dark"]` or `html:not([data-theme="light"])` scope
  </acceptance_criteria>
  <done>Four dark panels get seated-depth panel-dark via two grouped Precedent-B blocks in both dark paths; overlay panels compose; base definitions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Compose the violet glow onto route-mark, active nav, and the recommended card (dark only)</name>
  <files>scrypath_ops/assets/css/app.css</files>
  <read_first>
    - scrypath_ops/assets/css/app.css (lines 994-997 .ops-route-mark; 584-589 .ops-nav-item-active; 866-870 .ops-intent-card--recommended — read all verbatim, copy the existing box-shadow values exactly)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-PATTERNS.md (section 3 compose table + recommended-card 3-layer compose)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md (Findings F6, F7; D-02 / D-02a in user_constraints)
  </read_first>
  <action>
    Re-grep the three target line numbers first. For `.ops-route-mark`: dark-scoped Precedent-B
    block composing `box-shadow: <existing inset ring verbatim>, var(--shadow-ops-glow);` — copy the
    existing `0 0 0 1px color-mix(in oklch, var(--color-primary-content) 30%, transparent) inset`
    value EXACTLY from L996 so the ring is preserved (F7). For `.ops-nav-item-active`: dark-scoped
    block `box-shadow: var(--shadow-ops-surface), var(--shadow-ops-glow);` (keep the surface lift).
    For `.ops-intent-card--recommended`: LIGHT stays byte-unchanged (do NOT edit the base rule at
    L866-870, D-02a); add a dark-scoped override painting ring→seat→glow:
    `inset 0 0 0 1px color-mix(in oklch, var(--color-primary) 45%, transparent), var(--shadow-ops-panel-dark), var(--shadow-ops-glow);`.
    All three rules authored in BOTH dark paths (D-10 dual-path). Do NOT add `.ops-glow` as a class
    to any markup — these are dark-scoped selector rules consuming the `--shadow-ops-glow` token
    directly (the `.ops-glow` opt-in class declared in Plan 01 is the vocabulary; per D-01 the route
    mark / active pill / recommended card are wired here via dark-scoped rules, no markup edit). Glow
    is FORBIDDEN on text, resting `.ops-panel`/`.ops-muted-panel`/`.ops-data-card`, `.ops-shell` bg,
    `.ops-cmdk__panel`, `#flash-group` — apply ONLY to the three sites above.
  </action>
  <verify>
    <automated>grep -c 'shadow-ops-glow' scrypath_ops/assets/css/app.css | awk '$1>=8{print "GLOW_OK"; exit} {print "FAIL:"$1}'</automated>
  </verify>
  <acceptance_criteria>
    - `.ops-route-mark` dark rule contains BOTH the existing `inset` ring AND `var(--shadow-ops-glow)` (ring not dropped)
    - `.ops-nav-item-active` dark rule contains `var(--shadow-ops-surface)` AND `var(--shadow-ops-glow)`
    - `.ops-intent-card--recommended` base rule at L866-870 is byte-unchanged (still `var(--shadow-ops-surface), inset ...primary 45%...`); a SEPARATE dark override adds the 3-layer stack
    - The recommended-card dark override paints in order: inset ring, then `var(--shadow-ops-panel-dark)`, then `var(--shadow-ops-glow)`
    - `grep -n 'shadow-ops-glow' app.css` shows NO reference inside any `.ops-panel`/`.ops-data-card`/`.ops-shell`/`#flash-group`/`.ops-cmdk__panel` rule
  </acceptance_criteria>
  <done>Quiet glow composed onto route-mark (ring kept), active nav (surface kept), and recommended card (3-layer dark) — both dark paths; light recommended-card unchanged; glow absent from forbidden targets.</done>
</task>

<task type="auto">
  <name>Task 3: Tune the shell radial wash on mobile (DK-10) and prove light pixel-identity</name>
  <files>scrypath_ops/assets/css/app.css</files>
  <read_first>
    - scrypath_ops/assets/css/app.css (lines 240-244 base .ops-shell; the existing @media (max-width:640px) block ~L1015 — read verbatim)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-PATTERNS.md (section 5 — shell wash mobile tune)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md (Finding F6 shell row; F9 light-pixel-diff invocation; Pitfall 5 stale assets)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-01-SUMMARY.md (baseline-freshness verdict — pixel-diff only trustworthy if FRESH/RE-CAPTURED)
  </read_first>
  <action>
    This is the ONE both-theme (not dark-scoped) change. Re-grep the `@media (max-width: 640px)` block
    (~L1015). Add (or extend) a `.ops-shell` rule inside it that copies the base background EXACTLY but
    with two value changes: the radial primary alpha `14%` → `10%`, and the radial extent `34rem` →
    `28rem`. Keep the `linear-gradient(180deg, var(--color-base-200), var(--color-base-100))` second
    layer identical. Do NOT change the base `.ops-shell` at L240-244. Then prove light pixel-identity:
    run `mix assets.build` (scrypath_ops) so Tailwind regenerates priv/static (Pitfall 5 — diffing
    stale assets gives false PASS/FAIL); re-shoot the light matrix into the fresh dir; run
    `node e2e/light-pixel-diff.mjs` from `examples/scrypath_ecommerce/` (no npm script — invoke
    directly, F9). The light shell wash is barely perceptible at either alpha so 0/20 is expected, but
    this is a real light input change so the gate MUST be run and pass. If the baseline verdict from
    Plan 01 was not FRESH/RE-CAPTURED, re-capture before trusting the result.
  </action>
  <verify>
    <automated>grep -c 'transparent 28rem' scrypath_ops/assets/css/app.css | grep -qx 1 && echo SHELL_OK</automated>
  </verify>
  <acceptance_criteria>
    - A `.ops-shell` rule inside `@media (max-width: 640px)` uses `color-mix(in oklch, var(--color-primary) 10%, transparent)` and `transparent 28rem`
    - The base `.ops-shell` at L240-244 is byte-unchanged (still `14%` / `34rem`)
    - `node e2e/light-pixel-diff.mjs` (after `mix assets.build` + re-shoot) prints "Failed pairs: 0 / 20" and exits 0
    - `node contrast-checker.mjs` still reports 3 AA / 12 AAA (no light AA regression)
  </acceptance_criteria>
  <done>Shell mobile wash tuned to 10%/28rem; base shell unchanged; light pixel-diff passes 0/20 against the confirmed-fresh baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Dark-scoped CSS application rules + one mobile @media tune. Static box-shadow/gradient literals; no input, auth, network, or data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-02 | Tampering | CSS box-shadow/gradient literals in app.css | accept | Static design literals referencing in-repo tokens; no user input or interpolation; CSS parsed as text only (T-128-03). Near-zero attack surface. |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH audit N/A). No supply-chain surface. |
</threat_model>

<verification>
- `mix verify.opsui` exits 0 (129 tests + 4 a11y) — no behavior change.
- `node contrast-checker.mjs` (examples/scrypath_ecommerce/): 3 AA / 12 AAA unchanged.
- `node e2e/light-pixel-diff.mjs`: Failed pairs 0 / 20 (after `mix assets.build` + re-shoot).
- Compose check: route-mark inset ring + flash/cmdk overlay layers all still present (grep).
- Glow absent from every forbidden target (grep).
</verification>

<success_criteria>
- SC-1: dark .ops-panel/.ops-intent-card/.ops-cmdk__panel/#flash-group read as seated depth; light keeps vertical lift (0/20).
- SC-2: violet glow on route-mark / active nav / recommended-card only — never text, resting panels, bg floods.
- Shell wash mobile tune lands at 10%/28rem with light pixel-identity preserved.
</success_criteria>

<output>
Create `.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-02-SUMMARY.md` when done.
Record the `light-pixel-diff` result (Failed pairs N / 20) and the `contrast-checker` AA/AAA counts.
</output>
