---
phase: 131-glow-dark-shadow-and-copper-accent-system-r-g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scrypath_ops/assets/css/app.css
autonomous: true
requirements: [GLOW-01, COPPER-01]

must_haves:
  truths:
    - "The canonical Jun-3 light baseline (20 PNGs) is confirmed valid before any pixel-diff is trusted, OR re-captured once and recorded"
    - "--shadow-ops-panel-dark exists ONLY in the two dark paths, never in light"
    - "D-03: --shadow-ops-glow and --shadow-ops-glow-copper have a light default of none and a dark value in both dark paths; --shadow-ops-glow-copper is declared with no consumer in 131 (hover rule deferred to Phase 133/134)"
    - ".ops-glow and the four .ops-copper-* classes are declared in @layer components"
  artifacts:
    - path: "scrypath_ops/assets/css/app.css"
      provides: "Three new dark tokens (Precedent A) + two @theme light defaults + .ops-glow + .ops-copper-eyebrow/-badge/-node/-node--fill classes"
      contains: "--shadow-ops-panel-dark"
  key_links:
    - from: "@theme block (~L141-148)"
      to: "--shadow-ops-glow / --shadow-ops-glow-copper light defaults"
      via: "value: none"
      pattern: "--shadow-ops-glow:\\s*none"
    - from: "[data-theme=dark] + @media html:not([data-theme=light]) (~L1298-1313)"
      to: "three new dark token values"
      via: "Precedent A append (both blocks identical)"
      pattern: "--shadow-ops-panel-dark:\\s*0 0 0 1px"
---

<objective>
Declare every token and class this phase needs, BEFORE any application rule consumes them
(interface-first). This is the contracts plan: the three dark shadow tokens
(`--shadow-ops-panel-dark`, `--shadow-ops-glow`, `--shadow-ops-glow-copper`) via the D-10
dual-path Precedent A, the two `@theme` light defaults (`none`) that make glow a light no-op,
and the five new `@layer components` classes (`.ops-glow`, `.ops-copper-eyebrow`,
`.ops-copper-badge`, `.ops-copper-node`, `.ops-copper-node--fill`).

It also performs the mandatory Wave-0 baseline-freshness check (RESEARCH A1) so downstream
pixel-diff results can be trusted.

Purpose: downstream plans (02 application rules, 03 eyebrow re-style) reference these tokens
and classes directly — no codebase scavenger hunt. Covers GLOW-01 (token half) and COPPER-01
(class-declaration half).
Output: edited `scrypath_ops/assets/css/app.css`; a recorded baseline-freshness verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-UI-SPEC.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-PATTERNS.md

<interfaces>
<!-- Verbatim verbatim precedents the executor copies. Live line numbers re-verified this session. -->

Precedent A (token redefinition, OUTSIDE any @layer) — app.css:1298-1313:
- `[data-theme="dark"] { --shadow-ops-surface: 0 1px 2px rgba(0,0,0,0.40); ... --shadow-ops-overlay: 0 8px 24px rgba(0,0,0,0.55); }`
- `@media (prefers-color-scheme: dark) { html:not([data-theme="light"]) { ...same four lines... } }`
- The `[data-theme="dark"]` opening is at L1300; closing brace after the overlay line at ~L1305.
- The `html:not([data-theme="light"])` opening is at L1307; closing after its overlay line at ~L1312.

@theme shadow ladder — app.css:141-148 (light defaults live here):
- `--shadow-ops-surface: 0 1px 2px color-mix(in oklch, var(--color-base-content) 8%, transparent);` ... through `--shadow-ops-focus: 0 0 0 3px color-mix(in oklch, var(--color-primary) 30%, transparent);`

.ops-badge shape to compose against — app.css:418-433:
- min-height 1.625rem; border 1px solid color-mix(base-content 12%); border-radius 999px;
  padding-inline 0.625rem; font-size 0.75rem; line-height 1rem; font-weight 650; transition on
  background-color/border-color/color at `var(--duration-ops-fast) var(--ease-ops-standard)`.

Token VALUES to declare (from UI-SPEC / RESEARCH Code Examples — exact):
- `--shadow-ops-panel-dark: 0 0 0 1px rgba(0,0,0,0.30), 0 1px 3px rgba(0,0,0,0.45);` (dark ONLY)
- `--shadow-ops-glow: 0 0 8px 2px rgba(108,92,231,0.30);` (dark) / `none` (light @theme)
- `--shadow-ops-glow-copper: 0 0 6px 1px rgba(193,122,62,0.25);` (dark) / `none` (light @theme)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm light-baseline freshness (Wave-0 gate, RESEARCH A1)</name>
  <files>(no file edits — verification + recorded verdict)</files>
  <read_first>
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md (Wave 0 Gaps + Assumptions Log A1 + Finding F9)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    Confirm the 20 canonical light baseline PNGs are valid before any later pixel-diff is trusted.
    The baseline dir is `examples/scrypath_ecommerce/.tmp/admin-screenshots/` — it is gitignored
    (untracked), so `git log` will NOT show it; do not rely on git history for freshness.
    Verify exactly 20 files match `*--light--*.png` exist (RESEARCH confirms 20). Then determine
    whether they post-date Phase 130's last light-affecting commit: check the file mtimes against
    the Phase-130 completion date (2026-06-04) and against Phase 130's SUMMARY note that the
    baseline was "updated post-body-class fix." If the 20 files exist and are dated on/after the
    Phase-130 body-class fix, RECORD the verdict "baseline FRESH — A1 confirmed" and proceed.
    If they are stale or fewer than 20, re-capture once: rebuild assets (`mix assets.build` in
    scrypath_ops with the dev server / Postgres available per VALIDATION Wave 0) then re-shoot the
    light matrix into the baseline dir following the Phase-130 procedure, and record "baseline
    RE-CAPTURED". Do NOT trust a `0/20` pixel-diff result downstream until this verdict is recorded
    in the SUMMARY. This task makes ZERO edits to app.css — it only gates downstream pixel-diff.
  </action>
  <verify>
    <automated>ls examples/scrypath_ecommerce/.tmp/admin-screenshots/*--light--*.png | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls examples/scrypath_ecommerce/.tmp/admin-screenshots/*--light--*.png | wc -l` prints `20`
    - A freshness verdict ("FRESH — A1 confirmed" OR "RE-CAPTURED") is recorded for the SUMMARY
    - No edits made to app.css in this task
  </acceptance_criteria>
  <done>Baseline confirmed (20 light PNGs) and freshness verdict recorded; downstream pixel-diff may now be trusted.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the three dark shadow tokens (Precedent A) + two @theme light defaults</name>
  <files>scrypath_ops/assets/css/app.css</files>
  <read_first>
    - scrypath_ops/assets/css/app.css (lines 141-148 for the @theme ladder; lines 1298-1313 for Precedent A — read both regions verbatim before editing)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-PATTERNS.md (sections 1 and 2 — token declarations + @theme light defaults)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md (Findings F1, F3 + Code Examples "Declaring the new dark tokens")
  </read_first>
  <action>
    Re-grep before editing — confirm Precedent A still sits at app.css:1298-1313 (`grep -n 'D-10:' app.css`).
    Append into the `@theme` block alongside the existing `--shadow-ops-*` ladder (~L141-148) exactly
    two light-default lines: `--shadow-ops-glow: none;` and `--shadow-ops-glow-copper: none;`. Do NOT
    add a `@theme` default for `--shadow-ops-panel-dark` — it must remain UNDECLARED in light; that
    undeclared-ness is the pixel-identity mechanism (Light-Theme Invariants). Then append three
    identical lines into BOTH Precedent-A dark blocks (the `[data-theme="dark"]` block after its
    overlay line ~L1304, AND the `@media ... html:not([data-theme="light"])` block after its overlay
    line ~L1311): `--shadow-ops-panel-dark: 0 0 0 1px rgba(0,0,0,0.30), 0 1px 3px rgba(0,0,0,0.45);`,
    `--shadow-ops-glow: 0 0 8px 2px rgba(108,92,231,0.30);`, `--shadow-ops-glow-copper: 0 0 6px 1px rgba(193,122,62,0.25);`.
    Both dark blocks MUST be identical (D-10 dual-path — forgetting one breaks system-dark vs
    explicit-toggle dark). Do NOT create a new dark block; append into the existing two so the whole
    ladder stays in one cascade site. Do not touch any existing ladder value.
  </action>
  <verify>
    <automated>grep -c -- '--shadow-ops-panel-dark:' scrypath_ops/assets/css/app.css | grep -qx 2 && grep -c -- '--shadow-ops-glow:' scrypath_ops/assets/css/app.css | grep -qx 3 && echo TOKENS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `--shadow-ops-panel-dark:` appears EXACTLY 2 times (both dark paths; never in @theme/light)
    - `--shadow-ops-glow:` appears EXACTLY 3 times (1 @theme `none` + 2 dark)
    - `--shadow-ops-glow-copper:` appears EXACTLY 3 times (1 @theme `none` + 2 dark)
    - `grep -n -- '--shadow-ops-panel-dark: 0 0 0 1px rgba(0,0,0,0.30), 0 1px 3px rgba(0,0,0,0.45)' scrypath_ops/assets/css/app.css` returns 2 lines
    - No `--shadow-ops-panel-dark` line appears inside the `@theme` block (lines ~120-148)
  </acceptance_criteria>
  <done>Three dark tokens declared in both Precedent-A blocks; two glow tokens have `none` light defaults; panel-dark is dark-only.</done>
</task>

<task type="auto">
  <name>Task 3: Declare .ops-glow and the copper vocabulary classes in @layer components</name>
  <files>scrypath_ops/assets/css/app.css</files>
  <read_first>
    - scrypath_ops/assets/css/app.css (lines 418-433 for the .ops-badge shape to compose against; the @layer components block opening ~L232)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-PATTERNS.md (section 4 — new @layer components classes)
    - .planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-RESEARCH.md (Code Examples "The glow class" + "Copper classes" + Finding F8)
  </read_first>
  <action>
    Add five classes inside the existing `@layer components` block (place them near the other
    `.ops-*` accent/badge definitions; a site after `.ops-badge` (~L433) is natural). Author EXACTLY:
    `.ops-glow` = `box-shadow: var(--shadow-ops-glow);` + `transition: box-shadow var(--duration-ops-fast) var(--ease-ops-standard);` (light no-op because glow=none in light).
    `.ops-copper-eyebrow` = `font-size: var(--text-ops-sm); font-weight: 600; text-transform: uppercase; letter-spacing: 0.04em; color: var(--color-secondary);`.
    `.ops-copper-badge` = `border-color: color-mix(in oklch, var(--color-secondary) 44%, transparent); background: color-mix(in oklch, var(--color-secondary) 12%, transparent); color: var(--color-base-content);` (composes WITH `.ops-badge` for layout — usage `class="ops-badge ops-copper-badge"`; do NOT re-declare layout props).
    `.ops-copper-node` = `color: var(--color-secondary);`.
    `.ops-copper-node--fill` = `color: var(--color-secondary-content); background: var(--color-secondary);`.
    These class bodies are theme-agnostic — they reference `var(--color-*)` tokens that already flip
    per theme via the `@plugin` blocks, so they need NO D-10 dual-path (only the `--shadow-ops-glow`
    token value referenced by `.ops-glow` is dark-pathed, done in Task 2). Copper is a brand accent,
    NOT a status tone: do NOT route any of these through `tone_class/1` / `badge_class/1`. The copper
    badge/node/node--fill classes are DECLARED here but NOT wired into any per-screen template (D-01a
    → Phase 134). Per RESEARCH F8 these copper color refs intentionally bypass the contrast harness
    (it only scans base-content alpha mixes) — that is expected; copper AA is a static spec assertion.
  </action>
  <verify>
    <automated>grep -c -E '^\s*\.ops-glow\s*\{|^\s*\.ops-copper-eyebrow\s*\{|^\s*\.ops-copper-badge\s*\{|^\s*\.ops-copper-node\s*\{|^\s*\.ops-copper-node--fill\s*\{' scrypath_ops/assets/css/app.css | grep -qx 5 && echo CLASSES_OK</automated>
  </verify>
  <acceptance_criteria>
    - All five selectors (`.ops-glow`, `.ops-copper-eyebrow`, `.ops-copper-badge`, `.ops-copper-node`, `.ops-copper-node--fill`) are declared exactly once each
    - `.ops-glow` body references `var(--shadow-ops-glow)` (not a literal shadow)
    - `.ops-copper-badge` sets `color: var(--color-base-content)` (NOT `var(--color-secondary)` as text)
    - `.ops-copper-eyebrow` sets `color: var(--color-secondary)`
    - None of the copper classes is referenced from `tone_class/1` or `badge_class/1` (grep ops_ui.ex confirms zero references)
  </acceptance_criteria>
  <done>Five classes declared in @layer components with the exact spec values; copper badge text uses base-content; classes ship but copper badge/node remain un-wired (Phase 134).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds static CSS token literals + class declarations. No input crosses any boundary; no auth, network, or data flow is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Tampering | CSS token literals in app.css | accept | Values are static design literals (rgba/none); no user input, no interpolation, no `eval()` (T-128-03 already enforces CSS-as-text). Near-zero attack surface. |
| T-131-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A — zero external packages). No supply-chain surface. |
</threat_model>

<verification>
After all three tasks:
- `grep -c -- '--shadow-ops-panel-dark:' app.css` == 2; `--shadow-ops-glow:` == 3; `--shadow-ops-glow-copper:` == 3.
- Five new `@layer components` classes present.
- `node contrast-checker.mjs` (from examples/scrypath_ecommerce/) still reports 3 AA / 12 AAA (no regression — the new copper refs bypass the harness per F8, so the count must be unchanged).
- Baseline-freshness verdict recorded.
</verification>

<success_criteria>
- GLOW-01 (token half): `--shadow-ops-panel-dark`, `--shadow-ops-glow` declared; glow is `none` in light.
- COPPER-01 (class half): `.ops-copper-eyebrow/-badge/-node/-node--fill` declared, badge text = base-content.
- `--shadow-ops-glow-copper` declared dark-only with no consumer (D-03).
- Light AA count unchanged (3 AA / 12 AAA).
</success_criteria>

<output>
Create `.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-01-SUMMARY.md` when done.
Record the baseline-freshness verdict (FRESH or RE-CAPTURED) in the SUMMARY.
</output>
