---
phase: 147-ecommerce-mounted-ops-remediation-and-closure-evidence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/scrypath_ecommerce/mix.exs
  - examples/scrypath_ecommerce/mix.lock
autonomous: true
requirements:
  - SEC-04
  - COMPAT-01
must_haves:
  truths:
    - "SEC-04 probe (unclassified): ecommerce selects every approved fixed-compatible dependency range, has no unsuppressed advisory, and consumes the canonical root and ScrypathOps paths."
    - "COMPAT-01 probe (adjacency): a later remediation batch may begin only after the immediately preceding batch's required gates have passed."
    - "COMPAT-01 probe (empty): a missing service, empty receipt, or unavailable required prerequisite blocks acceptance rather than becoming a pass."
    - "COMPAT-01 probe (ordering): ecommerce gates run in the locked diagnostic order and execution stops at the first unexplained drift or failure."
  artifacts:
    - path: "examples/scrypath_ecommerce/mix.exs"
      provides: "The six approved direct ecommerce dependency bounds"
      contains: "{:phoenix, \"~> 1.8.9\"}"
    - path: "examples/scrypath_ecommerce/mix.lock"
      provides: "Reviewed causal ecommerce resolution"
      contains: "\"postgrex\": {:hex, :postgrex, \"0.22.4\""
  key_links:
    - from: "examples/scrypath_ecommerce/mix.exs"
      to: "examples/scrypath_ecommerce/mix.lock"
      via: "targeted Mix resolution followed by checked-lock proof"
      pattern: "phoenix.*1\\.8\\.(9|[1-9][0-9])"
    - from: "examples/scrypath_ecommerce/mix.exs"
      to: "../../scrypath_ops"
      via: "Mix.Project.deps_paths/0 canonical equality assertion"
      pattern: "scrypath_ops.*path:"
---

<objective>
Remediate the ecommerce Mix graph and prove one production-quality mounted path from approved resolution through required application gates before committing it, per D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-11, D-12, D-13, D-14, D-15, and D-16.

Purpose: Deliver the smallest complete end-to-end slice of Phase 147: reviewed dependency intent, canonical mounted sources, deterministic host behavior, required service preparation, root regression proof, and an atomic graph-local commit.
Output: Updated ecommerce manifest and causal lock, plus the executor-authored `147-01-SUMMARY.md` pre-commit provenance/gate receipt.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTRIBUTING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CONTEXT.md
@.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-RESEARCH.md
@.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-PATTERNS.md
@.planning/phases/146-scrypathops-web-client-remediation/146-04-SUMMARY.md
@prompts/elixir-best-practices-deep-research.md
@prompts/phoenix-best-practices-deep-research.md
@prompts/elixir-opensource-libs-best-practices-deep-research.md
@prompts/elixir-oss-lib-ci-cd-best-practices-deep-research.md
</context>

<tasks>

<task type="tracer">
  <name>Task 147-01-01: Commit the isolated ecommerce mounted-source remediation end to end</name>
  <precondition>Postgres accepts the ecommerce test connection and Meilisearch `/health` is healthy; Phase 146 commits `59d2e6a` and `ff1531c` exist and the protected `.planning/v1.36-v1.36-MILESTONE-AUDIT.md` baseline can be hashed if present.</precondition>
  <files>examples/scrypath_ecommerce/mix.exs, examples/scrypath_ecommerce/mix.lock</files>
  <read_first>
    - examples/scrypath_ecommerce/mix.exs
    - examples/scrypath_ecommerce/mix.lock
    - examples/scrypath_ecommerce/config/config.exs
    - examples/scrypath_ecommerce/config/runtime.exs
    - examples/scrypath_ecommerce/config/test.exs
    - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex
    - examples/scrypath_ecommerce/lib/mix/tasks/e2e.prepare_search.ex
    - examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/page_controller_test.exs
    - CONTRIBUTING.md
    - .planning/phases/146-scrypathops-web-client-remediation/146-04-SUMMARY.md
    - .planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-PATTERNS.md
  </read_first>
  <action>
Change exactly six direct requirement strings in `examples/scrypath_ecommerce/mix.exs`: Phoenix to `~> 1.8.9`, Phoenix LiveView to `~> 1.1.33`, Bandit to `~> 1.12.1`, Swoosh to `~> 1.26.3`, Postgrex to `~> 0.22.4`, and retain Req at `~> 0.6.1`, per D-01. Preserve both path dependencies, `phoenix_ecto`, Ecto SQL `~> 3.13`, Oban, every unrelated direct dependency, and transitive ownership of Plug, Mint, hpax, Finch, Ecto, and Decimal per D-02. Run targeted Mix resolution; accept only resolver-owned rows causally explained by these bounds and the already-landed Req handoff, and do not hand-edit Hex tuples or checksums per D-03 and D-04. If compile or the focused contract proves a source incompatibility, stop and re-plan rather than expanding beyond ecommerce without the D-05 causal evidence.

Before any proof, record HEAD/base, UTC start, OS and Elixir/OTP/Mix/Hex versions, source-scoped `git status --short`, SHA-256 for root/Ops/ecommerce manifests and locks, and the protected audit file when present. Create one owned temporary parent and export fresh `MIX_DEPS_PATH` and `MIX_BUILD_PATH` children for every ecommerce fetch, path assertion, compile, test, precommit, audit, and preparation command per D-06 and D-08. Never use or clear ordinary `deps/` or `_build/` caches. Run, in strict stop-on-failure order: inspect the manifest/lock diff and enumerate every moved lock row; `mix deps.get --check-locked`; a `Mix.Project.deps_paths/0` assertion that canonicalizes and equates `:scrypath` to the repository root and `:scrypath_ops` to `scrypath_ops/`; `mix compile --warnings-as-errors`; `mix test test/scrypath_ecommerce_web/controllers/page_controller_test.exs`; and `mix precommit`, per D-07 and D-11. The controller file is the inspected narrow existing contract: it proves the mounted posture route, Ops CSS/host JS, nested links, and absence of storefront asset bleed.

Immediately after `mix precommit`, re-run `mix format --check-formatted`, checked-lock proof, lock SHA-256, manifest/lock diff inspection, source-scoped dirty comparison, and protected-file hash comparison; any unexplained mutation blocks the commit per D-12. With Postgres and Meilisearch health checks green, run isolated `mix e2e.prepare`; unavailable prerequisites block the task per D-13. Then return to repository root and run exactly `mix compile --warnings-as-errors`, `mix test --exclude integration --exclude docs_contract --include requires_clean_workspace`, `mix verify --exclude integration`, `mix verify.phase11`, and `mix verify.phase99` per D-14. Do not run `mix verify.opsui` unless an Ops-owned file changed or mounted proof exposes an Ops-owned regression per D-15. Run ecommerce `mix hex.audit` unsuppressed. Stop before commit for every condition enumerated by D-16, including wrong mounted paths, nonzero audit, service failure, drift, or scope expansion.

After all required gates are green, remove only the owned temporary proof state using validated parent/exact-child/non-symlink/prefix checks, prove absence and unchanged protected baselines, and commit only the ecommerce manifest and lock as the graph-local ecommerce implementation. Preserve public/runtime/UI behavior and familiar Mix surfaces per D-29, D-30, D-31, and D-32. Record commands, exits, hashes, classifications, causal lock rows, and the commit SHA in `147-01-SUMMARY.md`; exclude raw logs, credentials, temporary paths, and generated state.
  </action>
  <acceptance_criteria>
    - The manifest contains exactly the six D-01 bounds and introduces no direct transitive package, override, public API, route, schema, UI, or CI change.
    - The reviewed lock contains only explained causal movement and passes checked-lock plus unsuppressed audit proof.
    - Canonical mounted root/Ops identity, warning-clean compile, the complete existing controller contract, ecommerce precommit, required `e2e.prepare`, and all five named root gates pass before commit.
    - Both isolation variables are present on every ecommerce proof command; the ordinary caches and protected user-owned file remain unchanged.
    - One atomic ecommerce implementation commit contains only `examples/scrypath_ecommerce/mix.exs` and `examples/scrypath_ecommerce/mix.lock`.
  </acceptance_criteria>
  <verify>
    <automated>test -n "${MIX_DEPS_PATH:-}" &amp;&amp; test -n "${MIX_BUILD_PATH:-}" &amp;&amp; cd examples/scrypath_ecommerce &amp;&amp; mix deps.get --check-locked &amp;&amp; mix run --no-start -e 'paths = Mix.Project.deps_paths(); expected = %{scrypath: Path.expand("../.."), scrypath_ops: Path.expand("../../scrypath_ops")}; Enum.each(expected, fn {dep, path} -&gt; File.realpath!(Map.fetch!(paths, dep)) == File.realpath!(path) || raise "wrong mounted path for #{dep}" end)' &amp;&amp; mix test test/scrypath_ecommerce_web/controllers/page_controller_test.exs &amp;&amp; mix hex.audit</automated>
  </verify>
  <done>The committed ecommerce graph is fixed-compatible, audit-clean, mounted to the intended root/Ops sources, green through required ecommerce/service/root gates, and represented by a compact redacted pre-commit receipt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest → Hex resolver/lock | Registry-selected code enters the ecommerce dependency graph. |
| Ecommerce → local path dependencies | Host resolution crosses into root Scrypath and ScrypathOps source trees. |
| Proof process → contributor workspace | Temporary resolution/build state must not corrupt user caches or protected files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-147-01 | Tampering / Elevation of privilege | `mix.exs` and `mix.lock` selection | high | mitigate | Enforce D-01 ranges, causal row review, checked lock, explicit version bounds, and unsuppressed `mix hex.audit`; stop on any unexplained or advisory-bearing selection. |
| T-147-02 | Tampering | Mounted `:scrypath` / `:scrypath_ops` paths | high | mitigate | Assert canonical equality with `Mix.Project.deps_paths/0` under fresh isolated Mix paths before acceptance. |
| T-147-06 | Denial of service / Tampering | Temporary proof cleanup | high | mitigate | Use owned exact-child/non-symlink/prefix validation and baseline/hash preservation; never touch ordinary caches. |
| T-147-SC | Tampering | Package installs | low | accept | No package is introduced; approved existing Hex bounds are changed under the research legitimacy audit. |
</threat_model>

## Artifacts this phase produces

- `examples/scrypath_ecommerce/mix.exs` — six direct bound strings; no new public symbol.
- `examples/scrypath_ecommerce/mix.lock` — resolver-owned causal rows for the reviewed graph.
- `147-01-SUMMARY.md` — pre-commit receipt shape: candidate/base, environment, source hashes, causal rows, ordered command/exits, proof class, cleanup result, protected baseline, implementation SHA.

<verification>
Inspect the implementation commit path set; confirm the exact six manifest strings, no direct transitive dependency/override, checked lock, canonical mounted paths, focused controller contract, audit result, required service preparation, and named root gates. Treat any missing required evidence as failure.
</verification>

<success_criteria>
The ecommerce implementation commit is isolated to its manifest/lock, all required pre-commit gates pass in order, mounted-source identity is directly proved, required services are genuinely available, and no protected contributor state changes.
</success_criteria>

<output>
Create `.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-01-SUMMARY.md` when done.
</output>
