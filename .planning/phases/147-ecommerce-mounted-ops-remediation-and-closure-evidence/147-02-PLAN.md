---
phase: 147-ecommerce-mounted-ops-remediation-and-closure-evidence
plan: "02"
type: execute
wave: 2
depends_on:
  - 147-01
files_modified:
  - .planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CLOSURE-EVIDENCE.md
autonomous: true
requirements:
  - SEC-04
  - COMPAT-03
must_haves:
  truths:
    - "COMPAT-03 probe (unclassified): browser proof is a separate passed, failed, or unavailable record; a retry-only pass carries flake: true and never substitutes for required deterministic or service proof."
  artifacts:
    - path: ".planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CLOSURE-EVIDENCE.md"
      provides: "Exact-SHA ecommerce proof, cleanup receipt, and classified focused browser evidence"
      contains: "## Ecommerce Exact-SHA Proof"
  key_links:
    - from: "147-01-SUMMARY.md"
      to: "147-CLOSURE-EVIDENCE.md"
      via: "implementation SHA and pre-commit hashes identify the detached proof candidate"
      pattern: "candidate SHA|implementation SHA"
    - from: "examples/scrypath_ecommerce/e2e/harness.spec.ts"
      to: "147-CLOSURE-EVIDENCE.md"
      via: "focused Playwright result classified independently"
      pattern: "passed|failed|unavailable"
  prohibitions:
    - requirement_id: COMPAT-03
      category: transparency
      status: unresolved
      verification: null
      statement: "Unavailable browser or service prerequisites MUST NOT be represented as passing evidence."
---

<objective>
Prove the committed ecommerce candidate at its exact SHA in a disposable graph and classify the proportional mounted browser result without overstating it, per D-09, D-10, D-17, D-18, D-19, D-20, and D-21.

Purpose: Convert the pre-commit tracer into reproducible post-commit provenance while keeping required deterministic/service proof distinct from advisory browser evidence.
Output: `147-CLOSURE-EVIDENCE.md` initialized with the exact-SHA ecommerce matrix, safe-cleanup receipt, and focused browser classification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTRIBUTING.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CONTEXT.md
@.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-RESEARCH.md
@.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-PATTERNS.md
@.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-01-SUMMARY.md
@.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 147-02-01: Capture exact-SHA mounted resolution and fail-closed cleanup evidence</name>
  <precondition>`147-01-SUMMARY.md` names an ecommerce implementation commit whose diff contains only the ecommerce manifest and lock, and the primary workspace/protected audit baseline can be recorded.</precondition>
  <files>.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CLOSURE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-01-SUMMARY.md
    - .planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CONTEXT.md
    - .planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-PATTERNS.md
    - .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md
    - examples/scrypath_ecommerce/mix.exs
    - examples/scrypath_ecommerce/mix.lock
    - examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/page_controller_test.exs
  </read_first>
  <action>
Create `147-CLOSURE-EVIDENCE.md` as a compact receipt, not a permanent proof subsystem. Record the exact ecommerce implementation SHA from Plan 01, UTC window, OS and Elixir/OTP/Mix/Hex versions, primary root/Ops/ecommerce manifest and lock hashes, source-scoped dirty status, and protected audit-file hash when present. Create an owned temporary parent and a disposable detached worktree at that exact SHA; validate ownership, non-symlink state, temporary-prefix/exact-child relationships, unique worktree registration, and canonical path equivalence before doing any destructive cleanup per D-09 and D-10.

Inside the worktree's ecommerce directory, remove only the disposable ecommerce lock, export fresh `MIX_DEPS_PATH` and `MIX_BUILD_PATH`, and propagate both to lockless `mix deps.get`, `Mix.Project.deps_paths/0` canonical assertions, warning-clean compile, the existing `page_controller_test.exs`, `mix precommit`, required `mix e2e.prepare`, and unsuppressed `mix hex.audit`. Assert the fresh lock versions exactly against D-04: Phoenix `>= 1.8.9 and < 1.9.0`, LiveView `>= 1.1.33 and < 1.2.0`, Bandit `>= 1.12.1 and < 1.13.0`, Swoosh `>= 1.26.3 and < 1.27.0`, Postgrex `>= 0.22.4 and < 0.23.0`, Req `>= 0.6.1 and < 0.7.0`, Plug `>= 1.19.5 and < 2.0.0`, Mint `>= 1.9.3`, and hpax `>= 1.0.4`. Stop on an out-of-range row, wrong mounted path, service failure, nonzero audit, or unexplained source mutation.

In an unconditional cleanup path, revalidate the same ownership/non-symlink/prefix/exact-child/registration facts immediately before `git worktree remove`; do not use a broad recursive deletion target or contributor caches. Prune only stale metadata after the validated removal, then prove the worktree and isolated state are absent and the primary workspace lock hashes, status baseline, and protected audit file remain unchanged. Write only redacted fields and results to the evidence file; exclude raw output, disposable locks, dependency trees, temporary paths, secrets, or generated service state. This task implements D-09 and D-10 without re-asking their locked cleanup choice.
  </action>
  <acceptance_criteria>
    - The evidence file binds fresh resolution, mounted paths, service preparation, focused/full gates, and audit to the exact ecommerce implementation SHA.
    - Every D-04 range passes on the disposable fresh lock and both mounted dependency paths canonicalize to the intended directories.
    - Cleanup is preceded by two validated guard passes and followed by absence, primary-lock, dirty-baseline, and protected-file equality checks.
    - No disposable lock, raw log, temporary path, dependency tree, credential, or generated service state is committed.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CLOSURE-EVIDENCE.md &amp;&amp; rg -n 'Ecommerce Exact-SHA Proof|canonical mounted|range matrix|unsuppressed|cleanup|primary.*unchanged|protected' .planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CLOSURE-EVIDENCE.md</automated>
  </verify>
  <done>The exact implementation SHA has fresh mounted-source, range, required-service, audit, and cleanup evidence with no primary-workspace or protected-file drift.</done>
</task>

<task type="auto">
  <name>Task 147-02-02: Classify focused mounted browser proof without promotion</name>
  <precondition>The exact-SHA ecommerce proof is green and the primary candidate still equals the implementation SHA recorded in `147-CLOSURE-EVIDENCE.md`.</precondition>
  <files>.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CLOSURE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CLOSURE-EVIDENCE.md
    - examples/scrypath_ecommerce/README.md
    - examples/scrypath_ecommerce/package.json
    - examples/scrypath_ecommerce/playwright.config.ts
    - examples/scrypath_ecommerce/e2e/harness.spec.ts
    - examples/scrypath_ecommerce/e2e/operator.spec.ts
    - examples/scrypath_ecommerce/e2e/helpers/e2e.ts
    - scripts/ci/phase105_evidence.sh
    - .github/workflows/ci.yml
  </read_first>
  <action>
Check and record Postgres, Meilisearch, Node/npm, Chromium, built assets, and exact-SHA Phoenix server readiness. When all prerequisites are available, run the existing focused command from the ecommerce directory: `npx playwright test e2e/harness.spec.ts e2e/operator.spec.ts --workers=1`; do not add a package script, spec, harness, or persistent evidence mechanism per D-17. Use the existing Playwright report/trace/test-results and Phoenix log only for diagnosis; do not commit them per D-21.

Append a browser section to `147-CLOSURE-EVIDENCE.md` with an explicit `state: passed`, `state: failed`, or `state: unavailable` scalar appearing exactly once; record candidate SHA, UTC time, command, prerequisite states, first-attempt outcome, final outcome, and `flake: true` only when a retry converts a first failure to pass per D-19. If an exact-candidate `phase105-e2e` run exists, reference its existing evidence summary and do not manufacture a second full local run per D-18. A causally mounted/dependency-related focused failure blocks closure; an advisory visual/infrastructure/flake failure remains honestly failed or unavailable and does not expand the phase into UI work per D-20. Keep deterministic, required-service, focused-browser, and optional full-lane classifications in separate rows. Preserve maintainer/adopter experience and scope constraints D-29, D-30, D-31, and D-32.
  </action>
  <acceptance_criteria>
    - The focused browser row is bound to the exact ecommerce SHA and uses only passed, failed, or unavailable.
    - Unavailable includes the missing prerequisite; a retry-only pass includes `flake: true`; skipped, cancelled, or unrelated-SHA CI is not a pass.
    - Required deterministic and `e2e.prepare` rows remain separate and are not replaced by browser evidence.
    - No generated browser artifact, new test, npm script, UI change, or brand change is committed.
  </acceptance_criteria>
  <verify>
    <automated>ruby -e 'p=ARGV.fetch(0); s=File.read(p); b=s[/## Focused Browser Evidence.*?(?=\n## |\z)/m] or abort("missing browser section"); states=b.scan(/^state: (passed|failed|unavailable)$/); abort("expected exactly one browser state") unless states.length == 1; abort("required proof conflated") unless s.include?("required_deterministic") &amp;&amp; s.include?("required_service_prepare")' .planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-CLOSURE-EVIDENCE.md</automated>
  </verify>
  <done>Focused browser evidence is truthfully classified and remains proportional, separate, redacted, and non-promoted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git history → disposable exact-SHA worktree | Evidence must execute the recorded candidate, not current mutable state. |
| Browser/services → closure receipt | External prerequisites and retries must not be misrepresented as deterministic success. |
| Temporary filesystem → primary workspace | Cleanup must remove only validated owned proof state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-147-02 | Tampering | Detached mounted `:scrypath` / `:scrypath_ops` paths | high | mitigate | Canonicalize and compare `Mix.Project.deps_paths/0` entries to the exact detached root and Ops directories under isolated paths. |
| T-147-03 | Repudiation | Exact-SHA/browser evidence | high | mitigate | Bind every row to candidate SHA/time/tool versions, use three-state browser classification, and keep proof classes separate. |
| T-147-04 | Tampering / Denial of service | Detached-worktree cleanup | high | mitigate | Validate owned non-symlink exact child, canonical unique registration, and prefix twice before removal; prove baseline equality afterward. |
| T-147-05 | Information disclosure | `147-CLOSURE-EVIDENCE.md` | medium | mitigate | Persist compact fields only and exclude raw logs, secrets, temporary paths, dependency trees, and generated browser/service artifacts. |
</threat_model>

## Artifacts this phase produces

- `147-CLOSURE-EVIDENCE.md` — sections and shapes: exact-SHA candidate/environment; nine-package range matrix; canonical mounted paths; ordered command/exit/classification rows; cleanup/preservation receipt; focused browser `{state, flake, prerequisites, candidate_sha, command, utc}` record.
- `147-02-SUMMARY.md` — executor summary referencing, not duplicating, the compact closure evidence.

<verification>
Confirm the ledger binds the post-commit proof to the Plan 01 implementation SHA, all fresh ranges and canonical paths pass, cleanup leaves no disposable state, and browser evidence uses the locked three-state vocabulary without replacing required proof.
</verification>

<success_criteria>
The exact ecommerce candidate is reproducibly fresh-resolved and safely cleaned up, and the focused mounted browser result is auditable without overstating unavailable, flaky, advisory, or unrelated evidence.
</success_criteria>

<output>
Create `.planning/phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-02-SUMMARY.md` when done.
</output>
