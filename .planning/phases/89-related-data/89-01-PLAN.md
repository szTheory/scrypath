---
phase: 89-related-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/options.ex
  - lib/scrypath.ex
  - lib/scrypath/sync.ex
  - test/scrypath/options_test.exs
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - Scrypath options schema recognizes and validates `fan_outs` metadata.
    - Public API exposes `Scrypath.sync_related/3`.
  artifacts:
    - path: lib/scrypath/options.ex
      provides: fan_outs validation logic
    - path: lib/scrypath.ex
      provides: sync_related delegation to internal runtime
    - path: lib/scrypath/sync.ex
      provides: internal sync_related skeleton
  key_links:
    - from: lib/scrypath.ex
      to: lib/scrypath/sync.ex
      via: delegation of sync_related/3
---

<objective>
Design the `Scrypath.sync_related/3` entrypoint and underlying capability struct for associating parent-child schemas.

Purpose: Establish the explicit public API and options schema modifications necessary to declare related-data fan-out capabilities, fulfilling DATA-01.
Output: Validated `fan_outs` metadata on schemas and a public entrypoint skeleton.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/89-RESEARCH.md
@.planning/phases/89-PATTERNS.md
@lib/scrypath/options.ex
@lib/scrypath.ex
@lib/scrypath/sync.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fan_outs validation to Scrypath.Options</name>
  <files>lib/scrypath/options.ex, test/scrypath/options_test.exs</files>
  <behavior>
    - Test: `validate_fan_outs` accepts a list of keyword configurations where each defines a `target:` (schema module) and a `resolver:` `{Module, :func, [args]}`.
    - Test: `validate_fan_outs` rejects if resolver is not a valid MFA.
  </behavior>
  <action>
    Add `fan_outs:` to `@schema_options` using `type: {:custom, __MODULE__, :validate_fan_outs, []}`, defaulting to `[]`.
    Implement `validate_fan_outs/1` which ensures it is a keyword list. For each entry, validate `target:` is a module and `resolver:` is a valid MFA tuple `{module, atom, list}`.
  </action>
  <verify>
    <automated>mix test test/scrypath/options_test.exs</automated>
  </verify>
  <done>Scrypath.Options enforces `fan_outs` metadata correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Define sync_related/3 entrypoint</name>
  <files>lib/scrypath.ex, lib/scrypath/sync.ex</files>
  <action>
    In `lib/scrypath.ex`, define `@spec sync_related(module(), struct() | [struct()], keyword()) :: {:ok, term()} | {:error, term()}`. Delegate it to `Scrypath.Sync.sync_related(schema_module, records, opts)` to follow the public delegation pattern identified in `lib/scrypath.ex` and `lib/scrypath/sync.ex`.
    In `lib/scrypath/sync.ex`, implement the initial `sync_related(schema_module, records, opts \\ [])` which currently just returns `{:ok, Scrypath.Operations.Result.new(mode: :inline, status: :noop)}`. (The full execution logic will be added in plan 89-02).
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>The function `sync_related/3` is available without crashing and can be called from user code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Context -> API | Developer code explicitly invokes sync_related |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Tampering | sync_related | mitigate | Validate inputs thoroughly in Options and reject missing or malformed resolver configurations. |
</threat_model>