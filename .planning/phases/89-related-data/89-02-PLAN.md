---
phase: 89-related-data
plan: 02
type: execute
wave: 2
depends_on: [89-01]
files_modified:
  - lib/scrypath/sync.ex
  - test/scrypath/sync_test.exs
  - lib/scrypath/sync/related_worker.ex
  - test/scrypath/sync/related_worker_test.exs
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - Scrypath.Sync.sync_related fetches schema metadata and runs the configured resolver MFA.
    - Resolvers run safely within a standard telemetry span.
    - Synchronous errors from the resolver propagate back cleanly to the caller.
  artifacts:
    - path: lib/scrypath/sync.ex
      provides: fan-out resolver execution and telemetry wrapping
  key_links:
    - from: lib/scrypath/sync.ex
      to: telemetry
      via: [:scrypath, :sync, :related, :resolve] span execution
---

<objective>
Update core execution runtime to explicitly accept and validate related-data fan-out intents.

Purpose: Implement the actual fan-out synchronization logic ensuring "no hidden magic" invariant, fulfilling DATA-02.
Output: `sync_related/3` fully executes declared resolvers and passes resulting documents to the standard `sync_records` flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/89-RESEARCH.md
@.planning/phases/89-PATTERNS.md
@lib/scrypath/sync.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement sync_related/3 execution flow</name>
  <files>lib/scrypath/sync.ex, test/scrypath/sync_test.exs, lib/scrypath/sync/related_worker.ex, test/scrypath/sync/related_worker_test.exs</files>
  <behavior>
    - Test: Evaluates the declared resolver MFA for the specified `fan_out` key and forwards resulting documents to the configured target via `sync_records`.
    - Test: Fails synchronously via `ArgumentError` if the `fan_out` key is missing from `opts` or not declared on the schema.
    - Test: Emits telemetry span `[:scrypath, :sync, :related, :resolve]`.
  </behavior>
  <action>
    Modify `Scrypath.Sync.sync_related/3`.
    Require `opts[:fan_out]`. Read `schema_module.__scrypath__(:fan_outs)` and fetch the configured `target:` and `resolver:` for the requested key.
    If the key is missing from the options or the schema metadata, raise `ArgumentError`.
    If `opts[:sync_mode]` is `:oban` (or if not provided and Oban is the configured default), enqueue a new `Scrypath.Sync.RelatedWorker` job containing the `schema_module`, the primary keys of `records`, and the `opts`.
    Otherwise, if inline, wrap the MFA resolution step in `Telemetry.span([:scrypath, :sync, :related, :resolve], metadata, fn -> ... end)` to follow the internal dispatch and telemetry pattern identified in `lib/scrypath/sync.ex` from PATTERNS.md.
    Execute the resolver using `apply(M, F, List.wrap(records) ++ args)`.
    Then, take the returned target records and route them to `Scrypath.Sync.sync_records(target_schema, resolved_records, opts)`.
    Create `lib/scrypath/sync/related_worker.ex` to perform the same inline resolution and routing when executed asynchronously.
  </action>
  <verify>
    <automated>mix test test/scrypath/sync_test.exs test/scrypath/sync/related_worker_test.exs</automated>
  </verify>
  <done>sync_related correctly evaluates the resolver and routes the resulting documents to sync_records without hidden magic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Execution -> Context MFA | Library running external context-owned MFA to resolve target records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-02 | Denial of Service | Resolver MFA | accept | The application code provides the resolver; if it hangs or consumes memory, it's an application concern. |
</threat_model>