---
phase: 89-related-data
plan: 03
type: execute
wave: 3
depends_on: [89-02]
files_modified:
  - test/scrypath/sync/related_test.exs
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - Hermetic tests ensure explicit orchestration works end-to-end.
    - Oban mode integration functions correctly.
  artifacts:
    - path: test/scrypath/sync/related_test.exs
      provides: Integration testing for fan-out api
  key_links:
    - from: test/scrypath/sync/related_test.exs
      to: lib/scrypath.ex
      via: integration test behavior assertions
---

<objective>
Establish baseline hermetic tests ensuring explicit orchestration overrides auto-magic execution.

Purpose: Provide solid coverage of the end-to-end related-data fan-out API, confirming it fulfills both DATA-01 and DATA-02 robustly.
Output: Integration tests covering standard `:inline` and `:oban` execution modes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/89-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration tests for sync_related</name>
  <files>test/scrypath/sync/related_test.exs</files>
  <action>
    Create integration tests verifying `Scrypath.sync_related/3`.
    Define simple in-memory `Source` and `Target` schemas using `use Scrypath` inside the test file, configuring a `fan_outs` rule on `Source` pointing to `Target`.
    Provide a dummy resolver function on a test module that returns some fixed `Target` structs.
    Test 1: Assert `sync_mode: :inline` verifies the resolver output is correctly indexed (mocking the backend call or asserting the resulting `Scrypath.Operations.Result`).
    Test 2: Assert `sync_mode: :oban` verifies an Oban job is enqueued correctly, processing the documents.
    Test 3: Assert missing or invalid `fan_out` key raises appropriate synchronous `ArgumentError`.
  </action>
  <verify>
    <automated>mix test test/scrypath/sync/related_test.exs</automated>
  </verify>
  <done>Hermetic tests successfully demonstrate `sync_related/3` behavior and its invariant of no-hidden-magic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing -> Framework | Testing framework isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03 | Information Disclosure | Test Suite | accept | Tests are internal only and contain no production secrets. |
</threat_model>