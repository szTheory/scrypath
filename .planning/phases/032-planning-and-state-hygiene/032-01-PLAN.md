---
phase: 32
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
  - .planning/v1.6-MILESTONE-AUDIT.md
  - .planning/PROJECT.md
autonomous: true
requirements: [AUDT-01]
tags: [planning, audit, state, requirements]

must_haves:
  truths:
    - "Every AUDT-01 deferred row in `.planning/STATE.md` has a **terminal** status (not `pending_triage_v1_6`) with a **one-line pointer** to evidence per `032-CONTEXT.md` D-02, D-04–D-05, D-08"
    - "`.planning/REQUIREMENTS.md` shows **AUDT-01** checkbox **`[x]`** and traceability table cell **Complete** per D-09"
    - "No `.planning/` file still implies **open** Phase 18 UAT debt or **pending** quick-task stubs without explicit superseded/waived/obsolete language (D-03)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Canonical triage ledger"
      contains: "resolved|obsolete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "AUDT-01 closure"
      contains: "[x] **AUDT-01**"
---

<objective>
Close **AUDT-01**: terminalize the three **v1.5-close deferred** rows in **`.planning/STATE.md`**, align **`.planning/REQUIREMENTS.md`** traceability, remove **stale ‘open gap’ narrative** from milestone docs, and refresh **`.planning/v1.6-MILESTONE-AUDIT.md`** + **`.planning/PROJECT.md`** so maintainer routing matches shipped evidence — **one atomic commit** per **032-CONTEXT.md D-11**.
</objective>

<threat_model>
**ASVS L1 — documentation integrity**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-32-DOC-01: Editing `STATE` / `REQUIREMENTS` to claim closure without evidence pointers | Medium | Every terminal row cites an **existing relative path** (`18-VERIFICATION.md`, `v1.4-MILESTONE-AUDIT.md`, each `SUMMARY.md`); acceptance criteria grep those paths |
| T-32-DOC-02: Declaring adopters must act on completed quick tasks | Low | Status **`obsolete`** (completed work), not `open`; do **not** open GitHub issues for mirror debt (**D-06**) |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Contradiction audit under `.planning/`</name>
  <files>.planning/</files>
  <read_first>
    - .planning/phases/032-planning-and-state-hygiene/032-CONTEXT.md
    - .planning/STATE.md
    - .planning/MILESTONES.md
    - .planning/v1.6-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Run and record (in commit message or `032-VERIFICATION.md` notes if needed) searches for stale deferral language:

    1. `rg -n 'pending_triage_v1_6' .planning` — expect hits only in **`STATE.md`** until Task 2 removes them.
    2. `rg -n 'UAT gap|audit-open|open.*18-UAT|Phase 18.*gap' .planning` — case-insensitive optional `-i`; inspect hits for **false “still open”** implications vs historical archive tone.

    Apply **minimal edits** outside **`STATE.md`** / **`REQUIREMENTS.md`** only where a hit violates **D-03** (parallel open-gap view contradicting Phase **passed**). Prefer **footnote or superseded-by pointer** over deleting milestone history in **`MILESTONES.md`** (e.g. change “Known deferred items at close” bullets to state **triaged in phase 32 / see `STATE.md`** once rows are terminal).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/docs_contract_test.exs` exits **0**
    - After subsequent tasks, `rg 'pending_triage_v1_6' .planning/STATE.md` returns **no lines** (post Task 2)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Terminalize `STATE.md` deferred rows</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md
    - .planning/phases/032-planning-and-state-hygiene/032-CONTEXT.md
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-VERIFICATION.md
    - .planning/milestones/v1.4-MILESTONE-AUDIT.md
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-UAT.md
    - .planning/quick/260416-eoj-automate-phase-5-verification-with-live-/260416-eoj-SUMMARY.md
    - .planning/quick/260416-if2-fix-mix-exs-github-urls-add-a-github-act/260416-if2-SUMMARY.md
  </read_first>
  <action>
    In **`.planning/STATE.md` §Deferred Items** table:

    1. **Row `uat_gap` (Phase 18):** Set **Status** to **`resolved`** (or **`obsolete`** if you prefer single vocabulary — pick **one** terminal label column-wide). One-line reason must cite **`18-VERIFICATION.md`**, **`v1.4-MILESTONE-AUDIT.md`** (Phase 18 passed line), and retain pointer to **`18-UAT.md`** as historical map — verbatim relative paths from repo root as in **032-CONTEXT D-02**.
    2. **Row `260416-eoj-...`:** Set **Status** to **`obsolete`** (completed work). Reason: single line citing **`.planning/quick/260416-eoj-automate-phase-5-verification-with-live-/260416-eoj-SUMMARY.md`**.
    3. **Row `260416-if2-...`:** Set **Status** to **`obsolete`**. Reason: single line citing **`.planning/quick/260416-if2-fix-mix-exs-github-urls-add-a-github-act/260416-if2-SUMMARY.md`**.

    Update **§Next Command** / **§Current Position** prose so it no longer instructs “plan phase 32 first” as the only next step once this work is executing—replace with pointer to **`/gsd-execute-phase 32`** then verification / milestone audit per **STATE** house style.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'pending_triage_v1_6' .planning/STATE.md` returns **empty**
    - `grep -F '18-VERIFICATION.md' .planning/STATE.md` returns **≥1** line
    - `grep -F 'v1.4-MILESTONE-AUDIT.md' .planning/STATE.md` returns **≥1** line
    - `grep -F '260416-eoj-SUMMARY.md' .planning/STATE.md` returns **≥1** line
    - `grep -F '260416-if2-SUMMARY.md' .planning/STATE.md` returns **≥1** line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Complete AUDT-01 in `REQUIREMENTS.md`</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  </read_first>
  <action>
    1. Change **`- [ ] **AUDT-01**`** to **`- [x] **AUDT-01**`** in the **Planning hygiene** list.
    2. In **Traceability** table, set **AUDT-01** row **Status** cell from **`Pending`** to **`Complete`**.
    3. Optional one short cross-pointer in that cell: **`closed per STATE.md §Deferred Items`** (max one line, **D-09**).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '[x] **AUDT-01**' .planning/REQUIREMENTS.md` returns **≥1** line
    - `grep -F '| AUDT-01 | Phase 32 | Complete |' .planning/REQUIREMENTS.md` returns **≥1** line
    - `grep -F '| AUDT-01 | Phase 32 | Pending |' .planning/REQUIREMENTS.md` returns **empty**
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Align `MILESTONES.md` deferred bullets</name>
  <files>.planning/MILESTONES.md</files>
  <read_first>
    - .planning/MILESTONES.md
    - .planning/STATE.md
    - .planning/phases/032-planning-and-state-hygiene/032-CONTEXT.md
  </read_first>
  <action>
    Update **v1.5** and **v1.4** sections’ **“Known deferred items at close”** bullets so they **do not read as still-actionable maintainer debt**. Acceptable patterns: **“Triaged phase 32 (AUDT-01); see `STATE.md` §Deferred Items.”** or equivalent ≤ **two** short sentences. Preserve historical fact that deferrals **existed at milestone close** without implying they remain **open tasks**.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/MILESTONES.md` exits 0
    - `grep -Eiq 'AUDT-01|phase 32' .planning/MILESTONES.md` exits 0
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 5: Refresh `v1.6-MILESTONE-AUDIT.md` + `PROJECT.md` routing</name>
  <files>.planning/v1.6-MILESTONE-AUDIT.md</files>
  <read_first>
    - .planning/v1.6-MILESTONE-AUDIT.md
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md
    - .planning/STATE.md
  </read_first>
  <action>
    1. **`v1.6-MILESTONE-AUDIT.md`:** Update YAML **frontmatter** and body tables so **AUDT-01** is no longer listed as **`unsatisfied`** / **`missing` verification** once `REQUIREMENTS.md` marks Complete **and** `STATE` rows are terminal. Set **`scores.requirements`** to **`8/8`**. Remove or rewrite the **`gaps.requirements`** entry for AUDT-01 to **`satisfied`** (or delete that list entry) with a **pointer** to **`STATE.md` §Deferred Items**. Remove the **`tech_debt`** bullet whose text contains **`STATE.md §Deferred Items — UAT gap`**. Keep **`scores.phases`** at **`3/4`** until **`032-VERIFICATION.md`** exists with **`status: passed`** — do **not** claim four verified phases prematurely. Update the **Executive summary** / rollup rows so **Phase 32** is not described as “AUDT-01 still open” after triage (e.g. **“executed — verification pending”** or equivalent honest wording).

    2. **`PROJECT.md`:** Update the **v1.6 / phase 32** sentence so “next” language matches post-triage reality (e.g. **execute + verify phase 32** or **milestone audit**), without claiming Hex/runtime changes.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'STATE.md §Deferred Items — UAT gap' .planning/v1.6-MILESTONE-AUDIT.md` returns **empty** (remove exact stale tech_debt string)
    - `grep -F '8/8' .planning/v1.6-MILESTONE-AUDIT.md` returns **≥1** line **after** REQUIREMENTS shows AUDT-01 Complete
    - `grep -F 'AUDT-01' .planning/PROJECT.md` returns **≥1** line
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <automated>mix format --check-formatted && mix test test/scrypath/docs_contract_test.exs</automated>
</verification>

<success_criteria>
  - **AUDT-01** satisfied per **REQUIREMENTS.md** Phase 32 success criterion: every targeted deferred row is **terminal** with short reason.
  - Docs contract tests pass; no contradictory “open UAT / pending triage” language remains without explicit resolution wording.
</success_criteria>
