---
phase: 100
plan: 2
type: execute
wave: 2
depends_on:
  - 100-01
files_modified:
  - test/scrypath/phase99_contract_test.exs
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - TRUTH-01
  - TRUTH-02
must_haves:
  - "Extend `test/scrypath/phase99_contract_test.exs` with deterministic `TRUTH-01`/`TRUTH-02` install and release-truth parity assertions."
  - "Assert conflicting token absence (`{:scrypath, \"~> 1.0\"}`) on intake and mapped trust surfaces."
  - "Keep assertion style bounded to token/anchor/order checks in phase99/docs seams; no snapshot-style prose matching."
---

<objective>
Lock Phase 100 install/release contract truth with deterministic tests that fail on token drift across canonical and intake surfaces in the existing phase99 trust lane.
</objective>

<context>
After wave-1 wording reconciliation, this plan establishes explicit contract assertions for `TRUTH-01` and `TRUTH-02` by extending existing enforcement seams (`phase99_contract_test.exs` plus a bounded docs evergreen guard). Enforcement remains deterministic, service-free, and docs-token based.
</context>

<tasks>
### Task 100-02-01 - Extend phase99 contract seam for install/release coherence
<read_first>
- `test/scrypath/phase99_contract_test.exs`
- `test/scrypath/docs_contract_test.exs`
- `README.md`
- `guides/support-and-compatibility.md`
- `guides/outside-adopter-intake.md`
- `CONTRIBUTING.md`
- `docs/templates/outside-adopter-evidence.md`
</read_first>
<action>
- Extend `test/scrypath/phase99_contract_test.exs` as the canonical enforcement seam for Phase-100 trust assertions.
- Add/expand read targets as needed for:
  - `README.md`
  - `guides/support-and-compatibility.md`
  - `guides/outside-adopter-intake.md`
  - `CONTRIBUTING.md`
  - `docs/templates/outside-adopter-evidence.md`
- Add `TRUTH-01` assertions that verify:
  - `{:scrypath, "~> 0.3"}` appears on canonical/install surfaces.
  - `{:scrypath, "~> 1.0"}` does not appear in intake/template/readme/support surfaces.
  - intake/template include `exact Hex package version` and `exact git ref/commit`.
- Add `TRUTH-02` assertions that verify:
  - `release-backed guidance` token appears across canonical + entry/intake surfaces.
  - `main may contain unreleased changes` appears across canonical + entry/intake surfaces.
  - route token `guides/support-and-compatibility.md` remains present in README/CONTRIBUTING/intake.
</action>
<acceptance_criteria>
- `test -f "test/scrypath/phase99_contract_test.exs"` exits 0.
- `rg "defmodule Scrypath\\.Phase99ContractTest|defp assert_contains_all|defp ordered\\?" "test/scrypath/phase99_contract_test.exs"` returns at least 3 matches.
- `rg "TRUTH-01|TRUTH-02|\\{:scrypath, \"~> 0\\.3\"\\}|\\{:scrypath, \"~> 1\\.0\"\\}" "test/scrypath/phase99_contract_test.exs"` returns at least 4 matches.
- `rg 'release-backed guidance|main may contain unreleased changes|exact Hex package version|exact git ref/commit' "test/scrypath/phase99_contract_test.exs"` returns at least 4 matches.
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/phase99_contract_test.exs"` exits 0.
- `mix test test/scrypath/phase99_contract_test.exs` exits 0.
</acceptance_criteria>
<verify>
- `test -f "test/scrypath/phase99_contract_test.exs"`
- `rg "defmodule Scrypath\\.Phase99ContractTest|defp assert_contains_all|defp ordered\\?" "test/scrypath/phase99_contract_test.exs"`
- `rg "TRUTH-01|TRUTH-02|\\{:scrypath, \"~> 0\\.3\"\\}|\\{:scrypath, \"~> 1\\.0\"\\}" "test/scrypath/phase99_contract_test.exs"`
- `rg 'release-backed guidance|main may contain unreleased changes|exact Hex package version|exact git ref/commit' "test/scrypath/phase99_contract_test.exs"`
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/phase99_contract_test.exs"`
- `mix test test/scrypath/phase99_contract_test.exs`
</verify>

### Task 100-02-02 - Add bounded evergreen guard in docs contract suite
<read_first>
- `test/scrypath/docs_contract_test.exs`
- `test/scrypath/phase99_contract_test.exs`
- `README.md`
- `guides/support-and-compatibility.md`
- `guides/outside-adopter-intake.md`
</read_first>
<action>
- Extend `test/scrypath/docs_contract_test.exs` with one focused Phase-100 evergreen guard test that asserts:
  - `README.md` contains `{:scrypath, "~> 0.3"}`.
  - `guides/outside-adopter-intake.md` contains `{:scrypath, "~> 0.3"}` and refutes `{:scrypath, "~> 1.0"}`.
  - `guides/support-and-compatibility.md` contains `release-backed guidance` and `main may contain unreleased changes`.
- Keep this guard bounded and non-authoritative: phase99 contract tests remain the primary seam for required `TRUTH-01` / `TRUTH-02` enforcement.
</action>
<acceptance_criteria>
- `rg 'phase 100|TRUTH-01|TRUTH-02|~> 0\\.3|~> 1\\.0|release-backed guidance|main may contain unreleased changes' "test/scrypath/docs_contract_test.exs"` returns at least 5 matches.
- `mix test test/scrypath/docs_contract_test.exs test/scrypath/phase99_contract_test.exs` exits 0.
- `mix verify.phase99` exits 0.
</acceptance_criteria>
<verify>
- `rg 'phase 100|TRUTH-01|TRUTH-02|~> 0\\.3|~> 1\\.0|release-backed guidance|main may contain unreleased changes' "test/scrypath/docs_contract_test.exs"`
- `mix test test/scrypath/docs_contract_test.exs test/scrypath/phase99_contract_test.exs`
- `mix verify.phase99`
</verify>
</tasks>

<threat_model>
- **T100-P2-001 (Undetected Contract Regression, High):** docs tokens drift again after wave-1 edits with no phase-owned tests to catch it.  
  **Mitigation:** extend `phase99_contract_test.exs` with explicit `TRUTH-01` and `TRUTH-02` token assertions.  
  **Detection:** `mix test test/scrypath/phase99_contract_test.exs`.
- **T100-P2-002 (Install Token Confusion, High):** stale `{:scrypath, "~> 1.0"}` wording reappears in intake/template paths.  
  **Mitigation:** add explicit refute assertions for `~> 1.0` and positive assertions for `~> 0.3`.  
  **Detection:** `rg "\\{:scrypath, \"~> 1\\.0\"\\}|\\{:scrypath, \"~> 0\\.3\"\\}" "test/scrypath/phase99_contract_test.exs"`.
- **T100-P2-003 (Brittle Test Noise, Medium):** paragraph snapshots create churn and reduce trust-gate signal quality.  
  **Mitigation:** enforce token/anchor/order helper style and ban snapshot helpers in phase99/docs seams.  
  **Detection:** `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/phase99_contract_test.exs" "test/scrypath/docs_contract_test.exs"`.
</threat_model>

<verification>
- `mix test test/scrypath/phase99_contract_test.exs`
- `mix test test/scrypath/docs_contract_test.exs`
- `mix verify.phase99`
- `rg 'TRUTH-01|TRUTH-02|\\{:scrypath, \"~> 0\\.3\"\\}|\\{:scrypath, \"~> 1\\.0\"\\}|release-backed guidance|main may contain unreleased changes' "test/scrypath/phase99_contract_test.exs" "test/scrypath/docs_contract_test.exs"`
</verification>

<success_criteria>
- Phase 100 contract extensions in `phase99_contract_test.exs` enforce install-version and release-truth parity across mapped surfaces.
- Conflicting major token `{:scrypath, "~> 1.0"}` is explicitly rejected by deterministic tests.
- Evergreen docs-contract guard contains at least one bounded Phase-100 invariant.
- Enforcement remains in the existing phase99 trust lane (`mix verify.phase99`) with no new gate proliferation.
</success_criteria>

<output>
- Updated `test/scrypath/phase99_contract_test.exs`
- Updated `test/scrypath/docs_contract_test.exs`
</output>
