---
phase: 25
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - guides/relevance-tuning.md
  - guides/operator-mix-tasks.md
  - CHANGELOG.md
autonomous: true
requirements: [TUNE14-02]
tags: [docs, operator, changelog]

must_haves:
  truths:
    - "`guides/relevance-tuning.md` replaces the v1.3-only deferral paragraph with a **hot vs managed** comparison that names **`mix scrypath.settings.diff`**, **`Scrypath.reindex/2`**, and **`Settings.hot_apply/3`** (25-CONTEXT D-12)"
    - "Explicit **non-goals**: no `ranking_rules` / broader settings on the hot path in v1.4 (TUNE14-02, REQUIREMENTS out-of-scope)"
    - "`CHANGELOG.md` Unreleased notes the real `hot_apply` implementation and points operators to the guide"
  artifacts:
    - path: "guides/relevance-tuning.md"
      provides: "TUNE14-02 operator guidance"
      contains: "hot_apply"
    - path: "CHANGELOG.md"
      provides: "release note"
      contains: "hot_apply"
---

<objective>
Deliver **TUNE14-02**: operator-facing documentation that explains when **`Scrypath.reindex/2`** (managed pipeline + optional full verify) is appropriate versus **`Scrypath.Meilisearch.Settings.hot_apply/3`** (bounded live PATCH for **`synonyms`**, **`stop_words`**, **`typo_tolerance`** only), lists **non-goals** for v1.4, references **`mix scrypath.settings.diff`** for declared-vs-applied parity, and documents **`mix scrypath.settings.hot_apply`** next to the other settings tasks. Update **CHANGELOG** Unreleased.
</objective>

<threat_model>
**ASVS L1 — documentation only**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-25-DOC: Operators misuse hot_apply for ranking | Low | Non-goals table + bold “do not use hot_apply for …” |

No executable code in this plan beyond markdown.
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Rewrite relevance-tuning `hot_apply` section (TUNE14-02)</name>
  <files>guides/relevance-tuning.md</files>
  <read_first>
    - guides/relevance-tuning.md (full file — especially `## Operator mix tasks` and `## hot_apply deferral`)
    - .planning/phases/25-settings-hot-apply-narrow/25-CONTEXT.md (D-10–D-12, canonical refs)
    - lib/scrypath/meilisearch/settings.ex (`@doc` for `hot_apply/3` after plan 01 — align wording)
  </read_first>
  <action>
    Replace **`## hot_apply deferral (v1.3)`** (or equivalent heading) with a new section titled **`## Settings hot apply (v1.4)`** containing:

    1. **What it is:** one paragraph — **`Settings.hot_apply/3`** patches **only** live **`synonyms`**, **`stop_words`**, **`typo_tolerance`**; requires **`acknowledge_live_index: true`**; errors **`{:error, :live_ack_required}`**, **`{:error, {:unsupported_hot_apply_keys, keys}}`**, **`{:error, {:hot_apply_failed, details}}`** (adjust atom names to match implementation).
    2. **Hot vs managed table** (markdown pipe table) with columns **Concern | Prefer `reindex/2` | Prefer `hot_apply/3`** and at least **three rows** (examples: parity proof / schema-wide changes / latency-only synonym tweak — use concrete operator language from 25-CONTEXT).
    3. **Non-goals:** bullet list explicitly excluding **`ranking_rules`**, **`distinct_attribute`**, and “any setting outside the three-key allowlist” for v1.4.
    4. **Proof of full parity:** sentence that **`mix scrypath.settings.diff`** (and managed reindex + **`verify_applied/3`**) remains the contract for **full** declared-vs-applied checks — hot apply does **not** replace diff (D-12).
    5. **CLI:** bullet naming **`mix scrypath.settings.hot_apply SCHEMA --settings-file path.json --ack-live`** plus shared runtime switches (link to **`guides/operator-mix-tasks.md`**).
    6. **`release eval` example:** one fenced **`bash`** block showing **`bin/my_release eval 'Application.ensure_all_started(:my_app); ... Settings.hot_apply(...)'`** placeholder using the **real module name** pattern from **`guides/relevance-tuning.md`** elsewhere (adapt from existing eval snippets if any; if none, write a generic **`MyApp.Repo`**-style comment).

    Update **`## Operator mix tasks`** list to include **`mix scrypath.settings.hot_apply`** alongside diff/read.
  </action>
  <verify>
    <automated>grep -c 'Settings hot apply (v1.4)' guides/relevance-tuning.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Settings hot apply (v1.4)' guides/relevance-tuning.md` is at least `1`
    - `grep -c 'mix scrypath.settings.diff' guides/relevance-tuning.md` is at least `1`
    - `grep -c 'Non-goals' guides/relevance-tuning.md` is at least `1`
    - `grep -c 'ranking_rules' guides/relevance-tuning.md` is at least `1`
    - `grep -c 'mix scrypath.settings.hot_apply' guides/relevance-tuning.md` is at least `1`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Cross-link from operator mix tasks guide</name>
  <files>guides/operator-mix-tasks.md</files>
  <read_first>
    - guides/operator-mix-tasks.md (entire file)
    - guides/relevance-tuning.md (after Task 1)
  </read_first>
  <action>
    Append a new **`## mix scrypath.settings.hot_apply`** section near other low-level operator tools (or after shared runtime options if no better place) with **one** usage example, the **`--ack-live`** requirement, pointer to **`guides/relevance-tuning.md`** for tradeoffs, and the **`--settings-file`** flag name matching the implementation.
  </action>
  <verify>
    <automated>grep -c 'scrypath.settings.hot_apply' guides/operator-mix-tasks.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'relevance-tuning' guides/operator-mix-tasks.md` is at least `1`
    - `grep -c '--ack-live' guides/operator-mix-tasks.md` is at least `1`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: CHANGELOG Unreleased entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (`## [Unreleased]` head)
    - .planning/REQUIREMENTS.md (TUNE14-01 / TUNE14-02 wording)
  </read_first>
  <action>
    Under **`## [Unreleased]`**, add a **Added** (or **Changed** — pick the conventional commit category that fits keep-a-changelog style already used in this file) bullet that states **`Settings.hot_apply/3`** is implemented for the allow-listed settings, mentions **`mix scrypath.settings.hot_apply`**, and cites **`TUNE14-01`** / **`TUNE14-02`** as literal substrings. Remove or rewrite any Unreleased bullet that still claims **`hot_apply`** is permanently stubbed / `:hot_apply_disabled` only if such a bullet exists.
  </action>
  <verify>
    <automated>grep -n 'TUNE14-0' CHANGELOG.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'TUNE14-01' CHANGELOG.md` is at least `1`
    - `grep -c 'TUNE14-02' CHANGELOG.md` is at least `1`
    - `sed -n '/## Unreleased/,/^## \\[/p' CHANGELOG.md | grep -c ':hot_apply_disabled'` is `0` (no stale stub wording in **Unreleased** — macOS **`sed`** supports this range)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix format --check-formatted` (no-op for markdown but keeps habit)
- `grep -E 'TUNE14|hot_apply' guides/relevance-tuning.md CHANGELOG.md guides/operator-mix-tasks.md`
</verification>

<success_criteria>
- TUNE14-02 documentation and changelog coverage are complete and consistent with implemented APIs from plan 01–02.
</success_criteria>
