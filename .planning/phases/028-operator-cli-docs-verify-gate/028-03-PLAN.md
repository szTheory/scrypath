---
phase: 28
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - lib/mix/tasks/verify.phase28.ex
  - mix.exs
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements: [OPS15-04]
tags: [verify, docs, exunit]

must_haves:
  truths:
    - "**`mix verify.phase28`** exists, rejects args like other verify tasks, runs focused **`mix test`** with **`--warnings-as-errors`**, then **`mix docs --warnings-as-errors`**, and includes **no** `HEX_API_KEY` substring in source (028-CONTEXT D-10)"
    - "Docs contract tests pin **`verify.phase28`** module contents and referenced test paths"
  artifacts:
    - path: "lib/mix/tasks/verify.phase28.ex"
      provides: "Mix.Tasks.Verify.Phase28"
      contains: "defmodule Mix.Tasks.Verify.Phase28"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "contract assertions for verify.phase28"
      contains: "verify.phase28"
---

<objective>
Add the auth-free milestone gate **`mix verify.phase28`** and wire it into **`mix.exs`** and **`docs_contract_test.exs`** so CI-style doc contracts know the v1.5 operator slice (OPS15-04).
</objective>

<threat_model>
**ASVS L1 — verify task**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-28-VERIFY: Publishing secrets via verify task | High | Copy **`verify.phase26`** structure; **`refute`** task source contains **`HEX_API_KEY`** in tests if pattern exists elsewhere |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Implement `Mix.Tasks.Verify.Phase28`</name>
  <files>lib/mix/tasks/verify.phase28.ex</files>
  <read_first>
    - lib/mix/tasks/verify.phase26.ex
    - lib/mix/tasks/verify.phase14.ex
    - .planning/phases/028-operator-cli-docs-verify-gate/028-CONTEXT.md
  </read_first>
  <action>
    Create **`lib/mix/tasks/verify.phase28.ex`** as **`Mix.Tasks.Verify.Phase28`** mirroring **`verify.phase26`**:

    - **`@shortdoc`** text explicitly mentions **index contract drift** CLI and **v1.5** operator/doc slice.
    - **`@focused_tests`** must include at minimum:
      - **`test/scrypath/operator/index_contract_drift_test.exs`**
      - **`test/scrypath/mix_tasks/operator_tasks_test.exs`**
      - **`test/scrypath/docs_contract_test.exs`**
    - Append **`"--warnings-as-errors"`** to the **`mix test`** argv list like phase26.
    - Run **`Mix.Task.run("docs", ["--warnings-as-errors"])`** after tests.
    - **`ensure_no_args!/1`** error message must contain the literal substring **`verify.phase28`**.
  </action>
  <verify>
    <automated>mix format --check-formatted && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/mix/tasks/verify.phase28.ex` exits 0
    - `grep -n 'test/scrypath/operator/index_contract_drift_test.exs' lib/mix/tasks/verify.phase28.ex` returns ≥1 line
    - `grep -n 'Mix.Task.run("docs"' lib/mix/tasks/verify.phase28.ex` returns ≥1 line
    - `grep HEX_API_KEY lib/mix/tasks/verify.phase28.ex`; exit code for grep must be **1** (no matches)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Register `verify.phase28` preferred env</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs
    - lib/mix/tasks/verify.phase28.ex
  </read_first>
  <action>
    Add **`"verify.phase28": :test`** to **`preferred_envs`** in **`mix.exs`**, adjacent to **`verify.phase26`**.
  </action>
  <verify>
    <automated>mix format --check-formatted && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '"verify.phase28"' mix.exs` returns exactly one line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Docs contract tests for `verify.phase28`</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs
    - lib/mix/tasks/verify.phase28.ex
    - docs/operator-support.md
    - .github/workflows/ci.yml (only if you add CI mention — optional)
  </read_first>
  <action>
    1. At module top, add **`@verify_phase28 File.read!("lib/mix/tasks/verify.phase28.ex")`** next to other **`@verify_phaseNN`** attributes.

    2. Extend **`@operator_support_docs`** consumers: add assertions that **`docs/operator-support.md`** contains **`mix verify.phase28`** (may already be satisfied by Plan 02 — then this test still validates contract).

    3. Add a new test **`test "verify.phase28 keeps the index contract operator gate auth-free and docs-focused"`** (or equivalent name) that **`assert_contains_all(@verify_phase28, [...])`** including at least:
       - **`test/scrypath/operator/index_contract_drift_test.exs`**
       - **`test/scrypath/mix_tasks/operator_tasks_test.exs`**
       - **`test/scrypath/docs_contract_test.exs`**
       - **`Mix.Task.run(\"docs\", [\"--warnings-as-errors\"])`** as an escaped substring match exactly as in phase26 test
       - **`--warnings-as-errors`**

    4. **`refute @verify_phase28 =~ "HEX_API_KEY"`**

    5. If **`docs_contract_test`** has a consolidated list test for CI workflow verify commands, add **`mix verify.phase28`** to that list **only if** `.github/workflows/ci.yml` is updated in the same phase to run it — **otherwise** skip CI list update to avoid false failures (note in SUMMARY if CI lags).

    6. Update any **`assert_contains_all(@operator_support_docs`, ...)** block that lists verify phases to include **`mix verify.phase28`** if not already present after Plan 02.
  </action>
  <verify>
    <automated>mix format --check-formatted && mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '@verify_phase28' test/scrypath/docs_contract_test.exs` returns ≥1 line
    - `grep -n 'verify.phase28' test/scrypath/docs_contract_test.exs` returns ≥2 lines
    - `mix test test/scrypath/docs_contract_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <automated>mix verify.phase28</automated>
</verification>

<success_criteria>
  - **`mix verify.phase28`** is discoverable, registered, green locally, and pinned by docs contract tests
</success_criteria>
