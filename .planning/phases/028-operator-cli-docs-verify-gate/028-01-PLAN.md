---
phase: 28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/scrypath/index/contract_drift.ex
  - mix.exs
  - test/scrypath/mix_tasks/operator_tasks_test.exs
autonomous: true
requirements: [OPS15-02]
tags: [operator, mix, cli, elixir]

must_haves:
  truths:
    - "`mix scrypath.index.contract_drift SCHEMA` delegates to `Scrypath.index_contract_drift/2` with resolved runtime config (028-CONTEXT D-02)"
    - "Exit **0** = comparison completed and all report dimensions `match: true`; exit **2** = comparison completed with ≥1 dimension `match: false`; exit **1** = could not complete (args, `:index_not_found`, network, internal) — identical semantics with and without `--json` (028-CONTEXT D-04, D-05)"
    - "`@moduledoc` / `@shortdoc` contain the literal substring `index_contract_drift/2` (028-CONTEXT D-03)"
  artifacts:
    - path: "lib/mix/tasks/scrypath/index/contract_drift.ex"
      provides: "Mix task `Mix.Tasks.Scrypath.Index.ContractDrift`"
      contains: "ContractDrift"
    - path: "mix.exs"
      provides: "preferred_envs entry for scrypath.index.contract_drift"
      contains: "scrypath.index.contract_drift"
---

<objective>
Ship **`mix scrypath.index.contract_drift`** as a thin Mix wrapper over **`Scrypath.index_contract_drift/2`** with **`--json`**, sparse human output with an explicit OK line on parity, and **0 / 2 / 1** exit codes matching **`mix scrypath.settings.diff`** (OPS15-02).
</objective>

<threat_model>
**ASVS L1 — operator read-only CLI**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-28-CLI: Accidental index mutation from “drift” command | Medium | Task only calls **`Scrypath.index_contract_drift/2`** (read path); no `update_settings`, document writes, or task-cancel APIs |
| T-28-CLI: Secrets in stdout/stderr | Low | Reuse report encoder only — no API keys, no document payloads |
| T-28-CLI: Wrong exit code under `--json` masking drift | High | Branch on **`{:ok, report}`** dimension matches, not on JSON string emptiness; use **`System.halt(2)`** for drift after printing JSON |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add `Mix.Tasks.Scrypath.Index.ContractDrift`</name>
  <files>lib/mix/tasks/scrypath/index/contract_drift.ex</files>
  <read_first>
    - lib/mix/tasks/scrypath.settings.diff.ex
    - lib/scrypath/cli/operator_task.ex
    - lib/scrypath.ex (delegate `index_contract_drift/2`)
    - lib/scrypath/operator/index_contract_drift/report.ex
    - .planning/phases/028-operator-cli-docs-verify-gate/028-CONTEXT.md
  </read_first>
  <action>
    Create **`lib/mix/tasks/scrypath/index/contract_drift.ex`** defining **`Mix.Tasks.Scrypath.Index.ContractDrift`**:

    1. **`use Mix.Task`**, **`@shortdoc`** and **`@moduledoc`** documenting: usage `mix scrypath.index.contract_drift SCHEMA [--json] [--repo ...] [--index-prefix ...]` (mirror settings.diff); exit codes **0 / 2 / 1** with the same meanings as **`mix scrypath.settings.diff`**; shell note with **`set +e`** / **`$?`** for exit-2 pipelines (028-CONTEXT D-06); explicit sentence that the API entry is **`Scrypath.index_contract_drift/2`** (028-CONTEXT D-03).

    2. **`@extra_switches [json: :boolean]`** passed into **`OperatorTask.parse!(args, @extra_switches)`**.

    3. **`run/1`:** `Mix.Task.run("app.start")`; **`{opts, argv} = OperatorTask.parse!(...)`**; **`schema = OperatorTask.schema_from_argv!(argv)`**; **`config = Scrypath.Config.resolve!(OperatorTask.runtime_opts(opts) ++ OperatorTask.test_operator_opts())`**.

    4. **`case Scrypath.index_contract_drift(schema, config)`**:
       - **`{:ok, report}`** — if **`report_dimensions_all_match?(report)`** (private helper: every dimension in **`report.dimensions`** has **`%Dimension{match: true}`**), emit **human** sparse output: first line **`Index contract OK for ... (index=...)`** (exact words must include **`Index contract OK`**); if **`opts[:json]`**, `Mix.shell().info(Jason.encode!(report))` and still exit 0.
       - **`{:ok, report}`** with any **`match: false`**: if **`opts[:json]`**, print **`Jason.encode!(report)`** then **`System.halt(2)`**; else print header line with schema + index, then **only** lines for dimensions where **`match: false`** (include dimension name as atom or string + short detail summary), then a 1–3 line footer mentioning **`--json`** and pointers to **`mix scrypath.settings.diff`**, **`Scrypath.reconcile_sync/2`**, **`Scrypath.reindex/2`** without introducing new verbs — then **`System.halt(2)`**.

    5. **`{:error, reason}`** — call **`OperatorTask.error!("scrypath.index.contract_drift", reason)`** (do not `halt(2)`).

    Implement **`report_dimensions_all_match?/1`** private in the same module (or tiny private helper module if you must, but prefer same file).
  </action>
  <verify>
    <automated>mix format --check-formatted && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/mix/tasks/scrypath/index/contract_drift.ex` exits 0
    - `grep -n 'defmodule Mix.Tasks.Scrypath.Index.ContractDrift' lib/mix/tasks/scrypath/index/contract_drift.ex` returns ≥1 line
    - `grep -F 'index_contract_drift/2' lib/mix/tasks/scrypath/index/contract_drift.ex` returns ≥1 line
    - `grep -n 'System.halt(2)' lib/mix/tasks/scrypath/index/contract_drift.ex` returns ≥1 line
    - `grep -n 'Jason.encode!' lib/mix/tasks/scrypath/index/contract_drift.ex` returns ≥1 line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Register preferred Mix env</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (`def cli` / `preferred_envs`)
    - lib/mix/tasks/scrypath/index/contract_drift.ex
  </read_first>
  <action>
    In **`mix.exs`** inside **`preferred_envs`**, add the key **`"scrypath.index.contract_drift": :test`** adjacent to the other **`scrypath.settings.*`** entries (keep alphabetical or existing grouping style used in file).
  </action>
  <verify>
    <automated>mix format --check-formatted && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '"scrypath.index.contract_drift"' mix.exs` returns exactly one line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: ExUnit coverage for the new Mix task</name>
  <files>test/scrypath/mix_tasks/operator_tasks_test.exs</files>
  <read_first>
    - test/scrypath/mix_tasks/operator_tasks_test.exs
    - test/scrypath/operator/index_contract_drift_test.exs
    - lib/mix/tasks/scrypath/index/contract_drift.ex
  </read_first>
  <action>
    Extend **`test/scrypath/mix_tasks/operator_tasks_test.exs`** (same **`setup`** as existing operator task tests):

    1. **Parity path:** Configure stubs so **`Scrypath.index_contract_drift(SearchablePost, opts)`** would return **`{:ok, %Report{}}`** with **all dimensions `match: true`** (reuse the same **`__get_settings_response__`** / config approach as **`index_contract_drift_test.exs`** if available; otherwise set **`Application.put_env(:scrypath, :operator_task_test_opts, ...)`** with a stub **`meilisearch_client`** module already used elsewhere). Run **`Mix.Task.run("scrypath.index.contract_drift", ["SearchablePost"])`** inside **`capture_io`**. Assert stdout contains the substring **`Index contract OK`**.

    2. **Drift path:** Stub so at least one dimension has **`match: false`** (minimal fake applied settings mismatch). Run task with **`capture_io`**. Assert output mentions a failing dimension key (e.g. **`fields`** or **`settings`**) and assert process exits **2** using **`catch_exit`** / **`ExUnit.assert_raise`** pattern consistent with how other tasks test **`System.halt`** if present — **if testing halt is too brittle**, spawn **`System.cmd("mix", ...)`** in **`MIX_ENV=test`** is acceptable; simplest acceptable approach: test **`Mix.Tasks.Scrypath.Index.ContractDrift.run/1`** via **private test hook** only if needed — **preferred:** use **`assert catch_exit(Mix.Task.run(...)) == {:shutdown, 2}`** or project’s existing halt test idiom from grep of **`System.halt`**.

    3. **`--json` parity:** Same parity stub; run with **`["SearchablePost", "--json"]`**; assert stdout parses as JSON with **`"version"`** key and **`"dimensions"`** key.

    Document in a comment if **`System.halt`** cannot be asserted and you used **`mix` subprocess** instead.
  </action>
  <verify>
    <automated>mix format --check-formatted && mix test test/scrypath/mix_tasks/operator_tasks_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'scrypath.index.contract_drift' test/scrypath/mix_tasks/operator_tasks_test.exs` returns ≥1 line
    - `mix test test/scrypath/mix_tasks/operator_tasks_test.exs` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <automated>mix format --check-formatted && mix compile --warnings-as-errors && mix test test/scrypath/mix_tasks/operator_tasks_test.exs</automated>
</verification>

<success_criteria>
  - `mix scrypath.index.contract_drift` exists, is registered in **`mix.exs`**, and has tests proving OK output and JSON shape
  - Exit semantics match **`settings.diff`** for parity vs drift vs error paths
</success_criteria>
