---
phase: 28
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - guides/drift-recovery.md
  - docs/operator-support.md
  - guides/operator-mix-tasks.md
autonomous: true
requirements: [OPS15-03]
tags: [docs, operator, guides]

must_haves:
  truths:
    - "Operators can find when to use **index contract drift** vs **`mix scrypath.settings.diff`**, **`Scrypath.reconcile_sync/2`**, and **`Scrypath.reindex/2`** from **`guides/drift-recovery.md`** and **`docs/operator-support.md`** (028-CONTEXT, OPS15-03)"
    - "`docs/operator-support.md` first-response list mentions **`mix scrypath.index.contract_drift`** (or grouped name) and **`Scrypath.index_contract_drift/2`** in the triage ordering (028-CONTEXT D-12)"
  artifacts:
    - path: "guides/drift-recovery.md"
      provides: "contract drift vs settings drift runbook"
      contains: "index.contract_drift"
    - path: "docs/operator-support.md"
      provides: "verify.phase28 callout in verify table"
      contains: "verify.phase28"
---

<objective>
Update operator-facing documentation so teams know **when** to run **index contract drift** reporting versus **settings-only diff**, **reconcile**, and **managed reindex**, with minimal duplicated matrices and cross-links (OPS15-03).
</objective>

<threat_model>
**ASVS L1 — documentation**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-28-DOC: Misrouting operators to destructive commands | Medium | Keep **report-first** language; never imply drift commands auto-heal; link to existing reconcile/reindex guides only as **explicit** follow-ups |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Extend drift-recovery guide</name>
  <files>guides/drift-recovery.md</files>
  <read_first>
    - guides/drift-recovery.md
    - .planning/phases/028-operator-cli-docs-verify-gate/028-CONTEXT.md
    - docs/operator-support.md
  </read_first>
  <action>
    Add a new section (suggested title: **## Index contract drift (fields, filterable, sortable, faceting, settings families)**) placed **after** the existing **## Settings drift** section (or merge into it with a clear subsection) that explains:

    - **Symptom:** search/index “looks wrong” in ways that may be **contract** mismatches beyond ranking-only settings.
    - **Use `mix scrypath.index.contract_drift YOUR_SCHEMA`** (and **`--json`**) or **`Scrypath.index_contract_drift/2`** for the structured **declared vs live** report from Phase 27.
    - **Contrast:** **`mix scrypath.settings.diff`** stays focused on **declared vs applied Meilisearch `settings:` keys** and rebuild-class posture; **contract drift** covers **searchable/filterable/sortable/facet** contract dimensions too.
    - **Cross-link** `docs/operator-support.md#first-response-path` (or plain path **`docs/operator-support.md`**) for maintainer ordering.

    Require the literal Markdown substring **`mix scrypath.index.contract_drift`** at least once for docs contract grep stability.
  </action>
  <verify>
    <automated>mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'mix scrypath.index.contract_drift' guides/drift-recovery.md` returns ≥1 line
    - `grep -n 'Scrypath.index_contract_drift' guides/drift-recovery.md` returns ≥1 line
    - `grep -n 'settings.diff' guides/drift-recovery.md` returns ≥1 line (retain or strengthen contrast)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Refresh operator-support maintainer path</name>
  <files>docs/operator-support.md</files>
  <read_first>
    - docs/operator-support.md
    - guides/drift-recovery.md
    - .planning/phases/028-operator-cli-docs-verify-gate/028-CONTEXT.md
  </read_first>
  <action>
    Update **`docs/operator-support.md`**:

    1. **First Response Path** — insert a step for **index contract drift** (CLI or API) **after** `mix scrypath.status` / **`failed`** and **before or alongside** `mix scrypath.reconcile`, describing that it answers **declared schema vs live index contract** questions distinct from queue failure triage.

    2. **Verification section** — add **`mix verify.phase28`** to the shell example block and to the markdown table describing what each verify gate covers. Wording must state **`mix verify.phase28`** covers **index contract drift CLI + operator docs contracts** (align with Phase 28 scope). Replace any implication that **`mix verify.phase14`** alone is the only doc/Mix alignment gate for **v1.5 index contract** concerns (028-CONTEXT D-12) without deleting phase14 row.

    Require literal substrings **`mix verify.phase28`** and **`mix scrypath.index.contract_drift`** each at least once in this file after edits.
  </action>
  <verify>
    <automated>mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'mix verify.phase28' docs/operator-support.md` returns ≥1 line
    - `grep -n 'mix scrypath.index.contract_drift' docs/operator-support.md` returns ≥1 line
    - `grep -n 'mix verify.phase14' docs/operator-support.md` returns ≥1 line (row retained)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Operator Mix tasks guide alignment</name>
  <files>guides/operator-mix-tasks.md</files>
  <read_first>
    - guides/operator-mix-tasks.md
    - lib/mix/tasks/scrypath/index/contract_drift.ex
  </read_first>
  <action>
    Add **`mix scrypath.index.contract_drift`** to the guide’s inventory of operator tasks (same style as other `scrypath.*` entries): one-line purpose, link or mention **`Scrypath.index_contract_drift/2`**, note **`--json`** and exit codes if the guide lists them for other tasks.

    Include literal substring **`scrypath.index.contract_drift`** in the markdown.
  </action>
  <verify>
    <automated>mix format --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'scrypath.index.contract_drift' guides/operator-mix-tasks.md` returns ≥1 line
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <automated>mix test test/scrypath/docs_contract_test.exs</automated>
</verification>

<success_criteria>
  - Drift recovery and operator support docs cross-link and distinguish contract vs settings drift without duplicating full matrices
  - Operator Mix tasks guide lists the new task
</success_criteria>
