---
phase: 40-all-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/multi_search/all_expansion.ex
  - lib/scrypath/search.ex
  - lib/scrypath/options.ex
autonomous: true
requirements:
  - FED-02
tags:
  - search_many
  - federation
  - expansion
must_haves:
  truths:
    - "`Search.run_search_many/2` expands `{:all, text}` / `{:all, text, keyword}` entries to explicit `{schema, text, keyword}` tuples **before** `Entries.normalize/2`, using a declared allowlist (`global_schemas:` override or `Application.get_env(otp_app, :scrypath_global_search_schemas, [])`)."
    - "Empty allowlist when `:all` is present returns `{:error, {:invalid_options, {:all_expansion, :empty_registry}}}}` — never `:empty_schema_list` for that case."
    - "`max_schemas` applies to the **expanded** flat entry list; overflow returns `{:error, {:too_many_schemas, count, max}}` unchanged."
  artifacts:
    - path: "lib/scrypath/multi_search/all_expansion.ex"
      provides: "Pure expansion + registry resolution"
      contains: "def expand"
    - path: "lib/scrypath/options.ex"
      provides: "Optional `global_schemas:` runtime option accepted by validate_runtime_options!"
      contains: "global_schemas"
  key_links:
    - from: "AllExpansion.expand/2"
    - to: "Entries.normalize/2"
    - via: "Search.run_search_many"
    - pattern: "expand then normalize"
---

<objective>
Add **`Scrypath.MultiSearch.AllExpansion.expand/2`**, register **`global_schemas:`** on **`@runtime_options`**, and wire **`Search.run_search_many/2`** so **`{:all, text}`** / **`{:all, text, entry_kw}`** splice to allowlisted schema tuples **before** **`MultiSearch.Entries.normalize/2`**, with **FED-02** error shapes and **post-expansion** cardinality rails.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-all-expansion/40-CONTEXT.md
@.planning/phases/40-all-expansion/40-RESEARCH.md
@.planning/phases/40-all-expansion/40-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `global_schemas` runtime option</name>
  <files>lib/scrypath/options.ex</files>
  <read_first>
    - lib/scrypath/options.ex (`@runtime_options`, `runtime_option_keys/0`, `validate_runtime_options!/1`)
    - .planning/phases/40-all-expansion/40-CONTEXT.md (D-06, D-07, D-13)
  </read_first>
  <action>
    1. Add **`global_schemas:`** to **`@runtime_options`** with **`default: nil`** and a **custom type** (or `{:list, :atom}` if sufficient) that accepts **`nil`** or a **non-empty list of module atoms**; reject non-modules with a **NimbleOptions**-compatible error.
    2. Document in the option’s **`doc:`** string that when **set**, it **replaces** the application-env allowlist for **`{:all, …}`** resolution for that `search_many/2` call (quote CONTEXT D-07).
    3. Ensure **`runtime_option_keys/0`** picks up the new key automatically (it uses **`Keyword.keys(@runtime_options)`** — confirm no manual list duplication).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'global_schemas' lib/scrypath/options.ex` returns ≥ 1 line inside `@runtime_options` (field definition block).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    Runtime opts schema accepts `global_schemas:` without breaking `Config.resolve!/1` on `search_many` paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: `MultiSearch.AllExpansion.expand/2`</name>
  <files>lib/scrypath/multi_search/all_expansion.ex</files>
  <read_first>
    - lib/scrypath/multi_search/entries.ex (`normalize_one/2` tuple shapes, `malformed_entry`)
    - .planning/phases/40-all-expansion/40-CONTEXT.md (D-01–D-03, D-11–D-12)
    - .planning/phases/21-multi-index-search/21-CONTEXT.md (tuple API invariants)
  </read_first>
  <action>
    1. Create **`lib/scrypath/multi_search/all_expansion.ex`** with **`@moduledoc false`** (or brief internal doc).
    2. If **`entries` contains no **`{:all`, ...}`** tuple at any position, return **`{:ok, entries}`** immediately (no **`otp_app`** / **`global_schemas`** required).
    3. Implement **`@spec expand(list(), keyword()) :: {:ok, list()} | {:error, term()}`** where the success list contains **only** **`{module(), String.t(), keyword()}`** or **`{module(), String.t()}`** forms that **`Entries.normalize/2` already accepts** — always emit **three-tuples** with **`[]`** third element when the caller passed a two-tuple schema entry.
    4. **Registry resolution:** If **`Keyword.has_key?(shared_opts, :global_schemas)`**, use **`Keyword.fetch!(shared_opts, :global_schemas)`** as the ordered allowlist (may be **`[]`** → **empty registry** when `:all` appears). **Else** use **`otp_app = Keyword.get(shared_opts, :otp_app)`**; if **`otp_app` is `nil`**, return **`{:error, {:invalid_options, {:all_expansion, :missing_otp_app}}}}`**. If **`otp_app` is set**, use **`Application.get_env(otp_app, :scrypath_global_search_schemas, [])`**.
    5. When **`{:all, text}`** or **`{:all, text, kw}`** is expanded and the resolved allowlist is **`[]`**, return **`{:error, {:invalid_options, {:all_expansion, :empty_registry}}}}`**.
    6. For each module **`m`** in the allowlist (in order), append **`{m, text, entry_kw}`** where **`entry_kw`** is **`[]`** for **`{:all, text}`** or the third element for **`{:all, text, kw}`** (must be a list; otherwise **`{:error, {:invalid_options, :malformed_entry}}`**).
    7. Non-`:all` entries pass through unchanged in order.
  </action>
  <verify>
    <automated>mix test test/scrypath/multi_search/entries_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/scrypath/multi_search/all_expansion.ex` exists and defines **`def expand(`**.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    Expansion module compiles; entries tests still green (no integration yet).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire `Search.run_search_many/2` + telemetry count</name>
  <files>lib/scrypath/search.ex</files>
  <read_first>
    - lib/scrypath/search.ex (`search_many/2`, `run_search_many/2`, telemetry `metadata`)
    - lib/scrypath/multi_search/all_expansion.ex (`expand/2`)
  </read_first>
  <action>
    1. Change **`run_search_many/2`** to: **`with {:ok, expanded} <- AllExpansion.expand(entries, shared_opts), {:ok, quads} <- Entries.normalize(expanded, shared_opts), ...`** (alias **`Scrypath.MultiSearch.AllExpansion`** as **`AllExpansion`**).
    2. Update **`search_many/2`** **`Telemetry.span`** metadata from **`%{schema_count: length(entries)}`** to **`%{schema_count: length(expanded), raw_entry_count: length(entries)}`** — but **`expanded` is inside `run_search_many`**; either **compute counts inside the span closure** after expand **or** pass both lengths from `run_search_many` — **minimum:** **`schema_count` must reflect post-expansion length** for operator truthfulness.
    3. Extend **`@moduledoc`** on **`search_many/2`** (the big doc block) with a subsection **`## :all expansion`** describing **`{:all, text}`**, **`{:all, text, keyword}`**, **`global_schemas:`**, and **`:scrypath_global_search_schemas`** env key under **`otp_app`** (exact strings for Phase 41 contract tests later).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'AllExpansion.expand' lib/scrypath/search.ex` returns ≥ 1 line.
    - `grep -n 'schema_count' lib/scrypath/search.ex` returns ≥ 1 line and the **`Telemetry.span`** metadata map binds **`schema_count`** to **`length(expanded)`** (or an equivalent post-expand variable), not **`length(entries)`** alone.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    Pipeline calls expansion before normalize; docs mention `:all` and registry keys.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → expansion | Caller supplies module atoms in **`global_schemas:`** or app env — same trust as explicit **`{Schema, _}`** tuples. |
| Registry → HTTP | Expansion errors return before **`Config.resolve!`** / backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01-01 | Elevation | Arbitrary module execution via strings | mitigate | Only accept **atom module references** from config/code — no **`String.to_existing_atom/1`** on user input in this path. |
| T-40-01-02 | DoS | Accidental huge allowlist | mitigate | **`max_schemas`** rail after expansion (existing **`Entries`** check). |

No HIGH-severity items beyond existing search surface.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/scrypath/multi_search/entries_test.exs test/scrypath/search_many_test.exs` exits 0.
</verification>

<success_criteria>
- FED-02 (partial): **`:all`** expansion + **declared registry** + **explicit error tuples** wired in **`search_many/2`** pipeline before backend I/O.
</success_criteria>
