---
phase: 40-all-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - "40-01"
files_modified:
  - test/scrypath/search_many_test.exs
  - test/scrypath/multi_search/all_expansion_test.exs
  - lib/scrypath/search.ex
autonomous: true
requirements:
  - FED-02
tags:
  - tests
  - search_many
must_haves:
  truths:
    - "Automated tests prove **happy** `:all` expansion (≥2 schemas) and **rejection** paths: **empty registry**, **`max_schemas`** exceeded after expansion, **`missing_otp_app`** when env path needed, **malformed** `{:all, ...}`."
    - "`grep` on `test/scrypath/search_many_test.exs` finds **`:all_expansion`** substring in at least one assertion or expected error tuple."
  artifacts:
    - path: "test/scrypath/multi_search/all_expansion_test.exs"
      provides: "Unit tests for `AllExpansion.expand/2` edge cases"
      contains: "AllExpansion"
    - path: "test/scrypath/search_many_test.exs"
      provides: "Integration tests via `search_many/2` + `FakeBackend`"
      contains: "global_schemas"
  key_links:
    - from: "search_many_test.exs"
    - to: "AllExpansion.expand/2"
    - via: "Search.run_search_many"
    - pattern: "fake backend + global_schemas override"
---

<objective>
Lock **FED-02** with **ExUnit** coverage: **`AllExpansion.expand/2`** unit cases and **`search_many/2`** integration tests for **happy expansion**, **empty registry**, **cardinality rail**, **`missing_otp_app`**, and **malformed** `{:all, ...}` — using **`global_schemas:`** in **`shared_opts`** where possible to avoid **`Application.put_env`**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-all-expansion/40-CONTEXT.md
@.planning/phases/40-all-expansion/40-RESEARCH.md
@.planning/phases/40-all-expansion/40-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `AllExpansion` unit tests</name>
  <files>test/scrypath/multi_search/all_expansion_test.exs</files>
  <read_first>
    - lib/scrypath/multi_search/all_expansion.ex
    - test/scrypath/multi_search/entries_test.exs (ExUnit style, schema modules used)
  </read_first>
  <action>
    1. Add **`test/scrypath/multi_search/all_expansion_test.exs`** with **`use ExUnit.Case, async: true`**.
    2. Cases (minimum):
       - **Happy:** entries **`[{:all, "q", federation_weight: 1.0}]`**, **`shared_opts`** **`[global_schemas: [ModA, ModB], otp_app: :irrelevant_here]`** → **`{:ok, list}`** with **two** triples, **`ModA`** first, both texts **`\"q\"`**, and **each** third-element keyword contains **`federation_weight: 1.0`** (float).
       - **Empty registry:** same but **`global_schemas: []`** → **`{:error, {:invalid_options, {:all_expansion, :empty_registry}}}}`**.
       - **Missing otp_app:** **`[{:all, \"x\"}]`**, **`shared_opts == []`** (no **`global_schemas`**, no **`otp_app`**) → **`{:error, {:invalid_options, {:all_expansion, :missing_otp_app}}}}`**.
       - **Malformed:** **`[{:all, 1}]`** or **`[{:all, \"t\", :not_a_kw_list}]`** → **`{:error, {:invalid_options, :malformed_entry}}`** (match shipped pattern for bad tuples).
    3. Use **existing test schema modules** from **`search_many_test.exs`** or **`entries_test.exs`** (e.g. modules already compiled for Scrypath tests) — **no new schema definitions** unless required for compilation.
  </action>
  <verify>
    <automated>mix test test/scrypath/multi_search/all_expansion_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/multi_search/all_expansion_test.exs` exits 0.
    - `grep -n ':all_expansion' test/scrypath/multi_search/all_expansion_test.exs` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    Expansion edge cases covered without HTTP.
  </done>
</task>

<task type="auto">
  <name>Task 2: `search_many` integration — happy + `max_schemas`</name>
  <files>test/scrypath/search_many_test.exs</files>
  <read_first>
    - test/scrypath/search_many_test.exs (`@base_opts`, `FakeBackend`, existing multi-schema tests)
    - lib/scrypath/search.ex (`search_many/2`)
  </read_first>
  <action>
    1. Add test: **`Scrypath.search_many([{:all, \"needle\"}], Keyword.merge(@base_opts, global_schemas: [SearchablePost, FacetableMovie], max_schemas: 10))`** asserts **`{:ok, %MultiSearchResult{}}`** and that **both** schemas appear in **results** (reuse patterns from existing multi-search tests).
    2. Add test: same **`global_schemas`** but **`max_schemas: 1`** → expect **`{:error, {:too_many_schemas, 2, 1}}`** (exact tuple shape per **`Entries`** today).
  </action>
  <verify>
    <automated>mix test test/scrypath/search_many_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n ':all' test/scrypath/search_many_test.exs` returns ≥ 1 line containing **`{:all,`**.
    - `mix test test/scrypath/search_many_test.exs` exits 0.
  </acceptance_criteria>
  <done>
    End-to-end expansion works with fake backend.
  </done>
</task>

<task type="auto">
  <name>Task 3: Docstring grep anchors for Phase 41</name>
  <files>lib/scrypath/search.ex</files>
  <read_first>
    - lib/scrypath/search.ex (existing `search_many/2` doc section)
  </read_first>
  <action>
    1. Ensure the **`## :all expansion`** section (from Plan 01) contains **literal** substrings **`global_schemas:`** and **`scrypath_global_search_schemas`** so future **`docs_contract_test`** can anchor (Phase 41 may add the test file).
  </action>
  <verify>
    <automated>mix test test/scrypath/search_many_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'global_schemas:' lib/scrypath/search.ex` returns ≥ 1 line in a **comment or `@doc`** block.
    - `grep -n 'scrypath_global_search_schemas' lib/scrypath/search.ex` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    Doc anchors present for downstream contract tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests → code | Tests use the same **module atom** surfaces as production; no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | Test-only | note | No new production trust boundaries in this plan. |

No HIGH-severity threats in test additions.
</threat_model>

<verification>
- `mix test test/scrypath/multi_search/all_expansion_test.exs test/scrypath/search_many_test.exs` exits 0.
- `mix compile --warnings-as-errors` exits 0.
</verification>

<success_criteria>
- FED-02: **Roadmap success (3)** — tests cover **happy** and **rejection** paths for **`:all`** expansion and rails.
</success_criteria>
