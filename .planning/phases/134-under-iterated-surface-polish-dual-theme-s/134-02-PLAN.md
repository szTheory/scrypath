---
phase: 134-under-iterated-surface-polish-dual-theme-s
plan: 02
type: execute
wave: 2
depends_on: ["134-01"]
files_modified:
  - scrypath_ops/assets/css/app.css
  - scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex
  - scrypath_ops/lib/scrypath_ops_web/live/control_room_live.ex
  - scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
  - scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs
  - examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts
autonomous: true
requirements: [SCREEN-DARK-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Dark hover-border boost applied to the PAIRED selector .ops-result-row:hover, .ops-object-item:hover in BOTH dark paths ([data-theme=dark] AND @media prefers-color-scheme: dark html:not([data-theme=light])) lifting the dark hover border from primary 32% to primary 55% (D-15/D-16); the base shared rule stays primary 32% as the light value + dark fallback; the shared rule is NOT split"
    - ".ops-result-row:hover AND .ops-object-item:hover borderColor resolve to primary 55% in dark and differ from resting on BOTH surfaces (D-12/D-15 proven by the depth spec)"
    - ".ops-copper-badge (composed class=ops-badge ops-copper-badge) is wired onto the Control Room RECOMMENDED intent-card head as a genuine key-fact callout — exactly ONE site this phase (D-01/DK-12); badge text is var(--color-base-content) not --color-secondary (D-03)"
    - "Negative: no tone_class/1 or badge_class/1 status chip computes to copper (--color-secondary) anywhere (D-02 'copper is NEVER a status tone'), proven by the depth spec negative assertion"
    - ".ops-result-row / .ops-data-card backgroundColor === rgb(27,34,48) (--ops-surface-2) AND sRGB rel-luminance exceeds the --ops-bg rgb(12,15,20) floor by >= the delta floor in dark + system-dark (elevation proof, criterion 1)"
    - ".ops-muted-panel and preflight cards proven raised by luminance-delta / relative-step (NOT exact rgb — R2: they are color-mix alpha fills); .ops-preflight__card--locked luminance > .ops-preflight__card (criterion 2)"
    - ".ops-object-item-active boxShadow contains the glow RGB 108,92,231 in dark and none in light (criterion 2, DK-18 active glow)"
    - "DK-13 objective trigger executed: the dark-390 posture-table row-border ↔ #1b2230 contrast ratio is MEASURED; if < 1.20:1 a dark-only border-color: color-mix(... base-content 18% ...) override is added (both paths, leaf-scoped to a .ops-posture-table table_class per R1); if >= 1.20:1 the ratio is recorded as the audit artifact and no edit is made"
    - "DK-11 (verdict panel), DK-14 (posture verdict hero shadow), DK-15 (failed-sync triage notice) are VERIFY-ONLY: confirmed reading raised via the inherited systemic surface-2 ramp; any real defect found is filed for Phase 135, NOT patched locally (D-05/D-06/D-07)"
  artifacts:
    - scrypath_ops/assets/css/app.css
    - scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex
    - scrypath_ops/lib/scrypath_ops_web/live/control_room_live.ex
    - examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts
  key_links:
    - "hover boost rides the EXISTING border-color var(--duration-ops-fast) transition on the paired selector — no new motion token"
    - ".ops-copper-badge is already defined (app.css ~499-503) and AA pre-cleared — this plan WIRES it onto markup, it does not redefine it"
    - "DK-13 override (if it fires) is leaf-scoped via the .ops-posture-table table_class on <.ops_table> so it cannot leak to other daisyUI tables (R1)"
  prohibitions:
    - "Do NOT add any new token, component, JS hook, or keyframe (hard phase invariant). Compose existing .ops-copper-badge; do NOT define a new --hover-border var; do NOT add a new keyframe"
    - "Do NOT split the shared .ops-result-row:hover, .ops-object-item:hover rule (re-introduces DK-17 faintness on Playbooks — D-15)"
    - "Do NOT change the light theme in any way (light hover stays primary 32%; light posture border stays 12%; copper badge is theme-agnostic). Light pixel-diff threshold 0 must stay green (verified in Plan 03)"
    - "Do NOT apply copper to a second site this phase — HOLD the Sync/Drift preflight copper (D-02); copper goes on exactly the recommended intent-card"
    - "Do NOT edit the systemic DK-11/DK-14/DK-15 tokens (.ops-verdict-neutral, --shadow-ops-raised, .ops-notice-surface) — they are verify-only; real gaps route to Phase 135 (D-05/06/07/09)"
    - "Do NOT assert exact rgb(27,34,48) on alpha-mixed surfaces (.ops-muted-panel, preflight cards) — use luminance-delta/relative-step (R2)"
---

<objective>
Apply the already-shipped design system to the three under-iterated dark surfaces and execute the cross-screen tune, then fill the depth spec's computed-style assertions so the binding gate proves criteria 1–3. This is the application core of SCREEN-DARK-01.

Three application moves, all leaf edits authored in BOTH dark paths with light untouched:
- DK-17 hover-border boost to primary 55% on the paired Search-row + Playbook-object-item selector (D-15/D-16).
- DK-12 earned copper: wire .ops-copper-badge onto the Control Room recommended intent-card head, exactly one site (D-01); HOLD the second copper site (D-02).
- DK-13 conditional posture-table row-border tune behind an objective measured 1.20:1 trigger (D-08, R1 — scope via a leaf table_class, since no .ops table-row selector exists).

Plus a verify-only sweep of DK-11/DK-14/DK-15 (no local edits; real gaps route to Phase 135) and the depth-spec assertions that pin all of the above deterministically.

Purpose: This is where the dark surfaces actually gain depth and the earned-copper moment lands.
Output: app.css hover/conditional-DK-13 overrides, copper-badge markup wiring, filled depth-spec assertions, appended token tripwire.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase contract + research + patterns (read before implementing — D-01→D-18 are LOCKED)
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-UI-SPEC.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-RESEARCH.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-PATTERNS.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-VALIDATION.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-CONTEXT.md

# Source files to edit + analogs
@scrypath_ops/assets/css/app.css
@scrypath_ops/assets/css/DESIGN-TOKENS.md
@scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex
@scrypath_ops/lib/scrypath_ops_web/live/control_room_live.ex
@scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
@examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts
@examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts
</context>

<artifacts_this_phase_produces>
This plan creates/modifies:
- the dark-only hover-border boost override selector (`.ops-result-row:hover, .ops-object-item:hover` at primary 55%, both dark paths) in `app.css`
- the `.ops-copper-badge` markup wiring on the Control Room recommended intent-card (and, if chosen, an optional badge slot on `ops_intent_card`)
- (conditionally) the DK-13 dark-only `.ops-posture-table` row-border override + the `table_class` on `<.ops_table>` in `posture_live.ex`
- the filled computed-style depth assertions in `admin_surface_depth.spec.ts`
- the appended `primary 55%` value-assertion in `surface_depth_token_contract_test.exs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Dark hover-border boost (D-15/D-16/DK-17) + append the primary-55% token tripwire assertion</name>
  <files>scrypath_ops/assets/css/app.css, scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs</files>
  <read_first>
    scrypath_ops/assets/css/app.css (the shared base hover rule .ops-result-row:hover, .ops-object-item:hover at ~979-983 — confirm it is border-color color-mix(... primary 32% ...) + box-shadow var(--shadow-ops-mid); the canonical dual-dark-path override idiom at .ops-result-row dark fill ~1433-1441),
    scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs (the file Plan 01 created — append the dark-hover value assertion here),
    .planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-PATTERNS.md (the exact target CSS shape at "Hover-border boost (D-15/D-16/DK-17)")
  </read_first>
  <action>
    In app.css, leave the shared base rule `.ops-result-row:hover, .ops-object-item:hover` UNCHANGED at border-color color-mix(in oklch, var(--color-primary) 32%, transparent) + box-shadow var(--shadow-ops-mid) — this is the light value and the dark fallback. Do NOT split this rule (splitting re-introduces DK-17 faintness on Playbooks — D-15). Add a NEW dark-only override on the SAME paired selector, authored in BOTH dark paths (the [data-theme="dark"] selector path AND the @media (prefers-color-scheme: dark) html:not([data-theme="light"]) path), lifting only border-color to color-mix(in oklch, var(--color-primary) 55%, transparent). Do NOT introduce a --hover-border var (no new tokens). The boost rides the existing border-color var(--duration-ops-fast) transition already on the rows — no new motion.

    Keep hover and active on DIFFERENT axes (D-17): hover = border 55% + var(--shadow-ops-mid) (neutral, NO glow); the active item (.ops-object-item-active) keeps its inset ring + violet --shadow-ops-glow. This makes rest < hover < active unambiguous and avoids any box-shadow conflict — the active item is the only one glowing.

    Then append two assertions to surface_depth_token_contract_test.exs (the file Plan 01 created): (1) the dark-only paired-selector override resolves to var(--color-primary) at 55% (regex the literal `--color-primary)\s*55%` over css()); (2) the base shared rule still references primary at the lower base alpha (proves the rule was extended, not replaced). Add a descriptive failure message to each.
  </action>
  <verify>
    <automated>cd scrypath_ops && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test test/scrypath_ops_web/surface_depth_token_contract_test.exs 2>&1 | tail -10</automated>
  </verify>
  <done>The dark-only primary-55% hover override exists on the paired .ops-result-row:hover, .ops-object-item:hover selector in BOTH dark paths; the base shared rule is unchanged at primary 32% and is NOT split; the token tripwire passes including the new primary-55% + base-rule assertions; mix compile is clean.</done>
  <acceptance_criteria>
    - Shared base hover rule unchanged (primary 32%, var(--shadow-ops-mid)); not split.
    - Dark-only override added in BOTH dark paths on the paired selector at primary 55%, border-color only.
    - No --hover-border var, no new token/keyframe added.
    - surface_depth_token_contract_test.exs gains a passing primary-55% assertion + a base-rule assertion.
    - mix compile --warnings-as-errors clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire the earned copper badge onto the Control Room recommended intent-card (D-01/DK-12), holding the second site (D-02)</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex, scrypath_ops/lib/scrypath_ops_web/live/control_room_live.ex</files>
  <read_first>
    scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex (the ops_badge/1 class-list merge idiom at ~329-335; ops_intent_card/1 at ~496-516 — note it currently has flag/icon/title/summary/cta and NO badge slot; tone_class/1 and badge_class/1 — confirm copper is NOT in their mappings),
    scrypath_ops/lib/scrypath_ops_web/live/control_room_live.ex (the recommended intent card at ~92-101 — recommended={@posture.state in [:degraded, :missing_backend]}, data-testid="intent-incident"),
    scrypath_ops/assets/css/app.css (.ops-copper-badge at ~499-503 — already defined, AA pre-cleared, text var(--color-base-content); do NOT redefine),
    scrypath_ops/assets/css/DESIGN-TOKENS.md (copper AA pre-clearance lines ~175-180; the "copper is never a status tone" governing law)
  </read_first>
  <action>
    Wire .ops-copper-badge onto the Control Room RECOMMENDED intent-card head as the single earned-copper site this phase (D-01/DK-12). Compose the class list `class="ops-badge ops-copper-badge"` on a key-callout / federation badge inside the recommended card head — this layers on top of the existing violet ring+seat+glow recommended treatment. Badge text is the already-baked var(--color-base-content) (D-03) — do NOT pass --color-secondary as label text. .ops-copper-badge is theme-agnostic (consumes --color-secondary, which remaps light #a85d2e / dark #c17a3e) and AA pre-cleared (dark 12.07:1 / light 14.86:1) — so per D-04 no hand-authored dual-path block is needed for the badge itself, and do NOT redefine the class.

    Implementation choice (Claude's Discretion per D / Open Q3): either (a) add an optional `badge` slot to ops_intent_card/1 rendered in the card head, or (b) compose the `ops-badge ops-copper-badge` span directly in control_room_live.ex near the recommended card. If you add a slot, it is a markup affordance on an EXISTING component, not a new component/primitive — keep it minimal and render nothing when the slot is empty so non-recommended cards are unchanged. Apply the badge ONLY to the recommended card (data-testid="intent-incident" path), never to the non-recommended cards.

    Badge content (Claude's Discretion per D-08 discretion note): a genuine key fact on the scan path — e.g. "Federated" or a key-scope callout — not a decorative label.

    HOLD the optional Sync/Drift preflight copper key-callout (D-02) — do NOT add a second copper site this phase. Confirm (read-only) that tone_class/1 and badge_class/1 do NOT map any status to copper/--color-secondary; if they did, that would violate D-02, but do not change their mappings (the negative is guarded by the e2e spec in Task 4).
  </action>
  <verify>
    <automated>cd scrypath_ops && mix compile --warnings-as-errors 2>&1 | tail -5 && mix test 2>&1 | tail -8</automated>
  </verify>
  <done>The recommended Control Room intent-card renders an ops-badge ops-copper-badge key-callout (exactly one copper site); badge text uses var(--color-base-content); non-recommended cards are unchanged; no second copper site added; tone_class/1 and badge_class/1 mappings remain copper-free; ScrypathOps LiveView suite green.</done>
  <acceptance_criteria>
    - Copper badge composed as class="ops-badge ops-copper-badge" on the recommended card head only.
    - .ops-copper-badge NOT redefined; no dual-path CSS added for it; text is base-content.
    - Exactly one copper site this phase; preflight copper HELD (D-02).
    - tone_class/1 / badge_class/1 mappings unchanged and copper-free.
    - If a badge slot was added to ops_intent_card, it renders nothing when empty (non-recommended cards unchanged) — no new component/primitive.
    - mix compile clean; LiveView suite green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: DK-13 objective-trigger posture-table row-border tune (D-08, R1) — measure first, conditional leaf edit</name>
  <files>scrypath_ops/assets/css/app.css, scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex</files>
  <read_first>
    scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex (the <.ops_table zebra> usage at ~234 for the per-schema posture table — this renders daisyUI table table-sm table-zebra; ops_table already accepts @table_class),
    scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex (ops_table at ~135-138 — confirm @table_class is threaded into the <table class=...> so a leaf scope class works),
    scrypath_ops/assets/css/app.css (the dual-dark-path idiom to clone at ~1433-1441; --border ~line 54; confirm there is NO existing .ops-* table-row border selector — R1),
    .planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-PATTERNS.md (the "DK-13 table-row border (D-08, CONDITIONAL)" target CSS shape),
    .planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-RESEARCH.md (R1 — the posture table has NO hand-authored row-border selector; brand §6.5 dark border target #2A3446)
  </read_first>
  <action>
    Execute the DK-13 OBJECTIVE trigger (D-08) — do NOT make a subjective read. R1: there is NO existing .ops-* table-row-border selector (the posture per-schema table is daisyUI `table table-sm table-zebra` from <.ops_table zebra>); row separators come from daisyUI's internal border driven by --border + base-300. So the measurement target is the daisyUI default row border ↔ the #1b2230 surface-2 row fill, at the dark 390px posture-incident state.

    Step A (measure): In the depth spec (admin_surface_depth.spec.ts), at the sync/posture seed (incident, dark, 390) read the computed row-border color and the row background, compute their contrast ratio, and record it. This measured ratio is the audit artifact and the trigger input.

    Step B (conditional edit): IF the measured ratio < 1.20:1 → add a dark-only override, authored in BOTH dark paths, scoped to a NEW leaf table_class so it cannot leak to other daisyUI tables (R1): pass `table_class="ops-posture-table"` to the <.ops_table> in posture_live.ex, and add `[data-theme="dark"] .ops-posture-table :where(td, th) { border-color: color-mix(in oklch, var(--color-base-content) 18%, transparent); }` plus the matching @media (prefers-color-scheme: dark) html:not([data-theme="light"]) path. Light row border stays at the inherited 12% (pixel-identical — do NOT touch the light path). IF the measured ratio >= 1.20:1 → make NO CSS edit; record the ratio in the SUMMARY as the DK-13 audit artifact and leave the table as-is.

    Either branch is a valid, passing outcome — the gate is that the trigger was MEASURED and acted on objectively, not that an edit was made. DK-13 is the ONLY in-pass-eligible cross-screen tune precisely because it is a leaf (one dark-only alpha on the posture table), not a systemic token edit (D-09 blast-radius rationale) — same edit-shape as the greenlit hover boost. Do not hardcode a raw hex; if the edit fires, use the base-content 18% alpha route (approximates brand §6.5 #2A3446 slate). The 1.20:1 / 18% values are starting points (Claude's Discretion) — tune to the smallest reliable non-flaking value, but keep the trigger an objective measured number.
  </action>
  <verify>
    <automated>cd scrypath_ops && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <done>The DK-13 trigger is measured in the depth spec (row-border ↔ #1b2230 contrast at dark 390); if < 1.20:1 a dark-only leaf-scoped .ops-posture-table border override (base-content 18%, both paths) is added behind a table_class with light untouched; if >= 1.20:1 no edit is made and the ratio is recorded; mix compile clean.</done>
  <acceptance_criteria>
    - The measured ratio is computed in the spec (objective trigger), not a subjective read.
    - If edited: dark-only, both paths, leaf-scoped via .ops-posture-table table_class (no leak to other tables — R1); light unchanged.
    - If not edited: ratio recorded as the audit artifact; no CSS change.
    - No new token; base-content alpha route only if edited.
    - mix compile clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Fill the depth-spec computed-style assertions (criteria 1–3) + verify-only DK-11/14/15 sweep</name>
  <files>examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts</files>
  <read_first>
    examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts (the skeleton + seed grid Plan 01 built),
    examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts (the GLOW_RGB="108, 92, 231" glowBoxShadow probe at ~202-216 and the hover read at ~318-327),
    .planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-PATTERNS.md ("Assertions to author (from D-11; R2 corrections applied)"),
    .planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-VALIDATION.md (Requirements → Test Map binding assertions),
    .planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-CONTEXT.md (D-05/D-06/D-07 verify-only rationale for DK-11/14/15)
  </read_first>
  <action>
    Fill the depth-spec's computed-style assertions so the binding gate proves criteria 1–3, using the getComputedStyle probe generalized to .backgroundColor / .borderColor / .boxShadow. Apply the R2 correction throughout: exact-rgb ONLY on flat-token surfaces; luminance-delta/relative on alpha-mixed surfaces.

    Criterion 1 (DK-17 + hover D-15): .ops-result-row and .ops-data-card backgroundColor === rgb(27,34,48) AND sRGB rel-luminance exceeds the --ops-bg rgb(12,15,20) floor by >= the delta floor (start 0.015, tunable to smallest non-flaking value) in dark + system-dark. Read .ops-result-row:hover AND .ops-object-item:hover borderColor at rest and after .hover(); assert both resolve to primary 55% in dark and DIFFER from resting (proves D-15 on BOTH surfaces).

    Criterion 2 (DK-16 + DK-18): .ops-muted-panel and the preflight cards proven by luminance-delta/relative-step ONLY (R2 — they are color-mix alpha fills; do NOT assert exact rgb); .ops-preflight__card--locked luminance > .ops-preflight__card. .ops-data-card raised proof on Playbooks; .ops-object-item-active boxShadow contains GLOW_RGB "108, 92, 231" in dark and === none in light (use the cloned glow probe). Ensure these run against the populated-playbooks seed Plan 01 created (R4) so .ops-object-item-active actually exists.

    Criterion 3 (D-01 copper + D-02 negative): .ops-copper-badge on the Control Room recommended card resolves to the copper --color-secondary tint (proves D-01 applied). NEGATIVE assertion: enumerate the visible tone_class/badge_class status chips and assert NONE computes to the copper --color-secondary (proves D-02 "never a status tone"). Use the light NEGATIVE assertions where applicable (glow === none in light).

    Verify-only sweep (D-05/D-06/D-07 — DK-11/DK-14/DK-15): add light read-only checks or spot assertions confirming .ops-verdict-neutral (DK-11) and the failed-sync notice (DK-15) read raised via the inherited surface-2 ramp, and that the posture verdict hero (DK-14) shadow is the cool dark recipe (no warm halo). Do NOT add any local CSS fix for these — if an assertion reveals a real coplanar/halo defect, the executor records it for Phase 135 (do NOT patch .ops-verdict-neutral / --shadow-ops-raised / .ops-notice-surface locally). The "earned copper feel" and warm-halo aesthetic reads stay human-spot-review per D-13/D-06 (not thresholded here).

    All assertions run the {explicit-dark, system-dark} × {390, 1440} grid with assertSystemDarkInvariants first on system-dark.
  </action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && npx tsc --noEmit -p . 2>&1 | head -20</automated>
    <human-check>Run `npm run test:e2e:admin-depth` against a booted, seeded ops server with ops assets built first (D-14: rebuild assets + restart mix phx.server — the ecommerce dev server does NOT live-reload the scrypath_ops path-dep). All criterion 1–3 assertions green in dark + system-dark; copper negative assertion passes. (This is the wave-merge run; per-task tsc is the fast gate.)</human-check>
  </verify>
  <done>The depth spec asserts criteria 1–3 deterministically: surface-2 elevation (exact-rgb on flat tokens + luminance-delta over the --ops-bg floor), hover primary 55% on BOTH surfaces differing from resting, locked preflight card above base, active-item glow 108,92,231 in dark / none in light, copper tint on the recommended badge, and the D-02 negative (no status chip computes to copper). DK-11/14/15 are verify-only with any real defect routed to Phase 135. Spec type-checks.</done>
  <acceptance_criteria>
    - Exact-rgb only on flat-token surfaces; luminance-delta/relative on alpha-mixed (R2).
    - Hover primary-55% proven on BOTH .ops-result-row and .ops-object-item, differing from resting.
    - Active-item glow 108,92,231 dark / none light; runs against a real populated playbook (R4).
    - Copper positive (recommended badge) + D-02 negative (no status chip computes to copper).
    - DK-11/14/15 verify-only; no local systemic-token edits; defects routed to Phase 135.
    - Grid {explicit-dark, system-dark} × {390,1440}; assertSystemDarkInvariants first on system-dark.
    - tsc --noEmit clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → local ops server | The depth spec drives a locally booted, seeded `scrypath_ops` server; it must target the trusted local server, never a remote host. |
| operator → admin UI (existing) | No change this phase — Phase 134 adds no new endpoint, input, or auth surface; the boundary is unchanged. |

## STRIDE Threat Register

Phase 134 is a CSS/visual + markup-wiring + e2e-test application pass. It introduces **no new endpoints, no auth surface, no data handling, no user input, and no new dependencies — no new attack surface.** Only the low-severity considerations below apply; manufacturing further threats for a styling pass would be noise.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-03 | Tampering | admin_surface_depth.spec.ts target host | low | mitigate | Spec runs against the locally booted/seeded ops server (D-14); base URL is the existing helpers/e2e local target, never an arbitrary remote. |
| T-134-04 | Information Disclosure | copper badge content | low | accept | Badge content is a synthetic key-fact label (e.g. "Federated"); no secret, credential, or PII is rendered. |
| T-134-05 | Tampering | leaf-scoped DK-13 CSS override | low | mitigate | If the DK-13 edit fires it is scoped to a dedicated .ops-posture-table table_class so it cannot leak to unrelated tables (R1); light path untouched (pixel-diff 0 guard in Plan 03). |
</threat_model>

<verification>
- `cd scrypath_ops && mix compile --warnings-as-errors` — clean after CSS + markup edits.
- `cd scrypath_ops && mix test test/scrypath_ops_web/surface_depth_token_contract_test.exs` — token tripwire green (incl. primary-55% + base-rule assertions).
- `cd scrypath_ops && mix test` — ScrypathOps LiveView suite green.
- `cd examples/scrypath_ecommerce && npx tsc --noEmit -p .` — depth spec type-clean.
- Wave-merge: `npm run test:e2e:admin-depth` against a booted, seeded, assets-built ops server (D-14) — criteria 1–3 green dark + system-dark; copper negative passes.
- DK-13 trigger measured (ratio recorded); DK-11/14/15 verify-only (no local systemic edits; defects → Phase 135).
</verification>

<success_criteria>
- Hover border 55% on the paired selector in both dark paths (rule not split); light unchanged.
- Exactly one earned copper site (recommended intent-card badge); preflight copper held; no status chip computes to copper.
- DK-13 objective trigger measured and acted on (conditional leaf edit or recorded ratio); light pixel-identical.
- DK-11/14/15 verify-only; real defects routed to Phase 135.
- Depth spec proves criteria 1–3 deterministically; no new token/component/hook/keyframe added.
</success_criteria>

<output>
Create `.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-02-SUMMARY.md` when done (record the DK-13 measured ratio + whether the edit fired, and any DK-11/14/15 defect filed for Phase 135).
</output>
