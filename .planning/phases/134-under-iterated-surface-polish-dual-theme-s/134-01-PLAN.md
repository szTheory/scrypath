---
phase: 134-under-iterated-surface-polish-dual-theme-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts
  - examples/scrypath_ecommerce/package.json
  - scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs
autonomous: true
requirements: [SCREEN-DARK-01]
nyquist_compliant: true

must_haves:
  truths:
    - "examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts exists, imports THEME_MODES / assertSystemDarkInvariants / VIEWPORTS / seed+nav helpers from admin_contrast_matrix.spec.ts and the GLOW_RGB computed-style probe idiom from admin_path_motion.spec.ts — it does NOT re-author them (D-10)"
    - "The depth spec runs the grid {explicit-dark, system-dark} × {mobile 390, desktop 1440} and calls assertSystemDarkInvariants(page) first on every system-dark run (D-10/D-08)"
    - "The depth spec's populated-Playbooks prepare seeds/creates a real playbook before asserting populated-state depth (R4) — .ops-object-item / .ops-object-item-active is present, not an empty screen"
    - "npm script test:e2e:admin-depth resolves to `playwright test e2e/admin_surface_depth.spec.ts` in examples/scrypath_ecommerce/package.json (D-10)"
    - "scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs is a NEW value-assertion ExUnit test (sibling of motion_contract_test.exs, NOT an extension of design_tokens_contract_test.exs) that reads app.css and regex-asserts --ops-surface-2 stays #1b2230 and raised-surface recipes reference the token (R3/D-13); it runs under mix verify.opsui"
  artifacts:
    - examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts
    - examples/scrypath_ecommerce/package.json
    - scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs
  key_links:
    - "depth spec imports (not copies) helpers/e2e + THEME_MODES + assertSystemDarkInvariants + seed/index map from admin_contrast_matrix.spec.ts — drift between the two is the failure mode"
    - "token tripwire runs under mix verify.opsui because it lives under scrypath_ops/test/ (the task is a mix test wrapper, R3)"
  prohibitions:
    - "Do NOT add any new token, component, JS hook, or keyframe — this plan adds only test + script files (NO app.css / ops_ui.ex edits in this plan)"
    - "Do NOT extend design_tokens_contract_test.exs (the orphan-checker) — the tripwire is a NEW sibling of motion_contract_test.exs (R3)"
    - "Do NOT re-author THEME_MODES / assertSystemDarkInvariants / VIEWPORTS / seed map — import them from admin_contrast_matrix.spec.ts (D-10, Don't-Hand-Roll)"
---

<objective>
Build the Wave 0 verification harness for Phase 134 (SCREEN-DARK-01) before any CSS/markup is applied: the new Playwright computed-style depth spec, its npm script, the populated-Playbooks seed fix, and the new ExUnit token-value tripwire. These artifacts are the binding gate that converts subjective dark-depth reads into CI-failing numbers (owner's 0-human-UAT preference) — every downstream application task in Plan 02 verifies against this spec.

This plan honors the hard phase invariants: it adds NO new tokens, components, JS hooks, or keyframes. It writes only test + script files; it imports (does not re-author) the existing theme grid + seed helpers per D-10.

Purpose: Wave 0 must exist before Plan 02 so the application tasks have a deterministic gate to run against.
Output: admin_surface_depth.spec.ts, test:e2e:admin-depth npm script, surface_depth_token_contract_test.exs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase contract + research (read before implementing)
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-UI-SPEC.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-RESEARCH.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-PATTERNS.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-VALIDATION.md

# Harness analogs to clone (do NOT re-author the shared helpers — import them)
@examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts
@examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts
@examples/scrypath_ecommerce/e2e/helpers/e2e.ts
@examples/scrypath_ecommerce/package.json
@scrypath_ops/test/scrypath_ops_web/motion_contract_test.exs
@scrypath_ops/assets/css/app.css
</context>

<artifacts_this_phase_produces>
This plan creates:
- `examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts` — the binding computed-style depth gate
- the `test:e2e:admin-depth` npm script in `examples/scrypath_ecommerce/package.json`
- `scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs` — the new ExUnit token-value tripwire
- the populated-Playbooks seed step inside the depth spec's prepare (R4)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the admin_surface_depth.spec.ts binding gate (skeleton + imports + theme grid + seed map + populated seed fix)</name>
  <files>examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts, examples/scrypath_ecommerce/package.json</files>
  <read_first>
    examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts (clone the newThemedPage builder, the GLOW_RGB constant, and the getComputedStyle probe shape — PATTERNS.md anchors :70-92 and :202-216; the hover read at :318-327),
    examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts (import THEME_MODES :44-65, VIEWPORTS :63-68, assertSystemDarkInvariants :123-133, the goto*/runSearch nav helpers :446-491, and the seed/index map :497-598),
    examples/scrypath_ecommerce/e2e/helpers/e2e.ts (seedScenario, waitForLiveConnected, waitForSearchVisible, drainSearchQueue, SeedScenario type),
    examples/scrypath_ecommerce/package.json (existing test:e2e:* script shape, lines 9-12)
  </read_first>
  <action>
    Create examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts by cloning the structure of admin_path_motion.spec.ts. IMPORT the shared theme/seed infrastructure from admin_contrast_matrix.spec.ts and helpers/e2e (do NOT re-author per D-10 / Don't-Hand-Roll): THEME_MODES (the explicit-light | explicit-dark | system-dark discriminated union), VIEWPORTS (mobile 390 / desktop 1440), assertSystemDarkInvariants, the goto*/runSearch nav helpers, and the seed/index map. If a symbol is not currently exported from admin_contrast_matrix.spec.ts, lift it into a small shared module that BOTH specs import, or re-export it — never copy-paste a divergent second copy (drift is the documented failure mode).

    Filter the run grid to {explicit-dark, system-dark} × {390, 1440} — light depth is covered by the pixel-diff gate in Plan 03, so the depth spec asserts dark behavior plus the light NEGATIVE assertions only (glow === none in light). Clone the newThemedPage builder (addInitScript phx:theme for explicit themes; colorScheme:'dark' with NO key for system-dark). On every system-dark test, call assertSystemDarkInvariants(page) FIRST so the @media branch is proven, not the explicit data-theme path.

    Wire the D-12 seed matrix — Search {results, zero-results} × Sync-Drift {drift} × Playbooks {empty-workspace, populated} — using seedScenario + the goto*/runSearch prepares from the contrast spec. For the populated Playbooks case (R4): the contrast spec's index-12 prepare creates NO playbook, so add an explicit seed/create step in THIS spec's prepare (capture a probe via the Search→Playbooks loop, or seed a playbook row) and assert .ops-object-item is present before any populated-depth assertion — otherwise populated tests silently pass against an empty screen. Leave the per-surface computed-style assertions as TODO-marked placeholders that Task 1 wires structurally; the depth assertions themselves are filled by Plan 02's tasks once the CSS lands. This task's deliverable is a runnable spec skeleton with real theme-grid + seeding + populated-seed fix; it may have zero hard depth assertions yet (Plan 02 adds them).

    In examples/scrypath_ecommerce/package.json, add the npm script line `"test:e2e:admin-depth": "playwright test e2e/admin_surface_depth.spec.ts"` matching the existing test:e2e:* shape at lines 9-12. Do not reorder or rename existing scripts.

    Do NOT edit app.css, ops_ui.ex, or any LiveView in this plan — this is harness-only Wave 0.
  </read_first>
  <action_note>The action above is directive prose; identifiers (THEME_MODES, VIEWPORTS, assertSystemDarkInvariants, GLOW_RGB, seedScenario) and the npm-script line are named, not implemented inline.</action_note>
  <verify>
    <automated>cd examples/scrypath_ecommerce && npx tsc --noEmit -p . 2>&1 | head -20; node -e "const p=require('./package.json'); if(!p.scripts['test:e2e:admin-depth']) {console.error('MISSING test:e2e:admin-depth'); process.exit(1)}; console.log('script OK:', p.scripts['test:e2e:admin-depth'])"</automated>
  </verify>
  <done>admin_surface_depth.spec.ts exists, type-checks, imports (does not re-author) THEME_MODES/assertSystemDarkInvariants/VIEWPORTS/seed map, runs the {explicit-dark, system-dark} × {390,1440} grid with assertSystemDarkInvariants called first on system-dark, seeds a real playbook for the populated case (R4), and the test:e2e:admin-depth npm script resolves to the new spec.</done>
  <acceptance_criteria>
    - Spec file present and type-clean (tsc --noEmit passes for the e2e project).
    - No second copy of THEME_MODES / assertSystemDarkInvariants — they are imported/shared.
    - Populated-Playbooks prepare creates a playbook (R4) before asserting object-item presence.
    - test:e2e:admin-depth npm script added; existing scripts untouched.
    - No edits to app.css / ops_ui.ex / LiveView files in this task.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author the surface_depth_token_contract_test.exs token-value tripwire (R3)</name>
  <files>scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs</files>
  <read_first>
    scrypath_ops/test/scrypath_ops_web/motion_contract_test.exs (clone the module + @app_css Path.expand + `defp css, do: File.read!` scaffold at :26-50 and the Regex.match? value-assertion shape at :60-77 — PATTERNS.md anchors),
    scrypath_ops/assets/css/app.css (confirm the literals to pin: --ops-surface-2 dark #1b2230 at line ~59; the raised-surface recipes that reference var(--ops-surface-2): .ops-data-card dark fill ~1424-1431, .ops-result-row dark fill ~1433-1441)
  </read_first>
  <action>
    Create scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs as a NEW value-assertion ExUnit test, modeled on motion_contract_test.exs — NOT an extension of design_tokens_contract_test.exs (that file is an orphan-checker asserting tokens RESOLVE, not their VALUES, per R3). Use the same scaffold: `use ExUnit.Case, async: true`, an `@app_css` module attribute pointing at `../../assets/css/app.css` via Path.expand, and a `defp css, do: File.read!(@app_css)` helper.

    Add value-regex assertions (each its own `test` with a descriptive failure message), pinning per D-13:
    1. `--ops-surface-2` stays `#1b2230` (the dark surface-2 elevation token) — regex the literal declaration.
    2. The raised-surface recipes reference the token, not a hardcoded hex: assert `.ops-data-card` and `.ops-result-row` dark fills resolve to `var(--ops-surface-2)` (regex `background:\s*var\(--ops-surface-2\)` appears for both).

    Do NOT add the dark-hover `primary 55%` assertion in THIS plan — that selector does not exist until Plan 02 wires the hover boost. Plan 02 (Task that adds the hover override) is responsible for adding the `primary 55%` tripwire assertion to this same file once the CSS lands. Keep the surface-2 assertions here so the tripwire is green immediately (the resting fills already exist in app.css). Note in a code comment which assertions Plan 02 will append (reference D-15/D-16 by concept, do not write the literal `primary 55%` string anywhere a negative-grep gate would later read it as a violation).
  </action>
  <verify>
    <automated>cd scrypath_ops && mix test test/scrypath_ops_web/surface_depth_token_contract_test.exs 2>&1 | tail -15</automated>
  </verify>
  <done>surface_depth_token_contract_test.exs exists as a sibling of motion_contract_test.exs, reads app.css, and passes: --ops-surface-2 is pinned to #1b2230 and the .ops-data-card / .ops-result-row dark fills reference var(--ops-surface-2). It runs green under `mix test` (and therefore under mix verify.opsui).</done>
  <acceptance_criteria>
    - New file is a sibling of motion_contract_test.exs, NOT a change to design_tokens_contract_test.exs (R3).
    - --ops-surface-2: #1b2230 value-assertion present and passing.
    - Raised-surface recipes (.ops-data-card / .ops-result-row dark) assert var(--ops-surface-2) reference.
    - Test is green now (no dependency on Plan 02 CSS); Plan 02's primary-55% assertion is documented as a follow-up append.
    - mix test for this file passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → local ops server | The Playwright depth spec drives a locally booted, seeded `scrypath_ops` server. Tests must target the trusted local server, never an arbitrary remote host. |

## STRIDE Threat Register

Phase 134 is a CSS/visual + e2e-test application pass. It adds NO new endpoints, no auth surface, no data handling, no user input, and no new dependencies — **no new attack surface is introduced.** Only the low-severity considerations below apply.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-01 | Tampering | admin_surface_depth.spec.ts target host | low | mitigate | Spec runs against the locally booted/seeded ops server (D-14) via the existing helpers/e2e base URL; no remote/arbitrary target is configured. |
| T-134-02 | Information Disclosure | test fixtures / seed data | low | accept | Seed scenarios use synthetic catalog fixtures only; no secrets or credentials are embedded in the spec or seed steps. |
</threat_model>

<verification>
- `cd examples/scrypath_ecommerce && npx tsc --noEmit -p .` — depth spec type-checks.
- `node -e` package.json check — `test:e2e:admin-depth` script present.
- `cd scrypath_ops && mix test test/scrypath_ops_web/surface_depth_token_contract_test.exs` — token tripwire green.
- No edits to app.css / ops_ui.ex / LiveView in this plan (grep the diff to confirm Wave 0 is harness-only).
</verification>

<success_criteria>
- admin_surface_depth.spec.ts exists, type-clean, imports shared helpers (no re-authoring), runs {explicit-dark, system-dark} × {390,1440}, seeds a real populated playbook (R4).
- test:e2e:admin-depth npm script wired.
- surface_depth_token_contract_test.exs is a new sibling of motion_contract_test.exs, green, pinning --ops-surface-2 #1b2230 + raised-recipe token references.
- No design-system primitives added; harness-only.
</success_criteria>

<output>
Create `.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-01-SUMMARY.md` when done.
</output>
