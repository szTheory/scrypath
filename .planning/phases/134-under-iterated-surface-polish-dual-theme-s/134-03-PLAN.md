---
phase: 134-under-iterated-surface-polish-dual-theme-s
plan: 03
type: execute
wave: 3
depends_on: ["134-02"]
files_modified:
  - .planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-03-SUMMARY.md
autonomous: false
requirements: [SCREEN-DARK-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Light theme is pixel-identical: `node e2e/light-pixel-diff.mjs` passes at threshold 0 after all Phase 134 edits (D-13) — no light regression from the hover boost, copper wiring, or conditional DK-13 edit"
    - "AA gate is green in light, dark, AND system-dark: `make contrast` + `npm run test:e2e:admin-contrast` report 0 AA color-contrast failures (UI-SPEC hard gate inherited from Phase 132); AAA-body advisory only"
    - "`mix verify.opsui` is green (static .ops-* CSS contract + MotionContractTest + the new surface_depth_token_contract_test.exs + light pixel-diff)"
    - "The full depth gate `npm run test:e2e:admin-depth` passes against a booted, seeded ops server with ops assets built first (D-14) across {explicit-dark, system-dark} × {390,1440}"
    - "The 40-shot human-spot-review confirms the three target surfaces read distinctly raised in dark across all seed scenarios and the earned-copper moment + warm-halo-absence read correctly (the two human-only aesthetic checks per D-13/D-06)"
  artifacts:
    - .planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-03-SUMMARY.md
  key_links:
    - "all browser-driven gates MUST run against a booted, seeded ops server with ops assets built FIRST (D-14) — the ecommerce dev server does NOT live-reload the scrypath_ops path-dep; a stale baked image silently tests old CSS"
    - "light-pixel-diff threshold 0 is the SOLE light-parity gate — do NOT duplicate light depth checks (D-13)"
  prohibitions:
    - "Do NOT add or change any token/component/hook/keyframe or any CSS in this plan — it is verification-only"
    - "Do NOT relax the AA gate or the light pixel-diff threshold to make the phase pass — a light regression or new AA failure FAILS the phase (route real systemic defects to Phase 135 per D-05/06/07, do not patch locally)"
    - "Do NOT run the gates against the stale baked :4002 image — build ops assets + restart mix phx.server first (D-14)"
---

<objective>
Close Phase 134 by proving the two hard invariants the application pass must not have broken: light stays pixel-identical (threshold 0) and the AA gate is green in all three themes — plus the full depth gate and the two human-only aesthetic spot-reviews. This is the milestone-shaped verification stop for SCREEN-DARK-01 (the full 40-shot re-capture + before/after gallery as the milestone GATE is Phase 136, not here).

Purpose: A dual-theme polish pass is only correct if light didn't move and AA didn't regress; this plan is the gate that proves it.
Output: green light pixel-diff (0), green AA in 3 themes, green mix verify.opsui + depth spec, recorded human spot-review, 134-03-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-UI-SPEC.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-VALIDATION.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-RESEARCH.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-CONTEXT.md

@examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs
@examples/scrypath_ecommerce/package.json
</context>

<artifacts_this_phase_produces>
This plan produces the verification evidence record only (134-03-SUMMARY.md). It adds NO code/CSS — it runs the inherited gates and records results, and routes any real systemic defect (DK-11/14/15) to Phase 135.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the light-parity + AA + static + depth gates against a booted, seeded, assets-built ops server (D-14)</name>
  <files>.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-03-SUMMARY.md</files>
  <read_first>
    .planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-VALIDATION.md (Sampling Rate "Before /gsd-verify-work: full suite green"; the hard D-14 precondition),
    .planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-RESEARCH.md (Runtime State Inventory + Pitfall 1 — build ops assets / restart mix phx.server before any browser gate),
    examples/scrypath_ecommerce/e2e/light-pixel-diff.mjs (threshold 0 light-parity gate),
    examples/scrypath_ecommerce/package.json (test:e2e, test:e2e:admin-contrast, test:e2e:admin-depth scripts)
  </read_first>
  <action>
    PRECONDITION (D-14, hard): build the scrypath_ops assets and restart mix phx.server (or run the dev compose --no-deps against source on a free port lane) BEFORE any browser-driven gate. The ecommerce dev server does NOT live-reload the scrypath_ops path-dep — a stale baked :4002 image silently tests old CSS (Pitfall 1; warning sign: computed backgroundColor doesn't match the token you changed). Seed the relevant scenarios.

    Run the inherited gates and capture pass/fail + key numbers into 134-03-SUMMARY.md:
    1. Light parity (D-13, the SOLE light-depth gate): `node e2e/light-pixel-diff.mjs` — MUST pass at threshold 0. A non-zero diff means the application pass leaked into light → FAIL the phase and fix (the hover boost, copper badge, and conditional DK-13 edit are all dark-only / theme-agnostic, so a light diff indicates an authoring mistake, not an accepted change).
    2. AA gate (UI-SPEC hard gate, all 3 themes): `make contrast` (fast token checker) + `npm run test:e2e:admin-contrast` (axe) — MUST report 0 AA color-contrast failures in light, dark, AND system-dark. AAA-body findings stay advisory/report-only. Any NEW AA failure FAILS the phase.
    3. Static contract: `cd scrypath_ops && mix verify.opsui` — green (includes the new surface_depth_token_contract_test.exs tripwire + MotionContractTest + light pixel-diff).
    4. Full depth gate: `npm run test:e2e:admin-depth` — green across {explicit-dark, system-dark} × {390,1440}; record the DK-13 measured ratio and whether the conditional edit fired (carried from Plan 02).

    Do NOT relax any threshold to pass. If a gate reveals a real SYSTEMIC defect on DK-11/DK-14/DK-15, record it and file it for Phase 135 (D-05/06/07) — do NOT patch .ops-verdict-neutral / --shadow-ops-raised / .ops-notice-surface locally this phase.
  </action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && node e2e/light-pixel-diff.mjs 2>&1 | tail -10 && npm run test:e2e:admin-contrast 2>&1 | tail -15</automated>
  </verify>
  <done>light-pixel-diff passes at threshold 0; make contrast + test:e2e:admin-contrast report 0 AA failures in light/dark/system-dark; mix verify.opsui green; test:e2e:admin-depth green across the grid — all run against a booted, seeded, assets-built ops server (D-14). Results recorded in 134-03-SUMMARY.md; any DK-11/14/15 systemic defect filed for Phase 135.</done>
  <acceptance_criteria>
    - Ops assets rebuilt + mix phx.server restarted before browser gates (D-14); not the stale baked image.
    - light-pixel-diff threshold 0 PASS (light pixel-identical).
    - 0 AA failures in light, dark, AND system-dark (axe + fast checker).
    - mix verify.opsui green (incl. new token tripwire).
    - test:e2e:admin-depth green {explicit-dark, system-dark} × {390,1440}; DK-13 ratio recorded.
    - No threshold relaxed; any DK-11/14/15 systemic defect routed to Phase 135, not patched locally.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human spot-review the two aesthetic reads that resist a threshold (D-13/D-06)</name>
  <action>Present the re-captured 40-shot dark matrix for the two human-only aesthetic reads (earned-copper feel; warm-halo absence) per D-13/D-06; everything threshold-able is already gated automatically in Task 1. Block on operator approval.</action>
  <what-built>
    The 40-shot screenshot matrix re-captured against the booted, seeded, assets-built ops server (build ops assets + restart mix phx.server first — D-14), covering the three target surfaces (Search rows, Sync/Drift depth, Playbooks empty + populated) in dark across all seed scenarios, plus the Control Room recommended intent-card with the new earned copper badge and the posture verdict hero.
  </what-built>
  <how-to-verify>
    1. Confirm the three target surfaces (Search result rows, Sync/Drift section panels + preflight cards, Playbooks data-cards) read as DISTINCTLY RAISED above the #0c0f14 shell floor in the dark target shots, across every seed scenario — not coplanar. (The deterministic part is already gated by the depth spec; this is the aesthetic confirmation.)
    2. Confirm the earned-copper badge on the Control Room recommended intent-card reads as "earned, on the scan path" — a genuine key fact, ~5%, "Scrypath brand," NOT decorative and NOT reading as a status tone.
    3. Confirm the posture verdict hero (DK-14) shows NO warm cream halo in dark — the cool dark ambient-shadow recipe should read clean. If a warm halo persists, it is a systemic shadow-recipe defect → file for Phase 135 (D-06), do NOT patch locally.
    These two reads (earned-copper feel; warm-halo absence) are the only human-spot-review items per D-13/D-06 — everything else is automated.
  </how-to-verify>
  <resume-signal>Type "approved" if the three surfaces read raised, copper reads earned (not a status tone), and there is no warm halo. Otherwise describe the issue; if it is a DK-11/14/15 systemic defect, confirm it is filed for Phase 135 rather than patched here.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → local ops server | All verification gates drive a locally booted, seeded `scrypath_ops` server with ops assets built first (D-14); never a remote or stale baked target. |

## STRIDE Threat Register

This plan is verification-only — it runs the inherited gates and records results. It adds **no endpoints, no inputs, no data handling, no dependencies, no new attack surface.** Only the operational/low-severity consideration below applies.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-06 | Tampering | verification target (stale vs source) | low | mitigate | Gates run against a booted, seeded, assets-built ops server (D-14), never the stale baked :4002 image — a stale target would yield a false pass/fail; the rebuild precondition mitigates it. |
</threat_model>

<verification>
- `node e2e/light-pixel-diff.mjs` — threshold 0 PASS (light pixel-identical).
- `make contrast` + `npm run test:e2e:admin-contrast` — 0 AA failures, light/dark/system-dark.
- `cd scrypath_ops && mix verify.opsui` — green (incl. new token tripwire).
- `npm run test:e2e:admin-depth` — green {explicit-dark, system-dark} × {390,1440}.
- Human spot-review (Task 2) — three surfaces raised, copper earned, no warm halo.
- All against a booted, seeded, assets-built ops server (D-14).
</verification>

<success_criteria>
- Light pixel-identical (threshold 0).
- 0 AA failures in all three themes.
- mix verify.opsui + depth spec green.
- Human aesthetic reads approved (earned copper, raised surfaces, no warm halo).
- Any DK-11/14/15 systemic defect routed to Phase 135, not patched locally.
</success_criteria>

<output>
Create `.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-03-SUMMARY.md` when done (record all gate results + numbers, the DK-13 measured ratio, the human spot-review outcome, and any DK-11/14/15 defect filed for Phase 135).
</output>
