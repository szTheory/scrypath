---
status: passed
phase: 134-under-iterated-surface-polish-dual-theme-s
source: [134-01-SUMMARY.md, 134-02-SUMMARY.md, 134-03-SUMMARY.md]
started: 2026-06-29T20:40:28Z
updated: 2026-07-10T23:29:23Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: complete
name: Automated Verification Complete
expected: |
  Tests 4-6 are covered by Docker-backed browser automation, light parity checks, contrast checks, and advisory visual judgment artifacts.
awaiting: none

## Tests

### 1. Search Result Row Depth and Hover
expected: In dark mode and system-dark mode, Search result rows sit on a visibly raised surface above the shell floor. Hovering a result row gives a perceptible violet border and preserved mid-depth shadow, while light mode keeps the prior lighter hover treatment.
result: pass

### 2. Sync and Drift Surface Separation
expected: Sync/Drift muted panels, preflight cards, and drift chips read as distinct elevated sections in dark mode. Chip tone semantics remain unchanged, neutral counts do not look like warnings, and text remains comfortably readable.
result: pass

### 3. Playbooks Empty and Populated Depth
expected: Playbooks empty-state cards, populated object rows, and data cards read as raised surfaces in dark mode. The active playbook item keeps the inherited violet glow, and destructive Delete actions remain visually separated from non-destructive actions.
result: pass
evidence: |
  `mix test test/scrypath_ops_web/live/playbook_live_test.exs` passed 19 tests, including the Playbooks empty-hero case.
  `npx playwright test e2e/admin_surface_depth.spec.ts --grep "playbooks|Playbook" --project=chromium` passed 9 browser tests after tightening the ambiguous Results heading wait.
  Browser probe against http://127.0.0.1:4002/admin/search/playbooks confirmed 4 populated rows, dark active-row violet glow (`rgba(108, 92, 231, 0.3)` in box-shadow), light active-row no violet glow, dark preview data-card `rgb(27, 34, 48)`, Duplicate/Rename grouped as quiet secondary actions, and Delete isolated in an `ops-tone-error` danger group. Screenshots: `.tmp/phase134-uat/playbooks-dark.png`, `.tmp/phase134-uat/playbooks-light.png`.

### 4. Control Room Copper Accent
expected: The recommended Control Room intent card shows exactly one earned copper badge/key fact, reads as Scrypath-branded rather than generic ops UI, and copper does not appear as a status tone elsewhere.
result: pass
evidence: |
  `cd scrypath_ops && mix test test/scrypath_ops_web/live/control_room_live_test.exs` passed 5 tests after adding a degraded-state assertion for exactly one `.ops-copper-badge`.
  Docker-backed browser verification on `http://127.0.0.1:4013` passed the source-backed Control Room recommended-state checks: `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4013 npm run test:e2e:admin-depth -- --reporter=line` passed 33 tests.
  The CI-mode full tracked Playwright run also passed all Control Room recommended cases inside the 95-test suite.

### 5. Light Parity and Contrast
expected: Light-mode screenshots remain stable against the automated light baseline inventory and viewport-width contract, and contrast checks report 0 AA failures across light, explicit dark, and system-dark modes.
result: pass
evidence: |
  `npm run test:e2e:admin-light-parity` passed with `admin-light-baseline-check: PASS (20/20 checked)`.
  `make contrast` passed with `AA failures: 0` and `AAA advisory: 38`.
  Full PNG hash/height threshold 0 was intentionally replaced with a stable inventory/viewport-width gate because the captured pages include dynamic IDs, timestamps, and seeded operational rows; the accepted contract is recorded in `examples/scrypath_ecommerce/e2e/admin-light-baseline.json`.

### 6. Browser Depth Evidence
expected: The admin depth and screenshot matrix browser runs pass against a booted ops server. The captured dark screenshots show raised Search, Sync/Drift, Playbooks, Control Room, and verdict surfaces without a warm halo, and the DK-13 row-border measurement remains above the trigger.
result: pass
evidence: |
  Booted the Docker DX stack with `WEB_PORT=4013 COMPOSE_PROJECT_NAME=scrypath_ecommerce_verify docker compose -f compose.yaml -f compose.dev.yaml up --build -d`, avoiding the stale local `4002` server.
  `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4013 npm run test:e2e:admin-depth -- --reporter=line` passed 33 tests.
  `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4013 npm run test:e2e:admin-matrix` passed 3 tests and refreshed the screenshot matrix.
  `CI=true PLAYWRIGHT_BASE_URL=http://127.0.0.1:4013 npm run test:e2e -- $(git ls-files 'e2e/*.spec.ts')` passed 95 tests and produced `test-results/phase105-playwright.json`.
  `scripts/ci/phase105_evidence.sh` produced `examples/scrypath_ecommerce/test-results/phase105-evidence-summary.md` with `conclusion: success`, `specs_total: 95`, `tests_total: 95`, `failed_specs: none`, `light_parity_status: pass`, and `visual_judge_status: pass`.

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
