---
phase: 37-disjunctive-facet-counts
plan: 02
type: execute
wave: 2
depends_on:
  - "37-01"
files_modified:
  - guides/faceted-search-with-phoenix-liveview.md
  - test/scrypath/docs_contract_test.exs
  - lib/mix/tasks/verify.phase37.ex
autonomous: true
requirements:
  - FACET-02
tags:
  - documentation
  - liveview-guide
  - verify-task
must_haves:
  truths:
    - "The LiveView faceted guide contains a first-class `##` section on disjunctive facet **counts** (single-search honesty + multi-search merge) and a **Genre OR + year AND** reference scenario without internal REQ IDs in prose."
    - "`docs_contract_test.exs` locks **3–5** new stable substrings in that guide section."
    - "`mix verify.phase37` runs a focused test set including the new facet merge tests and docs contract."
  artifacts:
    - path: "guides/faceted-search-with-phoenix-liveview.md"
      provides: "Operator-visible narrative for FACET-02"
      contains: "Disjunctive"
    - path: "lib/mix/tasks/verify.phase37.ex"
      provides: "Phase 37 verify entrypoint"
      contains: "Phase 37"
---

<objective>
Document **disjunctive facet count** behavior and the **Genre OR + year AND** reference scenario in **`guides/faceted-search-with-phoenix-liveview.md`**, add **`docs_contract_test.exs`** anchors, and ship **`mix verify.phase37`** mirroring Phase 36’s focused verify pattern.
</objective>

<threat_model>
**ASVS V5.1 L1 — documentation / social engineering**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| **T-37-03:** Docs imply **single** `search/3` returns unrefined OR-group counts | High | Explicit prose + table in guide; cross-link merge helper module name **`Scrypath.Facets.Disjunctive`** |
| **T-37-04:** Published markdown accidentally includes **internal REQ IDs** | Medium | Obey `docs_contract_test` hygiene regexes — **never** insert `FACET-02` literal in guide body |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: `Mix.Tasks.Verify.Phase37` focused verify task (run first)</name>
  <files>lib/mix/tasks/verify.phase37.ex</files>
  <read_first>
    - lib/mix/tasks/verify.phase36.ex (copy structure)
    - .planning/phases/37-disjunctive-facet-counts/37-VALIDATION.md
  </read_first>
  <action>
    1. Create **`lib/mix/tasks/verify.phase37.ex`** defining **`Mix.Tasks.Verify.Phase37`** with **`@shortdoc`** containing the substring **`Disjunctive facet`**.
    2. Set **`@focused_tests`** to exactly this list (order preserved):
       - `"test/scrypath/facets/disjunctive_test.exs"`
       - `"test/scrypath/meilisearch/query_test.exs"`
       - `"test/scrypath/docs_contract_test.exs"`
    3. Implement **`run/1`** identical pattern to Phase 36: `Mix.Task.run("app.start")`, reject args, `Mix.Task.run("test", @focused_tests)` via inline **`run_test!/2`** with label string **`Phase 37 disjunctive facet counts verification`**.
  </action>
  <verify>
    <automated>mix verify.phase37</automated>
  </verify>
  <acceptance_criteria>
    - `mix verify.phase37` exits **0** after Plan **01** is merged (executor runs Plan 01 before Plan 02).
    - `grep -n 'disjunctive_test.exs' lib/mix/tasks/verify.phase37.ex` returns **≥1** line.
    - `grep -n 'query_test.exs' lib/mix/tasks/verify.phase37.ex` returns **≥1** line.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Guide — `## Disjunctive facet counts` + reference scenario + appendix pointer</name>
  <files>guides/faceted-search-with-phoenix-liveview.md</files>
  <read_first>
    - guides/faceted-search-with-phoenix-liveview.md (full file — locate `## Hierarchical facets` and **Anti-pattern appendix** for Meilisearch)
    - lib/scrypath/facets/disjunctive.ex (final `merge_distributions/2` name and `@moduledoc` claims)
    - .planning/phases/37-disjunctive-facet-counts/37-CONTEXT.md (D-10, D-11, D-14)
  </read_first>
  <action>
    1. Insert a new **`## Disjunctive facet counts`** section in the **main narrative flow** (before appendices — **immediately after** **`## Hierarchical facets`** when that heading exists).
    2. Section must allow **`docs_contract_test.exs`** to find these **exact** substrings somewhere in the guide:
       - Markdown heading: **`## Disjunctive facet counts`**
       - **`OR within the same facet field`**
       - **`AND across different facet fields`**
       - **`single search`**
       - **`multi-search`**
       - **`Scrypath.Facets.Disjunctive`**
    3. Include the contiguous phrase **`Genre OR + year AND on the movies catalog`** in the section body (reference scenario).
    4. Add **one** bullet or sentence in the existing **Meilisearch** anti-pattern appendix that names the wrong mental model **counts ignore my OR group** and points readers to disjunctive facet counts (use the word **disjunctive** in lowercase at least once).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '## Disjunctive facet counts' guides/faceted-search-with-phoenix-liveview.md` returns **≥1** line.
    - `grep -n 'Genre OR + year AND on the movies catalog' guides/faceted-search-with-phoenix-liveview.md` returns **≥1** line.
    - `mix test test/scrypath/docs_contract_test.exs` exits **0** after Task **3** completes (contract tests reference new strings).
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: `docs_contract_test.exs` — Phase 37 substring test + verify.phase37 listing</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (top-of-file `@verify_phase36` pattern; `assert_contains_all` style)
    - guides/faceted-search-with-phoenix-liveview.md (post Task 2)
    - lib/mix/tasks/verify.phase37.ex (must exist before adding `@verify_phase37`)
  </read_first>
  <action>
    1. Add **`@verify_phase37 File.read!("lib/mix/tasks/verify.phase37.ex")`** next to other `@verify_phase*` reads.
    2. Add test **`test "phase 37 guide documents disjunctive facet counts with stable phrases"`** with **`assert_contains_all(@guides["guides/faceted-search-with-phoenix-liveview.md"], [...])`** and this **exact** string list (order preserved):
       - `"## Disjunctive facet counts"`
       - `"OR within the same facet field"`
       - `"AND across different facet fields"`
       - `"single search"`
       - `"multi-search"`
       - `"Scrypath.Facets.Disjunctive"`
       - `"Genre OR + year AND on the movies catalog"`
    3. Add test **`test "verify.phase37 lists disjunctive facet verification paths without Hex secrets"`** with **`assert_contains_all(@verify_phase37, [...])`** listing the three test paths as substrings and **`refute @verify_phase37 =~ "HEX_API_KEY"`** (mirror the Phase 36 verify test structure).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'verify.phase37' test/scrypath/docs_contract_test.exs` returns **≥2** lines.
    - `grep -n 'phase 37 guide documents disjunctive' test/scrypath/docs_contract_test.exs` returns **≥1** line.
    - `mix test test/scrypath/docs_contract_test.exs` exits **0**.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Execute tasks in numeric order **1 → 2 → 3** (verify task file exists before `@verify_phase37 File.read!`).
- `mix verify.phase37`
- `mix test test/scrypath/docs_contract_test.exs`
</verification>

<success_criteria>
- [ ] Guide section + appendix pointer shipped; contract tests green.
- [ ] Focused verify task exists and passes with Plan 01 tests present.
</success_criteria>
