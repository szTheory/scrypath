---
phase: 36-hierarchical-facets
plan: 02
type: execute
wave: 2
depends_on:
  - "36-01"
files_modified:
  - lib/scrypath/meilisearch/settings.ex
  - lib/scrypath/operator/index_contract_drift.ex
  - test/scrypath/meilisearch/settings_test.exs
  - test/scrypath/operator/index_contract_drift_test.exs
autonomous: true
requirements:
  - FACET-01
tags:
  - meilisearch
  - settings
  - drift
must_haves:
  truths:
    - "`merge_faceting_filterable_attributes/2` projects every declared faceting attribute (including dotted atoms) into Meilisearch filterable object entries without dropping `facetSearch` features."
    - "`IndexContractDrift` faceting declared vs applied comparison stays **green** for schemas using **`nested_facet_paths: true`** when live wire matches expanded declaration (no false positive drift for hierarchical attribute lists)."
  artifacts:
    - path: "lib/scrypath/meilisearch/settings.ex"
      provides: "filterable projection for dotted facet attribute names"
      contains: "merge_faceting_filterable_attributes"
    - path: "lib/scrypath/operator/index_contract_drift.ex"
      provides: "faceting wire normalization for hierarchical declarations"
      contains: "faceting_declared_wire"
---

<objective>
Align **Meilisearch settings merge** and **index contract drift** faceting normalization with hierarchical **`faceting.attributes`** so applied indexes match declared configuration (CONTEXT D-03, D-12).
</objective>

<threat_model>
**ASVS V14 / configuration integrity (L1)**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| **T-36-CFG-01:** Dotted attributes silently omitted from filterable projection → facets unavailable at runtime | Medium | Add/adjust **unit tests** in **`settings_test.exs`** asserting string keys **`"categories.lvl0"`** (or chosen example) appear in merged filterable payload |
| **T-36-CFG-02:** Drift normalizer hides real mismatch | Medium | Extend **`index_contract_drift_test.exs`** with a hierarchical declaration fixture and expected **` :ok`** or explicit diff shape |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Settings merge — dotted faceting attributes</name>
  <files>lib/scrypath/meilisearch/settings.ex, test/scrypath/meilisearch/settings_test.exs</files>
  <read_first>
    - lib/scrypath/meilisearch/settings.ex (`merge_faceting_filterable_attributes/2`, `transform_filterable_for_faceting/3`, `facet_filterable_object/1`)
    - test/scrypath/meilisearch/settings_test.exs (closest existing merge tests)
    - test/support/facetable_hierarchy.ex (from Plan 01 — or temporary inline schema module if not merged yet; executor reads Plan 01 output)
  </read_first>
  <action>
    1. Trace **`facet_names = MapSet.new(attrs, &Atom.to_string/1)`** path: confirm dotted atoms stringify to Meilisearch-compatible attribute strings; fix any code assuming **no dots** in facet name strings.
    2. Add **focused tests** that build settings from a schema module (or minimal stub returning faceting with **`:"demo.lvl0"`**) and assert merged **`filterable`** contains the expected **`%{"attribute" => ..., "features" => ["facetSearch"]}`** entry for that attribute string.
  </action>
  <verify>
    <automated>mix test test/scrypath/meilisearch/settings_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'merge_faceting_filterable_attributes' lib/scrypath/meilisearch/settings.ex` returns at least 1 line.
    - `mix test test/scrypath/meilisearch/settings_test.exs` exits 0.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Index contract drift — faceting wire parity</name>
  <files>lib/scrypath/operator/index_contract_drift.ex, test/scrypath/operator/index_contract_drift_test.exs</files>
  <read_first>
    - lib/scrypath/operator/index_contract_drift.ex (`compare_faceting/2`, `faceting_declared_wire/1`, `faceting_applied_wire/1`)
    - test/scrypath/operator/index_contract_drift_test.exs (faceting cases)
    - .planning/phases/36-hierarchical-facets/36-CONTEXT.md (D-12)
  </read_first>
  <action>
    1. Update **`faceting_declared_wire/1`** (and **`faceting_applied_wire/1`** if needed) so hierarchical attribute lists normalize to **comparable** maps/lists matching Meilisearch JSON (string keys stable, ordering documented or sorted consistently).
    2. Add or extend a drift test where **`applied_wire["faceting"]`** includes multi-attribute faceting matching a **`FacetableHierarchy`**-style declaration; expect **`:ok`** on the faceting dimension (or document expected diff if harness uses simplified maps).
  </action>
  <verify>
    <automated>mix test test/scrypath/operator/index_contract_drift_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/operator/index_contract_drift_test.exs` exits 0.
    - `grep -n 'faceting_declared_wire' lib/scrypath/operator/index_contract_drift.ex` returns at least 1 line.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix test test/scrypath/meilisearch/settings_test.exs test/scrypath/operator/index_contract_drift_test.exs`
</verification>

<success_criteria>
- Hierarchical facet attribute names flow through settings merge and drift comparison without regressing flat-only schemas (existing tests still pass).
</success_criteria>
