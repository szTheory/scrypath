---
phase: 36-hierarchical-facets
plan: 03
type: execute
wave: 3
depends_on:
  - "36-02"
files_modified:
  - lib/scrypath/search_result.ex
  - test/scrypath/search_test.exs
  - guides/faceted-search-with-phoenix-liveview.md
  - test/scrypath/docs_contract_test.exs
  - lib/mix/tasks/verify.phase36.ex
  - mix.exs
autonomous: true
requirements:
  - FACET-01
tags:
  - facets
  - docs
  - verify
must_haves:
  truths:
    - "Guide contains a stable **`## Hierarchical facets`** section (exact heading string) describing supported shapes, opt-in, and Meilisearch distribution semantics per CONTEXT D-13–D-15."
    - "Guide no longer states that **all** hierarchical dotted **`faceting.attributes`** are categorically wrong; unsupported wildcards remain documented as mistakes (CONTEXT D-14)."
    - "`docs_contract_test.exs` asserts **3–6** new stable substrings from the hierarchical section / related lines, none containing **`FACET-`** requirement IDs (hygiene test)."
    - "`mix verify.phase36` exists and runs a **focused** test list including **`test/scrypath/options_test.exs`**, **`test/scrypath/search_test.exs`**, **`test/scrypath/meilisearch/settings_test.exs`**, **`test/scrypath/operator/index_contract_drift_test.exs`**, **`test/scrypath/docs_contract_test.exs`**; **`mix.exs`** **`preferred_cli_env`** includes **`\"verify.phase36\": :test`**."
  artifacts:
    - path: "guides/faceted-search-with-phoenix-liveview.md"
      provides: "Hierarchical facets narrative + appendix correction"
      contains: "## Hierarchical facets"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Phase 36 doc substring locks"
      contains: "Hierarchical facets"
    - path: "lib/mix/tasks/verify.phase36.ex"
      provides: "Focused verify entrypoint"
      contains: "Verify.Phase36"
---

<objective>
Prove **stable facet keys** for hierarchical attributes in **`%SearchResult.Facets{}`**, ship **adopter-facing docs** and **doc contracts**, and add **`mix verify.phase36`** for CI-friendly focused verification (roadmap success criteria 1–3; CONTEXT D-12–D-16).
</objective>

<threat_model>
**ASVS V14 / documentation integrity (L1)**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| **T-36-DOC-01:** Doc contract strings drift to meaningless phrases | Low | Lock **specific** phrases tied to real section content; run **`mix test test/scrypath/docs_contract_test.exs`** |
| **T-36-DOC-02:** REQ IDs leak into published markdown | Medium | Follow existing **`docs_contract_test`** hygiene patterns; grep **`FACET-`** in guide after edits |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: `decode_facets/2` — hierarchical keys regression test</name>
  <files>test/scrypath/search_test.exs, lib/scrypath/search_result.ex</files>
  <read_first>
    - lib/scrypath/search_result.ex (`decode_facets/2`, `decode_distribution_buckets/1`)
    - test/scrypath/search_test.exs (facet-related tests ~L200+)
    - lib/scrypath/query.ex (`facets` field)
  </read_first>
  <action>
    1. If **`decode_facets/2`** already supports dotted atoms with no code change, add a **unit test** building a minimal **`%Query{facets: [:"categories.lvl0", :"categories.lvl1"], ...}`** and raw map **`%{"facetDistribution" => %{\"categories.lvl0\" => %{\"A\" => 1}, \"categories.lvl1\" => %{\"A > B\" => 1}}}`** (adjust to match real **`Query`** construction helpers used in this file) asserting **`result.facets.distribution[:"categories.lvl0"]`** is non-empty **`Bucket`** list.
    2. If any edge case fails (e.g. atom/string key mismatch in **`Map.get/3`**), make the **minimal** fix in **`lib/scrypath/search_result.ex`** and document in commit message.
  </action>
  <verify>
    <automated>mix test test/scrypath/search_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'categories.lvl' test/scrypath/search_test.exs` returns at least 1 line **or** equivalent dotted-attribute test string chosen in test.
    - `mix test test/scrypath/search_test.exs` exits 0.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Guide hierarchical section + `docs_contract_test.exs` substring locks</name>
  <files>guides/faceted-search-with-phoenix-liveview.md, test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - guides/faceted-search-with-phoenix-liveview.md (full file; locate hierarchy / wildcards / mistakes appendix)
    - test/scrypath/docs_contract_test.exs (existing guide assertion patterns)
    - .planning/phases/36-hierarchical-facets/36-CONTEXT.md (D-13, D-14, D-15, D-17)
    - .planning/phases/20-faceted-search-liveview-guide/20-CONTEXT.md (D-01 primary guide constraint)
  </read_first>
  <action>
    1. Add **`## Hierarchical facets`** section (exact heading) covering: **opt-in** flag name as implemented in Plan 01, **Meilisearch multi-attribute** pattern, **AND-across-levels** mental model, **non-nested** `facetDistribution` JSON shape, **out of scope** pointer to later facet phases **without** internal REQ-style tokens in prose.
    2. Revise mistaken blanket statement that **dotted hierarchical `faceting.attributes` are always wrong**; replace with accurate rules distinguishing **supported dotted paths + opt-in** vs **wildcards / unsupported** patterns.
    3. In **`test/scrypath/docs_contract_test.exs`**, add **`assert_contains_all`** (or equivalent) for **3–6** new substrings drawn from the new section (must include the exact heading **`## Hierarchical facets`**). Locked strings must **not** contain **`FACET-`** (hygiene).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '## Hierarchical facets' guides/faceted-search-with-phoenix-liveview.md` returns exactly 1 line.
    - `grep -n 'Hierarchical facets' test/scrypath/docs_contract_test.exs` returns at least 1 line.
    - `mix test test/scrypath/docs_contract_test.exs` exits 0.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: `mix verify.phase36` task + `mix.exs` env</name>
  <files>lib/mix/tasks/verify.phase36.ex, mix.exs</files>
  <read_first>
    - lib/mix/tasks/verify.phase20.ex (copy structure: `@shortdoc`, `@focused_tests`, `run/1`, `run_test!/2`)
    - mix.exs (`preferred_cli_env` block near other **`verify.phase`** entries)
  </read_first>
  <action>
    1. Create **`lib/mix/tasks/verify.phase36.ex`** as **`Mix.Tasks.Verify.Phase36`** mirroring **`Verify.Phase20`** but listing Phase 36 focused paths: **`test/scrypath/options_test.exs`**, **`test/scrypath/search_test.exs`**, **`test/scrypath/meilisearch/settings_test.exs`**, **`test/scrypath/operator/index_contract_drift_test.exs`**, **`test/scrypath/docs_contract_test.exs`** (add **`test/support/docs/phoenix_examples_test.exs`** only if hierarchical examples land there — optional).
    2. Add **`\"verify.phase36\": :test`** to **`mix.exs`** **`preferred_cli_env`** map adjacent to **`verify.phase20`**.
  </action>
  <verify>
    <automated>mix verify.phase36</automated>
  </verify>
  <acceptance_criteria>
    - File **`lib/mix/tasks/verify.phase36.ex`** exists.
    - `grep -n 'verify.phase36' mix.exs` returns at least 1 line.
    - `mix verify.phase36` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix verify.phase36`
- `mix test` (full suite before handoff if time permits)
</verification>

<success_criteria>
- End-to-end **facet distribution** decoding works for dotted facet field names in tests.
- Docs + contracts + verify task align with roadmap success criteria for Phase 36.
</success_criteria>
