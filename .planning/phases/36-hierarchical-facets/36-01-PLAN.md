---
phase: 36-hierarchical-facets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/options.ex
  - lib/scrypath/schema.ex
  - test/scrypath/options_test.exs
  - test/support/facetable_hierarchy.ex
autonomous: true
requirements:
  - FACET-01
tags:
  - faceting
  - compile-time
  - hierarchical
must_haves:
  truths:
    - "`faceting:` accepts `nested_facet_paths: true` (boolean) as an optional sibling of `:attributes`; when absent or false, dotted facet attribute atoms remain rejected with an `ArgumentError` whose message includes `hierarchical` (preserve Phase 20 default)."
    - "When `nested_facet_paths: true`, faceting attribute atoms may contain `.` only if they also appear in `filterable:` (subset rule unchanged) and pass any documented pattern guard added in this plan (CONTEXT D-05/D-09)."
    - "Optional `hierarchy:` sugar (keyword list under `faceting:`) expands into additional `:attributes` entries using the fixed `lvl0`..`lvl{N-1}` naming under the declared base field (CONTEXT D-04); expanded attributes are present in normalized `__scrypath__(:faceting)` output."
  artifacts:
    - path: "lib/scrypath/options.ex"
      provides: "nested path opt-in + hierarchy expansion + conditional dotted-atom rules"
      contains: "nested_facet_paths"
    - path: "test/support/facetable_hierarchy.ex"
      provides: "compile-time golden schema for hierarchical facets"
      contains: "Scrypath"
---

<objective>
Implement schema-layer **opt-in nested facet paths** and optional **`hierarchy:`** compile-time expansion per **36-CONTEXT.md** D-03–D-06, D-09–D-11, without changing default behavior for existing flat-only schemas.
</objective>

<threat_model>
**ASVS V5.1 / input validation (L1) — schema compile surface**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| **T-36-01:** Arbitrary dotted atoms bypass intent and complicate filter grammar | Medium | Gate dotted atoms behind **`nested_facet_paths: true`**; require membership in **`filterable:`**; optionally whitelist patterns (e.g. single `.` segment suffix `lvl\\d+`) — document exact rule in `@moduledoc` |
| **T-36-02:** Sugar expansion generates attributes not in `filterable:` | Medium | Run expansion **before** subset validation so missing filterable entries raise **`not in filterable`** with the generated atom name |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: `faceting:` schema — `nested_facet_paths` + `hierarchy:` expansion hook</name>
  <files>lib/scrypath/options.ex, lib/scrypath/schema.ex</files>
  <read_first>
    - lib/scrypath/options.ex (full `@schema_options` faceting types, `validate_schema_faceting/1`, `coerce_schema_faceting_kw/1`, `validate_faceting_declaration_shape/1`, `validate_faceting_rules!/1`)
    - lib/scrypath/schema.ex (how `@scrypath_config` is built from validated options)
    - .planning/phases/36-hierarchical-facets/36-CONTEXT.md (D-03, D-04, D-10, D-18)
    - .planning/phases/20-faceted-search-liveview-guide/20-01-PLAN.md (task structure reference only)
  </read_first>
  <action>
    1. Extend faceting keyword validation in **`lib/scrypath/options.ex`** to accept optional **`:nested_facet_paths`** boolean defaulting **`false`** when other faceting keys are present (choose NimbleOptions shape consistent with existing `faceting` custom validator).
    2. Add optional **`:hierarchy`** keyword list (shape TBD but **must** include at least **base** field atom and **depth** positive integer). In **`validate_schema_options!`** pipeline (or immediately after faceting coercion), **expand** `:hierarchy` into extra entries in **`:attributes`** using atoms like **`:"categories.lvl0"`**, **`:"categories.lvl1"`**, through **`:"categories.lvl{N-1}"`** for base `:categories` and depth `N`, using a **single** dot between base and level segment (matches Meilisearch dotted attribute style). Remove or clear `:hierarchy` from the **normalized** keyword list stored on the schema so **`__scrypath__(:faceting)`** exposes only canonical keys (`:attributes`, `:max_values_per_facet`, `:sort_facet_values_by`, `:nested_facet_paths`).
    3. If **`:hierarchy`** is absent, skip expansion.
    4. Ensure **`Scrypath.schema_faceting/1`** reflection returns the **post-expansion** list.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'nested_facet_paths' lib/scrypath/options.ex` returns at least 1 line.
    - `grep -n 'hierarchy' lib/scrypath/options.ex` returns at least 1 line.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Conditional dotted-atom rules in `validate_faceting_rules!/1`</name>
  <files>lib/scrypath/options.ex</files>
  <read_first>
    - lib/scrypath/options.ex (`validate_faceting_rules!/1` current dotted-atom rejection at lines ~933–937)
    - .planning/phases/36-hierarchical-facets/36-CONTEXT.md (D-05, D-10)
  </read_first>
  <action>
    1. Replace unconditional dotted-atom **`raise`** with: if **`nested_facet_paths`** is not truthy, keep current behavior (message still includes substring **`hierarchical`**).
    2. If **`nested_facet_paths`** is truthy, **allow** atoms whose string contains **`.`**, subject to the **pattern policy** you documented in Task 1 (at minimum: must be in **`filterable:`** — already enforced by following **`Enum.each`**; add explicit validation if CONTEXT pattern policy requires rejecting multiple dots or unknown suffixes).
  </action>
  <verify>
    <automated>mix test test/scrypath/options_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'validate_faceting_rules!' lib/scrypath/options.ex` returns at least 1 line.
    - `mix test test/scrypath/options_test.exs` exits 0.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Golden schema `FacetableHierarchy` + compile tests</name>
  <files>test/support/facetable_hierarchy.ex, test/scrypath/options_test.exs</files>
  <read_first>
    - test/support/facetable_movie.ex (style for `use Scrypath` test schema)
    - test/scrypath/options_test.exs (existing `Code.compile_string/2` patterns)
    - lib/scrypath/options.ex (post-change validation behavior)
  </read_first>
  <action>
    1. Add **`test/support/facetable_hierarchy.ex`** declaring **`filterable:`** that includes **`:"categories.lvl0"`**, **`:"categories.lvl1"`** (and any other fields required by **`embedded_schema`**), with **`faceting: [nested_facet_paths: true, attributes: [:"categories.lvl0", :"categories.lvl1"], max_values_per_facet: 100]`** (adjust keys to match normalized shape).
    2. Add tests proving: (a) module compiles; (b) **`nested_facet_paths: false`** (or omitted) with a dotted attribute still **raises** `ArgumentError` with substring **`hierarchical`**; (c) **`hierarchy:`** expansion case: a minimal **`Code.compile_string/2`** fragment compiles and yields expected **`:attributes`** list OR compile-time error lists expected missing filterable atom — pick one concrete assertion documented in test comment.
  </action>
  <verify>
    <automated>mix test test/scrypath/options_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File **`test/support/facetable_hierarchy.ex`** exists.
    - `grep -n 'FacetableHierarchy' test/scrypath/options_test.exs` returns at least 1 line **or** test file imports/aliases module under test.
    - `mix test test/scrypath/options_test.exs` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `mix compile --warnings-as-errors`
- `mix test test/scrypath/options_test.exs`
</verification>

<success_criteria>
- Default schemas unchanged: no **`nested_facet_paths`** → dotted **`faceting.attributes`** still invalid.
- Opt-in path: dotted attributes + optional hierarchy expansion compile and satisfy **`faceting.attributes ⊆ filterable:`**.
</success_criteria>
