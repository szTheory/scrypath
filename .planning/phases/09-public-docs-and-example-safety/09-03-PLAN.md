---
phase: 09-public-docs-and-example-safety
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - guides/phoenix-liveview.md
  - test/support/docs/phoenix_example_case.ex
  - test/support/docs/phoenix_examples_test.exs
  - test/support/docs/phoenix_request_shape_smoke_test.exs
autonomous: true
requirements:
  - DOCS-03
must_haves:
  truths:
    - "The LiveView/context publish examples model real Phoenix string-keyed nested attrs."
    - "One narrow request-shape smoke test proves the fixture boundary matches browser-shaped params without adding Phoenix as a dependency."
  artifacts:
    - path: "guides/phoenix-liveview.md"
      provides: "String-keyed public LiveView publish example"
    - path: "test/support/docs/phoenix_example_case.ex"
      provides: "Fixture implementation aligned with realistic request attrs"
    - path: "test/support/docs/phoenix_examples_test.exs"
      provides: "Behavioral checks for LiveView/context publish realism"
    - path: "test/support/docs/phoenix_request_shape_smoke_test.exs"
      provides: "Narrow Plug-shaped smoke coverage for nested request params"
  key_links:
    - from: "guides/phoenix-liveview.md"
      to: "test/support/docs/phoenix_example_case.ex"
      via: "publish event payload and context handoff"
      pattern: "%{\"id\" => id, \"post\" => attrs}|publish_post"
    - from: "test/support/docs/phoenix_example_case.ex"
      to: "test/support/docs/phoenix_request_shape_smoke_test.exs"
      via: "realistic nested string-key request payload shape"
      pattern: "post[title]"
---

<objective>
Make the LiveView/context examples trustworthy for copied Phoenix request shapes by aligning the fixture with nested string-key attrs and adding one narrow smoke test for real request decoding.

Purpose: Satisfy DOCS-03 while preserving the repo’s existing plain-fixture docs harness and avoiding a full Phoenix example app.
Output: Updated LiveView guide, aligned fixture/test behavior, and a focused request-shape smoke test.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-public-docs-and-example-safety/09-CONTEXT.md
@.planning/phases/09-public-docs-and-example-safety/09-RESEARCH.md
@.planning/phases/09-public-docs-and-example-safety/09-PATTERNS.md
@guides/phoenix-liveview.md
@guides/phoenix-contexts.md
@test/support/docs/phoenix_example_case.ex
@test/support/docs/phoenix_examples_test.exs
@mix.exs

<interfaces>
From guides/phoenix-liveview.md:
```elixir
def handle_event("publish", %{"id" => id, "post" => attrs}, socket) do
  post = load_post!(id)
  {:ok, _post} = Content.publish_post(post, attrs)

  {:noreply, socket}
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align the LiveView guide and fixture with real string-keyed publish attrs per D-09 through D-12</name>
  <files>guides/phoenix-liveview.md, test/support/docs/phoenix_example_case.ex, test/support/docs/phoenix_examples_test.exs</files>
  <read_first>.planning/phases/09-public-docs-and-example-safety/09-CONTEXT.md, .planning/phases/09-public-docs-and-example-safety/09-RESEARCH.md, guides/phoenix-liveview.md, guides/phoenix-contexts.md, test/support/docs/phoenix_example_case.ex, test/support/docs/phoenix_examples_test.exs</read_first>
  <acceptance_criteria>
    - `rg -n '%\\{\"id\" => id, \"post\" => attrs\\}' guides/phoenix-liveview.md test/support/docs/phoenix_example_case.ex` confirms the string-keyed event payload shape stays explicit.
    - `rg -n 'Map.get\\(attrs, \"title\"|attrs\\[\"title\"\\]' test/support/docs/phoenix_example_case.ex` shows the fixture now reads realistic request attrs.
    - `rg -n '%\\{\"id\" => 1, \"post\" => %\\{\"title\" => \"Published\"\\}\\}' test/support/docs/phoenix_examples_test.exs` confirms the behavioral test now uses string-keyed nested attrs.
  </acceptance_criteria>
  <action>Update the LiveView guide only as needed to keep the publish example explicitly string-keyed and context-first. In `test/support/docs/phoenix_example_case.ex`, make `Content.publish_post/2` and the `handle_event/3` path behave correctly with `%{\"post\" => %{\"title\" => ...}}` style attrs rather than relying on atom keys. Then update `test/support/docs/phoenix_examples_test.exs` so its publish-path assertions use real string-keyed nested params and keep proving the web layer delegates to the context boundary.</action>
  <verify>
    <automated>mix test test/support/docs/phoenix_examples_test.exs -x</automated>
  </verify>
  <done>The public LiveView/context examples now model the same request shape a Phoenix app actually receives from form events.</done>
</task>

<task type="auto">
  <name>Task 2: Add one narrow request-shape smoke test without introducing Phoenix as a dependency</name>
  <files>test/support/docs/phoenix_request_shape_smoke_test.exs</files>
  <read_first>.planning/phases/09-public-docs-and-example-safety/09-CONTEXT.md, .planning/phases/09-public-docs-and-example-safety/09-RESEARCH.md, mix.exs, test/support/docs/phoenix_example_case.ex, test/support/docs/phoenix_examples_test.exs</read_first>
  <acceptance_criteria>
    - `test -f test/support/docs/phoenix_request_shape_smoke_test.exs` exits 0.
    - `rg -n 'Plug\\.Conn\\.Query\\.decode|post\\[title\\]=Published' test/support/docs/phoenix_request_shape_smoke_test.exs` finds a realistic nested request-shape smoke path.
    - `mix test test/support/docs/phoenix_request_shape_smoke_test.exs test/support/docs/phoenix_examples_test.exs -x` exits 0.
  </acceptance_criteria>
  <action>Create `test/support/docs/phoenix_request_shape_smoke_test.exs` as a narrow trust booster for the fixture docs boundary. Use Plug’s request decoding helpers or an equivalent string-keyed request builder already available in test scope to construct nested params like `post[title]=Published`, then prove that the resulting shape matches what the fixture publish path accepts. Keep this test intentionally small and do not add Phoenix or a full web harness.</action>
  <verify>
    <automated>mix test test/support/docs/phoenix_request_shape_smoke_test.exs test/support/docs/phoenix_examples_test.exs -x</automated>
  </verify>
  <done>The docs suite has one focused proof that real nested request payloads line up with the publish-path fixture behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser/Phoenix event payload -> fixture publish path | The docs fixture must accept the same nested string-key params real LiveView events deliver. |
| Fixture docs boundary -> future contributors | Safety tests must make request-shape realism hard to accidentally regress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | T | `test/support/docs/phoenix_example_case.ex` | mitigate | Make publish/update fixture logic consume string-keyed attrs so the docs boundary matches real request payloads. |
| T-09-06 | R | `test/support/docs/phoenix_request_shape_smoke_test.exs` | mitigate | Add one narrow Plug-shaped smoke path that proves nested browser params map to the fixture publish contract. |
</threat_model>

<verification>
Run `mix test test/support/docs/phoenix_examples_test.exs -x` after Task 1 and `mix test test/support/docs/phoenix_request_shape_smoke_test.exs test/support/docs/phoenix_examples_test.exs -x` after Task 2 and before writing the summary.
</verification>

<success_criteria>
The LiveView/context docs examples use realistic string-keyed attrs, and the docs suite includes a focused proof that nested request params match the fixture boundary without adding Phoenix runtime dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/09-public-docs-and-example-safety/09-03-SUMMARY.md`
</output>
