---
phase: 09-public-docs-and-example-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - guides/phoenix-controllers-and-json.md
  - test/support/docs/phoenix_example_case.ex
  - test/support/docs/phoenix_examples_test.exs
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - DOCS-02
must_haves:
  truths:
    - "The public JSON example never raises on malformed `page` params."
    - "Missing, malformed, zero, and negative page input all normalize to page 1 in the primary docs path."
  artifacts:
    - path: "guides/phoenix-controllers-and-json.md"
      provides: "Safe public JSON controller example"
    - path: "test/support/docs/phoenix_example_case.ex"
      provides: "Executable fixture source of truth for the JSON example"
    - path: "test/support/docs/phoenix_examples_test.exs"
      provides: "Behavioral safety checks for page normalization"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "String-exact docs contract assertions for the safe helper"
  key_links:
    - from: "guides/phoenix-controllers-and-json.md"
      to: "test/support/docs/phoenix_example_case.ex"
      via: "safe page-normalization helper shape"
      pattern: "normalize_page|Integer.parse|page_number"
    - from: "test/support/docs/phoenix_example_case.ex"
      to: "test/support/docs/phoenix_examples_test.exs"
      via: "behavior and source assertions for invalid page input"
      pattern: "normalize_page|page == %{number: 1"
---

<objective>
Harden the public JSON controller example so copied request handling is lenient and non-raising while preserving the context-first search boundary.

Purpose: Satisfy DOCS-02 by replacing crash-prone pagination parsing with safe request normalization in both docs and executable fixtures after the shared docs-contract harness is updated in 09-01.
Output: Updated guide, aligned fixture implementation, and tests that lock the safe page contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-public-docs-and-example-safety/09-CONTEXT.md
@.planning/phases/09-public-docs-and-example-safety/09-RESEARCH.md
@.planning/phases/09-public-docs-and-example-safety/09-PATTERNS.md
@guides/phoenix-controllers-and-json.md
@guides/phoenix-contexts.md
@lib/scrypath/query.ex
@lib/scrypath/options.ex
@test/scrypath/search_test.exs
@test/support/docs/phoenix_example_case.ex
@test/support/docs/phoenix_examples_test.exs
@test/scrypath/docs_contract_test.exs

<interfaces>
From lib/scrypath/options.ex:
```elixir
page: [
  type: {:custom, __MODULE__, :validate_search_page, []},
  default: [],
  doc: "Nested common-path pagination options."
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace crash-prone page parsing in the JSON guide and fixture per D-05 through D-08</name>
  <files>guides/phoenix-controllers-and-json.md, test/support/docs/phoenix_example_case.ex</files>
  <read_first>.planning/phases/09-public-docs-and-example-safety/09-CONTEXT.md, .planning/phases/09-public-docs-and-example-safety/09-RESEARCH.md, guides/phoenix-controllers-and-json.md, test/support/docs/phoenix_example_case.ex, lib/scrypath/options.ex, test/scrypath/search_test.exs</read_first>
  <acceptance_criteria>
    - `rg -n 'String.to_integer\\(page\\)' guides/phoenix-controllers-and-json.md test/support/docs/phoenix_example_case.ex` returns no matches.
    - `rg -n 'Integer.parse|when number > 0|do: 1' guides/phoenix-controllers-and-json.md test/support/docs/phoenix_example_case.ex` finds the new non-raising normalization path.
    - `rg -n 'page: \\[number: page_number, size: 20\\]' guides/phoenix-controllers-and-json.md test/support/docs/phoenix_example_case.ex` confirms the context call shape remains unchanged.
  </acceptance_criteria>
  <action>Update the JSON controller guide and the fixture module to use a local, non-raising `normalize_page/1` helper. It should preserve positive integer input, parse positive integer strings with `Integer.parse/1`, and normalize missing, malformed, zero, negative, or otherwise unsupported input to `1`. Keep the normalization at the request/example boundary and keep the context call unchanged so the docs still teach that controllers handle request shape while contexts own search orchestration.</action>
  <verify>
    <automated>mix test test/support/docs/phoenix_examples_test.exs test/scrypath/docs_contract_test.exs -x</automated>
  </verify>
  <done>The public JSON example is copy-paste safe for invalid page input and still teaches the same context-first architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Expand fixture and docs tests to lock safe pagination behavior</name>
  <files>test/support/docs/phoenix_examples_test.exs, test/scrypath/docs_contract_test.exs</files>
  <read_first>.planning/phases/09-public-docs-and-example-safety/09-CONTEXT.md, guides/phoenix-controllers-and-json.md, test/support/docs/phoenix_example_case.ex, test/support/docs/phoenix_examples_test.exs, test/scrypath/docs_contract_test.exs</read_first>
  <acceptance_criteria>
    - `rg -n 'malformed|negative|zero|missing|page 1|Integer.parse' test/support/docs/phoenix_examples_test.exs test/scrypath/docs_contract_test.exs` finds explicit coverage for invalid page cases and the safe helper implementation.
    - `mix test test/support/docs/phoenix_examples_test.exs test/scrypath/docs_contract_test.exs -x` exits 0.
  </acceptance_criteria>
  <action>Extend `test/support/docs/phoenix_examples_test.exs` to assert that the JSON controller fixture normalizes missing, malformed, zero, and negative `page` input to page `1` while preserving valid positive input. Update `test/scrypath/docs_contract_test.exs` so the guide contract now checks for the safe helper shape and refutes `String.to_integer/1`. Keep the assertions exact and local to the docs safety contract.</action>
  <verify>
    <automated>mix test test/support/docs/phoenix_examples_test.exs test/scrypath/docs_contract_test.exs -x</automated>
  </verify>
  <done>The docs safety harness now rejects regressions back to exception-driven pagination parsing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted request params -> controller example | Public docs must not teach exception-driven parsing for malformed input. |
| Controller example -> context call | Request-shape normalization should stay local while the context contract remains stable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | D | `guides/phoenix-controllers-and-json.md` | mitigate | Replace `String.to_integer/1` with a non-raising normalization helper that defaults invalid page input to `1`. |
| T-09-04 | R | `test/support/docs/phoenix_examples_test.exs` | mitigate | Add explicit invalid-page behavior tests so future docs edits cannot silently reintroduce crash-prone parsing. |
</threat_model>

<verification>
Run `mix test test/support/docs/phoenix_examples_test.exs test/scrypath/docs_contract_test.exs -x` after each task and `mix test test/scrypath/docs_contract_test.exs test/support/docs/phoenix_examples_test.exs -x` before writing the summary.
</verification>

<success_criteria>
The Phoenix JSON docs and fixture examples normalize invalid page params safely to `1`, keep the context boundary intact, and are protected by executable tests.
</success_criteria>

<output>
After completion, create `.planning/phases/09-public-docs-and-example-safety/09-02-SUMMARY.md`
</output>
