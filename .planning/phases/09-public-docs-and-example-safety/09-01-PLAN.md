---
phase: 09-public-docs-and-example-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/getting-started.md
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "The canonical public install snippet lists only the direct Scrypath dependency."
    - "Optional integrations and backend/runtime guidance stay outside the base install contract."
  artifacts:
    - path: "README.md"
      provides: "Canonical consumer install contract"
    - path: "guides/getting-started.md"
      provides: "Install-adjacent Phoenix onboarding that preserves the same contract"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Executable guardrails against future install-contract drift"
  key_links:
    - from: "README.md"
      to: "guides/getting-started.md"
      via: "base install and setup wording"
      pattern: "deps|Meilisearch|Oban"
    - from: "README.md"
      to: "test/scrypath/docs_contract_test.exs"
      via: "canonical install snippet assertions"
      pattern: "scrypath|req|oban"
---

<objective>
Remove misleading install guidance from the public docs so the README and install-adjacent onboarding reflect the real consumer contract: add Scrypath, then configure backend/runtime behavior separately.

Purpose: Satisfy DOCS-01 by tightening the first-copy install path without changing the library runtime surface.
Output: Updated README/getting-started wording plus docs contract tests that lock the narrower dependency story.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-public-docs-and-example-safety/09-CONTEXT.md
@.planning/phases/09-public-docs-and-example-safety/09-RESEARCH.md
@.planning/phases/09-public-docs-and-example-safety/09-PATTERNS.md
@README.md
@guides/getting-started.md
@guides/sync-modes-and-visibility.md
@mix.exs
@test/scrypath/docs_contract_test.exs

<interfaces>
From README.md:
```elixir
def deps do
  [
    {:scrypath, "~> 0.1.0"},
    {:req, "~> 0.5"}
  ]
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow the public install snippet to direct Scrypath-only setup per D-01 through D-04</name>
  <files>README.md, guides/getting-started.md</files>
  <read_first>.planning/phases/09-public-docs-and-example-safety/09-CONTEXT.md, .planning/phases/09-public-docs-and-example-safety/09-RESEARCH.md, README.md, guides/getting-started.md, guides/sync-modes-and-visibility.md, mix.exs</read_first>
  <acceptance_criteria>
    - `rg -n '{:scrypath, "~> 0.1.0"}' README.md` finds the canonical Scrypath dependency snippet.
    - `rg -n '{:req, "~> 0.5"}' README.md guides/getting-started.md` returns no matches.
    - `rg -n 'Meilisearch|backend|runtime configuration|sync_mode: :oban' README.md guides/getting-started.md` shows backend and optional integration guidance still exists outside the install fence.
    - `rg -n 'optional|Oban' README.md guides/getting-started.md` shows any queue guidance remains explicitly optional.
  </acceptance_criteria>
  <action>Update `README.md` so the `## Installation` code fence lists only `{:scrypath, "~> 0.1.0"}`. Keep Meilisearch-first positioning explicit, but move transport/backend detail into prose or later setup sections rather than the dependency snippet. In `guides/getting-started.md`, make sure the setup language reinforces that the library owns its internal transport dependency and that `sync_mode: :oban` is an optional production path rather than part of base install. Preserve the existing Quick Path and context-first narrative.</action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs -x</automated>
  </verify>
  <done>The base install path is copy-paste safe and no longer teaches users to pin internal transport dependencies or optional queue setup as mandatory.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the install contract in docs tests so drift is caught automatically</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>.planning/phases/09-public-docs-and-example-safety/09-CONTEXT.md, README.md, guides/getting-started.md, test/scrypath/docs_contract_test.exs</read_first>
  <acceptance_criteria>
    - `rg -n 'req|Oban|optional' test/scrypath/docs_contract_test.exs` finds explicit assertions or refutations covering the transport and optional-queue boundary.
    - `rg -n '{:scrypath, "~> 0.1.0"}' test/scrypath/docs_contract_test.exs` finds the exact install-snippet guard.
    - `mix test test/scrypath/docs_contract_test.exs -x` exits 0.
  </acceptance_criteria>
  <action>Extend `test/scrypath/docs_contract_test.exs` so the README install contract does more than assert Scrypath is present. Add explicit refutations for `{:req, "~> 0.5"}` in the install path, and assert any Oban guidance is clearly optional rather than blended into the base setup. Keep the tests string-exact and scoped to the docs contract rather than adding broader documentation review logic.</action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs -x</automated>
  </verify>
  <done>The install contract is executable and future doc edits that leak internal or optional dependencies into the base path will fail fast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public README/install docs -> external consumers | Users will copy the first dependency snippet verbatim and treat it as the library’s intended support contract. |
| Install docs -> optional runtime integrations | Optional Oban/backend setup can be misread as mandatory if the docs blur the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | `README.md` | mitigate | Keep the install fence limited to `{:scrypath, "~> 0.1.0"}` so internal transport details cannot drift into the public contract. |
| T-09-02 | I | `guides/getting-started.md` | mitigate | Keep backend/runtime/Oban wording explicit and optional so consumers do not over-assume required setup. |
</threat_model>

<verification>
Run `mix test test/scrypath/docs_contract_test.exs -x` after each task and `mix test test/scrypath/docs_contract_test.exs test/support/docs/phoenix_examples_test.exs -x` before writing the summary.
</verification>

<success_criteria>
The README and install-adjacent guides teach the real consumer contract: install Scrypath directly, keep backend/runtime choices explicit, and treat queue integrations as optional.
</success_criteria>

<output>
After completion, create `.planning/phases/09-public-docs-and-example-safety/09-01-SUMMARY.md`
</output>
