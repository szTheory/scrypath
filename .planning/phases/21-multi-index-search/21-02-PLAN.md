---
phase: 21-multi-index-search
plan: 02
type: execute
wave: 2
depends_on:
  - "21-01"
files_modified:
  - lib/scrypath/backend.ex
  - lib/scrypath/meilisearch.ex
  - lib/scrypath/meilisearch/client.ex
  - lib/scrypath/meilisearch/federated_decode.ex
  - test/support/fake_backend.ex
  - test/scrypath/meilisearch/client_multi_search_test.exs
autonomous: true
requirements:
  - MULTI-08
  - MULTI-12
tags:
  - backend
  - meilisearch
  - multi-search
must_haves:
  truths:
    - "`Scrypath.Backend` declares `@optional_callback search_many/3` returning `{:ok, map()}` | `{:error, term()}`; default impl in `Scrypath.Search` (Plan 03) falls back to sequential `search/3` (MULTI-12)."
    - "`Scrypath.Meilisearch` implements `search_many/3` using `POST /multi-search` with `queries` array and `federation` object; request JSON **never** contains the string `mergeFacets` (MULTI-08)."
    - "`FederatedDecode` (or equivalent) maps `federation.facetsByIndex` + index order back to per-schema raw hit maps consumable by existing `SearchResult` facet decoders."
  artifacts:
    - path: "lib/scrypath/meilisearch/client.ex"
      provides: "multi_search HTTP"
      contains: "multi-search"
    - path: "lib/scrypath/meilisearch/federated_decode.ex"
      provides: "facetsByIndex unpack"
      contains: "facetsByIndex"
  key_links:
    - from: "search_many/3"
    - to: "Client.run_request"
    - via: "same auth/base URL as single search"
    - pattern: "reuse run_request/5"
---

<objective>
Extend the **internal backend seam** and **Meilisearch HTTP path** for federated search: optional `search_many/3`, native `/multi-search` client, deterministic decoding of federated response (hits + `facetsByIndex`) into per-schema maps aligned with existing `%SearchResult{}` construction expectations. Prepare `FakeBackend.search_many/3` for Plan 03 orchestration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/21-multi-index-search/21-CONTEXT.md
@.planning/phases/21-multi-index-search/21-RESEARCH.md
@.planning/phases/21-multi-index-search/21-PATTERNS.md
@.planning/research/deep/MULTI_INDEX.md
@lib/scrypath/search_result/facets.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: `Backend` optional callback + compile-time discovery helper</name>
  <files>lib/scrypath/backend.ex</files>
  <read_first>
    - lib/scrypath/backend.ex (entire file)
    - lib/scrypath/meilisearch.ex (existing @impl callbacks)
  </read_first>
  <action>
    1. Add `@callback search_many([{module(), Scrypath.Query.t()}], keyword()) :: {:ok, map()} | {:error, term()}` **and** `@optional_callbacks [search_many: 3]` after existing callbacks. Moduledoc note: return map is **raw** Meilisearch multi-search JSON (string keys) before `Search` decorates to structs.
    2. Run `mix format` on the file; ensure `mix compile --warnings-as-errors` still passes — optional callbacks must not break existing `@behaviour` implementors (only `FakeBackend` and `Meilisearch` in-repo; both updated in this plan).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'search_many' lib/scrypath/backend.ex` returns ≥ 2 lines (callback + optional_callbacks entry).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    Backend behaviour documents the extension point for MULTI-12.
  </done>
</task>

<task type="auto">
  <name>Task 2: `Client.multi_search/2` + `Meilisearch.search_many/3`</name>
  <files>lib/scrypath/meilisearch/client.ex, lib/scrypath/meilisearch.ex</files>
  <read_first>
    - lib/scrypath/meilisearch/client.ex (`run_request/5`, `search/3` payload style)
    - .planning/research/deep/MULTI_INDEX.md (federation object fields)
  </read_first>
  <action>
    1. In `Client`, add `multi_search(payload, config)` where `payload` is a map with string keys `"queries"` (list of per-index search bodies) and optional `"federation"` map built by caller. POST to `"/multi-search"` using the same `run_request` path as other endpoints.
    2. In `Scrypath.Meilisearch`, add `@impl` `search_many/3` that accepts `[{schema, %Query{}}]` + config, builds `indexUid` per schema via `index_name/2`, maps each `Query` through existing `Meilisearch.Query` JSON encoder used by single search (reuse `search_payload` pipeline — grep `search_payload` in client).
    3. Construct federation options from shared config keys (`federation_limit`, `federation_offset`) mapped to Meilisearch field names per pinned API — **do not** add `"mergeFacets"` key under any code path.
    4. Return `{:ok, body}` as decoded JSON map from Req.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'multi-search' lib/scrypath/meilisearch/client.ex` returns ≥ 1 line.
    - `grep -n 'mergeFacets' lib/scrypath/meilisearch` exits 1 (no matches in directory).
    - `grep -n 'def search_many' lib/scrypath/meilisearch.ex` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    Live `/multi-search` call path exists on Meilisearch backend module.
  </done>
</task>

<task type="auto">
  <name>Task 3: `FederatedDecode` + `FakeBackend.search_many/3`</name>
  <files>lib/scrypath/meilisearch/federated_decode.ex, test/support/fake_backend.ex, test/scrypath/meilisearch/client_multi_search_test.exs</files>
  <read_first>
    - lib/scrypath/search_result.ex (`new/4` expectations on raw map)
    - lib/scrypath/search_result/facets.ex
    - test/support/fake_backend.ex
  </read_first>
  <action>
    1. Add `lib/scrypath/meilisearch/federated_decode.ex` with a pure function `per_schema_maps(response, schemas_in_order)` returning `{:ok, [{schema, raw_map}]}` list aligned to declaration order, where each `raw_map` mimics a **single-index** search response map (hits, facetDistribution, etc.) reconstructed from federated `hits` + `facetsByIndex` entry for that schema’s `indexUid`. Document assumptions in moduledoc (string vs atom keys consistent with `SearchResult.new`).
    2. Extend `FakeBackend` with optional `@impl` `search_many/3` returning `{:ok, %{"results" => ...}}` **or** the federated shape your decode expects — pick one minimal stub sufficient for Plan 03 unit tests; must return deterministic hits when queries list length > 0.
    3. Add `test/scrypath/meilisearch/client_multi_search_test.exs` using `Req.Test` stub: assert POST path ends with `/multi-search`; assert JSON body has `"queries"` list length matching input; refute body contains substring `mergeFacets` (parse JSON to string round-trip or `Jason.encode!/1` on body map).
  </action>
  <verify>
    <automated>mix test test/scrypath/meilisearch/client_multi_search_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/meilisearch/client_multi_search_test.exs` exits 0.
    - `grep -n 'def search_many' test/support/fake_backend.ex` returns ≥ 1 line.
    - `grep -n 'def per_schema_maps' lib/scrypath/meilisearch/federated_decode.ex` returns ≥ 1 line OR the module’s primary public function name you chose appears once in moduledoc + def.
  </acceptance_criteria>
  <done>
    Decode path and test double exist; MULTI-08 wire prerequisites satisfied before orchestration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config → Meilisearch URL | Same as single search — network trust boundary unchanged. |
| Raw JSON → structs | Decoder must tolerate missing optional keys without crashing (graceful `Map.get`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Information Disclosure | Logging request bodies | accept | Do not add `Logger.info` of full payloads in this plan; keep existing telemetry discipline. |
| T-21-02-02 | Tampering | MITM on Meilisearch | mitigate | Reuse TLS + URL validation from existing `Client` configuration. |

</threat_model>

<verification>
- `mix test test/scrypath/meilisearch/client_multi_search_test.exs` exits 0.
- `mix compile --warnings-as-errors` exits 0.
- `grep -r 'mergeFacets' lib/scrypath/meilisearch` exits 1.
</verification>

<success_criteria>
- MULTI-12: optional callback + Meilisearch implementation + sequential fallback deferred explicitly to Plan 03 in `Search` module.
- MULTI-08: decoding + Req.Test prove `mergeFacets` absent from wire format.
</success_criteria>
