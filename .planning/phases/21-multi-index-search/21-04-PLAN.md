---
phase: 21-multi-index-search
plan: 04
type: execute
wave: 4
depends_on:
  - "21-03"
files_modified:
  - guides/multi-index-search.md
  - mix.exs
  - test/scrypath/docs_contract_test.exs
  - test/scrypath/search_many_integration_test.exs
  - README.md
autonomous: true
requirements:
  - MULTI-08
  - MULTI-11
tags:
  - documentation
  - liveview
  - integration
must_haves:
  truths:
    - "`guides/multi-index-search.md` exists, is listed in `mix.exs` `:docs` extras, and appears in `DocsContractTest` `@guide_paths` (MULTI-11)."
    - "Guide contains a worked **4-schema** LiveView dashboard narrative, `search_many/2` call site, per-schema facets, partial-failure banner with `aria-live=\"polite\"`, cross-links to `guides/faceted-search-with-phoenix-liveview.md` and `guides/sync-modes-and-visibility.md`, and a **duplicate schema** subsection mandating `ordered` iteration (CONTEXT D-01, D-03, D-06)."
    - "Integration test (tagged `:integration`) proves facet map / `%SearchResult.Facets{}` equality between solo `Scrypath.search/3` and `search_many/2` for the same schema + opts (MULTI-08)."
  artifacts:
    - path: "guides/multi-index-search.md"
      provides: "MULTI-11 narrative"
      contains: "search_many"
    - path: "test/scrypath/search_many_integration_test.exs"
      provides: "live facet parity"
      contains: "search_many"
  key_links:
    - from: "integration test"
    - to: ".github/workflows/ci.yml"
    - via: "SCRYPATH_INTEGRATION"
    - pattern: "reuse Phase 20 integration gating"
---

<objective>
Ship **documentation and verification closure** for Phase 21: the multi-index guide aligned with `21-UI-SPEC.md`, ExDoc wiring, docs contract coverage, README wayfinding, and a **thin** live Meilisearch integration test proving MULTI-08 facet parity against solo search.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-multi-index-search/21-UI-SPEC.md
@.planning/phases/21-multi-index-search/21-CONTEXT.md
@.planning/REQUIREMENTS.md
@guides/faceted-search-with-phoenix-liveview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `guides/multi-index-search.md` + ExDoc extras</name>
  <files>guides/multi-index-search.md, mix.exs</files>
  <read_first>
    - guides/faceted-search-with-phoenix-liveview.md (tone, heading depth, LiveView patterns)
    - .planning/phases/21-multi-index-search/21-UI-SPEC.md (colors, banner, spacing tokens)
    - mix.exs (`extras:` list near ExDoc)
  </read_first>
  <action>
    1. Create `guides/multi-index-search.md` with: H1 title; **primary** federated dashboard example (four schema sections, independent `text` / `filter:` / `page:` / `facets:` per tuple); **secondary** short “same `q` everywhere” recipe with explicit warning that relevance scores are not comparable across schemas.
    2. Include HEEx snippets for partial-failure banner using `aria-live="polite"` (not `role="alert"` unless whole page blocked), `<details>` for diagnostics, and `user_message(schema, reason)` helper pattern per CONTEXT D-06.
    3. Add “Duplicate schema in one call” subsection showing `for {schema, result} <- results.ordered` — warn `by_schema` is last-wins.
    4. Cross-link with relative paths: `faceted-search-with-phoenix-liveview.md`, `sync-modes-and-visibility.md`.
    5. Append short anti-pattern list (merged hits illusion, `mergeFacets`, silent truncation).
    6. Add the guide path to `mix.exs` `:docs` → `extras:` in alphabetical position among other guides.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test -f guides/multi-index-search.md` exits 0.
    - `grep -n 'multi-index-search.md' mix.exs` returns ≥ 1 line.
    - `grep -n 'search_many' guides/multi-index-search.md` returns ≥ 3 lines.
    - `grep -n 'aria-live' guides/multi-index-search.md` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    MULTI-11 content and ExDoc discoverability landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: `DocsContractTest` + README wayfinding</name>
  <files>test/scrypath/docs_contract_test.exs, README.md</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (`@guide_paths`, guide assertion style)
    - README.md (guide list section if present)
  </read_first>
  <action>
    1. Insert `"guides/multi-index-search.md"` into `@guide_paths` in correct lexical order.
    2. Add a focused test (or extend existing guide bundle test) asserting the new guide contains all substrings: `Scrypath.search_many`, `%Scrypath.MultiSearchResult{}`, `failures`, `ordered`, `faceted-search-with-phoenix-liveview.md`, `sync-modes-and-visibility.md`.
    3. In `README.md`, add a bullet or table row under the Phoenix / search guides list pointing to **Multi-index search** with the path `guides/multi-index-search.md` (match existing README markdown style exactly).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/docs_contract_test.exs` exits 0.
    - `grep -n 'multi-index-search.md' README.md` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    Contracts prevent guide drift; README surfaces the new doc.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integration test MULTI-08 facet parity</name>
  <files>test/scrypath/search_many_integration_test.exs</files>
  <read_first>
    - test/scrypath/search_test.exs or Phase 20 integration tests for `@moduletag :integration` pattern
    - .github/workflows/ci.yml (`SCRYPATH_INTEGRATION` env)
    - test/support/facetable_movie.ex or facet-capable schemas available for search
  </read_first>
  <action>
    1. Create `test/scrypath/search_many_integration_test.exs` with `@moduletag :integration` and runtime skip unless `System.get_env("SCRYPATH_INTEGRATION") == "1"` (copy exact predicate from existing integration tests).
    2. For one schema with `facets:` enabled, run `Scrypath.search(schema, text, opts)` and capture `%SearchResult{}` facets field.
    3. Run `Scrypath.search_many([{schema, text, opts}], shared_opts)` with a singleton list and identical effective opts; assert `result.by_schema[schema].facets == solo.facets` using **struct equality** or `==` on decoded facet maps as documented in MULTI-08.
    4. Keep test count ≤ 4 in this file for Phase 21 scope.
  </action>
  <verify>
    <automated>mix test test/scrypath/search_many_integration_test.exs --exclude integration</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/search_many_integration_test.exs --exclude integration` exits 0 (tests skip, not fail).
    - `grep -n 'SCRYPATH_INTEGRATION' test/scrypath/search_many_integration_test.exs` returns ≥ 1 line.
    - `grep -n 'search_many' test/scrypath/search_many_integration_test.exs` returns ≥ 2 lines.
  </acceptance_criteria>
  <done>
    MULTI-08 has a live parity hook; default CI stays green via `--exclude integration`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide examples | Static documentation — no runtime user input in samples. |
| Integration test | Hits real Meilisearch only in gated CI / local opt-in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Information Disclosure | Example API keys in markdown | mitigate | Use placeholders only (`YOUR_MEILISEARCH_URL` style) — never paste secrets. |

</threat_model>

<verification>
- `mix test test/scrypath/docs_contract_test.exs` exits 0.
- `mix test --exclude integration` exits 0 for full project.
- `mix format --check-formatted` exits 0 on touched Elixir files.
</verification>

<success_criteria>
- MULTI-11: guide + contracts + README.
- MULTI-08: integration parity test file exists with correct gating.
</success_criteria>
