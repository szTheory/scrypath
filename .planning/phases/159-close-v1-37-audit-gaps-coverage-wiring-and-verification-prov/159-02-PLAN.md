---
phase: 159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov
plan: "02"
type: execute
wave: 2
depends_on: [159-01]
files_modified:
  - .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-HISTORICAL-PROBES.md
  - .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-EVIDENCE-MATRIX.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-07/D-09: every retrospective claim is assigned exactly one allowed evidence class, and current tests, filenames, messages, or same-commit tests never prove earlier chronology."
    - "D-08: a historically proven characterization claim names an immutable extraction parent where the relevant contract test existed and passed in a detached worktree."
    - "D-10/D-11: fresh checks are date/SHA/environment-bound present-state evidence; unresolved TEST-01 chronology remains historically unprovable with a narrow waiver and the requirement unchanged."
    - "D-12: the matrix preserves the prospective test-only-commit-before-extraction convention without adding a permanent enforcement system."
    - "D-13/D-16: one canonical 31-row Markdown matrix preserves original Phase 148–158 ownership and does not make Phase 159 the requirement owner."
  artifacts:
    - path: .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-HISTORICAL-PROBES.md
      provides: "Bounded immutable Git topology inventory and exact parent-probe receipts"
      contains: "historically unprovable"
    - path: .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-EVIDENCE-MATRIX.md
      provides: "Canonical 31-row requirement-to-evidence source of truth"
      contains: "Evidence class"
  key_links:
    - from: .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-HISTORICAL-PROBES.md
      to: .git
      via: "resolved extraction and parent commit IDs plus detached-worktree test presence/result"
      pattern: "Extraction SHA.*Parent SHA"
    - from: .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-EVIDENCE-MATRIX.md
      to: .planning/REQUIREMENTS.md
      via: "all 31 original IDs appear exactly once with their original owning phase"
      pattern: "Requirement.*Original owning phase"
    - from: .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-EVIDENCE-MATRIX.md
      to: .planning/QUALITY-LEDGER.md
      via: "implementation and present-state evidence is checked against the existing quality ledger, not copied blindly"
      pattern: "Implementation commit / source"
---

<objective>
Reconstruct only the chronology Git can actually prove, then establish the single canonical 31-row evidence matrix required by D-07–D-13 and D-16.

Purpose: Repair verification provenance without laundering current behavior into historical process claims or reassigning the milestone's original requirements.
Output: A bounded Git-forensics receipt and a canonical Markdown matrix consumed by all later retrospective indexes, validations, hosted evidence, and the final audit.

Phase 159 has no new requirement IDs; `requirements: []` is deliberate. The 31 original IDs remain owned exactly once by Phases 148–158 per D-16.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/QUALITY-LEDGER.md
@.planning/v1.37-MILESTONE-AUDIT.md
@.planning/phases/148-quality-baseline/148-CONTEXT.md
@.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-CONTEXT.md
@.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-RESEARCH.md
@.planning/phases/136-milestone-verification-uat-s-g/136-DUALVERIFY-REPORT.md
@.planning/milestones/v1.36-phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory immutable extraction topology and run bounded parent probes</name>
  <reversibility rating="reversible">The probe receipt is evidence-only and disposable worktrees do not alter repository history.</reversibility>
  <files>.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-HISTORICAL-PROBES.md</files>
  <read_first>
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-CONTEXT.md
    .planning/phases/148-quality-baseline/148-CONTEXT.md
    .planning/QUALITY-LEDGER.md
    .planning/v1.37-MILESTONE-AUDIT.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-PATTERNS.md
  </read_first>
  <action>Before assigning any D-07 evidence class, build `159-HISTORICAL-PROBES.md` from a finite candidate inventory derived from the Phase 148–158 ledger entries, actual production/extraction diffs, and requirement ownership. For each candidate record requirement(s), extraction commit, resolved parent commit, production paths, candidate observable-contract test path/selector, why that test is discriminating, and a bounded probe command. Create each probe under a unique `mktemp -d` parent outside the primary checkout, use `git worktree add --detach` at the fully resolved parent SHA, record `git ls-files --error-unmatch` for the exact test, run only the focused pre-existing contract command with warning-fatal settings, record toolchain/date/exit, confirm the detached tree remains clean, and remove only the validated registered probe worktree. Never scan or execute unbounded history; cap work to the finite extraction inventory and record skipped/non-reproducible cases explicitly. Classify as `historically proven` only when both test presence and passing parent execution are recorded. If the test is absent, lands with production, the parent cannot be reproduced, or evidence is ambiguous, classify the claim `historically unprovable`; do not infer from messages, filenames, current tests, later receipts, or implementation intent per D-08/D-09. Record the prospective D-12 convention as review guidance, not a new gate.</action>
  <acceptance_criteria>
    - The receipt begins with primary-worktree status/hash facts and ends with the same user-owned dirty paths preserved; it records no branch change or primary-checkout command execution against an old revision.
    - Every candidate row has a fully resolved extraction SHA and parent SHA, exact test path/selector, presence result, focused command, UTC timestamp, Elixir/OTP/Mix environment, exit/result, one D-07 class, and limitation.
    - Every `historically proven` row contains both a successful parent `ls-files` result and a passing focused test at that parent; every other chronology row is fail-closed to `historically unprovable` per D-08/D-09.
    - Probe count is bounded to the enumerated extraction inventory, every created worktree is removed through a validated explicit path, and `git worktree list --porcelain` shows no leaked probe registration.
    - The TEST-01 requirement text is quoted unchanged and each unresolved portion is mapped to a narrow waiver candidate rather than a synthetic pass per D-11.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-HISTORICAL-PROBES.md &amp;&amp; git worktree list --porcelain &amp;&amp; git status --short</automated>
  </verify>
  <done>Immutable topology and focused parent results—not narrative inference—determine every historical characterization classification.</done>
</task>

<task type="auto">
  <name>Task 2: Build the canonical 31-row requirement-to-evidence matrix</name>
  <reversibility rating="costly">Changing the canonical matrix schema later requires coordinated edits to 33 phase-local indexes and the milestone audit.</reversibility>
  <files>.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-EVIDENCE-MATRIX.md</files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/QUALITY-LEDGER.md
    .planning/v1.37-MILESTONE-AUDIT.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-HISTORICAL-PROBES.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-CONTEXT.md
    .planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-RESEARCH.md
    .planning/phases/136-milestone-verification-uat-s-g/136-ARTIFACT-MANIFEST.json
    .planning/milestones/v1.36-phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-VERIFICATION.md
    .planning/milestones/v1.36-phases/146-scrypathops-web-client-remediation/146-VERIFICATION.md
    .planning/milestones/v1.36-phases/147-ecommerce-mounted-ops-remediation-and-closure-evidence/147-VERIFICATION.md
  </read_first>
  <action>Create `159-EVIDENCE-MATRIX.md` as the only canonical D-13 source. Include exactly one row for each of the 31 IDs in `.planning/REQUIREMENTS.md`, preserving the original owning phase from traceability. Each row must include requirement ID/text pointer, original phase, implementation commit and source paths, relevant test paths and commands, exactly one D-07 evidence class, exact SHA/date/environment or committed/hosted receipt reference supporting that class, limitation, and disposition. Reconcile every row against actual Git/source/tests and `.planning/QUALITY-LEDGER.md`; do not promote ledger prose without a concrete reference. Use `159-HISTORICAL-PROBES.md` exclusively for chronology claims. Mark fresh Phase 159 commands `present-state verified`; use `supported by prior committed evidence` only for claims the cited earlier immutable receipt actually records. Preserve TEST-01 verbatim and, for every irrecoverable chronology portion, record `historically unprovable` plus a narrow D-11 waiver; do not convert it to a pass. Add a short D-12 prospective convention section. Keep Markdown as the sole format and declare that later phase-local files are indexes, not alternate sources, per D-13/D-16/D-17.</action>
  <acceptance_criteria>
    - The matrix has exactly 31 requirement rows and the set of row IDs equals the set of `**ID**` entries in `.planning/REQUIREMENTS.md` with no duplicate, missing, or extra ID.
    - Every row's original phase equals `.planning/REQUIREMENTS.md` traceability and no row assigns Phase 159 ownership per D-16.
    - Every row contains nonempty implementation/source, tests/commands, evidence-class, provenance, limitation, and disposition cells; evidence class is exactly one of D-07's four literals.
    - TEST-01 retains its source wording, references concrete parent probes, and any unproven sequencing has an explicit narrow waiver without `passed`/`satisfied` historical language per D-09/D-11.
    - The document states that fresh results prove the recorded SHA/date only and that phase-local retrospective records must link here rather than duplicate its rows per D-10/D-15.
  </acceptance_criteria>
  <verify>
    <automated>mix run -e 'reqs = File.read!(".planning/REQUIREMENTS.md") |&gt; then(&amp;Regex.scan(~r/^\- \[x\] \*\*([A-Z]+\-\d+)\*\*:/m, &amp;1, capture: :all_but_first)) |&gt; List.flatten() |&gt; Enum.sort(); matrix = File.read!(".planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-EVIDENCE-MATRIX.md"); rows = Regex.scan(~r/^\| ((?:SAFE|ARCH|TEST|CI|PERF|QUAL)\-\d+) \|/m, matrix, capture: :all_but_first) |&gt; List.flatten() |&gt; Enum.sort(); if length(reqs) != 31 or length(rows) != 31 or reqs != rows, do: System.halt(1)'</automated>
  </verify>
  <done>One truthful matrix covers all 31 original requirements while keeping historical, current, prior-committed, and unprovable evidence distinct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dirty primary checkout → detached historical worktree | User-owned current changes must never enter old-revision probes. |
| Git topology/test results → evidence classification | Immutable facts cross into human-readable claims and waivers. |
| existing ledgers/receipts → canonical matrix | Prior prose may be incomplete and must be validated against source/history. |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-05 | Tampering | detached worktrees / primary checkout | high | mitigate | Use unique temp roots, fully resolved SHAs, registered-worktree validation, clean-tree checks, explicit removal targets, and before/after primary status preservation. |
| T-159-06 | Repudiation | historical chronology claims | high | mitigate | Require parent test presence plus passing parent execution for `historically proven`; all ambiguity fails closed. |
| T-159-07 | Tampering / Repudiation | canonical matrix | high | mitigate | Reconcile the 31-ID set and original ownership mechanically; require concrete immutable references and one allowed class per row. |
| T-159-08 | Denial of Service | Git history probes | medium | mitigate | Enumerate a finite extraction candidate list first, run only focused tests, and prohibit unbounded revision scans/suite runs. |
| T-159-09 | Information Disclosure | recorded commands/output | medium | mitigate | Record versions, exits, hashes, and bounded diagnostics; exclude environment secrets, credentials, and full raw logs. |
</threat_model>

## Artifacts this phase produces

- `159-HISTORICAL-PROBES.md`: finite extraction inventory, exact parent topology, focused detached probe commands/results, evidence class, limitation, and waiver candidate fields.
- `159-EVIDENCE-MATRIX.md`: canonical 31-row Markdown schema with original owner, implementation/source, test/command, evidence class, provenance, limitation, and disposition columns.
- Permanent enforcement daemon, JSON manifest, invented phase plans, rewritten requirements, and Phase 159 ownership: none per D-12/D-16/D-17.

<verification>
Run the 31-ID set-equality check, inspect all historical-proof rows for both parent-test predicates, confirm every probe worktree was cleaned up, and confirm the primary working tree retains all pre-existing user-owned paths.
</verification>

<success_criteria>
- The canonical matrix covers every original requirement exactly once under its original phase.
- No present-state or same-commit evidence is described as pre-extraction characterization.
- TEST-01 remains unchanged and any irrecoverable chronology is explicit, narrow, and auditable.
</success_criteria>

<output>
Create `.planning/phases/159-close-v1-37-audit-gaps-coverage-wiring-and-verification-prov/159-02-SUMMARY.md` when done.
</output>
