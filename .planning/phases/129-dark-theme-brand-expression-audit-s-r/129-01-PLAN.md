---
phase: 129-dark-theme-brand-expression-audit-s-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md
autonomous: true
requirements:
  - DARKAUDIT-01

must_haves:
  truths:
    - "129-DARK-AUDIT-BACKLOG.md exists in the phase directory and is committed"
    - "Finding #1 is the #1B2230 surface-2 ramp gap, explicitly labeled finding #1"
    - "Every dark surface is scored on all six DD1–DD6 dimensions with 0–3 scale (0=worst)"
    - "All DD6 AA values are promoted from 128-CONTRAST-REPORT.md, not re-derived"
    - "Three severity tables exist: Blockers / Structural / Polish, in that order"
    - "Every finding row carries non-empty Phase (130–135) and Req columns from the REQUIREMENTS.md §13 map"
    - "Executive summary reports counts by severity, by fix-class, and systemic promotion count"
    - "No source file outside the phase directory is modified"
  artifacts:
    - path: ".planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md"
      provides: "Ranked dark-expression backlog driving phases 130–135"
      contains: "# Phase 129"
    - path: ".planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md"
      provides: "Three severity tables"
      contains: "### Blockers"
    - path: ".planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md"
      provides: "DD6 AA spine promoted from 128"
      contains: "DD6"
  key_links:
    - from: "129-DARK-AUDIT-BACKLOG.md DD6 column"
      to: "128-CONTRAST-REPORT.md"
      via: "Promote not re-derive"
      pattern: "128-CONTRAST-REPORT"
    - from: "Phase/Req columns"
      to: "REQUIREMENTS.md §13 traceability table"
      via: "req→phase map authority"
      pattern: "DARKTOKEN-01|GLOW-01|COPPER-01|A11Y-TOKEN-01|DARKMOTION-01|SCREEN-DARK-01"
---

<objective>
Produce `129-DARK-AUDIT-BACKLOG.md` — a ranked, fix-class-tagged dark-brand-expression backlog
that scores every dark surface of the scrypath_ops admin UI on DD1–DD6 dimensions, promotes
the 128-CONTRAST-REPORT.md AA spine into DD6/Sev/Fix-class/Scope without re-deriving, overlays
brand-expression findings (DD1–DD5) from the 40-shot screenshot matrix and brand book, and
emits a single authoritative backlog (mirroring 120-AUDIT-BACKLOG.md's shape) that phases
130–135 will filter to drive their work.

Purpose: This is the pivot document for the entire v1.34 milestone. Without it, phases 130–135
have no ranked, routed, evidence-backed starting point. The #1B2230 surface-2 ramp gap must be
confirmed and anchored as finding #1.

Output: One committed markdown file — 129-DARK-AUDIT-BACKLOG.md — in the phase directory.
No source file changes.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-dark-theme-brand-expression-audit-s-r/129-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Score every dark touchpoint on DD1–DD6 across the 6-screen × dark-viewport matrix</name>

  <read_first>
    1. CONTROLLING METHODOLOGY: `.planning/phases/129-dark-theme-brand-expression-audit-s-r/129-CONTEXT.md`
       — All decisions D-01 through D-13 are locked. Read the full `<decisions>` block before scoring anything.
    2. AA SPINE (promote, do not re-derive): `.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md`
       — 108 AA violations, 3 systemic clusters (`.leading-4` 1.08:1; dark form inputs 1.19:1; `.bg-primary` 4.3:1),
       pre-tagged scope/fix_class, finding #1 anchor, and the prioritized fix list mapping clusters to 130–132.
       This is the objective DD6 data. Copy it, do not recompute it.
    3. VISUAL SUBSTRATE FOR DD1–DD5: `.tmp/admin-screenshots/` — all 40 shots.
       The 20 dark shots are: `00-control-room--dark--{desktop,mobile}--incident`,
       `01-posture--dark--{desktop,mobile}--incident`,
       `02-failed-sync--dark--{desktop,mobile}--populated`,
       `03-sync-drift--dark--{desktop,mobile}--drift`,
       `04-control-room--dark--{desktop,mobile}--all-green`,
       `05-posture--dark--{desktop,mobile}--all-green`,
       `06-search--dark--{desktop,mobile}--results`,
       `07-failed-sync--dark--{desktop,mobile}--empty`,
       `08-search--dark--{desktop,mobile}--zero-results`,
       `09-playbooks--dark--{desktop,mobile}--empty-workspace`.
       Read BOTH desktop (1440) and mobile (390) shots for DD1–DD5 (D-05: brand gaps are visible
       on desktop where AA passes). DD6 inherits 128's per-viewport AA result.
    4. SCORING RUBRIC: `prompts/scrypath-brand-book.md`
       — The dark-signature rules. Sections to key on:
         * 4-step midnight ramp: Night #0C0F14 (bg) → Ink #141923 (surface) → #1B2230 (surface-2/raised) → Slate #2A3446 (border/divide)
         * 65/20/10/5 ratio: neutrals 65% / structural/interactive 20% / violet primary 10% / copper accent 5%
         * Glow: quiet/low-spread glow reserved for route/diagram lines and key hover — never text, resting panels, or bg floods
         * Ambient shadow + border: dark panels should read as seated depth via a dark ambient shadow plus the existing border; not flat fills
         * Path-line glow restraint: the violet route/diagram-line glow stays reserved for path/route elements only
         * AA/AAA targets (the objective gate — fed from 128)
         * Paper-on-dark typography norms, copper as 5% accent role
    5. TOKEN SOURCE (read-only for evidence citations): `scrypath_ops/assets/css/app.css`
       — The two daisyUI theme blocks (search for `[data-theme="dark"]` and `@media (prefers-color-scheme: dark)`),
       the dark `@custom-variant dark` block, the `color-mix(... base-content N%, transparent)` muted patterns.
       Needed for `file:line` evidence citations like `app.css:614` in the Evidence column.
    6. TOKEN DOC (read-only for ramp gap evidence): `scrypath_ops/assets/css/DESIGN-TOKENS.md`
       — Confirms which elevation tokens are defined/missing, the surface-2 gap, the dark ramp state.
    7. COMPONENT SOURCE (read-only for touchpoint citations): `scrypath_ops/lib/scrypath_ops/components/ops_ui.ex`
       — The `.ops-*` components rendered on each dark surface. Needed to name which component
       renders on which screen in the Evidence column (e.g. `ops_ui.ex:941 ops_code_block`).
  </read_first>

  <files>
    (internal analysis work only — no file written yet; findings captured in memory for Task 2)
  </files>

  <action>
    Perform a full per-touchpoint dark-brand scoring pass. Produce a structured findings list
    (held in-context for Task 2 assembly) covering all 6 screens in both dark viewports.

    DIMENSION DEFINITIONS (D-01, locked):
    - DD1: 4-step midnight ramp adherence — does the surface honor #0C0F14 → #141923 → #1B2230 → #2A3446
      elevation steps, or does "raised" flatten/darken instead of lifting?
    - DD2: 65/20/10/5 neutral/structure/violet/copper ratio adherence — is the dark surface
      dominated by the correct midnight-neutral palette, with violet and copper at restrained ratios?
    - DD3: quiet-vs-loud glow — is any glow present restrained and low-spread (OK), or is it loud,
      flooding backgrounds or text (fail)?
    - DD4: ambient-shadow plus border depth — do dark panels read as seated depth (ambient dark shadow
      + border), or are they flat fills without depth cues?
    - DD5: path-line glow restraint — is the violet glow reserved for route/diagram/path lines and
      key hover states, or applied decoratively/broadly?
    - DD6: AA pass/fail — PROMOTE DIRECTLY from 128-CONTRAST-REPORT.md. Do not independently
      measure or re-derive any ratio. Copy the cluster membership, ratio, and scope tags verbatim.

    SCORING SCALE (D-02): 0 = worst (completely fails the dimension), 1 = partial/marginal,
    2 = adequate/near-complete, 3 = fully satisfies the dimension.

    SEVERITY RULES (D-06 through D-10):
    - Any finding where DD6 fails → Sev: blocker (automatic, per D-07)
    - DD1–DD5 findings: if same token/recipe fails on ≥3 screens → Scope: systemic → Sev: structural
    - DD1–DD5 findings: if single screen → Scope: per-screen → Sev: polish
    - Sort key (D-09): severity (blocker→structural→polish) → scope (systemic first) → score (0 worst first) → dimension (DD1…DD6)

    FINDING #1 ANCHOR (D-10): The #1B2230 surface-2 ramp gap is finding #1 by construction —
    it is simultaneously the .leading-4 systemic AA blocker (DD6, Cluster 1 from 128) AND a
    DD1 ramp violation AND a DD4 depth violation on ≥3 screens. Label it explicitly as #1.

    REQ→PHASE ROUTING MAP (D-11, D-12 — authority is REQUIREMENTS.md §13):
    - DARKTOKEN-01 → Phase 130 (surface ramp, depth tokens)
    - GLOW-01 → Phase 131 (glow, dark shadow)
    - COPPER-01 → Phase 131 (copper accent system)
    - A11Y-TOKEN-01 → Phase 132 (AA contrast remediation)
    - DARKMOTION-01 → Phase 133 (dark/path motion)
    - SCREEN-DARK-01 → Phase 134 (under-iterated surface polish)
    - SHELL-DARK-01 → Phase 135 (shell chrome polish)

    FIX-CLASS VOCABULARY (from 120 + 128 schema): token | component | screen | motion | seed

    SCREENS TO SCORE (6 screens, dark theme, both viewports):
    00/04: Control Room (incident + all-green states)
    01/05: Posture (incident + all-green states)
    02/07: Failed Sync (populated + empty states)
    03: Sync/Drift (drift state)
    06/08: Search (results + zero-results states)
    09: Playbooks (empty-workspace state)

    For each screen + dark viewport combination, score DD1–DD5 by inspecting the screenshots and
    cross-referencing app.css and ops_ui.ex for file:line evidence. For DD6, import the 128 cluster
    and per-screen data directly.

    The Evidence cell MUST contain: screenshot slug (NN-screen--dark--viewport--state) + at least
    one file:line reference to app.css or ops_ui.ex + 128-report reference for any DD6 finding.

    Do NOT add "deferred" findings for any of the items in 129-CONTEXT.md `<deferred>` section
    (actual fixes, AAA remediation, light-theme polish).
  </action>

  <verify>
    Internal analysis step — no file output yet. Verification is at Task 2 completion.
    Mental check: Can enumerate at minimum 3 blocker findings (the 3 clusters from 128),
    at minimum 2 structural DD1/DD4 findings (ramp flatness and depth absence systemic across
    screens), and per-screen polish items for DD2/DD3/DD5 gaps per screen. Systemic set should
    yield ≥3 promotions.
  </verify>

  <acceptance_criteria>
    - In-context findings list covers all 6 screens in dark theme at both desktop 1440 and mobile 390 viewports
    - Every DD6 finding maps 1:1 to a row or cluster in 128-CONTRAST-REPORT.md with the same ratio/scope/fix_class values
    - Every finding has a proposed screenshot slug in Evidence (correct NN-screen--dark--viewport--state format)
    - Every finding has at least one file:line citation from app.css or ops_ui.ex
    - The #1B2230 finding is identified as finding #1 and tagged blocker + systemic + DD1+DD4+DD6
    - Systemic promotion count is known (same token/recipe failing ≥3 screens)
    - No finding in the list proposes a code/token/CSS fix as deliverable — only names the gap and proposed fix text
    - All Phase/Req cells use only values from the req→phase map above
  </acceptance_criteria>

  <done>
    In-context findings list ready for Task 2 assembly: all 6 screens scored DD1–DD6,
    DD6 promoted from 128, #1B2230 confirmed as finding #1, systemic count known,
    Phase/Req routing filled per REQUIREMENTS.md §13.
  </done>
</task>

<task type="auto">
  <name>Task 2: Assemble and write 129-DARK-AUDIT-BACKLOG.md mirroring 120-AUDIT-BACKLOG.md format</name>

  <read_first>
    1. FORMAT EXEMPLAR: `.planning/milestones/v1.33-phases/120-per-touchpoint-audit/120-AUDIT-BACKLOG.md`
       — Mirror this structure EXACTLY: YAML/metadata header, Executive summary (totals by severity
       + by fix-class + systemic promotion count + "plan-hypothesis verification" section if useful),
       three severity tables (Blockers / Structural / Polish) with identical column headers, Systemic
       cluster analysis section, Prioritized fix list. Column headers must be:
       ID · Alt · Touchpoint · Dim · Score · Sev · Scope · Evidence · Proposed fix · Fix-class · Phase · Req
    2. CONTROLLING DECISIONS: `.planning/phases/129-dark-theme-brand-expression-audit-s-r/129-CONTEXT.md`
       — Verify the backlog structure block (under `### Backlog structure`) is honored.
    3. AA SPINE (for citing cluster descriptions): `.planning/phases/128-contrast-gate-harness-dark-seed-coverage-s-g/128-CONTRAST-REPORT.md`
       — Every DD6 finding's ratio and scope must trace to a row or cluster name in this file.
  </read_first>

  <files>
    .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md
  </files>

  <action>
    Write `.planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md`
    using the Write tool (never a heredoc bash command).

    FILE STRUCTURE (mirror 120-AUDIT-BACKLOG.md exactly):

    1. YAML/metadata header block (fenced ---):
       - generated date (2026-06-04)
       - phase: 129
       - inputs: the three substrate files (128-CONTRAST-REPORT.md, .tmp/admin-screenshots/ 40 shots, prompts/scrypath-brand-book.md)
       - method: 6 dark-specific DD dimensions (DD1–DD6), 0–3 scale, promote AA from 128, brand overlay from screenshots

    2. Executive summary section:
       - Total findings count, broken down by severity: blockers N / structural N / polish N
       - Breakdown by fix-class: token N / component N / screen N / motion N / seed N
       - Systemic promotion count: N findings promoted to systemic (≥3 screens), routing to phases 130–132
       - One-sentence highlight of finding #1 (#1B2230 ramp gap) and the three 128 clusters

    3. Section header: "## Ranked consolidated backlog"
       Explanatory line: Score = the audited dimension's 0–3 rating (0 worst). DD = dark-brand dimension. SYS = promoted systemic.

    4. THREE SEVERITY TABLES in order (D-06):

       ### Blockers (any DD6 = AA fail, per D-07)
       Column headers: | ID | Alt | Touchpoint | Dim | Score | Sev | Scope | Evidence | Proposed fix | Fix-class | Phase | Req |

       All AA-fail findings from 128 clusters + per-screen DD6 data. Finding #1 MUST be the first
       row: the .leading-4 / #1B2230 ramp-collapse (Cluster 1 from 128, DD6 + DD1 + DD4, systemic,
       blocker). Additional blocker rows: dark form inputs cluster (DD6, systemic), .bg-primary
       near-miss (DD6, local), pre code-block in dark (DD6, local).

       ### Structural (brand-fidelity systemic gaps, DD1–DD5, ≥3 screens)
       Same column headers.
       DD1–DD4 systemic brand gaps that affect ≥3 screens: ramp flatness (DD1 structural), absent
       ambient-shadow depth (DD4 structural), ratio imbalance (DD2 if systemic), etc.

       ### Polish (brand-fidelity per-screen gaps, DD1–DD5, single screen)
       Same column headers.
       Per-screen items: glow/copper absence on specific screens, DD5 path-line glow off on one
       screen, DD2 ratio concerns local to one screen, etc.

    5. Systemic cluster analysis section (mirror 120's format):
       For each systemic cluster: name, IDs, phase, anchor file:line.
       Must include the three 128 clusters + any new DD1–DD5 systemic clusters identified.
       Label the #1B2230 cluster as Cluster 1 / finding #1.

    6. Prioritized fix list for phases 130–135:
       A table ordered by phase, with one row per finding group, showing Phase · Req · Findings · Fix description.

    COLUMN CONTENT RULES:
    - ID: DK-NN (DK prefix for dark-audit, NN sequential, leading zeros to 2 digits). Finding #1 = DK-01.
    - Alt: element / component / flow / page (the altitude of the finding)
    - Touchpoint: screen name + component/element name (e.g. "Search, dark form inputs")
    - Dim: DD1, DD2, DD3, DD4, DD5, DD6, or a combo like DD1+DD4+DD6 for compound findings
    - Score: 0–3 (0 = worst). For compound DD findings, use the lowest-scoring dimension's score.
    - Sev: blocker / structural / polish
    - Scope: systemic / per-screen
    - Evidence: comma-separated screenshot slugs (NN-screen--dark--viewport--state) + file:line (e.g. app.css:614) + 128-report ref for DD6 (e.g. "128-report cluster 1")
    - Proposed fix: plain English imperative, names the target token/component but proposes no implementation code
    - Fix-class: token | component | screen | motion | seed (from vocab)
    - Phase: 130–135 integer matching the req→phase map
    - Req: the requirement ID from REQUIREMENTS.md §13 (DARKTOKEN-01 / GLOW-01 / COPPER-01 / A11Y-TOKEN-01 / DARKMOTION-01 / SCREEN-DARK-01 / SHELL-DARK-01)

    FINDING #1 ANCHOR RULE (D-10): DK-01 MUST be the #1B2230 surface-2 ramp gap. The row must:
    - Be the first row of the Blockers table
    - Have Dim: DD1+DD4+DD6
    - Have Scope: systemic
    - Have Sev: blocker
    - State in Proposed fix: "Introduce the missing #1B2230 surface-2 elevation step in the dark daisyUI theme; route .ops-* raised-surface fill recipes to the new token so dark steps up in elevation rather than flattening. Light stays pixel-identical."
    - Reference 128-CONTRAST-REPORT.md Cluster 1 in Evidence

    Do NOT add findings that propose code/CSS/token changes as deliverables in this phase —
    all Proposed fix text is forward-looking guidance for phases 130–135.
    Do NOT modify any file outside the phase directory.
  </action>

  <verify>
    <automated>
      test -f .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md && echo "FILE EXISTS" || echo "MISSING"
    </automated>
    <automated>
      grep -c "### Blockers" .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md && grep -c "### Structural" .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md && grep -c "### Polish" .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md
    </automated>
    <automated>
      grep -c "DK-01" .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md
    </automated>
    <automated>
      grep -c "1B2230" .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md
    </automated>
    <automated>
      grep -c "128-report\|128-CONTRAST" .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md
    </automated>
    <automated>
      grep -c "DARKTOKEN-01\|A11Y-TOKEN-01\|GLOW-01\|COPPER-01\|SCREEN-DARK-01\|DARKMOTION-01" .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md
    </automated>
    <automated>
      git -C /Users/jon/projects/scrypath status --short | grep -v "129-DARK-AUDIT-BACKLOG.md" | grep -v "129-01-PLAN.md" | grep "scrypath_ops\|app.css\|DESIGN-TOKENS\|ops_ui.ex" && echo "SCOPE VIOLATION" || echo "SCOPE CLEAN"
    </automated>
  </verify>

  <acceptance_criteria>
    - `129-DARK-AUDIT-BACKLOG.md` exists in `.planning/phases/129-dark-theme-brand-expression-audit-s-r/`
    - File contains a YAML/metadata header block with generated date, phase, inputs, and method fields
    - File contains a `## Executive summary` (or equivalent) with total findings count and severity breakdown (blockers / structural / polish)
    - Executive summary includes a fix-class breakdown (token / component / screen / motion / seed counts)
    - Executive summary includes a systemic promotion count (N findings promoted ≥3-screen systemic)
    - File contains exactly three severity tables headed `### Blockers`, `### Structural`, `### Polish`
    - First row in Blockers table is DK-01 (the #1B2230 surface-2 ramp gap) explicitly labeled with Dim DD1+DD4+DD6, Sev blocker, Scope systemic
    - Every DD6 row in the backlog cites a matching entry in 128-CONTRAST-REPORT.md (via "128-report cluster N" or equivalent reference in Evidence column) — zero independently re-derived AA ratios
    - Every Phase/Req cell uses only values from the REQUIREMENTS.md §13 traceability table (DARKTOKEN-01→130, GLOW-01→131, COPPER-01→131, A11Y-TOKEN-01→132, DARKMOTION-01→133, SCREEN-DARK-01→134, SHELL-DARK-01→135)
    - Every finding row has non-empty values in all 12 columns: ID, Alt, Touchpoint, Dim, Score, Sev, Scope, Evidence, Proposed fix, Fix-class, Phase, Req
    - Evidence column of each finding contains at least one screenshot slug in `NN-screen--dark--viewport--state` format
    - Evidence column of each finding with source-code basis contains at least one file:line reference
    - File contains a `## Systemic cluster analysis` (or equivalent) section naming the clusters
    - File contains a `## Prioritized fix list` (or equivalent) for phases 130–135
    - No source file outside the phase directory is modified (git status shows only the backlog md + plan md in the diff)
  </acceptance_criteria>

  <done>
    129-DARK-AUDIT-BACKLOG.md is written, all tables populated, finding #1 anchored as the
    #1B2230 ramp gap, AA spine promoted from 128 (not re-derived), every finding routed with
    correct Phase + Req columns, executive summary complete, no source files modified.
    The file is the single authoritative input for phases 130–135.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No runtime boundary | This phase reads in-repo files and writes one markdown file. No external inputs, no endpoints, no user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-129-SC | Tampering | No npm/pip/cargo installs in this phase | accept | No package manager operations; no legitimacy audit needed |
| T-129-01 | Information Disclosure | Markdown file committed to repo | accept | File contains only design audit findings (no PII, no secrets, no credentials) — safe to commit |
| T-129-02 | Tampering | Scope drift — executor modifies source files | mitigate | Acceptance criteria includes git-status scope check; executor must verify only the backlog md is in the diff before commit |

No runtime attack surface — read-only analysis producing a static markdown artifact; no untrusted
input, no new endpoints, no secrets handled, no npm/pip/cargo installs.
</threat_model>

<verification>
## Phase-Level Verification

After Task 2 completes:

1. File existence: `test -f .planning/phases/129-dark-theme-brand-expression-audit-s-r/129-DARK-AUDIT-BACKLOG.md`
2. Severity table presence: grep confirms `### Blockers`, `### Structural`, `### Polish` all present
3. Finding #1 anchor: `grep "DK-01" ...` and `grep "1B2230" ...` both return matches
4. DD6 traceability: grep for "128-report" or "128-CONTRAST" in Evidence cells confirms AA spine promotion
5. Req column coverage: grep for all six req IDs (DARKTOKEN-01, GLOW-01, COPPER-01, A11Y-TOKEN-01, DARKMOTION-01, SCREEN-DARK-01) confirms routing
6. Scope guard: `git status --short` shows no modifications outside the phase directory
</verification>

<success_criteria>
1. `129-DARK-AUDIT-BACKLOG.md` exists in the phase directory and is committed to git
2. Every dark touchpoint across 6 screens is scored on DD1–DD6 (0–3 scale)
3. Finding #1 is the #1B2230 surface-2 ramp gap, anchored as DK-01 in the Blockers table
4. All DD6 AA values promoted from 128-CONTRAST-REPORT.md with zero independent re-derivation
5. Three severity tables (Blockers / Structural / Polish) populated with ID, Alt, Touchpoint, Dim, Score, Sev, Scope, Evidence, Proposed fix, Fix-class, Phase, Req columns
6. Executive summary reports totals by severity, by fix-class, and systemic promotion count
7. Every Phase/Req pair matches the REQUIREMENTS.md §13 traceability table authority
8. No source file outside the phase directory is modified (git status clean except backlog md)
</success_criteria>

<output>
Create `.planning/phases/129-dark-theme-brand-expression-audit-s-r/129-01-SUMMARY.md` when done.
</output>
