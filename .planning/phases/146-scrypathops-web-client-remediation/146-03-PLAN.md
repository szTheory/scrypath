---
phase: 146-scrypathops-web-client-remediation
plan: 03
type: execute
wave: 3
depends_on: [146-02]
files_modified:
  - .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md
autonomous: true
requirements: [SEC-03, EVID-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Per D-02/EVID-03, live Hex and EEF CNA are re-queried immediately before fresh proof and must still establish stable unretired Postgrex 0.22.4 with < 0.22.4 affected; any loss or contradiction blocks completion."
    - "Per amended D-05, a detached lockless resolution at the exact candidate selects Phoenix >= 1.8.9 < 1.9.0, LiveView >= 1.1.33 < 1.2.0, Bandit >= 1.12.1 < 1.13.0, Swoosh >= 1.26.3 < 1.27.0, Postgrex >= 0.22.4 < 0.23.0, Req >= 0.6.1 < 0.7.0, Plug >= 1.19.5 < 2.0.0, Mint >= 1.9.3, and hpax >= 1.0.4; Plug remains transitive per D-03."
    - "Per D-11/D-15, the detached fresh graph passes unsuppressed ScrypathOps mix hex.audit; only the disposable lock is removed, and MIX_DEPS_PATH plus MIX_BUILD_PATH are isolated beneath the disposable worktree."
    - "Per D-15/D-16, an EXIT trap is installed before the first fallible authority or probe command, captures the original status, and unconditionally removes the exact validated task-created worktree plus isolated dependency/build state on success, range/audit/authority failure, interruption, or any other non-zero exit."
    - "Per D-16, any registry/feed outage, audit/range/gate failure, dirty primary lock, unexplained churn, Postgrex-evidence loss, or Swoosh failure is blocking and never recorded as passed."
    - "Per D-17, evidence records candidate SHA, UTC window, tool versions, commands/exits, exact target versions, audit result, causal lock rows, evidence classification, and cleanup without raw logs, locks, trees, snapshots, credentials, or service artifacts."
    - "Per D-18/D-19, the branch contains exactly one isolated ScrypathOps implementation remediation commit and no ecommerce, public API, route/UI, mail semantic, schema, provider, permanent CI/security, or modernization change."
    - "Per D-20 through D-22, maintainers receive familiar Mix/Hex operations and a truthful Phase 147 handoff with all eight approved UI preservation states unchanged."
  artifacts:
    - path: ".planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md"
      provides: "Compact exact-SHA fresh-resolution, audit, cleanup, and handoff evidence"
  key_links:
    - from: "Plan 01 implementation SHA"
      to: "detached worktree HEAD"
      via: "git worktree add --detach at the exact recorded commit"
    - from: "disposable scrypath_ops/mix.lock"
      to: "D-05 version matrix"
      via: "Mix.Dep.Lock.read plus Version.match?/2 assertions after lockless resolution"
    - from: "fresh resolved graph"
      to: "unsuppressed advisory status"
      via: "mix hex.audit with no ignore flags or ignore configuration"
    - from: "disposable proof"
      to: "primary workspace"
      via: "lock-hash equality, tracked-state preservation, and validated worktree cleanup"
  flagged_assumptions:
    - statement: "EVID-03 remains an unclassified/unresolved edge-probe assumption; this plan assumes two live authorities, exact-version assertions, and unsuppressed audit are sufficient to establish a truthful Postgrex and graph remediation handoff."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "Must not report unavailable, contradictory, suppressed, or non-zero external evidence as passing, and must not substitute any prerelease, invented version, or affected fallback."
      status: unverified
      flagged: true
    - statement: "Must not retain or commit raw logs, disposable locks/worktrees, dependency trees, advisory snapshots, provider credentials, generated service artifacts, or isolated build/dependency directories."
      status: unverified
      flagged: true
---

<objective>
Seal the exact ScrypathOps candidate with detached lockless version proof, live Postgrex authority evidence, an unsuppressed advisory audit, and validated cleanup.

Purpose: Give maintainers reproducible and honest evidence that the manifest resolves safely without relying on the reviewed lock, while protecting the primary workspace and the single implementation-commit boundary (SEC-03, EVID-03; D-02, D-05, D-15 through D-22).
Output: Compact fresh-resolution/audit evidence and a green-or-blocked Phase 147 handoff.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@scrypath_ops/AGENTS.md
@CONTRIBUTING.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
@.planning/phases/146-scrypathops-web-client-remediation/146-RESEARCH.md
@.planning/phases/146-scrypathops-web-client-remediation/146-VALIDATION.md
@.planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md
@.planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md
@.planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md
</context>

## Artifacts this phase produces

- `.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md` with exact candidate SHA, UTC evidence window, Elixir/OTP/Mix/Hex versions, live Postgrex source predicates, D-05 selected-version matrix, unsuppressed audit exit, causal lock summary, deterministic/network classification, primary-lock hash equality, disposable cleanup, and handoff status.
- No retained lock, worktree, raw log, dependency tree, advisory snapshot, credential, service artifact, build directory, production symbol, route, UI flag, schema file, CI task, or product capability.

<tasks>

<task type="auto">
  <name>Task 1: Resolve and audit the exact candidate in a detached isolated worktree</name>
  <precondition>Plan 146-02 records zero exits for mix verify.opsui and all four named root gates on the exact Plan 01 implementation SHA; the primary scrypath_ops/mix.lock hash and tracked status are recorded before this task.</precondition>
  <files>.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md</files>
  <read_first>
    AGENTS.md
    scrypath_ops/AGENTS.md
    CONTRIBUTING.md
    .planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
    .planning/phases/146-scrypathops-web-client-remediation/146-RESEARCH.md
    .planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md
    .planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md
    .planning/phases/145-legacy-phoenix-and-ecto-decimal-remediation/145-02-SUMMARY.md
    scrypath_ops/mix.exs
    scrypath_ops/mix.lock
  </read_first>
  <action>Before the first fallible authority or worktree command, create a mode-0700 state directory with `mktemp -d "${TMPDIR:-/tmp}/scrypath-phase146-state.XXXXXX"`, reject symlink/non-owned/unexpected-prefix paths, and place a mode-0600 locator at `git rev-parse --git-path gsd-phase146-03.locator`; fail if a prior locator exists. Initialize `probe_parent` and `worktree_path` to empty before enabling errexit. Persist separate single-value receipt files for the exact implementation SHA, Phase 145 handoff SHA, primary Ops lock SHA-256, task-created probe parent/worktree paths, the sorted approved candidate path list from the Plan 01 commit, and a NUL-delimited `git status --porcelain=v1 -z` baseline that excludes only this task-owned 146-03-SUMMARY.md. Before accepting the candidate path list, prove it contains the manifest, lock, and focused test and no path other than those three plus an explicitly recorded D-13 fix. Do not source the receipt as shell code. Install an EXIT trap immediately: capture `$?`, disable errexit inside cleanup, conditionally handle only non-empty probe variables, validate the probe parent against the mktemp prefix and ownership plus the worktree as exactly its `worktree` child, remove that exact registered worktree with `git worktree remove --force`, prune, remove the validated parent containing isolated deps/build state, and re-check primary-lock hash plus the recorded status baseline. Any cleanup or preservation mismatch converts the result to non-zero. Write the final probe exit to the receipt. On any non-zero result, append only the failed stage, exit, cleanup result, and `Final handoff: BLOCKED` to the summary, never a success label, then delete the locator/state receipt and exit non-zero; on success, retain only the secure locator/state receipt through Task 2 while the disposable worktree, lock, deps, and build state are already gone. With that trap active under `set -e`, record the exact implementation SHA, UTC time, and tool versions; re-query both D-02 authorities with the Plan 01 predicates; create the worktree as the `worktree` child of a separately validated `mktemp -d "${TMPDIR:-/tmp}/scrypath-phase146-probe.XXXXXX"` parent; detach it at the exact SHA; set `MIX_DEPS_PATH` and `MIX_BUILD_PATH` beneath that worktree; remove only its Ops lock; run fresh `mix deps.get`; then, from the detached `scrypath_ops` directory, run the lock-only matrix as `elixir -e 'Mix.start(); lock = Mix.Dep.Lock.read(); [{"Phoenix", :phoenix, ">= 1.8.9 and < 1.9.0"}, {"LiveView", :phoenix_live_view, ">= 1.1.33 and < 1.2.0"}, {"Bandit", :bandit, ">= 1.12.1 and < 1.13.0"}, {"Swoosh", :swoosh, ">= 1.26.3 and < 1.27.0"}, {"Postgrex", :postgrex, ">= 0.22.4 and < 0.23.0"}, {"Req", :req, ">= 0.6.1 and < 0.7.0"}, {"Plug", :plug, ">= 1.19.5 and < 2.0.0"}, {"Mint", :mint, ">= 1.9.3"}, {"hpax", :hpax, ">= 1.0.4"}] |> Enum.each(fn {label, dep, requirement} -> version = lock |> Map.fetch!(dep) |> elem(2); Version.match?(version, requirement) || raise "#{label} #{version} violates #{requirement}"; IO.puts("#{label} #{version} satisfies #{requirement}") end)'`. Use this `elixir -e` lock reader rather than `mix run`, because `mix run` initializes Phoenix dependency load paths and previously failed before reading the lock. This amended D-05 check preserves all eight original non-Plug ranges, accepts only compatible Plug 1.x selected transitively per D-03, and does not alter the reviewed primary Plug `1.19.5` lock. After the matrix passes, run unsuppressed `mix hex.audit` (D-11/D-15/D-16). Record only compact versions, commands/exits, source status, audit result, cleanup result, and network classification in 146-03-SUMMARY.md—never raw output, receipt values, disposable paths/locks/trees, credentials, or service artifacts (D-17).</action>
  <verify>
    <automated>set -eu; locator=$(git rev-parse --git-path gsd-phase146-03.locator); test -f "$locator"; test ! -L "$locator"; test -O "$locator"; state_dir=$(sed -n '1p' "$locator"); case "$state_dir" in "${TMPDIR:-/tmp}"/scrypath-phase146-state.*) ;; *) exit 1 ;; esac; test -d "$state_dir"; test ! -L "$state_dir"; test -O "$state_dir"; test "$(cat "$state_dir/probe-exit")" = 0; worktree_path=$(cat "$state_dir/worktree-path"); probe_parent=$(cat "$state_dir/probe-parent"); test "$worktree_path" = "$probe_parent/worktree"; test ! -e "$worktree_path"; test ! -e "$probe_parent"; test -s .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; rg -q 'Phoenix.*>= 1\.8\.9.*< 1\.9\.0' .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; rg -q 'Postgrex.*>= 0\.22\.4.*< 0\.23\.0' .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; rg -q 'mix hex\.audit.*0' .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; ! rg -q '^Final handoff: PASS' .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - "Fresh proof runs in a detached worktree at exactly the recorded implementation SHA with both build and dependency paths isolated; only its disposable Ops lock is removed (D-15)."
    - "All nine amended D-05 ranges pass against actual fresh selections—including transitive Plug >= 1.19.5 and < 2.0.0 per D-03—and unsuppressed mix hex.audit exits zero."
    - "Live Postgrex stable/unretired and CNA predicates pass again in the same evidence window; any outage or contradiction blocks EVID-03 (D-02/D-16)."
    - "The summary contains compact evidence only and contains no raw log, disposable lock/tree, credential, snapshot, or generated service artifact (D-17)."
  </acceptance_criteria>
  <done>The exact candidate freshly resolves within every approved range and passes an unsuppressed audit under current dual-source Postgrex evidence, or the phase is explicitly blocked without fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Validate cleanup, commit topology, and Phase 147 handoff</name>
  <precondition>Task 1 exited zero after recording successful live dual-source predicates, all nine fresh version assertions, a zero unsuppressed audit exit, and unconditional cleanup for the exact implementation SHA; its secure ephemeral receipt and locator remain, but its worktree/deps/build/lock state does not.</precondition>
  <files>.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md</files>
  <read_first>
    AGENTS.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    .planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
    .planning/phases/146-scrypathops-web-client-remediation/146-UI-SPEC.md
    .planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md
    .planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md
    .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md
  </read_first>
  <action>Read, never source, the mode-0600 locator and separate receipt files left by Task 1; validate the locator/state ownership, non-symlink status, and mktemp prefix before trusting any value. Install failure handling before assertions: capture status, disable errexit, safely delete only the validated state directory and locator on non-zero exit, and append a compact `Final handoff: BLOCKED` proof without raw paths or replacing the non-zero exit. Assert the exact recorded worktree is absent from both `git worktree list --porcelain` and the filesystem, and assert its validated probe parent (therefore isolated deps/build state) is absent. Re-hash the primary Ops lock and require equality with the recorded pre-probe hash. Recreate the same NUL-delimited primary status view excluding only 146-03-SUMMARY.md and require byte equality with the recorded dirty baseline, so pre-existing user dirt is preserved exactly. Resolve the Phase 145 handoff and candidate receipt SHAs, require the former is an ancestor, enumerate commits in that range touching `scrypath_ops/mix.exs`, `scrypath_ops/mix.lock`, or the focused Swoosh contract, and require exactly one unique commit equal to the recorded implementation SHA; separately inspect that commit's paths for only the three owned files plus any Plan 01-recorded D-13 fix (D-18). Record cleanup, equality assertions, commit topology, no-UI/no-schema/no-route/no-provider scope, and `Final handoff: PASS` only after every assertion and D-16 stop check has succeeded. On this success path retain the secure receipt only until the automated verify below reruns the assertions; that verify's EXIT trap removes the receipt/locator whether verify passes or fails. Do not record raw receipt values/paths, close the four-graph todo, or claim ecommerce/all-graph evidence (D-12/D-17/D-22).</action>
  <verify>
    <automated>set -eu; summary=.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md; locator=$(git rev-parse --git-path gsd-phase146-03.locator); state_dir=; finish() { rc=$?; set +e; if test "$rc" -ne 0; then printf '\nFinal handoff: BLOCKED — executable cleanup/topology verification failed (exit %s).\n' "$rc" >> "$summary"; fi; case "$state_dir" in "${TMPDIR:-/tmp}"/scrypath-phase146-state.*) test -O "$state_dir" && test ! -L "$state_dir" && rm -rf -- "$state_dir" ;; esac; rm -f -- "$locator"; exit "$rc"; }; trap finish EXIT; trap 'exit 130' INT TERM; test -f "$locator"; test ! -L "$locator"; test -O "$locator"; state_dir=$(sed -n '1p' "$locator"); case "$state_dir" in "${TMPDIR:-/tmp}"/scrypath-phase146-state.*) ;; *) exit 1 ;; esac; test -d "$state_dir"; test ! -L "$state_dir"; test -O "$state_dir"; worktree_path=$(cat "$state_dir/worktree-path"); probe_parent=$(cat "$state_dir/probe-parent"); case "$probe_parent" in "${TMPDIR:-/tmp}"/scrypath-phase146-probe.*) ;; *) exit 1 ;; esac; test "$worktree_path" = "$probe_parent/worktree"; test ! -e "$worktree_path"; test ! -e "$probe_parent"; ! git worktree list --porcelain | awk '/^worktree / {sub(/^worktree /, ""); print}' | grep -Fx -- "$worktree_path"; test "$(shasum -a 256 scrypath_ops/mix.lock | awk '{print $1}')" = "$(cat "$state_dir/primary-lock.before")"; git status --porcelain=v1 -z -- . ':(exclude).planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md' > "$state_dir/primary-status.after"; cmp -s "$state_dir/primary-status.before" "$state_dir/primary-status.after"; base_sha=$(cat "$state_dir/phase145-sha"); candidate_sha=$(cat "$state_dir/candidate-sha"); git merge-base --is-ancestor "$base_sha" "$candidate_sha"; impl_commits=$(git rev-list "$base_sha..$candidate_sha" -- scrypath_ops/mix.exs scrypath_ops/mix.lock scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs); test "$(printf '%s\n' "$impl_commits" | sed '/^$/d' | wc -l | tr -d ' ')" = 1; test "$impl_commits" = "$candidate_sha"; candidate_paths="$state_dir/candidate-paths.after"; git diff-tree --no-commit-id --name-only -r "$candidate_sha" | LC_ALL=C sort > "$candidate_paths"; cmp -s "$state_dir/candidate-paths.approved" "$candidate_paths"; test "$(rg '^Final handoff: (PASS|BLOCKED)' "$summary" | tail -n 1)" = 'Final handoff: PASS'</automated>
  </verify>
  <acceptance_criteria>
    - "The disposable worktree and its isolated build/dependency state are gone, the primary Ops lock hash is unchanged, and pre-existing user changes remain untouched (D-15/D-17)."
    - "Exactly one implementation commit owns the ScrypathOps manifest, lock, focused Swoosh contract, and any explicitly justified D-13 fix; planning evidence commits are separate (D-18)."
    - "The summary records green handoff only when every D-16 deterministic, Swoosh, range, audit, authority, cleanliness, and causal-diff condition passed."
    - "No ecommerce/four-graph closure, UI, public API, route, schema, provider, CI subsystem, policy language, or modernization claim is made (D-19 through D-22)."
  </acceptance_criteria>
  <done>Fresh evidence is compact and auditable; exact-path cleanup, dirty-baseline equality, primary-lock equality, and exactly-one implementation topology are executable assertions; and the summary's final outcome is PASS only when they all hold, otherwise BLOCKED with a non-zero plan result.</done>
</task>

</tasks>

<threat_model>
## Assets and Trust Boundaries

| Boundary | Protected asset | Exposure |
|---|---|---|
| Live Hex/CNA → evidence summary | Truthful fixed-release status | Outage, malformed response, contradiction, or stale assumption can be mislabeled as pass. |
| Exact Git SHA → detached worktree | Candidate identity | Wrong revision or copied dirty state invalidates proof. |
| Lockless Mix solver → external registry | Fresh dependency graph | Resolver can select out-of-range or newly vulnerable packages. |
| Disposable worktree → primary workspace | Reviewed lock and user changes | Unsafe cleanup or shared build paths can mutate/delete primary state. |
| Command output → committed summary | Credential/log safety | Raw logs or environment data can disclose secrets or create unverifiable noise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-146-09 | Spoofing / Repudiation | Candidate SHA | high | mitigate | Detach at the recorded Plan 01 SHA and record HEAD before resolution. |
| T-146-10 | Tampering | Fresh dependency selection | high | mitigate | Remove only disposable lock, isolate paths, assert all nine D-05 ranges, and run unsuppressed audit. |
| T-146-11 | Repudiation | External evidence | high | mitigate | Two live fail-closed predicates, UTC/source recording, zero audit exit, and no unavailable-as-pass classification. |
| T-146-12 | Information disclosure | Evidence summary | medium | mitigate | Persist compact status/version evidence only; exclude raw logs, environment secrets, provider credentials, and service artifacts. |
| T-146-13 | DoS / Tampering | Cleanup | high | mitigate | mktemp path validation, explicit worktree removal, primary-lock hash equality, and tracked baseline comparison. |
| T-146-SC | Tampering | Hex supply chain | high | mitigate | Existing official package identities only, exact bounds, fresh solver assertions, checked SHA, and unsuppressed audit; no new install identity. |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Independently run ScrypathOps on a fixed-compatible web, LiveView, mailer, HTTP, database, and transitive graph | 01-03 | COVERED | Atomic candidate, full deterministic gates, fresh resolve/audit. |
| REQ | SEC-03 | Independently resolve recorded Ops advisories | 01-03 | COVERED | Exact bounds, causal lock, Swoosh contract, Ops/root gates, audit. |
| REQ | EVID-03 | Block Postgrex until live advisory and Hex confirm stable fixed release | 01,03 | COVERED | Dual-source checks before edit and before final fresh proof. |
| RESEARCH | — | Real Swoosh.ApiClient.Req via Req.Test | 01 | COVERED | Direct init/post request, precedence, tuple, and error contract. |
| RESEARCH | — | Binding deterministic gates and exact-SHA detached proof | 02-03 | COVERED | Ordered gates, isolation, version assertions, audit, cleanup. |
| CONTEXT | D-01-D-05 | Bounds, ownership, causal lock, fresh range matrix | 01,03 | COVERED | Exact strings and nine assertions. |
| CONTEXT | D-06-D-09 | Production-shaped Swoosh/Req proof and config preservation | 01 | COVERED | Service-free direct real-client test. |
| CONTEXT | D-10-D-13 | Ops application proof and minimal-fix boundary | 01-02 | COVERED | mix verify.opsui, upstream-proof ownership, hard stop. |
| CONTEXT | D-14-D-19 | Gate order, detached evidence, stops, compact record, one commit | 01-03 | COVERED | Explicit ordered tasks and cleanup. |
| CONTEXT | D-20-D-22 | Maintainer experience, no new subsystem/capability/UI | 01-03 | COVERED | Preservation truths and prohibitions. |

<verification>
- Exact-SHA detached proof selects all nine approved ranges and passes unsuppressed audit.
- Both live Postgrex authorities pass in the same evidence window; unavailable/conflicting data blocks.
- Cleanup preserves the primary lock and dirty-worktree baseline and removes all disposable state.
- Evidence distinguishes deterministic from network proof and contains no sensitive/raw artifacts.
</verification>

<success_criteria>
- SEC-03 fresh resolution and advisory evidence are green at the exact implementation SHA.
- EVID-03 is satisfied by live dual-source stable fixed-release evidence without substitutes.
- The phase hands off exactly one isolated ScrypathOps implementation commit and no ecommerce or product/UI/schema expansion.
</success_criteria>

<output>
Create `.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md` when done.
</output>
