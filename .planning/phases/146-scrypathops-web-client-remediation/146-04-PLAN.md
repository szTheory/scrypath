---
phase: 146-scrypathops-web-client-remediation
plan: 04
type: execute
wave: 4
depends_on: [146-03]
files_modified:
  - scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs
autonomous: true
gap_closure: true
requirements: [SEC-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Per D-06/D-07, the direct service-free Swoosh.ApiClient.Req happy-path contract returns an application/json response and proves post/4 preserves the exact raw JSON binary even though email.private[:client_options] contains decode_body: true."
    - "Per D-07, the existing request method, full URL/path, query option, provider headers, forwarded client header, Swoosh user-agent, caller header/body precedence, exact request body, normalized response tuple, and transport-error assertions remain present and green."
    - "Per amended D-18, 59d2e6a894d97a16fd8acc624281c8b2c38777c1 remains the one dependency-remediation implementation commit, while exactly one separately labeled verifier-driven closure commit changes only the focused test and Git history is not rewritten."
    - "Per D-01 through D-05 and D-11/D-15, scrypath_ops/mix.exs and scrypath_ops/mix.lock remain byte-identical to Phase 146 evidence; the completed detached fresh-resolution evidence is retained rather than repeated."
    - "Per D-08 through D-10 and D-12/D-13, test/prod Swoosh configuration, ScrypathOps runtime behavior, public APIs, routes, schemas, providers, and ecommerce integration remain unchanged while the focused contract, local precommit, checked lock, warnings-as-errors compile, mix verify.opsui, and named root gates pass."
    - "Per D-14/D-16/D-17 and D-20/D-21, the closure is tied to exact commit-path and command evidence; any test, audit, hash, scope, or required-gate failure blocks re-verification rather than being omitted or substituted."
    - "Per D-19/D-22 and 146-UI-SPEC.md, the existing operator empty, loading, error, populated, partial-data, overflow, zero/one/many, and long-text states remain unchanged because no UI-owned file is touched and mix verify.opsui passes."
  artifacts:
    - path: "scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs"
      provides: "Discriminating raw-JSON response contract for the production-selected Swoosh Req client"
      contains: "decode_body: true"
    - path: ".planning/phases/146-scrypathops-web-client-remediation/146-04-SUMMARY.md"
      provides: "Exact closure SHA, test-only path proof, preserved dependency hashes, and green release-train evidence"
  key_links:
    - from: "Swoosh.ApiClient.Req.post/4"
      to: "Req.Test application/json response"
      via: "email.private[:client_options] retains conflicting decode_body: true while the returned body is asserted as the exact raw JSON binary"
      pattern: "application/json"
    - from: "59d2e6a894d97a16fd8acc624281c8b2c38777c1"
      to: "verifier-driven closure commit"
      via: "git ancestry plus exact diff-tree path assertions that preserve the dependency commit and isolate the follow-up to one test file"
    - from: "Phase 146 dependency evidence"
      to: "current scrypath_ops manifest and lock"
      via: "SHA-256 equality for a624ce14afb45f37d4c027ad5b7de748be94dbc312401c3e62f908998d0bd07e and 30c54587258cf29674af0b5e9f1c71799ac44f82ef9227fd6d9e2d1776588ea4"
  prohibitions:
    - statement: "Must not modify any manifest, lock, production/runtime source, UI/template/asset, route, schema/migration, provider configuration, CI/workflow, ecommerce, or public API file."
      status: unverified
      flagged: true
    - statement: "Must not amend, squash, reset, rebase, replace, or otherwise rewrite 59d2e6a or pretend the verifier-driven closure is part of that original commit."
      status: unverified
      flagged: true
    - statement: "Must not redo detached dependency resolution or mutate the primary lock; the prior exact-SHA evidence remains authoritative only after current manifest/lock hashes and the unsuppressed checked-graph audit pass."
      status: unverified
      flagged: true
    - statement: "Must not stage or commit user-owned .planning/REQUIREMENTS.md, .planning/config.json, or .planning/v1.36-v1.36-MILESTONE-AUDIT.md."
      status: unverified
      flagged: true
---

<objective>
Close the Phase 146 verification gap with a discriminating raw-JSON Swoosh Req contract and exact test-only closure evidence.

Purpose: Make SEC-03's configured Req-backed Swoosh behavior genuinely observable without changing the already-proven dependency graph or runtime surface (D-06, D-07, amended D-18).
Output: One test-only verifier-driven closure commit plus a compact summary proving raw JSON preservation, unchanged dependency evidence, and a green release-train gate set.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@scrypath_ops/AGENTS.md
@CONTRIBUTING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
@.planning/phases/146-scrypathops-web-client-remediation/146-RESEARCH.md
@.planning/phases/146-scrypathops-web-client-remediation/146-VALIDATION.md
@.planning/phases/146-scrypathops-web-client-remediation/146-UI-SPEC.md
@.planning/phases/146-scrypathops-web-client-remediation/146-REVIEW.md
@.planning/phases/146-scrypathops-web-client-remediation/146-VERIFICATION.md
@.planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md
@.planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md
@.planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md
@scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs
</context>

## Artifacts this phase produces

- A strengthened happy-path case in `scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs` that returns JSON with an `application/json` content type and asserts the exact raw JSON response binary while retaining conflicting `decode_body: true` and every existing request/response assertion.
- Exactly one commit titled `test(146-04): harden Swoosh raw JSON contract` whose diff contains only the focused test file; dependency-remediation commit `59d2e6a894d97a16fd8acc624281c8b2c38777c1` remains unchanged and separately identified.
- `.planning/phases/146-scrypathops-web-client-remediation/146-04-SUMMARY.md` with exact closure SHA, dependency hashes, commit-path proof, commands/exits, and readiness for code review plus phase re-verification.

<tasks>

<task type="auto">
  <name>Task 1: Harden the raw-JSON contract and prove the isolated closure</name>
  <precondition>Commit 59d2e6a894d97a16fd8acc624281c8b2c38777c1 exists; current scrypath_ops/mix.exs and scrypath_ops/mix.lock hash to the exact Phase 146 evidence values recorded in this plan; and the only pre-existing dirty paths are preserved without staging.</precondition>
  <files>scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs</files>
  <read_first>
    AGENTS.md
    scrypath_ops/AGENTS.md
    CONTRIBUTING.md
    .planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
    .planning/phases/146-scrypathops-web-client-remediation/146-RESEARCH.md
    .planning/phases/146-scrypathops-web-client-remediation/146-UI-SPEC.md
    .planning/phases/146-scrypathops-web-client-remediation/146-REVIEW.md
    .planning/phases/146-scrypathops-web-client-remediation/146-VERIFICATION.md
    .planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md
    .planning/phases/146-scrypathops-web-client-remediation/146-02-SUMMARY.md
    .planning/phases/146-scrypathops-web-client-remediation/146-03-SUMMARY.md
    scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs
    prompts/elixir-opensource-libs-best-practices-deep-research.md
    prompts/elixir-oss-lib-ci-cd-best-practices-deep-research.md
  </read_first>
  <action>First record `git status --short`, require the full candidate commit object, and verify current `scrypath_ops/mix.exs` plus `scrypath_ops/mix.lock` SHA-256 values exactly match the Phase 146 evidence. Modify only the first Req.Test stub in `ScrypathOps.SwooshApiClientReqTest`: preserve every existing POST method, host/path/query, provider header, forwarded client header, Swoosh user-agent, conflicting-header absence, and exact raw request-body assertion; replace the text response with the exact JSON binary `~s({"accepted":true})`, set the response content type to `application/json`, and assert `Swoosh.ApiClient.Req.post/4` returns that exact binary rather than a decoded map. Keep `decode_body: true`, the conflicting caller headers/body, the client option forwarding, response status/header normalization, init assertion, and the transport-error test unchanged in substance (D-06/D-07). Do not alter global Swoosh test configuration, production selection, mailer/runtime code, manifest, lock, UI, routes, schemas, providers, CI, or ecommerce (D-08 through D-13, D-19/D-22).

Run the focused test, format the focused file, rerun the focused test, and run `mix precommit` from `scrypath_ops`. Then run checked-lock, warnings-as-errors Ops compile, the unsuppressed checked-graph `mix hex.audit`, root `mix verify.opsui`, and the root command equivalents for `main-ci`, `repo-hygiene`, `release-truth`, and `phase99-trust` in CONTRIBUTING order (D-14/D-16). Re-hash manifest and lock afterward and require the same evidence values; detached lockless resolution is not repeated because no manifest/lock byte changed and 146-03-SUMMARY.md already binds that proof to `59d2e6a` (D-15/D-17).

Before committing, inspect the complete tracked diff and stage only the focused test. Create exactly one separate commit titled `test(146-04): harden Swoosh raw JSON contract`; do not amend or rewrite history. Resolve the closure SHA from the newest commit touching the focused test, require `59d2e6a` is its ancestor, require exactly one post-`59d2e6a` commit touches the focused test, require the closure commit's path list equals only `scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs`, and require its subject matches the separately labeled verifier-closure title. Record the original dependency SHA, closure SHA, exact path proof, unchanged dependency hashes, and every gate exit in 146-04-SUMMARY.md. Preserve the user-owned planning changes and leave code review plus phase re-verification to the execute-phase follow-up (amended D-18, D-20/D-21).</action>
  <verify>
    <automated>set -e; candidate=59d2e6a894d97a16fd8acc624281c8b2c38777c1; test "$(git rev-parse "$candidate^{commit}")" = "$candidate"; test "$(shasum -a 256 scrypath_ops/mix.exs | awk '{print $1}')" = a624ce14afb45f37d4c027ad5b7de748be94dbc312401c3e62f908998d0bd07e; test "$(shasum -a 256 scrypath_ops/mix.lock | awk '{print $1}')" = 30c54587258cf29674af0b5e9f1c71799ac44f82ef9227fd6d9e2d1776588ea4; (cd scrypath_ops && mix test test/scrypath_ops/swoosh_api_client_req_test.exs && mix format --check-formatted test/scrypath_ops/swoosh_api_client_req_test.exs && mix precommit && mix deps.get --check-locked && mix compile --warnings-as-errors && mix hex.audit); mix verify.opsui; mix compile --warnings-as-errors; mix test --exclude integration --exclude docs_contract --include requires_clean_workspace; mix verify --exclude integration; mix verify.phase11; mix verify.phase99; test "$(shasum -a 256 scrypath_ops/mix.exs | awk '{print $1}')" = a624ce14afb45f37d4c027ad5b7de748be94dbc312401c3e62f908998d0bd07e; test "$(shasum -a 256 scrypath_ops/mix.lock | awk '{print $1}')" = 30c54587258cf29674af0b5e9f1c71799ac44f82ef9227fd6d9e2d1776588ea4; closure=$(git log -1 --format=%H -- scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs); test "$closure" != "$candidate"; git merge-base --is-ancestor "$candidate" "$closure"; test "$(git rev-list "$candidate..$closure" -- scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs | sed '/^$/d' | wc -l | tr -d ' ')" = 1; test "$(git diff-tree --no-commit-id --name-only -r "$closure")" = scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs; test "$(git show -s --format=%s "$closure")" = 'test(146-04): harden Swoosh raw JSON contract'</automated>
  </verify>
  <acceptance_criteria>
    - "The happy-path stub returns an application/json response containing the exact JSON binary, `client_options` still contains `decode_body: true`, and post/4 returns the exact raw binary rather than a decoded value (D-06/D-07)."
    - "Every pre-existing happy-path request/header/body/URL/user-agent/client-option assertion and the transport-error assertion remains present and green (D-07)."
    - "Focused test, focused format check, local mix precommit, checked lock, warnings-as-errors compile, unsuppressed checked-graph audit, root mix verify.opsui, and all named root required gates exit zero (D-10/D-11/D-14/D-16)."
    - "Manifest and lock hashes remain exactly a624ce14afb45f37d4c027ad5b7de748be94dbc312401c3e62f908998d0bd07e and 30c54587258cf29674af0b5e9f1c71799ac44f82ef9227fd6d9e2d1776588ea4, so completed detached evidence remains applicable without re-resolution (D-01 through D-05, D-15/D-17)."
    - "Commit 59d2e6a remains intact; exactly one later verifier-driven commit with the required subject touches the focused contract and its diff contains only that test file (amended D-18)."
    - "No production/runtime, manifest/lock, UI, route, schema, provider, CI, ecommerce, public API, or user-owned planning artifact is changed or staged (D-08/D-09/D-12/D-13/D-19/D-22)."
  </acceptance_criteria>
  <done>SEC-03's Swoosh raw/decode precedence gap is closed by a discriminating test-only commit, all release-train gates are green, dependency evidence remains byte-identical, and Phase 146 is ready for code review and phase re-verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Protected asset | Exposure |
|---|---|---|
| Req.Test response → Swoosh.ApiClient.Req | Raw response-body contract | A non-JSON response can spoof coverage because decoded and raw outcomes are identical. |
| Git working tree → verifier closure commit | Dependency history and user-owned changes | Broad staging or history rewriting can misstate the remediation boundary. |
| Existing Phase 146 evidence → current graph | Audited dependency selection | A manifest/lock drift could make prior detached proof inapplicable. |
| Local commands → closure summary | Release-train truth | Skipped, failed, or substituted gates could be reported as green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-146-14 | Spoofing | Raw-response contract | high | mitigate | Use an application/json response, retain conflicting decode_body: true, and assert the exact raw JSON binary from real Swoosh.ApiClient.Req.post/4 (D-06/D-07). |
| T-146-15 | Tampering | Closure commit scope | high | mitigate | Stage one explicit path, preserve 59d2e6a, and prove the closure subject, ancestry, commit count, and exact diff-tree path (amended D-18). |
| T-146-16 | Repudiation | Gate evidence | high | mitigate | Record exact closure SHA and require focused, Ops, audit, and named root commands to exit zero; no substitute evidence is accepted (D-14/D-16/D-17). |
| T-146-17 | Tampering | Manifest/lock evidence | high | mitigate | Require exact pre/post SHA-256 equality with Phase 146 evidence and make any drift blocking; do not repeat or mutate detached resolution (D-01 through D-05, D-15). |
| T-146-18 | Information disclosure | Test fixture and summary | low | accept | The fixture uses synthetic URL, token, body, and response values only; no provider network or credential is involved (D-06/D-17). |
| T-146-SC | Tampering | Existing Hex dependency graph | high | mitigate | No package install or identity change occurs; checked lock, exact hashes, unsuppressed audit, and prior exact-SHA fresh evidence preserve the reviewed supply-chain boundary (D-11/D-15). |
</threat_model>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Independently run ScrypathOps on its fixed-compatible web, LiveView, mailer, HTTP, database, and transitive graph | 01-04 | COVERED | Plans 01-03 prove graph/application/audit; Plan 04 closes the only failed mail-client behavior proof. |
| REQ | SEC-03 | Independently resolve ScrypathOps beyond recorded advisories | 01-04 | COVERED | Plan 04 supplies the missing discriminating configured-client contract and reruns binding gates. |
| REQ | EVID-03 | Block Postgrex until stable fixed release is live-confirmed | 01,03 | COVERED | No manifest/lock change occurs in Plan 04; exact hashes preserve the completed dual-source evidence. |
| RESEARCH | — | Direct real Swoosh.ApiClient.Req contract through Req.Test with caller-option conflicts and raw response preservation | 01,04 | COVERED | Plan 04 corrects the text/plain blind spot with application/json while retaining every other assertion. |
| RESEARCH | — | Binding Ops/root gates and exact-SHA detached proof | 02-04 | COVERED | Plan 04 reruns deterministic gates and retains detached proof only after exact graph-hash equality. |
| CONTEXT | D-01, D-02, D-03, D-04, D-05 | Approved bounds, ownership, causal lock, and fresh matrix | 01,03,04 | COVERED | Plan 04 preserves exact manifest/lock evidence and adds no dependency work. |
| CONTEXT | D-06, D-07, D-08, D-09 | Production-shaped Swoosh/Req behavior and configuration isolation | 01,04 | COVERED | Plan 04 directly closes D-07 raw/decode precedence without configuration change. |
| CONTEXT | D-10, D-11, D-12, D-13 | Binding Ops proof, audit ownership, and narrow compatibility boundary | 02-04 | COVERED | Plan 04 reruns existing proof and changes only the focused test. |
| CONTEXT | D-14, D-15, D-16, D-17 | Gate order, evidence, stops, and detached-proof discipline | 02-04 | COVERED | All deterministic gates rerun; exact hashes retain rather than recreate detached evidence. |
| CONTEXT | D-18 | One dependency commit plus the execution-discovered verifier-only exception | 01-04 | COVERED | Plan 04 requires exactly one separately labeled test-only closure and truthful history. |
| CONTEXT | D-19, D-20, D-21, D-22 | Stop boundary, maintainer DX, familiar tools, and no UI/product expansion | 01-04 | COVERED | Explicit scope prohibitions, familiar Mix gates, and UI preservation proof remain binding. |

## UI Preservation Coverage

| UI-SPEC consideration | Plan 04 coverage | Status |
|---|---|---|
| empty | No UI-owned file; mix verify.opsui rerun | COVERED |
| loading | No UI-owned file; mix verify.opsui rerun | COVERED |
| error | No UI-owned file or copy change; mix verify.opsui rerun | COVERED |
| populated | No UI-owned file; mix verify.opsui rerun | COVERED |
| partial | No UI-owned file; mix verify.opsui rerun | COVERED |
| overflow | No layout/style change; mix verify.opsui rerun | COVERED |
| zero-one-many | No collection/render change; mix verify.opsui rerun | COVERED |
| long-text | No label/navigation/container change; mix verify.opsui rerun | COVERED |

<verification>
- The focused test discriminates `decode_body: false` from the retained conflicting caller option by using JSON content and asserting the exact raw binary.
- The closure commit has one exact path and follows, but does not rewrite, `59d2e6a`.
- Manifest/lock hashes remain identical to recorded Phase 146 evidence; checked-lock audit and all specified deterministic gates pass.
- The implementation diff contains no UI, route, schema, provider, CI, ecommerce, production/runtime, or user-owned planning file.
</verification>

<success_criteria>
- SEC-03's sole verification gap is closed by automated behavior proof.
- The dependency remediation remains attributable to `59d2e6a`; the verifier closure is a transparent, separately labeled test-only commit.
- Phase 146 is release-train green and ready for code review plus phase re-verification without repeating detached dependency resolution.
</success_criteria>

<output>
Create `.planning/phases/146-scrypathops-web-client-remediation/146-04-SUMMARY.md` when done.
</output>
