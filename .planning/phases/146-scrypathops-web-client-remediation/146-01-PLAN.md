---
phase: 146-scrypathops-web-client-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scrypath_ops/mix.exs
  - scrypath_ops/mix.lock
  - scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs
autonomous: true
requirements: [SEC-03, EVID-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Per D-01 through D-05, ScrypathOps directly requires Phoenix ~> 1.8.9, Phoenix LiveView ~> 1.1.33, Bandit ~> 1.12.1, Swoosh ~> 1.26.3, Postgrex ~> 0.22.4, and retained Req ~> 0.6.1; Plug, Mint, hpax, Finch, Ecto, Decimal, Phoenix Ecto, and Ecto SQL remain transitive or otherwise unchanged, with no override."
    - "Per D-02 and EVID-03, the Postgrex edit happens only after live Hex reports stable unretired 0.22.4 and the EEF CNA reports versions >= 0.19.3 and < 0.22.4 affected; missing or contradictory evidence stops before any manifest or lock edit."
    - "Per D-06 through D-09, a service-free test exercises the real Swoosh.ApiClient.Req init/post contract through Req.Test, including request precedence, Swoosh user-agent, client_options forwarding, raw response normalization, and transport-error propagation while the normal test and production mailer configuration remain unchanged."
    - "Per D-13 and D-18, any compatibility source fix is absent unless a focused failure proves it, and the manifest, lock, focused client contract, plus any proven internal fix form exactly one isolated ScrypathOps remediation implementation commit."
    - "Per D-20 through D-22, the candidate adds no public API, capability, route, UI, schema, mail-provider choice, policy language, CI subsystem, or broader modernization."
    - "The existing operator empty state remains unchanged and is covered by the no-UI-file diff plus mix verify.opsui (D-10/D-22)."
    - "The existing operator loading state remains unchanged and is covered by the no-UI-file diff plus mix verify.opsui (D-10/D-22)."
    - "The existing operator error state and user-visible copy remain unchanged and are covered by the no-UI-file diff plus mix verify.opsui (D-10/D-22)."
    - "The existing populated operator views remain unchanged and are covered by the no-UI-file diff plus mix verify.opsui (D-10/D-22)."
    - "The existing partial-data operator views remain unchanged and are covered by the no-UI-file diff plus mix verify.opsui (D-10/D-22)."
    - "The existing overflow behavior remains unchanged and is covered by the no-UI-file diff plus mix verify.opsui (D-10/D-22)."
    - "The existing zero/one/many collection behavior remains unchanged and is covered by the no-UI-file diff plus mix verify.opsui (D-10/D-22)."
    - "The existing long-text, label, navigation, and container behavior remains unchanged and is covered by the no-UI-file diff plus mix verify.opsui (D-10/D-22)."
  artifacts:
    - path: "scrypath_ops/mix.exs"
      provides: "Six approved direct fixed-compatible requirements"
      contains: "{:phoenix, \"~> 1.8.9\"}"
    - path: "scrypath_ops/mix.lock"
      provides: "Reviewed causal deterministic resolution for the standalone Ops graph"
    - path: "scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs"
      provides: "Direct service-free Swoosh.ApiClient.Req compatibility contract"
      contains: "Swoosh.ApiClient.Req"
  key_links:
    - from: "scrypath_ops/mix.exs"
      to: "scrypath_ops/mix.lock"
      via: "targeted Mix solver update followed by checked-lock verification"
      pattern: "~> 1\\.(8\\.9|1\\.33|12\\.1|26\\.3)"
    - from: "Swoosh.ApiClient.Req.post/4"
      to: "Req.Test"
      via: "email.private[:client_options] plug tuple with retry disabled"
      pattern: "Req\\.Test"
    - from: "live Postgrex authorities"
      to: "{:postgrex, \"~> 0.22.4\"}"
      via: "fail-closed Hex registry and EEF CNA checks before dependency editing"
  flagged_assumptions:
    - statement: "SEC-03 was classified unclassified/unresolved by the spec-less edge probe; this plan assumes the explicit fixed ranges, causal lock review, real Swoosh client contract, and binding Ops gates fully delimit its edge behavior."
      status: unresolved
      flagged: true
    - statement: "EVID-03 was classified unclassified/unresolved by the spec-less edge probe; this plan assumes live Hex stable/unretired status plus the EEF CNA affected range are the complete dual-source publication guard."
      status: unresolved
      flagged: true
  prohibitions:
    - statement: "Must not silently turn SEC-03 into product/runtime expansion, UI work, route or schema change, provider selection, permanent security infrastructure, ecommerce remediation, or broad package modernization."
      status: unverified
      flagged: true
    - statement: "Must not treat missing or contradictory Postgrex evidence as passing, or substitute a prerelease, invented version, or known-affected fallback."
      status: unverified
      flagged: true
---

<objective>
Create the atomic fixed-compatible ScrypathOps candidate and prove its real production-selected Swoosh Req client through a service-free transport seam.

Purpose: Establish the thinnest production-quality path from approved manifest bounds through a deterministic lock and the configured mail HTTP client before broader application gates run (SEC-03, EVID-03; D-01 through D-09).
Output: One isolated implementation commit containing the reviewed manifest, causal lock closure, and focused Swoosh.ApiClient.Req contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@scrypath_ops/AGENTS.md
@CONTRIBUTING.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
@.planning/phases/146-scrypathops-web-client-remediation/146-RESEARCH.md
@.planning/phases/146-scrypathops-web-client-remediation/146-PATTERNS.md
@.planning/phases/146-scrypathops-web-client-remediation/146-VALIDATION.md
@.planning/phases/146-scrypathops-web-client-remediation/146-UI-SPEC.md
@scrypath_ops/mix.exs
@scrypath_ops/mix.lock
@scrypath_ops/config/test.exs
@scrypath_ops/config/prod.exs
@scrypath_ops/lib/scrypath_ops/mailer.ex
@test/scrypath/meilisearch/client_test.exs
</context>

## Artifacts this phase produces

- Direct dependency strings: Phoenix `~> 1.8.9`, Phoenix LiveView `~> 1.1.33`, Bandit `~> 1.12.1`, Swoosh `~> 1.26.3`, Postgrex `~> 0.22.4`, and retained Req `~> 0.6.1` in `scrypath_ops/mix.exs`.
- Solver-generated causal rows in `scrypath_ops/mix.lock`; every moved row is explained during execution.
- New test module `ScrypathOps.SwooshApiClientReqTest` in `scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs`; no production symbol is added.
- Exactly one ScrypathOps remediation implementation commit containing these three files, plus only a proven minimal internal compatibility fix if a fixed-candidate failure requires one.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build and commit the fixed-candidate Swoosh Req path end to end</name>
  <reversibility rating="costly">The direct bounds alter the standalone Ops resolver contract; reverting is file-local but reopens the advisories and requires the complete proof sequence again.</reversibility>
  <files>scrypath_ops/mix.exs, scrypath_ops/mix.lock, scrypath_ops/test/scrypath_ops/swoosh_api_client_req_test.exs</files>
  <read_first>
    AGENTS.md
    scrypath_ops/AGENTS.md
    CONTRIBUTING.md
    .planning/phases/146-scrypathops-web-client-remediation/146-CONTEXT.md
    .planning/phases/146-scrypathops-web-client-remediation/146-RESEARCH.md
    .planning/phases/146-scrypathops-web-client-remediation/146-PATTERNS.md
    .planning/phases/146-scrypathops-web-client-remediation/146-UI-SPEC.md
    scrypath_ops/mix.exs
    scrypath_ops/mix.lock
    scrypath_ops/config/test.exs
    scrypath_ops/config/prod.exs
    scrypath_ops/lib/scrypath_ops/mailer.ex
    test/scrypath/meilisearch/client_test.exs
    prompts/elixir-opensource-libs-best-practices-deep-research.md
    prompts/elixir-oss-lib-ci-cd-best-practices-deep-research.md
    prompts/phoenix-best-practices-deep-research.md
    prompts/phoenix-live-view-best-practices-deep-research.md
  </read_first>
  <behavior>
    - "D-02/EVID-03 guard: before editing, curl https://hex.pm/api/packages/postgrex and require jq to prove latest_stable_version == 0.22.4 plus an unretired 0.22.4 release; curl https://cna.erlef.org/cves/CVE-2026-66838.json and require jq to prove a postgrex semver affected row begins at 0.19.3 and has lessThan 0.22.4. Any request, parse, or predicate failure stops with no implementation edit."
    - "D-01/D-03 manifest: replace only the five stale direct requirements with the exact approved strings; retain Req ~> 0.6.1; add no direct Plug, Mint, hpax, Finch, Ecto, Decimal, Phoenix Ecto, or Ecto SQL ownership and no override."
    - "D-04 lock: run targeted `mix deps.update phoenix phoenix_live_view bandit swoosh postgrex` from scrypath_ops after recording the pre-edit lock hash; reject and revert unexplained rows rather than accepting package-head churn. Preserve Req >= 0.6.1 < 0.7.0, Mint >= 1.9.3, and hpax >= 1.0.4."
    - "D-06/D-07 happy path: call `Swoosh.ApiClient.Req.init/0` and `post/4` directly. Use a per-test Req.Test plug in `%Swoosh.Email{private: %{client_options: [...]}}`, set retry: false, assert POST method/full URL/request path, exact raw body, provider header, Swoosh user-agent, and a harmless forwarded client option. Supply conflicting client_options values for headers, body, and decode_body and prove the explicit post/4 arguments plus decode_body: false win; return a non-JSON body and assert `{:ok, status, headers, raw_body}`."
    - "D-07 transport path: use Req.Test.transport_error/2 with retry disabled and assert the exact `{:error, %Req.TransportError{reason: :timeout}}` shape returned by the selected Swoosh version; derive the assertion from the installed source if the wrapper adds a documented tag."
    - "D-08/D-09 isolation: keep the module async when direct invocation suffices; do not mutate global Swoosh config. Assert existing test.exs still selects Swoosh.Adapters.Test and `api_client: false`, prod.exs still selects Swoosh.ApiClient.Req, and ScrypathOps.Mailer is unchanged."
    - "D-13/D-18 stop boundary: if fixed dependencies expose a source incompatibility, stop and report it unless the fix is demonstrably ScrypathOps-internal, behavior-preserving, and covered in this same task. Before the executor's single atomic implementation commit, inspect the complete diff, list every lock row as direct or causal, and prove the diff contains no ecommerce, template, LiveView render, router, layout, CSS, JS, component API, schema, migration, provider, or generated artifact change. Preserve unrelated dirty-worktree changes."
  </behavior>
  <action>Start RED by adding the direct real-client tests and confirming the focused file fails against the old Swoosh selection or missing contract. Then perform the live D-02 guard before touching Postgrex, apply exactly the D-01 manifest strings, resolve only the named cohort, review every lock row under D-03/D-04, and make the tests GREEN. Run focused test, checked-lock, and warnings-as-errors compile. Commit all implementation-owned files together once; do not create separate commits for the test, manifest, or lock. Do not stage pre-existing user changes or planning artifacts. D-05 fresh exact-candidate assertions and the full D-10/D-14 gates are intentionally executed against this commit in Plans 02-03, while D-16 makes any failure there block handoff.</action>
  <verify>
    <automated>set -e; curl --fail --silent --show-error https://hex.pm/api/packages/postgrex | jq -e '.latest_stable_version == "0.22.4" and any(.releases[]; .version == "0.22.4" and (.retired == null))' >/dev/null; curl --fail --silent --show-error https://cna.erlef.org/cves/CVE-2026-66838.json | jq -e 'any(.containers.cna.affected[]; .product == "postgrex" and any(.versions[]; .versionType == "semver" and .version == "0.19.3" and .lessThan == "0.22.4" and .status == "affected"))' >/dev/null; cd scrypath_ops; mix test test/scrypath_ops/swoosh_api_client_req_test.exs; mix deps.get --check-locked; mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - "The five edited direct requirements and retained Req requirement exactly match D-01, and no forbidden direct or override ownership appears."
    - "Live Postgrex dual-source predicates pass in the same execution window before the Postgrex edit; otherwise no candidate is committed (D-02/EVID-03)."
    - "The focused contract proves every D-06/D-07 init, request, precedence, raw-body, normalization, and error behavior without network, credentials, provider calls, or fake production adapter."
    - "Only the three declared files, plus a separately explained and focused-test-covered internal compatibility fix if required by D-13, enter one implementation commit (D-18)."
  </acceptance_criteria>
  <done>The atomic ScrypathOps candidate exists, focused Swoosh Req behavior passes, the checked lock compiles without warnings, and the UI/config/schema/public-surface preservation boundary is intact.</done>
</task>

</tasks>

<threat_model>
## Assets and Trust Boundaries

| Boundary | Protected asset | Exposure |
|---|---|---|
| Hex registry / EEF CNA → maintainer process | Correct Postgrex fixed-release decision | Network data can be unavailable, stale, malformed, or contradictory. |
| Mix manifest → Hex solver → checked lock | Deterministic fixed-compatible Ops graph | Solver churn or dependency confusion can select vulnerable/out-of-scope versions. |
| Swoosh.ApiClient.Req → Req.Test plug | Production mail HTTP-client semantics | A fake or globally mutated seam can conceal request/response regressions. |
| Git working tree → implementation commit | User changes and isolated remediation history | Broad staging can mix unrelated or sensitive files into the candidate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-146-01 | Tampering | Postgrex live evidence | high | mitigate | Require both exact jq predicates before editing; any fetch/parse/conflict stops under D-02/D-16. |
| T-146-02 | Tampering / DoS | Mix solver and lock | high | mitigate | Exact direct bounds, targeted update, causal row review, checked-lock compile, and later detached D-05 assertions. |
| T-146-03 | Spoofing | Swoosh compatibility test | medium | mitigate | Invoke the real Swoosh.ApiClient.Req module and supported Req.Test seam; preserve test/prod config selections. |
| T-146-04 | Information disclosure | Req test and commit | medium | mitigate | Use synthetic URL/body/headers only; no provider credentials/network; inspect staged paths before commit. |
| T-146-SC | Tampering | Existing Hex packages | high | mitigate | No package identity is introduced; use only official existing Hex packages from the research legitimacy audit, fixed bounds, lock checks, and unsuppressed audit in Plan 03. |
</threat_model>

<verification>
- Focused real-client contract, checked lock, and warnings-as-errors compile pass on the same candidate.
- Live Hex/CNA evidence passes before Postgrex changes and is not cached as a substitute for Plan 03's recheck.
- Git diff/staging shows only declared Ops implementation files; no UI/config/schema/ecommerce surface changes.
</verification>

<success_criteria>
- SEC-03 has an atomic fixed-compatible implementation candidate with a real service-free Swoosh Req contract.
- EVID-03 blocks the candidate unless stable unretired Postgrex 0.22.4 and the CNA fixed boundary are both live-confirmed.
- Exactly one implementation commit contains the manifest, causal lock, and focused contract.
</success_criteria>

<output>
Create `.planning/phases/146-scrypathops-web-client-remediation/146-01-SUMMARY.md` when done.
</output>
