---
phase: 83-composition-presets-and-scope-contract
plan: 03
type: execute
wave: 2
depends_on: [83-01, 83-02]
files_modified:
  - lib/scrypath/composition.ex
  - lib/scrypath/composition/normalize.ex
  - lib/scrypath/composition/merge.ex
  - lib/scrypath/composition/result.ex
  - test/scrypath/composition_test.exs
  - test/scrypath/composition_property_test.exs
autonomous: true
requirements: [CMP-01, CMP-02, CMP-03, CMP-04]
must_haves:
  truths:
    - "Named presets and additive scopes resolve into canonical single-search args without introducing a second runtime."
    - "Defaults, fixed constraints, and conflict errors behave deterministically across repeated calls."
    - "The composed output exposes enough coarse visibility for tests and logs while keeping policy ownership in the host app."
  artifacts:
    - path: "lib/scrypath/composition/normalize.ex"
      provides: "fragment normalization and canonicalization"
    - path: "lib/scrypath/composition/merge.ex"
      provides: "field-specific precedence and conflict detection"
    - path: "lib/scrypath/composition.ex"
      provides: "public compose/to_search_args implementation"
  key_links:
    - from: "lib/scrypath/composition/merge.ex"
      to: "lib/scrypath/options.ex"
      via: "reusing canonical search option vocabulary and validation rules"
      pattern: "validate_search_filter|validate_per_query_map|validate_search_page"
    - from: "lib/scrypath/composition.ex"
      to: "test/scrypath/composition_test.exs"
      via: "green contract implementation"
      pattern: "compose\\(|to_search_args"
---

<objective>
Implement the actual Phase 83 merge engine and make the new contract green.

Purpose: Deliver the bounded composition seam promised by the milestone, using field-specific precedence and explicit conflict detection while keeping `Scrypath.search/3` canonical.
Output: Working composition modules that satisfy the new tests and focused verify gate.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/83-composition-presets-and-scope-contract/83-CONTEXT.md
@.planning/phases/83-composition-presets-and-scope-contract/83-RESEARCH.md
@.planning/phases/83-composition-presets-and-scope-contract/83-PATTERNS.md
@.planning/phases/83-composition-presets-and-scope-contract/83-01-PLAN.md
@.planning/phases/83-composition-presets-and-scope-contract/83-02-PLAN.md
@lib/scrypath/search.ex
@lib/scrypath/options.ex
@lib/scrypath/query_params.ex
@lib/scrypath/multi_search/entries.ex
@test/scrypath/composition_test.exs
@test/scrypath/composition_property_test.exs

<interfaces>
From `lib/scrypath/search.ex`:
```elixir
@spec search(module(), String.t(), keyword()) :: {:ok, SearchResult.t()} | {:error, term()}
```

From `lib/scrypath/options.ex`:
```elixir
def validate_search_filter(value) when is_list(value) ...
def validate_per_query_map(value) ...
def validate_search_page(value) when is_list(value) ...
```

From `lib/scrypath/multi_search/entries.ex`:
```elixir
merged <- Keyword.merge(shared, entry_core, fn _k, _s, e -> e end)
Map.merge(per_query_as_map(s), per_query_as_map(e))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fragment normalization and field-scoped validation</name>
  <read_first>lib/scrypath/composition.ex, lib/scrypath/composition/normalize.ex, lib/scrypath/options.ex, test/scrypath/composition_test.exs, .planning/phases/83-composition-presets-and-scope-contract/83-CONTEXT.md</read_first>
  <files>lib/scrypath/composition.ex, lib/scrypath/composition/normalize.ex, lib/scrypath/composition/result.ex</files>
  <behavior>
    - Test 1: only `text`, `filter`, `sort`, `page`, `facets`, `facet_filter`, and `per_query` are accepted in `defaults`.
    - Test 2: only `filter` and `facet_filter` are accepted in `fixed`.
    - Test 3: keyword-bearing fields normalize to deterministic unique-key forms before policy logic runs.
  </behavior>
  <acceptance_criteria>
    - `rg -n "invalid_fragment|invalid_defaults_field|invalid_fixed_field" lib/scrypath/composition.ex lib/scrypath/composition/normalize.ex` finds the explicit validation heads.
    - `rg -n "Keyword\\.new|Map\\.new|validate_per_query_map|validate_search_page" lib/scrypath/composition/normalize.ex` finds canonicalization and validator reuse.
    - `mix test test/scrypath/composition_test.exs --seed 0` passes the validation-oriented cases.
  </acceptance_criteria>
  <action>Implement normalization per D-05 through D-12. Create `lib/scrypath/composition/normalize.ex` to accept one fragment map or a list of fragment maps plus caller criteria, canonicalize `filter` and `facet_filter` with unique-key ordering before merge policy, treat blank caller text like absent text per D-08, whole-value normalize `sort`, `page`, and `facets`, and normalize `per_query` through the existing `Scrypath.Options.validate_per_query_map/1`. Reject any fixed keys outside `filter` and `facet_filter` with stable field-scoped tuple errors, and reject surprising unlock sentinels by treating `nil`/empty values as ordinary data rather than “remove fixed” commands per D-12.</action>
  <verify>
    <automated>mix test test/scrypath/composition_test.exs</automated>
  </verify>
  <done>Normalization accepts only the bounded fragment grammar and feeds the merge layer with deterministic, runtime-compatible values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement precedence, conflict detection, and visibility output</name>
  <read_first>lib/scrypath/composition/merge.ex, lib/scrypath/composition/result.ex, lib/scrypath/multi_search/entries.ex, test/scrypath/composition_test.exs, test/scrypath/composition_property_test.exs, .planning/phases/83-composition-presets-and-scope-contract/83-CONTEXT.md</read_first>
  <files>lib/scrypath/composition.ex, lib/scrypath/composition/merge.ex, lib/scrypath/composition/result.ex, test/scrypath/composition_test.exs, test/scrypath/composition_property_test.exs</files>
  <behavior>
    - Test 1: caller wins over `defaults` on overlapping `filter`/`facet_filter` keys, but `fixed` collisions fail explicitly.
    - Test 2: competing fixed constraints on the same key fail explicitly.
    - Test 3: `per_query` shallow-merges with caller bias.
    - Test 4: visibility buckets report exactly what final criteria were applied, defaulted, or fixed.
  </behavior>
  <acceptance_criteria>
    - `rg -n "composition_conflict" lib/scrypath/composition/merge.ex` finds explicit conflict tuple generation.
    - `rg -n "Map\\.merge\\(.*per_query|Keyword\\.put\\(.*page|facets|sort" lib/scrypath/composition/merge.ex` finds field-specific merge logic instead of a generic deep merge.
    - `rg -n "applied|defaulted|fixed|sources|warnings" lib/scrypath/composition/result.ex lib/scrypath/composition.ex` finds the visibility builder.
    - `mix test test/scrypath/composition_test.exs test/scrypath/composition_property_test.exs` passes.
  </acceptance_criteria>
  <action>Implement `lib/scrypath/composition/merge.ex` and finish `Scrypath.Composition.compose/2` per D-09 through D-18. Use whole-value caller override for `sort`, `page`, and `facets`; caller-wins key merges for default `filter` and `facet_filter`; shallow map merge for `per_query`; and explicit `{:error, {:composition_conflict, field, key, details}}` on caller-vs-fixed or fixed-vs-fixed collisions per D-13 and D-14. Build the public result map from the same canonicalized final state so `applied`, `defaulted`, and `fixed` cannot drift from `to_search_args/1`. Keep `compose!/2` as a thin raising wrapper over `compose/2`, and have `to_search_args/1` emit the exact `QueryParams.to_search_args/1` field order: `filter`, `sort`, `page`, `facets`, `facet_filter`, `per_query`.</action>
  <verify>
    <automated>mix test test/scrypath/composition_test.exs test/scrypath/composition_property_test.exs</automated>
  </verify>
  <done>The implementation matches the locked precedence model and returns debug-friendly visibility without leaking internals or adding an executor.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the full phase gate and keep the public seam honest</name>
  <read_first>lib/mix/tasks/verify.phase83.ex, lib/scrypath.ex, test/scrypath/docs_contract_test.exs, mix.exs, CONTRIBUTING.md</read_first>
  <files>lib/scrypath/composition.ex, test/scrypath/composition_test.exs, test/scrypath/composition_property_test.exs</files>
  <acceptance_criteria>
    - `mix verify.phase83` exits successfully.
    - `mix test --exclude integration --exclude docs_contract` exits successfully after the new composition files are added.
    - `rg -n "search/3 remains the canonical runtime entrypoint|Scrypath\\.Composition" lib/scrypath.ex` still reflects the public boundary after implementation.
  </acceptance_criteria>
  <action>Run the full focused gate and fix any remaining contract drift in the implementation, not by weakening tests. Treat docs-contract failures that would blur D-19 through D-22 as correctness bugs. Do not add Phoenix-owned composition helpers, schema macros, saved-search persistence, public `%Scrypath.Query{}` exposure, or any helper that directly executes search. Leave `Scrypath.search/3` as the canonical runtime and make the composition layer stop at `{text, keyword_opts}`.</action>
  <verify>
    <automated>mix verify.phase83</automated>
  </verify>
  <done>Phase 83 closes with one working public seam, a passing focused verify task, and no boundary drift into deferred scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host fragments → normalization/merge | Callers can send malformed or conflicting policy data that must not silently bypass fixed constraints |
| composition output → host logging/tests | Visibility metadata must reflect final criteria exactly without becoming an execution authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-07 | T | `lib/scrypath/composition/merge.ex` | mitigate | Canonicalize keyword fields before merge and reject caller-vs-fixed or fixed-vs-fixed collisions with explicit tuples instead of last-write wins. |
| T-83-08 | I | `lib/scrypath/composition/result.ex` | mitigate | Keep visibility coarse-grained (`applied/defaulted/fixed/sources/warnings`) and derive it from the final canonical state so it does not leak internal merge traces. |
| T-83-09 | E | `lib/scrypath/composition.ex` | mitigate | Keep composition as data transformation only and make callers explicitly pass `to_search_args/1` output into `Scrypath.search/3`; do not add runtime helpers. |
</threat_model>

<verification>
Primary gate: `mix verify.phase83`

Secondary confidence check: `mix test --exclude integration --exclude docs_contract`
</verification>

<success_criteria>
The new public composition seam composes presets and scopes into canonical single-search args with deterministic precedence, explicit fixed-constraint failures, and stable visibility metadata, all while preserving the existing runtime boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/83-composition-presets-and-scope-contract/83-03-SUMMARY.md`
</output>
