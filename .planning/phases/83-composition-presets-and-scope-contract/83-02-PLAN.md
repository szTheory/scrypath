---
phase: 83-composition-presets-and-scope-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scrypath/composition_test.exs
  - test/scrypath/composition_property_test.exs
  - test/scrypath/docs_contract_test.exs
  - lib/mix/tasks/verify.phase83.ex
  - mix.exs
  - mix.lock
autonomous: true
requirements: [CMP-01, CMP-02, CMP-03, CMP-04]
must_haves:
  truths:
    - "Phase 83 has explicit executable proof that presets and scopes must resolve into canonical single-search args."
    - "Phase 83 has explicit executable proof that precedence, conflict handling, and visibility metadata stay deterministic."
    - "Phase 83 has explicit executable proof that the public composition seam stays context-owned and Phoenix-optional."
  artifacts:
    - path: "test/scrypath/composition_test.exs"
      provides: "unit and parity contract coverage for composition"
    - path: "test/scrypath/composition_property_test.exs"
      provides: "property coverage for idempotence and deterministic output"
    - path: "lib/mix/tasks/verify.phase83.ex"
      provides: "focused phase verification task"
  key_links:
    - from: "lib/mix/tasks/verify.phase83.ex"
      to: "test/scrypath/composition_test.exs"
      via: "focused test list"
      pattern: "composition_test\\.exs"
    - from: "test/scrypath/docs_contract_test.exs"
      to: "lib/scrypath.ex"
      via: "public boundary wording assertions"
      pattern: "Scrypath\\.Composition|search/3|Scrypath\\.Phoenix"
---

<objective>
Create the Wave 0 verification harness for Phase 83.

Purpose: Lock the contract before implementation so the merge engine can be built against deterministic tests and a focused verify command.
Output: Composition unit/property tests, docs-contract assertions, and `mix verify.phase83`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/83-composition-presets-and-scope-contract/83-CONTEXT.md
@.planning/phases/83-composition-presets-and-scope-contract/83-RESEARCH.md
@.planning/phases/83-composition-presets-and-scope-contract/83-PATTERNS.md
@test/scrypath/query_params_test.exs
@test/scrypath/multi_search/entries_test.exs
@test/scrypath/docs_contract_test.exs
@lib/mix/tasks/verify.phase82.ex
@mix.exs

<interfaces>
From `test/scrypath/query_params_test.exs`:
```elixir
assert {"phoenix", [filter: ..., sort: ..., page: ..., facets: ..., facet_filter: ..., per_query: ...]} =
         QueryParams.to_search_args(query_params)
```

From `lib/mix/tasks/verify.phase82.ex`:
```elixir
@focused_tests [...]
Mix.Task.run("docs", ["--warnings-as-errors"])
```

From `test/scrypath/multi_search/entries_test.exs`:
```elixir
assert {:error, {:invalid_options, {:page_size, 51, 50}}} = ...
assert Keyword.get(merged, :page) == [size: 5]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit-level composition contract tests</name>
  <read_first>test/scrypath/composition_test.exs, test/scrypath/query_params_test.exs, test/scrypath/multi_search/entries_test.exs, .planning/phases/83-composition-presets-and-scope-contract/83-CONTEXT.md</read_first>
  <files>test/scrypath/composition_test.exs</files>
  <behavior>
    - Test 1: `compose/2` expands plain-data presets into the same `{text, keyword_opts}` shape accepted by `Scrypath.search/3` per D-01, D-06, and CMP-01.
    - Test 2: caller text overrides default text while blank or missing caller text receives the preset default per D-08.
    - Test 3: `sort`, `page`, and `facets` replace whole defaults rather than deep-merging per D-09.
    - Test 4: `filter` and `facet_filter` merge defaults by key with caller bias, while fixed conflicts return explicit field-scoped errors per D-10, D-13, and D-14.
    - Test 5: result visibility exposes `applied`, `defaulted`, `fixed`, optional `sources`, and optional `warnings` in the public criteria vocabulary per D-15 through D-17.
  </behavior>
  <acceptance_criteria>
    - `rg -n "caller text overrides default text|fixed conflicts|defaulted|to_search_args" test/scrypath/composition_test.exs` finds explicit contract cases.
    - `rg -n "refute match\\?\\(%Scrypath\\.Query\\{\\}" test/scrypath/composition_test.exs` finds the public-boundary regression.
    - `mix test test/scrypath/composition_test.exs` fails before plan `83-03` implementation and passes after it.
  </acceptance_criteria>
  <action>Create `test/scrypath/composition_test.exs` around the exact public names chosen in plan `83-01`. Use the same plain-data assertion style as `QueryParamsTest`, not backend-specific or schema-macro APIs. Pin all field rules from D-05 through D-18 with direct assertions over the returned result map and `to_search_args/1`, including: whole-value override for `sort`, `page`, `facets`; shallow caller-wins merge for `per_query`; key-based caller-wins defaults for `filter` and `facet_filter`; explicit `{:error, {:composition_conflict, field, key, details}}`-style failures for caller-vs-fixed and fixed-vs-fixed conflicts; and visibility maps that mirror the final criteria keys.</action>
  <verify>
    <automated>mix test test/scrypath/composition_test.exs</automated>
  </verify>
  <done>The main Phase 83 semantics are executable, precise, and independent of Phoenix or schema-owned declarations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic verification wiring and property coverage</name>
  <read_first>test/scrypath/docs_contract_test.exs, lib/mix/tasks/verify.phase82.ex, mix.exs, .planning/phases/83-composition-presets-and-scope-contract/83-RESEARCH.md, CONTRIBUTING.md</read_first>
  <files>test/scrypath/composition_property_test.exs, test/scrypath/docs_contract_test.exs, lib/mix/tasks/verify.phase83.ex, mix.exs, mix.lock</files>
  <behavior>
    - Test 1: repeated composition of equivalent inputs stays idempotent and deterministic.
    - Test 2: docs contracts fail if the public story drifts toward `%Scrypath.Query{}`, schema-owned composition, or Phoenix-owned composition helpers.
    - Test 3: `mix verify.phase83` runs the focused composition tests and `mix docs --warnings-as-errors`.
  </behavior>
  <acceptance_criteria>
    - `rg -n "verify\\.phase83" lib/mix/tasks/verify.phase83.ex mix.exs` finds the new task and preferred env wiring.
    - `rg -n "Scrypath\\.Composition|schema-generated|Scrypath\\.Phoenix" test/scrypath/docs_contract_test.exs` finds boundary assertions for the new seam.
    - `rg -n "use ExUnitProperties|property " test/scrypath/composition_property_test.exs` finds real property tests.
    - `rg -n "stream_data" mix.exs` finds the test-only dependency when property tests are added.
    - `rg -n "stream_data" mix.lock` finds the resolved lock entry after `mix deps.get`.
  </acceptance_criteria>
  <action>Add `test/scrypath/composition_property_test.exs` using `StreamData` and `ExUnitProperties`, and add `{:stream_data, "~> 1.3", only: :test}` to `mix.exs` plus the corresponding `mix.lock` entry by running `mix deps.get`, so the dependency is resolved in-repo and the verification command is runnable from a clean checkout. Cover idempotence, stable ordering after keyword canonicalization, and repeated-call equality for equivalent fragment/input shapes per D-09 through D-14. Add narrow `DocsContractTest` assertions that the published boundary names `Scrypath.Composition`, keeps `Scrypath.search/3` canonical, keeps `Scrypath.Phoenix` request-edge-only, and rejects public `%Scrypath.Query{}` or schema/Phoenix-owned composition per D-19 through D-22. Create `lib/mix/tasks/verify.phase83.ex` by copying the focused Phase 82 pattern: no args, run only the composition unit/property/docs-contract files, then run `mix docs --warnings-as-errors`.</action>
  <verify>
    <automated>mix deps.get && mix test test/scrypath/composition_property_test.exs test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <done>Phase 83 has a focused red/green verification seam and property coverage for the merge rules most likely to drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer edits → focused verify task | The gate must detect composition drift before public docs or behavior diverge |
| docs/examples → adopter understanding | Boundary wording must not imply unauthorized runtime ownership or security guarantees |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-04 | T | `test/scrypath/composition_test.exs` | mitigate | Pin exact precedence and conflict behavior with executable cases before implementation lands. |
| T-83-05 | S | `test/scrypath/docs_contract_test.exs` | mitigate | Add contract assertions that public docs keep contexts canonical and keep tenant/authz claims out of the composition story. |
| T-83-06 | D | `lib/mix/tasks/verify.phase83.ex` | mitigate | Keep the verify task focused and auth-free so contributors can run it locally without external services or optional app boot. |
</threat_model>

<verification>
This plan’s task-level checks should fail until plan `83-03` lands the implementation. After plan `83-03`, `mix verify.phase83` is the phase gate.
</verification>

<success_criteria>
There is a narrow, contributor-runnable verification lane that proves the composition contract, docs boundary, and deterministic merge semantics without involving live services.
</success_criteria>

<output>
After completion, create `.planning/phases/83-composition-presets-and-scope-contract/83-02-SUMMARY.md`
</output>
