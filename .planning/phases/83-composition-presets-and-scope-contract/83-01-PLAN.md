---
phase: 83-composition-presets-and-scope-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/composition.ex
  - lib/scrypath/composition/result.ex
  - lib/scrypath.ex
autonomous: true
requirements: [CMP-01, CMP-03, CMP-04]
must_haves:
  truths:
    - "The public `Scrypath.Composition` contract is frozen as a plain-data seam over the existing search-args vocabulary."
    - "The result/visibility vocabulary is documented without exposing `%Scrypath.Query{}` or promising a second runtime."
    - "Root boundary docs keep composition context-owned and keep `Scrypath.Phoenix` as optional request-edge glue."
  artifacts:
    - path: "lib/scrypath/composition.ex"
      provides: "public plain-data composition API"
      exports: ["compose/2", "compose!/2", "to_search_args/1"]
    - path: "lib/scrypath/composition/result.ex"
      provides: "typed visibility/result helper without exposing internal query structs"
    - path: "lib/scrypath.ex"
      provides: "root boundary docs that place composition before context-owned `search/3`"
  key_links:
    - from: "lib/scrypath/composition.ex"
      to: "lib/scrypath/query_params.ex"
      via: "shared plain-data criteria vocabulary"
      pattern: "text|filter|sort|page|facets|facet_filter|per_query"
    - from: "lib/scrypath/composition.ex"
      to: "lib/scrypath.ex"
      via: "public entrypoint docs"
      pattern: "Scrypath.Composition|search/3"
---

<objective>
Freeze the public Phase 83 composition contract before merge logic lands.

Purpose: Make the plain-data seam explicit so downstream implementation and tests build against one stable contract instead of inferring it from merge internals.
Output: Public `Scrypath.Composition` and root boundary docs that define the result shape, exported functions, and non-goals.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/83-composition-presets-and-scope-contract/83-CONTEXT.md
@.planning/phases/83-composition-presets-and-scope-contract/83-RESEARCH.md
@.planning/phases/83-composition-presets-and-scope-contract/83-PATTERNS.md
@.planning/phases/81-edge-normalization-errors-and-phoenix-wrappers/81-CONTEXT.md
@.planning/phases/82-docs-examples-and-drift-protection/82-CONTEXT.md
@lib/scrypath.ex
@lib/scrypath/query_params.ex
@lib/scrypath/search.ex

<interfaces>
From `lib/scrypath/query_params.ex`:
```elixir
@type t :: %{
        text: String.t(),
        filter: keyword(),
        sort: keyword(),
        page: keyword(),
        facets: [atom()],
        facet_filter: keyword(),
        per_query: map()
      }

@spec to_search_args(t()) :: {String.t(), keyword()}
```

From `lib/scrypath.ex`:
```elixir
@spec search(module(), String.t(), keyword()) :: {:ok, term()} | {:error, term()}
@spec search!(module(), String.t(), keyword()) :: term()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the public plain-data composition contract</name>
  <read_first>lib/scrypath/composition.ex, lib/scrypath/query_params.ex, .planning/phases/83-composition-presets-and-scope-contract/83-CONTEXT.md, .planning/phases/83-composition-presets-and-scope-contract/83-PATTERNS.md</read_first>
  <files>lib/scrypath/composition.ex, lib/scrypath/composition/result.ex</files>
  <acceptance_criteria>
    - `rg -n "@spec compose\\(.*\\) :: \\{:ok,.*\\} \\| \\{:error, term\\(\\)\\}" lib/scrypath/composition.ex` finds the tuple API.
    - `rg -n "@spec compose!\\(" lib/scrypath/composition.ex` finds the bang API.
    - `rg -n "@spec to_search_args\\(" lib/scrypath/composition.ex` finds canonical conversion back to `{text, keyword}`.
    - `rg -n "defaults|fixed|applied|defaulted|sources|warnings" lib/scrypath/composition.ex lib/scrypath/composition/result.ex` finds the public envelope/result vocabulary.
    - `rg -n "%Scrypath.Query\\{\\}" lib/scrypath/composition.ex lib/scrypath/composition/result.ex` returns no matches.
  </acceptance_criteria>
  <action>Author `Scrypath.Composition` as the public Phase 83 seam per D-01 through D-04, D-15 through D-18, and D-23. Use a plain-map contract, not a public struct, for both fragment input and composed result. Export exactly `compose/2`, `compose!/2`, and `to_search_args/1`. Freeze the fragment envelope to `%{defaults: ..., fixed: ..., sources: ..., warnings: ...}` with `defaults` admitting `text`, `filter`, `sort`, `page`, `facets`, `facet_filter`, and `per_query` per D-06, and `fixed` admitting only `filter` and `facet_filter` per D-07. Define a result map that carries final criteria plus coarse visibility buckets `applied`, `defaulted`, `fixed`, optional `sources`, and optional `warnings` per D-16. Keep the module data-only and make the docs explicit that it never executes search and never exposes `%Scrypath.Query{}`.</action>
  <verify>
    <automated>rg -n "@spec compose\\(.*\\) :: \\{:ok,.*\\} \\| \\{:error, term\\(\\)\\}" lib/scrypath/composition.ex && rg -n "@spec compose!\\(" lib/scrypath/composition.ex && rg -n "@spec to_search_args\\(" lib/scrypath/composition.ex && rg -n "defaults|fixed|applied|defaulted|sources|warnings" lib/scrypath/composition.ex lib/scrypath/composition/result.ex && ! rg -n "%Scrypath.Query\\{\\}" lib/scrypath/composition.ex lib/scrypath/composition/result.ex</automated>
  </verify>
  <done>Executors have one stable public contract to implement, and the file documents the exact plain-data input/output vocabulary without freezing a schema DSL or Phoenix-owned surface.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the root boundary docs to the new composition seam</name>
  <read_first>lib/scrypath.ex, lib/scrypath/composition.ex, .planning/phases/83-composition-presets-and-scope-contract/83-CONTEXT.md, .planning/ROADMAP.md</read_first>
  <files>lib/scrypath.ex</files>
  <acceptance_criteria>
    - `rg -n "Scrypath\\.Composition" lib/scrypath.ex` finds the new entrypoint mention.
    - `rg -n "contexts|search/3 remains the canonical runtime entrypoint" lib/scrypath.ex` still finds the context-owned runtime boundary.
    - `rg -n "optional glue" lib/scrypath.ex` still describes `Scrypath.Phoenix` as request-edge only.
    - `rg -n "schema-specific runtime verbs|generated UI|tenant" lib/scrypath.ex` finds boundary-honest non-goal language for the new seam.
  </acceptance_criteria>
  <action>Update the `Scrypath` moduledoc and entrypoint list so the new public surface is described as plain-data composition before a context-owned `Scrypath.search/3` call, per D-02, D-19, D-20, D-21, and D-22. Keep `Scrypath.Phoenix` framed as optional request-edge glue from Phase 81/82, and add one concise sentence that composition does not move policy onto schemas, does not create generated runtime verbs, and does not solve tenant authz or related-data propagation.</action>
  <verify>
    <automated>rg -n "Scrypath\\.Composition" lib/scrypath.ex && rg -n "contexts|search/3 remains the canonical runtime entrypoint" lib/scrypath.ex && rg -n "optional glue" lib/scrypath.ex && rg -n "schema-specific runtime verbs|generated UI|tenant" lib/scrypath.ex && mix docs --warnings-as-errors</automated>
  </verify>
  <done>The root public docs place composition on the same boundary line as the existing request-edge toolkit: composition is data preparation, contexts still call `search/3`, and Phoenix stays optional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host context code → `Scrypath.Composition` | Untrusted fragment shapes and policy declarations cross into the library surface here |
| `Scrypath.Composition` → `Scrypath.search/3` callers | Final criteria and visibility metadata must remain data-only so the resolver does not become an executor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | T | `lib/scrypath/composition.ex` | mitigate | Freeze the exact accepted fragment keys and result keys in typedocs and docs so later implementation can reject undeclared shapes instead of accepting hidden policy input. |
| T-83-02 | S | `lib/scrypath.ex` docs | mitigate | Keep explicit non-goal wording that composition does not imply tenant-safe access, Phoenix-owned policy, or schema-generated verbs. |
| T-83-03 | E | public API boundary | mitigate | Keep composition data-only and route execution explicitly to existing `Scrypath.search/3`; do not expose a helper that performs search from fragments. |
</threat_model>

<verification>
Run the self-contained contract/docs checks in the task `<verify>` blocks after the public seam and root docs are in place, then let plan `83-03` prove full phase behavior with `mix verify.phase83`.
</verification>

<success_criteria>
The repo exposes one explicit public composition contract that reuses the existing search-args vocabulary, documents the visibility buckets and non-goals, and keeps `Scrypath.search/3` as the only runtime executor.
</success_criteria>

<output>
After completion, create `.planning/phases/83-composition-presets-and-scope-contract/83-01-SUMMARY.md`
</output>
