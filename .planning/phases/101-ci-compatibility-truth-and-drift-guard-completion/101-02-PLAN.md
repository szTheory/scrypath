---
phase: 101
plan: 2
type: execute
wave: 2
depends_on:
  - 101-01
files_modified:
  - test/scrypath/phase99_contract_test.exs
autonomous: true
requirements:
  - TEST-01
  - TRUTH-03
must_haves:
  - "Extend `test/scrypath/phase99_contract_test.exs` with semantic tuple extraction/parity helpers comparing support-guide claims to `.github/workflows/ci.yml` compatibility tuples."
  - "Assert floor/head evidence explicitly: `mix.exs` floor `~> 1.17`, plus CI tuple coverage for `1.17.x` and `1.19.0`."
  - "Keep assertions token/semantic based with actionable mismatch output; no prose snapshot assertions."
---

<objective>
Convert compatibility truth into deterministic drift guards by adding semantic parity assertions that compare canonical support claims, executable CI tuples, and floor-policy signals.
</objective>

<context>
Wave 2 closes TEST-01 for phase 101 by extending the existing phase99 trust seam. Enforcement remains under `mix verify.phase99`; no new verify alias or required-check category is introduced.
</context>

<tasks>
### Task 101-02-01 - Add normalized compatibility tuple extraction helpers
<read_first>
- `test/scrypath/phase99_contract_test.exs`
- `guides/support-and-compatibility.md`
- `.github/workflows/ci.yml`
- `mix.exs`
- `.planning/phases/101-ci-compatibility-truth-and-drift-guard-completion/101-PATTERNS.md`
</read_first>
<action>
- Add helper functions in `test/scrypath/phase99_contract_test.exs` to normalize tuple facts:
  - `extract_support_ci_tuples/1` parses support-guide bullets under `GitHub Actions exercises:`.
  - `extract_workflow_ci_tuples/1` parses the `compatibility-truth` lane tuples from `.github/workflows/ci.yml`.
  - `normalize_tuple_set/1` canonicalizes `{elixir, otp}` pair ordering and de-duplicates.
- Keep helper output structured as `[{\"1.17.3\", \"26.2.5\"}, {\"1.19.0\", \"28.1\"}]` style tuples for deterministic comparisons.
- Add helper-level assertion messages that print expected/actual tuple sets on mismatch.
</action>
<acceptance_criteria>
- `rg "extract_support_ci_tuples|extract_workflow_ci_tuples|normalize_tuple_set" "test/scrypath/phase99_contract_test.exs"` returns at least 3 matches.
- `rg "1\\.17\\.3|26\\.2\\.5|1\\.19\\.0|28\\.1" "test/scrypath/phase99_contract_test.exs"` returns at least 4 matches.
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/phase99_contract_test.exs"` exits 0.
</acceptance_criteria>
<verify>
- `rg "extract_support_ci_tuples|extract_workflow_ci_tuples|normalize_tuple_set" "test/scrypath/phase99_contract_test.exs"`
- `rg "1\\.17\\.3|26\\.2\\.5|1\\.19\\.0|28\\.1" "test/scrypath/phase99_contract_test.exs"`
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/phase99_contract_test.exs"`
</verify>

### Task 101-02-02 - Add TRUTH-03 semantic parity assertions
<read_first>
- `test/scrypath/phase99_contract_test.exs`
- `guides/support-and-compatibility.md`
- `.github/workflows/ci.yml`
- `README.md`
- `CONTRIBUTING.md`
- `guides/outside-adopter-intake.md`
</read_first>
<action>
- Add a new `describe "TRUTH-03 compatibility truth parity"` block to assert:
  - Canonical support tuples equal workflow compatibility-truth tuples exactly.
  - Required route token `guides/support-and-compatibility.md` remains present in README/CONTRIBUTING/intake surfaces.
  - Non-owner surfaces do not carry full tuple-value copies (`1.17.3/26.2.5`, `1.19.0/28.1`).
- Keep assertion failures explicit with tuple-diff output and targeted token names.
</action>
<acceptance_criteria>
- `rg "TRUTH-03 compatibility truth parity|compatibility-truth|guides/support-and-compatibility\\.md" "test/scrypath/phase99_contract_test.exs"` returns at least 3 matches.
- `rg "extract_support_ci_tuples\\(@support_guide\\)|extract_workflow_ci_tuples\\(@ci_workflow\\)" "test/scrypath/phase99_contract_test.exs"` returns at least 2 matches.
- `mix test test/scrypath/phase99_contract_test.exs` exits 0.
</acceptance_criteria>
<verify>
- `rg "TRUTH-03 compatibility truth parity|compatibility-truth|guides/support-and-compatibility\\.md" "test/scrypath/phase99_contract_test.exs"`
- `rg "extract_support_ci_tuples\\(@support_guide\\)|extract_workflow_ci_tuples\\(@ci_workflow\\)" "test/scrypath/phase99_contract_test.exs"`
- `mix test test/scrypath/phase99_contract_test.exs`
</verify>

### Task 101-02-03 - Add TEST-01 floor/head evidence assertions
<read_first>
- `test/scrypath/phase99_contract_test.exs`
- `mix.exs`
- `guides/support-and-compatibility.md`
- `.github/workflows/ci.yml`
</read_first>
<action>
- Add a new `describe "TEST-01 CI-version parity anchors"` block that asserts:
  - `mix.exs` keeps the floor token `elixir: "~> 1.17"`.
  - Parsed compatibility tuples include one floor-line tuple (`1.17.3`) and one head tuple (`1.19.0`).
  - OTP tuple set includes `26.2.5` and `28.1`.
- Keep these assertions in `phase99_contract_test.exs` (not workflow wiring tests) to preserve semantic-vs-wiring ownership boundaries.
</action>
<acceptance_criteria>
- `rg 'elixir: "~> 1\\.17"|TEST-01 CI-version parity anchors|1\\.17\\.3|1\\.19\\.0|26\\.2\\.5|28\\.1' "test/scrypath/phase99_contract_test.exs"` returns at least 6 matches.
- `mix test test/scrypath/phase99_contract_test.exs` exits 0.
</acceptance_criteria>
<verify>
- `rg 'elixir: "~> 1\\.17"|TEST-01 CI-version parity anchors|1\\.17\\.3|1\\.19\\.0|26\\.2\\.5|28\\.1' "test/scrypath/phase99_contract_test.exs"`
- `mix test test/scrypath/phase99_contract_test.exs`
</verify>
</tasks>

<threat_model>
- **T101-P2-001 (Hidden Tuple Divergence, High):** docs and CI silently drift after wave-1 reconciliation.
  **Mitigation:** semantic tuple extraction and set equality assertions in phase99 contract tests.
  **Detection:** `mix test test/scrypath/phase99_contract_test.exs`.
- **T101-P2-002 (Floor Claim Without Evidence, High):** floor `~> 1.17` remains policy text without CI tuple proof.
  **Mitigation:** explicit floor/head tuple coverage assertions (`1.17.3`, `1.19.0`) in TEST-01 block.
  **Detection:** `rg 'elixir: "~> 1\\.17"|1\\.17\\.3|1\\.19\\.0' "test/scrypath/phase99_contract_test.exs"`.
- **T101-P2-003 (Brittle Drift Guards, Medium):** prose snapshots create flaky churn.
  **Mitigation:** enforce helper-driven semantic checks and ban snapshot helpers in this seam.
  **Detection:** `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/phase99_contract_test.exs"`.
</threat_model>

<verification>
- `mix test test/scrypath/phase99_contract_test.exs`
- `rg "TRUTH-03 compatibility truth parity|TEST-01 CI-version parity anchors" "test/scrypath/phase99_contract_test.exs"`
- `rg "extract_support_ci_tuples|extract_workflow_ci_tuples|normalize_tuple_set" "test/scrypath/phase99_contract_test.exs"`
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/phase99_contract_test.exs"`
</verification>

<success_criteria>
- `phase99_contract_test.exs` compares support-guide and workflow tuples semantically with deterministic expected-vs-actual diagnostics.
- TRUTH-03 parity is enforced via tuple equality and route-only owner-boundary assertions.
- TEST-01 parity is enforced for floor/head CI evidence tokens tied to `mix.exs` floor policy.
- All new assertions run green via the existing phase99 trust seam.
</success_criteria>

<output>
- Updated `test/scrypath/phase99_contract_test.exs`
</output>
