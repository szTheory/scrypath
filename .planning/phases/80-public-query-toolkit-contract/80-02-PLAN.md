---
phase: 80
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/scrypath.ex
  - test/scrypath/search_test.exs
  - test/scrypath/meilisearch/query_test.exs
autonomous: true
requirements:
  - QTK-04
must_haves:
  truths:
    - "Toolkit output feeds the existing `Scrypath.search/3` path cleanly instead of introducing a second runtime."
    - "Contexts remain the application boundary; the toolkit prepares arguments but does not move orchestration out of app-owned search functions."
    - "The public contract stays aligned with existing runtime and adapter grammar all the way through Meilisearch payload generation."
  artifacts:
    - path: "lib/scrypath.ex"
      provides: "Root-level discoverability language that places `Scrypath.QueryParams` at the request edge and `Scrypath.search/3` as the canonical runtime"
    - path: "test/scrypath/search_test.exs"
      provides: "Delegation parity tests from toolkit output into the common search path"
    - path: "test/scrypath/meilisearch/query_test.exs"
      provides: "Adapter-shape proof that toolkit-produced args still generate the existing Meilisearch payload grammar"
  key_links:
    - from: "test/scrypath/search_test.exs"
      to: "lib/scrypath/query_params.ex"
      via: "cast output converted into `Scrypath.search/3` inputs"
      pattern: "QueryParams|to_search_args"
    - from: "test/scrypath/search_test.exs"
      to: "lib/scrypath/search.ex"
      via: "common search path assertions on the resulting internal `%Scrypath.Query{}`"
      pattern: "Scrypath\\.search|%Query\\{"
    - from: "test/scrypath/meilisearch/query_test.exs"
      to: "lib/scrypath/meilisearch/query.ex"
      via: "payload assertions using toolkit-produced search args"
      pattern: "facetFilters|facets|filter|sort"
---

<objective>
Prove that the new public toolkit contract is only an edge-preparation seam and that its output feeds the existing runtime and adapter pipeline unchanged.

Purpose: satisfy `QTK-04` by locking runtime delegation and public discoverability around `Scrypath.search/3` as the canonical executor.
Output: root-level runtime/toolkit boundary docs plus focused parity tests from `Scrypath.QueryParams` output through `Scrypath.search/3` and the Meilisearch payload builder.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-public-query-toolkit-contract/80-RESEARCH.md
@.planning/phases/80-public-query-toolkit-contract/80-PATTERNS.md
@.planning/phases/80-public-query-toolkit-contract/80-VALIDATION.md
@.planning/phases/80-public-query-toolkit-contract/80-01-PLAN.md
@lib/scrypath.ex
@lib/scrypath/search.ex
@lib/scrypath/options.ex
@lib/scrypath/query.ex
@test/scrypath/search_test.exs
@test/scrypath/meilisearch/query_test.exs
@guides/phoenix-contexts.md
@guides/phoenix-liveview.md
@guides/phoenix-controllers-and-json.md

<interfaces>
From `lib/scrypath.ex`:
```elixir
@spec search(module(), String.t(), keyword()) :: {:ok, term()} | {:error, term()}
def search(schema_module, text, opts \\ []) do
  Scrypath.Search.search(schema_module, text, opts)
end
```

From `lib/scrypath/search.ex`:
```elixir
query = Query.new(text, search_opts)
with {:ok, raw_result} <- backend.search(schema_module, query, config) do
  {:ok, decorate_result(schema_module, query, raw_result, config)}
end
```

From `test/scrypath/search_test.exs`:
```elixir
assert {:ok, %SearchResult{query: %Query{text: "phoenix", ...}}} =
         Scrypath.search(SearchablePost, "phoenix", ...)
```

From `test/scrypath/meilisearch/query_test.exs`:
```elixir
payload = MeilisearchQuery.to_payload(q)
assert payload["facetFilters"] == ["genre = \"Action\""]
```
</interfaces>
</context>

<source_audit>
- GOAL: Phase 80 must keep `Scrypath.search/3` canonical and avoid a second runtime; this plan covers the delegation and parity half of the phase.
- REQ: `QTK-04` is covered here by tests and boundary docs proving toolkit output passes cleanly into the current runtime and adapter grammar.
- RESEARCH: This plan follows the verified architecture: toolkit above the runtime, `Scrypath.Options.validate_search_options/2` and `Query.new/2` still inside the common path, contexts still canonical.
- CONTEXT: No `80-CONTEXT.md` exists. Prompt guardrails are applied directly: no `Scrypath.SearchModule`, no Phoenix dependency in core, no schema-generated verbs, no public `%Scrypath.Query{}` contract.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime delegation parity tests for toolkit output</name>
  <files>test/scrypath/search_test.exs, test/scrypath/meilisearch/query_test.exs</files>
  <behavior>
    - Test 1: `Scrypath.QueryParams` output can be turned into `{text, opts}` and passed into `Scrypath.search/3`, producing the same internal `%Scrypath.Query{}` shape as an equivalent direct keyword call.
    - Test 2: Toolkit-produced args still drive the same Meilisearch payload keys (`filter`, `sort`, `facets`, `facetFilters`, `page`) rather than a second vocabulary or adapter branch.
    - Test 3: The parity assertions never call a toolkit `search/…` helper because runtime execution remains on `Scrypath.search/3`.
  </behavior>
  <action>Extend `test/scrypath/search_test.exs` and `test/scrypath/meilisearch/query_test.exs` with a small, explicit parity matrix for the new toolkit output. In `search_test.exs`, construct toolkit output through the resolved `Scrypath.QueryParams` surface, convert it with the resolved data-only helper `to_search_args/1`, and feed the resulting `{text, opts}` into `Scrypath.search/3`; assert that the resulting `%Scrypath.SearchResult{query: %Scrypath.Query{...}}` matches the direct-call baseline for the same input. In `meilisearch/query_test.exs`, reuse toolkit-produced args to build the downstream `%Scrypath.Query{}` and prove the final payload still uses the existing Meilisearch key grammar. Keep these tests narrow and representative. Do not build a wrapper harness, do not add Phoenix fixtures here, and do not add a toolkit executor shortcut that bypasses `Scrypath.search/3`.</action>
  <verify>
    <automated>mix test test/scrypath/query_params_test.exs test/scrypath/search_test.exs test/scrypath/meilisearch/query_test.exs</automated>
  </verify>
  <done>The test suite now fails if toolkit output stops feeding the common runtime and adapter pipeline transparently.</done>
</task>

<task type="auto">
  <name>Task 2: Make the root boundary explicit without widening the public runtime</name>
  <files>lib/scrypath.ex</files>
  <action>Update the root `Scrypath` `@moduledoc` so the new toolkit becomes discoverable in the same place users already find `search/3`, while keeping the boundary explicit. Add one concise request-edge note that points readers to `Scrypath.QueryParams` for plain-data search-arg preparation and states that contexts still own orchestration and `Scrypath.search/3` remains canonical runtime. Keep the wording aligned with `guides/phoenix-contexts.md`, `guides/phoenix-liveview.md`, and `guides/phoenix-controllers-and-json.md`: request-edge helpers prepare data; application contexts execute search. Do not add new root functions, schema-generated verbs, or any prose that implies Phoenix is required.</action>
  <verify>
    <automated>mix test test/scrypath/query_params_test.exs test/scrypath/search_test.exs test/scrypath/meilisearch/query_test.exs</automated>
  </verify>
  <done>`Scrypath` docs point to the new toolkit while preserving `Scrypath.search/3` as the only canonical runtime entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toolkit output -> `Scrypath.search/3` | prepared plain data crosses into validated runtime execution here |
| runtime query struct -> Meilisearch payload | adapter grammar must stay the same when fed by toolkit-produced args |
| root docs -> library consumers | wording can accidentally imply a second runtime or Phoenix dependency if imprecise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-04 | T | `test/scrypath/search_test.exs` | mitigate | Add direct parity assertions from toolkit output into `%Scrypath.Query{}` so any runtime-shape drift fails in the existing common-path tests. |
| T-80-05 | T | `test/scrypath/meilisearch/query_test.exs` | mitigate | Prove toolkit-fed runtime queries still emit the defended adapter keys and `facetFilters` layout instead of a parallel payload grammar. |
| T-80-06 | S | `lib/scrypath.ex` | mitigate | Document `Scrypath.QueryParams` as request-edge preparation only and keep `Scrypath.search/3` as the sole canonical executor so consumers are not steered toward a spoofed second runtime. |
</threat_model>

<verification>
Run `mix test test/scrypath/query_params_test.exs test/scrypath/search_test.exs test/scrypath/meilisearch/query_test.exs`.
</verification>

<success_criteria>
- Toolkit output can be fed into `Scrypath.search/3` without an adapter shim or second vocabulary.
- Existing search and Meilisearch tests prove parity between toolkit-produced args and direct runtime calls.
- Root `Scrypath` docs make the boundary explicit: toolkit at the request edge, contexts and `search/3` at runtime.
- No new canonical runtime entrypoint appears.
</success_criteria>

<output>
After completion, create `.planning/phases/80-public-query-toolkit-contract/80-02-SUMMARY.md`
</output>
