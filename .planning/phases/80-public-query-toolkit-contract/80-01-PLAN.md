---
phase: 80
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/query_params.ex
  - lib/scrypath/query_params/caster.ex
  - test/scrypath/query_params_test.exs
autonomous: true
requirements:
  - QTK-01
must_haves:
  truths:
    - "A host app can turn request-shaped search input into one stable plain-data Scrypath toolkit shape without touching `%Scrypath.Query{}`."
    - "The public toolkit contract stays framework-light and usable outside Phoenix."
    - "The toolkit surface reuses the existing `filter`, `sort`, `page`, `facets`, `facet_filter`, and `per_query` vocabulary instead of inventing a second query grammar."
  artifacts:
    - path: "lib/scrypath/query_params.ex"
      provides: "Public plain-data query toolkit facade and stable typed contract"
    - path: "lib/scrypath/query_params/caster.ex"
      provides: "Internal request-shape casting seam behind the public facade"
    - path: "test/scrypath/query_params_test.exs"
      provides: "Focused contract tests for plain-data output and public-boundary behavior"
  key_links:
    - from: "lib/scrypath/query_params.ex"
      to: "lib/scrypath/query_params/caster.ex"
      via: "delegating public facade over an internal casting seam"
      pattern: "Scrypath\\.QueryParams\\.Caster"
    - from: "lib/scrypath/query_params.ex"
      to: "lib/scrypath/query.ex"
      via: "explicit public-vs-internal boundary wording"
      pattern: "%Scrypath\\.Query\\{|internal normalized query state"
    - from: "test/scrypath/query_params_test.exs"
      to: "lib/scrypath/query_params.ex"
      via: "contract assertions against the public output shape and helper names"
      pattern: "Scrypath\\.QueryParams"
---

<objective>
Freeze the Phase 80 public query-toolkit contract as plain data under the resolved `Scrypath.QueryParams` surface, without exposing `%Scrypath.Query{}` or pulling Phoenix semantics into runtime core.

Purpose: satisfy `QTK-01` by giving host apps one stable, documented request-edge contract before Phase 81 adds richer normalization and structured field errors.
Output: one public `Scrypath.QueryParams` module, one internal casting seam, and contract tests that fail if the surface drifts into structs, Phoenix coupling, or a second vocabulary.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/80-public-query-toolkit-contract/80-RESEARCH.md
@.planning/phases/80-public-query-toolkit-contract/80-PATTERNS.md
@.planning/phases/80-public-query-toolkit-contract/80-VALIDATION.md
@lib/scrypath.ex
@lib/scrypath/search.ex
@lib/scrypath/options.ex
@lib/scrypath/query.ex
@test/scrypath/search_test.exs
@test/scrypath/meilisearch/query_test.exs
@test/support/docs/phoenix_example_case.ex

<interfaces>
From `lib/scrypath/query.ex`:
```elixir
@moduledoc """
**`%Scrypath.Query{}` is internal normalized query state** ...
It is **not** a semver-stable pattern-match target for application code ...
"""
```

From `lib/scrypath/search.ex`:
```elixir
case Scrypath.Options.validate_search_options(schema_module, opts) do
  {:ok, search_opts} ->
    do_search(schema_module, text, search_opts, opts, [])
end
```

From `lib/scrypath/options.ex`:
```elixir
@spec validate_search_options(module(), keyword()) :: {:ok, keyword()} | {:error, term()}
```

From `test/support/docs/phoenix_example_case.ex`:
```elixir
q = Map.get(params, "q", "")
genres = parse_genres(params["genre"])
```
</interfaces>
</context>

<source_audit>
- GOAL: Phase 80 must carve out a small public plain-data edge contract over the existing runtime; this plan covers the contract surface itself, not Phoenix wrappers or structured edge errors.
- REQ: `QTK-01` is covered here by a stable public `Scrypath.QueryParams` shape and request-edge casting seam that never returns `%Scrypath.Query{}`.
- RESEARCH: This plan follows the verified recommendation to publish one data-first facade above `Scrypath.search/3`, reuse the current runtime key vocabulary, and keep `%Scrypath.Query{}` internal.
- CONTEXT: No `80-CONTEXT.md` exists. This plan applies the locked prompt guardrails directly: live-code reality only, no `Scrypath.SearchModule` assumptions, contexts stay canonical, Phoenix stays optional.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the public contract tests for `Scrypath.QueryParams`</name>
  <files>test/scrypath/query_params_test.exs</files>
  <behavior>
    - Test 1: A request-shaped map can produce one plain-data result with top-level `:text`, `:filter`, `:sort`, `:page`, `:facets`, `:facet_filter`, and `:per_query` keys, without returning `%Scrypath.Query{}`.
    - Test 2: The public facade accepts both string-keyed and atom-keyed envelope keys for `q` or `text` plus the existing runtime option names when values are already runtime-compatible shapes.
    - Test 3: Unrelated request fields do not widen the public contract, and the module does not expose `search/…` helpers or any second runtime entrypoint.
  </behavior>
  <action>Create `test/scrypath/query_params_test.exs` as the RED-first contract spine for the new public toolkit. Lock the public shape to plain data, not a public struct: use map/keyword assertions and explicitly refute `%Scrypath.Query{}` as a return contract. Use the resolved Phase 80 public module name `Scrypath.QueryParams`. The tests should exercise request-shaped maps with browser-friendly string keys while keeping Phase 80 scope narrow: accept the runtime grammar keys (`filter`, `sort`, `page`, `facets`, `facet_filter`, `per_query`) when their values are already compatible with the current `Scrypath.search/3` grammar, but do not require Phase 81 scalar parsing, field-scoped error entries, or Phoenix helpers here. Also assert the module exposes the resolved data-only `to_search_args/1` bridge and does not export a `search/…` function so the toolkit cannot silently become a second executor.</action>
  <verify>
    <automated>mix test test/scrypath/query_params_test.exs</automated>
  </verify>
  <done>`query_params_test.exs` fails loudly until the repo has one public plain-data toolkit surface and keeps the runtime boundary honest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the public facade and internal casting seam</name>
  <files>lib/scrypath/query_params.ex, lib/scrypath/query_params/caster.ex</files>
  <behavior>
    - Test 1: `Scrypath.QueryParams` documents `%Scrypath.Query{}` as internal and presents one stable public data contract.
    - Test 2: The facade delegates casting to an internal module and returns a plain-data map plus a helper that can turn that map into `{text, opts}` for `Scrypath.search/3`.
    - Test 3: The module remains Phoenix-agnostic and does not call `Scrypath.search/3` or `Scrypath.Search.search/3`.
  </behavior>
  <action>Implement `lib/scrypath/query_params.ex` as the public Phase 80 contract and `lib/scrypath/query_params/caster.ex` as its private normalization seam. The public surface should stay small and data-first: expose only helper(s) that build and convert one plain-data search-args shape, with explicit `@doc` language that `%Scrypath.Query{}` remains internal and that contexts still own search execution. Reuse the current runtime vocabulary exactly: `text`, `filter`, `sort`, `page`, `facets`, `facet_filter`, and `per_query`. Keep the output directly consumable by `Scrypath.search/3` via the resolved data-only helper `to_search_args/1`, which returns `{text, keyword_opts}` without performing validation or backend dispatch itself. The internal caster may accept string-keyed request maps and atom-keyed maps, but it must only map recognized top-level keys and must not introduce Phoenix dependencies, schema-generated verbs, or `String.to_atom/1` on untrusted input.</action>
  <verify>
    <automated>mix test test/scrypath/query_params_test.exs</automated>
  </verify>
  <done>The repo exposes one public `Scrypath.QueryParams` contract that is plain-data, framework-light, and clearly separated from the internal runtime query struct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted request params -> `Scrypath.QueryParams` | browser-shaped data enters the public edge contract here |
| public toolkit facade -> internal caster | only recognized keys should cross into the normalized plain-data shape |
| public toolkit docs -> library consumers | contract wording can accidentally freeze internal runtime details if imprecise |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | D | `lib/scrypath/query_params/caster.ex` | mitigate | Map only explicit known keys and forbid `String.to_atom/1` on request input so unknown fields cannot cause atom exhaustion or uncontrolled key creation. |
| T-80-02 | I | `lib/scrypath/query_params.ex` | mitigate | Document `%Scrypath.Query{}` as internal and expose only plain data, so the public toolkit does not leak runtime implementation detail as a semver contract. |
| T-80-03 | S | `lib/scrypath/query_params.ex` | mitigate | Do not export `search/…` helpers or call `Scrypath.search/3` from the toolkit module, preventing a spoofed second runtime surface. |
</threat_model>

<verification>
Run `mix test test/scrypath/query_params_test.exs`.
</verification>

<success_criteria>
- `Scrypath.QueryParams` exists as one documented public surface for plain-data search args.
- The public contract is plain data and never returns `%Scrypath.Query{}`.
- The toolkit uses the same key vocabulary as `Scrypath.search/3` and remains Phoenix-agnostic.
- No toolkit function performs search execution.
</success_criteria>

<output>
After completion, create `.planning/phases/80-public-query-toolkit-contract/80-01-SUMMARY.md`
</output>
