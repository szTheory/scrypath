---
phase: 18
plan: 07
type: execute
wave: 4
depends_on: [18-02, 18-03, 18-04, 18-05, 18-06]
files_modified:
  - docs/releasing.md
  - CHANGELOG.md
autonomous: false
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04]
tags: [docs, changelog, release-parity, closing-commit, release-please]

must_haves:
  truths:
    - "Maintainer-facing `docs/releasing.md` has a new 'Release parity gate' section explaining what workspace_clean catches (tag-vs-source drift at publish time), what release_parity catches (tarball-vs-tag drift after publish), and points at v1.2-MILESTONE-AUDIT.md as historical context (D-23)"
    - "`docs/releasing.md` ships to HexDocs via the existing `mix.exs` `extras:` list (no mix.exs change needed — the file is already listed)"
    - "`CHANGELOG.md` Unreleased section names both new Mix tasks (workspace_clean, release_parity), notes the Node 24 runtime upgrade, and carries a `### Notes` bullet pointing to `.planning/milestones/v1.2-MILESTONE-AUDIT.md` for traceability (D-24)"
    - "Phase 18 closes with a Conventional Commit of the form `feat(18): add release-parity gates + Node 20 CI cleanup` so release-please auto-cuts 0.4.0 and the Hex-vs-main divergence closes in the same cycle (D-22)"
  artifacts:
    - path: "docs/releasing.md"
      provides: "New 'Release parity gate' section (HexDocs-visible)"
      contains: "Release parity gate"
    - path: "CHANGELOG.md"
      provides: "Unreleased entry describing Phase 18 deliverables"
      contains: "verify.workspace_clean"
      contains_alt: "verify.release_parity"
  key_links:
    - from: "docs/releasing.md §Release parity gate"
      to: ".planning/milestones/v1.2-MILESTONE-AUDIT.md"
      via: "prose pointer explaining why the gates exist (D-23)"
      pattern: "v1\\.2-MILESTONE-AUDIT\\.md"
    - from: "CHANGELOG.md Unreleased Notes bullet"
      to: ".planning/milestones/v1.2-MILESTONE-AUDIT.md"
      via: "bullet explicitly linking the audit doc (D-24 traceability)"
      pattern: "v1\\.2-MILESTONE-AUDIT\\.md"
    - from: "Phase 18 closing commit"
      to: "release-please 0.4.0 cut"
      via: "Conventional-commit `feat(18):` subject triggers release-please minor-bump (D-22)"
      pattern: "feat\\(18\\): add release-parity gates"
---

<objective>
Document the Phase 18 gates in the user-facing and maintainer-facing surfaces (HexDocs via `docs/releasing.md`, changelog in `CHANGELOG.md`), then close Phase 18 with the Conventional Commit `feat(18): add release-parity gates + Node 20 CI cleanup` so release-please auto-cuts 0.4.0 and the v1.2 Hex-vs-main divergence closes in the same release cycle.

Purpose: Close INFRA-01..04 publicly. The gates are useless if adopters (and future maintainers) can't read what they do and why they exist. The closing-commit ritual is planning-level (D-22) because it triggers the release automation that re-aligns Hex with main — Phase 18's raison d'être.

Output: Modified `docs/releasing.md`, modified `CHANGELOG.md`, and a human-verified closing commit. The final task is a checkpoint:human-verify to let the maintainer review the full Phase 18 diff and execute the `feat(18):` commit themselves (the commit message shape is planning-level per D-22).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-CONTEXT.md
@.planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-PATTERNS.md
@.planning/milestones/v1.2-MILESTONE-AUDIT.md

@docs/releasing.md
@CHANGELOG.md
@mix.exs

<interfaces>
<!-- Anchor points for the two doc edits -->

### docs/releasing.md (D-23)

File already ships via `mix.exs docs.extras` (line 82 includes `"docs/releasing.md"`). No mix.exs change needed.

New section shape (prose; follow the tone of existing sections in the same file):

```markdown
## Release parity gate

Scrypath publishes through two mechanized gates that prevent the v1.2-era
divergence where a git tag and the published Hex tarball drifted.

### `mix verify.workspace_clean`

Runs before `mix hex.publish` on every publish path (`.github/workflows/ci.yml`
quality job, `.github/workflows/release-please.yml` publish-hex job, and
`.github/workflows/publish-hex.yml` manual recovery). Fails if the working
tree has any uncommitted or untracked files under the paths that ship in the
tarball (`lib/`, `test/`, `guides/`, `docs/`, `mix.exs`, `.formatter.exs`,
top-level `.md` files). Catches tag-vs-source drift at publish time.

There is deliberately no escape-hatch flag or environment variable — in a
true emergency, comment out the workflow step in a PR. The friction is
the feature.

### `mix verify.release_parity X.Y.Z`

Runs daily via `.github/workflows/verify-published-release.yml` (cron) and
compares the published Hex tarball's `lib/ + guides/ + docs/` file list against
the git tag of the same version. Catches tarball-vs-tag drift AFTER publish.

Exit codes:
- `0` — parity (no drift)
- `2` — drift detected
- `1` — runtime error (network, missing tag, etc.)

On drift during a scheduled run, a deduplicated GitHub issue is auto-filed
using `.github/ISSUE_TEMPLATE/release-parity-drift.md`.

### Historical context

These gates exist because v1.2 shipped with a tag/main divergence that went
uncaught until the next release cycle. See
[`.planning/milestones/v1.2-MILESTONE-AUDIT.md`](../.planning/milestones/v1.2-MILESTONE-AUDIT.md)
for the full incident narrative.
```

Section placement: at the end of `docs/releasing.md`, before any existing `## Troubleshooting` / `## FAQ` (if present). Insert as a top-level `##` heading to match the existing file's heading hierarchy.

### CHANGELOG.md (D-24)

File already has an `## Unreleased` section. Add/extend entries under it. Keep-a-Changelog subsections (`### Added`, `### Changed`, `### Notes`) as appropriate.

Target entries:

```markdown
## Unreleased

### Added

- `mix verify.workspace_clean` — fails if the working tree has uncommitted or untracked files under packaged paths (`lib/`, `test/`, `guides/`, `docs/`, and other `mix.exs` `package.files` entries). Integrated into all three publish paths (`ci.yml` quality job, `release-please.yml` publish-hex job, `publish-hex.yml` manual recovery).
- `mix verify.release_parity X.Y.Z` — compares the published Hex tarball's `lib/ + guides/ + docs/` file list against the git tag of the same version. Exit codes: 0 parity / 2 drift / 1 runtime error. Runs daily via `verify-published-release.yml` cron and auto-files a deduplicated GitHub issue on drift.

### Changed

- GitHub Actions CI runtime upgraded to Node 24 — `actions/checkout@v6` and `actions/cache@v5` across all jobs in `.github/workflows/ci.yml` (clears the Node 20 deprecation before the 2026-09 removal).

### Notes

- See `.planning/milestones/v1.2-MILESTONE-AUDIT.md` for the historical v1.2 tag-vs-main divergence that motivates the new release-parity gates in this phase.
```

If the existing `## Unreleased` section already has content, preserve it and add these entries in the appropriate subsection ordering (Added, Changed, Notes).

### Closing commit (D-22)

At Phase 18 close, the final commit MUST use the subject line:

    feat(18): add release-parity gates + Node 20 CI cleanup

This subject line is what release-please parses to decide the next version bump. The `feat:` prefix triggers a minor bump on pre-1.0 (per release-please-config.json); the result is `0.3.0 → 0.4.0`. The `(18)` scope ties the commit to the phase for traceability and Performance Metrics bookkeeping.

Do NOT use `chore:`, `fix:`, `docs:`, or any other prefix — those would not cut a minor version and Phase 18 would not produce a 0.4.0 tag, leaving the Hex-vs-main divergence open.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Release parity gate section to docs/releasing.md (D-23 HexDocs-visible prose)</name>
  <files>docs/releasing.md</files>
  <read_first>
    - docs/releasing.md (the file being modified — read full file to understand existing structure and find the correct insertion anchor)
    - .planning/milestones/v1.2-MILESTONE-AUDIT.md (context for the "Historical context" subsection — link it correctly)
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-CONTEXT.md D-23
    - mix.exs (line 82 — confirm `docs/releasing.md` is already in the `extras:` list so the new section ships to HexDocs without further changes)
  </read_first>
  <behavior>
    - After edit: `docs/releasing.md` contains a new `## Release parity gate` top-level section.
    - Section includes three subsections: `### mix verify.workspace_clean`, `### mix verify.release_parity X.Y.Z`, `### Historical context`.
    - The Historical context subsection contains a markdown link to `.planning/milestones/v1.2-MILESTONE-AUDIT.md` (relative path from docs/ to .planning/ works for HexDocs source links; HexDocs does not render `.planning/` paths as clickable links, which is acceptable — the file reference is the documentation value).
    - `grep -c "## Release parity gate" docs/releasing.md` returns exactly 1.
  </behavior>
  <action>
    Read the current `docs/releasing.md` to locate the correct insertion point. Add the new `## Release parity gate` section at the END of the file (or before an existing `## Troubleshooting` / `## FAQ` section if one exists). Use the Edit tool to append; if the file ends with a blank line, insert before the terminating newline.

    Insert the following exact content, separated from preceding content by a blank line:

    ```markdown

    ## Release parity gate

    Scrypath publishes through two mechanized gates that prevent the v1.2-era
    divergence where a git tag and the published Hex tarball drifted.

    ### `mix verify.workspace_clean`

    Runs before `mix hex.publish` on every publish path (`.github/workflows/ci.yml`
    quality job, `.github/workflows/release-please.yml` publish-hex job, and
    `.github/workflows/publish-hex.yml` manual recovery). Fails if the working
    tree has any uncommitted or untracked files under the paths that ship in
    the tarball (`lib/`, `test/`, `guides/`, `docs/`, `mix.exs`,
    `.formatter.exs`, top-level `.md` files). Catches tag-vs-source drift at
    publish time.

    There is deliberately no escape-hatch flag or environment variable — in a
    true emergency, comment out the workflow step in a PR. The friction is the
    feature.

    ### `mix verify.release_parity X.Y.Z`

    Runs daily via `.github/workflows/verify-published-release.yml` (cron) and
    compares the published Hex tarball's `lib/ + guides/ + docs/` file list
    against the git tag of the same version. Catches tarball-vs-tag drift
    AFTER publish.

    Exit codes:

    - `0` — parity (no drift)
    - `2` — drift detected
    - `1` — runtime error (network, missing tag, etc.)

    On drift during a scheduled run, a deduplicated GitHub issue is auto-filed
    using `.github/ISSUE_TEMPLATE/release-parity-drift.md`.

    ### Historical context

    These gates exist because v1.2 shipped with a tag/main divergence that went
    uncaught until the next release cycle. See
    `.planning/milestones/v1.2-MILESTONE-AUDIT.md` for the full incident
    narrative.
    ```

    Preserve all existing content in `docs/releasing.md`. Do NOT reorder sections. Do NOT update `mix.exs` `docs.extras` — `docs/releasing.md` is already listed.
  </action>
  <verify>
    <automated>test -f docs/releasing.md && grep -c "## Release parity gate" docs/releasing.md | tr -d '\n' | grep -qx 1 && grep -q "mix verify.workspace_clean" docs/releasing.md && grep -q "mix verify.release_parity" docs/releasing.md && grep -q "v1.2-MILESTONE-AUDIT.md" docs/releasing.md && grep -q "Exit codes" docs/releasing.md && grep -q "### Historical context" docs/releasing.md && grep -q "\"docs/releasing.md\"" mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Release parity gate" docs/releasing.md` returns exactly 1
    - Contains all three subsections: `### \`mix verify.workspace_clean\``, `### \`mix verify.release_parity X.Y.Z\``, `### Historical context`
    - Contains the string `v1.2-MILESTONE-AUDIT.md` (linked or prose reference)
    - Contains all three exit-code explanations: `0`, `2`, `1`
    - Contains reference to all three workflow files: `ci.yml`, `release-please.yml`, `publish-hex.yml`
    - Mentions `verify-published-release.yml` as the cron host
    - Mentions `.github/ISSUE_TEMPLATE/release-parity-drift.md`
    - `grep '"docs/releasing.md"' mix.exs` matches (confirms file ships to HexDocs via existing extras — no mix.exs edit needed)
    - `git diff docs/releasing.md` shows only additive changes — no reordering or modification of pre-existing sections
  </acceptance_criteria>
  <done>
    docs/releasing.md carries the new Release parity gate section with three subsections; HexDocs will pick it up via existing extras.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 18 deliverables to CHANGELOG.md Unreleased section (D-24 with v1.2 traceability bullet)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the file being modified — locate the `## Unreleased` section and any existing content under it)
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-CONTEXT.md D-24
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-PATTERNS.md CHANGELOG.md section (around lines 801-809)
  </read_first>
  <behavior>
    - After edit: `## Unreleased` section contains three subsections with the Phase 18 deliverables.
    - `### Added`: two bullets naming `mix verify.workspace_clean` and `mix verify.release_parity`.
    - `### Changed`: one bullet describing Node 24 runtime upgrade (`actions/checkout@v6`, `actions/cache@v5`).
    - `### Notes`: one bullet pointing at `.planning/milestones/v1.2-MILESTONE-AUDIT.md` for traceability.
    - Keep-a-Changelog format preserved; subsection headings exactly `### Added`, `### Changed`, `### Notes`.
  </behavior>
  <action>
    Read `CHANGELOG.md`. Locate the `## Unreleased` section. If it has subsections already, add entries to the existing `### Added`, `### Changed`, and `### Notes` subsections (creating them if absent). If `## Unreleased` is empty, replace its content with the structured block below.

    Insert/merge the following entries under `## Unreleased`:

    ```markdown
    ### Added

    - `mix verify.workspace_clean` — fails if the working tree has uncommitted or untracked files under packaged paths (`lib/`, `test/`, `guides/`, `docs/`, and other `mix.exs` `package.files` entries). Integrated into all three publish paths (`ci.yml` quality job, `release-please.yml` publish-hex job, `publish-hex.yml` manual recovery).
    - `mix verify.release_parity X.Y.Z` — compares the published Hex tarball's `lib/ + guides/ + docs/` file list against the git tag of the same version. Exit codes: `0` parity, `2` drift, `1` runtime error. Runs daily via `verify-published-release.yml` cron and auto-files a deduplicated GitHub issue on drift.

    ### Changed

    - GitHub Actions CI runtime upgraded to Node 24 — `actions/checkout@v6` and `actions/cache@v5` across all jobs in `.github/workflows/ci.yml` (clears the Node 20 deprecation ahead of the 2026-09 removal).

    ### Notes

    - See `.planning/milestones/v1.2-MILESTONE-AUDIT.md` for the historical v1.2 tag-vs-main divergence that motivated this phase's release-parity gates.
    ```

    If the existing `## Unreleased` section already has `### Added`, `### Changed`, or `### Notes` subsections, APPEND these bullets to the existing subsections rather than creating duplicates. Preserve any prior entries.

    Do NOT bump the `@version` in mix.exs — that is release-please's job when the `feat(18):` commit lands. Do NOT create a `## 0.4.0 — YYYY-MM-DD` release entry — release-please generates that entry automatically from the Unreleased content.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -q "## Unreleased" CHANGELOG.md && grep -q "mix verify.workspace_clean" CHANGELOG.md && grep -q "mix verify.release_parity" CHANGELOG.md && grep -q "actions/checkout@v6" CHANGELOG.md && grep -q "actions/cache@v5" CHANGELOG.md && grep -q "v1.2-MILESTONE-AUDIT.md" CHANGELOG.md && awk '/## Unreleased/{f=1} f && /^## [0-9]/{exit 0} f && /mix verify.workspace_clean/{w=1} f && /mix verify.release_parity/{r=1} f && /v1.2-MILESTONE-AUDIT/{n=1} END{exit !(w && r && n)}' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Unreleased" CHANGELOG.md` returns exactly 1
    - Under the `## Unreleased` section (and above any `## X.Y.Z` release heading), the following substrings appear:
      - `mix verify.workspace_clean`
      - `mix verify.release_parity`
      - `actions/checkout@v6` (Node 24 runtime bullet)
      - `actions/cache@v5`
      - `v1.2-MILESTONE-AUDIT.md` (traceability note)
    - All three subsection headings `### Added`, `### Changed`, `### Notes` present under `## Unreleased`
    - Awk verification: all three key substrings (verify.workspace_clean, verify.release_parity, v1.2-MILESTONE-AUDIT) appear BEFORE the first `## N.N.N` release heading
    - `@version "0.3.0"` in mix.exs is unchanged (release-please handles the bump)
    - No `## 0.4.0` release heading created manually in CHANGELOG.md
  </acceptance_criteria>
  <done>
    CHANGELOG.md Unreleased section names both new Mix tasks, notes the Node 24 upgrade, and carries the v1.2 traceability bullet; mix.exs @version untouched (release-please will bump).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer reviews full Phase 18 diff and creates the closing commit feat(18): add release-parity gates + Node 20 CI cleanup (D-22)</name>
  <files>git commit on current branch (no file modifications by Claude in this task)</files>
  <action>
    This is a human-verify checkpoint — Claude performs no file edits. See `<what-built>`, `<how-to-verify>`, and `<resume-signal>` below for the maintainer-driven workflow. The maintainer will run tests, review the aggregate diff, and create the closing `feat(18):` Conventional Commit per D-22.
  </action>
  <verify>
    Human verification only (see `<how-to-verify>`). No automated Claude-runnable command.
  </verify>
  <done>
    Maintainer types "approved" in chat after (a) all 5 manual verification steps pass green, (b) the `feat(18):` closing commit is pushed, (c) the PR is opened.
  </done>
  <what-built>
    Phase 18 deliverables across 7 plans:
    - Plan 01: Wave 0 test scaffolding (3 test files + mix.exs cli.preferred_envs)
    - Plan 02: `lib/mix/tasks/verify.workspace_clean.ex` (INFRA-01 task)
    - Plan 03: `lib/mix/tasks/verify.release_parity.ex` (INFRA-02 task)
    - Plan 04: ci.yml 8 pin swaps (INFRA-03)
    - Plan 05: workspace_clean wired into ci.yml, release-please.yml, publish-hex.yml (INFRA-01 cross-workflow)
    - Plan 06: release_parity + create-an-issue in verify-published-release.yml + issue template (INFRA-02 cron, INFRA-04)
    - Plan 07 Tasks 1-2: docs/releasing.md + CHANGELOG.md updated (D-23, D-24)

    After this checkpoint the maintainer creates the final `feat(18):` Conventional Commit that release-please will parse to cut 0.4.0, closing the v1.2 Hex-vs-main divergence in the same release cycle.
  </what-built>
  <how-to-verify>
    1. Run the full test suite (green):

           mix test --exclude integration

       Expected: 0 failures across all test files; `test/mix/tasks/workflow_wiring_test.exs` reports 15 tests green (2 cli.preferred_envs + 3 INFRA-01 + 1 INFRA-02 + 4 INFRA-03 + 5 INFRA-04); `test/mix/tasks/verify_workspace_clean_test.exs` + `test/mix/tasks/verify_release_parity_test.exs` all unit tests green; `:integration` tag excluded.

    2. Run the D-21 canary (integration):

           SCRYPATH_INTEGRATION=1 mix test test/mix/tasks/verify_release_parity_test.exs --only integration

       Expected: 1 test, 0 failures (live Hex 0.3.0 matches git tag scrypath-v0.3.0).

    3. Run each new Mix task against the current tree:

           mix verify.workspace_clean
           mix verify.release_parity 0.3.0

       Expected: both exit 0.

    4. Manually trigger `verify-published-release.yml` via workflow_dispatch on the maintainer's branch (before merging to main):
       - Navigate to the Actions tab on GitHub
       - Select "Verify Published Release" workflow
       - Click "Run workflow" → select current branch → Run
       - Expected: workflow completes green; no GitHub issue filed (non-schedule run per D-19)

    5. Review the aggregate Phase 18 diff:

           git diff main...HEAD --stat
           git diff main...HEAD

       Verify:
       - 8 @v4 → @v6/@v5 swaps in ci.yml
       - 3 workflow files have a `mix verify.workspace_clean` step (ci.yml, release-please.yml, publish-hex.yml)
       - `verify-published-release.yml` has a release_parity step + create-an-issue step + issues: write permission
       - `.github/ISSUE_TEMPLATE/release-parity-drift.md` is a new file
       - `lib/mix/tasks/verify.workspace_clean.ex` and `lib/mix/tasks/verify.release_parity.ex` are new files
       - 3 test files under `test/mix/tasks/` are new
       - `mix.exs` has 2 new `cli.preferred_envs` entries (no @version change)
       - `docs/releasing.md` has a new "## Release parity gate" section
       - `CHANGELOG.md` Unreleased section names both new tasks + Node 24 + v1.2 traceability bullet

    6. Create the closing commit per D-22. This is the last action of Phase 18:

           git add <all-phase-18-files>
           git commit -m "$(cat <<'EOF'
           feat(18): add release-parity gates + Node 20 CI cleanup

           - Introduce mix verify.workspace_clean (INFRA-01): fails if working tree
             has uncommitted/untracked files in packaged paths. Wired into ci.yml
             quality job, release-please.yml publish-hex job, and publish-hex.yml
             manual recovery. No escape hatch by design (D-04).
           - Introduce mix verify.release_parity X.Y.Z (INFRA-02): compares Hex
             tarball file list against git tag. Exit 0/2/1 (parity/drift/runtime).
             Daily cron via verify-published-release.yml; dedup'd GitHub issue
             auto-filed on scheduled-run drift.
           - Upgrade ci.yml to Node 24 runtime: actions/checkout@v6 and
             actions/cache@v5 (INFRA-03). Clears Node 20 deprecation ahead of
             2026-09 removal.
           - docs/releasing.md + CHANGELOG.md document the new gates with
             v1.2-MILESTONE-AUDIT.md traceability.

           Closes INFRA-01, INFRA-02, INFRA-03, INFRA-04.
           EOF
           )"

       The subject line `feat(18): add release-parity gates + Node 20 CI cleanup` is LOAD-BEARING — release-please parses it to decide the version bump. Do NOT change `feat:` to any other type; do NOT drop the `(18)` scope.

    7. Push the commit + open a PR to main. Release-please will, on merge, open a release-PR bumping `mix.exs @version 0.3.0 → 0.4.0`. Merging the release-PR triggers the canonical publish path, which now enforces all four INFRA gates. This IS the Phase 18 exit.
  </how-to-verify>
  <resume-signal>
    Type "approved" after:
    (a) all 5 manual verification steps pass green,
    (b) the `feat(18):` commit is pushed,
    (c) the PR is opened.

    If any step fails, describe the failure — the plan will be revised and re-executed. Do not proceed to `/gsd-verify-work` until this checkpoint is approved.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer keyboard → closing commit | Commit subject shape is D-22 load-bearing; typo leaks into release-please's minor-bump logic |
| CHANGELOG.md content → HexDocs / release-please template | Content ships verbatim to end users |
| docs/releasing.md → HexDocs publication | Content is HexDocs-visible; no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07-01 | R (Repudiation) | Commit subject diverges from D-22 ritual | mitigate | Task 3's `how-to-verify` quotes the exact subject line; maintainer checkpoint + D-22 verbatim spec in CONTEXT.md reduces the chance of typo. release-please will otherwise silently not cut 0.4.0 and the divergence stays open. |
| T-18-07-02 | I (Info Disclosure) | CHANGELOG leaks internal references | accept | `.planning/milestones/v1.2-MILESTONE-AUDIT.md` is an internal planning path; it ships in the repo already (not in the Hex tarball — not in `package.files`). Adopters reading the changelog who click through get "file not found" on HexDocs; acceptable because the note's value is historical traceability for maintainers, not adopter action. |
| T-18-07-03 | T (Tampering) | Manual `@version` bump in mix.exs pre-empts release-please | mitigate | Acceptance criteria explicitly check that `mix.exs @version "0.3.0"` is unchanged. release-please owns the bump; any manual bump breaks the release-PR flow. |
</threat_model>

<verification>
1. `mix test --exclude integration` green across all test files (~30s)
2. `SCRYPATH_INTEGRATION=1 mix test --only integration` green (D-21 canary)
3. `docs/releasing.md` contains the new section + links to v1.2 audit
4. `CHANGELOG.md` Unreleased section names both tasks + Node 24 + v1.2 traceability
5. `mix.exs @version` unchanged (release-please owns the bump)
6. Manual checkpoint (Task 3): maintainer confirms full diff and creates the `feat(18):` commit with the D-22 subject line
7. Release-please opens a PR bumping 0.3.0 → 0.4.0 on merge to main
</verification>

<success_criteria>
- All 4 INFRA-0X requirements publicly documented (HexDocs + CHANGELOG)
- Historical traceability preserved (v1.2-MILESTONE-AUDIT.md referenced from both artifacts)
- Phase 18 closes with a Conventional Commit matching the D-22 shape
- Release-please can parse the commit and cut 0.4.0
- No manual @version bump in mix.exs (release-please authority preserved)
</success_criteria>

<output>
After completion (including maintainer checkpoint), create `.planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-07-SUMMARY.md`.
</output>
