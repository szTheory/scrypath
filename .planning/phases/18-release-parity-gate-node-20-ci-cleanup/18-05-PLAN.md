---
phase: 18
plan: 05
type: execute
wave: 3
depends_on: [18-02, 18-04]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release-please.yml
  - .github/workflows/publish-hex.yml
autonomous: true
requirements: [INFRA-01]
tags: [ci, github-actions, workspace-clean, release-parity, publish-gate]

must_haves:
  truths:
    - "Every push to main triggers `mix verify.workspace_clean` in ci.yml's `quality` job (per-push workspace gate)"
    - "Canonical publish path (`release-please.yml` `publish-hex` job) runs `mix verify.workspace_clean` immediately before the heavyweight `verify.phase11` release-contract gate"
    - "Manual recovery path (`publish-hex.yml`) runs `mix verify.workspace_clean` at the same position as the canonical path (D-17 parity)"
    - "D-14 cross-workflow symmetry holds: `mix verify.workspace_clean` appears in all three publish paths — no gap between canonical and emergency lanes"
    - "INFRA-01 describe block (3 tests) in test/mix/tasks/workflow_wiring_test.exs goes GREEN"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "per-push workspace-clean gate in quality job (D-15)"
      contains: "mix verify.workspace_clean"
    - path: ".github/workflows/release-please.yml"
      provides: "canonical publish path workspace-clean gate in publish-hex job (D-16)"
      contains: "mix verify.workspace_clean"
    - path: ".github/workflows/publish-hex.yml"
      provides: "manual recovery path workspace-clean gate (D-17)"
      contains: "mix verify.workspace_clean"
  key_links:
    - from: "ci.yml quality job"
      to: "mix verify.workspace_clean step"
      via: "YAML step insertion after 'Check formatting' and before 'Run Credo' (D-15)"
      pattern: "name: Verify workspace is clean"
    - from: "release-please.yml publish-hex job"
      to: "mix verify.workspace_clean step"
      via: "YAML step insertion after 'Install dependencies' and before 'Verify release version' (D-16)"
      pattern: "name: Verify workspace is clean"
    - from: "publish-hex.yml"
      to: "mix verify.workspace_clean step"
      via: "YAML step insertion after 'Install dependencies' and before 'Run release contract gate' (D-17)"
      pattern: "name: Verify workspace is clean"
---

<objective>
Wire `mix verify.workspace_clean` into all three publish paths per D-14 cross-workflow symmetry: `ci.yml` quality job (per-push gate), `release-please.yml` publish-hex job (canonical publish), `publish-hex.yml` (manual recovery). This is the load-bearing invariant that mechanizes v1.2's divergence-prevention across the entire release surface.

Purpose: Close INFRA-01 cross-workflow requirement. The Mix task (Plan 02) is useless without workflow wiring — this plan makes the gate mandatory on every code-shipping path. Turns the 3 INFRA-01 tests in `workflow_wiring_test.exs` green.

Output: Three YAML step insertions (one per file). No other changes; per-workflow step positions follow D-15/D-16/D-17. Per D-20, zero new workflow files are created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-CONTEXT.md
@.planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-PATTERNS.md

@.github/workflows/ci.yml
@.github/workflows/release-please.yml
@.github/workflows/publish-hex.yml
@test/mix/tasks/workflow_wiring_test.exs
@lib/mix/tasks/verify.workspace_clean.ex

<interfaces>
<!-- Step-insertion positions (verbatim target YAML from Pattern Map) -->

### ci.yml `quality` job (D-15)

Target insertion position: after `mix format --check-formatted` step, before `mix credo` step.
New step block (6-space step indentation, 8-space `run:` indentation — matches existing quality-job style):

```yaml
      - name: Verify workspace is clean
        run: mix verify.workspace_clean
```

### release-please.yml `publish-hex` job (D-16)

Target insertion position: after `Install dependencies` (mix deps.get) step, before `Verify release version` step (which greps `@version`).

### publish-hex.yml (D-17)

Target insertion position: after `Install dependencies` (mix deps.get) step, before `Run release contract gate` step.

Step name MUST be literally `Verify workspace is clean` in all three workflows (reviewer consistency).
The tests in `workflow_wiring_test.exs` grep the substring `mix verify.workspace_clean` — step-name wording is not directly asserted but should stay consistent for readability.

Do NOT add the step to `verify-published-release.yml` — that workflow is read-only post-publish monitoring; `workspace_clean` would be meaningless there. Plan 06 adds `release_parity` to that file instead.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert Verify workspace is clean step in ci.yml quality job (D-15)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (after Plan 04 pin-swaps — locate the `quality` job and the `mix format --check-formatted` line)
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-PATTERNS.md (New workspace_clean step block in the ci.yml section, around line 656)
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-CONTEXT.md D-15
  </read_first>
  <behavior>
    - After edit: `ci.yml` `quality` job contains a step with name `Verify workspace is clean` and `run: mix verify.workspace_clean`, positioned immediately after the `mix format --check-formatted` step and before `mix credo`.
    - `grep -c "mix verify.workspace_clean" .github/workflows/ci.yml` returns exactly 1.
    - Pin-swap state from Plan 04 remains intact: 4 checkout@v6 + 4 cache@v5, zero @v4 refs for checkout/cache.
  </behavior>
  <action>
    In `.github/workflows/ci.yml`, locate the `quality` job's step sequence. Use the Edit tool with an anchor containing BOTH the format-check step and the Credo step for uniqueness.

    Anchor to replace (old_string — adapt literal whitespace to file's actual content after reading):

        - name: Check formatting
            run: mix format --check-formatted

          - name: Run Credo
            run: mix credo

    Replace with (new_string — inserts the new step preserving a blank line separator between each step):

        - name: Check formatting
            run: mix format --check-formatted

          - name: Verify workspace is clean
            run: mix verify.workspace_clean

          - name: Run Credo
            run: mix credo

    Note: the pattern map's representation uses extra leading spaces for presentation; trust the actual file content when constructing the old_string. If the existing `quality` job step for Credo has no explicit `name:` key (e.g., it's just `run: mix credo` with a different surrounding structure), use a different uniqueness anchor — the core requirement is "after format-check, before credo" in the `quality` job.

    Do NOT change any action pin, do NOT touch the `test` matrix, `phase5-verification`, or `phase13-verification` jobs. Do NOT add the step to any job other than `quality`.
  </action>
  <verify>
    <automated>grep -c "mix verify.workspace_clean" .github/workflows/ci.yml | tr -d '\n' | grep -qx 1 && grep -c "actions/checkout@v6" .github/workflows/ci.yml | tr -d '\n' | grep -qx 4 && grep -c "actions/cache@v5" .github/workflows/ci.yml | tr -d '\n' | grep -qx 4 && awk '/mix format --check-formatted/{f=NR} /mix verify.workspace_clean/{w=NR} /mix credo/{c=NR} END{exit !(f && w && c && f < w && w < c)}' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mix verify.workspace_clean" .github/workflows/ci.yml` returns exactly 1
    - The `mix verify.workspace_clean` line number is AFTER the `mix format --check-formatted` line number (awk-verifiable)
    - The `mix verify.workspace_clean` line number is BEFORE the `mix credo` line number
    - Pin-swap invariants from Plan 04 preserved: 4 checkout@v6, 4 cache@v5, zero @v4 refs for checkout/cache
    - `git diff .github/workflows/ci.yml` shows only an additive step block (the new `Verify workspace is clean` step) — no modifications to pre-existing step contents
  </acceptance_criteria>
  <done>
    ci.yml quality job gates on `mix verify.workspace_clean` between format-check and credo steps; pin state from Plan 04 preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insert Verify workspace is clean step in release-please.yml publish-hex job (D-16)</name>
  <files>.github/workflows/release-please.yml</files>
  <read_first>
    - .github/workflows/release-please.yml (locate `publish-hex` job; find `Install dependencies` and `Verify release version` steps)
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-PATTERNS.md (release-please.yml target insertion block, around lines 705-716)
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-CONTEXT.md D-16
  </read_first>
  <behavior>
    - After edit: `release-please.yml` contains a step named `Verify workspace is clean` running `mix verify.workspace_clean` in the `publish-hex` job, positioned AFTER the `Install dependencies` step (`mix deps.get`) and BEFORE the existing `Verify release version` step (which greps `@version`).
    - `grep -c "mix verify.workspace_clean" .github/workflows/release-please.yml` returns exactly 1.
    - No changes to the `release-please` job (the semantic-release creation job upstream of `publish-hex`).
  </behavior>
  <action>
    In `.github/workflows/release-please.yml`, locate the `publish-hex` job. Using the Edit tool, find the uniqueness anchor that contains BOTH the `Install dependencies` / `mix deps.get` step AND the existing `Verify release version` step (the grep of `@version`).

    Anchor to replace (old_string — content follows existing 6-space step indentation in this file; adapt if file differs):

        - name: Install dependencies
            run: mix deps.get

          - name: Verify release version
            run: grep -n "@version \"${{ needs.release-please.outputs.version }}\"" mix.exs

    Replace with (new_string):

        - name: Install dependencies
            run: mix deps.get

          - name: Verify workspace is clean
            run: mix verify.workspace_clean

          - name: Verify release version
            run: grep -n "@version \"${{ needs.release-please.outputs.version }}\"" mix.exs

    Do NOT touch the `release-please` job (upstream of `publish-hex`). Do NOT add a separate checkout or setup-beam call — the existing `publish-hex` job already has them.
  </action>
  <verify>
    <automated>grep -c "mix verify.workspace_clean" .github/workflows/release-please.yml | tr -d '\n' | grep -qx 1 && awk '/mix deps.get/{d=NR} /mix verify.workspace_clean/{w=NR} /Verify release version/{v=NR; exit} END{exit !(d && w && v && d < w && w < v)}' .github/workflows/release-please.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mix verify.workspace_clean" .github/workflows/release-please.yml` returns exactly 1
    - The `mix verify.workspace_clean` line number is AFTER the `mix deps.get` line number
    - The `mix verify.workspace_clean` line number is BEFORE the `Verify release version` step line number
    - The `release-please` job (upstream) is unchanged — `git diff .github/workflows/release-please.yml` shows additions only in the `publish-hex` job section
    - No existing step contents were modified — diff shows only additive insertion
  </acceptance_criteria>
  <done>
    release-please.yml publish-hex job gates on `mix verify.workspace_clean` between `mix deps.get` and the release-version grep step; upstream `release-please` job unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Insert Verify workspace is clean step in publish-hex.yml manual-recovery workflow (D-17)</name>
  <files>.github/workflows/publish-hex.yml</files>
  <read_first>
    - .github/workflows/publish-hex.yml (locate `Install dependencies` and `Run release contract gate` steps)
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-PATTERNS.md (publish-hex.yml target insertion block, around lines 720-734)
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-CONTEXT.md D-17
  </read_first>
  <behavior>
    - After edit: `publish-hex.yml` contains a step named `Verify workspace is clean` running `mix verify.workspace_clean`, positioned AFTER `Install dependencies` (`mix deps.get`) and BEFORE the existing `Run release contract gate` step (`mix verify.phase11`). D-17 mandates position parity with the canonical path.
    - `grep -c "mix verify.workspace_clean" .github/workflows/publish-hex.yml` returns exactly 1.
  </behavior>
  <action>
    In `.github/workflows/publish-hex.yml`, using the Edit tool, find the uniqueness anchor containing BOTH the `Install dependencies` (`mix deps.get`) step AND the existing `Run release contract gate` step (`mix verify.phase11`).

    Anchor to replace (old_string — adapt exact indentation to file's actual content after reading):

        - name: Install dependencies
            run: mix deps.get

          - name: Run release contract gate
            run: mix verify.phase11

    Replace with (new_string):

        - name: Install dependencies
            run: mix deps.get

          - name: Verify workspace is clean
            run: mix verify.workspace_clean

          - name: Run release contract gate
            run: mix verify.phase11

    Do NOT add a new workflow file (D-20 prohibits). Do NOT add an escape-hatch flag or env-var (D-04 enforcement). Do NOT change the workflow's `permissions:` block or its `on:` trigger (this workflow is `workflow_dispatch` only — manual recovery).
  </action>
  <verify>
    <automated>grep -c "mix verify.workspace_clean" .github/workflows/publish-hex.yml | tr -d '\n' | grep -qx 1 && awk '/mix deps.get/{d=NR} /mix verify.workspace_clean/{w=NR} /Run release contract gate/{r=NR; exit} END{exit !(d && w && r && d < w && w < r)}' .github/workflows/publish-hex.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mix verify.workspace_clean" .github/workflows/publish-hex.yml` returns exactly 1
    - The `mix verify.workspace_clean` line is AFTER `mix deps.get` and BEFORE `Run release contract gate` (awk-verifiable)
    - No new files created in `.github/workflows/` (D-20) — `ls .github/workflows/` shows the same 4 files as before Plan 05
    - `grep -E "(--allow-dirty|SCRYPATH_WORKSPACE_CLEAN_SKIP)" .github/workflows/publish-hex.yml` returns empty (D-04 enforcement)
    - `git diff .github/workflows/publish-hex.yml` shows only the additive step block
  </acceptance_criteria>
  <done>
    publish-hex.yml manual-recovery workflow gates on `mix verify.workspace_clean` at the same position as the canonical publish path; D-14 cross-workflow symmetry complete.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Validate cross-workflow symmetry (D-14) by running INFRA-01 describe block of workflow_wiring_test</name>
  <files>test/mix/tasks/workflow_wiring_test.exs</files>
  <read_first>
    - test/mix/tasks/workflow_wiring_test.exs (confirm INFRA-01 describe block asserts presence in all three publish paths)
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-CONTEXT.md D-14
  </read_first>
  <behavior>
    - Running the test subset for INFRA-01 cross-workflow symmetry reports 3 tests, 0 failures.
    - This task is a verification checkpoint — it does NOT modify the test file. The test file was created in Plan 01 Task 4; this task just confirms Plan 05 Tasks 1/2/3 produced a state the test file accepts.
    - If ANY of the 3 tests fails, D-14 is violated and Plan 05 MUST be revisited. A failure here is a BLOCKER finding per phase_specific_constraint #5.
  </behavior>
  <action>
    Run the INFRA-01 describe subset of the workflow-wiring test:

        mix test test/mix/tasks/workflow_wiring_test.exs --only "INFRA-01 D-14: workspace_clean gate on all three publish paths"

    The expected result is 3 tests, 0 failures. If any test fails, inspect the failing file (one of `ci.yml`, `release-please.yml`, `publish-hex.yml`) and re-run the corresponding Plan 05 task.

    No file modifications in this task — it's a validation-only step. Record the passing test output in the Plan 05 SUMMARY.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/workflow_wiring_test.exs --only "INFRA-01 D-14: workspace_clean gate on all three publish paths" 2>&1 | grep -E "3 tests, 0 failures"</automated>
  </verify>
  <acceptance_criteria>
    - The `INFRA-01 D-14` describe block in `workflow_wiring_test.exs` reports exactly 3 tests, 0 failures
    - Manual verification via three greps (all return exactly 1):
      - `grep -c "mix verify.workspace_clean" .github/workflows/ci.yml` = 1
      - `grep -c "mix verify.workspace_clean" .github/workflows/publish-hex.yml` = 1
      - `grep -c "mix verify.workspace_clean" .github/workflows/release-please.yml` = 1
    - `grep -c "mix verify.workspace_clean" .github/workflows/verify-published-release.yml` returns 0 (workspace_clean is NOT wired into the post-publish monitor — Plan 06 adds `release_parity` there instead)
  </acceptance_criteria>
  <done>
    INFRA-01 cross-workflow symmetry verified automatically; D-14 invariant holds across all three publish paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → mix verify.workspace_clean | Workflow-scoped invocation; no user input beyond commit SHA that was checked out |
| ci.yml per-push trigger → quality job | Runs on every push and PR; branch protection should require green `quality` job before merge |
| publish-hex workflow → Hex API | Downstream of workspace_clean gate; this plan only wires the pre-Hex gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | E (Elevation of Privilege) | Silent removal of workspace_clean step | mitigate | `workflow_wiring_test.exs` asserts presence in all three files; removing the step breaks the suite. D-04 grep check also runs on the workflow YAML. |
| T-18-05-02 | T (Tampering) | Attacker adds `--allow-dirty` or `continue-on-error: true` to workspace_clean step | mitigate | Acceptance criteria grep for `--allow-dirty` / `SCRYPATH_WORKSPACE_CLEAN_SKIP`. Extending the grep to `continue-on-error: true` within the workspace_clean step would be a future defense-in-depth; for now the D-04 rule is documented in CONTEXT.md and grep-enforced at commit review. |
| T-18-05-03 | D (DoS) | workspace_clean fails on clean CI runner (false positive) | mitigate | The CI runner always does a fresh checkout — `git status --porcelain` on a freshly-checked-out repo yields empty output, so the gate is a no-op noise check on CI. Failures can only occur when the maintainer is testing locally against a dirty tree, where that IS the intended behavior. |
| T-18-05-04 | I (Info Disclosure) | Workflow logs surface internal file paths | accept | Paths are from package.files scope (all public). No secret-containing paths in scope. |
</threat_model>

<verification>
1. All three target workflow files contain exactly one `mix verify.workspace_clean` reference each
2. `verify-published-release.yml` still contains ZERO references to `mix verify.workspace_clean` (per-workflow scope)
3. `mix test test/mix/tasks/workflow_wiring_test.exs --only "INFRA-01"` passes 3 tests, 0 failures
4. No new files in `.github/workflows/` (D-20)
5. Pin-swap state from Plan 04 remains intact in ci.yml
6. `grep -r "allow-dirty\|WORKSPACE_CLEAN_SKIP" .github/workflows/` returns empty (D-04)
</verification>

<success_criteria>
- INFRA-01 cross-workflow wiring complete: `mix verify.workspace_clean` gates all three publish paths
- D-14 symmetry verified: canonical (`release-please.yml`), manual recovery (`publish-hex.yml`), and per-push CI (`ci.yml`) all carry the gate
- 3 INFRA-01 tests in `workflow_wiring_test.exs` green
- D-04 (no escape hatch) and D-20 (no new workflow files) constraints held
- Per-workflow step positions per D-15/D-16/D-17
</success_criteria>

<output>
After completion, create `.planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-05-SUMMARY.md`
</output>
