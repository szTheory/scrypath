---
phase: 135-shell-chrome-polish-dual-theme-s
plan: 02
type: execute
wave: 2
depends_on:
  - 135-01
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
  - scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex
  - scrypath_ops/assets/css/app.css
  - scrypath_ops/assets/css/DESIGN-TOKENS.md
  - scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs
  - scrypath_ops/test/scrypath_ops_web/shell_chrome_token_contract_test.exs
autonomous: true
requirements:
  - SHELL-DARK-01
must_haves:
  truths:
    - "Header/nav contrast passes AA in dark and system dark while light stays unchanged by default, per D-01, D-02, and D-03."
    - "The `.ops-shell` wash remains one quiet top-left radial and does not become a violet blob, per D-07."
    - "Theme toggle selected state is testable and accurate across system, light, and dark, per D-10 and D-11."
  artifacts:
    - path: "scrypath_ops/lib/scrypath_ops_web/components/layouts.ex"
      provides: "Header/nav/brand/theme-toggle shell markup"
      contains: "ops-theme-toggle"
    - path: "scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex"
      provides: "Theme preference metadata and selected button sync"
      contains: "data-theme-preference"
    - path: "scrypath_ops/assets/css/app.css"
      provides: "Header/nav/wash/theme-toggle chrome CSS"
      contains: ".ops-shell"
    - path: "scrypath_ops/test/scrypath_ops_web/shell_chrome_token_contract_test.exs"
      provides: "Static shell chrome CSS tripwires"
  key_links:
    - from: "scrypath_ops/lib/scrypath_ops_web/components/layouts.ex"
      to: "scrypath_ops/assets/css/app.css"
      via: "durable `.ops-*` classes for header/nav/brand/theme-toggle"
      pattern: "ops-theme-toggle|ops-brand-mark|ops-nav-item-active"
    - from: "scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex"
      to: "examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts"
      via: "browser-visible `data-theme*`, localStorage, and selected button state"
      pattern: "data-theme-preference|aria-pressed"
---

<objective>
Polish the shell header, nav, live brand mark, `.ops-shell` wash, and theme toggle.

Purpose: SHELL-DARK-01 needs the shared shell to read as a seated, useful operator surface in Night without broad light redesign or IA churn.
Output: Durable shell selectors, dual-dark CSS for header/nav/wash/theme-toggle, token docs, and static tripwires.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-shell-chrome-polish-dual-theme-s/135-CONTEXT.md
@.planning/phases/135-shell-chrome-polish-dual-theme-s/135-RESEARCH.md
@.planning/phases/135-shell-chrome-polish-dual-theme-s/135-PATTERNS.md
@.planning/phases/135-shell-chrome-polish-dual-theme-s/135-VALIDATION.md
@.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-02-SUMMARY.md
@.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-03-SUMMARY.md
@scrypath_ops/AGENTS.md
@scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
@scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex
@scrypath_ops/lib/scrypath_ops_web/nav.ex
@scrypath_ops/assets/css/app.css
@scrypath_ops/assets/css/DESIGN-TOKENS.md
@scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs
@scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs
</context>

## Artifacts this phase produces

- `examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts` - new Playwright proof for shell chrome, hidden palette/sheet, theme toggle transitions, flash, nav, and `.ops-shell` wash.
- `examples/scrypath_ecommerce/package.json` - new `test:e2e:admin-shell` script.
- `scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` - live inline brand mark selector, header/nav/theme-toggle classes and attributes.
- `scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex` - existing theme sync script updates selected theme button state.
- `scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` - command palette option IDs/ARIA hooks planned in Plan 03 if modal proof requires them.
- `scrypath_ops/lib/scrypath_ops_web/components/core_components.ex` - `ops-flash` classes and close-label hardening planned in Plan 03.
- `scrypath_ops/assets/js/app.js` - existing `CommandPalette` hook hardening planned in Plan 03 if `aria-modal` stays.
- `scrypath_ops/assets/css/app.css` - shell chrome selectors and dual-dark ambient-shadow-plus-border rules.
- `scrypath_ops/assets/css/DESIGN-TOKENS.md` - lockstep shell chrome token documentation.
- `scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs` - DOM marker assertions for durable shell selectors.
- `scrypath_ops/test/scrypath_ops_web/shell_chrome_token_contract_test.exs` - static shell chrome CSS tripwires.
- `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-SHELL-CHROME-REPORT.md` - final evidence report planned in Plan 04.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable header, brand, nav, and theme-toggle markers</name>
  <files>scrypath_ops/lib/scrypath_ops_web/components/layouts.ex, scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex, scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs</files>
  <read_first>
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-CONTEXT.md`
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-PATTERNS.md`
    - `scrypath_ops/AGENTS.md`
    - `scrypath_ops/lib/scrypath_ops_web/components/layouts.ex`
    - `scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex`
    - `scrypath_ops/lib/scrypath_ops_web/nav.ex`
    - `scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs`
    - `scrypath_ops/assets/css/app.css`
    - `examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts`
  </read_first>
  <behavior>
    - Test first: `ops_shell_contract_test.exs` asserts the ops shell renders `ops-brand-mark`, `ops-theme-toggle`, `ops-theme-toggle__pill`, `ops-theme-toggle__button`, `data-phx-theme="system|light|dark"`, one `aria-current="page"`, and one `#flash-group`.
    - Test first: the theme toggle selected state is browser-visible through `aria-pressed` or an equivalent accurate current-state attribute after root script sync.
  </behavior>
  <action>Update `ScrypathOpsWeb.Layouts.app/1`, private `brand_mark/1`, and `theme_toggle/1` per D-04, D-05, D-08, D-10, D-11, D-19, D-20, and D-21. Preserve the current Phoenix structure: `Layouts.app/1` owns the shell and `ScrypathOpsWeb.Nav.primary/1` owns the ordered nav; do not restructure nav IA or introduce a shell variant. Add a small stable class to the live inline SVG brand mark, such as `ops-brand-mark`, so proof targets the current v1.35 mark instead of stale `.ops-route-mark`. Add durable `ops-theme-toggle`, `ops-theme-toggle__pill`, and `ops-theme-toggle__button` classes while preserving `id="theme-toggle"`, `id="theme-toggle-pill"`, `data-phx-theme`, and existing dispatch behavior. Extend the existing root theme sync mechanism in `root.html.heex` to keep selected theme button state accurate after system/light/dark changes; this may update `aria-pressed` and a selected data attribute on `[data-phx-theme]` buttons. Do not add a new inline script block outside the established theme provider, do not add a new JS hook, and do not change route labels or nav order.</action>
  <acceptance_criteria>
    - Source assertion: `layouts.ex` contains a stable live brand mark class and no plan relies only on `.ops-route-mark`.
    - Source assertion: `layouts.ex` keeps `id="theme-toggle"`, `id="theme-toggle-pill"`, and three `data-phx-theme` buttons for `system`, `light`, and `dark`.
    - Source assertion: `root.html.heex` updates selected theme state whenever the existing `setTheme` / `syncThemeMeta` flow runs.
    - Test assertion: `ops_shell_contract_test.exs` proves the durable classes and one `aria-current="page"` marker render through the ops shell.
    - Behavior assertion: no route label, path, group, or `Nav.primary/1` ordering is changed.
  </acceptance_criteria>
  <verify>
    <automated>cd scrypath_ops &amp;&amp; mix test test/scrypath_ops_web/ops_shell_contract_test.exs</automated>
  </verify>
  <done>The header/nav/brand/theme-toggle DOM has durable proof selectors and accurate selected-state semantics without nav IA restructuring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tune header, nav, shell wash, and theme-toggle CSS in dual dark paths</name>
  <files>scrypath_ops/assets/css/app.css, scrypath_ops/assets/css/DESIGN-TOKENS.md, scrypath_ops/test/scrypath_ops_web/shell_chrome_token_contract_test.exs</files>
  <read_first>
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-CONTEXT.md`
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-RESEARCH.md`
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-PATTERNS.md`
    - `.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-02-SUMMARY.md`
    - `.planning/phases/134-under-iterated-surface-polish-dual-theme-s/134-03-SUMMARY.md`
    - `scrypath_ops/assets/css/app.css`
    - `scrypath_ops/assets/css/DESIGN-TOKENS.md`
    - `scrypath_ops/test/scrypath_ops_web/surface_depth_token_contract_test.exs`
    - `scrypath_ops/test/scrypath_ops_web/motion_contract_test.exs`
    - `scrypath_ops/test/scrypath_ops_web/shell_chrome_token_contract_test.exs` (new file target if not present)
    - `examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts`
  </read_first>
  <behavior>
    - Test first: static contract asserts `.ops-shell` keeps exactly one top-left radial wash plus the existing linear page floor, with no extra gradient layers.
    - Test first: static contract asserts header/nav custom dark styling has both explicit `[data-theme="dark"]` and system-dark `html:not([data-theme="light"])` mirrors.
    - Test first: static contract asserts active nav uses `--color-primary-strong` and composed shadow/glow rather than a raw violet fill.
  </behavior>
  <action>Update `app.css` for the header/nav/wash/theme-toggle portion of SHELL-DARK-01 per D-01 through D-08, D-10, D-17, and D-21. Keep light pixel-identical by default: prefer dark-only explicit and system-dark mirrors for seated header separation, active nav glow composition, and any `.ops-shell` Night wash tuning. Preserve `.ops-shell` as one quiet top-left radial plus the existing linear base; reduce dark/mobile strength or extent before it reads as a blob, and do not add extra gradient layers, orbs, bokeh, noise, texture, or loops. Keep `.ops-nav-item-active` text-bearing fill on `--color-primary-strong` and compose restrained dark glow on top of existing surface lift. Add CSS for `.ops-theme-toggle*` selected/pressed states using named tokens and existing `data-theme-preference` / `data-theme-effective` attributes. Update `DESIGN-TOKENS.md` in lockstep for any selector, token, or recipe change. Add `shell_chrome_token_contract_test.exs` only for durable invariants that can be expressed cheaply: one-radial wash, dual-dark mirror presence, active nav primary-strong, and required shell selector names. Avoid raw `color-mix(... base-content NN%, transparent)` `color:` declarations; if one is truly introduced, update `contrast-pairs.mjs` in the same task and explain the D-03 light exception in the summary.</action>
  <acceptance_criteria>
    - Source assertion: `.ops-shell` background contains one radial-gradient and one linear-gradient for the shell wash recipe.
    - Source assertion: custom dark rules for `.ops-header`, `.ops-nav-item-active`, and `.ops-theme-toggle*` are mirrored in explicit dark and system-dark paths when they do not ride semantic daisyUI tokens.
    - Source assertion: `.ops-nav-item-active` uses `var(--color-primary-strong)` for its text-bearing selected fill.
    - Source assertion: `DESIGN-TOKENS.md` documents any changed shell chrome selector or dark ambient-shadow-plus-border recipe.
    - Test assertion: `shell_chrome_token_contract_test.exs` fails on missing dual-dark mirrors, extra shell wash layers, or stale brand proof selectors.
    - Behavior assertion: any light CSS movement is recorded as a D-03 exception with selector, rationale, and verification path; otherwise no light exception is claimed.
  </acceptance_criteria>
  <verify>
    <automated>cd scrypath_ops &amp;&amp; mix test test/scrypath_ops_web/ops_shell_contract_test.exs test/scrypath_ops_web/shell_chrome_token_contract_test.exs</automated>
  </verify>
  <done>Header/nav/wash/theme-toggle chrome is tokenized, dual-dark mirrored, statically guarded, and documented without broad light redesign.</done>
</task>

<task type="auto">
  <name>Task 3: Prove header, nav, wash, and theme-toggle browser behavior</name>
  <files>examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts</files>
  <read_first>
    - `examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts`
    - `examples/scrypath_ecommerce/e2e/helpers/theme-grid.ts`
    - `examples/scrypath_ecommerce/playwright.config.ts`
    - `scrypath_ops/assets/css/app.css`
    - `scrypath_ops/lib/scrypath_ops_web/components/layouts.ex`
    - `scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex`
  </read_first>
  <action>Run the Wave 0 spec subset that covers `[shell-chrome] shared surfaces` and `[shell-chrome] theme toggle` after rebuilding ops assets and booting or restarting a source ecommerce Phoenix server. Use `PLAYWRIGHT_BASE_URL` explicitly if the server is not on the default configured port. Confirm D-15 explicit-light, explicit-dark, and system-dark coverage and D-17 computed-style assertions for header separation, active nav fill/glow, bounded `.ops-shell` wash, selected theme state, localStorage, `data-theme`, `data-theme-effective`, `data-theme-preference`, and pill position.</action>
  <acceptance_criteria>
    - CLI assertion: the targeted browser subset passes against the current source server, not a stale baked image.
    - Browser assertion: system-dark has no `data-theme` attribute and `data-theme-effective="dark"`.
    - Browser assertion: selected theme state, localStorage, and pill position update for system, light, and dark.
    - Browser assertion: all six surfaces render shared shell chrome at mobile and desktop.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce &amp;&amp; npm run test:e2e:admin-shell -- --grep "shared surfaces|theme toggle" --reporter=line</automated>
  </verify>
  <done>The first browser subset proves shared header/nav/wash/theme-toggle behavior across all required themes, viewports, and screens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage to root theme script | Non-sensitive theme preference crosses from client storage into DOM attributes and selected control state. |
| Phoenix HEEx shell to browser CSS | Server-rendered classes and ARIA attributes drive browser-visible state and proof selectors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-04 | Spoofing | `theme_toggle/1` and root theme script | mitigate | Keep selected state accurate for system/light/dark through `data-theme-preference`, `data-theme-effective`, localStorage, and `aria-pressed` or equivalent browser-visible state. |
| T-135-05 | Tampering | Header/nav classes and `Nav.primary/1` | mitigate | Preserve `Nav.primary/1` as the only nav source, keep `aria-current="page"`, and assert DOM markers in `ops_shell_contract_test.exs`. |
| T-135-06 | Denial of Service | `.ops-shell` wash and dark glow CSS | mitigate | Use static CSS only, no decorative loops, no new JS hook, no heavy texture/noise layers, and reduced-motion-safe existing tokens. |
| T-135-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install is planned; do not change lockfiles for this source polish. |
</threat_model>

<verification>
- `cd scrypath_ops &amp;&amp; mix test test/scrypath_ops_web/ops_shell_contract_test.exs test/scrypath_ops_web/shell_chrome_token_contract_test.exs`
- `cd examples/scrypath_ecommerce &amp;&amp; npm run test:e2e:admin-shell -- --grep "shared surfaces|theme toggle" --reporter=line`
</verification>

<success_criteria>
- Header/nav dark contrast and selected states are AA-clean and browser-proven.
- `.ops-shell` Night wash is bounded by source and browser assertions.
- Light theme remains unchanged unless a D-03 shell-only exception is explicitly documented and verified.
</success_criteria>

<output>
Create `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-02-SUMMARY.md` when done.
</output>
