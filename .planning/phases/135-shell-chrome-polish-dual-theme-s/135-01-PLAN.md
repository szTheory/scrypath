---
phase: 135-shell-chrome-polish-dual-theme-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts
  - examples/scrypath_ecommerce/package.json
autonomous: true
requirements:
  - SHELL-DARK-01
must_haves:
  truths:
    - "SHELL-DARK-01 has focused browser proof before chrome source work relies on it, per D-14, D-15, D-16, and D-17."
    - "The proof covers explicit light, explicit dark, and system dark across mobile and desktop."
    - "The proof visits Control Room, Posture, Failed Sync, Sync/Drift, Search, and Playbooks."
  artifacts:
    - path: "examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts"
      provides: "Focused shell chrome Playwright proof"
      exports: ["shell chrome tests"]
    - path: "examples/scrypath_ecommerce/package.json"
      provides: "test:e2e:admin-shell npm script"
      contains: "test:e2e:admin-shell"
  key_links:
    - from: "examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts"
      to: "examples/scrypath_ecommerce/e2e/helpers/theme-grid.ts"
      via: "imports THEME_MODES, VIEWPORT_NAMES, VIEWPORTS, assertSystemDarkInvariants, and six route helpers"
      pattern: "THEME_MODES|VIEWPORT_NAMES|gotoControlRoom|gotoPlaybooks"
    - from: "examples/scrypath_ecommerce/package.json"
      to: "examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts"
      via: "test:e2e:admin-shell script runs the focused spec"
      pattern: "admin_shell_chrome\\.spec\\.ts"
---

<objective>
Create the missing Wave 0 browser proof scaffold for SHELL-DARK-01.

Purpose: Phase 135 must not rely on screenshots or static contracts alone for hidden shell chrome. This plan creates the focused browser spec and package script required by D-14 through D-17 before source polishing starts.
Output: `admin_shell_chrome.spec.ts` and `test:e2e:admin-shell`.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/135-shell-chrome-polish-dual-theme-s/135-CONTEXT.md
@.planning/phases/135-shell-chrome-polish-dual-theme-s/135-RESEARCH.md
@.planning/phases/135-shell-chrome-polish-dual-theme-s/135-PATTERNS.md
@.planning/phases/135-shell-chrome-polish-dual-theme-s/135-VALIDATION.md
@examples/scrypath_ecommerce/e2e/helpers/theme-grid.ts
@examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts
@examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts
@examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts
@examples/scrypath_ecommerce/package.json
</context>

## Artifacts this phase produces

- `examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts` - new Playwright proof for shell chrome, hidden palette/sheet, theme toggle transitions, flash, nav, and `.ops-shell` wash.
- `examples/scrypath_ecommerce/package.json` - new `test:e2e:admin-shell` script.
- `scrypath_ops/lib/scrypath_ops_web/components/layouts.ex` - material changes planned later: live inline brand mark selector, header/nav/theme-toggle classes and attributes.
- `scrypath_ops/lib/scrypath_ops_web/components/layouts/root.html.heex` - material changes planned later: existing theme sync script updates selected theme button state.
- `scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex` - material changes planned later: command palette option IDs/ARIA hooks if modal proof requires them.
- `scrypath_ops/lib/scrypath_ops_web/components/core_components.ex` - material changes planned later: `ops-flash` classes and close-label hardening.
- `scrypath_ops/assets/js/app.js` - material changes planned later: existing `CommandPalette` hook focus/active-option hardening if `aria-modal` stays.
- `scrypath_ops/assets/css/app.css` - material changes planned later: shell chrome selectors and dual-dark ambient-shadow-plus-border rules.
- `scrypath_ops/assets/css/DESIGN-TOKENS.md` - material changes planned later: lockstep shell chrome token documentation.
- `scrypath_ops/test/scrypath_ops_web/ops_shell_contract_test.exs` - material changes planned later: DOM marker assertions for durable shell selectors.
- `scrypath_ops/test/scrypath_ops_web/shell_chrome_token_contract_test.exs` - material changes planned later if durable CSS tripwires are added.
- `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-SHELL-CHROME-REPORT.md` - final evidence report planned in Plan 04.

<tasks>

<task type="auto">
  <name>Task 1: Create focused shell chrome browser proof</name>
  <files>examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts</files>
  <read_first>
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-CONTEXT.md`
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-RESEARCH.md`
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-PATTERNS.md`
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-VALIDATION.md`
    - `examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts` (new file target)
    - `examples/scrypath_ecommerce/e2e/helpers/theme-grid.ts`
    - `examples/scrypath_ecommerce/e2e/admin_surface_depth.spec.ts`
    - `examples/scrypath_ecommerce/e2e/admin_contrast_matrix.spec.ts`
    - `examples/scrypath_ecommerce/e2e/admin_path_motion.spec.ts`
    - `scrypath_ops/lib/scrypath_ops_web/components/layouts.ex`
    - `scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex`
    - `scrypath_ops/lib/scrypath_ops_web/components/core_components.ex`
    - `scrypath_ops/assets/js/app.js`
    - `scrypath_ops/assets/css/app.css`
  </read_first>
  <action>Create `examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts` as the Wave 0 proof for SHELL-DARK-01. Reuse `THEME_MODES`, `VIEWPORT_NAMES`, `VIEWPORTS`, `themeSlug`, `assertSystemDarkInvariants`, and the six route helpers from `helpers/theme-grid.ts` per D-14 and D-15. Include stable grep-friendly test titles for `[shell-chrome] shared surfaces`, `[shell-chrome] theme toggle`, `[shell-chrome] command palette`, `[shell-chrome] shortcut sheet`, and `[shell-chrome] flash`. The spec must visit Control Room, Posture, Failed Sync, Sync/Drift, Search, and Playbooks in explicit light, explicit dark, and system dark at mobile 390 and desktop 1440. Add helper functions for `newThemedPage`, computed style reads, contrast ratio or axe `color-contrast` checks, `Meta+K`/`Control+K` palette opening, `?` shortcut-sheet opening, and a real flash trigger from an existing operator action. Encode D-12 as a browser proof branch: if `aria-modal="true"` remains on `#ops-cmdk` or `#ops-cheatsheet`, assert bounded focus behavior and close/focus outcome; if semantics are downgraded by a later plan, assert the element no longer overclaims modal behavior. Do not add dependencies, do not create a new JS hook, and do not move Phase 136's full 40-shot gallery, milestone audit, or human UAT into this spec per D-18.</action>
  <acceptance_criteria>
    - Source assertion: `examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts` imports from `./helpers/theme-grid` and references `THEME_MODES`, `VIEWPORT_NAMES`, and all six route helper functions.
    - Source assertion: the spec contains tests titled with `[shell-chrome] shared surfaces`, `[shell-chrome] theme toggle`, `[shell-chrome] command palette`, `[shell-chrome] shortcut sheet`, and `[shell-chrome] flash`.
    - Source assertion: the spec explicitly checks `#ops-cmdk`, `#ops-cheatsheet`, `#theme-toggle`, `#theme-toggle-pill`, `#flash-group`, `.ops-header`, `.ops-nav-item-active`, and `.ops-shell`.
    - Source assertion: the spec asserts `aria-modal` is either defended by focus behavior or absent when the behavior is not modal.
    - CLI assertion: Playwright can discover the new spec without syntax or import errors.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce &amp;&amp; npm run test:e2e:list -- e2e/admin_shell_chrome.spec.ts</automated>
  </verify>
  <done>The focused shell browser spec exists, lists cleanly, names all required interactive chrome states, and encodes the command-palette `aria-modal` proof required by D-12 before source changes rely on it.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the focused shell spec npm script</name>
  <files>examples/scrypath_ecommerce/package.json</files>
  <read_first>
    - `examples/scrypath_ecommerce/package.json`
    - `examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts`
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-RESEARCH.md`
    - `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-VALIDATION.md`
  </read_first>
  <action>Add the package script `test:e2e:admin-shell` with value `playwright test e2e/admin_shell_chrome.spec.ts` per D-14. Preserve the existing Playwright and axe dependency versions and do not run `npm install`, `npm update`, or any package-manager command that changes `package-lock.json`; the research package audit says no new package install is needed for SHELL-DARK-01.</action>
  <acceptance_criteria>
    - Source assertion: `examples/scrypath_ecommerce/package.json` contains `test:e2e:admin-shell`.
    - Source assertion: the new script points only at `e2e/admin_shell_chrome.spec.ts`.
    - Source assertion: `examples/scrypath_ecommerce/package-lock.json` is unchanged by this task.
    - CLI assertion: `npm run test:e2e:admin-shell -- --list` lists the new tests.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce &amp;&amp; npm run test:e2e:admin-shell -- --list</automated>
  </verify>
  <done>`npm run test:e2e:admin-shell` is available and resolves to the focused shell spec without dependency churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser test to seeded demo server | Playwright drives local admin routes and existing dev seed endpoints; no production data or secrets cross this boundary. |
| Package scripts to local developer shell | The new npm script invokes an existing locked Playwright binary and must not install or upgrade packages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-01 | Spoofing | `admin_shell_chrome.spec.ts` palette/theme assertions | mitigate | Prove that visible state and ARIA state match actual browser behavior, especially D-12 modal semantics and selected theme state. |
| T-135-02 | Tampering | `examples/scrypath_ecommerce/package.json` | mitigate | Add only `test:e2e:admin-shell`; do not modify dependencies or package lockfiles. |
| T-135-03 | Information Disclosure | Browser screenshots/reports | accept | The spec uses seeded demo operator data only and writes local Playwright artifacts outside committed source. |
| T-135-SC | Tampering | npm installs | mitigate | No install task exists; package legitimacy audit says locked Playwright/axe are already present and must not be upgraded in this phase. |
</threat_model>

<verification>
- `cd examples/scrypath_ecommerce &amp;&amp; npm run test:e2e:admin-shell -- --list`
- `rg -n "test:e2e:admin-shell|admin_shell_chrome" examples/scrypath_ecommerce/package.json examples/scrypath_ecommerce/e2e/admin_shell_chrome.spec.ts`
</verification>

<success_criteria>
- Wave 0 validation gaps from `135-VALIDATION.md` are closed by a concrete spec and script.
- The spec can be used by Plans 02 and 03 with `--grep` filters before the full shell proof is expected to pass.
- No deferred Phase 136 gallery, audit, or UAT work is moved into Phase 135.
</success_criteria>

<output>
Create `.planning/phases/135-shell-chrome-polish-dual-theme-s/135-01-SUMMARY.md` when done.
</output>
