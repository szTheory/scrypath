---
phase: 57
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/EVID-01-b1-v1.14.md
  - CONTRIBUTING.md
  - .github/pull_request_template.md
  - CODEOWNERS
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - EVID-01
---

# Plan 01 — EVID-01 ledger, triage, and B1 freeze records

<objective>
Ship **EVID-01** as **`.planning/EVID-01-b1-v1.14.md`** with **≥2** concrete **`EVID-57-*`** rows, explicit **LIB-01..LIB-03** triage (map, cut, or defer), contributor discoverability (**`CONTRIBUTING.md`**, **`.github/pull_request_template.md`**), optional path ownership (**`CODEOWNERS`** for **`lib/scrypath/`**), and normative mirrors in **`.planning/REQUIREMENTS.md`**, **`.planning/STATE.md`**, and **`.planning/ROADMAP.md`** per **`57-CONTEXT.md`**.
</objective>

<threat_model>
| ID | Threat | Severity | Control |
|----|--------|----------|---------|
| TM-57-01 | Silent retroactive edits to frozen evidence rows | Medium | Ledger header states **append-only after freeze**; all changes go through PR review |
| TM-57-02 | Core library changes merged without maintainer awareness | Low | **`CODEOWNERS`** on **`lib/scrypath/`**; PR template requests **`Evidence: EVID-57-NN`** for B1 work |
| TM-57-03 | Traceability drift between **REQUIREMENTS** and the ledger | Low | **REQUIREMENTS** row for **EVID-01** includes repo-relative path **`.planning/EVID-01-b1-v1.14.md`** |

**ASVS-style scope:** documentation integrity and review process only; no runtime attack surface added.
</threat_model>

<tasks>

<task id="57-01-01" type="execute">
  <read_first>
    - `.planning/phases/57-evidence-triage-and-b1-scope-lock/57-CONTEXT.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/research/SUMMARY.md`
    - `.planning/research/PITFALLS.md`
    - `milestones/v1.12-REQUIREMENTS.md`
  </read_first>
  <action>
    Create **new file** **`.planning/EVID-01-b1-v1.14.md`** with:
    1. Title `# EVID-01 — B1 evidence ledger (v1.14)`.
    2. Immediately below, lines **exactly**: `**Freeze date:** 2026-04-21` and `**Rule:** Append-only after freeze — errata = new rows or explicit SUPERCEDED-OF markers; do not silently edit frozen rows.`
    3. A markdown table **Evidence rows** with columns **ID | Claim | Evidence | LIB mapping** containing **at least** rows **`EVID-57-01`** and **`EVID-57-02`** where **Evidence** cells each include either a **repo path** (backticks, e.g. **`guides/common-mistakes.md`**) **or** a **https://** URL plus a **short quoted excerpt** (one line in straight quotes).
    4. A section **`## LIB-01..LIB-03 triage (B1)`** with **three** bullet lines that start **exactly** with **`- LIB-01:`**, **`- LIB-02:`**, and **`- LIB-03:`**; each line ends with either **`Maps to EVID-57-`****NN****`** (one or more IDs)** or **`Cut for v1.14 —`** *one sentence* or **`Defer to`** *milestone name* **`—`** *one sentence*.
  </action>
  <acceptance_criteria>
    - Shell: `test -f .planning/EVID-01-b1-v1.14.md` exits **0**.
    - `rg -n 'EVID-57-01' .planning/EVID-01-b1-v1.14.md` prints **≥1** line.
    - `rg -n 'EVID-57-02' .planning/EVID-01-b1-v1.14.md` prints **≥1** line.
    - `rg -n 'Freeze date:' .planning/EVID-01-b1-v1.14.md` matches line containing **`2026-04-21`**.
    - `rg -n 'Append-only after freeze' .planning/EVID-01-b1-v1.14.md` exits **0**.
    - `rg -n '^- LIB-01:' .planning/EVID-01-b1-v1.14.md` and same for **`LIB-02:`**, **`LIB-03:`** each exit **0**.
  </acceptance_criteria>
</task>

<task id="57-01-02" type="execute">
  <read_first>
    - `CONTRIBUTING.md`
    - `.planning/EVID-01-b1-v1.14.md`
  </read_first>
  <action>
    In **`CONTRIBUTING.md`**, under the section **`# Contributing`**, add a new **`## Maintainer: B1 evidence`** section (if missing) containing **one** bullet whose text includes the **verbatim** substring **`.planning/EVID-01-b1-v1.14.md`** and mentions **frozen** or **freeze**.
  </action>
  <acceptance_criteria>
    - `rg -n '## Maintainer: B1 evidence' CONTRIBUTING.md` exits **0**.
    - `rg -n '\.planning/EVID-01-b1-v1\.14\.md' CONTRIBUTING.md` exits **0**.
  </acceptance_criteria>
</task>

<task id="57-01-03" type="execute">
  <read_first>
    - `.github/ISSUE_TEMPLATE/release-parity-drift.md`
    - `.planning/phases/57-evidence-triage-and-b1-scope-lock/57-CONTEXT.md`
  </read_first>
  <action>
    Create **`.github/pull_request_template.md`** with markdown body that includes **both** verbatim substrings **`Evidence:`** and **`EVID-57`** on separate lines or same line, and explains that PRs touching **`lib/scrypath/`** or implementing **LIB-01** / **LIB-02** / **LIB-03** for **v1.14 B1** should cite a row **`EVID-57-NN`**.
  </action>
  <acceptance_criteria>
    - `test -f .github/pull_request_template.md` exits **0**.
    - `rg -n 'Evidence:' .github/pull_request_template.md` exits **0**.
    - `rg -n 'EVID-57' .github/pull_request_template.md` exits **0**.
    - `rg -n 'lib/scrypath/' .github/pull_request_template.md` exits **0**.
  </acceptance_criteria>
</task>

<task id="57-01-04" type="execute">
  <read_first>
    - `.planning/phases/57-evidence-triage-and-b1-scope-lock/57-CONTEXT.md`
  </read_first>
  <action>
    Create **new root file** **`CODEOWNERS`** with **non-empty** first line assigning **`lib/scrypath/`** to **`@szTheory`** (if that handle is wrong for this repo, substitute the **GitHub** owner or team that matches **`mix.exs`** **`:source_url`** host path **without** `@` if org style differs — **minimum**: line contains **`lib/scrypath/`** and **`@`**).
  </action>
  <acceptance_criteria>
    - `test -f CODEOWNERS` exits **0** from repo root.
    - `rg -n 'lib/scrypath/' CODEOWNERS` exits **0**.
    - `rg '@' CODEOWNERS` exits **0**.
  </acceptance_criteria>
</task>

<task id="57-01-05" type="execute">
  <read_first>
    - `.planning/REQUIREMENTS.md`
    - `.planning/EVID-01-b1-v1.14.md`
  </read_first>
  <action>
    In **`.planning/REQUIREMENTS.md`**, update the **EVID-01** requirement bullet (first item under **v1.14 Requirements**) so the paragraph contains the **verbatim** path **`.planning/EVID-01-b1-v1.14.md`** once. In the **Traceability** table, ensure the **EVID-01** row’s text or an adjacent note references the same path substring **`.planning/EVID-01-b1-v1.14.md`** (add a parenthetical in the **Requirement** column if needed).
  </action>
  <acceptance_criteria>
    - `rg -n '\.planning/EVID-01-b1-v1\.14\.md' .planning/REQUIREMENTS.md` prints **≥2** lines (bullet + table path).
  </acceptance_criteria>
</task>

<task id="57-01-06" type="execute">
  <read_first>
    - `.planning/STATE.md`
    - `.planning/EVID-01-b1-v1.14.md`
  </read_first>
  <action>
    In **`.planning/STATE.md`** under **`### Decisions`**, append one bullet containing **all three** substrings **`B1 scope frozen`**, **`EVID-01`**, and **`.planning/EVID-01-b1-v1.14.md`**.
  </action>
  <acceptance_criteria>
    - `rg -n 'B1 scope frozen' .planning/STATE.md` exits **0**.
    - `rg -n 'EVID-01' .planning/STATE.md` exits **0**.
    - `rg -n '\.planning/EVID-01-b1-v1\.14\.md' .planning/STATE.md` exits **0**.
  </acceptance_criteria>
</task>

<task id="57-01-07" type="execute">
  <read_first>
    - `.planning/ROADMAP.md`
  </read_first>
  <action>
    In **`.planning/ROADMAP.md`**, under **`### Phase 57: Evidence triage and B1 scope lock`**, add **one** sentence or parenthetical that contains **both** **`EVID-01`** and **`.planning/EVID-01-b1-v1.14.md`** without copying the full ledger table.
  </action>
  <acceptance_criteria>
    - `rg -n '### Phase 57' .planning/ROADMAP.md` exits **0**.
    - `rg -n 'EVID-01' .planning/ROADMAP.md` exits **0** (at least one mention after Phase 57 opens — typically same subsection as plan edit).
    - `rg -n '\.planning/EVID-01-b1-v1\.14\.md' .planning/ROADMAP.md` exits **0**.
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `mix format --check-formatted` from repo root (only if any **`.ex` / `.exs`** file was touched; otherwise skip and note **N/A** in SUMMARY).
2. Run all **`rg`** / **`test -f`** commands from **`<acceptance_criteria>`** blocks above; each must exit **0**.
3. Read **`.planning/EVID-01-b1-v1.14.md`** and confirm **≥2** data rows exist under the evidence table (not counting header row).
</verification>

<must_haves>
- **≥2** evidence rows with IDs **`EVID-57-01`** … in **`.planning/EVID-01-b1-v1.14.md`**
- **LIB-01**, **LIB-02**, **LIB-03** each triaged in that file
- **CONTRIBUTING** + **PR template** + **CODEOWNERS** + **REQUIREMENTS** + **STATE** + **ROADMAP** strings per acceptance criteria
</must_haves>

<success_criteria>
- Phase 57 roadmap success criteria (1)(2)(3) satisfied by committed files and grep checks
- No changes under **`lib/scrypath/`** in this phase (governance-only)
</success_criteria>

---

## PLANNING COMPLETE
