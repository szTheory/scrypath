---
phase: 105
plan: 03
type: execute
wave: 2
depends_on:
  - 105-01
files_modified:
  - examples/scrypath_ecommerce/e2e/operator.spec.ts
  - examples/scrypath_ecommerce/e2e/helpers/e2e.ts
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
  - examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs
  - scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
  - scrypath_ops/test/scrypath_ops_web/live/failed_sync_live_test.exs
autonomous: true
requirements:
  - E2E-05

must_haves:
  truths:
    - "D-12 D-13: Operator triage proof combines browser-visible failed-sync UI with stable backend truth from Scrypath operator APIs"
    - "D-15: Browser assertions avoid private queue args, raw task JSON, and fragile LiveView internals"
    - "D-16 D-17 D-18 D-19: Failure injection is deterministic, one-shot, scenario-scoped, and dev/test-only"
    - "E2E-05: Playwright proves intentional failed sync appears in scrypath_ops and can be actioned through operator UI"
  artifacts:
    - path: "examples/scrypath_ecommerce/e2e/operator.spec.ts"
      provides: "Failed-sync operator triage browser proof"
    - path: "examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex"
      provides: "One-shot failed-sync injection and operator-state probe"
    - path: "scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex"
      provides: "Stable failed-sync UI surface for browser assertions"
  key_links:
    - from: "examples/scrypath_ecommerce/e2e/operator.spec.ts"
      to: "scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex"
      via: "browser navigation to /admin/search/failed-sync"
---

<objective>
Build the operator failed-sync triage E2E proof: inject a scenario-scoped failed sync through dev/test harness behavior, verify durable failed-work state, then assert the operator can see and action it through mounted `scrypath_ops`.

Purpose: To prove operational failure visibility through the embedded admin UI without introducing public failure-injection APIs or browser-coupling to private queue payloads.
Output: A Playwright operator spec covering E2E-05 plus controller/LiveView support for stable, observable triage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/105-hermetic-e2e-pipeline/105-CONTEXT.md
@.planning/phases/105-hermetic-e2e-pipeline/105-RESEARCH.md
@.planning/phases/105-hermetic-e2e-pipeline/105-VALIDATION.md
@.planning/phases/105-hermetic-e2e-pipeline/105-UI-SPEC.md
@.planning/phases/105-hermetic-e2e-pipeline/105-PATTERNS.md
@scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
@scrypath_ops/test/scrypath_ops_web/live/failed_sync_live_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add One-Shot Failed-Sync Injection Harness</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs
  </files>
  <read_first>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs
    test/scrypath/operator/failed_work_test.exs
    lib/scrypath/operator/failed_work.ex
    lib/scrypath/oban/upsert_worker.ex
    scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
  </read_first>
  <action>Implement `POST /dev/e2e/inject-failed-sync` so it creates one deterministic failed work item for `ScrypathEcommerce.Catalog.Product` without changing Scrypath public APIs. Prefer a scenario-scoped backend-style failure that flows through existing Oban/Scrypath operator visibility; if the existing worker path cannot be made deterministic without global config mutation, create a test-only harness helper under the example app that writes a failed Oban job compatible with `Scrypath.failed_sync_work/2`. Return a stable JSON summary with `failed_work_id`, `schema`, `state`, and `reason_class`, not raw job args. Add tests proving the endpoint is one-shot per scenario key and cleans up or isolates state between calls.</action>
  <acceptance_criteria>
    - `e2e_controller_test.exs` asserts `/dev/e2e/inject-failed-sync` returns `"failed_work_id"`, `"schema"`, `"state"`, and `"reason_class"`.
    - The response `"schema"` equals `"Elixir.ScrypathEcommerce.Catalog.Product"` or `"ScrypathEcommerce.Catalog.Product"` consistently.
    - `e2e_controller_test.exs` asserts the injected failure is visible through `Scrypath.failed_sync_work/2` or the exact operator API used by `FailedSyncLive`.
    - `e2e_controller.ex` does not add any new public Scrypath API call for failure injection.
    - `cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs</automated>
  </verify>
  <done>Failed-sync injection creates operationally visible failed work through a dev/test-only harness path.</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize Failed-Sync Browser Surface</name>
  <files>
    scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
    scrypath_ops/test/scrypath_ops_web/live/failed_sync_live_test.exs
  </files>
  <read_first>
    scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
    scrypath_ops/test/scrypath_ops_web/live/failed_sync_live_test.exs
    .planning/phases/105-hermetic-e2e-pipeline/105-UI-SPEC.md
  </read_first>
  <action>Add stable `data-testid` attributes only where role/text selectors cannot identify failed-sync rows and retry controls. Preserve visible copy `Refresh failed sync jobs`, `Failed sync jobs`, `Row detail`, and `Retry job`. Suggested hooks are `data-testid="failed-sync-row"` on each row and `data-testid="failed-sync-retry"` on the retry button. Update existing LiveView tests to assert these hooks if added.</action>
  <acceptance_criteria>
    - `failed_sync_live.ex` preserves visible text `Refresh failed sync jobs`.
    - `failed_sync_live.ex` preserves visible text `Failed sync jobs`.
    - `failed_sync_live.ex` preserves visible text `Retry job`.
    - If `data-testid="failed-sync-row"` is added, `failed_sync_live_test.exs` asserts it appears.
    - `cd scrypath_ops && mix test test/scrypath_ops_web/live/failed_sync_live_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd scrypath_ops && mix test test/scrypath_ops_web/live/failed_sync_live_test.exs</automated>
  </verify>
  <done>Failed-sync operator UI has stable selectors without weakening user-visible assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Add Operator-State Probe Helper</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
    examples/scrypath_ecommerce/e2e/helpers/e2e.ts
  </files>
  <read_first>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
    examples/scrypath_ecommerce/e2e/helpers/e2e.ts
    lib/scrypath/operator/failed_sync_work_inspection.ex
    lib/scrypath/operator/failed_work.ex
  </read_first>
  <action>Ensure `GET /dev/e2e/operator-state` returns a stable summary for `ScrypathEcommerce.Catalog.Product`: failed work count, first failed work ID, reason class counts, and whether a retry action is present. Add helper `operatorState(request)` that polls this summary until expected failed work is visible. Do not include raw backend task JSON, Oban args, or private payloads in the JSON response.</action>
  <acceptance_criteria>
    - `e2e_controller.ex` response for `/dev/e2e/operator-state` contains `"failed_count"` and `"retryable"`.
    - `e2e_controller.ex` response does not contain `"args"` or `"documents"`.
    - `e2e.ts` exports `operatorState`.
    - `e2e.ts` includes a bounded wait for failed work visibility with an error message containing `operator-state`.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs</automated>
  </verify>
  <done>Operator backend truth is available to Playwright as a small stable summary.</done>
</task>

<task type="auto">
  <name>Task 4: Write Failed-Sync Operator Playwright Spec</name>
  <files>
    examples/scrypath_ecommerce/e2e/operator.spec.ts
    examples/scrypath_ecommerce/e2e/helpers/e2e.ts
  </files>
  <read_first>
    examples/scrypath_ecommerce/e2e/helpers/e2e.ts
    scrypath_ops/lib/scrypath_ops_web/live/failed_sync_live.ex
    scrypath_ops/test/scrypath_ops_web/live/failed_sync_live_test.exs
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex
  </read_first>
  <action>Create or extend `operator.spec.ts` with a test named `operator can triage intentionally failed sync work`. The test must seed the deterministic catalog if needed, call `injectFailedSync`, wait for `operatorState` to show one failed/retryable work item, navigate to `/admin/search/failed-sync`, click `Refresh failed sync jobs`, assert the failed sync table/row is visible, open `Row detail`, and if retry is exposed safely in the current auth mode click `Retry job` and assert the UI shows a retry result or still exposes a stable failure row. Keep the browser assertions to visible text, roles, and stable test IDs.</action>
  <acceptance_criteria>
    - `operator.spec.ts` contains test name `operator can triage intentionally failed sync work`.
    - `operator.spec.ts` navigates to `/admin/search/failed-sync`.
    - `operator.spec.ts` asserts visible text `Failed sync jobs`.
    - `operator.spec.ts` calls `injectFailedSync` and `operatorState`.
    - `operator.spec.ts` contains no raw assertions for Oban args such as `"documents"` or `"document_ids"`.
    - `operator.spec.ts` contains no `waitForTimeout`.
    - `cd examples/scrypath_ecommerce && npm run test:e2e -- e2e/operator.spec.ts -g "failed sync"` exits 0 when services are running.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce && npm run test:e2e -- e2e/operator.spec.ts -g "failed sync"</automated>
  </verify>
  <done>Browser E2E proves operator failed-sync triage on the mounted admin UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Failure Injection Harness | Can create failed work and must never be available in production. |
| Operator UI | Exposes operational failure state and retry actions. |
| Auth/Sensitive Action Gate | Existing `scrypath_ops` gating must remain active for retry behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-07 | Elevation of Privilege | Failure injection endpoint | mitigate | Keep endpoint under dev/test router guard; do not introduce public Scrypath failure APIs. |
| T-105-08 | Information Disclosure | Operator-state probe | mitigate | Return only stable counts, IDs, reason classes, and retryability; omit raw Oban args and backend payloads. |
| T-105-09 | Tampering | Retry action | mitigate | Preserve existing `ScrypathOps.Integrations.Sigra.Gating.gate_sensitive_action/3` behavior and test it remains active. |
</threat_model>

<verification>
cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs
cd scrypath_ops && mix test test/scrypath_ops_web/live/failed_sync_live_test.exs
cd examples/scrypath_ecommerce && npm run test:e2e -- e2e/operator.spec.ts -g "failed sync"
</verification>

<success_criteria>
- A scenario-scoped failed sync can be injected only through the dev/test harness.
- The failed work is visible through stable Scrypath operator APIs.
- Playwright proves the mounted failed-sync UI surfaces and can action the failure without relying on private internals.
</success_criteria>

<output>
Create `.planning/phases/105-hermetic-e2e-pipeline/105-03-SUMMARY.md` when done.
</output>
