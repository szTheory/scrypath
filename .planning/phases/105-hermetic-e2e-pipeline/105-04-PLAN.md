---
phase: 105
plan: 04
type: execute
wave: 3
depends_on:
  - 105-02
  - 105-03
files_modified:
  - examples/scrypath_ecommerce/e2e/operator.spec.ts
  - examples/scrypath_ecommerce/e2e/helpers/e2e.ts
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
  - examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs
  - scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
  - scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
autonomous: true
requirements:
  - E2E-02
  - E2E-06

must_haves:
  truths:
    - "D-08 D-09 D-10 D-11: Phase 105 has a separate advisory E2E CI lane with stable job name, real services, health checks, artifacts, and documented promotion criteria"
    - "D-07: Real-worker canary lane remains deferred until the deterministic E2E lane is stable"
    - "D-12 D-14 D-15: Zero-downtime swap proof combines operator UI outcome with stable backend/search outcome"
    - "E2E-02: GitHub Actions runs the E2E lane against real Postgres and Meilisearch"
    - "E2E-06: Playwright proves zero-downtime swap initiated through scrypath_ops"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Advisory phase105-e2e lane with real services and artifacts"
      contains: "phase105-e2e"
    - path: "examples/scrypath_ecommerce/e2e/operator.spec.ts"
      provides: "Zero-downtime swap browser proof"
      contains: "Swap live index"
    - path: "CONTRIBUTING.md"
      provides: "Phase 105 E2E lane usage and promotion criteria"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "examples/scrypath_ecommerce/package.json"
      via: "npm ci and npm run test:e2e"
---

<objective>
Complete the Phase 105 E2E pipeline by adding the zero-downtime swap browser proof and wiring the full suite into an advisory GitHub Actions lane with real Postgres and Meilisearch services, explicit health checks, and failure artifacts.

Purpose: To continuously validate the full browser/operator search proof while keeping the release train green and avoiding premature required-check escalation.
Output: A stable `phase105-e2e` CI job and Playwright coverage for E2E-06.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/105-hermetic-e2e-pipeline/105-CONTEXT.md
@.planning/phases/105-hermetic-e2e-pipeline/105-RESEARCH.md
@.planning/phases/105-hermetic-e2e-pipeline/105-VALIDATION.md
@.planning/phases/105-hermetic-e2e-pipeline/105-UI-SPEC.md
@.planning/phases/105-hermetic-e2e-pipeline/105-PATTERNS.md
@.github/workflows/ci.yml
@scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
@scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Swap Outcome Harness Probe</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs
    examples/scrypath_ecommerce/e2e/helpers/e2e.ts
  </files>
  <read_first>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/controllers/e2e_controller.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs
    lib/scrypath/meilisearch/client.ex
    lib/scrypath/meilisearch/tasks.ex
    scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
  </read_first>
  <action>Extend the harness with a stable endpoint or `operatorState` shape that can prove zero-downtime swap completion without exposing raw task JSON. The summary must include a boolean `swap_terminal_success` or equivalent, the active logical index/search result used as proof, and a bounded error state. Add tests proving the probe can observe a successful swap outcome using the same Scrypath/Meilisearch client seam that `PostureLive` uses.</action>
  <acceptance_criteria>
    - `e2e_controller_test.exs` asserts the swap probe response includes `"swap_terminal_success"` or `"active_index_visible"`.
    - The probe response does not include raw `"taskUid"` or full `"raw"` task payloads.
    - `e2e.ts` exposes a helper named `waitForSwapOutcome` or equivalent.
    - `cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/controllers/e2e_controller_test.exs</automated>
  </verify>
  <done>Playwright can verify swap completion through stable operational truth.</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize Posture Swap Browser Surface</name>
  <files>
    scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
    scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs
  </files>
  <read_first>
    scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
    scrypath_ops/test/scrypath_ops_web/live/posture_live_test.exs
    .planning/phases/105-hermetic-e2e-pipeline/105-UI-SPEC.md
  </read_first>
  <action>Preserve the existing posture copy and add only minimal stable hooks if required by Playwright. Keep `Refresh posture`, `data-testid="posture-row"`, and visible button text `Swap live index`. If a completion flash or row state is needed for browser assertion, render a concise visible outcome such as `Swap live index completed` after successful swap and cover it in `posture_live_test.exs`.</action>
  <acceptance_criteria>
    - `posture_live.ex` contains visible text `Refresh posture`.
    - `posture_live.ex` contains `data-testid="posture-row"`.
    - `posture_live.ex` contains visible text `Swap live index`.
    - If a completion message is added, `posture_live_test.exs` asserts `Swap live index completed`.
    - `cd scrypath_ops && mix test test/scrypath_ops_web/live/posture_live_test.exs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd scrypath_ops && mix test test/scrypath_ops_web/live/posture_live_test.exs</automated>
  </verify>
  <done>Posture UI exposes stable browser hooks for zero-downtime swap assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Write Zero-Downtime Swap Playwright Spec</name>
  <files>
    examples/scrypath_ecommerce/e2e/operator.spec.ts
    examples/scrypath_ecommerce/e2e/helpers/e2e.ts
  </files>
  <read_first>
    examples/scrypath_ecommerce/e2e/operator.spec.ts
    examples/scrypath_ecommerce/e2e/helpers/e2e.ts
    scrypath_ops/lib/scrypath_ops_web/live/posture_live.ex
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex
  </read_first>
  <action>Add a Playwright test named `operator can initiate zero-downtime swap from posture UI`. The test must seed/search-ready the deterministic catalog if needed, navigate to `/admin/search/posture`, click `Refresh posture`, locate a `posture-row`, click `Swap live index`, then wait for the swap outcome helper to report terminal success and assert the browser still shows a healthy/search-visible posture outcome. Do not assert raw task JSON or private Meilisearch payload structure.</action>
  <acceptance_criteria>
    - `operator.spec.ts` contains test name `operator can initiate zero-downtime swap from posture UI`.
    - `operator.spec.ts` navigates to `/admin/search/posture`.
    - `operator.spec.ts` uses visible text `Refresh posture` and `Swap live index`.
    - `operator.spec.ts` calls `waitForSwapOutcome` or equivalent stable helper.
    - `operator.spec.ts` contains no `waitForTimeout`.
    - `cd examples/scrypath_ecommerce && npm run test:e2e -- e2e/operator.spec.ts -g "zero-downtime swap"` exits 0 when services are running.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/scrypath_ecommerce && npm run test:e2e -- e2e/operator.spec.ts -g "zero-downtime swap"</automated>
  </verify>
  <done>Browser E2E proves zero-downtime swap through mounted scrypath_ops UI and stable backend outcome.</done>
</task>

<task type="auto">
  <name>Task 4: Add Advisory Phase 105 E2E CI Lane</name>
  <files>
    .github/workflows/ci.yml
    CONTRIBUTING.md
  </files>
  <read_first>
    .github/workflows/ci.yml
    CONTRIBUTING.md
    examples/scrypath_ecommerce/mix.exs
    examples/scrypath_ecommerce/package.json
    examples/scrypath_ecommerce/playwright.config.ts
    .planning/phases/105-hermetic-e2e-pipeline/105-CONTEXT.md
  </read_first>
  <action>Add a separate advisory CI job named exactly `phase105-e2e` to `.github/workflows/ci.yml`. It must run on `main`, scheduled monitoring if the workflow already has or can safely add a schedule, and PRs without becoming a required check. The job must start real Postgres and Meilisearch services, use explicit health checks (`pg_isready` for Postgres and `curl http://127.0.0.1:7700/health` for Meilisearch), install Elixir/OTP with `erlef/setup-beam@v1`, install Node with `actions/setup-node@v6`, run example app deps/setup, install Playwright Chromium with deps, start Phoenix with `MIX_ENV=test PHX_SERVER=true` or an explicit Mix-owned command, wait for app readiness, run `npm run test:e2e`, and upload Playwright report/traces/screenshots/videos on failure with `actions/upload-artifact@v7`. Update `CONTRIBUTING.md` with how to run the lane locally and promotion criteria: stable job name, sustained low flake rate, bounded runtime, useful artifacts/logs, owner expectations, and no skipped-workflow pending-check ambiguity.</action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains job key `phase105-e2e:`.
    - `.github/workflows/ci.yml` contains `meilisearch` service with port `7700`.
    - `.github/workflows/ci.yml` contains `postgres` service and `pg_isready`.
    - `.github/workflows/ci.yml` contains `actions/setup-node@v6`.
    - `.github/workflows/ci.yml` contains `actions/upload-artifact@v7`.
    - `.github/workflows/ci.yml` contains `npm run test:e2e`.
    - `CONTRIBUTING.md` contains `phase105-e2e` and `Promotion criteria`.
    - `mix verify --exclude integration` exits 0 from the repository root.
  </acceptance_criteria>
  <verify>
    <automated>mix verify --exclude integration</automated>
  </verify>
  <done>The full browser/live-service E2E suite runs continuously as an advisory lane with diagnosable artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Service Network | GitHub Actions services expose Postgres and Meilisearch only inside the job network. |
| Operator Swap Action | Browser initiates operational index swap through existing `scrypath_ops` sensitive-action boundary. |
| Artifact Upload | Failure traces/screenshots may include app state and must be limited to CI diagnostics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-10 | Denial of Service | CI E2E lane | mitigate | Keep lane advisory initially, bound runtime, workers=1 in CI, and document promotion criteria before required-check escalation. |
| T-105-11 | Information Disclosure | Playwright artifacts | mitigate | Upload only failure artifacts from the test app; avoid secrets in fixture data and harness responses. |
| T-105-12 | Tampering | Swap operation | mitigate | Use existing operator route and gating behavior; prove terminal outcome through stable summary rather than private payload mutation. |
</threat_model>

<verification>
cd examples/scrypath_ecommerce && npm run test:e2e -- e2e/operator.spec.ts -g "zero-downtime swap"
mix verify --exclude integration
</verification>

<success_criteria>
- Zero-downtime swap can be initiated from the mounted `scrypath_ops` posture UI and verified via stable search/backend outcome.
- The `phase105-e2e` CI job uses real Postgres and Meilisearch services with explicit health checks.
- Playwright artifacts are uploaded on CI failure.
- Promotion criteria are documented before any required-check escalation.
</success_criteria>

<output>
Create `.planning/phases/105-hermetic-e2e-pipeline/105-04-SUMMARY.md` when done.
</output>
