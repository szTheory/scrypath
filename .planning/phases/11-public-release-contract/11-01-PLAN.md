---
phase: 11-public-release-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - test/release/package_metadata_test.exs
  - lib/mix/tasks/verify.phase11.ex
autonomous: true
requirements:
  - REL-01
must_haves:
  truths:
    - "Maintainer can prove the repo's declared release version, tag ref, and package metadata all describe the same package release."
    - "Maintainer has one automated command that fails when release metadata or workflow wiring drifts."
    - "Release-facing links no longer point at moving main-branch targets for release artifacts."
  artifacts:
    - path: "mix.exs"
      provides: "Version-derived package and docs metadata for the public package contract"
    - path: "test/release/package_metadata_test.exs"
      provides: "Exact assertions for release-aware package metadata"
    - path: "lib/mix/tasks/verify.phase11.ex"
      provides: "Canonical automated Phase 11 release-contract verification entrypoint"
  key_links:
    - from: "mix.exs"
      to: "test/release/package_metadata_test.exs"
      via: "MixProject.project/0 assertions for exact package/docs link values"
      pattern: "MixProject\\.project\\("
    - from: "lib/mix/tasks/verify.phase11.ex"
      to: ".release-please-manifest.json"
      via: "manifest/version alignment shell check"
      pattern: "release-please-manifest"
    - from: "lib/mix/tasks/verify.phase11.ex"
      to: ".github/workflows/release-please.yml"
      via: "same-workflow publish contract validation"
      pattern: "release_created|tag_name|mix hex.publish --yes"
---

<objective>
Make the release contract mechanically checkable so the canonical Release Please -> tag checkout -> Hex publish flow stays aligned with Scrypath's package metadata.

Purpose: Close the highest-risk Phase 11 gap first by turning release/version drift into an automated failure instead of a maintainer memory exercise.
Output: Version-scoped package metadata assertions and a canonical `mix verify.phase11` gate for release alignment.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-public-release-contract/11-RESEARCH.md
@.planning/phases/11-public-release-contract/11-PATTERNS.md
@mix.exs
@test/release/package_metadata_test.exs
@lib/mix/tasks/verify.phase10.ex
@.github/workflows/release-please.yml
@release-please-config.json
@.release-please-manifest.json

<interfaces>
From `mix.exs`:

```elixir
@version "0.1.0"
@source_url "https://github.com/szTheory/scrypath"
@source_ref "v#{@version}"
```

```elixir
def project do
  [
    version: @version,
    docs: docs(),
    package: package(),
    homepage_url: @source_url,
    source_url: @source_url,
    source_ref: @source_ref
  ]
end
```

From `lib/mix/tasks/verify.phase10.ex`:

```elixir
def run(args) do
  Mix.Task.run("app.start")
  ensure_no_args!(args)
  run_test!(["test/release/package_metadata_test.exs", "test/scrypath/docs_contract_test.exs"], "Phase 10 release-confidence tests")
  Mix.Task.reenable("docs")
  Mix.Task.run("docs", ["--warnings-as-errors"])
  validate_release_config!()
  run_command!(["hex.build", "--unpack"], "Building and unpacking Hex package")
end
```

From `.github/workflows/release-please.yml`:

```yaml
publish-hex:
  needs: release-please
  if: ${{ needs.release-please.outputs.release_created == 'true' }}
  steps:
    - uses: actions/checkout@v4
      with:
        ref: ${{ needs.release-please.outputs.tag_name }}
    - run: mix hex.publish --yes
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock release-facing metadata to the package version</name>
  <read_first>mix.exs, test/release/package_metadata_test.exs, .planning/phases/11-public-release-contract/11-RESEARCH.md, .planning/phases/11-public-release-contract/11-PATTERNS.md</read_first>
  <files>mix.exs, test/release/package_metadata_test.exs</files>
  <behavior>
    - Test 1: `MixProject.project/0` returns release-aware package links derived from `@version` / `@source_ref`.
    - Test 2: package metadata assertions fail if `Changelog` still points at `blob/main` or if guides/docs links point at moving latest-doc targets.
  </behavior>
  <action>Update `mix.exs` so release-facing package links are derived from `@version` and `@source_ref` instead of moving branch targets, while keeping the repo homepage/source URL pinned to the canonical GitHub repo. Extend `test/release/package_metadata_test.exs` to assert the exact release-scoped link values exposed by `MixProject.project/0`, including a tag-anchored changelog URL and version-aware docs/guides URLs. Do not introduce a new metadata helper module; keep the existing `MixProject.project/0` contract pattern from Phase 10.</action>
  <acceptance_criteria>
    - `mix test test/release/package_metadata_test.exs` passes.
    - `rg -n 'blob/main/CHANGELOG.md|hexdocs.pm/scrypath/readme.html' mix.exs test/release/package_metadata_test.exs` returns no matches.
    - `rg -n '@source_ref \"v#\\{@version\\}\"|version: @version' mix.exs` returns the version-anchor definitions used by the package links.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/release/package_metadata_test.exs</automated>
  </verify>
  <done>Release package metadata is version-derived and its exact public URLs are enforced by tests rather than maintainer convention.</done>
</task>

<task type="auto">
  <name>Task 2: Add the canonical Phase 11 release-alignment gate</name>
  <read_first>lib/mix/tasks/verify.phase10.ex, mix.exs, .github/workflows/release-please.yml, release-please-config.json, .release-please-manifest.json, .planning/phases/11-public-release-contract/11-VALIDATION.md</read_first>
  <files>lib/mix/tasks/verify.phase11.ex, mix.exs</files>
  <action>Create `Mix.Tasks.Verify.Phase11` by copying the narrow orchestration shape of `verify.phase10` and extending it for REL-01: run the focused release contract tests, build docs with warnings as errors, unpack the Hex package locally, and add explicit shell checks that `mix.exs`, `.release-please-manifest.json`, `release-please-config.json`, and `.github/workflows/release-please.yml` agree on the canonical same-workflow release path. Register `verify.phase11` in `MixProject.cli/0` as a test-environment preferred task. Keep publish auth unchanged: same workflow, `GITHUB_TOKEN` for Release Please, `HEX_API_KEY` only inside the existing publish job, and no second workflow or PAT-based auth change.</action>
  <acceptance_criteria>
    - `mix verify.phase11` exits 0 on the planned repo state.
    - `rg -n '\"verify.phase11\": :test' mix.exs` returns the CLI registration.
    - `rg -n 'release_created|tag_name|mix hex.publish --yes' .github/workflows/release-please.yml` still shows the same-workflow publish contract.
    - `rg -n 'release-please-manifest|hex.build --unpack|docs --warnings-as-errors' lib/mix/tasks/verify.phase11.ex` returns the Phase 11 checks.
  </acceptance_criteria>
  <verify>
    <automated>mix verify.phase11</automated>
  </verify>
  <done>`mix verify.phase11` becomes the canonical auth-free gate for version/tag/manifest/workflow/package alignment, and it preserves the existing Release Please publish model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo metadata -> public package page | Incorrect version/link metadata becomes part of the public package contract |
| GitHub release workflow -> Hex publish | Release automation can publish the wrong ref if tag and manifest assumptions drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T | `mix.exs` package/docs metadata | mitigate | Assert exact release-scoped URLs in `test/release/package_metadata_test.exs` so moving links fail in CI. |
| T-11-02 | T | `lib/mix/tasks/verify.phase11.ex` + release workflow contract | mitigate | Check manifest, workflow, and package version agreement in one automated gate before any live publish attempt. |
| T-11-03 | R | maintainer release verification trail | accept | The phase gate is read-only and local; it reduces ambiguity but does not replace GitHub/Hex audit logs. |
</threat_model>

<verification>
Run `mix verify.phase11` after this plan completes. The command must cover release metadata tests, docs build, release-config grep validation, and local Hex package assembly without requiring live publish credentials.
</verification>

<success_criteria>
- `mix.exs` exposes release-aware public package links derived from `@version` / `@source_ref`.
- `test/release/package_metadata_test.exs` fails on moving release links and passes on the committed contract.
- `mix verify.phase11` is the single canonical auth-free release-alignment command for Phase 11.
</success_criteria>

<output>
After completion, create `.planning/phases/11-public-release-contract/11-public-release-contract-01-SUMMARY.md`
</output>
