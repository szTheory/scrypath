---
phase: 94
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/verify.phase94.ex
  - mix.exs
autonomous: true
requirements:
  - TNNT-05
must_haves:
  truths:
    - User can run `mix verify.phase94` locally without errors
    - The task executes the 5 focused test files identified in research
    - The task builds docs with warnings as errors
  artifacts:
    - path: lib/mix/tasks/verify.phase94.ex
      provides: The verification mix task runner
    - path: mix.exs
      provides: The task alias registration mapped to test environment
  key_links:
    - from: mix.exs
      to: lib/mix/tasks/verify.phase94.ex
      via: preferred_envs registration
---

<objective>
Create the hermetic `verify.phase94` mix task to guard tenant-safety surfaces and register it in the project.

Purpose: TNNT-05 requires a single regression-guarded command for all tenant-safety features.
Output: A new mix task and alias registration in `mix.exs`.
</objective>

<context>
@.planning/phases/94-verification-gate/94-CONTEXT.md
@.planning/phases/94-verification-gate/94-PATTERNS.md
@.planning/phases/94-verification-gate/94-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (94-01): Create verify.phase94 Mix Task</name>
  <files>lib/mix/tasks/verify.phase94.ex</files>
  <action>
    Create the task module `Mix.Tasks.Verify.Phase94` following the exact pattern from `94-PATTERNS.md`.
    Include the `@focused_tests` list:
    - `test/scrypath/schema_test.exs`
    - `test/scrypath/metadata_test.exs`
    - `test/scrypath/options_test.exs`
    - `test/scrypath/projection_test.exs`
    - `test/scrypath/docs_contract_test.exs`
    
    Implement the `run/1` function to execute `Mix.Task.run("app.start")`, ensure no args, run the test files, and then run docs compilation with warnings as errors.
  </action>
  <verify>
    <automated>mix help verify.phase94</automated>
  </verify>
  <done>The task file is created and the help documentation is visible.</done>
</task>

<task type="auto">
  <name>Task 2 (94-02): Register alias in mix.exs</name>
  <files>mix.exs</files>
  <action>
    Update `mix.exs` to add `"verify.phase94": :test` to the `preferred_envs:` block within the `cli` function, mapping it to the `:test` environment.
  </action>
  <verify>
    <automated>grep '"verify.phase94": :test' mix.exs</automated>
  </verify>
  <done>The task is registered correctly as an alias for the test environment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/CLI | Local user or CI runner executing test suite code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-01 | Tampering | verify.phase94.ex | accept | Internal developer tooling only; no external untrusted input processed |
</threat_model>

<verification>
Run `mix verify.phase94` locally to ensure the task executes cleanly and runs the targeted test files.
</verification>

<success_criteria>
- `mix verify.phase94` executes cleanly.
- Test suite for the tenant-safety files and the docs generation execute in a single hermetic pass.
</success_criteria>

<output>
After completion, create `.planning/phases/94-verification-gate/94-01-SUMMARY.md`
</output>
