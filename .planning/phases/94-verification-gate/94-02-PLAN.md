---
phase: 94
plan: 02
type: execute
wave: 2
depends_on:
  - 94-01
files_modified:
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
autonomous: true
requirements:
  - TNNT-05
must_haves:
  truths:
    - CI quality job runs the Phase 94 verification task
    - Contributors are instructed to run `mix verify.phase94` in the CONTRIBUTING guide
  artifacts:
    - path: .github/workflows/ci.yml
      provides: CI execution step
    - path: CONTRIBUTING.md
      provides: Contributor documentation reference
  key_links:
    - from: .github/workflows/ci.yml
      to: mix verify.phase94
      via: CI step in quality job
---

<objective>
Integrate the `verify.phase94` task into the CI pipeline and contributor documentation.

Purpose: To ensure any pull request that breaks tenant-safety contracts fails CI, and contributors are aware of the gate.
Output: Updated CI workflow (`.github/workflows/ci.yml`) and `CONTRIBUTING.md` guide.
</objective>

<context>
@.planning/phases/94-verification-gate/94-CONTEXT.md
@.planning/phases/94-verification-gate/94-PATTERNS.md
@.planning/phases/94-verification-gate/94-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (94-03): Update CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Add a new step to the `quality` job in `.github/workflows/ci.yml` that runs `mix verify.phase94` to enforce the tenant-safety + docs-contract gate. Use the pattern from `94-PATTERNS.md` (e.g., `- name: Tenant-safety + docs-contract gate... run: mix verify.phase94`).
  </action>
  <verify>
    <automated>grep -A 1 'verify.phase94' .github/workflows/ci.yml</automated>
  </verify>
  <done>CI contains the verification step in the quality job.</done>
</task>

<task type="auto">
  <name>Task 2 (94-04): Update CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <action>
    Add a paragraph to `CONTRIBUTING.md` instructing contributors to run `mix verify.phase94` when changing tenant-safety features (`tenant_field:`, `schema_capabilities/1`, `tenant_scope:`, or `multitenancy.md` anchors). Reference the analog pattern in `94-PATTERNS.md`.
  </action>
  <verify>
    <automated>grep 'verify.phase94' CONTRIBUTING.md</automated>
  </verify>
  <done>Contributor guidance explicitly references the new verification task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI System | GitHub Actions executing PR code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-02 | Tampering | ci.yml | mitigate | CI requires approvals for external forks; execution is sandboxed |
</threat_model>

<verification>
Review the diffs for `.github/workflows/ci.yml` and `CONTRIBUTING.md` to ensure correct additions.
</verification>

<success_criteria>
- CI configuration correctly specifies the `mix verify.phase94` command.
- CONTRIBUTING.md accurately reflects when and how to run the verification gate.
</success_criteria>

<output>
After completion, create `.planning/phases/94-verification-gate/94-02-SUMMARY.md`
</output>
