---
phase: 84-metadata-reflection-and-multi-search-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath.ex
  - lib/scrypath/metadata.ex
  - lib/scrypath/composition.ex
  - guides/multi-index-search.md
  - guides/faceted-search-with-phoenix-liveview.md
autonomous: true
requirements: [META-01, META-02, META-03, MSCH-01, MSCH-02]
must_haves:
  truths:
    - "The public metadata surface is frozen as declaration-backed capability metadata plus resolved call-state metadata, both plain-data and framework-agnostic."
    - "The public multi-search composition surface lowers into the existing tuple/shared-option contract instead of creating a second runtime or DSL."
    - "Root and guide docs keep host-owned concerns explicit and do not imply generated UI or tenant/authz automation."
  artifacts:
    - path: "lib/scrypath/metadata.ex"
      provides: "public capability/reflection API shape"
    - path: "lib/scrypath/composition.ex"
      provides: "public compose-many and lowering contract"
    - path: "lib/scrypath.ex"
      provides: "root entrypoint docs for metadata reflection and multi-search parity"
  key_links:
    - from: "lib/scrypath/metadata.ex"
      to: "lib/scrypath/options.ex"
      via: "capability truth derived from declarations and validators"
      pattern: "filter|sort|page|facets|facet_filter|per_query"
    - from: "lib/scrypath/composition.ex"
      to: "lib/scrypath/multi_search/entries.ex"
      via: "shared-vs-entry lowering semantics"
      pattern: "Keyword.merge|per_query"
---

<objective>
Freeze the public Phase 84 contract before implementation lands.

Purpose: Define exactly what metadata reflection and multi-search composition lowering expose so implementation and tests build against one stable public seam.
Output: Public metadata API shape, public `search_many/2` lowering API shape, and boundary-honest docs.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-CONTEXT.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-RESEARCH.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-PATTERNS.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-VALIDATION.md
@lib/scrypath.ex
@lib/scrypath/schema.ex
@lib/scrypath/options.ex
@lib/scrypath/composition.ex
@lib/scrypath/multi_search/entries.ex
@guides/multi-index-search.md
@guides/faceted-search-with-phoenix-liveview.md

<interfaces>
From `lib/scrypath.ex`:
```elixir
@spec schema_config(module()) :: map()
@spec schema_fields(module()) :: [atom()]
@spec schema_faceting(module()) :: keyword()
@spec search_many(list(), keyword()) :: {:ok, term()} | {:error, term()}
```

From `lib/scrypath/composition.ex`:
```elixir
@spec compose(fragment() | [fragment()], criteria()) :: {:ok, result()} | {:error, term()}
@spec to_search_args(result()) :: {String.t(), keyword()}
```

From `lib/scrypath/multi_search/entries.ex`:
```elixir
@spec normalize(list(), keyword()) ::
        {:ok, [{module(), String.t(), keyword(), keyword()}]} | {:error, term()}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the public metadata reflection contract</name>
  <read_first>lib/scrypath.ex, lib/scrypath/metadata.ex, lib/scrypath/schema.ex, lib/scrypath/options.ex, .planning/phases/84-metadata-reflection-and-multi-search-parity/84-CONTEXT.md, .planning/phases/84-metadata-reflection-and-multi-search-parity/84-PATTERNS.md</read_first>
  <files>lib/scrypath.ex, lib/scrypath/metadata.ex</files>
  <acceptance_criteria>
    - `rg -n "schema_capabilities|reflect_search|reflect_search_many" lib/scrypath.ex lib/scrypath/metadata.ex` finds the public Phase 84 entrypoints.
    - `rg -n "capabilities|resolved|host_owned|unsupported|defaulted|fixed" lib/scrypath/metadata.ex` finds the frozen envelope vocabulary.
    - `rg -n "%Scrypath\\.Query\\{\\}" lib/scrypath/metadata.ex` returns no matches.
  </acceptance_criteria>
  <action>Create `lib/scrypath/metadata.ex` as the public Phase 84 reflection seam and wire convenience references from `lib/scrypath.ex`. Freeze exactly three public entrypoints: `schema_capabilities/1`, `reflect_search/2`, and `reflect_search_many/2`. `schema_capabilities/1` must return declaration-backed capability data for filters, sorts, facets, paging, and stable limits without requiring caller input. `reflect_search/2` must accept either explicit plain-data single-search criteria or Phase 83 composition output and return one plain-data envelope with `capabilities`, `resolved`, and `host_owned` sections. `reflect_search_many/2` must return entry-scoped reflection rather than a merged cross-schema capability surface. Keep `unsupported` field-scoped, keep `defaulted` and `fixed` distinct, and keep host-owned concerns explicit as advisory metadata for tenant policy, authorization, and related-data behavior.</action>
  <verify>
    <automated>rg -n "schema_capabilities|reflect_search|reflect_search_many" lib/scrypath.ex lib/scrypath/metadata.ex && rg -n "capabilities|resolved|host_owned|unsupported|defaulted|fixed" lib/scrypath/metadata.ex && ! rg -n "%Scrypath\\.Query\\{\\}" lib/scrypath/metadata.ex</automated>
  </verify>
  <done>The repo exposes one explicit, bounded metadata contract that hosts can inspect without reverse-engineering validator semantics or relying on Phoenix-only helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the public multi-search composition lowering contract</name>
  <read_first>lib/scrypath/composition.ex, lib/scrypath/multi_search/entries.ex, guides/multi-index-search.md, .planning/phases/84-metadata-reflection-and-multi-search-parity/84-CONTEXT.md</read_first>
  <files>lib/scrypath/composition.ex, guides/multi-index-search.md</files>
  <acceptance_criteria>
    - `rg -n "compose_many|compose_many!|to_search_many_args" lib/scrypath/composition.ex` finds the new public lowering API.
    - `rg -n "shared .*defaults|per-entry|tuple/shared-option contract|no shared fixed" lib/scrypath/composition.ex guides/multi-index-search.md` finds the public semantics and non-goals.
    - `rg -n "DSL|runtime entrypoint|generated widget" guides/multi-index-search.md` returns no new public claims that widen scope.
  </acceptance_criteria>
  <action>Extend `lib/scrypath/composition.ex` with the Phase 84 lowering contract by exporting exactly `compose_many/2`, `compose_many!/2`, and `to_search_many_args/1`. Freeze the public semantics to match the locked context: per-entry composition is canonical; shared composition lowers `defaults` only; shared `fixed` is not supported; multi-search-only rails like `federation_weight`, `global_schemas`, `max_schemas`, and federation limits stay outside composition; and `to_search_many_args/1` emits the existing `Scrypath.search_many/2` input shape of entry tuples plus shared opts rather than a new executor contract. Document `:all` honesty and per-entry boundaries in `guides/multi-index-search.md` so callers understand that the helper lowers into existing semantics instead of flattening them away.</action>
  <verify>
    <automated>rg -n "compose_many|compose_many!|to_search_many_args" lib/scrypath/composition.ex && rg -n "shared .*defaults|per-entry|tuple/shared-option contract|no shared fixed" lib/scrypath/composition.ex guides/multi-index-search.md</automated>
  </verify>
  <done>The public seam for multi-search is frozen as a lowering helper over existing semantics rather than a new search language.</done>
</task>

<task type="auto">
  <name>Task 3: Wire root and UI-facing docs to the new bounded public story</name>
  <read_first>lib/scrypath.ex, guides/multi-index-search.md, guides/faceted-search-with-phoenix-liveview.md, .planning/phases/84-metadata-reflection-and-multi-search-parity/84-UI-SPEC.md, test/scrypath/docs_contract_test.exs</read_first>
  <files>lib/scrypath.ex, guides/multi-index-search.md, guides/faceted-search-with-phoenix-liveview.md</files>
  <acceptance_criteria>
    - `rg -n "metadata|honest controls|host-owned|tenant|authorization|related-data" lib/scrypath.ex guides/multi-index-search.md guides/faceted-search-with-phoenix-liveview.md` finds the Phase 84 boundary language.
    - `rg -n "generated widget|component macro|schema-generated runtime verbs" lib/scrypath.ex guides/multi-index-search.md guides/faceted-search-with-phoenix-liveview.md` returns no new widened-scope claims.
    - `mix docs --warnings-as-errors` exits successfully.
  </acceptance_criteria>
  <action>Update `lib/scrypath.ex` and the two relevant guides so the new metadata story is explicit: hosts may inspect declaration-backed capabilities and resolved state to render honest controls, but Scrypath does not generate those controls. Carry the Phase 84 UI-spec language into docs at a high level by making `applied`, `defaulted`, `fixed`, and `unsupported` visible in examples, while keeping tenant policy, authorization, and related-data behavior labeled `host-owned`. Do not widen into Phoenix-only runtime helpers, generated widgets, or schema-generated verbs.</action>
  <verify>
    <automated>rg -n "metadata|honest controls|host-owned|tenant|authorization|related-data" lib/scrypath.ex guides/multi-index-search.md guides/faceted-search-with-phoenix-liveview.md && mix docs --warnings-as-errors</automated>
  </verify>
  <done>The public docs teach one bounded metadata/lowering story that host apps can trust without suggesting framework magic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema declarations and validator truth → public metadata | Capability data must not drift from runtime behavior |
| host composition input → multi-search lowering | Lowering helpers must not introduce new precedence or policy semantics |
| public docs → adopter understanding | Wording must not imply widgets, authz guarantees, or a second executor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | T | `lib/scrypath/metadata.ex` | mitigate | Freeze capability keys and derive them from declaration/validator seams instead of freeform docs or hand-maintained lists. |
| T-84-02 | E | `lib/scrypath/composition.ex` | mitigate | Keep lowering bounded to existing `search_many/2` tuples/shared opts and explicitly reject shared `fixed` semantics. |
| T-84-03 | S | docs and guides | mitigate | Keep host-owned labels explicit and ban generated-widget / tenant-policy claims in public language. |
</threat_model>

<verification>
Run the task-level grep/doc checks in this plan to freeze the contract, then let plans `84-02` and `84-03` prove the behavior with focused tests and `mix verify.phase84`.
</verification>

<success_criteria>
The repo has one explicit metadata contract and one explicit multi-search lowering contract, both bounded to existing runtime semantics and honest about host-owned concerns.
</success_criteria>

<output>
After completion, create `.planning/phases/84-metadata-reflection-and-multi-search-parity/84-01-SUMMARY.md`
</output>
