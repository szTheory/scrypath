---
phase: 84-metadata-reflection-and-multi-search-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scrypath/metadata_test.exs
  - test/scrypath/composition_many_test.exs
  - test/scrypath/docs_contract_test.exs
  - lib/mix/tasks/verify.phase84.ex
  - mix.exs
autonomous: true
requirements: [META-01, META-02, META-03, MSCH-01, MSCH-02]
must_haves:
  truths:
    - "Phase 84 has executable proof that metadata reflection is derived from declaration and validator truth rather than from duplicated hand-maintained capability lists."
    - "Phase 84 has executable proof that multi-search composition lowers into the current tuple/shared-option contract without semantic widening."
    - "Phase 84 has executable proof that public docs keep host-owned boundaries explicit and avoid generated UI claims."
  artifacts:
    - path: "test/scrypath/metadata_test.exs"
      provides: "capability and resolved metadata contract coverage"
    - path: "test/scrypath/composition_many_test.exs"
      provides: "multi-search lowering and precedence coverage"
    - path: "lib/mix/tasks/verify.phase84.ex"
      provides: "focused phase verification task"
  key_links:
    - from: "test/scrypath/metadata_test.exs"
      to: "lib/scrypath/options.ex"
      via: "declaration and validator parity"
      pattern: "filterable|sortable|faceting|page|per_query"
    - from: "test/scrypath/composition_many_test.exs"
      to: "lib/scrypath/multi_search/entries.ex"
      via: "shared-vs-entry semantics and rails"
      pattern: "Keyword.merge|federation_weight|max_schemas"
---

<objective>
Create the Wave 0 verification harness for Phase 84.

Purpose: Lock metadata derivation, lowering parity, and docs honesty before the implementation fills them in.
Output: Focused unit/parity tests, docs-contract assertions, and `mix verify.phase84`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-CONTEXT.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-RESEARCH.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-PATTERNS.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-VALIDATION.md
@test/scrypath/composition_test.exs
@test/scrypath/multi_search/entries_test.exs
@test/scrypath/search_many_test.exs
@test/scrypath/search_many_integration_test.exs
@test/scrypath/docs_contract_test.exs
@lib/mix/tasks/verify.phase83.ex
@mix.exs

<interfaces>
From `test/scrypath/composition_test.exs`:
```elixir
assert composition.defaulted == %{...}
assert composition.fixed == %{...}
```

From `test/scrypath/multi_search/entries_test.exs`:
```elixir
assert Keyword.get(merged, :page) == [size: 5]
assert {:error, {:invalid_options, {:federation_key_in_entry, :federation_limit}}} = ...
```

From `lib/mix/tasks/verify.phase83.ex`:
```elixir
@focused_tests [...]
Mix.Task.run("docs", ["--warnings-as-errors"])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add metadata capability and resolved-state tests</name>
  <read_first>test/scrypath/metadata_test.exs, test/scrypath/composition_test.exs, lib/scrypath/options.ex, lib/scrypath/schema.ex, .planning/phases/84-metadata-reflection-and-multi-search-parity/84-CONTEXT.md</read_first>
  <files>test/scrypath/metadata_test.exs</files>
  <acceptance_criteria>
    - `rg -n "schema_capabilities|reflect_search|reflect_search_many|unsupported|defaulted|fixed" test/scrypath/metadata_test.exs` finds explicit contract cases.
    - `rg -n "host_owned|tenant_policy|authorization|related_data" test/scrypath/metadata_test.exs` finds boundary assertions.
    - `mix test test/scrypath/metadata_test.exs` fails before plan `84-03` implementation and passes after it.
  </acceptance_criteria>
  <action>Create `test/scrypath/metadata_test.exs` around the exact public names frozen in plan `84-01`. Add direct plain-data assertions for: declaration-backed filter/sort/facet/paging capability reflection; explicit `defaulted` vs `fixed` distinction; field-scoped `unsupported` when attempted caller criteria reference unsupported capabilities; resolved metadata derived from Phase 83 composition output; and host-owned advisory fields for tenant policy, authorization, and related-data behavior. Keep the tests hermetic and data-only; do not execute live search or depend on Phoenix helpers.</action>
  <verify>
    <automated>mix test test/scrypath/metadata_test.exs</automated>
  </verify>
  <done>The main Phase 84 metadata contract is pinned with executable expectations before implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add multi-search lowering and parity tests</name>
  <read_first>test/scrypath/composition_many_test.exs, test/scrypath/multi_search/entries_test.exs, test/scrypath/search_many_test.exs, lib/scrypath/composition.ex, .planning/phases/84-metadata-reflection-and-multi-search-parity/84-CONTEXT.md</read_first>
  <files>test/scrypath/composition_many_test.exs</files>
  <acceptance_criteria>
    - `rg -n "compose_many|to_search_many_args|shared defaults|shared fixed|:all|entry-scoped" test/scrypath/composition_many_test.exs` finds explicit parity cases.
    - `rg -n "federation_weight|max_schemas|global_schemas|per_query" test/scrypath/composition_many_test.exs` finds current rails and precedence cases.
    - `mix test test/scrypath/composition_many_test.exs` fails before plan `84-03` implementation and passes after it.
  </acceptance_criteria>
  <action>Create `test/scrypath/composition_many_test.exs` to pin the lowering contract. Cover: per-entry composition as the canonical unit; shared defaults lowering only into shared opts; explicit rejection of shared `fixed`; `to_search_many_args/1` emitting existing `{schema, text, keyword}` entry tuples plus shared opts; preserved `:per_query` shallow merge expectations; `:all` entries staying tagged and honest until existing expansion; and field-scoped unsupported/entry differences staying visible rather than merged away. Reuse the assertion style from `EntriesTest` and `SearchManyTest`, and keep the file focused on lowering and parity rather than backend execution details.</action>
  <verify>
    <automated>mix test test/scrypath/composition_many_test.exs</automated>
  </verify>
  <done>The multi-search lowering seam is executable and bounded to the current runtime contract before implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add docs contracts and focused Phase 84 verify wiring</name>
  <read_first>test/scrypath/docs_contract_test.exs, lib/mix/tasks/verify.phase83.ex, mix.exs, guides/multi-index-search.md, guides/faceted-search-with-phoenix-liveview.md, .planning/phases/84-metadata-reflection-and-multi-search-parity/84-RESEARCH.md</read_first>
  <files>test/scrypath/docs_contract_test.exs, lib/mix/tasks/verify.phase84.ex, mix.exs</files>
  <acceptance_criteria>
    - `rg -n "verify\\.phase84" lib/mix/tasks/verify.phase84.ex mix.exs` finds the new task and preferred env wiring.
    - `rg -n "schema_capabilities|reflect_search|compose_many|host-owned|generated UI" test/scrypath/docs_contract_test.exs` finds new public-story assertions.
    - `mix test test/scrypath/docs_contract_test.exs` passes once the public docs are aligned.
  </acceptance_criteria>
  <action>Extend `test/scrypath/docs_contract_test.exs` with narrow public-language assertions that the repo now exposes metadata reflection and multi-search lowering as bounded plain-data helpers, keeps `Scrypath.search/3` and `search_many/2` as the only runtime entrypoints, keeps tenant policy/authz/related-data concerns host-owned, and does not claim generated widgets or a merged global capability surface. Add `lib/mix/tasks/verify.phase84.ex` by copying the focused Phase 83 pattern: no args, run `test/scrypath/metadata_test.exs`, `test/scrypath/composition_many_test.exs`, `test/scrypath/search_many_test.exs`, and `test/scrypath/docs_contract_test.exs`, then run `mix docs --warnings-as-errors`. Wire the task into `mix.exs` under preferred CLI env settings.</action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <done>Phase 84 has a focused contributor-runnable verification lane that will fail on capability drift, lowering drift, or public-language drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation edits → focused phase tests | The gate must detect semantic widening and capability drift before execution claims are accepted |
| docs/examples → adopter expectations | Boundary wording must not imply unsupported UI or policy guarantees |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-04 | T | `test/scrypath/metadata_test.exs` | mitigate | Pin capability derivation and resolved-state semantics against exact fields and vocabulary. |
| T-84-05 | E | `test/scrypath/composition_many_test.exs` | mitigate | Pin current `search_many/2` lowering semantics and reject shared `fixed` widening with explicit tests. |
| T-84-06 | S | `test/scrypath/docs_contract_test.exs` | mitigate | Add boundary assertions for host-owned concerns and no-generated-UI claims. |
</threat_model>

<verification>
This plan’s task-level checks should fail until plan `84-03` lands the implementation. After plan `84-03`, `mix verify.phase84` is the phase gate.
</verification>

<success_criteria>
There is a narrow, contributor-runnable verification lane that proves metadata derivation, multi-search lowering parity, and public-boundary honesty without requiring live external services.
</success_criteria>

<output>
After completion, create `.planning/phases/84-metadata-reflection-and-multi-search-parity/84-02-SUMMARY.md`
</output>
