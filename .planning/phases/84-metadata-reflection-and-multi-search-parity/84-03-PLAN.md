---
phase: 84-metadata-reflection-and-multi-search-parity
plan: 03
type: execute
wave: 2
depends_on: [84-01, 84-02]
files_modified:
  - lib/scrypath.ex
  - lib/scrypath/metadata.ex
  - lib/scrypath/metadata/capabilities.ex
  - lib/scrypath/metadata/resolve.ex
  - lib/scrypath/composition.ex
  - lib/scrypath/composition/multi.ex
  - test/scrypath/metadata_test.exs
  - test/scrypath/composition_many_test.exs
  - test/scrypath/docs_contract_test.exs
  - guides/multi-index-search.md
  - guides/faceted-search-with-phoenix-liveview.md
autonomous: true
requirements: [META-01, META-02, META-03, MSCH-01, MSCH-02]
must_haves:
  truths:
    - "Declared filters, sorts, facets, and paging reflect through one public capability surface derived from the same declaration and validation seams that drive runtime behavior."
    - "Resolved metadata exposes `applied`, `defaulted`, `fixed`, and `unsupported` honestly for single-search and entry-scoped multi-search flows."
    - "Multi-search composition lowers into the existing tuple/shared-option contract while preserving current precedence, `:all` honesty, and failure boundaries."
  artifacts:
    - path: "lib/scrypath/metadata/capabilities.ex"
      provides: "declaration-backed capability derivation"
    - path: "lib/scrypath/metadata/resolve.ex"
      provides: "resolved metadata overlay for explicit or composed criteria"
    - path: "lib/scrypath/composition/multi.ex"
      provides: "multi-search composition lowering without a second runtime"
  key_links:
    - from: "lib/scrypath/metadata/capabilities.ex"
      to: "lib/scrypath/schema.ex"
      via: "declaration storage"
      pattern: "__scrypath__"
    - from: "lib/scrypath/metadata/resolve.ex"
      to: "lib/scrypath/options.ex"
      via: "validator-backed capability truth and unsupported detection"
      pattern: "validate_search_options"
    - from: "lib/scrypath/composition/multi.ex"
      to: "lib/scrypath/multi_search/entries.ex"
      via: "shared-vs-entry semantics and rails"
      pattern: "Keyword.merge|per_query|max_schemas"
---

<objective>
Implement the actual Phase 84 metadata and multi-search parity seam and make the new contract green.

Purpose: Deliver honest capability reflection and `search_many/2` composition parity without widening runtime semantics or boundary claims.
Output: Working metadata modules and multi-search lowering helpers that satisfy the focused Phase 84 tests and verify task.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-CONTEXT.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-RESEARCH.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-PATTERNS.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-VALIDATION.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-01-PLAN.md
@.planning/phases/84-metadata-reflection-and-multi-search-parity/84-02-PLAN.md
@lib/scrypath/schema.ex
@lib/scrypath/options.ex
@lib/scrypath/composition.ex
@lib/scrypath/multi_search/entries.ex
@lib/scrypath/search.ex
@test/scrypath/metadata_test.exs
@test/scrypath/composition_many_test.exs
@test/scrypath/search_many_test.exs

<interfaces>
From `lib/scrypath/schema.ex`:
```elixir
def __scrypath__(:filterable), do: @scrypath_config.filterable
def __scrypath__(:faceting), do: @scrypath_config.faceting
def __scrypath__(:sortable), do: @scrypath_config.sortable
```

From `lib/scrypath/options.ex`:
```elixir
@spec validate_search_options(module(), keyword()) :: {:ok, keyword()} | {:error, term()}
```

From `lib/scrypath/multi_search/entries.ex`:
```elixir
merged <- Keyword.merge(shared, entry_core, fn _k, _s, e -> e end)
merged <- merge_per_query_shallow(shared, entry_core, merged)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement declaration-backed capability metadata and resolved single-search reflection</name>
  <read_first>lib/scrypath/metadata.ex, lib/scrypath/metadata/capabilities.ex, lib/scrypath/metadata/resolve.ex, lib/scrypath/schema.ex, lib/scrypath/options.ex, test/scrypath/metadata_test.exs, .planning/phases/84-metadata-reflection-and-multi-search-parity/84-CONTEXT.md</read_first>
  <files>lib/scrypath.ex, lib/scrypath/metadata.ex, lib/scrypath/metadata/capabilities.ex, lib/scrypath/metadata/resolve.ex, test/scrypath/metadata_test.exs</files>
  <acceptance_criteria>
    - `rg -n "schema_capabilities|reflect_search" lib/scrypath.ex lib/scrypath/metadata.ex` finds the implemented public API.
    - `rg -n "filterable|sortable|faceting|page|limits|unsupported|defaulted|fixed" lib/scrypath/metadata/capabilities.ex lib/scrypath/metadata/resolve.ex` finds explicit derivation and resolved-state logic.
    - `mix test test/scrypath/metadata_test.exs --seed 0` passes.
  </acceptance_criteria>
  <action>Implement `lib/scrypath/metadata/capabilities.ex` and `lib/scrypath/metadata/resolve.ex`, then wire them through `lib/scrypath/metadata.ex` and root convenience functions in `lib/scrypath.ex`. Build capability metadata from `__scrypath__(:filterable)`, `:sortable`, `:faceting`, and the current search option families validated by `Scrypath.Options.validate_search_options/2`; include paging and stable limits that are true before caller input is applied. Implement `reflect_search/2` so it accepts either explicit criteria maps or Phase 83 composition output, computes the final canonical criteria, and returns field-scoped `applied`, `defaulted`, `fixed`, and `unsupported` state alongside the capability envelope. Keep `unsupported` explicit only when a caller attempted something the schema does not support. Do not expose `%Scrypath.Query{}` or backend-specific payloads.</action>
  <verify>
    <automated>mix test test/scrypath/metadata_test.exs</automated>
  </verify>
  <done>Single-search metadata is honest, derived from real runtime truth, and usable by host UIs without generated widget semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement multi-search composition lowering and entry-scoped reflection</name>
  <read_first>lib/scrypath/composition.ex, lib/scrypath/composition/multi.ex, lib/scrypath/multi_search/entries.ex, lib/scrypath/search.ex, test/scrypath/composition_many_test.exs, test/scrypath/search_many_test.exs, .planning/phases/84-metadata-reflection-and-multi-search-parity/84-CONTEXT.md</read_first>
  <files>lib/scrypath/composition.ex, lib/scrypath/composition/multi.ex, lib/scrypath/metadata.ex, test/scrypath/composition_many_test.exs</files>
  <acceptance_criteria>
    - `rg -n "compose_many|compose_many!|to_search_many_args" lib/scrypath/composition.ex lib/scrypath/composition/multi.ex` finds the implemented lowering path.
    - `rg -n "shared.*defaults|shared.*fixed|per_query|:all|entry-scoped" lib/scrypath/composition/multi.ex` finds explicit semantics and rejection paths.
    - `mix test test/scrypath/composition_many_test.exs test/scrypath/search_many_test.exs` passes.
  </acceptance_criteria>
  <action>Implement `lib/scrypath/composition/multi.ex` and finish the new public helpers on `lib/scrypath/composition.ex`. Use per-entry composition as the canonical unit by reusing existing Phase 83 single-search composition on each entry, then lower the result into the current `{schema, text, keyword}` tuple shape. Support shared defaults only for shared search-option vocabulary and explicitly reject shared `fixed` with stable tuple errors. Keep `federation_weight`, `global_schemas`, `max_schemas`, federation limits, and backend/runtime config outside composition, and preserve `:per_query` shallow merge semantics aligned with `Entries.normalize/2`. Ensure `reflect_search_many/2` stays entry-scoped by default and never publishes a merged global capability surface across schemas.</action>
  <verify>
    <automated>mix test test/scrypath/composition_many_test.exs test/scrypath/search_many_test.exs</automated>
  </verify>
  <done>Multi-search composition now reuses the existing public model and lowers into current runtime semantics without widening them.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the focused phase gate and keep docs/examples honest</name>
  <read_first>lib/mix/tasks/verify.phase84.ex, test/scrypath/docs_contract_test.exs, guides/multi-index-search.md, guides/faceted-search-with-phoenix-liveview.md, lib/scrypath.ex, CONTRIBUTING.md</read_first>
  <files>test/scrypath/docs_contract_test.exs, guides/multi-index-search.md, guides/faceted-search-with-phoenix-liveview.md, lib/scrypath.ex</files>
  <acceptance_criteria>
    - `mix verify.phase84` exits successfully.
    - `mix test --exclude integration --exclude docs_contract` exits successfully after the new modules are added.
    - `rg -n "host-owned|generated UI|search_many/2|compose_many|schema_capabilities" test/scrypath/docs_contract_test.exs guides/multi-index-search.md guides/faceted-search-with-phoenix-liveview.md lib/scrypath.ex` finds the final public boundary.
  </acceptance_criteria>
  <action>Run the full focused gate and fix any remaining contract drift in the implementation rather than weakening tests. Update `guides/multi-index-search.md` and `guides/faceted-search-with-phoenix-liveview.md` so examples show metadata-driven honest controls and per-entry parity without implying generated widgets, merged global capabilities, or authz/tenant-policy guarantees. Treat any docs-contract failure that blurs host-owned concerns or widens runtime semantics as a correctness bug. Leave `Scrypath.search/3` and `search_many/2` as the only runtime entrypoints, and keep metadata/lowering helpers data-only.</action>
  <verify>
    <automated>mix verify.phase84</automated>
  </verify>
  <done>Phase 84 closes with a passing focused gate, honest docs, and no drift into deferred product scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declarations and validators → public metadata | Reflection must stay synced with real runtime support |
| lowering helpers → executor inputs | Helpers must not create hidden new semantics |
| docs/examples → adopter decisions | Public language must stay precise enough to avoid false trust in unsupported UI/policy behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-07 | T | `lib/scrypath/metadata/capabilities.ex` | mitigate | Derive capability truth from current declaration and validation seams instead of freeform manual maps. |
| T-84-08 | E | `lib/scrypath/composition/multi.ex` | mitigate | Reject shared `fixed`, preserve current precedence, and lower to existing tuple/shared-option args only. |
| T-84-09 | I | docs and resolved metadata | mitigate | Keep `host_owned` advisory fields explicit and keep generated-widget / authorization claims out of public examples and docs. |
</threat_model>

<verification>
Primary gate: `mix verify.phase84`

Secondary confidence check: `mix test --exclude integration --exclude docs_contract`
</verification>

<success_criteria>
Phase 84 ships one honest metadata reflection seam and one honest multi-search lowering seam, both derived from existing runtime truths and both verified against focused tests and public-language contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/84-metadata-reflection-and-multi-search-parity/84-03-SUMMARY.md`
</output>
