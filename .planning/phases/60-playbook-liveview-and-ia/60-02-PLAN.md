---
phase: 60
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/router.ex
  - scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
  - scrypath_ops/lib/scrypath_ops_web/live/on_mount.ex
autonomous: true
requirements:
  - OPS-PB-02
---

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| XSS via playbook JSON in `<pre>` | Medium | Escape output (use **`Phoenix.HTML.html_escape/1`** on string fragments or render via components that escape) |
| Huge paste / upload DoS | Medium | **`max_file_size`**, paste **`byte_size`** cap before **`Jason.decode/1`** per **D-05/D-06** |
| CSRF on mutating events | Low | LiveView CSRF + **`protect_from_forgery`** already on browser pipeline |
</threat_model>

<objective>
Ship **`ScrypathOpsWeb.PlaybookLive`** at **`/ops/playbooks`** inside **`live_session :ops`**, matching **`60-UI-SPEC.md`**: honesty panel (**Non-production playbook workspace**), upload-primary import, paste-secondary import, list workspace JSON files, load read-only preview + **Run saved playbook**, save current validated playbook to disk (when workspace writable), delete with destructive confirm pattern. Wire run to **`Playbook.Runner.run_validated/3`**. Align validation error copy with **CONTEXT** (no **“Edit JSON (if inline)”** branch — use **Import playbook JSON** / offline fix only).
</objective>

<tasks>

<task>
<type>execute</type>
<id>60-02-01</id>
<title>Router: live "/playbooks"</title>
<read_first>
- scrypath_ops/lib/scrypath_ops_web/router.ex
- scrypath_ops/lib/scrypath_ops_web/live/on_mount.ex
</read_first>
<action>
Insert **`live("/playbooks", PlaybookLive)`** inside the existing **`live_session :ops`** block after **`SearchLive`** (order matches nav later: playbooks after search). Compile must succeed.
</action>
<acceptance_criteria>
- **`grep -n 'playbooks' scrypath_ops/lib/scrypath_ops_web/router.ex`** shows the new **`live`** line
- **`cd scrypath_ops && mix compile`** exits **0**
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>60-02-02</id>
<title>PlaybookLive — mount, assigns, list</title>
<read_first>
- scrypath_ops/lib/scrypath_ops_web/live/search_live.ex
- scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
- .planning/phases/60-playbook-liveview-and-ia/60-UI-SPEC.md
- scrypath_ops/lib/scrypath_ops/playbook/store.ex
</read_first>
<action>
Create **`scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex`**. **`mount/3`**: assign **`page_title`** **"Saved playbooks"** (or UI-SPEC-consistent), **`workspace_root`** from **`Application.get_env(:scrypath_ops, :playbook_workspace_dir)`**, **`workspace_writable?`** boolean (**`true`** iff env-backed root set), **`examples` / `workspace_files`** per **D-08** (if root nil: load read-only filenames from **`Application.app_dir(:scrypath_ops, "priv/playbooks")`** when that directory exists). Use **`Layouts.app`** with **`shell={:ops}`**, **`ops_main_width={:wide}`** to match list/table UX. Render honesty panel per **UI-SPEC** (title **Non-production playbook workspace**; body mentions logging risk + bounded **`page.size`** / schema caps).
</action>
<acceptance_criteria>
- **`mix compile`** succeeds
- **`cd scrypath_ops && mix test test/scrypath_ops_web/live/playbook_live_test.exs`** exists and passes a **"mount shows honesty panel"** assertion with string **`Non-production playbook workspace`**
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>60-02-03</id>
<title>Upload + paste import converge on validate</title>
<read_first>
- scrypath_ops/lib/scrypath_ops_web/live/search_live.ex
- scrypath_ops/lib/scrypath_ops/playbook/v1.ex
- .planning/phases/60-playbook-liveview-and-ia/60-CONTEXT.md
</read_first>
<action>
Add **`allow_upload`** on mount: **`.json`**, content types including **`application/json`**, **`max_entries: 1`**, **`max_file_size`** set to a concrete integer (e.g. **256_000** bytes) matching paste cap policy. Secondary collapsed **`details`** or tab for paste: textarea + **`phx-submit="import_paste"`** with **`byte_size`** guard (same order of magnitude as upload cap). Both paths: read UTF-8 binary → **`Jason.decode/1`** → **`Playbook.V1.validate/1`**; on success store in assign **`:draft_playbook`** (validated map) and flash success; on **`Jason`** error show UI-SPEC string **`This file is not valid playbook JSON.`**; on validation error show **`Playbook failed validation:`** prefix plus safe reason (no raw upload logged).
</action>
<acceptance_criteria>
- LiveView module contains **`allow_upload`**
- **`playbook_live_test.exs`** includes test importing valid JSON string via event (or upload test) asserting **`draft_playbook`** assign or rendered preview marker
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>60-02-04</id>
<title>Load preview, run, save, delete</title>
<read_first>
- scrypath_ops/lib/scrypath_ops/playbook/runner.ex
- scrypath_ops/lib/scrypath_ops_web/live/search_live.ex
- .planning/phases/60-playbook-liveview-and-ia/60-UI-SPEC.md
</read_first>
<action>
Implement events: **`load`** (basename from list only) reads file via **`Store`**, decodes+validates, assigns preview via **`Jason.encode!(map, pretty: true)`** shown inside **escaped** `<pre>`. **`run`** calls **`Runner.run_validated/3`** with **`Schemas.allowlist/0`** and **`Schemas.scrypath_opts/0`**; render results or reuse compact result partials analogous to search (minimal: show **:ok** hit stub empty state or error alert using operator-safe copy). **`save`** writes **`Playbook.V1.encode/1`** output only when **`workspace_writable?`** and basename user input passes **`Store.safe_basename?/1`**. **`delete`** shows confirm pattern per UI-SPEC (**typed slug** or modal — pick one; document in module doc). Disable save/delete when read-only examples mode per **D-08**.
</action>
<acceptance_criteria>
- Template or render function includes primary CTA string **`Run saved playbook`** (from UI-SPEC)
- **`mix test test/scrypath_ops_web/live/playbook_live_test.exs`** exits **0** including a stub-backed **run** test
- **`grep -R "Edit JSON (if inline)" scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex`** returns no matches
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd scrypath_ops && mix test test/scrypath_ops_web/live/playbook_live_test.exs
```
</verification>

<success_criteria>
- **`/ops/playbooks`** renders with honesty + list + import + run path on stub adapter.
- Copy matches locked UI-SPEC CTAs and destructive confirm body pattern.
</success_criteria>

<must_haves>
- Dedicated LiveView (not embedded in **`SearchLive`**).
- Import/run/save/delete behaviors align with **60-CONTEXT** decisions **D-04–D-14**.
</must_haves>

## PLANNING COMPLETE
