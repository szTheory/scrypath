---
phase: 60
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/nav.ex
  - scrypath_ops/docs/operator-ia.md
  - scrypath_ops/lib/scrypath_ops_web/live/search_live.ex
  - scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs
autonomous: true
requirements:
  - OPS-PB-02
  - OPS-PB-04
---

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Stale operator IA misleads on-call | Medium | Nav table + JTBD prose updated honestly; **`check_nav_contract`** enforced |
| Phishing via forged internal links | Low | Use verified **`~p`** routes only |
</threat_model>

<objective>
Complete **OPS-PB-04**: extend **`Nav.primary/0`** with **`Saved playbooks`** → **`/ops/playbooks`** as **fifth** entry after search; sync **`operator-ia.md`** primary nav table and **`nav-contract`** JSON; add cross-link from **`SearchLive`** to playbooks per **D-03**. Run **`mix scrypath_ops.check_nav_contract`** and full **`scrypath_ops`** tests.
</objective>

<tasks>

<task>
<type>execute</type>
<id>60-03-01</id>
<title>Nav.primary fifth entry</title>
<read_first>
- scrypath_ops/lib/scrypath_ops_web/nav.ex
- scrypath_ops/lib/scrypath_ops_web/router.ex
- .planning/phases/60-playbook-liveview-and-ia/60-CONTEXT.md
</read_first>
<action>
Append **`%{path: ~p"/ops/playbooks", label: "Saved playbooks"}`** to **`Nav.primary/0`** immediately after the **`Search & federation`** entry (verified path must match **`router.ex`** **`live "/playbooks"`** URL).
</action>
<acceptance_criteria>
- **`grep -A2 'Search & federation' scrypath_ops/lib/scrypath_ops_web/nav.ex`** shows **`playbooks`** path on following lines
- **`cd scrypath_ops && mix compile`** exits **0**
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>60-03-02</id>
<title>operator-ia.md — table + JTBD + nav-contract</title>
<read_first>
- scrypath_ops/docs/operator-ia.md
- scrypath_ops/lib/mix/tasks/scrypath_ops/check_nav_contract.ex
- .planning/phases/60-playbook-liveview-and-ia/60-UI-SPEC.md
</read_first>
<action>
1. Add a **Jobs-to-be-done** bullet (position after search-related bullets) describing **replay saved searches / playbooks** honestly (ops-local JSON, bounded runs).
2. Add a **Navigation** table row: persona **Search owner** or **Library maintainer**, label **`Saved playbooks`**, route **`/ops/playbooks`**, follow-up **`scrypath_ops/docs/playbook-schema-v1.md`**.
3. Update **`nav-contract`** JSON between markers to exactly reflect **`Nav.primary/0`** (label string **`Saved playbooks`**). Prefer running **`mix scrypath_ops.check_nav_contract`** and committing the formatter output if the task rewrites the fence.
</action>
<acceptance_criteria>
- **`grep -n 'Saved playbooks' scrypath_ops/docs/operator-ia.md`** returns ≥ **2** lines (table + JSON)
- **`cd scrypath_ops && mix scrypath_ops.check_nav_contract`** exits **0**
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>60-03-03</id>
<title>operator_ia_contract_test — five routes + five nav labels</title>
<read_first>
- scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs
- scrypath_ops/lib/scrypath_ops_web/router.ex
- scrypath_ops/lib/scrypath_ops_web/nav.ex
</read_first>
<action>
Update **`OperatorIaContractTest`** so router parity and **`Nav.primary/0`** expectations include **`/ops/playbooks`** and label **`Saved playbooks`**. Specifically: extend **`test "every live /ops route in router.ex is documented in operator-ia.md"`** to assert **`live("/playbooks"`** in **`@router`** and **`/ops/playbooks`** in **`@operator_ia`**. Change **`Nav.primary/0 exposes four ordered ops routes`** to **five** routes with the new path/label appended after search. Keep ordering assertions consistent with **`Nav.primary/0`** implementation.
</action>
<acceptance_criteria>
- **`grep -n 'playbooks' scrypath_ops/test/scrypath_ops_web/operator_ia_contract_test.exs`** matches at least **3** lines (router doc + ia doc + nav list)
- **`cd scrypath_ops && mix test test/scrypath_ops_web/operator_ia_contract_test.exs`** exits **0** (after executor implements nav/router/docs changes from prior tasks)
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>60-03-05</id>
<title>SearchLive cross-link</title>
<read_first>
- scrypath_ops/lib/scrypath_ops_web/live/search_live.ex
</read_first>
<action>
Add a subdued **`Phoenix.Component.link`** ( **`navigate={~p"/ops/playbooks"}`** ) near **`ops_page_header`** or secondary actions row with text **`Saved playbooks`** (or shorter **Playbooks** if space tight — must remain recognizable). Use **`link link-hover`** styling per UI-SPEC (not **`btn-primary`**).
</action>
<acceptance_criteria>
- **`grep -n 'playbooks' scrypath_ops/lib/scrypath_ops_web/live/search_live.ex`** shows **`~p"/ops/playbooks"`**
- **`cd scrypath_ops && mix test test/scrypath_ops_web/live/search_live_test.exs`** exits **0**
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<id>60-03-06</id>
<title>Regression sweep</title>
<read_first>
- scrypath_ops/mix.exs
</read_first>
<action>
Run **`cd scrypath_ops && mix test`** and fix any failures from nav/layout changes.
</action>
<acceptance_criteria>
- **`cd scrypath_ops && mix test`** exits **0**
</acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd scrypath_ops && mix scrypath_ops.check_nav_contract && mix test
```
</verification>

<success_criteria>
- Nav contract and router labels stay aligned.
- Search surface links to playbook library without nesting LiveViews.
</success_criteria>

<must_haves>
- **`mix scrypath_ops.check_nav_contract`** green in CI-local run.
- OPS-PB-04 doc + JSON fence updated.
</must_haves>

## PLANNING COMPLETE
