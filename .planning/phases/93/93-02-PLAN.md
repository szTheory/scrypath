---
phase: 93-reflection-and-runtime-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/options.ex
  - test/scrypath/options_test.exs
autonomous: true
requirements: [TNNT-04]

must_haves:
  truths:
    - "tenant_scope: is accepted as a valid search option"
    - "Using tenant_scope: automatically injects the tenant field into the filter: keyword list"
    - "Caller-supplied filter: cannot shadow or override the tenant_scope:"
    - "Using tenant_scope: on a schema without a tenant_field: raises a deterministic error"
  artifacts:
    - path: "lib/scrypath/options.ex"
      provides: "tenant_scope: in @search_options and inject_tenant_scope!/2 pipeline step"
      contains: "tenant_scope:"
    - path: "test/scrypath/options_test.exs"
      provides: "Tests for successful injection, conflict raising, and missing tenant_field: raising"
  key_links:
    - from: "lib/scrypath/options.ex"
      to: "inject_tenant_scope!/2"
      via: "Pipeline inside validate_search_options/2"
      pattern: "inject_tenant_scope!"
---

<objective>
Implement runtime enforcement of the tenant filter using `tenant_scope:`.

Purpose: Provide a library-level mechanism to hard-inject the tenant guard into search options. This bypasses caller `Keyword.merge` footguns and guarantees the tenant filter is present and cannot be overridden by context-layer filter arguments.
Output: `tenant_scope:` option is parsed, enforced, and automatically injected into `filter:` during validation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/scrypath/options.ex
@test/scrypath/options_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenant_scope: to @search_options and injection logic</name>
  <files>lib/scrypath/options.ex</files>
  <action>
    1. Add `tenant_scope:` to `@search_options` with `type: :any` and `doc: "Hard-injected filter for the declared tenant_field."`. Do not provide a `default:` so its explicit presence is preserved by NimbleOptions.
       **Analog Reference (`lib/scrypath/options.ex`):** Follow the structure of `filter:` in `@search_options`. Omit the `default:` key and set `required: false` so `NimbleOptions` preserves its explicit presence/absence.
    2. Add a private `inject_tenant_scope!(opts, schema_module)` function that intercepts the validated options:
       - Use `Keyword.fetch(opts, :tenant_scope)` to detect presence.
       - If present and `schema_module.__scrypath__(:tenant_field)` is `nil`, raise `ArgumentError, "tenant_scope: provided but schema #{inspect(schema_module)} does not declare a tenant_field:"`.
       - If present and `tenant_field` exists, check the existing `:filter` in `opts`. If `Keyword.has_key?(filter, tenant_field)`, raise `ArgumentError, "tenant_scope: cannot be used because filter: already contains the tenant_field #{inspect(tenant_field)}. Remove it from filter: to allow tenant enforcement."`
       - If valid, inject the filter: `new_filter = Keyword.put(filter, tenant_field, tenant_scope)` and return `opts |> Keyword.delete(:tenant_scope) |> Keyword.put(:filter, new_filter)`.
       - If `tenant_scope:` is not present, return `opts` unchanged.
       **Analog Reference & Pattern Excerpt:**
       For conflict checking, mirror `merge_facet_bucket_into_opts!` (`lib/scrypath/search.ex`):
       ```elixir
       if Keyword.has_key?(existing, attr) do
         raise ArgumentError, "..."
       else
         Keyword.put(opts, :facet_filter, Keyword.put(existing, attr, value))
       end
       ```
    3. Call `inject_tenant_scope!(opts, schema_module)` in the `try do` block of `validate_search_options/2`, right before `validate_filterable_fields!(filterable)`.
       **Analog Reference & Pattern Excerpt (`lib/scrypath/options.ex`):**
       ```elixir
       try do
         validated
         |> inject_tenant_scope!(schema_module)
         |> validate_filterable_fields!(filterable)
         |> validate_sortable_fields!(sortable)
         |> then(&{:ok, &1})
       rescue
         e in ArgumentError -> {:error, {:validation, Exception.message(e)}}
       end
       ```
       Placing the injection step here automatically enables `tenant_scope:` for `Scrypath.search/3`, `Scrypath.search_within_facet/4`, and `Scrypath.search_many/2` concurrently.
  </action>
  <verify>
    <automated>grep -q "inject_tenant_scope!" lib/scrypath/options.ex && echo "OK"</automated>
  </verify>
  <done>The pipeline safely injects the tenant filter and removes `:tenant_scope` before returning options.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test tenant_scope: injection and validations</name>
  <files>test/scrypath/options_test.exs</files>
  <behavior>
    - Test 1: `validate_search_options!(MockTenantSchema, tenant_scope: 123)` returns options where `filter: [tenant_id: 123]`.
    - Test 2: `validate_search_options!(MockTenantSchema, tenant_scope: 123, filter: [status: "active"])` returns options where `filter: [tenant_id: 123, status: "active"]`.
    - Test 3: `validate_search_options!(MockTenantSchema, tenant_scope: 123, filter: [tenant_id: 456])` raises `ArgumentError` about existing filter.
    - Test 4: `validate_search_options!(FacetableMovie, tenant_scope: 123)` (where schema lacks `tenant_field:`) raises `ArgumentError` about missing declaration.
  </behavior>
  <action>
    Add a new `describe "tenant_scope: search option"` block. Define a `MockTenantSchema` inline (or use an existing one if available) that returns `:tenant_id` for `__scrypath__(:tenant_field)` and includes `:tenant_id` in `:filterable`. Implement tests covering successful injection, missing schema declaration, and filter key conflicts. Ensure errors raised by `validate_search_options!` are caught/asserted properly.
  </action>
  <verify>
    <automated>mix test test/scrypath/options_test.exs</automated>
  </verify>
  <done>Tests pass, guaranteeing the library-level guard correctly enforces multitenancy scopes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller -> Library | `tenant_scope:` creates a library-enforced mechanism ensuring that caller data-layer queries cannot bypass or overwrite the tenant identity check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02 | Elevation of Privilege | `inject_tenant_scope!/2` | mitigate | `tenant_scope:` forces prepending/putting the tenant field to `:filter`. If the caller tries to manually supply the tenant field in `:filter` alongside `tenant_scope:`, `ArgumentError` is raised, preventing shadow overwriting. |
</threat_model>

<verification>
mix test test/scrypath/options_test.exs
</verification>

<success_criteria>
`Scrypath.search/3` (via validation) securely translates `tenant_scope:` into an inescapable tenant filter. Misconfigurations fail deterministically.
</success_criteria>

<output>
After completion, create `.planning/phases/93/93-02-SUMMARY.md`
</output>