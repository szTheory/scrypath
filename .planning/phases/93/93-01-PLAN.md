---
phase: 93-reflection-and-runtime-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/metadata/capabilities.ex
  - test/scrypath/metadata_test.exs
autonomous: true
requirements: [TNNT-03]

must_haves:
  truths:
    - "schema_capabilities/1 returns a map containing a :tenant key"
    - "The :tenant key is nil for schemas without a declared tenant_field:"
    - "The :tenant key holds the declared atom for schemas with a tenant_field:"
  artifacts:
    - path: "lib/scrypath/metadata/capabilities.ex"
      provides: "tenant key in schema_capabilities/1 return map"
      contains: "tenant: schema_module.__scrypath__(:tenant_field)"
    - path: "test/scrypath/metadata_test.exs"
      provides: "Tests for :tenant key in capabilities map"
  key_links:
    - from: "lib/scrypath/metadata/capabilities.ex"
      to: "__scrypath__(:tenant_field)"
      via: "Function call inside schema_capabilities/1"
      pattern: "__scrypath__\\(:tenant_field\\)"
---

<objective>
Expose the schema's `tenant_field:` declaration via the `schema_capabilities/1` reflection function.

Purpose: Allow developers to introspect whether a schema is tenant-isolated and which field it uses, closing the credibility gap for B2B Phoenix adopters by making the isolation pattern visible.
Output: `schema_capabilities/1` includes a `:tenant` key.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/scrypath/metadata/capabilities.ex
@test/scrypath/metadata_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add :tenant key to schema_capabilities/1</name>
  <files>lib/scrypath/metadata/capabilities.ex</files>
  <action>
    Update `schema_capabilities/1` to include a `:tenant` key at the root level of the returned capabilities map.
    The value should be extracted directly from `schema_module.__scrypath__(:tenant_field)`.

    **Analog Reference & Pattern Excerpt:**
    Based on existing patterns in `lib/scrypath/metadata/capabilities.ex` (analog: `schema_capabilities/1` core reflection pattern):
    ```elixir
      %{
        filters: %{
          supported: schema_module.__scrypath__(:filterable) != [],
          # ...
    ```
    Add `tenant: schema_module.__scrypath__(:tenant_field),` at the root level of the returned map. Since Phase 92 ensures `__scrypath__(:tenant_field)` returns `nil` when not declared, this safely covers both the populated and `nil` requirements.
  </action>
  <verify>
    <automated>grep -q "tenant: schema_module.__scrypath__(:tenant_field)" lib/scrypath/metadata/capabilities.ex && echo "OK"</automated>
  </verify>
  <done>The `:tenant` key is present in the returned map.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test :tenant key in schema_capabilities/1</name>
  <files>test/scrypath/metadata_test.exs</files>
  <behavior>
    - Test 1: `Scrypath.schema_capabilities(FacetableMovie)` (which lacks a tenant_field) returns a map where `tenant: nil`. (Update existing test)
    - Test 2: A mock schema module with a defined `__scrypath__(:tenant_field)` returning `:account_id` returns a capabilities map where `tenant: :account_id`. You can define a simple inline `defmodule` in the test file (e.g., `TenantSchema`) that implements the required `__scrypath__/1` callbacks (`:tenant_field`, `:faceting`, `:filterable`, `:sortable`) and assert against it.
  </behavior>
  <action>
    Implement the tests confirming the reflection works for both present and absent `tenant_field:` declarations. Ensure `tenant:` is checked in the existing `schema_capabilities/1` test for `FacetableMovie`.
  </action>
  <verify>
    <automated>mix test test/scrypath/metadata_test.exs</automated>
  </verify>
  <done>Tests pass, proving the `tenant` key is exposed based on the schema's metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller -> Library | Exposing configuration data, no runtime control boundary changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01 | Information Disclosure | `schema_capabilities/1` | accept | Only exposing static schema configuration; no user data or secret disclosure. |
</threat_model>

<verification>
mix test test/scrypath/metadata_test.exs
</verification>

<success_criteria>
`schema_capabilities/1` accurately reflects the presence or absence of a `tenant_field:` on the schema via the `:tenant` key.
</success_criteria>

<output>
After completion, create `.planning/phases/93/93-01-SUMMARY.md`
</output>