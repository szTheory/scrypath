---
phase: 132-a11y-contrast-remediation-both-themes-hard-gate-g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scrypath_ops/assets/css/app.css
  - scrypath_ops/assets/css/contrast-pairs.mjs
  - scrypath_ops/assets/css/DESIGN-TOKENS.md
  - examples/scrypath_ecommerce/contrast-checker.mjs
autonomous: true
requirements:
  - A11Y-TOKEN-01
must_haves:
  truths:
    - "D-02: Readable muted text uses named --ops-text-muted instead of scattered raw percentages."
    - "D-02a: The flagged selectors and ops-owned `/60` utility text route through the named muted token."
    - "D-02b: DESIGN-TOKENS.md records the final muted alpha floor in the same plan as the CSS change."
    - "D-01: Text-bearing selected violet fills use --color-primary-strong; decorative --color-primary and --color-accent values remain unchanged."
    - "D-01a: Dark/system-dark are confirmed by the gate and not regressed; latest Phase 131 evidence overrides the stale light-only wording by proving the current failing pair is dark/system-dark #6c5ce7 on cream."
    - "D-03: Header/nav text contrast is addressed with token math only; header chrome, layout, wash, and depth remain Phase 135 scope."
    - "The fast token checker understands the new named muted token and primary-strong pair without suppressing AA failures."
  artifacts:
    - path: "scrypath_ops/assets/css/app.css"
      provides: "Theme-scoped AA text tokens and selector routing"
      contains: "--ops-text-muted"
    - path: "scrypath_ops/assets/css/contrast-pairs.mjs"
      provides: "Muted text manifest entries for named token consumers"
      contains: "css_var"
    - path: "examples/scrypath_ecommerce/contrast-checker.mjs"
      provides: "Fast AA gate support for --color-primary-strong and named muted tokens"
      contains: "primary-strong"
    - path: "scrypath_ops/assets/css/DESIGN-TOKENS.md"
      provides: "Lockstep documentation for Phase 132 contrast floors"
      contains: "A11y contrast floors"
  key_links:
    - from: "scrypath_ops/assets/css/app.css"
      to: "scrypath_ops/assets/css/contrast-pairs.mjs"
      via: "selector + css_var manifest entries"
      pattern: "css_var.*ops-text-muted"
    - from: "examples/scrypath_ecommerce/contrast-checker.mjs"
      to: "scrypath_ops/assets/css/app.css"
      via: "parse theme blocks and D-15 lockstep guard"
      pattern: "primary-strong"
---

<objective>
Implement the Phase 132 source remediation for A11Y-TOKEN-01.

Purpose: Turn the remaining muted-text and primary-violet AA failures into named, enforceable tokens without changing Phoenix components, LiveView behavior, layout, copy, spacing, motion, or Phase 135 shell chrome.
Output: Updated CSS tokens/selectors, updated contrast manifest/checker support, and lockstep design-token documentation.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-CONTEXT.md
@.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-RESEARCH.md
@.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-VALIDATION.md
@.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-PATTERNS.md
@.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-UI-SPEC.md
@.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md
@scrypath_ops/assets/css/app.css
@scrypath_ops/assets/css/contrast-pairs.mjs
@scrypath_ops/assets/css/DESIGN-TOKENS.md
@examples/scrypath_ecommerce/contrast-checker.mjs
@scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
@scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex
@prompts/scrypath-brand-book.md

<interfaces>
Key local contracts the executor must preserve:
- `examples/scrypath_ecommerce/contrast-checker.mjs` parses only `--color-*` hex declarations from the daisyUI `@plugin` blocks; adding `--color-primary-strong` requires updating the D-15 token count guard from 20 to 21.
- Existing `PAIR_RULES` treats `primary-content/primary` as role `ui` at 3.0. Phase 132 needs a separate `primary-content/primary-strong` rule with role `text` at 4.5.
- Existing D-15 Guard 2 only sees raw `color: color-mix(in oklch, var(--color-base-content) NN%, transparent)` declarations. Phase 132 named muted tokens require the guard to also accept `color: var(--ops-text-muted)` when the matching `MUTED_PAIRS` entry names `css_var: "ops-text-muted"`.
- Current source has dark `--color-primary: #6c5ce7`, light `--color-primary: #5b4ad1`, and cream `--color-primary-content: #f4f1ea`; `#f4f1ea` on `#6c5ce7` is the 4.3:1 failing pair recorded by Phase 131.
</interfaces>
</context>

<source_audit>
| Source | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | - | Re-tune muted text and text-bearing violet fills so AA passes in both themes | 132-01, 132-02 | COVERED | Plan 01 changes source tokens; Plan 02 proves the hard gate |
| REQ | A11Y-TOKEN-01 | Muted alphas, header nav `/60`, handoff/palette/preflight hints, primary violet fills clear AA | 132-01, 132-02 | COVERED | Requirement appears in both plan frontmatter fields |
| RESEARCH | - | No new packages; use existing checker and Playwright harness | 132-01, 132-02 | COVERED | Plans include no install tasks |
| RESEARCH | - | Update checker for `--color-primary-strong` and named muted tokens instead of hiding failures | 132-01 | COVERED | Task 1 updates D-15 guard and pair rules |
| CONTEXT | D-01 | Scoped `--color-primary-strong`, not global `--color-primary` retune | 132-01 | COVERED | Task 2 routes only `.ops-nav-item-active` and `.bg-primary.text-primary-content` |
| CONTEXT | D-01a | Confirm dark/system-dark and do not regress dark when applying primary strong | 132-01, 132-02 | COVERED | Plan follows latest Phase 131 failure evidence: dark/system-dark `#6c5ce7` on cream fails, so the scoped strong token fixes the current failing text-bearing dark/system pair without changing decorative primary |
| CONTEXT | D-02 | Named muted AA-floor token(s), not scattered percentages | 132-01 | COVERED | Task 2 routes flagged selectors and utility `/60` text to `--ops-text-muted` |
| CONTEXT | D-02a | Route `.ops-text-meta`, `.ops-trail__crumb`, `.ops-cmdk__item-hint`, `.ops-cmdk__empty`, and header nav `/60` through the floor | 132-01 | COVERED | Task 2 includes all named selectors plus `.ops-header .text-base-content\/60` and `.ops-shell .text-base-content\/60` |
| CONTEXT | D-02b | Record new alpha floors in `DESIGN-TOKENS.md` in lockstep | 132-01 | COVERED | Task 3 writes the exact `64%` floor and consumer list |
| CONTEXT | D-03 | Phase 132 owns text contrast only; Phase 135 owns chrome/depth/wash | 132-01, 132-02 | COVERED | Task actions forbid layout/chrome changes |
| CONTEXT | D-04 | AA hard gate; AAA body is advisory/report only | 132-02 | COVERED | Plan 02 report captures AAA advisory without making it blocking |
| UI-SPEC | - | CSS/token-only; no typography, spacing, copy, behavior, or component changes | 132-01 | COVERED | Acceptance criteria forbid HEEx/component changes |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the fast contrast gate for named AA tokens</name>
  <files>examples/scrypath_ecommerce/contrast-checker.mjs, scrypath_ops/assets/css/contrast-pairs.mjs</files>
  <read_first>
    @examples/scrypath_ecommerce/contrast-checker.mjs
    @scrypath_ops/assets/css/contrast-pairs.mjs
    @scrypath_ops/assets/css/app.css
    @.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-CONTEXT.md
    @.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-RESEARCH.md
    @.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-PATTERNS.md
    @.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `node examples/scrypath_ecommerce/contrast-checker.mjs --self-test` exits 0 after guard changes.
    - Test 2: `node --check examples/scrypath_ecommerce/contrast-checker.mjs` exits 0.
    - Test 3: After Task 2 adds CSS tokens, `CONTRAST_REPORT_DIR=test-results/contrast/phase132-token node contrast-checker.mjs` reports `AA failures:  0`.
  </behavior>
  <action>Update `contrast-checker.mjs` so D-15 Guard 1 accepts exactly 21 `--color-*` tokens per theme and requires the new parsed key `primary-strong`. Add a semantic pair rule for `primary-content/primary-strong` with role `text` so the fast checker requires 4.5:1 for the scoped text-bearing fill while leaving `primary-content/primary` as role `ui`. Extend Guard 2 to recognize readable muted CSS rules of the form `color: var(--ops-text-muted)` and match those rules to `MUTED_PAIRS` entries with `css_var: "ops-text-muted"` and `alpha: 0.64`. Parse the `--ops-text-muted` declaration in both daisyUI theme blocks and fail if its `color-mix(in oklch, var(--color-base-content) 64%, transparent)` alpha differs from the manifest. In `contrast-pairs.mjs`, update the readable selectors `.ops-text-meta`, `.ops-trail__crumb`, `.ops-handoff__hint`, `.ops-preflight__hint`, `.ops-cmdk__item-hint`, and `.ops-cmdk__empty` to use `css_var: "ops-text-muted"` with `alpha: 0.64`; keep unchanged raw entries such as `.ops-handoff__eyebrow`, `.ops-intent-card__summary`, `.ops-tone-chip__value`, `.ops-badge-neutral`, `.ops-signal-table th[scope="row"]`, `.ops-cheatsheet__row dd`, and decorative `.ops-trail__sep` in the existing raw-alpha shape. Do not add packages, do not disable D-15 guards, and do not add axe exclusions or rule disables.</action>
  <verify>
    <automated>node --check examples/scrypath_ecommerce/contrast-checker.mjs && node examples/scrypath_ecommerce/contrast-checker.mjs --self-test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `examples/scrypath_ecommerce/contrast-checker.mjs` contains `primary-strong` in the token-count guard path and in a `PAIR_RULES` entry with role `text`.
    - Source assertion: `scrypath_ops/assets/css/contrast-pairs.mjs` contains `css_var: "ops-text-muted"` for exactly these readable selectors: `.ops-text-meta`, `.ops-trail__crumb`, `.ops-handoff__hint`, `.ops-preflight__hint`, `.ops-cmdk__item-hint`, `.ops-cmdk__empty`.
    - Source assertion: no `exclude(`, `.exclude(`, `disableRules`, or `withRules(["color-contrast"])` weakening is introduced by this task.
    - CLI assertion: both automated commands exit 0.
  </acceptance_criteria>
  <done>The fast checker is ready to validate named muted AA floors and the scoped primary-strong text pair before CSS remediation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route muted text and selected violet fills through scoped AA tokens</name>
  <files>scrypath_ops/assets/css/app.css, scrypath_ops/assets/css/contrast-pairs.mjs, examples/scrypath_ecommerce/contrast-checker.mjs</files>
  <read_first>
    @scrypath_ops/assets/css/app.css
    @scrypath_ops/assets/css/contrast-pairs.mjs
    @examples/scrypath_ecommerce/contrast-checker.mjs
    @scrypath_ops/lib/scrypath_ops_web/components/layouts.ex
    @scrypath_ops/lib/scrypath_ops_web/components/ops_ui.ex
    @.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-CONTEXT.md
    @.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-UI-SPEC.md
    @.planning/phases/131-glow-dark-shadow-and-copper-accent-system-r-g/131-VALIDATION.md
    @prompts/scrypath-brand-book.md
  </read_first>
  <behavior>
    - Test 1: The fast checker exits 0 with `AA failures:  0`.
    - Test 2: CSS source contains no remaining raw muted `color:` declarations for the six selectors moved to `--ops-text-muted`.
    - Test 3: `--color-primary` remains `#6c5ce7` in dark and `#5b4ad1` in light; decorative primary/accent roles are not globally retuned.
  </behavior>
  <action>In both daisyUI `@plugin "../vendor/daisyui-theme"` blocks in `app.css`, add `--color-primary-strong: #5b4ad1;` near `--color-primary-content` and add `--ops-text-muted: color-mix(in oklch, var(--color-base-content) 64%, transparent);` with the other `--ops-*` tokens. Per D-02a, change `.ops-text-meta`, `.ops-trail__crumb`, `.ops-handoff__hint`, `.ops-preflight__hint`, `.ops-cmdk__item-hint`, and `.ops-cmdk__empty` to `color: var(--ops-text-muted)`. Add a scoped utility override for `.ops-header .text-base-content\/60` and `.ops-shell .text-base-content\/60` that sets `color: var(--ops-text-muted)` so the header tagline and other ops-owned utility muted text clear AA without editing HEEx. Change `.ops-nav-item-active` from `background: var(--color-primary)` to `background: var(--color-primary-strong)`. Add a `.bg-primary.text-primary-content` CSS override that sets `background-color: var(--color-primary-strong)` so all text-bearing selected `bg-primary text-primary-content` states use the AA-safe fill; this is the verifiable criterion for D-01. For D-01a, explicitly preserve dark/system-dark by fixing the current Phase 131-proven dark/system failing pair with the scoped strong token and by not changing decorative `--color-primary` or `--color-accent`. Do not modify Phoenix component files, LiveView hooks, routes, copy, layout, spacing, typography, motion, `--color-primary`, `--color-accent`, glow, radial wash, borders, or Phase 135 chrome/depth recipes.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && CONTRAST_REPORT_DIR=test-results/contrast/phase132-token node contrast-checker.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the dark theme block still contains `--color-primary: #6c5ce7;` and the light theme block still contains `--color-primary: #5b4ad1;`.
    - Source assertion: both theme blocks contain `--color-primary-strong: #5b4ad1;` and `--ops-text-muted: color-mix(in oklch, var(--color-base-content) 64%, transparent);`.
    - Source assertion: `.ops-nav-item-active` uses `background: var(--color-primary-strong)`.
    - Source assertion: `.bg-primary.text-primary-content` exists in `app.css` and uses `background-color: var(--color-primary-strong)`.
    - Source assertion: `.ops-header .text-base-content\/60` and `.ops-shell .text-base-content\/60` use `color: var(--ops-text-muted)`.
    - Source assertion: `git diff --name-only -- scrypath_ops/lib` is empty for this task.
    - CLI assertion: `node contrast-checker.mjs` prints `Contrast check: PASS` and `AA failures:  0`.
  </acceptance_criteria>
  <done>All Phase 132 source-level muted and text-bearing primary fixes are routed through named AA tokens while preserving the Phase 132/135 boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Document Phase 132 contrast floors in lockstep</name>
  <files>scrypath_ops/assets/css/DESIGN-TOKENS.md, scrypath_ops/assets/css/app.css, scrypath_ops/assets/css/contrast-pairs.mjs</files>
  <read_first>
    @scrypath_ops/assets/css/DESIGN-TOKENS.md
    @scrypath_ops/assets/css/app.css
    @scrypath_ops/assets/css/contrast-pairs.mjs
    @.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-CONTEXT.md
    @.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-RESEARCH.md
    @.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-UI-SPEC.md
  </read_first>
  <action>Per D-02b, update `DESIGN-TOKENS.md` in the existing token-catalog style. Add or update a section titled `## A11y contrast floors -- Phase 132`. Record `--ops-text-muted` as `color-mix(in oklch, var(--color-base-content) 64%, transparent)` for both themes and list its consumers: `.ops-text-meta`, `.ops-trail__crumb`, `.ops-handoff__hint`, `.ops-preflight__hint`, `.ops-cmdk__item-hint`, `.ops-cmdk__empty`, `.ops-header .text-base-content/60`, and `.ops-shell .text-base-content/60`. Record `--color-primary-strong` as `#5b4ad1` and state that it is allowed only for text-bearing interactive/selected fills: `.ops-nav-item-active` and `.bg-primary.text-primary-content`. Update the muted registry prose so named-token entries with `css_var` remain part of the D-15 lockstep contract. State explicitly that AA is the hard gate and AAA body/long-form status is advisory/report-only per D-04. Do not add copy, layout, component, or Phase 135 chrome guidance.</action>
  <verify>
    <automated>cd scrypath_ops && mix assets.build && mix verify.opsui</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `DESIGN-TOKENS.md` contains the heading `## A11y contrast floors -- Phase 132`.
    - Source assertion: `DESIGN-TOKENS.md` contains the exact token strings `--ops-text-muted`, `64%`, `--color-primary-strong`, and `#5b4ad1`.
    - Source assertion: `DESIGN-TOKENS.md` names `.ops-nav-item-active` and `.bg-primary.text-primary-content` as the only primary-strong consumers.
    - Source assertion: `DESIGN-TOKENS.md` says AAA is advisory/report-only and AA failures are the hard gate.
    - CLI assertion: `mix assets.build` and `mix verify.opsui` both exit 0 from `scrypath_ops/`.
  </acceptance_criteria>
  <done>The design-token catalog is in lockstep with the Phase 132 CSS and checker contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CSS source -> generated assets | Source token edits must be rebuilt before browser proof or stale CSS can produce false evidence. |
| Contrast checker -> planning evidence | Static reports can mislead if the checker ignores named tokens or weakens thresholds. |
| Browser axe matrix -> phase close | AA failures must not be hidden with exclusions, rule disables, or reclassified thresholds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-132-01 | Repudiation | `contrast-checker.mjs` | mitigate | Keep `process.exit(report.summary.aa_fail > 0 ? 1 : 0)`, add primary-strong and named muted validation, and require `node contrast-checker.mjs` to print `AA failures:  0`. |
| T-132-02 | Tampering | `app.css` token edits | mitigate | Preserve `--color-primary` and `--color-accent`; add only `--color-primary-strong` and `--ops-text-muted`; verify source assertions in Task 2. |
| T-132-03 | Repudiation | `DESIGN-TOKENS.md` | mitigate | Require exact token/value strings and allowed consumer lists in Task 3 acceptance criteria. |
| T-132-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; package manifests must remain unchanged. |
</threat_model>

<verification>
Plan-level automated proof:
- `node --check examples/scrypath_ecommerce/contrast-checker.mjs`
- `node examples/scrypath_ecommerce/contrast-checker.mjs --self-test`
- `cd examples/scrypath_ecommerce && CONTRAST_REPORT_DIR=test-results/contrast/phase132-token node contrast-checker.mjs`
- `cd scrypath_ops && mix assets.build && mix verify.opsui`
</verification>

<success_criteria>
- A11Y-TOKEN-01 source remediation is complete without component, layout, copy, motion, route, or behavior changes.
- `--ops-text-muted` is the named AA floor for all in-scope readable muted text.
- `--color-primary-strong` is scoped to text-bearing selected fills only.
- The fast checker remains a real AA gate and reports zero AA failures after the CSS changes.
- `DESIGN-TOKENS.md` records the exact Phase 132 floors and the AA-hard/AAA-advisory posture.
</success_criteria>

<output>
Create `.planning/phases/132-a11y-contrast-remediation-both-themes-hard-gate-g/132-01-SUMMARY.md` when done.
</output>
