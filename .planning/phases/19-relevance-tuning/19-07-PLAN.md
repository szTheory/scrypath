---
phase: 19-relevance-tuning
plan: 07
type: execute
wave: 6
depends_on:
  - "19-01"
  - "19-02"
  - "19-03"
  - "19-04"
  - "19-05"
  - "19-06"
files_modified:
  - guides/relevance-tuning.md
  - CHANGELOG.md
  - mix.exs
autonomous: false
requirements: []
tags:
  - relevance
  - docs
  - changelog
  - release
must_haves:
  truths:
    - "guides/relevance-tuning.md exists and covers all 5 declarative settings, the ranking-rules safety rail, verify/drift semantics, the two new mix tasks, the settings_merge example with three-source cascade, the one_way: true synonym sugar, and the :hot_apply_disabled v1.4 deferral note."
    - "guides/relevance-tuning.md is registered in mix.exs docs.extras so ExDoc picks it up."
    - "mix.exs cli.preferred_envs has new entries for scrypath.settings.diff and scrypath.settings.read (both :test)."
    - "CHANGELOG.md Unreleased section names all 8 TUNE-IDs (TUNE-01 through TUNE-08) as literal strings and includes the :hot_apply_disabled deliberate-deferral bullet, the :settings-from-backfill removal-of-inert-opt bullet, the Posture D normalization bullet, and the per-repo cascade bullet."
    - "After maintainer confirms CHANGELOG wording, the closing feat(19): ... Conventional Commit is created, triggering release-please to cut 0.4.0 -> 0.5.0 (D-24 Phase 18 ritual inherited)."
  artifacts:
    - path: "guides/relevance-tuning.md"
      provides: "relevance-tuning guide covering all 5 settings + safety rail + verify/drift + mix tasks + cascade + hot_apply deferral"
    - path: "CHANGELOG.md"
      provides: "Unreleased entry naming all 8 TUNE-IDs + deliberate-deferral + inert-opt-removal + Posture D"
    - path: "mix.exs"
      provides: "two new cli.preferred_envs entries + docs.extras entry for the new guide"
  key_links:
    - from: "mix.exs docs.extras"
      to: "guides/relevance-tuning.md"
      via: "extras list entry"
      pattern: "relevance-tuning.md"
    - from: "mix.exs cli.preferred_envs"
      to: "scrypath.settings.diff, scrypath.settings.read"
      via: "entries"
      pattern: "scrypath.settings.diff|scrypath.settings.read"
    - from: "CHANGELOG.md"
      to: "TUNE-01..TUNE-08"
      via: "Unreleased section"
      pattern: "TUNE-0[1-8]"
---

<objective>
Close Phase 19 with the user-facing deliverables: a complete guides/relevance-tuning.md covering every user-visible knob this phase ships; mix.exs additions to register the guide with ExDoc and the two new mix tasks with cli.preferred_envs; CHANGELOG.md Unreleased entry naming all 8 TUNE-IDs and the three phase-specific callouts (Posture D, inert-opt removal, per-repo cascade); and the closing feat(19): ... Conventional Commit that triggers release-please to cut 0.4.0 -> 0.5.0 (inherited from Phase 18).

Purpose: Docs, release notes, and the final commit are the phase's external face. Maintainer checkpoint after CHANGELOG wording is drafted (D-25 autonomous: false for 19-07) ensures the published entry matches intent. The closing commit ritual is mechanical but load-bearing — release-please consumes the feat(19): message and automates the rest.

Output: New guides/relevance-tuning.md, extended mix.exs, extended CHANGELOG.md, one closing feat(19): ... commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-relevance-tuning/19-CONTEXT.md
@.planning/phases/19-relevance-tuning/19-PATTERNS.md
@.planning/phases/19-relevance-tuning/19-01-PLAN.md
@.planning/phases/19-relevance-tuning/19-02-PLAN.md
@.planning/phases/19-relevance-tuning/19-03-PLAN.md
@.planning/phases/19-relevance-tuning/19-04-PLAN.md
@.planning/phases/19-relevance-tuning/19-05-PLAN.md
@.planning/phases/19-relevance-tuning/19-06-PLAN.md
@.planning/research/deep/RELEVANCE.md
@CHANGELOG.md
@mix.exs
@guides/operator-mix-tasks.md

<interfaces>
<!-- Current mix.exs cli.preferred_envs shape (L36-52 roughly) -->
def cli do
  [
    preferred_envs: [
      "verify.phase5": :test,
      "verify.release_parity": :test,
      credo: :test,
      dialyzer: :test
    ]
  ]
end

<!-- Current mix.exs docs.extras shape (likely around L74-86) -->
def docs do
  [
    main: "readme",
    extras: [
      "README.md",
      "guides/operator-mix-tasks.md"
    ]
  ]
end

<!-- Current CHANGELOG.md conventions (check head of file). Phase 18's entry is the direct precedent; Keep-a-Changelog style with Added / Changed / Removed subsections. -->

<!-- Guide content outline (D-23 plan 19-07 + 19-PATTERNS.md §"9.") -->
Section 1 - Intro - what is relevance tuning; what's in scope for v1.3
Section 2 - The five declarative settings - synonyms, typo_tolerance, ranking_rules, distinct_attribute, stop_words (one subsection each)
Section 3 - Synonym sugar - list-of-groups bidirectional AND one_way: true
Section 4 - Ranking-rules safety rail - the six defaults, compile-time warning + reindex-time error + ranking_rules_strict?: false opt-out
Section 5 - Verify / drift - the verify step; what blocks cutover; skip_settings_verification? runtime opt
Section 6 - mix scrypath.settings.diff - usage, exit codes, table / JSON
Section 7 - mix scrypath.settings.read - usage, when to reach for it vs diff
Section 8 - Settings-merge and cascade - :replace (default) vs :deep; three-source cascade (library-global -> per-repo -> per-call) with full example
Section 9 - Three-shape input note - atom-snake (canonical), atom-camelCase (v0.3.0 compat), string-camelCase (all three work permanently)
Section 10 - Hot-apply v1.4 deferral - single paragraph pointing at {:error, :hot_apply_disabled} stub
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create guides/relevance-tuning.md + register in mix.exs docs.extras + add cli.preferred_envs entries</name>
  <files>guides/relevance-tuning.md, mix.exs</files>
  <read_first>
    - guides/operator-mix-tasks.md (sibling ops guide; mirror section structure, tone, code-block conventions, cross-link idioms)
    - all files in guides/ (check full list for cross-link filenames — run `ls guides/`)
    - mix.exs (full file; confirm exact `docs` function shape and the exact list shape — atoms vs strings, with-title tuples vs plain paths)
    - .planning/research/deep/RELEVANCE.md (design source for guide body; do NOT cite "DeepMerge ~40% figure" per D-14)
    - .planning/phases/19-relevance-tuning/19-CONTEXT.md D-02 (ranking_rules_strict? placement rationale), D-03 (skip_settings_verification? placement rationale), D-10 + D-11 (cascade), D-12 (hand-rolled deep_merge), D-15..21 (Posture D), D-14 (DeepMerge ~40% prohibition)
    - .planning/phases/19-relevance-tuning/19-PATTERNS.md §"9." outline
  </read_first>
  <behavior>
    - Guide contains every section listed in the outline (sections 1-10).
    - Every code block uses Elixir syntax highlighting (triple-backtick elixir fences).
    - Every section has at least one concrete code example the reader can copy/paste.
    - Cross-links to sibling guides where relevant (at minimum: operator-mix-tasks.md; grep guides/ for other actual filenames).
    - mix.exs docs.extras includes "guides/relevance-tuning.md" (or the tuple form if that's the existing convention).
    - mix.exs cli.preferred_envs includes the two new entries "scrypath.settings.diff": :test and "scrypath.settings.read": :test.
    - Guide does NOT contain the phrase "DeepMerge ~40%" or any variant (D-14 prohibition).
  </behavior>
  <action>
    1. Read guides/operator-mix-tasks.md and list the guides/ directory to understand the sibling-guide conventions and available cross-link filenames.

    2. Create guides/relevance-tuning.md. The guide must include all 10 outline sections. Each section must have a heading and at least one concrete Elixir code example. Use this structural skeleton (copy the structure; fill the prose and code blocks):

       - Section 1: Introductory paragraph + one quick-example code block showing a schema declaring all 5 settings + one sentence about what happens on next Scrypath.reindex/2 (applied, verified, cutover-blocked-on-drift).

       - Section 2 "The five declarative settings": One subsection per setting (H3). Each subsection shows the declaration shape as an Elixir code block, notes the Meilisearch-native camelCase wire name (e.g., synonyms -> "synonyms", typo_tolerance -> "typoTolerance", ranking_rules -> "rankingRules", distinct_attribute -> "distinctAttribute", stop_words -> "stopWords"), and gives one usage example.

       - Section 3 "Synonym sugar: bidirectional + one_way": show all three input shapes (Meilisearch-native map, list-of-groups bidirectional, list-of-groups with one_way: true in the parent settings map) as Elixir code blocks. Explain that all three produce identical Meilisearch payloads.

       - Section 4 "Ranking-rules safety rail": list the six Meilisearch defaults VERBATIM (:words, :typo, :proximity, :attribute, :sort, :exactness). Explain the compile-time stderr warning (plan 19-01) and the reindex-time ArgumentError (plan 19-04). Show ranking_rules_strict?: false opt-out co-located in the settings map. Cite D-02's Ecto.Schema on_replace analog (one sentence — "modifier co-located with the declaration").

       - Section 5 "Verify and drift detection": explain the verify step runs between apply_settings-wait and backfill. On drift, cutover is BLOCKED and Scrypath.reindex/2 returns {:error, {:settings_drift, [...]}}. Show skip_settings_verification?: true runtime opt-out with the note that it emits Logger.warning and [:scrypath, :reindex, :verify_skipped] telemetry (D-03).

       - Section 6 "mix scrypath.settings.diff": show usage lines (plain, --json, --repo + --index-prefix). Document exit codes 0 / 2 / 1 explicitly. Show a sample three-column table output.

       - Section 7 "mix scrypath.settings.read": show usage. One paragraph: "when to use this vs diff".

       - Section 8 "Settings merge and per-repo cascade": show :replace default (matches v1.2 Map.merge/2; D-09) vs :deep (recursive). Show the three-source cascade (library-global config :scrypath, :defaults -> per-repo config :my_app, MyApp.Repo, scrypath: [...] -> per-call runtime opts) as both prose and an ASCII diagram. Explicit D-11 right-biased precedence. Note D-10: the per-repo source is NEW in v0.5.0.

       - Section 9 "Input shape flexibility": show all three shapes (atom-snake canonical, atom-camelCase v0.3.0-compat, string-camelCase) as separate Elixir code blocks. State that camelCase inputs emit a one-time informational stderr hint; no action required (D-18, D-19).

       - Section 10 "Hot-apply is deferred to v1.4": one paragraph pointing at Scrypath.Meilisearch.Settings.hot_apply/3 returning {:error, :hot_apply_disabled}. State that in v1.3, all settings flow through Scrypath.reindex/2.

       - Footer: "See also" cross-links to operator-mix-tasks.md and any other relevant sibling guides (grep guides/ for filenames first).

    3. In mix.exs:
       - Locate the docs function and add "guides/relevance-tuning.md" to the extras: list. Mirror the exact shape of the existing entries (bare string path or title-tuple — whichever is in use).
       - Locate the cli.preferred_envs list and append two entries:
         "scrypath.settings.diff": :test,
         "scrypath.settings.read": :test,

    4. Run mix docs locally to generate doc/ output; confirm doc/relevance-tuning.html (or equivalent) exists.

    5. Run mix compile --warnings-as-errors to verify mix.exs edits are syntactically valid.

    6. Self-check: grep the guide body for "~40%" and "DeepMerge.*40" — both must return zero matches (D-14 prohibition).
  </action>
  <verify>
    <automated>mix docs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - guides/relevance-tuning.md exists.
    - wc -l guides/relevance-tuning.md shows at least 100 lines (guide is substantive, not a stub).
    - grep -cE '^## ' guides/relevance-tuning.md returns at least 7 (major sections).
    - grep -c '~40%' guides/relevance-tuning.md returns 0 (D-14 prohibition).
    - grep -c 'DeepMerge.*40' guides/relevance-tuning.md returns 0 (D-14 prohibition).
    - grep -c '```elixir' guides/relevance-tuning.md returns at least 6 (concrete code examples).
    - grep -c 'ranking_rules_strict?: false' guides/relevance-tuning.md is at least 1 (opt-out documented).
    - grep -c 'skip_settings_verification?' guides/relevance-tuning.md is at least 1 (opt-out documented).
    - grep -c 'settings_merge' guides/relevance-tuning.md is at least 2 (cascade section).
    - grep -c 'hot_apply_disabled\|hot_apply/3' guides/relevance-tuning.md is at least 1 (deferral note).
    - grep -c 'one_way' guides/relevance-tuning.md is at least 1 (synonym sugar).
    - grep -c ':words.*:typo.*:proximity.*:attribute.*:sort.*:exactness' guides/relevance-tuning.md is at least 1 (six defaults listed verbatim in the safety-rail section).
    - grep -n 'relevance-tuning.md' mix.exs returns at least 1 match (docs.extras registration).
    - grep -n 'scrypath.settings.diff' mix.exs returns at least 1 match in the cli.preferred_envs context.
    - grep -n 'scrypath.settings.read' mix.exs returns at least 1 match in the cli.preferred_envs context.
    - mix docs exits 0 and produces doc/relevance-tuning.html (or appropriate ExDoc output).
    - mix compile --warnings-as-errors exits 0.
  </acceptance_criteria>
  <done>
    guides/relevance-tuning.md exists with all 10 outlined sections, concrete code examples, cross-links to sibling guides, and NO citation of the D-14-prohibited "DeepMerge ~40%" figure. mix.exs has the guide registered in docs.extras and the two new mix tasks registered in cli.preferred_envs. mix docs generates successfully.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Draft CHANGELOG.md Unreleased entry naming all 8 TUNE-IDs + phase-specific callouts</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file; examine the Phase 18 / v0.4.0 entry — the conventions for subsections, verb tense, how it references INFRA-01..04)
    - .planning/phases/19-relevance-tuning/19-CONTEXT.md D-22 (TUNE-ID naming tight enough for verifier), D-08 (inert opt removal phrasing), D-15..21 (Posture D), D-10 (per-repo cascade), "Claude's Discretion" (CHANGELOG wording for D-08 removal of inert plumbing)
  </read_first>
  <behavior>
    - CHANGELOG.md Unreleased section contains an Added subsection listing TUNE-01 through TUNE-08 explicitly, each as a literal string exactly matching /TUNE-0[1-8]/.
    - Every TUNE-ID appears as a literal string searchable with grep (e.g., TUNE-01, NOT "tune 1" or "TUNE 01" or "#TUNE-01").
    - Contains a bullet for the :hot_apply_disabled deliberate deferral (TUNE-03 stub).
    - Contains a bullet for Posture D three-shape normalize-on-entry.
    - Contains a bullet for the per-repo cascade source added to Scrypath.Config.resolve!/1 (D-10).
    - Contains a Removed subsection with one bullet: the inert :settings opt removed from @backfill_options, phrased as "removal of inert plumbing; no behavior change" per Claude's Discretion in CONTEXT.md.
  </behavior>
  <action>
    1. Read CHANGELOG.md head (first 100 lines) to understand the current Unreleased section shape and subsection conventions.

    2. Draft the v0.5.0-cut-target entry (under whatever section header the project uses — Unreleased or [Unreleased] or ## vNext). The entry has an Added subsection with eight TUNE-ID bullets plus four phase-callout bullets, and a Removed subsection with one bullet. Exact content:

       Added bullets:
       - TUNE-01: nested-subkey validation for the settings: schema key; recognized subkeys (synonyms, typo_tolerance, ranking_rules, distinct_attribute, stop_words); unknown subkeys pass through :__unrecognized__ bucket.
       - TUNE-02: synonyms accepts list-of-groups sugar with bidirectional expansion; one_way: true disables bidirectional expansion.
       - TUNE-03: settings flow through Scrypath.reindex/2's managed pipeline; Scrypath.Meilisearch.Settings.hot_apply/3 stub returns {:error, :hot_apply_disabled}; guarded impl deferred to v1.4.
       - TUNE-04: ranking-rules safety rail; compile-time :stderr warning + reindex-time ArgumentError when declared ranking_rules omits any of the six Meilisearch defaults (words, typo, proximity, attribute, sort, exactness); opt-out via ranking_rules_strict?: false in the same settings: map.
       - TUNE-05: post-apply settings verification step in Scrypath.Reindex.run/2; blocks cutover on drift; opt-out via skip_settings_verification?: true runtime opt (emits Logger.warning + [:scrypath, :reindex, :verify_skipped] telemetry); success path emits [:scrypath, :reindex, :settings_verified] telemetry.
       - TUNE-06: settings_merge: :replace | :deep runtime opt on Scrypath.reindex/2; default :replace (identical to v1.2 Map.merge/2); three-source config cascade (library-global -> per-repo -> per-call) with right-biased precedence.
       - TUNE-07: new mix task mix scrypath.settings.diff [Schema]; three-column declared-vs-applied output; exit 0 parity, 2 drift, 1 runtime error; --json flag.
       - TUNE-08: new mix task mix scrypath.settings.read [Schema]; pretty-prints current applied Meilisearch settings for debuggability.
       - Posture D three-shape input normalization: atom-snake (canonical), atom-camelCase (v0.3.0 compat), string-camelCase all produce identical Meilisearch wire payloads; no breaking changes.
       - Per-repo scrypath: config source added to Scrypath.Config.resolve!/1, keyed by {otp_app, repo_module}; enables per-environment relevance-tuning overrides without per-call threading.
       - New telemetry events: [:scrypath, :reindex, :settings_verified] and [:scrypath, :reindex, :verify_skipped] via Scrypath.Telemetry.span/3 envelope.
       - New guide guides/relevance-tuning.md covering all five settings, the safety rail, verify/drift, the two mix tasks, settings_merge cascade, and the hot-apply v1.4 deferral.

       Removed bullet:
       - Inert :settings opt removed from @backfill_options. The opt was plumbing that looked like it did something but did not (backfill never called Settings.apply/3). Removal of inert plumbing, not a behavior change — no existing caller relied on it.

    3. Insert the drafted entry into CHANGELOG.md under the appropriate section header, preserving all prior Unreleased bullets that came from Phase 18 or earlier.
  </action>
  <verify>
    <automated>grep -cE 'TUNE-0[1-8]' CHANGELOG.md && grep -c 'hot_apply' CHANGELOG.md && grep -c 'Posture D\|three-shape' CHANGELOG.md && grep -c 'per-repo' CHANGELOG.md && grep -ci 'inert' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE 'TUNE-0[1-8]' CHANGELOG.md returns at least 8 (all 8 IDs present as literal strings).
    - grep -c 'hot_apply_disabled\|hot_apply' CHANGELOG.md is at least 1.
    - grep -c 'Posture D\|three-shape' CHANGELOG.md is at least 1.
    - grep -c 'per-repo' CHANGELOG.md is at least 1.
    - grep -ci 'inert' CHANGELOG.md is at least 1.
    - grep -c '@backfill_options' CHANGELOG.md is at least 1 (the Removed bullet mentions the schema by name).
    - grep -c ':settings_verified\|:verify_skipped' CHANGELOG.md is at least 1 (new telemetry events named).
    - grep -c 'words.*typo.*proximity.*attribute.*sort.*exactness' CHANGELOG.md is at least 1 (six defaults in TUNE-04 bullet).
    - grep -c 'settings_merge' CHANGELOG.md is at least 1.
  </acceptance_criteria>
  <done>
    CHANGELOG.md Unreleased section contains all 8 TUNE-IDs as literal strings, all four phase-specific callouts (hot_apply deferral, Posture D, per-repo cascade, telemetry), the Removed bullet for inert :settings opt in @backfill_options, and the new-guide reference. No prior entries disturbed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer checkpoint — confirm CHANGELOG wording before commit (D-25)</name>
  <files>CHANGELOG.md, guides/relevance-tuning.md</files>
  <what-built>
    guides/relevance-tuning.md (Task 1) and CHANGELOG.md Unreleased entry (Task 2) are drafted. The guide has all 10 outline sections, concrete code examples, cross-links, and respects the D-14 prohibition. The CHANGELOG names all 8 TUNE-IDs and the four phase-specific callouts (hot_apply, Posture D, per-repo cascade, telemetry) plus the Removed bullet for the inert :settings opt.
  </what-built>
  <how-to-verify>
    1. Open guides/relevance-tuning.md in a preview tool (or run mix docs and open doc/relevance-tuning.html in a browser).
    2. Scan each of the 10 sections for technical accuracy (especially the six ranking-rules defaults, the three synonym input shapes, the three-source cascade example).
    3. Open CHANGELOG.md and read the Unreleased entry top-to-bottom. Check:
       - Every TUNE-01 through TUNE-08 bullet matches the user's expected summary of that requirement.
       - The :hot_apply_disabled deferral is phrased clearly as "deferred to v1.4", not "not implemented" or "broken".
       - The Removed bullet for @backfill_options :settings is phrased as "removal of inert plumbing, not a behavior change" per D-08.
       - No typos in TUNE-ID numbering.
    4. Confirm mix docs and mix compile --warnings-as-errors both pass locally.
    5. Type "approved" if the wording is good; otherwise describe specific edits and Claude will revise.
  </how-to-verify>
  <action>
    This is a human-verification checkpoint. Claude's role here is to pause execution after Tasks 1 and 2 have completed and wait for the maintainer to follow the <how-to-verify> steps and respond with "approved" or revision instructions. No code changes. See <how-to-verify> for the full review protocol and <resume-signal> for how the maintainer signals completion.
  </action>
  <verify>
    <automated>mix docs && mix compile --warnings-as-errors && grep -cE 'TUNE-0[1-8]' CHANGELOG.md</automated>
  </verify>
  <done>
    Maintainer has typed "approved" (or Claude has applied requested revisions and returned for re-approval). guides/relevance-tuning.md and CHANGELOG.md wording is signed off and ready for the closing commit in Task 4.
  </done>
  <resume-signal>Type "approved" or describe desired edits.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create the closing feat(19): commit triggering release-please (D-24 inherited from Phase 18)</name>
  <files>CHANGELOG.md, guides/relevance-tuning.md, mix.exs, (all Phase 19 library + test files touched in 19-01..06)</files>
  <read_first>
    - Run git log --oneline -- <Phase 18 files> to review Phase 18's closing feat(18): commit message format.
    - .planning/phases/19-relevance-tuning/19-CONTEXT.md D-24 (release-please trigger ritual) and D-25 (autonomous: false for 19-07 — this task runs AFTER Task 3's approval).
    - .planning/phases/18-release-parity-gate-node-20-ci-cleanup/18-CONTEXT.md D-22 for the exact closing-commit ritual.
  </read_first>
  <behavior>
    - git status shows a clean working tree except for the Phase 19 changes awaiting commit.
    - The commit message is a single Conventional Commit with subject feat(19): ... and a body listing the TUNE-IDs and the user-visible scope.
    - After the commit lands on main, release-please opens a release PR proposing 0.4.0 -> 0.5.0 (this is external behavior; the plan does not need to verify the PR opens — just that the commit is well-formed).
    - The commit does NOT include --no-verify or any hook bypass.
    - If mix verify.workspace_clean is wired via Phase 18's pre-commit or CI hook, the commit passes that gate.
  </behavior>
  <action>
    1. Run git status to confirm the working tree contains all Phase 19 changes (from 19-01 through 19-07 Tasks 1-2) and nothing unrelated.

    2. Run git diff --stat to review the full change set size. For a v0.5.0-cut phase this is expected to be in the low-thousands of LOC across ~15 files (lib/, test/, guides/, mix.exs, CHANGELOG.md).

    3. Run mix test --exclude external_meilisearch and confirm it exits 0 (all Phase 19 unit tests green).

    4. Run mix verify.workspace_clean (Phase 18 gate) and confirm it exits 0.

    5. Stage all Phase 19 files explicitly by name (do NOT use git add -A or git add .):
       - lib/scrypath/options.ex
       - lib/scrypath/meilisearch/settings.ex
       - lib/scrypath/meilisearch/client.ex
       - lib/scrypath/reindex.ex
       - lib/scrypath/config.ex
       - lib/mix/tasks/scrypath.settings.diff.ex
       - lib/mix/tasks/scrypath.settings.read.ex
       - guides/relevance-tuning.md
       - CHANGELOG.md
       - mix.exs
       - test/scrypath/options_test.exs
       - test/scrypath/meilisearch/settings_test.exs
       - test/scrypath/meilisearch/client_test.exs
       - test/scrypath/reindex_test.exs
       - test/scrypath/config_test.exs
       - test/mix/tasks/scrypath_settings_diff_test.exs
       - test/mix/tasks/scrypath_settings_read_test.exs

    6. Create the commit using a heredoc (per the repo's git-commit conventions, matching Phase 18's closing commit):

       git commit -m "$(cat <<'EOF'
       feat(19): add declarative relevance tuning (TUNE-01..TUNE-08)

       Phoenix devs declare synonyms, typo tolerance, ranking rules, distinct
       attribute, and stop words on their schema and see them applied safely
       through the managed reindex pipeline with drift detection and a ranking-
       rules safety rail.

       TUNE-01: nested settings subkey validation + :__unrecognized__ passthrough
       TUNE-02: synonyms list-of-groups sugar + one_way: true
       TUNE-03: managed pipeline only; hot_apply/3 stub returns :hot_apply_disabled
       TUNE-04: ranking-rules safety rail (compile-time + reindex-time)
       TUNE-05: post-apply drift verification blocks cutover
       TUNE-06: settings_merge :replace | :deep + three-source config cascade
       TUNE-07: mix scrypath.settings.diff (exit codes 0/2/1)
       TUNE-08: mix scrypath.settings.read

       Also:
       - Posture D three-shape input normalization (no breaking changes)
       - Per-repo scrypath: config source in Scrypath.Config.resolve!/1
       - [:scrypath, :reindex, :settings_verified] and [:scrypath, :reindex, :verify_skipped] telemetry
       - guides/relevance-tuning.md
       - Removed inert :settings opt from @backfill_options
       EOF
       )"

    7. Run git log -1 --format=%B to verify the commit message formatted correctly.

    8. Do NOT push. The push to main is the maintainer's call (standard Scrypath release ritual).
  </action>
  <verify>
    <automated>git log -1 --format=%B | grep -cE 'TUNE-0[1-8]' && mix test --exclude external_meilisearch</automated>
  </verify>
  <acceptance_criteria>
    - git log -1 --format=%s matches the pattern ^feat\(19\): (Conventional Commit with phase-scoped feat:).
    - git log -1 --format=%B | grep -cE 'TUNE-0[1-8]' returns at least 8.
    - git log -1 --format=%B | grep -c 'Posture D\|three-shape' is at least 1.
    - git log -1 --format=%B | grep -c 'backfill_options\|inert' is at least 1.
    - git status --porcelain produces no output (clean working tree post-commit).
    - git show HEAD --stat lists all expected Phase 19 files (at least: lib/scrypath/options.ex, lib/scrypath/meilisearch/settings.ex, lib/scrypath/meilisearch/client.ex, lib/scrypath/reindex.ex, lib/scrypath/config.ex, lib/mix/tasks/scrypath.settings.diff.ex, lib/mix/tasks/scrypath.settings.read.ex, guides/relevance-tuning.md, CHANGELOG.md, mix.exs, and the 7 test files).
    - mix test --exclude external_meilisearch exits 0 post-commit.
    - mix verify.workspace_clean exits 0 post-commit.
  </acceptance_criteria>
  <done>
    A single commit on the current branch has subject feat(19): add declarative relevance tuning (TUNE-01..TUNE-08), a body naming all eight TUNE-IDs and the four phase-specific callouts, and a clean working tree post-commit. The commit is ready to push to main; the push triggers release-please to open a 0.5.0 PR (external; not verified by this plan).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> CHANGELOG wording | Human-readable release notes; no machine-readable parser downstream beyond release-please's version-bump detection. |
| feat(19): commit -> release-please -> Hex publish | Phase 18 gates (workspace_clean, release_parity) protect this path. The commit itself adds no new privileged surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07-01 | Information Disclosure | CHANGELOG.md content | accept | Public release notes. Content is user-facing feature summary; no secrets. No URLs to internal systems. |
| T-19-07-02 | Tampering | closing commit message | mitigate | Conventional Commit format is mechanically parsed by release-please. Task 4 action step 6 pins the exact message format; Task 3 (human checkpoint) confirms the wording. No --no-verify flag. Phase 18's workspace_clean gate verifies no stray files before the commit lands. |
| T-19-07-03 | Elevation of Privilege | release-please automation | accept | Inherited from Phase 18 — release-please is an existing workflow with an audited permissions surface. No new permission grants in this plan. |
| T-19-07-04 | Information Disclosure | guides/relevance-tuning.md content | accept | Public documentation; no secrets. Code examples use placeholder values (MyApp.Blog.Post, http://localhost:7700) that are already in existing guides. |

No new security-relevant code paths. Plan 19-07 is documentation + release-notes + the closing commit ritual.
</threat_model>

<verification>
- mix test --exclude external_meilisearch exits 0.
- mix verify.workspace_clean exits 0.
- mix docs exits 0 and the new guide appears in ExDoc output.
- mix compile --warnings-as-errors exits 0.
- git log -1 subject starts with feat(19):
- grep -cE 'TUNE-0[1-8]' CHANGELOG.md returns at least 8.
- grep -cE 'TUNE-0[1-8]' in the HEAD commit body returns at least 8.
</verification>

<success_criteria>
- guides/relevance-tuning.md covers all 10 outline sections with concrete code examples and cross-links.
- mix.exs docs.extras registers the guide; cli.preferred_envs registers the two new mix tasks.
- CHANGELOG.md Unreleased section names all 8 TUNE-IDs plus the four phase-specific callouts and the Removed bullet.
- Maintainer checkpoint (Task 3) gates the commit on human-verified wording.
- Closing feat(19): commit is well-formed and triggers release-please to cut v0.5.0 (external verification not in-scope for this plan).
- All Phase 19 deliverables (from 19-01..06) are contained in the single closing commit.
</success_criteria>

<output>
After completion, create .planning/phases/19-relevance-tuning/19-07-SUMMARY.md per the summary template.
</output>
