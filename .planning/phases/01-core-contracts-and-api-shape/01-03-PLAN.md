---
phase: "01-core-contracts-and-api-shape"
plan: "01-03"
type: "docs"
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - README.md
  - ARCHITECTURE.md
  - lib/scrypath.ex
  - lib/scrypath/schema.ex
  - lib/scrypath/projection.ex
autonomous: true
requirements:
  - SCMA-01
  - SCMA-02
  - SCMA-03
  - BACK-02
must_haves:
  truths:
    - "Top-level docs state that Scrypath v1 is Meilisearch-first and that the backend seam is internal."
    - "Docs explain the schema declaration contract, projection hook precedence, and runtime reflection helpers with exact API names."
    - "Docs acknowledge operational truth: projection changes and backend settings can require reindex work in later phases."
  artifacts:
    - "README.md"
    - "ARCHITECTURE.md"
    - "lib/scrypath.ex"
    - "lib/scrypath/schema.ex"
    - "lib/scrypath/projection.ex"
  key_links:
    - "README.md -> `## Schema Declaration`, `## Projection`, and `## Product Boundary` sections"
    - "ARCHITECTURE.md -> `## Public Surface`, `## Internal Backend Seam`, and `## Deferred Work` sections"
    - "lib/scrypath*.ex module docs mirror the README terminology"
---

<objective>
Document the Phase 1 product boundary and API contracts so future implementation and external adopters both understand what Scrypath guarantees now and what remains deferred to later phases.
</objective>

<tasks>
  <task id="T1" type="docs">
    <read_first>
      - README.md
      - .planning/PROJECT.md
      - .planning/ROADMAP.md
      - .planning/REQUIREMENTS.md
      - .planning/phases/01-core-contracts-and-api-shape/01-CONTEXT.md
      - .planning/phases/01-core-contracts-and-api-shape/01-RESEARCH.md
      - prompts/search-lib-use-cases-deep-research.md
      - prompts/scrypath-brand-book.md
    </read_first>
    <action>
      Create or replace `README.md` with Phase 1 product-boundary documentation. Include exact top-level sections `# Scrypath`, `## Product Boundary`, `## Schema Declaration`, `## Projection`, `## Runtime Reflection`, and `## Roadmap`. In `## Product Boundary`, state that Scrypath v1 publicly targets Meilisearch first, keeps the backend seam internal, and does not promise public multi-backend parity. In `## Schema Declaration`, include a code sample using `use Scrypath, fields: [:title, :body], filterable: [:status], sortable: [:inserted_at]`. In `## Projection`, document that `search_document/1` takes precedence over declarative fields and that association data requires explicit loading. In `## Runtime Reflection`, list `Scrypath.schema_config/1`, `Scrypath.schema_fields/1`, `Scrypath.document_source/1`, and `Scrypath.document_id_field/1`.
    </action>
    <acceptance_criteria>
      - `README.md` contains `# Scrypath`
      - `README.md` contains `## Product Boundary`
      - `README.md` contains `Meilisearch`
      - `README.md` contains `backend seam is internal`
      - `README.md` contains `use Scrypath,`
      - `README.md` contains `search_document/1`
      - `README.md` contains `Scrypath.schema_config/1`
    </acceptance_criteria>
    <verify>
      rg -n "^## Product Boundary$|^## Schema Declaration$|search_document/1|Scrypath\\.schema_config/1" README.md
    </verify>
  </task>

  <task id="T2" type="docs">
    <read_first>
      - ARCHITECTURE.md
      - README.md
      - lib/scrypath.ex
      - lib/scrypath/schema.ex
      - lib/scrypath/projection.ex
      - lib/scrypath/config.ex
      - lib/scrypath/backend.ex
      - .planning/phases/01-core-contracts-and-api-shape/01-CONTEXT.md
      - .planning/phases/01-core-contracts-and-api-shape/01-RESEARCH.md
    </read_first>
    <action>
      Create `ARCHITECTURE.md` with exact sections `# Scrypath Architecture`, `## Public Surface`, `## Projection Flow`, `## Internal Backend Seam`, `## Runtime Configuration`, and `## Deferred Work`. In `## Public Surface`, name the public modules `Scrypath`, `Scrypath.Schema`, and `Scrypath.Projection`. In `## Internal Backend Seam`, describe `Scrypath.Backend` as internal and list callback names `name/0`, `index_name/2`, `upsert_documents/3`, `delete_documents/3`, and `search/3`. In `## Deferred Work`, explicitly list Meilisearch sync execution, Oban integration, query execution, and managed reindex orchestration as later-phase work.
    </action>
    <acceptance_criteria>
      - `ARCHITECTURE.md` contains `# Scrypath Architecture`
      - `ARCHITECTURE.md` contains `## Public Surface`
      - `ARCHITECTURE.md` contains `## Internal Backend Seam`
      - `ARCHITECTURE.md` contains `upsert_documents/3`
      - `ARCHITECTURE.md` contains `delete_documents/3`
      - `ARCHITECTURE.md` contains `managed reindex orchestration`
    </acceptance_criteria>
    <verify>
      rg -n "^## Public Surface$|^## Internal Backend Seam$|upsert_documents/3|managed reindex orchestration" ARCHITECTURE.md
    </verify>
  </task>

  <task id="T3" type="docs">
    <read_first>
      - lib/scrypath.ex
      - lib/scrypath/schema.ex
      - lib/scrypath/projection.ex
      - README.md
      - ARCHITECTURE.md
      - .planning/phases/01-core-contracts-and-api-shape/01-RESEARCH.md
    </read_first>
    <action>
      Add module docs that mirror the README and architecture language. In `lib/scrypath.ex`, add `@moduledoc` describing the top-level runtime reflection API and naming `schema_config/1`, `schema_fields/1`, `document_source/1`, and `document_id_field/1`. In `lib/scrypath/schema.ex`, add `@moduledoc` documenting the metadata-only macro contract and the `__scrypath__/1` keys. In `lib/scrypath/projection.ex`, add `@moduledoc` describing default field projection, `search_document/1` precedence, and the explicit preload requirement for association-derived data. Include at least one doctest-ready code block in either `lib/scrypath.ex` or `lib/scrypath/schema.ex`.
    </action>
    <acceptance_criteria>
      - `lib/scrypath.ex` contains `@moduledoc`
      - `lib/scrypath.ex` contains `schema_config/1`
      - `lib/scrypath/schema.ex` contains `__scrypath__/1`
      - `lib/scrypath/projection.ex` contains `search_document/1`
      - `lib/scrypath/projection.ex` contains `explicit preload`
    </acceptance_criteria>
    <verify>
      rg -n "@moduledoc|schema_config/1|__scrypath__/1|explicit preload" lib/scrypath.ex lib/scrypath/schema.ex lib/scrypath/projection.ex
    </verify>
  </task>
</tasks>

<verification>
  <step>Run `rg -n "^## Product Boundary$|^## Schema Declaration$|^## Internal Backend Seam$" README.md ARCHITECTURE.md`.</step>
  <step>Run `mix docs` if ExDoc is added during execution; otherwise verify module docs with `rg -n "@moduledoc" lib/scrypath.ex lib/scrypath/schema.ex lib/scrypath/projection.ex`.</step>
</verification>

<success_criteria>
  <criterion>The README explains what Scrypath is, what it is not, and why v1 is Meilisearch-first.</criterion>
  <criterion>The architecture doc separates public APIs from the internal backend seam and deferred work.</criterion>
  <criterion>Module docs reinforce the same projection and reflection contracts implemented in code.</criterion>
</success_criteria>

<threat_model>
  <trust_boundaries>
    <boundary>Public documentation shapes user expectations about stability, backend support, and operational semantics.</boundary>
    <boundary>Module docs connect runtime API names to the implementation surface used by later phases.</boundary>
  </trust_boundaries>
  <stride_register>
    <threat>
      <threat_id>T01-07</threat_id>
      <category>repudiation</category>
      <component>public docs</component>
      <severity>medium</severity>
      <disposition>mitigate</disposition>
      <mitigation_plan>State the product boundary, deferred work, and internal adapter seam explicitly in README and ARCHITECTURE.md so later changes can be audited against written contracts.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T01-08</threat_id>
      <category>information_disclosure</category>
      <component>projection documentation</component>
      <severity>low</severity>
      <disposition>mitigate</disposition>
      <mitigation_plan>Document that only declared fields or explicit `search_document/1` output become indexed data and that association-derived fields require deliberate loading.</mitigation_plan>
    </threat>
  </stride_register>
</threat_model>
