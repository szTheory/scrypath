---
phase: "01-core-contracts-and-api-shape"
plan: "01-01"
type: "feature"
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .formatter.exs
  - lib/scrypath.ex
  - lib/scrypath/schema.ex
  - lib/scrypath/options.ex
  - test/test_helper.exs
  - test/scrypath/schema_test.exs
  - test/support/searchable_post.ex
autonomous: true
requirements:
  - SCMA-01
  - SCMA-03
  - BACK-02
must_haves:
  truths:
    - "`use Scrypath` remains metadata-only and does not generate runtime verbs on schema modules."
    - "Schema modules expose `__scrypath__/1` with stable keys for config, fields, filterable, sortable, document_id, and document_source."
    - "Option validation is centralized in `Scrypath.Options` and rejects unknown or invalid declaration keys."
    - "The initial Mix project pins Elixir support at `~> 1.17` and includes `:ecto` plus `:nimble_options`."
  artifacts:
    - "mix.exs"
    - ".formatter.exs"
    - "lib/scrypath.ex"
    - "lib/scrypath/schema.ex"
    - "lib/scrypath/options.ex"
    - "test/test_helper.exs"
    - "test/scrypath/schema_test.exs"
    - "test/support/searchable_post.ex"
  key_links:
    - "mix.exs -> Scrypath.MixProject.deps/0 declares `{:ecto, \"~> 3.13\"}` and `{:nimble_options, \"~> 1.1\"}`"
    - "lib/scrypath/schema.ex -> `__using__/1` stores normalized metadata and defines `__scrypath__/1`"
    - "lib/scrypath/options.ex -> `validate_schema_options!/1` enforces the declaration contract"
    - "test/test_helper.exs -> starts ExUnit and requires `test/support/**/*.ex` files so shared schema fixtures compile during targeted test runs"
---

<objective>
Establish the initial Elixir library scaffold and lock the searchable schema declaration contract around a small `use Scrypath` macro, centralized option validation, and inspectable runtime metadata.
</objective>

<tasks>
  <task id="T1" type="feature">
    <read_first>
      - mix.exs
      - .formatter.exs
      - lib/scrypath.ex
      - .planning/PROJECT.md
      - .planning/ROADMAP.md
      - .planning/REQUIREMENTS.md
      - .planning/phases/01-core-contracts-and-api-shape/01-CONTEXT.md
      - .planning/phases/01-core-contracts-and-api-shape/01-RESEARCH.md
      - .planning/phases/01-core-contracts-and-api-shape/01-PATTERNS.md
      - prompts/elixir-opensource-libs-best-practices-deep-research.md
      - prompts/elixir-best-practices-deep-research.md
      - prompts/ecto-best-practices-deep-research.md
    </read_first>
    <action>
      Create the initial Mix project files for the library. Add `mix.exs` with `defmodule Scrypath.MixProject`, `app: :scrypath`, `version: "0.1.0"`, `elixir: "~> 1.17"`, `start_permanent: Mix.env() == :prod`, `deps: [{:ecto, "~> 3.13"}, {:nimble_options, "~> 1.1"}]`, and `package`/`docs` metadata placeholders that name Scrypath as an Ecto-native search indexing library. Add `.formatter.exs` configured for `inputs: ["{mix,.formatter}.exs", "{config,lib,test}/**/*.{ex,exs}"]`. Create `test/test_helper.exs` that starts ExUnit and requires `test/support/**/*.ex` so targeted test runs load shared schema fixtures. Create `lib/scrypath.ex` with public helpers `schema_config/1`, `schema_fields/1`, `document_source/1`, and `document_id_field/1` that delegate to schema reflection without introducing sync or query behavior.
    </action>
    <acceptance_criteria>
      - `mix.exs` contains `defmodule Scrypath.MixProject do`
      - `mix.exs` contains `app: :scrypath`
      - `mix.exs` contains `elixir: "~> 1.17"`
      - `mix.exs` contains `{:ecto, "~> 3.13"}`
      - `mix.exs` contains `{:nimble_options, "~> 1.1"}`
      - `test/test_helper.exs` contains `ExUnit.start()`
      - `test/test_helper.exs` contains `Path.wildcard("test/support/**/*.ex")`
      - `lib/scrypath.ex` contains `def schema_config(schema_module) do`
      - `lib/scrypath.ex` contains `def schema_fields(schema_module) do`
      - `lib/scrypath.ex` contains `def document_source(schema_module) do`
      - `.formatter.exs` contains `"{config,lib,test}/**/*.{ex,exs}"`
    </acceptance_criteria>
    <verify>
      mix format --check-formatted
    </verify>
  </task>

  <task id="T2" type="feature">
    <read_first>
      - lib/scrypath/schema.ex
      - lib/scrypath/options.ex
      - lib/scrypath.ex
      - .planning/phases/01-core-contracts-and-api-shape/01-CONTEXT.md
      - .planning/phases/01-core-contracts-and-api-shape/01-RESEARCH.md
      - prompts/elixir-best-practices-deep-research.md
      - prompts/ecto-best-practices-deep-research.md
    </read_first>
    <action>
      Implement the schema declaration contract in `lib/scrypath/schema.ex` and `lib/scrypath/options.ex`. Define `Scrypath.Options.validate_schema_options!/1` using `NimbleOptions` with exact accepted keys `fields`, `filterable`, `sortable`, `document_id`, `index_prefix`, and `backend`; require `fields` as a non-empty list of atoms; default `document_id` to `:id`; default `document_source` to `:fields`; and reject unknown keys. In `Scrypath.Schema.__using__/1`, validate options, register a persisted module attribute for the normalized config, and define `__scrypath__/1` clauses for `:config`, `:fields`, `:filterable`, `:sortable`, `:document_id`, `:document_source`, and `:backend`. Expose the macro from `Scrypath.__using__/1` by delegating to `use Scrypath.Schema`.
    </action>
    <acceptance_criteria>
      - `lib/scrypath/options.ex` contains `NimbleOptions`
      - `lib/scrypath/options.ex` contains `def validate_schema_options!(opts) do`
      - `lib/scrypath/options.ex` contains `fields: [`
      - `lib/scrypath/options.ex` contains `document_id: [`
      - `lib/scrypath/schema.ex` contains `defmacro __using__(opts) do`
      - `lib/scrypath/schema.ex` contains `def __scrypath__(:config), do:`
      - `lib/scrypath/schema.ex` contains `def __scrypath__(:fields), do:`
      - `lib/scrypath/schema.ex` contains `def __scrypath__(:document_source), do:`
      - `lib/scrypath/schema.ex` does not contain `.search(`
      - `lib/scrypath/schema.ex` does not contain `.reindex(`
    </acceptance_criteria>
    <verify>
      rg -n "def __scrypath__\\(:config\\)|def __scrypath__\\(:document_source\\)|def validate_schema_options!" lib/scrypath/schema.ex lib/scrypath/options.ex
    </verify>
  </task>

  <task id="T3" type="test">
    <read_first>
      - test/scrypath/schema_test.exs
      - test/support/searchable_post.ex
      - lib/scrypath.ex
      - lib/scrypath/schema.ex
      - lib/scrypath/options.ex
      - .planning/phases/01-core-contracts-and-api-shape/01-RESEARCH.md
      - .planning/phases/01-core-contracts-and-api-shape/01-PATTERNS.md
    </read_first>
    <action>
      Add contract tests for schema declaration and reflection. Create `test/support/searchable_post.ex` defining a minimal `SearchablePost` schema that uses `Scrypath` with `fields: [:title, :body]`, `filterable: [:status]`, and `sortable: [:inserted_at]`. In `test/scrypath/schema_test.exs`, add assertions that `SearchablePost.__scrypath__(:config)` returns normalized metadata, `SearchablePost.__scrypath__(:fields)` returns `[:title, :body]`, `SearchablePost.__scrypath__(:document_id)` returns `:id`, and invalid options raise `ArgumentError` with `unknown options` or validation failure text. Add one assertion proving no generated runtime API exists by checking `function_exported?(SearchablePost, :search, 2) == false`.
    </action>
    <acceptance_criteria>
      - `test/support/searchable_post.ex` contains `use Scrypath,`
      - `test/support/searchable_post.ex` contains `fields: [:title, :body]`
      - `test/scrypath/schema_test.exs` contains `__scrypath__(:config)`
      - `test/scrypath/schema_test.exs` contains `__scrypath__(:fields)`
      - `test/scrypath/schema_test.exs` contains `function_exported?(SearchablePost, :search, 2) == false`
      - `mix test test/scrypath/schema_test.exs` exits 0
    </acceptance_criteria>
    <verify>
      mix test test/scrypath/schema_test.exs
    </verify>
  </task>
</tasks>

<verification>
  <step>Run `mix format --check-formatted`.</step>
  <step>Run `mix test test/scrypath/schema_test.exs`.</step>
  <step>Run `rg -n "def __scrypath__\\(:config\\)|def __scrypath__\\(:fields\\)|def validate_schema_options!" lib test` to confirm the schema contract landed in code and tests.</step>
</verification>

<success_criteria>
  <criterion>`use Scrypath` can be applied to an Ecto schema with explicit `fields`, `filterable`, and `sortable` options.</criterion>
  <criterion>Schema metadata is inspectable through `__scrypath__/1` and public `Scrypath` helper functions.</criterion>
  <criterion>The macro remains declaration-only; no schema-specific runtime verbs are generated.</criterion>
</success_criteria>

<threat_model>
  <trust_boundaries>
    <boundary>Schema modules supply compile-time declaration data to Scrypath macros.</boundary>
    <boundary>Validated metadata crosses from macro expansion into runtime reflection helpers.</boundary>
    <boundary>Library consumers depend on reflection output as a stable contract for later sync and query flows.</boundary>
  </trust_boundaries>
  <stride_register>
    <threat>
      <threat_id>T01-01</threat_id>
      <category>tampering</category>
      <component>schema option validation</component>
      <severity>medium</severity>
      <disposition>mitigate</disposition>
      <mitigation_plan>Centralize declaration validation in `Scrypath.Options.validate_schema_options!/1` with an allowlist schema and explicit rejection of unknown keys.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T01-02</threat_id>
      <category>spoofing</category>
      <component>document identity metadata</component>
      <severity>medium</severity>
      <disposition>mitigate</disposition>
      <mitigation_plan>Default `document_id` to the schema primary key field and expose the normalized value through `__scrypath__(:document_id)` so later phases do not infer identity from unvalidated input.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T01-03</threat_id>
      <category>repudiation</category>
      <component>public API boundary</component>
      <severity>low</severity>
      <disposition>mitigate</disposition>
      <mitigation_plan>Keep the macro metadata-only and cover the absence of generated runtime verbs in tests so future work cannot silently add hidden sync behavior.</mitigation_plan>
    </threat>
  </stride_register>
</threat_model>
