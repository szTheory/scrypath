---
phase: "01-core-contracts-and-api-shape"
plan: "01-02"
type: "feature"
wave: 2
depends_on:
  - "01-01"
files_modified:
  - lib/scrypath/projection.ex
  - lib/scrypath/config.ex
  - lib/scrypath/backend.ex
  - lib/scrypath/document.ex
  - test/scrypath/projection_test.exs
  - test/scrypath/backend_test.exs
  - test/support/fake_backend.ex
autonomous: true
requirements:
  - SCMA-02
  - SCMA-03
  - BACK-02
must_haves:
  truths:
    - "Projection supports a default field-list path plus an explicit `search_document/1` override with documented precedence."
    - "Association-derived projection stays explicit; Scrypath does not preload associations or reach through unloaded data implicitly."
    - "The internal backend seam is a narrow behavior, not a public adapter registration API."
    - "Runtime config normalization prefers explicit options and only falls back to application defaults as convenience."
  artifacts:
    - "lib/scrypath/projection.ex"
    - "lib/scrypath/config.ex"
    - "lib/scrypath/backend.ex"
    - "lib/scrypath/document.ex"
    - "test/scrypath/projection_test.exs"
    - "test/scrypath/backend_test.exs"
    - "test/support/fake_backend.ex"
  key_links:
    - "lib/scrypath/projection.ex -> `document/2` and `document_source/1` implement hook precedence"
    - "lib/scrypath/backend.ex -> behavior callbacks define the internal backend seam"
    - "lib/scrypath/config.ex -> `resolve!/1` normalizes explicit opts before app config defaults"
---

<objective>
Define the projection runtime, explicit runtime configuration rules, and a narrow internal backend behavior so later Meilisearch and sync work extends stable foundations instead of redesigning them.
</objective>

<tasks>
  <task id="T1" type="feature">
    <read_first>
      - lib/scrypath/projection.ex
      - lib/scrypath/document.ex
      - lib/scrypath/schema.ex
      - lib/scrypath.ex
      - .planning/phases/01-core-contracts-and-api-shape/01-CONTEXT.md
      - .planning/phases/01-core-contracts-and-api-shape/01-RESEARCH.md
      - prompts/search-lib-use-cases-deep-research.md
      - prompts/ecto-best-practices-deep-research.md
    </read_first>
    <action>
      Implement the projection contract in `lib/scrypath/projection.ex` and `lib/scrypath/document.ex`. Add `Scrypath.Projection.document(schema_module, source_record)` that checks `function_exported?(schema_module, :search_document, 1)` first and uses that hook when present; otherwise project the exact atom list from `schema_module.__scrypath__(:fields)` into a map plus the configured document id. Add `Scrypath.Projection.document_source/1` returning `:custom` when the hook exists and `:fields` otherwise. Create `Scrypath.Document` as a small struct with keys `:id`, `:data`, and `:source`, and make projection return that struct. If a configured field is missing on the source struct or map, raise `ArgumentError` naming the missing field so association-based projection stays explicit.
    </action>
    <acceptance_criteria>
      - `lib/scrypath/projection.ex` contains `def document(schema_module, source_record) do`
      - `lib/scrypath/projection.ex` contains `function_exported?(schema_module, :search_document, 1)`
      - `lib/scrypath/projection.ex` contains `def document_source(schema_module) do`
      - `lib/scrypath/document.ex` contains `defstruct [:id, :data, :source]`
      - `lib/scrypath/projection.ex` contains `raise ArgumentError`
      - `lib/scrypath/projection.ex` contains `schema_module.__scrypath__(:fields)`
    </acceptance_criteria>
    <verify>
      rg -n "def document\\(|function_exported\\?\\(schema_module, :search_document, 1\\)|defstruct \\[:id, :data, :source\\]" lib/scrypath/projection.ex lib/scrypath/document.ex
    </verify>
  </task>

  <task id="T2" type="feature">
    <read_first>
      - lib/scrypath/config.ex
      - lib/scrypath/backend.ex
      - lib/scrypath/options.ex
      - .planning/phases/01-core-contracts-and-api-shape/01-CONTEXT.md
      - .planning/phases/01-core-contracts-and-api-shape/01-RESEARCH.md
      - prompts/elixir-opensource-libs-best-practices-deep-research.md
      - prompts/elixir-best-practices-deep-research.md
    </read_first>
    <action>
      Add `lib/scrypath/config.ex` with `Scrypath.Config.resolve!/1` and `Scrypath.Config.fetch_backend!/1`. `resolve!/1` must accept keyword opts, merge them over `Application.get_env(:scrypath, :defaults, [])`, and validate the merged result with a dedicated runtime option schema that includes `:backend`, `:repo`, `:index_prefix`, and `:sync_mode`. Add `lib/scrypath/backend.ex` defining the internal `@callback` set `name/0`, `index_name/2`, `upsert_documents/3`, `delete_documents/3`, and `search/3`. Do not add any public backend registration API. Use `lib/scrypath/document.ex` in callback specs so the seam is concrete without promising backend neutrality in the public surface.
    </action>
    <acceptance_criteria>
      - `lib/scrypath/config.ex` contains `def resolve!(opts) when is_list(opts) do`
      - `lib/scrypath/config.ex` contains `Application.get_env(:scrypath, :defaults, [])`
      - `lib/scrypath/config.ex` contains `def fetch_backend!(config) do`
      - `lib/scrypath/backend.ex` contains `@callback name() :: atom()`
      - `lib/scrypath/backend.ex` contains `@callback index_name(`
      - `lib/scrypath/backend.ex` contains `@callback upsert_documents(`
      - `lib/scrypath/backend.ex` contains `@callback delete_documents(`
      - `lib/scrypath/backend.ex` contains `@callback search(`
      - `lib/scrypath/backend.ex` does not contain `register_backend`
    </acceptance_criteria>
    <verify>
      rg -n "@callback name\\(\\)|@callback upsert_documents|Application.get_env\\(:scrypath, :defaults, \\[\\]\\)" lib/scrypath/config.ex lib/scrypath/backend.ex
    </verify>
  </task>

  <task id="T3" type="test">
    <read_first>
      - test/scrypath/projection_test.exs
      - test/scrypath/backend_test.exs
      - test/support/fake_backend.ex
      - test/support/searchable_post.ex
      - lib/scrypath/projection.ex
      - lib/scrypath/config.ex
      - lib/scrypath/backend.ex
      - lib/scrypath/document.ex
      - .planning/phases/01-core-contracts-and-api-shape/01-RESEARCH.md
    </read_first>
    <action>
      Add focused tests for the projection and backend seam. In `test/scrypath/projection_test.exs`, cover default field projection, `search_document/1` override precedence, `document_source/1` returning `:fields` or `:custom`, and an explicit failure case where an unloaded or missing field raises `ArgumentError` with the field name in the message. In `test/support/fake_backend.ex`, define `Scrypath.TestSupport.FakeBackend` implementing every callback from `Scrypath.Backend`. In `test/scrypath/backend_test.exs`, assert that `Scrypath.Config.resolve!/1` prefers explicit `backend:` over app defaults and that the fake backend satisfies the callback contract by returning index names and handling document lists built from `Scrypath.Document`.
    </action>
    <acceptance_criteria>
      - `test/scrypath/projection_test.exs` contains `search_document/1`
      - `test/scrypath/projection_test.exs` contains `document_source`
      - `test/scrypath/projection_test.exs` contains `assert_raise ArgumentError`
      - `test/support/fake_backend.ex` contains `@behaviour Scrypath.Backend`
      - `test/scrypath/backend_test.exs` contains `Scrypath.Config.resolve!`
      - `test/scrypath/backend_test.exs` contains `Scrypath.TestSupport.FakeBackend`
      - `mix test test/scrypath/projection_test.exs test/scrypath/backend_test.exs` exits 0
    </acceptance_criteria>
    <verify>
      mix test test/scrypath/projection_test.exs test/scrypath/backend_test.exs
    </verify>
  </task>
</tasks>

<verification>
  <step>Run `mix test test/scrypath/projection_test.exs test/scrypath/backend_test.exs`.</step>
  <step>Run `rg -n "@callback|def document\\(|document_source" lib/scrypath test` to confirm the projection contract and backend seam are represented in both code and tests.</step>
</verification>

<success_criteria>
  <criterion>Projection is testable through a default field path and a custom hook path with explicit precedence.</criterion>
  <criterion>Runtime configuration and backend selection are centralized under Scrypath-managed modules.</criterion>
  <criterion>The adapter seam is concrete enough for Phase 2 work without exposing a public multi-backend facade.</criterion>
</success_criteria>

<threat_model>
  <trust_boundaries>
    <boundary>Source records cross from application schemas into Scrypath projection code.</boundary>
    <boundary>Normalized runtime config crosses from user opts and application env into backend selection.</boundary>
    <boundary>Internal backend implementations receive projected documents and backend-specific options.</boundary>
  </trust_boundaries>
  <stride_register>
    <threat>
      <threat_id>T01-04</threat_id>
      <category>information_disclosure</category>
      <component>projection fallback</component>
      <severity>medium</severity>
      <disposition>mitigate</disposition>
      <mitigation_plan>Only project fields explicitly declared in `__scrypath__(:fields)` and raise when expected data is absent instead of traversing associations or dumping full structs.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T01-05</threat_id>
      <category>tampering</category>
      <component>runtime config resolution</component>
      <severity>medium</severity>
      <disposition>mitigate</disposition>
      <mitigation_plan>Validate merged runtime options in `Scrypath.Config.resolve!/1` before selecting a backend and cover explicit-over-default precedence in tests.</mitigation_plan>
    </threat>
    <threat>
      <threat_id>T01-06</threat_id>
      <category>elevation_of_privilege</category>
      <component>backend seam</component>
      <severity>low</severity>
      <disposition>mitigate</disposition>
      <mitigation_plan>Keep backend hooks internal to `Scrypath.Backend` and omit any public runtime adapter registration or dynamic execution path in Phase 1.</mitigation_plan>
    </threat>
  </stride_register>
</threat_model>
