---
phase: 22-operator-polish-drift-recovery-guide
plan: 02
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - guides/drift-recovery.md
  - mix.exs
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - OPS-09
tags:
  - documentation
  - operations
must_haves:
  truths:
    - "`guides/drift-recovery.md` exists with **six** scenarios matching ROADMAP naming: empty index; stale results after sync success; backfill/count divergence; failed-work pileup; settings drift; stuck reindex mid-cutover — each with symptom → diagnosis → action → verify using only `Scrypath.*` and `mix scrypath.*` (OPS-09, ROADMAP SC3)."
    - "Guide includes a short **Guide map** after intro (CONTEXT D-08) pointing to `guides/relevance-tuning.md`, `guides/faceted-search-with-phoenix-liveview.md`, `guides/multi-index-search.md` with one-line when-to-open, plus **Related guides** footer."
    - "At most one escape-hatch anchor link inline per scenario (CONTEXT D-07); each scenario states coupling in one sentence when overlap with settings/facet/federation (CONTEXT D-09)."
    - "ExDoc lists the guide in `mix.exs` `:extras` and `groups_for_extras` under **Operations**; `DocsContractTest` includes the path and key substrings (prevents drift)."
  artifacts:
    - path: "guides/drift-recovery.md"
      provides: "OPS-09 runbook"
      contains: "symptom"
    - path: "mix.exs"
      provides: "HexDocs discoverability"
      contains: "drift-recovery.md"
---

<objective>
Ship **`guides/drift-recovery.md`** as the v1.3 operator drift runbook (six scenarios, hybrid cross-link posture) and wire it into ExDoc + the docs contract test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-operator-polish-drift-recovery-guide/22-CONTEXT.md
@guides/sync-modes-and-visibility.md
@guides/operator-mix-tasks.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `guides/drift-recovery.md`</name>
  <files>guides/drift-recovery.md</files>
  <read_first>
    - guides/sync-modes-and-visibility.md (tone, heading depth, operational honesty)
    - guides/operator-mix-tasks.md (Mix task names and operator vocabulary)
    - .planning/phases/22-operator-polish-drift-recovery-guide/22-CONTEXT.md (D-06–D-09, scenario list)
  </read_first>
  <action>
    1. Create `guides/drift-recovery.md` with H1 focused on **drift recovery** for Scrypath + Meilisearch operators.
    2. Opening sections: purpose, audience, then **Guide map** (3–5 lines) with relative links to `relevance-tuning.md`, `faceted-search-with-phoenix-liveview.md`, `multi-index-search.md` — one line each on when to open.
    3. Write **six** scenarios as separate H2 sections in this order (align with ROADMAP SC3): (a) empty index, (b) stale results after sync success, (c) backfill/count divergence, (d) failed-work pileup, (e) settings drift, (f) stuck reindex mid-cutover. Each must use headings or bold labels for **Symptom**, **Diagnosis**, **Action**, **Verify** and reference only existing `Scrypath.sync_status/2`, `Scrypath.failed_sync_work/2`, `Scrypath.retry_sync_work/2`, `Scrypath.reconcile_sync/2`, `Scrypath.backfill/2`, `Scrypath.reindex/2`, and `mix scrypath.status`, `mix scrypath.failed`, `mix scrypath.retry`, `mix scrypath.reconcile` (no invented tasks).
    4. Per CONTEXT D-09, where a scenario touches relevance/settings vs facets vs federation, add **one sentence** of disambiguation in-scenario.
    5. Per CONTEXT D-07, at most **one** extra anchor link per scenario body (besides Guide map / Related) — use smallest relevant guide anchor.
    6. End with **Related guides** consolidating links.
    7. Use placeholders like `YOUR_SCHEMA` / `http://localhost:7700` — never real secrets.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test -f guides/drift-recovery.md` exits 0.
    - `grep -c '^##' guides/drift-recovery.md` returns ≥ 8 (intro + map + six scenarios + related minimum).
    - `grep -n 'mix scrypath.status' guides/drift-recovery.md` returns ≥ 1 line.
    - `grep -n 'Scrypath.reindex' guides/drift-recovery.md` returns ≥ 1 line.
    - `grep -n 'multi-index-search.md' guides/drift-recovery.md` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    OPS-09 narrative landed in repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: ExDoc extras + docs contract</name>
  <files>mix.exs, test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - mix.exs (`extras:` list and `groups_for_extras` → Operations)
    - test/scrypath/docs_contract_test.exs (`@guide_paths` ordering and guide assertion style)
  </read_first>
  <action>
    1. Insert `"guides/drift-recovery.md"` into `mix.exs` `extras:` in **lexical** order among guide paths.
    2. Add the same path to `groups_for_extras` under the **Operations** group (near `guides/sync-modes-and-visibility.md` / `guides/operator-mix-tasks.md`).
    3. Add `"guides/drift-recovery.md"` to `@guide_paths` in `docs_contract_test.exs` in lexical order.
    4. Add a new test (or extend the phase-6 guide bundle test) that reads the file and `assert_contains_all` on substrings including: `symptom`, `Diagnosis`, `Action`, `Verify`, `Scrypath.failed_sync_work`, `mix scrypath.failed`, `relevance-tuning.md`, `multi-index-search.md`, `sync-modes-and-visibility.md` (pick set that proves map + scenarios without being brittle to exact casing — match file's chosen heading style consistently).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/docs_contract_test.exs` exits 0.
    - `grep -n 'drift-recovery.md' mix.exs` returns ≥ 2 lines (extras + groups).
  </acceptance_criteria>
  <done>
    Guide is discoverable and contract-locked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown guide | Static content — examples are illustrative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Information Disclosure | Copy-paste of real URLs/secrets in examples | mitigate | Placeholders only; no `masterKey` literals. |

</threat_model>

<verification>
- `mix test test/scrypath/docs_contract_test.exs` exits 0.
- `mix format --check-formatted` on touched Elixir files exits 0.
</verification>

<success_criteria>
- OPS-09: six-scenario drift runbook + ExDoc + contract coverage.
</success_criteria>
