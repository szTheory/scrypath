---
phase: 22-operator-polish-drift-recovery-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/operator/failed_work.ex
  - test/scrypath/operator/failed_work_test.exs
  - docs/search-backend-sre.md
autonomous: true
requirements:
  - OPS-05
  - OPS-06
  - OPS-07
  - OPS-08
  - OPS-10
tags:
  - operator
  - telemetry
  - observability
must_haves:
  truths:
    - "`%FailedWork{}` exposes `attempt`, `max_attempts`, `reason_class`, `last_attempt_at` as additive defaulted fields; `@enforce_keys` unchanged (OPS-05)."
    - "`reason_class` is always one of `:transport`, `:validation`, `:backend_rejected`, `:queue_exhausted`, `:unknown`; ambiguous input maps to `:unknown` (OPS-06)."
    - "Backend and non-Oban-listed rows keep `attempt: nil` and `max_attempts: nil`; Oban rows populate integers from the job when present (OPS-07)."
    - "`last_attempt_at` matches `failed_at` for every constructor path (OPS-08)."
    - "`[:scrypath, :operator, :failed_work, :observed]` fires once per `from_backend_task/3` and `from_queue_job/3` with metadata containing `reason_class`, `schema`, `mode`; measurements `%{count: 1}` (OPS-10, ROADMAP SC4)."
  artifacts:
    - path: "lib/scrypath/operator/failed_work.ex"
      provides: "FailedWork polish + telemetry"
      contains: "reason_class"
    - path: "test/scrypath/operator/failed_work_test.exs"
      provides: "Regression + classifier coverage"
      contains: "failed_sync_work"
---

<objective>
Implement **operator-facing `FailedWork` polish**: additive fields, deterministic `reason_class`, Oban attempt metadata, `last_attempt_at` alias semantics, and the OPS-10 telemetry observation event — without breaking 0.3.0 struct usage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-operator-polish-drift-recovery-guide/22-CONTEXT.md
@.planning/research/deep/OPERATOR_POLISH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive struct fields + backend classification + telemetry</name>
  <files>lib/scrypath/operator/failed_work.ex</files>
  <read_first>
    - lib/scrypath/operator/failed_work.ex (full file — current `from_backend_task/3`, `backend_reason/1`, `defstruct`)
    - lib/scrypath/reindex.ex (lines 129–133 — `:telemetry.execute` shape)
    - .planning/phases/22-operator-polish-drift-recovery-guide/22-CONTEXT.md (D-12, D-16, telemetry D-01–D-05)
  </read_first>
  <action>
    1. Append to `defstruct` (after existing optional fields, **outside** `@enforce_keys`): `attempt: nil`, `max_attempts: nil`, `reason_class: nil`, `last_attempt_at: nil`.
    2. Extend `@type t` with the four fields: `attempt` and `max_attempts` as `non_neg_integer() | nil`, `reason_class` as the five-atom union, `last_attempt_at` as `DateTime.t() | nil`.
    3. Add private functions to **normalize** Meilisearch task `raw` error maps into a small map (`:error_type`, `:error_code` as strings or nil) and **classify** into the five atoms per CONTEXT / OPERATOR_POLISH (`invalid_request` → `:validation`, `internal` → `:backend_rejected`, `auth` → `:transport`, missing → `:unknown`).
    4. In `from_backend_task/3`, set `attempt: nil`, `max_attempts: nil`, `last_attempt_at:` to the same `DateTime.t()` value assigned to `failed_at`, and `reason_class:` from the classifier.
    5. At the **end** of `from_backend_task/3` (single return expression), call `:telemetry.execute([:scrypath, :operator, :failed_work, :observed], %{count: 1}, %{reason_class: ..., schema: schema_module, mode: Keyword.fetch!(config, :sync_mode), operation: operation, retryable?: false})` — include `operation` and `retryable?` per CONTEXT D-02 (`retryable?` mirrors struct field, here `false` for backend failures).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'attempt:' lib/scrypath/operator/failed_work.ex | head -1` exits 0.
    - `grep -n 'reason_class' lib/scrypath/operator/failed_work.ex` returns ≥ 3 lines.
    - After this task only: `grep -c ':operator, :failed_work, :observed' lib/scrypath/operator/failed_work.ex` outputs `1`.
    - `mix format --check-formatted lib/scrypath/operator/failed_work.ex` exits 0.
  </acceptance_criteria>
  <done>
    Backend path carries new fields + first telemetry execute.
  </done>
</task>

<task type="auto">
  <name>Task 2: Queue job classification, attempt fields, second telemetry</name>
  <files>lib/scrypath/operator/failed_work.ex</files>
  <read_first>
    - lib/scrypath/operator/failed_work.ex (post-Task-1 state)
    - .planning/research/deep/OPERATOR_POLISH.md ("`:queue_exhausted`" and precedence vs validation)
  </read_first>
  <action>
    1. In `from_queue_job/3`, read `attempt` and `max_attempts` using the same atom/string key pattern as existing `state` reads (e.g. `Map.get(job, :attempt) || Map.get(job, "attempt")`). Coerce to integers when values are present; otherwise leave `attempt` / `max_attempts` as `nil` only if truly absent (Oban normally supplies them).
    2. Normalize Oban failure signals from `errors` list (first entry string or structured map) plus `state` into an internal map for classification. Implement `:queue_exhausted` when job state is discarded (string or atom) **unless** a higher-precedence class applies from the last error (validation patterns win).
    3. Set `last_attempt_at` equal to `failed_at` (from `queue_timestamp/1` path).
    4. Add second `:telemetry.execute([:scrypath, :operator, :failed_work, :observed], %{count: 1}, metadata)` at end of `from_queue_job/3` with required keys plus `operation:` and `retryable?:` matching the struct fields being returned.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ':operator, :failed_work, :observed' lib/scrypath/operator/failed_work.ex` outputs `2`.
    - `grep -n 'max_attempts' lib/scrypath/operator/failed_work.ex` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    Queue path complete; both constructors emit telemetry.
  </done>
</task>

<task type="auto">
  <name>Task 3: ExUnit — classifier, fields, telemetry contract</name>
  <files>test/scrypath/operator/failed_work_test.exs</files>
  <read_first>
    - test/scrypath/operator/failed_work_test.exs (entire file)
    - test/scrypath/search_many_test.exs (lines ~154–190 — `:telemetry.attach_many` test pattern)
    - lib/scrypath/operator/failed_work.ex (final constructor logic)
  </read_first>
  <action>
    1. Add table-style tests (or one test per row) feeding `FailedWork.list/3` via the existing fake client pattern with **minimal** `meilisearch_tasks` / `oban_jobs` maps covering: `invalid_request` → `:validation`, `internal` → `:backend_rejected`, `auth` → `:transport`, missing error → `:unknown`, and at least one discarded Oban job → `:queue_exhausted`.
    2. Assert `attempt`/`max_attempts` are `nil` on backend rows; non-nil on Oban rows when job maps include integer attempt fields.
    3. Use the repo’s existing idiom from `test/scrypath/search_many_test.exs` (`:telemetry.attach_many/4` + `send(parent, {:telemetry, event, measurements, metadata})` + `on_exit` detach) to assert **one** `[:scrypath, :operator, :failed_work, :observed]` event per returned failed-work row in the primary integration-style test, and assert metadata contains keys `:reason_class`, `:schema`, `:mode`.
    4. Update existing assertions if they pattern-match `%FailedWork{}` without breaking — new fields may be ignored in matches.
  </action>
  <verify>
    <automated>mix test test/scrypath/operator/failed_work_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/operator/failed_work_test.exs` exits 0.
    - `grep -n ':operator, :failed_work, :observed' test/scrypath/operator/failed_work_test.exs` returns ≥ 1 line **or** grep finds `attach`/`execute` test helper referencing that event name in test file.
  </acceptance_criteria>
  <done>
    OPS-06/07/08/10 covered by automated tests.
  </done>
</task>

<task type="auto">
  <name>Task 4: Operator docs — moduledoc + SRE telemetry table</name>
  <files>lib/scrypath/operator/failed_work.ex, docs/search-backend-sre.md</files>
  <read_first>
    - docs/search-backend-sre.md (telemetry table section from docs contract grep context)
    - lib/scrypath/operator/failed_work.ex (@moduledoc)
  </read_first>
  <action>
    1. Expand `@moduledoc` to document the four new fields, the `reason_class` enum, and the `[:scrypath, :operator, :failed_work, :observed]` event with **required** vs **optional** metadata keys; include one sentence that **`schema` module atoms are not safe as low-cardinality metric labels** without rollup (CONTEXT D-05).
    2. Add a row to the telemetry table in `docs/search-backend-sre.md` for `[:scrypath, :operator, :failed_work, :observed]` with guidance: useful for dashboards/logs; **do not** page on every event — treat as diagnostic volume.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'failed_work, :observed' docs/search-backend-sre.md` returns ≥ 1 line.
    - `grep -A40 '@moduledoc """' lib/scrypath/operator/failed_work.ex | grep -q reason_class` exits 0.
  </acceptance_criteria>
  <done>
    Operators can discover telemetry from HexDocs + SRE doc.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telemetry handlers | Untrusted user code in app — metadata must stay low-cardinality. |
| FailedWork listing | Reads already-persisted task/job shapes from configured backends. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Information Disclosure | Telemetry metadata carries schema module | mitigate | Document cardinality guidance; do not add raw document bodies or freeform error blobs to default metadata (CONTEXT D-03). |
| T-22-01-02 | Tampering | Misclassification hides validation failures | mitigate | Default `:unknown`; table tests for precedence; never silently map to a specific non-unknown class without signals. |

</threat_model>

<verification>
- `mix test test/scrypath/operator/failed_work_test.exs` exits 0.
- `mix test test/scrypath/docs_contract_test.exs -t "search backend SRE"` exits 0 (or full file if name not matched).
- `mix format --check-formatted` on touched `.ex` files exits 0.
</verification>

<success_criteria>
- OPS-05..08 and OPS-10 satisfied with tests and docs.
- Two `:telemetry.execute` sites on canonical constructors only.
</success_criteria>
