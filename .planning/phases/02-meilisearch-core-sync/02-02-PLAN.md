---
phase: "02-meilisearch-core-sync"
plan: "02-02"
type: "execute"
wave: 2
depends_on:
  - "02-01"
files_modified:
  - mix.exs
  - lib/scrypath/options.ex
  - lib/scrypath/config.ex
  - lib/scrypath/meilisearch.ex
  - lib/scrypath/meilisearch/client.ex
  - test/scrypath/options_test.exs
  - test/scrypath/meilisearch_test.exs
autonomous: true
requirements:
  - BACK-01
must_haves:
  truths:
    - "Meilisearch is the concrete public backend for v1, but the runtime seam remains the existing internal `Scrypath.Backend` behavior."
    - "The backend stays list-oriented for writes so single-record and batch flows share one path."
    - "Meilisearch-specific transport lives under `Scrypath.Meilisearch.*`, not in the top-level facade."
    - "Phase 2 only implements the minimum callback surface needed for sync flows and the pre-existing behavior contract."
  artifacts:
    - "mix.exs"
    - "lib/scrypath/options.ex"
    - "lib/scrypath/config.ex"
    - "lib/scrypath/meilisearch.ex"
    - "lib/scrypath/meilisearch/client.ex"
    - "test/scrypath/options_test.exs"
    - "test/scrypath/meilisearch_test.exs"
  key_links:
    - "mix.exs -> narrow transport dependencies are added explicitly"
    - "lib/scrypath/meilisearch.ex -> concrete `Scrypath.Backend` implementation for v1"
    - "lib/scrypath/meilisearch/client.ex -> thin Req boundary for sync endpoints and task lookup primitives"
---

<objective>
Implement the concrete Meilisearch backend and its transport/config plumbing without drifting into the Phase 3 search API surface.

Purpose: make Meilisearch the real v1 backend while keeping the adapter seam internal and the transport surface narrow.
Output: runtime deps, config validation, a concrete backend module, and focused backend tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-meilisearch-core-sync/02-CONTEXT.md
@.planning/phases/02-meilisearch-core-sync/02-RESEARCH.md
@.planning/phases/02-meilisearch-core-sync/02-PATTERNS.md
@mix.exs
@lib/scrypath/backend.ex
@lib/scrypath/config.ex
@lib/scrypath/options.ex
@lib/scrypath/sync.ex
@test/support/fake_backend.ex
@prompts/elixir-opensource-libs-best-practices-deep-research.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Meilisearch runtime dependencies and config</name>
  <files>mix.exs, lib/scrypath/options.ex, lib/scrypath/config.ex, test/scrypath/options_test.exs</files>
  <read_first>
    - mix.exs
    - lib/scrypath/options.ex
    - lib/scrypath/config.ex
    - .planning/phases/02-meilisearch-core-sync/02-CONTEXT.md
    - .planning/phases/02-meilisearch-core-sync/02-RESEARCH.md
  </read_first>
  <action>Update `mix.exs` to add `{:req, "~> 0.5"}` and `{:jason, "~> 1.4"}` as runtime dependencies. Extend runtime validation and config helpers only for config required by the concrete backend and upcoming inline task waiting: `meilisearch_url`, `meilisearch_api_key`, `inline_poll_interval`, `inline_timeout`, and any helper accessors needed by later plans. Add focused tests in `test/scrypath/options_test.exs` that prove the new runtime options validate correctly, merge through `Scrypath.Config.resolve!/1`, and reject invalid values. Keep explicit runtime opts canonical and avoid adding broader backend-neutral search knobs that belong to later phases.</action>
  <acceptance_criteria>
    - `mix.exs` contains `{:req, "~> 0.5"}`
    - `mix.exs` contains `{:jason, "~> 1.4"}`
    - `lib/scrypath/options.ex` contains `meilisearch_url: [`
    - `lib/scrypath/options.ex` contains `inline_timeout: [`
    - `lib/scrypath/config.ex` contains `inline_timeout`
    - `test/scrypath/options_test.exs` contains `Scrypath.Config.resolve!`
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scrypath/options_test.exs test/scrypath/backend_test.exs</automated>
  </verify>
  <done>Scrypath can load the Meilisearch transport stack and validate the runtime config required for the concrete backend path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the concrete Meilisearch backend and thin client</name>
  <files>lib/scrypath/meilisearch.ex, lib/scrypath/meilisearch/client.ex, test/scrypath/meilisearch_test.exs</files>
  <read_first>
    - lib/scrypath/backend.ex
    - lib/scrypath/sync.ex
    - test/support/fake_backend.ex
    - .planning/phases/02-meilisearch-core-sync/02-CONTEXT.md
    - .planning/phases/02-meilisearch-core-sync/02-RESEARCH.md
    - .planning/phases/02-meilisearch-core-sync/02-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `Scrypath.Meilisearch` satisfies the existing `Scrypath.Backend` callbacks for name, index naming, upsert, and delete.
    - Test 2: the client module shapes document write and task lookup requests needed by sync flows.
    - Test 3: if `search/3` remains required by the behavior contract, it is implemented minimally without planning richer Phase 3 search transport work.
  </behavior>
  <action>Implement `lib/scrypath/meilisearch.ex` as the concrete `Scrypath.Backend` for v1 and add `lib/scrypath/meilisearch/client.ex` as a thin `Req` boundary for document upserts, document deletes, and task lookup primitives. Keep writes list-oriented. If `search/3` must exist to satisfy the current behavior, implement only the minimum callback body needed to keep the behavior coherent; do not expand this plan into a user-facing search transport design owned by Phase 3.</action>
  <acceptance_criteria>
    - `lib/scrypath/meilisearch.ex` contains `@behaviour Scrypath.Backend`
    - `lib/scrypath/meilisearch.ex` contains `def name, do: :meilisearch`
    - `lib/scrypath/meilisearch/client.ex` contains `Req`
    - `test/scrypath/meilisearch_test.exs` contains `assert Scrypath.Meilisearch.name() == :meilisearch`
    - `test/scrypath/meilisearch_test.exs` contains `index_name`
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scrypath/meilisearch_test.exs</automated>
  </verify>
  <done>The codebase has a concrete Meilisearch backend and a focused client boundary for sync-related calls without drifting into the Phase 3 search API scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime config -> external requests | Caller-provided config influences external Meilisearch connections. |
| projected documents -> backend writes | Internal document structs cross from Scrypath into backend-specific transport. |
| backend behavior -> later phases | The concrete backend must fit the existing seam without overcommitting the public API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | backend transport | high | mitigate | Keep Meilisearch network requests behind a thin client and verify the concrete backend through focused tests that assert callback behavior and request shaping. |
| T-02-05 | Information Disclosure | backend config | medium | mitigate | Centralize URL and API key handling in config/client modules and keep the common runtime contract limited to config actually needed for sync execution. |
| T-02-06 | Repudiation | backend boundary | low | mitigate | Preserve the narrow `Scrypath.Backend` seam and avoid expanding this plan into Phase 3 query design so the public boundary remains auditable. |
</threat_model>

<verification>
Run `mix test test/scrypath/options_test.exs test/scrypath/backend_test.exs test/scrypath/meilisearch_test.exs`.
</verification>

<success_criteria>
Scrypath has a real Meilisearch backend for v1, backed by explicit runtime config and a thin transport boundary, without absorbing the later search API scope.
</success_criteria>

<output>
After completion, create `.planning/phases/02-meilisearch-core-sync/02-02-SUMMARY.md`
</output>
