---
phase: "02-meilisearch-core-sync"
plan: "02-04"
type: "execute"
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - README.md
  - ARCHITECTURE.md
  - lib/scrypath.ex
  - lib/scrypath/sync.ex
  - lib/scrypath/meilisearch.ex
  - test/scrypath/sync_test.exs
autonomous: true
requirements:
  - SYNC-06
  - BACK-01
  - SYNC-03
must_haves:
  truths:
    - "Developers can run manual sync for a single record or a batch without switching to a separate conceptual API family."
    - "Developers can see from the docs that inline and manual share verbs but not completion guarantees."
    - "Meilisearch-specific escape hatches are documented under `Scrypath.Meilisearch.*`."
  artifacts:
    - "README.md"
    - "ARCHITECTURE.md"
    - "lib/scrypath.ex"
    - "lib/scrypath/sync.ex"
    - "lib/scrypath/meilisearch.ex"
    - "test/scrypath/sync_test.exs"
  key_links:
    - "README.md -> examples use explicit context-level `Scrypath.sync_*` and `Scrypath.delete_*` calls"
    - "ARCHITECTURE.md -> distinguishes common path from `Scrypath.Meilisearch.*` escape hatch"
    - "lib/scrypath/sync.ex -> manual single-record and batch flows reuse the same common verbs"
---

<objective>
Finish the Phase 2 operator-facing surface: manual single-record and batch flows, explicit documentation of lifecycle guarantees, and the Meilisearch-specific escape hatch.

Purpose: Make the write path usable by real Ecto and Phoenix teams for imports, migrations, and explicit orchestration without hiding the operational tradeoffs.
Output: Completed manual/batch contract, updated docs, and end-to-end contract tests for manual behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-meilisearch-core-sync/02-CONTEXT.md
@.planning/phases/02-meilisearch-core-sync/02-RESEARCH.md
@.planning/phases/02-meilisearch-core-sync/02-03-PLAN.md
@README.md
@ARCHITECTURE.md
@lib/scrypath.ex
@lib/scrypath/sync.ex
@lib/scrypath/meilisearch.ex
@prompts/search-lib-use-cases-deep-research.md
@prompts/elixir-best-practices-deep-research.md
@prompts/ecto-best-practices-deep-research.md
@prompts/elixir-opensource-libs-best-practices-deep-research.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete manual and batch sync behavior</name>
  <files>lib/scrypath.ex, lib/scrypath/sync.ex, test/scrypath/sync_test.exs</files>
  <read_first>
    - lib/scrypath.ex
    - lib/scrypath/sync.ex
    - test/scrypath/sync_test.exs
    - .planning/phases/02-meilisearch-core-sync/02-CONTEXT.md
    - .planning/phases/02-meilisearch-core-sync/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `sync_records/3` in manual mode returns accepted task metadata for a batch without waiting.
    - Test 2: `delete_document/3` in manual mode returns accepted task metadata for an explicit id.
    - Test 3: manual and inline share the same verb names while producing different completion states.
  </behavior>
  <action>Per D-05, D-07, D-13, D-20, and D-29, finish `Scrypath.Sync` and `Scrypath` so manual workflows support intentional single-record and batch upsert/delete through the same public verbs created earlier. Do not introduce a second manual-only API family. Ensure the returned result exposes task metadata for operator-controlled workflows and preserves the explicit document-id delete path for cases where no source struct exists.</action>
  <acceptance_criteria>
    - `test/scrypath/sync_test.exs` contains `assert {:ok, %{mode: :manual`
    - `test/scrypath/sync_test.exs` contains `sync_records`
    - `test/scrypath/sync_test.exs` contains `delete_document`
    - `test/scrypath/sync_test.exs` contains `delete_documents`
  </acceptance_criteria>
  <verify>
    <automated>mix test test/scrypath/sync_test.exs</automated>
  </verify>
  <done>Manual sync covers single-record and batch operator workflows through the same common verbs and returns accepted-task metadata without waiting.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Phase 2 sync contract and Meilisearch escape hatch</name>
  <files>README.md, ARCHITECTURE.md, lib/scrypath/meilisearch.ex</files>
  <read_first>
    - README.md
    - ARCHITECTURE.md
    - lib/scrypath/meilisearch.ex
    - .planning/ROADMAP.md
    - .planning/phases/02-meilisearch-core-sync/02-CONTEXT.md
    - .planning/phases/02-meilisearch-core-sync/02-RESEARCH.md
    - prompts/elixir-opensource-libs-best-practices-deep-research.md
    - prompts/search-lib-use-cases-deep-research.md
  </read_first>
  <action>Per D-22, D-23, D-26, D-28, D-29, D-30, and D-31, update `README.md` and `ARCHITECTURE.md` to document the common sync verbs, the difference between `:inline` and `:manual`, the fact that inline waits for terminal backend success without making DB and search writes atomic, and the rule that sync should run after successful repo persistence. Add or extend module docs in `lib/scrypath/meilisearch.ex` so the Meilisearch-specific escape hatch is explicit and small. Keep docs centered on explicit context-level orchestration; do not add callback-style auto-sync guidance or promise deferred Phase 5 operator features.</action>
  <acceptance_criteria>
    - `README.md` contains `Scrypath.sync_record`
    - `README.md` contains `sync_mode: :inline`
    - `README.md` contains `sync_mode: :manual`
    - `README.md` contains `does not make database and search writes atomic`
    - `README.md` contains `search_document_id/1`
    - `ARCHITECTURE.md` contains `Scrypath.Meilisearch`
    - `ARCHITECTURE.md` contains `after successful repo persistence`
    - `lib/scrypath/meilisearch.ex` contains `@moduledoc`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Scrypath\\.sync_record|sync_mode: :inline|sync_mode: :manual|does not make database and search writes atomic|Scrypath\\.Meilisearch|after successful repo persistence" README.md ARCHITECTURE.md lib/scrypath/meilisearch.ex</automated>
  </verify>
  <done>The public docs explain the real sync guarantees, the Meilisearch namespace is clearly documented, and the examples keep orchestration explicit in normal Ecto application code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> developer usage | Public docs influence when and how callers trigger sync operations. |
| operator workflows -> backend tasks | Manual mode exposes task acceptance to imports, migrations, and admin workflows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Repudiation | public docs for sync guarantees | high | mitigate | Document exact inline and manual guarantees, include the non-atomic write warning, and require examples that run sync after repo persistence so callers do not infer false transactional guarantees. |
| T-02-11 | Tampering | manual workflow API shape | medium | mitigate | Reuse the same top-level verbs for manual mode so operator paths cannot drift into a second inconsistent API family. |
| T-02-12 | Information Disclosure | Meilisearch-specific escape hatch | low | mitigate | Keep backend-native power in `Scrypath.Meilisearch.*` docs and module boundaries so callers know when they are leaving the common path. |
</threat_model>

<verification>
Run `mix test test/scrypath/sync_test.exs` and `rg -n "Scrypath\\.sync_record|sync_mode: :inline|sync_mode: :manual|does not make database and search writes atomic|Scrypath\\.Meilisearch|after successful repo persistence" README.md ARCHITECTURE.md lib/scrypath/meilisearch.ex`.
</verification>

<success_criteria>
Manual mode is a first-class operator path, not a hidden variant, and the docs make the completion guarantees and Meilisearch-specific escape hatch obvious.
</success_criteria>

<output>
After completion, create `.planning/phases/02-meilisearch-core-sync/02-04-SUMMARY.md`
</output>
