---
phase: 33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CONTRIBUTING.md
  - guides/golden-path.md
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements: [ADPT-01, EXAM-02, VRFY-02, AUDT-01]
tags: [docs, smoke, cwd, contract-test, adoption, verification]

must_haves:
  truths:
    - "`examples/phoenix_meilisearch/scripts/smoke.sh` exists; **`scripts/smoke.sh` does not exist at the repository root** (filesystem matches `033-RESEARCH.md`)"
    - "Published **README**, **CONTRIBUTING**, and **guides/golden-path.md** never teach **`./scripts/smoke.sh`** from the **clone root** without an explicit **`cd examples/phoenix_meilisearch`**, **root-relative script path**, or equivalent scoped wording per `REQUIREMENTS.md` Phase 33 success criteria"
    - "`docs_contract_test.exs` asserts those filesystem + string invariants so regressions fail in CI (`mix test test/scrypath/docs_contract_test.exs`)"
  artifacts:
    - path: "README.md"
      provides: "ADPT-01-safe integration smoke pointer from repo root"
      contains: "examples/phoenix_meilisearch"
    - path: "CONTRIBUTING.md"
      provides: "VRFY-02 / EXAM-02 local approximation with correct cwd"
      contains: "phoenix-example-integration"
    - path: "guides/golden-path.md"
      provides: "Runbook bullet scoped to example app directory"
      contains: "examples/phoenix_meilisearch/README.md"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "AUDT-01 doc–filesystem contract depth"
      contains: "File.regular?"
---

<objective>
Close **Phase 33** gap **INT-ROOT-SMOKE-PATH** / **INT-DOCS-CONTRACT-DEPTH**: align **root-facing** prose with filesystem truth (**sole** `smoke.sh` under **`examples/phoenix_meilisearch/scripts/`**), and extend **`Scrypath.DocsContractTest`** per **`033-RESEARCH.md` → Validation Architecture** — satisfying **ADPT-01**, **EXAM-02**, **VRFY-02**, and **AUDT-01** for this slice without opening **Phase 34** (golden-path ↔ PR **`phoenix-example-integration`** narrative).
</objective>

<threat_model>
**ASVS V14 / documentation integrity (L1)**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| **T-33-DOC-01:** Misleading **current working directory** in copy-paste shell snippets causes readers to run **`./scripts/smoke.sh`** from the **repository root** where the file does not exist — failed onboarding, wasted triage, possible unsafe “workarounds” | Medium | Use **one canonical pattern** (see tasks): either **`cd examples/phoenix_meilisearch && ./scripts/smoke.sh`** or **`bash examples/phoenix_meilisearch/scripts/smoke.sh`** with explicit “from clone root” where needed; lock with **`File.regular?/1`** + substring **`ordered?/2`** contracts |
| **T-33-DOC-02:** **Golden-path** edits accidentally drop the **example README** link while tightening smoke wording | Low | Contract test keeps **`examples/phoenix_meilisearch/README.md`** in the golden-path guide block (extend Phase 29 assert or sibling test) |

**Out of scope (Phase 34 — do not implement here):** Rewriting **`guides/golden-path.md`** bullets that claim the Phoenix example smoke is “**local** / optional CI wiring” vs **`.github/workflows/ci.yml`** `phoenix-example-integration` on PRs (**INT-GOLDEN-PATH-CI-STORY**). If a task touches adjacent lines, add a **HTML comment or single-line maintainer note** only if needed to avoid accidental scope creep; prefer **no CI copy change** in Phase 33.

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: README — integration smoke paragraph (cwd-safe)</name>
  <files>README.md</files>
  <read_first>
    - README.md (§Installation, **Integration smoke** paragraph ~L19)
    - examples/phoenix_meilisearch/README.md (canonical commands)
    - .planning/phases/033-example-smoke-paths-and-doc-contracts/033-RESEARCH.md
    - .planning/phases/033-example-smoke-paths-and-doc-contracts/033-PATTERNS.md
  </read_first>
  <action>
    In **`README.md`**, the sentence that currently bundles **`examples/phoenix_meilisearch`**, Compose, and bare **`./scripts/smoke.sh`** (see ~**L19**):

    1. **Remove** the orphan fragment **`./scripts/smoke.sh`** unless it is **scoped** to the example directory.
    2. **Replace** with **one** canonical pattern (Tasks 1–2 **must match**):
       - **Pattern A (default for Phase 33):** Inline or fenced: **`cd examples/phoenix_meilisearch && ./scripts/smoke.sh`** (or two-line fenced block: line 1 `cd examples/phoenix_meilisearch`, line 2 `./scripts/smoke.sh`).
       - **Pattern B (only if explicitly chosen over A):** From clone root: **`bash examples/phoenix_meilisearch/scripts/smoke.sh`** with explicit “from the repository root” wording.

    **Executor default:** use **Pattern A** unless `033-PATTERNS.md` already records a different repo-wide choice.

    Preserve existing links to **`CONTRIBUTING.md`** (CI job names) and **`examples/phoenix_meilisearch`** as the example pointer.
  </action>
  <verify>
    <automated>sh -c 'test -f examples/phoenix_meilisearch/scripts/smoke.sh && test ! -f scripts/smoke.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f examples/phoenix_meilisearch/scripts/smoke.sh` exits **0**
    - `test ! -f scripts/smoke.sh` exits **0**
    - After edits, `rg -n '\./scripts/smoke\.sh' README.md` — every hit must appear **on the same line** as **`cd examples/phoenix_meilisearch`** **or** the line must use **`examples/phoenix_meilisearch/scripts/smoke.sh`** / **`bash examples/phoenix_meilisearch/scripts/smoke.sh`** (no bare root-voice `./scripts/smoke.sh`)
    - `sed -n '17,22p' README.md | grep -F 'examples/phoenix_meilisearch'` exits **0** (integration smoke paragraph band)
    - `grep -F 'cd examples/phoenix_meilisearch' README.md` exits **0** **or** `grep -F 'bash examples/phoenix_meilisearch/scripts/smoke.sh' README.md` exits **0** (canonical Pattern A or B present)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: CONTRIBUTING — `phoenix-example-integration` cell (local approximation cwd)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (CI table, **`phoenix-example-integration`** row ~L43)
    - README.md (match Pattern A/B chosen in Task 1)
    - examples/phoenix_meilisearch/README.md
    - .planning/phases/033-example-smoke-paths-and-doc-contracts/033-RESEARCH.md
  </read_first>
  <action>
    In **`CONTRIBUTING.md`** table row **`phoenix-example-integration`**, replace the trailing clause that currently ends with bare **`./scripts/smoke.sh`** (~**L43**):

    1. Use the **same** Pattern **A** or **B** as **Task 1** (e.g. **`cd examples/phoenix_meilisearch && ./scripts/smoke.sh`** **or** **`bash examples/phoenix_meilisearch/scripts/smoke.sh`**).
    2. Keep **`cd examples/phoenix_meilisearch && mix test`** and env var list intact; only fix the **smoke** approximation phrase.
    3. Do **not** change other job rows or the **`test` / `quality`** default-matrix story (**VRFY-02** scope is footnote correctness only).
  </action>
  <verify>
    <automated>sh -c 'test -f examples/phoenix_meilisearch/scripts/smoke.sh && test ! -f scripts/smoke.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'phoenix-example-integration' CONTRIBUTING.md` exits **0**
    - `rg -n '\./scripts/smoke\.sh' CONTRIBUTING.md` — same **scoping rule** as Task 1 (no orphan `./scripts/smoke.sh` from implied clone root)
    - `grep -F 'examples/phoenix_meilisearch' CONTRIBUTING.md` exits **0**
    - `grep -F 'cd examples/phoenix_meilisearch' CONTRIBUTING.md` exits **0** **or** `grep -F 'bash examples/phoenix_meilisearch/scripts/smoke.sh' CONTRIBUTING.md` exits **0**
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: `guides/golden-path.md` — runbook bullet cwd clarity only</name>
  <files>guides/golden-path.md</files>
  <read_first>
    - guides/golden-path.md (**§Integration smoke**, bullet ~**L122–L127**)
    - examples/phoenix_meilisearch/README.md
    - .planning/phases/033-example-smoke-paths-and-doc-contracts/033-RESEARCH.md
  </read_first>
  <action>
    In **`guides/golden-path.md`**, the runbook bullet that lists **`./scripts/smoke.sh`** (~**L126**):

    1. **Scope** `./scripts/smoke.sh` to the **example app directory** in prose — e.g. rename bullet to state **“from `examples/phoenix_meilisearch/`”** or **“the example app’s `./scripts/smoke.sh`”** so a reader never copies **`./scripts/smoke.sh`** believing cwd is repo root.
    2. Keep the Markdown link to **`examples/phoenix_meilisearch/README.md`** unchanged.
    3. **Deferred / out-of-scope (Phase 34):** Do **not** rewrite the **second bullet** (~**L127**) CI vs “local / optional CI wiring” claims to match **`ci.yml`** — label that alignment **Phase 34** in commit message only; no prose change required for Phase 33 unless a typo fix is unavoidable in the same line (then **minimal** edit).

    Optional: if `./scripts/smoke.sh` remains in the bullet, ensure **`ordered?`-style clarity** in tests (Task 4) by pairing **`examples/phoenix_meilisearch`** substring **before** first **`./scripts/smoke.sh`** in that file section (implementation detail left to executor).
  </action>
  <verify>
    <automated>sh -c 'test -f examples/phoenix_meilisearch/scripts/smoke.sh && test ! -f scripts/smoke.sh'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'examples/phoenix_meilisearch/README.md' guides/golden-path.md` exits **0**
    - `rg -n '\./scripts/smoke\.sh' guides/golden-path.md` — each hit line also contains **`examples/phoenix_meilisearch`** **or** **`cd`** **or** explicit **“example app”** / **“from `examples/phoenix_meilisearch`”** wording; document the exact paired substring in Task 4 test comments
    - `grep -F 'optional CI wiring' guides/golden-path.md` exits **0** (second integration-smoke bullet unchanged for Phase 34 deferral — phrase must remain until Phase 34 rewrites CI copy)
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: `docs_contract_test.exs` — filesystem + README / CONTRIBUTING / golden-path contracts</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (module attrs, **`ordered?/2`**, **`assert_contains_all/2`**, Phase 29 + CONTRIBUTING tests)
    - README.md, CONTRIBUTING.md, guides/golden-path.md (post-edit bodies)
    - .planning/phases/033-example-smoke-paths-and-doc-contracts/033-RESEARCH.md (Validation Architecture §§1–3)
    - .planning/phases/033-example-smoke-paths-and-doc-contracts/033-PATTERNS.md
  </read_first>
  <action>
    Add **ExUnit** tests (async-safe: only read files / module attrs):

    1. **`"example smoke script exists only under phoenix_meilisearch"`** (or equivalent name):
       - `assert File.regular?("examples/phoenix_meilisearch/scripts/smoke.sh")`
       - `refute File.regular?("scripts/smoke.sh")`

    2. **`"README and CONTRIBUTING document example smoke cwd (Phase 33)"`**:
       - **`@readme`**: assert **`cd examples/phoenix_meilisearch`** appears **OR** assert **`bash examples/phoenix_meilisearch/scripts/smoke.sh`** / **`examples/phoenix_meilisearch/scripts/smoke.sh`** (match Task 1 pattern).
       - **`@contributing`**: assert the **`phoenix-example-integration`** row contains the **same** canonical pattern.
       - If Pattern **A** uses **`./scripts/smoke.sh`** after **`cd`**, add **`ordered?(@readme, "cd examples/phoenix_meilisearch", "./scripts/smoke.sh")`** **only if** both needles exist in **`README.md`**; mirror for **`CONTRIBUTING.md`** if applicable.
       - **Optional strictness** (team choice): `refute String.contains?(@readme, "./scripts/smoke.sh")` **only if** docs adopt Pattern **B** everywhere; otherwise prefer **ordered** pairing over forbidden bare substring.

    3. **`"golden path scopes example smoke script to the phoenix_meilisearch example (Phase 33)"`** (sibling to Phase 29 test):
       - `golden = @guides["guides/golden-path.md"]`
       - Keep / assert **`examples/phoenix_meilisearch/README.md`** still present (extend **`"phase 29 golden path guide and adoption readme contract"`** or add **`assert_contains_all(golden, [...])`** for the link + cwd anchor phrase chosen in Task 3).
       - Assert **`./scripts/smoke.sh`** in **`golden`** appears **only** after **`examples/phoenix_meilisearch`** on the same line **or** use **`ordered?(golden, "examples/phoenix_meilisearch", "./scripts/smoke.sh")`** if ordering holds file-wide.

    Do **not** add **`ADPT-NN` / `VRFY-NN`** strings to published markdown (hygiene test); requirement traceability stays in **this plan** only.

    Run **`mix format`** on the test file.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/docs_contract_test.exs` exits **0**
    - `mix format --check-formatted test/scrypath/docs_contract_test.exs` exits **0**
    - `grep -F 'File.regular?("examples/phoenix_meilisearch/scripts/smoke.sh")' test/scrypath/docs_contract_test.exs` returns **≥1** line
    - `grep -F 'refute File.regular?("scripts/smoke.sh")' test/scrypath/docs_contract_test.exs` returns **≥1** line
    - Repo gate from research: `rg '\./scripts/smoke\.sh' README.md CONTRIBUTING.md guides/golden-path.md` shows **no orphan** root-voice paths per Tasks 1–3
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <note>Execute **Tasks 1–3** (markdown) before **Task 4** (ExUnit). Per-task `mix test` is required only on **Task 4** and in this block — not after Tasks 1–3 alone.</note>
  <automated>
    - `mix test test/scrypath/docs_contract_test.exs`
    - `mix format --check-formatted`
  </automated>
  <manual>
    - Fresh clone: read **README** integration paragraph only; confirm no step implies **`./scripts/smoke.sh`** from repo root without **`cd`** or root-relative script path (**EXAM-02** spot-check per **`033-VALIDATION.md`**)
  </manual>
</verification>

<success_criteria>
  1. **`REQUIREMENTS.md` Phase 33 (gap closure):** No published maintainer path implies **`./scripts/smoke.sh`** from **repository root** unless that file exists there; docs show **`cd examples/phoenix_meilisearch`** (or equivalent) / **root-relative script path** before example-local **`./scripts/smoke.sh`**.
  2. **`docs_contract_test.exs`** covers **cwd-dependent** strings and **filesystem** layout for the smoke script (**AUDT-01** contract depth for this gap).
  3. **Phase 34** scope (**README ↔ golden path first schema**, **CI narrative ↔ `phoenix-example-integration` on PRs**) remains **unchanged** in this wave — no edits to **`guides/golden-path.md`** CI bullet solely for **`ci.yml`** parity unless explicitly labeled **deferred** and **skipped**.
</success_criteria>
