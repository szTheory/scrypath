---
phase: 91-integration-guides-and-verification
plan: 04
type: execute
wave: 2
depends_on: ["91-03"]
files_modified:
  - guides/related-data-and-reindexing.md
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements: [EXEC-02, TEST-01, TEST-02]
gap_closure: true
gaps_addressed: [CR-01, WR-02]

must_haves:
  truths:
    - "An adopter can read the guide and declare fan_outs: using the working hand-written accessor pattern — the broken `use Scrypath, fan_outs:` snippet is gone."
    - "The guide explicitly warns that `use Scrypath` does not generate a `__scrypath__(:fan_outs)` accessor and that hand-written reflection is required."
    - "The guide's update_author/3 worked example returns `{:ok, result, updated}` matching the canonical blog.ex implementation and smoke-test assertions."
    - "The docs-contract test asserts `def __scrypath__(:fan_outs)` is present in the guide, so the working pattern cannot silently regress."
  artifacts:
    - path: "guides/related-data-and-reindexing.md"
      provides: "Fixed fan_outs: schema-side example (hand-written accessor) + prose note + corrected update_author/3 return"
      contains: "def __scrypath__(:fan_outs)"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Regression-proof assertion that guide contains the hand-written accessor pattern"
      contains: "def __scrypath__(:fan_outs)"
  key_links:
    - from: "guides/related-data-and-reindexing.md (section a)"
      to: "examples/phoenix_meilisearch/lib/scrypath_demo/blog/author.ex"
      via: "hand-written __scrypath__(:fan_outs) pattern (lines 33-43)"
      pattern: "def __scrypath__\\(:fan_outs\\)"
    - from: "test/scrypath/docs_contract_test.exs"
      to: "guides/related-data-and-reindexing.md"
      via: "@guides module attribute (File.read! at compile time)"
      pattern: "assert.*__scrypath__.*:fan_outs"
---

<objective>
Close two gaps identified in 91-VERIFICATION.md that prevented the phase from passing.

Gap 1 (BLOCKER — CR-01): Section (a) of `guides/related-data-and-reindexing.md` teaches
`use Scrypath, fan_outs: [...]` as the canonical schema declaration. The shipped
`lib/scrypath/schema.ex` macro generates `__scrypath__` accessors for `:config`, `:fields`,
`:filterable`, `:faceting`, `:sortable`, `:settings`, `:document_id`, `:document_source`, and
`:backend` — NOT `:fan_outs`. The catch-all clause raises `ArgumentError: unknown Scrypath
metadata key: :fan_outs`, so an adopter following the guide verbatim cannot successfully call
`Scrypath.sync_related/3`. Replace the broken snippet with the working hand-written accessor
pattern from `examples/phoenix_meilisearch/lib/scrypath_demo/blog/author.ex` (lines 33-43),
add a prose note explaining the limitation, and add a docs-contract assertion so the fix
cannot silently regress.

Gap 2 (WARNING — WR-02): The guide's `update_author/3` worked example (line 193) returns
`{:ok, updated}` (2-tuple). The canonical `examples/phoenix_meilisearch/lib/scrypath_demo/blog.ex`
returns `{:ok, result, updated}` (3-tuple), and both smoke tests assert the 3-tuple shape.
Fix the guide return to match.

Purpose: EXEC-02 — an adopter following the guide must be able to use the documented API without
hitting a runtime crash on the schema declaration or constructing a wrong function signature.

Output: corrected guide (schema pattern + return value) + new regression-proof docs-contract assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-integration-guides-and-verification/91-CONTEXT.md
@.planning/phases/91-integration-guides-and-verification/91-VERIFICATION.md

<interfaces>
<!-- Source of truth files. Extracted 2026-05-25. -->

From lib/scrypath/schema.ex — what `use Scrypath` DOES and DOES NOT generate:
  Generated accessors: :config, :fields, :filterable, :faceting, :sortable, :settings,
    :document_id, :document_source, :backend
  NOT generated: :fan_outs
  Catch-all: def __scrypath__(key) -> raise ArgumentError, "unknown Scrypath metadata key: #{inspect(key)}"

From examples/phoenix_meilisearch/lib/scrypath_demo/blog/author.ex (lines 22-43) —
the working hand-written accessor pattern the guide must teach:
  use Ecto.Schema           # NOT use Scrypath
  schema "authors" do ... end
  def __scrypath__(:fan_outs) do
    [
      posts: [
        target: ScrypathDemo.Blog.Post,
        resolver: {ScrypathDemo.Blog, :resolve_posts_for_authors, []}
      ]
    ]
  end
  def __scrypath__(:document_id), do: :id
  # @moduledoc explains WHY: "use Scrypath does not yet resolve module aliases in
  #   fan_outs: at macro-expansion time, and it does not generate a __scrypath__(:fan_outs)
  #   accessor, so the hand-written reflection is the correct, library-respecting path."

From examples/phoenix_meilisearch/lib/scrypath_demo/blog.ex (lines 37-48) —
canonical update_author/3 (the 3-tuple return the guide must show):
  def update_author(%Author{} = author, attrs, sync_opts) do
    {:ok, updated} = author |> Author.changeset(attrs) |> Repo.update()
    from(p in Post, where: p.author_id == ^updated.id)
    |> Repo.update_all(set: [author_name: updated.name])
    {:ok, result} =
      Scrypath.sync_related(Author, updated, Keyword.put(sync_opts, :fan_out, :posts))
    {:ok, result, updated}   # <-- 3-tuple
  end

From test/scrypath/docs_contract_test.exs (lines 1137-1155) — existing test to extend:
  test "related-data guide adopts sync_related/3 as the canonical fan-out story" do
    guide = @guides["guides/related-data-and-reindexing.md"]
    refute String.contains?(guide, "temporary workaround"), ...
    refute String.contains?(guide, "first-class feature"), ...
    assert_contains_all(guide, [
      "Scrypath.sync_related/3", "fan_outs:", "sync_mode: :inline",
      "sync_mode: :oban", "callback magic", "contexts own orchestration",
      "library owns execution"
    ])
  end
  # @guides is built from @guide_paths at compile time via File.read! — the guide
  # content is already loaded. No new module attribute needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix guide section (a) — replace broken use Scrypath snippet with hand-written accessor pattern</name>
  <files>guides/related-data-and-reindexing.md</files>
  <read_first>
    - guides/related-data-and-reindexing.md (lines 94-130 — section "(a) Declare the fan-out on the owning schema" being replaced)
    - examples/phoenix_meilisearch/lib/scrypath_demo/blog/author.ex (whole file — the working pattern to replicate; lines 1-50)
    - lib/scrypath/schema.ex (whole file — confirms which keys the macro does and does NOT generate)
    - guides/related-data-and-reindexing.md (lines 174-210 — section "(c) Write an arity-safe resolver" where update_author/3 return must also be fixed per Gap 2)
    - examples/phoenix_meilisearch/lib/scrypath_demo/blog.ex (lines 37-48 — canonical 3-tuple return)
  </read_first>
  <action>
    Make two targeted edits to `guides/related-data-and-reindexing.md`. Do NOT restructure or
    rewrite sections that are not listed here — the guide passed 9/11 truths and all non-gap
    prose must be preserved verbatim.

    EDIT 1 — Section (a), lines 101-126 (the broken `use Scrypath, fan_outs:` code block):

    Replace the entire fenced code block that begins:
      ```elixir
      defmodule MyApp.Accounts.Author do
        use Ecto.Schema

        use Scrypath,
          fields: [:name],
          fan_outs: [
            ...
          ]
      ```
    with the working hand-written accessor pattern modelled on
    `examples/phoenix_meilisearch/lib/scrypath_demo/blog/author.ex` lines 33-43.

    The replacement block must:
    - Use `use Ecto.Schema` without `use Scrypath` (the owning schema is NOT a Scrypath
      search target; it just carries fan_outs: metadata)
    - Define `def __scrypath__(:fan_outs)` returning the keyword list with
      `target: MyApp.Blog.Post` and `resolver: {MyApp.Accounts, :resolve_posts_for_authors, []}`
    - Define `def __scrypath__(:document_id), do: :id` (required by the :oban enqueue path that
      calls `Scrypath.Identity.document_ids/2` to extract the IDs to serialize into the job)
    - Keep the existing `schema "authors"` block, `has_many`, `timestamps()`, and `changeset/2`
      verbatim

    Immediately after the replacement code block (still inside section (a)), add a prose
    paragraph that explains the limitation. The paragraph must contain the exact phrase
    `use Scrypath` does not generate a `__scrypath__(:fan_outs)` accessor — the paragraph
    should read approximately:

      > Note: `use Scrypath` does not generate a `__scrypath__(:fan_outs)` accessor.
      > The macro generates reflection keys for fields, settings, and document identity
      > (see `lib/scrypath/schema.ex`) but not for fan-out declarations. Until macro-expansion
      > support for `fan_outs:` is added, declare `__scrypath__(:fan_outs)` and
      > `__scrypath__(:document_id)` by hand, as shown above. This is the same pattern
      > used by the library's own hermetic fan-out tests.

    The exact wording may vary to fit guide prose style, but these two strings must be present
    verbatim in the note (they will be asserted by the docs-contract test in Task 2):
      - `use Scrypath` does not generate a `__scrypath__(:fan_outs)` accessor
      - `__scrypath__(:document_id)`

    EDIT 2 — Section (c), update_author/3 return value (line 193):

    In the `update_author/3` code block inside section (c), locate the line:
      `{:ok, updated}`
    (the final return of the function) and replace it with:
      `{:ok, result, updated}`

    Matching the context from blog.ex, the `result` variable is bound by the line above:
      `{:ok, result} = Scrypath.sync_related(Author, updated, ...)`
    The replacement must update the return line and nothing else in that block — the
    `Repo.update()`, `Repo.update_all`, and `sync_related` lines above are correct and must
    not change.
  </action>
  <verify>
    <automated>grep -n "def __scrypath__(:fan_outs)" guides/related-data-and-reindexing.md</automated>
    <automated>grep -n "__scrypath__(:document_id)" guides/related-data-and-reindexing.md</automated>
    <automated>grep -n "use Scrypath.*does not generate" guides/related-data-and-reindexing.md || grep -n "does not generate a" guides/related-data-and-reindexing.md</automated>
    <automated>grep -n "{:ok, result, updated}" guides/related-data-and-reindexing.md</automated>
    <automated>grep -c "use Scrypath," guides/related-data-and-reindexing.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def __scrypath__(:fan_outs)" guides/related-data-and-reindexing.md` returns at least one match in section (a) (before line 160 or wherever the section ends).
    - `grep -n "__scrypath__(:document_id)" guides/related-data-and-reindexing.md` returns at least one match (the hand-written `:document_id` accessor is shown alongside `:fan_outs`).
    - `grep -n "{:ok, result, updated}" guides/related-data-and-reindexing.md` returns at least one match in section (c)'s `update_author/3` code block.
    - `grep -c "{:ok, updated}$" guides/related-data-and-reindexing.md` returns 0 — the bare 2-tuple is gone.
    - The guide's section (a) no longer contains the 4-line `use Scrypath, fan_outs: [` block. Verify: `grep -c "use Scrypath,\s*$\|use Scrypath,$" guides/related-data-and-reindexing.md` returns 0 OR the only remaining `use Scrypath` occurrences are in prose (not code blocks). More precisely: `grep -n "fan_outs:" guides/related-data-and-reindexing.md` must show `fan_outs:` only inside the `def __scrypath__(:fan_outs)` return value — not as a keyword argument to `use Scrypath`.
    - A prose note is present in section (a) containing both of: `does not generate a \`__scrypath__(:fan_outs)\` accessor` AND `__scrypath__(:document_id)`.
    - `mix docs --warnings-as-errors` exits 0 (no broken ExDoc references introduced by the edit).
  </acceptance_criteria>
  <done>
    Section (a) shows the working hand-written accessor pattern (matching author.ex lines 33-43),
    a prose note states the `use Scrypath` macro limitation, `__scrypath__(:document_id)` is shown
    alongside `:fan_outs`, and the update_author/3 return in section (c) is the 3-tuple
    `{:ok, result, updated}`. The guide compiles (ExDoc) without warnings.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add docs-contract assertion that guide contains the hand-written __scrypath__(:fan_outs) accessor</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (lines 1137-1186 — the existing related-data test and assert_contains_all helper to extend)
    - guides/related-data-and-reindexing.md (after Task 1 — confirm the exact strings now present, so assertions match)
    - test/scrypath/docs_contract_test.exs (lines 1-57 — @guides module attribute build; confirms the guide is already loaded at compile time, no new attribute needed)
  </read_first>
  <action>
    Edit `test/scrypath/docs_contract_test.exs`. The single change is:

    Extend the `assert_contains_all/2` call inside the existing test
    "related-data guide adopts sync_related/3 as the canonical fan-out story"
    (currently lines 1146-1154) to add two more required strings to the list:

      "def __scrypath__(:fan_outs)"
      "does not generate a `__scrypath__(:fan_outs)` accessor"

    The updated `assert_contains_all` call should read:

      assert_contains_all(guide, [
        "Scrypath.sync_related/3",
        "fan_outs:",
        "sync_mode: :inline",
        "sync_mode: :oban",
        "callback magic",
        "contexts own orchestration",
        "library owns execution",
        "def __scrypath__(:fan_outs)",
        "does not generate a `__scrypath__(:fan_outs)` accessor"
      ])

    Do NOT add a new test. Do NOT modify any other line in the file. The two new strings
    anchor to the exact prose and code that Task 1 introduced:
    - "def __scrypath__(:fan_outs)" — the working accessor function the guide now teaches
    - "does not generate a `__scrypath__(:fan_outs)` accessor" — the prose note explaining
      the macro limitation

    If Task 1 used slightly different prose for the note, align the asserted string exactly
    to what Task 1 wrote. The assertion must be a verbatim substring of the guide.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs --only docs_contract -x 2>&1 | tail -5</automated>
    <automated>mix verify.phase91 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/docs_contract_test.exs --only docs_contract -x` exits 0. In particular, the test "related-data guide adopts sync_related/3 as the canonical fan-out story" passes — meaning the guide (as fixed by Task 1) contains both new asserted strings.
    - `mix verify.phase91` exits 0, same as before this plan (73 tests or more, 0 failures).
    - `grep -c "def __scrypath__(:fan_outs)" test/scrypath/docs_contract_test.exs` returns at least 1 (the assertion string is present in the test file).
    - `grep -c "does not generate a" test/scrypath/docs_contract_test.exs` returns at least 1 (the prose note string is present in the test file).
    - No new test function is added (the existing test function is extended, not duplicated). `grep -c "test \"related-data guide" test/scrypath/docs_contract_test.exs` returns exactly 1.
  </acceptance_criteria>
  <done>
    The docs-contract test asserts both the hand-written `def __scrypath__(:fan_outs)` accessor and
    the prose note about the macro limitation. `mix verify.phase91` and the docs-contract test suite
    both pass with 0 failures. The broken `use Scrypath, fan_outs:` pattern can no longer be
    silently re-introduced without a failing CI gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide prose -> adopter implementation | Adopter copies schema pattern verbatim; a wrong pattern causes a runtime crash in their app |
| docs_contract_test.exs -> guides/*.md | Test reads guide content at compile time via File.read!; assertion failure reveals guide regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-04-WRONG-PATTERN | Tampering | guides/related-data-and-reindexing.md section (a) | mitigate | Replace broken `use Scrypath, fan_outs:` snippet with working hand-written accessor pattern; add prose note. The docs-contract assertion in Task 2 is the regression gate. |
| T-91-04-RETURN-SHAPE | Tampering | guides/related-data-and-reindexing.md update_author/3 return | mitigate | Fix 2-tuple to 3-tuple `{:ok, result, updated}` matching blog.ex and smoke test assertions. |
| T-91-04-REGRESSION | Repudiation | docs_contract_test.exs canonical-strings list | mitigate | Add `"def __scrypath__(:fan_outs)"` and the prose note string to assert_contains_all so future guide edits that remove the working pattern cause an immediate CI failure. |
| T-91-04-SC | Tampering | npm/pip/cargo installs | n/a | No package installs. Guide and test edits only. |
</threat_model>

<verification>
After both tasks complete, all of the following must hold:

1. `grep -n "def __scrypath__(:fan_outs)" guides/related-data-and-reindexing.md` — at least one match in section (a).
2. `grep -n "__scrypath__(:document_id)" guides/related-data-and-reindexing.md` — at least one match.
3. `grep -n "{:ok, result, updated}" guides/related-data-and-reindexing.md` — at least one match in the update_author/3 block.
4. `grep -c "{:ok, updated}$" guides/related-data-and-reindexing.md` — 0 (bare 2-tuple gone).
5. `mix test test/scrypath/docs_contract_test.exs --only docs_contract -x` — exits 0.
6. `mix verify.phase91` — exits 0, 0 failures (same hermetic gate as before).
7. `mix docs --warnings-as-errors` — exits 0 (guide edits do not break ExDoc).
</verification>

<success_criteria>
- The guide's section (a) teaches the working `def __scrypath__(:fan_outs)` hand-written accessor
  pattern, matching `examples/phoenix_meilisearch/lib/scrypath_demo/blog/author.ex` lines 33-43.
- A prose note explicitly states that `use Scrypath` does not generate `__scrypath__(:fan_outs)`.
- `def __scrypath__(:document_id)` is shown alongside `:fan_outs` (required by the :oban enqueue path).
- The guide's `update_author/3` returns `{:ok, result, updated}` (3-tuple), matching blog.ex and smoke tests.
- The docs-contract test asserts both the accessor pattern and the prose note — the broken pattern cannot silently return.
- `mix verify.phase91` passes with 0 failures.
- VERIFICATION.md gaps CR-01 and WR-02 are closed.
</success_criteria>

<output>
Create `.planning/phases/91-integration-guides-and-verification/91-04-SUMMARY.md` when done.
</output>
