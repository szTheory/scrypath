---
phase: 91-integration-guides-and-verification
plan: 02
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - lib/mix/tasks/verify.phase91.ex
  - test/scrypath/docs_contract_test.exs
  - mix.exs
autonomous: true
requirements: [TEST-01, TEST-02]
tags: [verify, docs-contract, mix-task, related-data]

must_haves:
  truths:
    - "mix verify.phase91 exists, runs the three hermetic related-data tests (no live Meilisearch), then builds docs with warnings-as-errors, and exits 0."
    - "The docs-contract test asserts the related-data guide adopted the canonical sync_related/3 story and forbids the temporary-workaround / first-class-feature strings (D-11, TEST-02)."
    - "The new verify.phase91 task is discoverable: registered via @verify_phase91 in docs_contract_test.exs, wired in mix.exs preferred_envs as :test, and covered by a 'verify.phase91 stays wired' test."
    - "verify.phase91's focused test list contains ONLY hermetic library tests — no example smoke paths (TEST-01 hermetic boundary, Pitfall 4)."
  artifacts:
    - path: "lib/mix/tasks/verify.phase91.ex"
      provides: "Mix.Tasks.Verify.Phase91 — focused hermetic related-data + docs-contract gate"
      contains: "Mix.Tasks.Verify.Phase91"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Inverted related-data assertion (D-11) + @verify_phase91 attr + 'stays wired' test (D-12)"
      contains: "@verify_phase91"
    - path: "mix.exs"
      provides: "preferred_envs registration so verify.phase91 runs under :test"
      contains: "\"verify.phase91\": :test"
  key_links:
    - from: "lib/mix/tasks/verify.phase91.ex"
      to: "test/scrypath/sync/related_test.exs + related_worker_test.exs + docs_contract_test.exs"
      via: "@focused_tests list run in-process via Mix.Task.run(\"test\", ...)"
      pattern: "related_test.exs"
    - from: "test/scrypath/docs_contract_test.exs"
      to: "guides/related-data-and-reindexing.md (canonical strings authored in 91-01)"
      via: "String.contains? substring contract"
      pattern: "Scrypath\\.sync_related/3"
    - from: "test/scrypath/docs_contract_test.exs @verify_phase91"
      to: "lib/mix/tasks/verify.phase91.ex"
      via: "File.read! of the task source"
      pattern: "verify\\.phase91\\.ex"
---

<objective>
Add `Mix.Tasks.Verify.Phase91` (mirroring `verify.phase85`) that runs the three HERMETIC
related-data tests then `mix docs --warnings-as-errors` (D-10, satisfies TEST-01 by *running*
the existing hermetic propagation tests). Invert the existing docs-contract related-data
assertion so it locks the canonical `sync_related/3` story and forbids the
temporary-workaround / first-class-feature framing (D-11, TEST-02). Register the new task
for discoverability (D-12): a `@verify_phase91` attribute, a "verify.phase91 stays wired"
test, and a `"verify.phase91": :test` entry in `mix.exs` preferred_envs.

This plan touches NO library runtime code (`lib/scrypath*` is read-only). It adds one mix
task, edits one test file, and edits `mix.exs` config only.

Purpose: TEST-01 (a gate runs the hermetic tests with no live backend) + TEST-02 (the docs
contract correctly articulates the context-owned-orchestration vs library-owned-execution
boundary and the no-callback-magic invariant).

Output: `lib/mix/tasks/verify.phase91.ex`, edited `docs_contract_test.exs`, edited `mix.exs`.

## DEPENDENCY ON 91-01 (locked coupling)
The inverted docs-contract assertion in this plan asserts LITERAL substrings that must already
exist in `guides/related-data-and-reindexing.md` as authored by 91-01 (Pitfall 2 — no fuzzy
matching). Read 91-01-SUMMARY.md "canonical strings" before writing the assertion. This plan
runs in wave 2 AFTER 91-01 so the guide it asserts against actually exists. Assert exactly the
shared-contract strings: `Scrypath.sync_related/3`, `fan_outs:`, `sync_mode: :inline`,
`sync_mode: :oban`, `callback magic`, plus a boundary phrase the guide literally contains
(`contexts own orchestration` and `library owns execution`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-integration-guides-and-verification/91-CONTEXT.md
@.planning/phases/91-integration-guides-and-verification/91-PATTERNS.md
@.planning/phases/91-integration-guides-and-verification/91-RESEARCH.md
@.planning/phases/91-integration-guides-and-verification/91-01-SUMMARY.md

<interfaces>
<!-- Analog shapes verified on disk 2026-05-25. -->

verify.phase85.ex (lib/mix/tasks/verify.phase85.ex, whole 37-line file) — copy verbatim, swap label + test list:
  defmodule Mix.Tasks.Verify.Phase85 ... @focused_tests [...] run(args): app.start; ensure_no_args!; run_test!(@focused_tests, label); reenable("docs"); Mix.Task.run("docs", ["--warnings-as-errors"])
  Load-bearing: Mix.Task.reenable("test") and reenable("docs") are MANDATORY; tests run in-process via Mix.Task.run("test", args), NOT System.cmd.

docs_contract_test.exs @verify_phaseNN attrs (lines 26-29):
  @verify_phase82 File.read!("lib/mix/tasks/verify.phase82.ex")
  ... @verify_phase85 File.read!("lib/mix/tasks/verify.phase85.ex")    <- add @verify_phase91 immediately after line 29

docs_contract_test.exs "verify.phase85 stays wired" test (lines 333-339) — copy + swap asserted paths:
  assert String.contains?(@verify_phase85, "test/scrypath/composition_test.exs") ... "Mix.Task.run(\"docs\", [\"--warnings-as-errors\"])"
  assert String.contains?(File.read!("mix.exs"), "\"verify.phase85\": :test")

docs_contract_test.exs related-data test to REPLACE (lines 1128-1136):
  test "related-data guide explicitly mentions temporary Oban workaround" do
    guide = @guides["guides/related-data-and-reindexing.md"]
    assert_contains_all(guide, ["temporary workaround", "first-class feature", "Oban"])
  end
  (guide key already registered in @guide_paths line 54 / materialized in @guides — no change there.)

docs_contract_test.exs helpers (read-only, do NOT modify):
  assert_contains_all/2 (lines 1150-1155) — literal String.contains? per snippet.

mix.exs preferred_envs (lines 39-58) — last entry "verify.phase85": :test at line 58; add "verify.phase91": :test after it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Mix.Tasks.Verify.Phase91 + register it in mix.exs preferred_envs</name>
  <files>lib/mix/tasks/verify.phase91.ex, mix.exs</files>
  <read_first>
    - lib/mix/tasks/verify.phase85.ex (the whole 37-line analog to copy verbatim)
    - .planning/phases/91-integration-guides-and-verification/91-PATTERNS.md (section "lib/mix/tasks/verify.phase91.ex (NEW)" — required swaps + load-bearing details; section "mix.exs preferred_envs")
    - mix.exs (the preferred_envs keyword block, lines 39-58 — last entry is "verify.phase85": :test at line 58)
    - test/scrypath/sync/related_test.exs (one of the hermetic focused tests; confirm path)
    - test/scrypath/sync/related_worker_test.exs (one of the hermetic focused tests; confirm path)
  </read_first>
  <action>
    Create `lib/mix/tasks/verify.phase91.ex` by mirroring `lib/mix/tasks/verify.phase85.ex` exactly,
    with these swaps (D-10): module name -> `Mix.Tasks.Verify.Phase91`; `@shortdoc` + the run label string
    -> Phase 91 related-data fan-out + docs-contract wording; `ensure_no_args!/1` raise message ->
    `"verify.phase91 does not accept arguments, got: ..."`; and `@focused_tests` -> EXACTLY these three
    hermetic paths in this order:
      "test/scrypath/sync/related_test.exs"
      "test/scrypath/sync/related_worker_test.exs"
      "test/scrypath/docs_contract_test.exs"
    Keep the structure verbatim: `app.start` -> `ensure_no_args!` -> `run_test!(@focused_tests, label)` ->
    `Mix.Task.reenable("docs")` -> `Mix.Task.run("docs", ["--warnings-as-errors"])`. The `reenable("test")`
    and `reenable("docs")` calls are MANDATORY (app.start consumes the task lock; omitting them silently
    no-ops). Tests run in-process via `Mix.Task.run("test", args)` — do NOT use System.cmd.

    Do NOT add any example smoke test path (anything under `examples/.../test/smoke/`) to `@focused_tests` —
    those require live Meilisearch and run on a separate surface; including them violates the TEST-01
    hermetic boundary (Pitfall 4).

    Then edit `mix.exs`: add `"verify.phase91": :test` to the `preferred_envs` keyword list in `cli/0`,
    immediately after the existing `"verify.phase85": :test` entry (line 58). Without this the task runs
    under :dev and cannot load test files (Pitfall 3).
  </action>
  <verify>
    <automated>mix verify.phase91</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/verify.phase91.ex` exists and defines `defmodule Mix.Tasks.Verify.Phase91`.
    - The file contains all three focused-test paths: `test/scrypath/sync/related_test.exs`, `test/scrypath/sync/related_worker_test.exs`, `test/scrypath/docs_contract_test.exs`.
    - The file contains the literal `Mix.Task.run("docs", ["--warnings-as-errors"])` and both `Mix.Task.reenable("test")` and `Mix.Task.reenable("docs")`.
    - The file contains NO path under `examples/` (TEST-01 hermetic boundary): `grep -c 'examples/' lib/mix/tasks/verify.phase91.ex` returns 0.
    - `mix.exs` contains the literal `"verify.phase91": :test`.
    - `mix verify.phase91` exits 0 (runs the three hermetic tests + `mix docs --warnings-as-errors`). NOTE: this depends on Task 2 having inverted the docs-contract assertion against the 91-01 guide; run Task 2 first or run `mix verify.phase91` only after Task 2 lands.
  </acceptance_criteria>
  <done>`mix verify.phase91` runs the three hermetic tests and builds docs with warnings-as-errors, exiting 0; the task is registered in `mix.exs` preferred_envs.</done>
</task>

<task type="auto">
  <name>Task 2: Invert the related-data docs-contract assertion + register @verify_phase91 + add the "stays wired" test</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - .planning/phases/91-integration-guides-and-verification/91-01-SUMMARY.md (the EXACT canonical strings 91-01 authored into the guide — assert these literally)
    - guides/related-data-and-reindexing.md (the rewritten guide from 91-01 — confirm the asserted strings literally appear before asserting them)
    - test/scrypath/docs_contract_test.exs (lines 26-29 @verify_phaseNN attrs; lines 333-339 "verify.phase85 stays wired" analog; lines 1128-1136 the related-data test to replace; helper assert_contains_all/2 at 1150-1155)
    - .planning/phases/91-integration-guides-and-verification/91-PATTERNS.md (section "test/scrypath/docs_contract_test.exs (EDIT)" — Edit A / Edit B / Edit C)
    - lib/mix/tasks/verify.phase91.ex (created in Task 1 — its source is what @verify_phase91 reads)
  </read_first>
  <action>
    Make three coordinated edits to `test/scrypath/docs_contract_test.exs`:

    EDIT A (D-12) — add `@verify_phase91 File.read!("lib/mix/tasks/verify.phase91.ex")` immediately after
    the `@verify_phase85` attribute (current line 29), matching the existing `@verify_phaseNN` block style.

    EDIT B (D-12) — add a new test `"verify.phase91 stays wired into the focused maintainer flow"` mirroring
    the `verify.phase85 stays wired` test (current lines 333-339). Assert `String.contains?(@verify_phase91, ...)`
    for each of the three focused paths (`test/scrypath/sync/related_test.exs`,
    `test/scrypath/sync/related_worker_test.exs`, `test/scrypath/docs_contract_test.exs`), assert it contains
    the literal `Mix.Task.run("docs", ["--warnings-as-errors"])`, and assert
    `String.contains?(File.read!("mix.exs"), "\"verify.phase91\": :test")`. Place it near the other
    "stays wired" tests.

    EDIT C (D-11, TEST-02) — REPLACE the test at current lines 1128-1136
    (`"related-data guide explicitly mentions temporary Oban workaround"`) with an inverted test
    (suggested name `"related-data guide adopts sync_related/3 as the canonical fan-out story"`) that, using
    `guide = @guides["guides/related-data-and-reindexing.md"]`:
      - `refute String.contains?(guide, "temporary workaround")` (with a D-09 message)
      - `refute String.contains?(guide, "first-class feature")` (with a D-09 message)
      - `assert_contains_all(guide, [...])` over EXACTLY the shared-contract strings from 91-01-SUMMARY.md:
        `"Scrypath.sync_related/3"`, `"fan_outs:"`, `"sync_mode: :inline"`, `"sync_mode: :oban"`,
        `"callback magic"`, `"contexts own orchestration"`, `"library owns execution"`.
        (The last two encode the context-owned-orchestration vs library-owned-execution boundary, TEST-02.)
    Use only the existing read-only helper `assert_contains_all/2`; do NOT modify the helpers. Every asserted
    string MUST literally exist in the 91-01 guide — verify against the guide file before finalizing
    (Pitfall 2). The `@guides`/`@guide_paths` registration already includes the guide key (line 54) — no
    change there.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/scrypath/docs_contract_test.exs` contains `@verify_phase91 File.read!("lib/mix/tasks/verify.phase91.ex")`.
    - The file contains a test named with `verify.phase91 stays wired` that asserts all three focused paths, the `--warnings-as-errors` docs string, and the `"verify.phase91": :test` mix.exs entry.
    - The file NO LONGER contains the old assertion line asserting the guide includes `"temporary workaround"`/`"first-class feature"` (the line-1128 test is replaced): `grep -c 'related-data guide explicitly mentions temporary Oban workaround' test/scrypath/docs_contract_test.exs` returns 0.
    - The inverted test `refute`s both `temporary workaround` and `first-class feature` and `assert_contains_all`s the seven canonical strings.
    - `mix test test/scrypath/docs_contract_test.exs` exits 0 (the inverted assertion passes against the 91-01 guide; the new "stays wired" test passes against the Task 1 task + mix.exs).
  </acceptance_criteria>
  <done>The docs-contract test forbids the two banned strings, asserts the canonical sync_related/3 story + the orchestration/execution boundary, registers `@verify_phase91`, and includes a passing "verify.phase91 stays wired" test; `mix test test/scrypath/docs_contract_test.exs` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer CLI -> local test runner | `mix verify.phase91` runs trusted, in-repo hermetic tests + `mix docs` locally. No network, no untrusted input, no live backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-02-ARG | Tampering | verify.phase91 task args | mitigate | `ensure_no_args!/1` raises on any argument (mirrors verify.phase85), preventing accidental arg injection into the in-process test runner. |
| T-91-02-LIVE | Information Disclosure / availability | TEST-01 hermetic boundary | mitigate | `@focused_tests` contains ONLY hermetic library tests; example smoke paths (live Meilisearch) are explicitly excluded (Pitfall 4), so the gate never reaches a live service or credential. Acceptance criterion asserts no `examples/` path. |
| T-91-02-DRIFT | Repudiation (contract integrity) | docs-contract substring assertions | mitigate | The inverted assertion + "stays wired" test lock the canonical strings and task discoverability; substring drift fails the gate (this is the intended control, not a vulnerability). |
| T-91-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan (no new deps; ExUnit/ex_doc already present). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `mix verify.phase91` exits 0 (Task 1 + Task 2 together).
- `mix test test/scrypath/docs_contract_test.exs` exits 0.
- `grep -q '"verify.phase91": :test' mix.exs` succeeds.
- `grep -q '@verify_phase91' test/scrypath/docs_contract_test.exs` succeeds.
- Confirm no `examples/` smoke path was added to `@focused_tests` in `verify.phase91.ex`.
</verification>

<success_criteria>
- TEST-01: `mix verify.phase91` runs `related_test.exs`, `related_worker_test.exs`, `docs_contract_test.exs` (all hermetic) + `mix docs --warnings-as-errors`, exits 0, touches no live backend.
- TEST-02: the docs-contract test forbids the banned strings, asserts the canonical sync_related/3 story, and asserts the context-owned-orchestration vs library-owned-execution boundary + no-callback-magic invariant.
- The new task is discoverable: `@verify_phase91` attr + "stays wired" test + `mix.exs` preferred_envs registration.
- No `lib/scrypath*` runtime file was modified.
</success_criteria>

<output>
Create `.planning/phases/91-integration-guides-and-verification/91-02-SUMMARY.md` when done.
</output>
