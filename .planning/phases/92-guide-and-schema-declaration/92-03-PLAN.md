---
phase: "92"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/multitenancy.md
  - mix.exs
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - TNNT-01

must_haves:
  truths:
    - "A developer can read `guides/multitenancy.md` and understand the shared-index model, the filter merge order footgun (with working wrong/correct examples), when tenant tokens apply, and how `tenant_field:` works with `search_document/1`"
    - "`guides/multitenancy.md` is discoverable via ExDoc — registered in both `extras:` and the Getting Started `groups_for_extras` group"
    - "The filter footgun examples use the exact `Keyword.merge` last-key-wins failure pattern, not a strawman"
    - "The guide never mentions `tenant_scope:` or forward-references Phase 93"
    - "Docs-contract test asserts required section anchors exist in the guide and that mix.exs registers it"
  artifacts:
    - path: "guides/multitenancy.md"
      provides: "Canonical multitenancy guide with all 6 required sections per D-12"
      contains: "Keyword.merge"
    - path: "mix.exs"
      provides: "`guides/multitenancy.md` added to both `extras:` and Getting Started `groups_for_extras`"
      contains: "guides/multitenancy.md"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Guide anchor test and ExDoc registration test for multitenancy.md"
      contains: "multitenancy"
  key_links:
    - from: "mix.exs extras:"
      to: "guides/multitenancy.md"
      via: "string path entry in extras list"
      pattern: "multitenancy.md"
    - from: "mix.exs groups_for_extras Getting Started"
      to: "guides/multitenancy.md"
      via: "string path entry after related-data-and-reindexing.md"
      pattern: "multitenancy.md"
    - from: "test/scrypath/docs_contract_test.exs"
      to: "guides/multitenancy.md"
      via: "File.read! at module attribute, anchor assertions"
      pattern: "multitenancy"
---

<objective>
Write the canonical `guides/multitenancy.md` guide and register it in ExDoc; add docs-contract test coverage.

Purpose: TNNT-01 requires that a developer can follow this guide to implement tenant-safe search correctly in a Phoenix SaaS app — without reading source code. The guide is the primary defense against the filter merge order footgun, the most dangerous silent data-leak failure mode in multitenancy. It also documents what `tenant_field:` auto-injection guarantees for `search_document/1` schemas. The guide and ExDoc registration are co-delivered with the schema declaration (Plans 01 and 02) so that adopters encounter both the declaration and its documentation together.

Output: `guides/multitenancy.md` with all 6 required D-12 sections; `mix.exs` updated for ExDoc; docs-contract test added to `docs_contract_test.exs`.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/scrypath/.planning/PROJECT.md
@/Users/jon/projects/scrypath/.planning/ROADMAP.md
@/Users/jon/projects/scrypath/.planning/STATE.md
@/Users/jon/projects/scrypath/.planning/phases/92-guide-and-schema-declaration/92-CONTEXT.md
@/Users/jon/projects/scrypath/.planning/phases/92-guide-and-schema-declaration/92-RESEARCH.md
@/Users/jon/projects/scrypath/.planning/phases/92-guide-and-schema-declaration/92-PATTERNS.md

<interfaces>
From mix.exs (ExDoc registration — read before editing):
  extras: list currently ends near "guides/related-data-and-reindexing.md" then continues with other entries.
  groups_for_extras "Getting Started" group ends with "guides/common-mistakes.md".
  Insert "guides/multitenancy.md" in BOTH extras: (after "guides/related-data-and-reindexing.md")
  and groups_for_extras Getting Started (after "guides/related-data-and-reindexing.md", before "guides/common-mistakes.md").

From test/scrypath/docs_contract_test.exs (anchor test pattern — read before editing):
  @guide_paths module attribute lists guide file paths — add "guides/multitenancy.md".
  Existing anchor tests use String.contains?(@module_attr, "anchor text") assertions.
  New @multitenancy_guide = File.read!("guides/multitenancy.md") module attribute.

Guide style targets:
  guides/related-data-and-reindexing.md — most recent guide; match structure/depth/tone (read before writing)
  guides/common-mistakes.md — wrong/correct example format (read before writing footgun section)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write guides/multitenancy.md with all 6 required sections</name>
  <files>guides/multitenancy.md</files>
  <read_first>
    - guides/related-data-and-reindexing.md — read the full file for structure, depth, tone, and opening paragraph style (new guide must match)
    - guides/common-mistakes.md — read the wrong/correct example format (lines 1–50 minimum) for the footgun section
    - guides/phoenix-contexts.md — read for context-layer pattern used in code examples
  </read_first>
  <action>
    Create `guides/multitenancy.md` as a new file. All six sections below are REQUIRED (D-12). Do not add sections outside this list. Do not mention `tenant_scope:` or Phase 93.

    Section 1 — Shared-index model (overview):
    - Explain that Scrypath's default model is a shared index filtered per tenant, not per-tenant indexes
    - Explain WHY per-tenant indexes are not the default: Meilisearch processes index tasks sequentially, so N tenants sharing an index get O(1) indexing throughput; N separate indexes get O(N) write contention at scale — specifically state this throughput reason
    - One short paragraph, no code

    Section 2 — The correct pattern: explicit tenant parameter (context-layer):
    - Show a context module function `search_posts_for_tenant(query, tenant_id, opts \\ [])` calling `Scrypath.search(Post, query, filter: [tenant_id: tenant_id], ...)` with `tenant_id` as an explicit parameter
    - State explicitly: tenant_id must be an explicit function parameter — NEVER extracted from conn, plug assigns, or the process dictionary; explain that Task.async, assign_async, and Oban workers do not inherit process dictionary values
    - Show the `use Scrypath` declaration with `tenant_field: :tenant_id` alongside it

    Section 3 — Filter merge order footgun (wrong/correct examples):
    - Label per D-10: `## ❌ Wrong — tenant filter silently dropped`
    - Show the exact `Keyword.merge(base_opts, [filter: [tenant_id: tenant_id]])` failure from PATTERNS.md: base_opts contains `filter: [status: "published"]`, the merge drops it silently, comment explains why (Keyword.merge last-key-wins), note that no error is raised — wrong results appear silently
    - Then `## ✅ Correct — explicit AND-combination` showing `filter: [tenant_id: tenant_id, status: "published"]` in a single list
    - Inline explanation of WHY Keyword.merge silently drops the first filter key

    Section 4 — Meilisearch tenant tokens:
    - Explain that Meilisearch tenant tokens are for browser-direct search only (the browser calls Meilisearch directly without going through the server)
    - State explicitly: server-side Scrypath search uses `filter:` with the tenant field — tenant tokens are NOT used with `Scrypath.search/3`
    - Link to Joken hex docs or the Meilisearch tenant token docs for the generation recipe — note "a Joken recipe for token generation belongs in the host app, not in Scrypath"
    - Keep this section short (2–3 paragraphs)

    Section 5 — `search_document/1` custom hook edge case:
    - Show a code example of a schema with `tenant_field: :tenant_id` and a `search_document/1` that FORGETS to include `:tenant_id` in its return map
    - Explain what happens without Scrypath's post-hook merge: tenant field missing from indexed doc → empty results for entire tenant with no error raised
    - Explain Scrypath's guarantee: even if `search_document/1` omits the tenant field, Scrypath injects it from the source record after the hook returns — "declare once, it works correctly"
    - Note the no-op behavior: if `search_document/1` already includes the tenant field, the merge does not overwrite it

    Section 6 — Schema declaration reference:
    - Concise code example showing `use Scrypath` with `fields:`, `filterable:`, `tenant_field:`, and a brief note on the compile-time auto-injection behavior (IO.warn advisory when field not in fields:)
    - Keep this as a quick reference, not a tutorial repeat

    Format rules:
    - Use the same heading depth as related-data-and-reindexing.md (H1 title, H2 sections)
    - Open with one or two sentences framing the problem this guide solves, then the "if you remember one thing" anchor sentence
    - Code blocks use ```elixir fences
    - No forward references to Phase 93, tenant_scope:, or schema_capabilities/1
  </action>
  <verify>
    <automated>test -f guides/multitenancy.md && grep -c "Keyword.merge" guides/multitenancy.md</automated>
  </verify>
  <done>
    - `guides/multitenancy.md` exists and is non-empty
    - `grep -c "Keyword.merge" guides/multitenancy.md` returns >= 1
    - `grep -c "tenant token" guides/multitenancy.md` returns >= 1
    - `grep -c "search_document" guides/multitenancy.md` returns >= 1
    - `grep -c "tenant_field" guides/multitenancy.md` returns >= 3
    - The string "tenant_scope" does NOT appear in the file: `grep -c "tenant_scope" guides/multitenancy.md` returns 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register guide in mix.exs ExDoc and add docs-contract test</name>
  <files>mix.exs, test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - mix.exs — read the `docs/0` function; identify the exact position of `"guides/related-data-and-reindexing.md"` in both `extras:` and `groups_for_extras` "Getting Started" — insert `"guides/multitenancy.md"` immediately after it in both locations
    - test/scrypath/docs_contract_test.exs — read the full file; note `@guide_paths` module attribute list, the module attribute + File.read! pattern for existing guide tests, and anchor assertion style
  </read_first>
  <behavior>
    - Test: `guides/multitenancy.md` file exists and is readable (File.read! at module attribute compile time — test fails at compile if file missing)
    - Test: guide contains required anchor strings: "shared-index", "Keyword.merge", "tenant token", "search_document", "tenant_field"
    - Test: `mix.exs` contains `"guides/multitenancy.md"` (ExDoc registration assertion)
  </behavior>
  <action>
    In mix.exs `docs/0` function:
    1. In `extras:` list, add `"guides/multitenancy.md"` immediately after `"guides/related-data-and-reindexing.md"`
    2. In `groups_for_extras` "Getting Started" list, add `"guides/multitenancy.md"` immediately after `"guides/related-data-and-reindexing.md"` and before `"guides/common-mistakes.md"`

    In test/scrypath/docs_contract_test.exs:
    3. Add `"guides/multitenancy.md"` to the `@guide_paths` module attribute list
    4. Add `@multitenancy_guide File.read!("guides/multitenancy.md")` module attribute (alongside existing guide File.read! attributes)
    5. Add a test `"multitenancy guide contains required section anchors (TNNT-01)"` that asserts all five required anchor strings are present: `"shared-index"`, `"Keyword.merge"`, `"tenant token"`, `"search_document"`, `"tenant_field"`
    6. Add a test `"multitenancy guide is registered in ExDoc extras and Getting Started group (TNNT-01)"` that reads mix.exs (using `@mix_exs` if already defined, or `File.read!("mix.exs")` inline) and asserts it contains `~s("guides/multitenancy.md")`
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs --no-start</automated>
  </verify>
  <done>
    - `mix test test/scrypath/docs_contract_test.exs` exits 0
    - `grep -c "multitenancy.md" mix.exs` returns >= 2 (one in extras:, one in groups_for_extras)
    - `grep -c "multitenancy" test/scrypath/docs_contract_test.exs` returns >= 3 (module attr + two tests)
    - `mix test` (full suite) exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide content → adopter understanding | Developer reads the guide and implements tenant filtering in their app — the guide is the trust boundary; it must make the correct pattern obvious and the failure mode impossible to miss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-20 | Information Disclosure | Filter merge order footgun in guide examples | mitigate | Guide explicitly shows the exact Keyword.merge failure with working wrong/correct code examples (D-10); prominent labeling per D-08; correct pattern shown first per D-08 |
| T-92-21 | Information Disclosure | Tenant ID extraction from conn/process dict in guide examples | mitigate | Guide examples use explicit `tenant_id` function parameter only (D-11); async boundary rationale stated inline |
| T-92-22 | Information Disclosure | Tenant token misuse (server-side) | mitigate | Guide section 4 explicitly states tenant tokens are browser-direct only and are NOT used with Scrypath.search/3 |
| T-92-23 | Elevation of Privilege | Guide forward-referencing tenant_scope: enforcement before it ships | accept | D-09 explicitly forbids Phase 93 forward-references; docs-contract test cannot catch this (content judgment), but the constraint is documented in CONTEXT.md and review will catch it |
| T-92-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
- `guides/multitenancy.md` exists with all 6 required sections
- `grep -c "Keyword.merge" guides/multitenancy.md` >= 1
- `grep -c "tenant_scope" guides/multitenancy.md` == 0 (no forward reference per D-09)
- `grep -c "multitenancy.md" mix.exs` >= 2
- `mix test test/scrypath/docs_contract_test.exs` exits 0
- `mix test` exits 0
</verification>

<success_criteria>
- `guides/multitenancy.md` exists with all 6 required D-12 sections: shared-index model, correct context-layer pattern, filter footgun (wrong/correct), tenant tokens, search_document/1 edge case, schema declaration reference
- Guide registered in ExDoc `extras:` and "Getting Started" `groups_for_extras`
- Docs-contract test asserts guide anchors and ExDoc registration — green
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/92-guide-and-schema-declaration/92-03-SUMMARY.md` when done
</output>
