---
phase: 98
plan: 4
type: execute
wave: 3
depends_on:
  - 98-01
  - 98-02
  - 98-03
files_modified:
  - lib/mix/tasks/verify.phase98.ex
  - test/mix/tasks/verify.phase98_test.exs
  - test/scrypath/phase98_contract_test.exs
  - test/scrypath/readiness_contract_test.exs
  - test/scrypath/docs_contract_test.exs
  - test/mix/tasks/workflow_wiring_test.exs
  - mix.exs
  - CONTRIBUTING.md
autonomous: true
requirements:
  - PROOF-01
  - PROOF-02
  - PROOF-03
  - SUP-01
  - SUP-02
must_haves:
  - "Create a service-free `mix verify.phase98` gate that enforces phase-98 contract surfaces."
  - "Wire gate task + tests into CLI preferred env and workflow wiring checks."
  - "Keep assertions bounded to high-risk proof/support surfaces; do not introduce generic docs-policing."
---

<objective>
Convert the reconciled Phase 98 contract surfaces into a durable, focused verification seam that fails on real drift without adding CI noise or runtime scope.
</objective>

<context>
This plan is the enforcement layer for PROOF/SUP requirements. It should mirror the phase97 gate pattern, stay deterministic and service-free, and lock only high-risk phase98 surfaces.
</context>

<tasks>
### Task 98-04-01 - Implement `Mix.Tasks.Verify.Phase98` with focused test list
<read_first>
- `lib/mix/tasks/verify.phase97.ex`
- `lib/mix/tasks/verify.adopter.ex`
- `.planning/phases/98-surface-reconciliation-and-adopter-flow-clarity/98-PATTERNS.md`
- `.planning/phases/98-surface-reconciliation-and-adopter-flow-clarity/98-VALIDATION.md`
</read_first>
<action>
- Create `lib/mix/tasks/verify.phase98.ex` by following the structure used in `verify.phase97`:
  - `Mix.Task.run("app.start")`,
  - no-argument guard (`ensure_no_args!/1`),
  - focused test invocation using an `@focused_tests` list,
  - `mix docs --warnings-as-errors`.
- Set `@focused_tests` to include all phase-98 trust files:
  - `test/scrypath/phase98_contract_test.exs`
  - `test/scrypath/readiness_contract_test.exs`
  - `test/scrypath/docs_contract_test.exs`
  - `test/mix/tasks/verify.phase98_test.exs`
- Include a stable shell marker line containing `verify.phase98` for debugging and CI logs.
</action>
<acceptance_criteria>
- `test -f "lib/mix/tasks/verify.phase98.ex"` exits 0.
- `rg "defmodule Mix\\.Tasks\\.Verify\\.Phase98" "lib/mix/tasks/verify.phase98.ex"` returns 1 match.
- `rg "ensure_no_args!|@focused_tests|verify\\.phase98|Mix\\.Task\\.run\\(\"docs\", \\[\"--warnings-as-errors\"\\]\\)" "lib/mix/tasks/verify.phase98.ex"` returns required matches.
- `rg "test/scrypath/phase98_contract_test\\.exs|test/scrypath/readiness_contract_test\\.exs|test/scrypath/docs_contract_test\\.exs|test/mix/tasks/verify\\.phase98_test\\.exs" "lib/mix/tasks/verify.phase98.ex"` returns 4 matches.
</acceptance_criteria>
<verify>
- `test -f "lib/mix/tasks/verify.phase98.ex"`
- `rg "defmodule Mix\\.Tasks\\.Verify\\.Phase98" "lib/mix/tasks/verify.phase98.ex"`
- `rg "ensure_no_args!|@focused_tests|verify\\.phase98|Mix\\.Task\\.run\\(\"docs\", \\[\"--warnings-as-errors\"\\]\\)" "lib/mix/tasks/verify.phase98.ex"`
- `rg "test/scrypath/phase98_contract_test\\.exs|test/scrypath/readiness_contract_test\\.exs|test/scrypath/docs_contract_test\\.exs|test/mix/tasks/verify\\.phase98_test\\.exs" "lib/mix/tasks/verify.phase98.ex"`
</verify>

### Task 98-04-02 - Add task tests and workflow wiring coverage
<read_first>
- `test/mix/tasks/verify.phase97_test.exs`
- `test/mix/tasks/workflow_wiring_test.exs`
- `mix.exs`
- `lib/mix/tasks/verify.phase98.ex`
</read_first>
<action>
- Create `test/mix/tasks/verify.phase98_test.exs` mirroring phase97 task-test shape:
  - asserts arg rejection message for stray args,
  - asserts source markers for each focused test path,
  - asserts docs command marker.
- Update `mix.exs` `cli().preferred_envs` with `:"verify.phase98" => :test`.
- Extend `test/mix/tasks/workflow_wiring_test.exs` with assertion: `envs[:"verify.phase98"] == :test`.
</action>
<acceptance_criteria>
- `test -f "test/mix/tasks/verify.phase98_test.exs"` exits 0.
- `rg "verify\\.phase98|does not accept arguments|capture_io" "test/mix/tasks/verify.phase98_test.exs"` returns required matches.
- `rg "verify\\.phase98" "mix.exs"` returns at least 1 match.
- `rg "envs\\[:\"verify\\.phase98\"\\] == :test" "test/mix/tasks/workflow_wiring_test.exs"` returns 1 match.
- `mix test test/mix/tasks/verify.phase98_test.exs test/mix/tasks/workflow_wiring_test.exs` exits 0.
</acceptance_criteria>
<verify>
- `test -f "test/mix/tasks/verify.phase98_test.exs"`
- `rg "verify\\.phase98|does not accept arguments|capture_io" "test/mix/tasks/verify.phase98_test.exs"`
- `rg "verify\\.phase98" "mix.exs"`
- `rg "envs\\[:\"verify\\.phase98\"\\] == :test" "test/mix/tasks/workflow_wiring_test.exs"`
- `mix test test/mix/tasks/verify.phase98_test.exs test/mix/tasks/workflow_wiring_test.exs`
</verify>

### Task 98-04-03 - Add focused phase-98 contract assertions and document gate usage
<read_first>
- `test/scrypath/readiness_contract_test.exs`
- `test/scrypath/docs_contract_test.exs`
- `CONTRIBUTING.md`
- `README.md`
- `guides/support-and-compatibility.md`
- `guides/outside-adopter-intake.md`
- `docs/templates/outside-adopter-evidence.md`
</read_first>
<action>
- Create `test/scrypath/phase98_contract_test.exs` with token-based assertions that cover:
  - PROOF discoverability (`mix verify.adopter`, `mix verify.adopter --live`),
  - live env prerequisites (`SCRYPATH_EXAMPLE_INTEGRATION`, `PGPORT`, `SCRYPATH_MEILISEARCH_URL`),
  - intake/escalation tokens (Class A-D, findings buckets, required evidence headings).
- Keep `test/scrypath/readiness_contract_test.exs` and `test/scrypath/docs_contract_test.exs` changes minimal and additive; avoid moving existing assertions unless necessary for clarity.
- Update CONTRIBUTING verification section with a Phase 98 gate entry: when to run `mix verify.phase98`, what it protects, and that it remains trust-hardening scope only.
</action>
<acceptance_criteria>
- `test -f "test/scrypath/phase98_contract_test.exs"` exits 0.
- `rg "mix verify\\.adopter|mix verify\\.adopter --live|SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "test/scrypath/phase98_contract_test.exs"` returns at least 5 matches.
- `rg "Class A|Class B|Class C|Class D|Bug in Scrypath|Doc or Contract Gap|App-Side Error|Environment Failure" "test/scrypath/phase98_contract_test.exs"` returns at least 8 matches.
- `rg "mix verify\\.phase98" "CONTRIBUTING.md"` returns at least 1 match.
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/phase98_contract_test.exs" "test/scrypath/readiness_contract_test.exs" "test/scrypath/docs_contract_test.exs"` exits 0.
- `mix test test/scrypath/phase98_contract_test.exs test/scrypath/readiness_contract_test.exs test/scrypath/docs_contract_test.exs` exits 0.
- `mix verify.phase98` exits 0.
</acceptance_criteria>
<verify>
- `test -f "test/scrypath/phase98_contract_test.exs"`
- `rg "mix verify\\.adopter|mix verify\\.adopter --live|SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "test/scrypath/phase98_contract_test.exs"`
- `rg "Class A|Class B|Class C|Class D|Bug in Scrypath|Doc or Contract Gap|App-Side Error|Environment Failure" "test/scrypath/phase98_contract_test.exs"`
- `rg "mix verify\\.phase98" "CONTRIBUTING.md"`
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/phase98_contract_test.exs" "test/scrypath/readiness_contract_test.exs" "test/scrypath/docs_contract_test.exs"`
- `mix test test/scrypath/phase98_contract_test.exs test/scrypath/readiness_contract_test.exs test/scrypath/docs_contract_test.exs`
- `mix verify.phase98`
</verify>
</tasks>

<threat_model>
- **T98-P4-001 (Coverage Bypass, High, ASVS-L1 V1.1):** reconciled docs drift after merge because no enforceable phase gate exists.  
  **Mitigation:** implement `mix verify.phase98`, wire preferred env, and assert task presence in workflow wiring tests.  
  **Detection:** `rg "verify\\.phase98" "lib/mix/tasks/verify.phase98.ex" "mix.exs" "test/mix/tasks/verify.phase98_test.exs" "test/mix/tasks/workflow_wiring_test.exs"`.
- **T98-P4-002 (Integrity, High, ASVS-L1 V5.1):** contract tests become brittle prose snapshots and fail on non-semantic edits.  
  **Mitigation:** use token and order assertions only; ban snapshot helpers in phase-98 contract tests.  
  **Detection:** `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/phase98_contract_test.exs" "test/scrypath/docs_contract_test.exs"`.
- **T98-P4-003 (Scope Creep, High, ASVS-L1 V14.2):** verification gate adds runtime feature tests beyond Phase 98 contract-hardening boundary.  
  **Mitigation:** restrict `@focused_tests` to proof/support/intake contract suites and task wiring tests only.  
  **Detection:** `rg "@focused_tests|phase98_contract_test|readiness_contract_test|docs_contract_test" "lib/mix/tasks/verify.phase98.ex"`.
</threat_model>

<verification>
- `mix test test/mix/tasks/verify.phase98_test.exs`
- `mix test test/mix/tasks/workflow_wiring_test.exs`
- `mix test test/scrypath/phase98_contract_test.exs test/scrypath/readiness_contract_test.exs test/scrypath/docs_contract_test.exs`
- `mix verify.phase98`
</verification>

<success_criteria>
- `mix verify.phase98` exists, is wired, and runs a bounded service-free phase gate.
- Focused tests protect PROOF and SUP contract tokens across high-risk surfaces.
- CONTRIBUTING documents when to run the gate and what contract scope it enforces.
- Phase 98 enforcement is durable without introducing runtime breadth checks.
</success_criteria>

<output>
- `lib/mix/tasks/verify.phase98.ex`
- `test/mix/tasks/verify.phase98_test.exs`
- `test/scrypath/phase98_contract_test.exs`
- Updated `mix.exs`
- Updated `test/mix/tasks/workflow_wiring_test.exs`
- Updated `test/scrypath/readiness_contract_test.exs`
- Updated `test/scrypath/docs_contract_test.exs`
- Updated `CONTRIBUTING.md`
</output>
