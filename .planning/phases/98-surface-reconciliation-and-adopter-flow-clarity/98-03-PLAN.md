---
phase: 98
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/outside-adopter-intake.md
  - docs/templates/outside-adopter-evidence.md
autonomous: true
requirements:
  - SUP-01
  - SUP-02
must_haves:
  - "Define deterministic required evidence fields in intake guidance and mirror them in the evidence template."
  - "Add explicit class-to-routing and finding-to-action triage guidance for maintainers."
  - "Keep Phase 98 bounded to support/intake contract hardening; no runtime behavior expansion."
---

<objective>
Harden outside-adopter intake and escalation semantics so maintainers can classify submissions consistently and request missing data without ambiguous back-and-forth.
</objective>

<context>
Phase 98 requires actionable intake/evidence language. This plan updates intake policy text and template structure as a synchronized pair so the documented contract is executable by maintainers.
</context>

<tasks>
### Task 98-03-01 - Tighten intake evidence contract with mandatory fields
<read_first>
- `guides/outside-adopter-intake.md`
- `docs/templates/outside-adopter-evidence.md`
- `.planning/REQUIREMENTS.md`
- `.planning/phases/98-surface-reconciliation-and-adopter-flow-clarity/98-CONTEXT.md`
</read_first>
<action>
- Update `guides/outside-adopter-intake.md` section `## Submitting an Evidence Bundle` to use mandatory language (`must include`) instead of advisory phrasing.
- Require the following fields as literal checklist items:
  1. Environment matrix,
  2. Scrypath ref or Hex version,
  3. Proof path and sync mode,
  4. Ordered commands,
  5. Expected vs actual outcome,
  6. First failure point,
  7. Supporting logs.
- Ensure wording states that incomplete bundles are classified as Class D and returned for clarification.
</action>
<acceptance_criteria>
- `rg "must include|required evidence" "guides/outside-adopter-intake.md"` returns at least 1 match.
- `rg "Environment matrix|Scrypath Ref or Hex version|Chosen proof path|Sync mode|Ordered commands|Expected versus actual|First failure point|Supporting logs" "guides/outside-adopter-intake.md"` returns at least 8 matches.
- `rg "Class D|Returned for clarification|missing context|incomplete evidence" "guides/outside-adopter-intake.md"` returns at least 2 matches.
</acceptance_criteria>
<verify>
- `rg "must include|required evidence" "guides/outside-adopter-intake.md"`
- `rg "Environment matrix|Scrypath Ref or Hex version|Chosen proof path|Sync mode|Ordered commands|Expected versus actual|First failure point|Supporting logs" "guides/outside-adopter-intake.md"`
- `rg "Class D|clarification|incomplete evidence" "guides/outside-adopter-intake.md"`
</verify>

### Task 98-03-02 - Add explicit escalation routing matrix and carve-outs
<read_first>
- `guides/outside-adopter-intake.md`
- `guides/support-and-compatibility.md`
- `.planning/phases/98-surface-reconciliation-and-adopter-flow-clarity/98-RESEARCH.md`
</read_first>
<action>
- Add one triage routing section in `guides/outside-adopter-intake.md` that maps each finding bucket to maintainer action:
  - `Bug in Scrypath` -> open/route bugfix issue with repro references.
  - `Doc or Contract Gap` -> open docs correction issue linked to offending surface.
  - `App-Side Error` -> respond with correction guidance and close as user-integration issue.
  - `Environment Failure` -> request environment fix and rerun evidence path.
- Add explicit "Needs information" response text for missing logs/steps (Class D flow).
- Add a security carve-out sentence directing vulnerability reports away from public issue details and toward `SECURITY.md`.
</action>
<acceptance_criteria>
- `rg "Bug in Scrypath|Doc or Contract Gap|App-Side Error|Environment Failure" "guides/outside-adopter-intake.md"` returns at least 4 matches.
- `rg "Needs information|missing logs|missing ordered commands|Class D" "guides/outside-adopter-intake.md"` returns at least 2 matches.
- `rg "SECURITY\\.md|security report|vulnerability" "guides/outside-adopter-intake.md"` returns at least 1 match.
</acceptance_criteria>
<verify>
- `rg "Bug in Scrypath|Doc or Contract Gap|App-Side Error|Environment Failure" "guides/outside-adopter-intake.md"`
- `rg "Needs information|missing logs|Class D" "guides/outside-adopter-intake.md"`
- `rg "SECURITY\\.md|security report|vulnerability" "guides/outside-adopter-intake.md"`
</verify>

### Task 98-03-03 - Mirror intake contract exactly in evidence template
<read_first>
- `docs/templates/outside-adopter-evidence.md`
- `guides/outside-adopter-intake.md`
</read_first>
<action>
- Update `docs/templates/outside-adopter-evidence.md` headings so they 1:1 match intake required fields and ordering.
- Keep a dedicated `## Maintainer Review Block` that includes:
  - `Classification` (Class A/B/C/D),
  - `Findings`,
  - `Action`.
- Add a short template note stating that omitted required sections can be returned as "needs information."
</action>
<acceptance_criteria>
- `rg "^## Adopter Context|^## Environment Matrix|^## Scrypath Ref or Hex version|^## Chosen Proof Path|^## Sync Mode|^## Ordered Commands|^## Expected versus Actual Outcome|^## First Failure/Confusion Point|^## Supporting Logs" "docs/templates/outside-adopter-evidence.md"` returns 9 matches.
- `rg "Classification|Class A|Class B|Class C|Class D|Findings|Action" "docs/templates/outside-adopter-evidence.md"` returns at least 7 matches.
- `rg "needs information|missing required sections" "docs/templates/outside-adopter-evidence.md"` returns at least 1 match.
</acceptance_criteria>
<verify>
- `rg "^## Adopter Context|^## Environment Matrix|^## Scrypath Ref or Hex version|^## Chosen Proof Path|^## Sync Mode|^## Ordered Commands|^## Expected versus Actual Outcome|^## First Failure/Confusion Point|^## Supporting Logs" "docs/templates/outside-adopter-evidence.md"`
- `rg "Classification|Class A|Class B|Class C|Class D|Findings|Action" "docs/templates/outside-adopter-evidence.md"`
- `rg "needs information|missing required sections" "docs/templates/outside-adopter-evidence.md"`
</verify>
</tasks>

<threat_model>
- **T98-P3-001 (Integrity, High, ASVS-L1 V5.3):** maintainers triage incomplete evidence as valid and misclassify root cause, producing false roadmap signals.  
  **Mitigation:** enforce deterministic required fields and explicit Class D "needs information" handling in intake + template.  
  **Detection:** `rg "must include|Class D|Needs information|Ordered commands|First failure point|Supporting logs" "guides/outside-adopter-intake.md" "docs/templates/outside-adopter-evidence.md"`.
- **T98-P3-002 (Information Disclosure, High, ASVS-L1 V9.1):** vulnerability details are posted publicly through intake flow.  
  **Mitigation:** add security-report carve-out directing sensitive issues to `SECURITY.md`.  
  **Detection:** `rg "SECURITY\\.md|security report|vulnerability" "guides/outside-adopter-intake.md"`.
- **T98-P3-003 (Repudiation, Medium, ASVS-L1 V7.2):** class labels exist but maintainer actions are undefined, creating inconsistent responses.  
  **Mitigation:** include explicit finding-bucket-to-action mapping in intake guide.  
  **Detection:** `rg "Bug in Scrypath|Doc or Contract Gap|App-Side Error|Environment Failure" "guides/outside-adopter-intake.md"`.
</threat_model>

<verification>
- `mix test test/scrypath/docs_contract_test.exs`
- `rg "Class A|Class B|Class C|Class D" "guides/outside-adopter-intake.md" "docs/templates/outside-adopter-evidence.md"`
- `rg "must include|Ordered commands|Expected versus actual|First failure point" "guides/outside-adopter-intake.md"`
</verification>

<success_criteria>
- Intake guide defines a mandatory evidence bundle with deterministic fields.
- Escalation and findings routing are explicit, including Class D follow-up behavior.
- Evidence template mirrors intake requirements exactly, including maintainer review block.
- Security-report carve-out is documented and discoverable.
</success_criteria>

<output>
- Updated `guides/outside-adopter-intake.md`
- Updated `docs/templates/outside-adopter-evidence.md`
</output>
