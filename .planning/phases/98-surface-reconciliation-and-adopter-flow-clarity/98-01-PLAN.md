---
phase: 98
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/support-and-compatibility.md
  - README.md
  - CONTRIBUTING.md
autonomous: true
requirements:
  - PROOF-01
  - PROOF-02
must_haves:
  - "Keep `guides/support-and-compatibility.md` as canonical policy owner for proof boundary text."
  - "Make `mix verify.adopter` and `mix verify.adopter --live` discoverable in one hop from README and CONTRIBUTING."
  - "Preserve contract-surface scope only; do not add runtime features, backend breadth, or new runtime API categories."
---

<objective>
Reconcile the canonical proof boundary language across support, README, and CONTRIBUTING so maintainers can find the proof command family quickly and interpret fast-vs-live behavior consistently.
</objective>

<context>
Phase 98 hardens adopter-facing contract surfaces. This plan sets authoritative wording first, then applies concise micro-contract restatements in entry surfaces without duplicating policy bodies.
</context>

<tasks>
### Task 98-01-01 - Normalize canonical proof boundary wording in support guide
<read_first>
- `.planning/phases/98-surface-reconciliation-and-adopter-flow-clarity/98-CONTEXT.md`
- `.planning/phases/98-surface-reconciliation-and-adopter-flow-clarity/98-RESEARCH.md`
- `.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-CONTRACT-STATEMENTS.md`
- `guides/support-and-compatibility.md`
- `lib/mix/tasks/verify.adopter.ex`
</read_first>
<action>
- Update `guides/support-and-compatibility.md` section `## Maintainer proof command family` to include exactly two bullets, in this order:
  1. `mix verify.adopter` as the fast, service-free contract check.
  2. `mix verify.adopter --live` as prerequisite-bound live proof with required env vars.
- In the live bullet, include all required env var tokens as literal code: `SCRYPATH_EXAMPLE_INTEGRATION`, `PGPORT`, `SCRYPATH_MEILISEARCH_URL`.
- Add one sentence that states the live command executes `cd examples/phoenix_meilisearch && mix deps.get && mix test`.
- Keep this section policy-focused; do not add Docker startup instructions or runtime feature claims.
</action>
<acceptance_criteria>
- `rg "mix verify\\.adopter|mix verify\\.adopter --live" "guides/support-and-compatibility.md"` returns at least 2 matches.
- `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "guides/support-and-compatibility.md"` returns 3 matches.
- `rg "cd examples/phoenix_meilisearch && mix deps.get && mix test" "guides/support-and-compatibility.md"` returns 1 match.
- `! rg "autocomplete|vector|hybrid|new public runtime API" "guides/support-and-compatibility.md"` exits 0 for newly added proof-boundary lines.
</acceptance_criteria>
<verify>
- `rg "mix verify\\.adopter|mix verify\\.adopter --live" "guides/support-and-compatibility.md"`
- `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "guides/support-and-compatibility.md"`
- `rg "cd examples/phoenix_meilisearch && mix deps.get && mix test" "guides/support-and-compatibility.md"`
</verify>

### Task 98-01-02 - Reconcile README one-hop proof discoverability micro-contract
<read_first>
- `README.md`
- `guides/support-and-compatibility.md`
- `.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-CONTRACT-STATEMENTS.md`
- `.planning/research/v1.27-contract-surface-map.md`
</read_first>
<action>
- Update the existing README bullet that starts with `**Support and readiness:**`.
- Ensure that bullet explicitly names both commands (`mix verify.adopter` and `mix verify.adopter --live`) and points to `guides/support-and-compatibility.md`.
- Keep the README wording as a concise route-and-context sentence; do not duplicate the full support matrix.
- Keep the existing one-hop link to `guides/outside-adopter-intake.md` for evidence submission.
</action>
<acceptance_criteria>
- `rg "Support and readiness:.*mix verify\\.adopter" "README.md"` returns at least 1 match.
- `rg "mix verify\\.adopter --live" "README.md"` returns at least 1 match.
- `rg "guides/support-and-compatibility\\.md" "README.md"` returns at least 1 match.
- `rg "guides/outside-adopter-intake\\.md" "README.md"` returns at least 1 match.
</acceptance_criteria>
<verify>
- `rg "Support and readiness:.*mix verify\\.adopter" "README.md"`
- `rg "mix verify\\.adopter --live" "README.md"`
- `rg "guides/support-and-compatibility\\.md|guides/outside-adopter-intake\\.md" "README.md"`
</verify>

### Task 98-01-03 - Reconcile CONTRIBUTING proof section language to canonical split
<read_first>
- `CONTRIBUTING.md`
- `guides/support-and-compatibility.md`
- `examples/phoenix_meilisearch/README.md`
- `lib/mix/tasks/verify.adopter.ex`
</read_first>
<action>
- Update CONTRIBUTING verification prose around `mix verify.adopter` so it explicitly states:
  - fast mode is service-free,
  - live mode requires the three env vars,
  - live mode follows example runbook/CI sequence (`mix deps.get` then `mix test`).
- Keep wording in one paragraph plus one CI-parity sentence; do not copy the full example README env table.
- Retain existing CI job name `phoenix-example-integration` in the parity sentence.
</action>
<acceptance_criteria>
- `rg "mix verify\\.adopter" "CONTRIBUTING.md"` returns at least 2 matches.
- `rg "mix verify\\.adopter --live" "CONTRIBUTING.md"` returns at least 1 match.
- `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "CONTRIBUTING.md"` returns 3 matches.
- `rg "phoenix-example-integration|mix deps.get|mix test" "CONTRIBUTING.md"` returns at least 3 matches.
</acceptance_criteria>
<verify>
- `rg "mix verify\\.adopter|mix verify\\.adopter --live" "CONTRIBUTING.md"`
- `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "CONTRIBUTING.md"`
- `rg "phoenix-example-integration|mix deps.get|mix test" "CONTRIBUTING.md"`
</verify>
</tasks>

<threat_model>
- **T98-P1-001 (Integrity, High, ASVS-L1 V1.2):** fast and live proof paths drift semantically, causing false confidence in unsupported states.  
  **Mitigation:** enforce identical command-family tokens and required env vars across support, README, and CONTRIBUTING.  
  **Detection:** `rg "mix verify\\.adopter --live|SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "guides/support-and-compatibility.md" "README.md" "CONTRIBUTING.md"`.
- **T98-P1-002 (Tampering, Medium, ASVS-L1 V1.5):** entry surfaces become competing policy authorities with contradictory wording.  
  **Mitigation:** keep normative policy in `guides/support-and-compatibility.md`; use route-and-context text in README/CONTRIBUTING only.  
  **Detection:** `rg "single current support and readiness authority|source of truth|route" "guides/support-and-compatibility.md" "README.md" "CONTRIBUTING.md"`.
- **T98-P1-003 (Scope Creep, High, ASVS-L1 V14.2):** documentation edits smuggle runtime capability promises into contract-hardening phase.  
  **Mitigation:** reject additions that mention new runtime APIs, backend expansion, or feature wedges in these edits.  
  **Detection:** `rg "autocomplete|vector|hybrid|new public runtime API|multi-backend" "guides/support-and-compatibility.md" "README.md" "CONTRIBUTING.md"`.
</threat_model>

<verification>
- `mix test test/scrypath/readiness_contract_test.exs`
- `rg "mix verify\\.adopter|mix verify\\.adopter --live" "guides/support-and-compatibility.md" "README.md" "CONTRIBUTING.md"`
- `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "guides/support-and-compatibility.md" "CONTRIBUTING.md"`
</verification>

<success_criteria>
- Canonical proof command family is discoverable from README and CONTRIBUTING in one hop.
- Support guide remains canonical policy owner and contains explicit fast/live split semantics.
- CI parity sequence and required live env vars are consistent across the reconciled surfaces.
- No runtime breadth or public API scope expansion is introduced.
</success_criteria>

<output>
- Updated `guides/support-and-compatibility.md`
- Updated `README.md`
- Updated `CONTRIBUTING.md`
</output>
