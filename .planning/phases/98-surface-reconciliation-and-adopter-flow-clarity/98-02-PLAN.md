---
phase: 98
plan: 2
type: execute
wave: 2
depends_on:
  - 98-01
files_modified:
  - examples/phoenix_meilisearch/README.md
  - lib/mix/tasks/verify.adopter.ex
  - test/scrypath/readiness_contract_test.exs
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - PROOF-02
  - PROOF-03
must_haves:
  - "Keep `examples/phoenix_meilisearch/README.md` as live runbook authority and avoid turning root docs into full runbook clones."
  - "Lock command and env token parity between example README and `Mix.Tasks.Verify.Adopter`."
  - "Use token- and order-based tests, not prose snapshots."
---

<objective>
Make the Phoenix example live proof path and executable verifier agree exactly on prerequisites, command order, and CI parity so proof-boundary guidance remains trustworthy.
</objective>

<context>
After Plan 98-01 normalizes root-level discoverability and policy wording, this plan hardens the runbook/executable seam where most drift risk occurs: example README, verify task contract, and focused parity assertions.
</context>

<tasks>
### Task 98-02-01 - Reconcile example README live runbook tokens and sequence
<read_first>
- `examples/phoenix_meilisearch/README.md`
- `CONTRIBUTING.md`
- `.github/workflows/ci.yml`
- `lib/mix/tasks/verify.adopter.ex`
</read_first>
<action>
- Update `examples/phoenix_meilisearch/README.md` sections `From the repository root...`, `## Configure ports and flags`, and `## GitHub Actions (phoenix-example-integration)` so the same three env vars appear exactly as:
  - `SCRYPATH_EXAMPLE_INTEGRATION`
  - `PGPORT`
  - `SCRYPATH_MEILISEARCH_URL`
- Ensure the CI command sequence remains explicitly ordered as three lines: `cd examples/phoenix_meilisearch`, `mix deps.get`, `mix test`.
- Keep this file as runbook authority by preserving explicit statement that CI uses `mix` commands while `./scripts/smoke.sh` is local orchestration.
</action>
<acceptance_criteria>
- `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "examples/phoenix_meilisearch/README.md"` returns at least 3 matches.
- `rg -U "(?s)cd examples/phoenix_meilisearch\\nmix deps.get\\nmix test" "examples/phoenix_meilisearch/README.md"` returns 1 match.
- `rg "phoenix-example-integration|\\.\\/scripts\\/smoke\\.sh|not what GitHub Actions invokes" "examples/phoenix_meilisearch/README.md"` returns at least 3 matches.
</acceptance_criteria>
<verify>
- `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "examples/phoenix_meilisearch/README.md"`
- `rg -U "(?s)cd examples/phoenix_meilisearch\\nmix deps.get\\nmix test" "examples/phoenix_meilisearch/README.md"`
- `rg "phoenix-example-integration|\\.\\/scripts\\/smoke\\.sh" "examples/phoenix_meilisearch/README.md"`
</verify>

### Task 98-02-02 - Align `Mix.Tasks.Verify.Adopter` contract text with runbook authority
<read_first>
- `lib/mix/tasks/verify.adopter.ex`
- `examples/phoenix_meilisearch/README.md`
- `guides/support-and-compatibility.md`
</read_first>
<action>
- Update `lib/mix/tasks/verify.adopter.ex` module documentation and shell info strings to keep the live branch wording synchronized with the example runbook:
  - include the exact env var set,
  - include the exact command sequence `cd examples/phoenix_meilisearch && mix deps.get && mix test`,
  - keep explicit prohibition on silent live-to-fast downgrade.
- Keep runtime behavior unchanged except text/token reconciliation; do not change option parsing or network checks.
</action>
<acceptance_criteria>
- `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "lib/mix/tasks/verify.adopter.ex"` returns at least 3 matches.
- `rg "cd examples/phoenix_meilisearch && mix deps.get && mix test" "lib/mix/tasks/verify.adopter.ex"` returns at least 1 match.
- `rg "does not start Docker|does not.*silently downgrade" "lib/mix/tasks/verify.adopter.ex"` returns at least 1 match.
</acceptance_criteria>
<verify>
- `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "lib/mix/tasks/verify.adopter.ex"`
- `rg "cd examples/phoenix_meilisearch && mix deps.get && mix test" "lib/mix/tasks/verify.adopter.ex"`
- `rg "does not.*silently downgrade|orchestration-only" "lib/mix/tasks/verify.adopter.ex"`
</verify>

### Task 98-02-03 - Add proof-boundary parity assertions for example/runbook seams
<read_first>
- `test/scrypath/readiness_contract_test.exs`
- `test/scrypath/docs_contract_test.exs`
- `examples/phoenix_meilisearch/README.md`
- `lib/mix/tasks/verify.adopter.ex`
</read_first>
<action>
- Extend `test/scrypath/readiness_contract_test.exs` with assertions that both README/CONTRIBUTING discoverability and live-prerequisite env vars remain explicit.
- Add/adjust `test/scrypath/docs_contract_test.exs` assertions to verify command-order parity for the example CI runbook (`cd`, then `mix deps.get`, then `mix test`).
- Keep assertions token-based (`String.contains?`, helper-based order checks) and avoid paragraph snapshots.
</action>
<acceptance_criteria>
- `rg "mix verify\\.adopter --live|SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "test/scrypath/readiness_contract_test.exs"` returns at least 4 matches.
- `rg "cd examples/phoenix_meilisearch|mix deps.get|mix test|ordered\\?" "test/scrypath/docs_contract_test.exs"` returns at least 4 matches.
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/readiness_contract_test.exs" "test/scrypath/docs_contract_test.exs"` exits 0.
- `mix test test/scrypath/readiness_contract_test.exs test/scrypath/docs_contract_test.exs` exits 0.
</acceptance_criteria>
<verify>
- `rg "mix verify\\.adopter --live|SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "test/scrypath/readiness_contract_test.exs"`
- `rg "cd examples/phoenix_meilisearch|mix deps.get|mix test|ordered\\?" "test/scrypath/docs_contract_test.exs"`
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/readiness_contract_test.exs" "test/scrypath/docs_contract_test.exs"`
- `mix test test/scrypath/readiness_contract_test.exs test/scrypath/docs_contract_test.exs`
</verify>
</tasks>

<threat_model>
- **T98-P2-001 (Integrity, High, ASVS-L1 V1.3):** example runbook and executable verifier diverge, causing false-negative or false-positive maintainer proof outcomes.  
  **Mitigation:** lock shared env tokens and command sequence in both files and assert parity in readiness/docs contract tests.  
  **Detection:** `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL|cd examples/phoenix_meilisearch && mix deps.get && mix test" "examples/phoenix_meilisearch/README.md" "lib/mix/tasks/verify.adopter.ex"`.
- **T98-P2-002 (Repudiation, Medium, ASVS-L1 V7.4):** contributors cannot reproduce CI proof due to ambiguous command ordering text.  
  **Mitigation:** keep explicit three-step CI sequence and add order assertions in docs contract tests.  
  **Detection:** `rg -U "(?s)cd examples/phoenix_meilisearch.*mix deps.get.*mix test" "examples/phoenix_meilisearch/README.md" "test/scrypath/docs_contract_test.exs"`.
- **T98-P2-003 (Scope Creep, High, ASVS-L1 V14.2):** runbook reconciliation introduces runtime feature changes under documentation edits.  
  **Mitigation:** constrain code edits in `verify.adopter` to wording/token parity, not behavioral expansion.  
  **Detection:** `rg "OptionParser|ensure_live_env!|ensure_live_services!" "lib/mix/tasks/verify.adopter.ex"` plus targeted test pass: `mix test test/mix/tasks/verify_adopter_test.exs`.
</threat_model>

<verification>
- `mix test test/mix/tasks/verify_adopter_test.exs`
- `mix test test/scrypath/readiness_contract_test.exs test/scrypath/docs_contract_test.exs`
- `rg "SCRYPATH_EXAMPLE_INTEGRATION|PGPORT|SCRYPATH_MEILISEARCH_URL" "examples/phoenix_meilisearch/README.md" "lib/mix/tasks/verify.adopter.ex"`
</verification>

<success_criteria>
- Example README and `verify.adopter` encode the same live prerequisites and command order.
- Proof-boundary tests assert parity via stable tokens and ordering helpers.
- Contributors can map root proof guidance to the exact CI-shaped example runbook.
- No runtime capability class is added while reconciling proof surfaces.
</success_criteria>

<output>
- Updated `examples/phoenix_meilisearch/README.md`
- Updated `lib/mix/tasks/verify.adopter.ex`
- Updated `test/scrypath/readiness_contract_test.exs`
- Updated `test/scrypath/docs_contract_test.exs`
</output>
