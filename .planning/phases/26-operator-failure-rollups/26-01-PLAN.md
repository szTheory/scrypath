---
phase: 26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/operator/failed_work.ex
  - lib/scrypath/operator.ex
  - lib/scrypath/operator/failed_sync_work_inspection.ex
  - lib/scrypath/operator/reason_class_counts.ex
  - lib/scrypath.ex
  - test/scrypath/operator/failed_work_test.exs
autonomous: true
requirements: [OPS14-01]
tags: [operator, failed_work, rollups, elixir]

must_haves:
  truths:
    - "`FailedWork.reason_class_counts/1` is the single pure aggregator; every later consumer in Phase 26 calls it on the exact row list they display (26-CONTEXT D-01)"
    - "`Scrypath.failed_sync_work/2` without `reason_class_counts: true` returns `{:ok, [FailedWork.t()]}` unchanged (26-CONTEXT D-03)"
    - "`%ReasonClassCounts{}` has `version: 1`, `total`, and `by_class` with all five `FailedWork.reason_class/0` atoms each `non_neg_integer` (26-CONTEXT D-06, D-07, D-08)"
  artifacts:
    - path: "lib/scrypath/operator/reason_class_counts.ex"
      provides: "public counts struct + Jason-friendly map shape"
      contains: "defstruct"
    - path: "lib/scrypath/operator/failed_sync_work_inspection.ex"
      provides: "opt-in ok-tuple wrapper"
      contains: "defstruct"
    - path: "test/scrypath/operator/failed_work_test.exs"
      provides: "rollup contract tests"
      contains: "reason_class_counts"
---

<objective>
Ship the **typed rollup core** for Phase 26: `FailedWork.reason_class_counts/1`, public structs `%ReasonClassCounts{}` and `%FailedSyncWorkInspection{}`, and **`Operator.failed_sync_work/2` + `Scrypath.failed_sync_work/2`** opt-in via **`reason_class_counts: true`** in the operator-options split (additive; default list return preserved). Lock exhaustive `by_class` keys and totals with tests (OPS14-01).
</objective>

<threat_model>
**ASVS L1 — library operator path**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-26-API: Callers break on new return shape | Medium | Default path unchanged; opt-in only via explicit keyword |
| T-26-API: Misleading counts | Medium | Dense five-key map; tests tie counts to row frequencies |
| T-26-API: Secrets in structs | Low | Counts contain only integers and atoms — no payloads |

No new network calls; pure CPU over existing list results.
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add `%ReasonClassCounts{}` and `%FailedSyncWorkInspection{}`</name>
  <files>lib/scrypath/operator/reason_class_counts.ex, lib/scrypath/operator/failed_sync_work_inspection.ex</files>
  <read_first>
    - lib/scrypath/operator/failed_work.ex (`@type reason_class`, `@enforce_keys` on `%FailedWork{}`)
    - .planning/phases/26-operator-failure-rollups/26-CONTEXT.md (D-05 through D-10)
    - lib/scrypath/operator/reconcile.ex (defstruct style for additive fields)
  </read_first>
  <action>
    Create **`lib/scrypath/operator/reason_class_counts.ex`** defining **`Scrypath.Operator.ReasonClassCounts`** with:

    - **`@enforce_keys [:version, :total, :by_class]`**
    - **`defstruct [:version, :total, :by_class]`** with documented defaults: **`version` is integer `1`**, **`total` is `non_neg_integer`**, **`by_class` is `%{atom => non_neg_integer}`**
    - **`@moduledoc`** stating dense contract: keys in **`by_class` MUST be exactly** `[:transport, :validation, :backend_rejected, :queue_exhausted, :unknown]` in **that order** when serializing to JSON (implement **`Jason.Encoder`** for `ReasonClassCounts` that emits object keys in that fixed order — use explicit key list or `Jason.OrderedObject` if already a dependency; if not, plain map is acceptable if tests assert key **set** equality and docs state iteration order is not guaranteed for bare `Jason.encode!` on map).
    - **`@type t`** reflecting the five atoms

    Create **`lib/scrypath/operator/failed_sync_work_inspection.ex`** defining **`Scrypath.Operator.FailedSyncWorkInspection`** with:

    - **`@enforce_keys [:entries, :counts]`**
    - **`defstruct [:entries, :counts]`** where **`entries`** is **`[Scrypath.Operator.FailedWork.t()]`** and **`counts`** is **`ReasonClassCounts.t()`**
    - **`@moduledoc`** one paragraph tying to **`failed_sync_work/2`** opt-in branch

    **Do not** add these structs to **`FailedWork`** `@enforce_keys`.
  </action>
  <verify>
    <automated>mix format --check-formatted && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `test -f lib/scrypath/operator/reason_class_counts.ex` exits `0`
    - `test -f lib/scrypath/operator/failed_sync_work_inspection.ex` exits `0`
    - `grep -n 'defmodule Scrypath.Operator.ReasonClassCounts' lib/scrypath/operator/reason_class_counts.ex` returns at least one line
    - `grep -n 'defmodule Scrypath.Operator.FailedSyncWorkInspection' lib/scrypath/operator/failed_sync_work_inspection.ex` returns at least one line
    - `grep ':version' lib/scrypath/operator/reason_class_counts.ex` returns at least one line
    - `grep ':by_class' lib/scrypath/operator/reason_class_counts.ex` returns at least one line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Implement `FailedWork.reason_class_counts/1`</name>
  <files>lib/scrypath/operator/failed_work.ex</files>
  <read_first>
    - lib/scrypath/operator/failed_work.ex (full module)
    - lib/scrypath/operator/reason_class_counts.ex (after Task 1)
  </read_first>
  <action>
    Add **`@spec reason_class_counts([t()]) :: Scrypath.Operator.ReasonClassCounts.t()`** and **`def reason_class_counts(rows) when is_list(rows)`** that:

    1. Treats **`nil`** `reason_class` on a row as **`:unknown`** for counting (defensive; should not occur for constructed rows).
    2. Builds frequencies (e.g. **`Enum.frequencies_by(..., &class/1)`**).
    3. For each atom in **`[:transport, :validation, :backend_rejected, :queue_exhausted, :unknown]`**, sets **`by_class[atom] = Map.get(freqs, atom, 0)`**.
    4. Sets **`total` to `length(rows)`** (per 26-CONTEXT D-07: sum of class counts equals row count for unfiltered input).
    5. Returns **`%ReasonClassCounts{version: 1, total: total, by_class: dense_map}`**.

    Add **`@moduledoc`** subsection **"Rollups"** documenting the invariant: callers passing a **filtered** list must not compare `total` to an unfiltered source length.

    **`defdelegate`** or **`alias`** `ReasonClassCounts` as needed; keep **`FailedWork`** as the home of the pure function per 26-CONTEXT D-01.
  </action>
  <verify>
    <automated>mix test test/scrypath/operator/failed_work_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def reason_class_counts(' lib/scrypath/operator/failed_work.ex` returns at least one line
    - `grep ':transport' lib/scrypath/operator/failed_work.ex` appears in `reason_class_counts` implementation context (frequency default path) OR `grep 'reason_class_counts' lib/scrypath/operator/failed_work.ex` shows adjacent lines including `:queue_exhausted`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Wire `reason_class_counts` opt-in through `Operator` and `Scrypath`</name>
  <files>lib/scrypath/operator.ex, lib/scrypath.ex</files>
  <read_first>
    - lib/scrypath/operator.ex (`@operator_only_opts`, `failed_sync_work/2`)
    - lib/scrypath.ex (`failed_sync_work/2`, `@spec`)
    - lib/scrypath/operator/failed_work.ex (`list/3`)
    - lib/scrypath/operator/failed_sync_work_inspection.ex
  </read_first>
  <action>
    In **`lib/scrypath/operator.ex`**:

    1. Add **`:reason_class_counts`** to **`@operator_only_opts`** (or equivalent explicit list used by **`Keyword.split/2`**).
    2. In **`failed_sync_work/2`**, after **`Keyword.split`**, **`pop`** `:reason_class_counts` from **`operator_opts`** before passing to **`FailedWork.list/3`** so only recognized operator keys reach backends (mirror pattern if other keys are already stripped — if `list/3` ignores unknown keys, popping is still required by this plan for clarity).
    3. On **`{:ok, rows}`**, if the popped value is **`true`**, return **`{:ok, %FailedSyncWorkInspection{entries: rows, counts: FailedWork.reason_class_counts(rows)}}`**; else return **`{:ok, rows}`**.
    4. Update **`@spec`** to a union: **`{:ok, [FailedWork.t()]} | {:ok, FailedSyncWorkInspection.t()} | {:error, term()}`** (use fully qualified struct names or aliases).

    In **`lib/scrypath.ex`**:

    - Update **`@doc`** for **`failed_sync_work/2`** with a short subsection **"Reason class rollups"** documenting **`reason_class_counts: true`**, the **`FailedSyncWorkInspection`** shape, and pointer to **`ReasonClassCounts`**.
    - Update **`@spec`** to match the **`Operator`** union.

    **Do not** add new public recovery functions.
  </action>
  <verify>
    <automated>mix test test/scrypath/operator/failed_work_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep ':reason_class_counts' lib/scrypath/operator.ex` returns at least one line
    - `grep 'FailedSyncWorkInspection' lib/scrypath/operator.ex` returns at least one line
    - `grep 'failed_sync_work' lib/scrypath.ex` includes updated `@spec` line containing **`FailedSyncWorkInspection`** OR **`Operator.FailedSyncWorkInspection`**
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Tests — exhaustive keys, totals, default path, opt-in shape</name>
  <files>test/scrypath/operator/failed_work_test.exs</files>
  <read_first>
    - test/scrypath/operator/failed_work_test.exs (existing `failed_sync_work` tests, fixtures)
    - lib/scrypath/operator/failed_work.ex (`reason_class_counts/1`)
    - .planning/phases/26-operator-failure-rollups/26-CONTEXT.md (D-17, D-18)
  </read_first>
  <action>
    Add tests (new **`describe`** block **`"reason_class_counts and failed_sync_work rollups (OPS14-01)"`** or similar):

    1. **Pure function:** Build 3–5 synthetic **`%FailedWork{}`** structs (minimal fields — copy pattern from existing tests: set **`@enforce_keys`** fields plus **`reason_class`** varied across at least three classes including **`:unknown`**). Call **`FailedWork.reason_class_counts/1`**. Assert **`counts.total == length(rows)`**, **`Enum.sum(Map.values(counts.by_class)) == counts.total`**, and **`MapSet.new(Map.keys(counts.by_class)) == MapSet.new([:transport, :validation, :backend_rejected, :queue_exhausted, :unknown])`**.

    2. **Match frequencies:** For the same rows, assert **`counts.by_class ==`** map built from **`Enum.frequencies_by(rows, & &1.reason_class)`** with missing keys filled with **`0`** (or compare via small helper in test file).

    3. **Default API:** **`Scrypath.failed_sync_work(SearchablePost, opts_without_rollups)`** still returns **`{:ok, list}`** where **`is_list(elem(tuple, 1))`** and first element is not a struct wrapper (use **`match?({:ok, [%FailedWork{} | _]}, ...)`** or refute match on **`FailedSyncWorkInspection`**).

    4. **Opt-in API:** With **`OperatorTask.test_operator_opts()`**-compatible opts plus **`reason_class_counts: true`**, assert **`{:ok, %FailedSyncWorkInspection{entries: es, counts: c}}}`**, **`length(es) == c.total`**, and **`c.version == 1`**.

    5. **D-18 honesty sketch:** Add a comment in test referencing D-18: if a row used an atom **not** in the five (would require **`%FailedWork{}` with invalid reason_class** — **do not** construct invalid structs if Dialyzer forbids). Instead: test that **`reason_class_counts`** normalizes **`nil`** to **`:unknown`** only (documented), proving unknown bucket exists; optionally test internal helper if you extract **`normalize_class/1`** — keep test green without breaking types.

    Reuse existing schema and config stubs from **`failed_work_test.exs`**; do not duplicate entire file setup.
  </action>
  <verify>
    <automated>mix test test/scrypath/operator/failed_work_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'OPS14-01' test/scrypath/operator/failed_work_test.exs` returns at least one line
    - `grep 'reason_class_counts' test/scrypath/operator/failed_work_test.exs` returns at least two lines
    - `mix test test/scrypath/operator/failed_work_test.exs` exits `0`
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <automated>mix format --check-formatted && mix test test/scrypath/operator/failed_work_test.exs</automated>
</verification>

<success_criteria>
  - `FailedWork.reason_class_counts/1` exists and is pure
  - Opt-in **`reason_class_counts: true`** returns **`FailedSyncWorkInspection`**; default returns list only
  - Tests prove dense keys and total consistency (OPS14-01)
</success_criteria>
