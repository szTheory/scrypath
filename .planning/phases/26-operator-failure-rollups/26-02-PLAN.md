---
phase: 26
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/scrypath/operator/reconcile.ex
  - lib/scrypath/cli/operator_task.ex
  - lib/mix/tasks/scrypath.failed.ex
  - lib/mix/tasks/scrypath.reconcile.ex
  - guides/operator-mix-tasks.md
  - guides/drift-recovery.md
  - CHANGELOG.md
  - test/scrypath/operator/reconcile_test.exs
  - test/scrypath/mix_tasks/operator_tasks_test.exs
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements: [OPS14-01]
tags: [operator, mix, cli, reconcile, docs]

must_haves:
  truths:
    - "`%Reconcile{}` exposes rollup counts derived via `FailedWork.reason_class_counts/1` on the same `failed_work` list attached to the report (26-CONTEXT D-11)"
    - "`mix scrypath.failed` human output shows a rollup block before rows when failed count > 0, and each row includes `reason_class=` (26-CONTEXT D-13)"
    - "`mix scrypath.failed --json` emits exactly one JSON document to stdout with no `Mix.shell().info` on that path (26-CONTEXT D-14)"
  artifacts:
    - path: "lib/scrypath/cli/operator_task.ex"
      provides: "render_failed_work + reconcile rollup lines"
      contains: "reason_class"
    - path: "guides/operator-mix-tasks.md"
      provides: "documented flags and sample output"
      contains: "reason_class" or "rollup"
---

<objective>
Complete OPS14-01 **operator surfaces**: additive **`failed_work_counts`** (or the exact name chosen in code, but must be **`ReasonClassCounts.t()`**) on **`%Reconcile{}`**, CLI/Mix parity (**`mix scrypath.failed`**, **`mix scrypath.reconcile`** report), **`--json`** / **`--no-class-summary`** on failed task, guides + CHANGELOG + **`docs_contract_test`** updates.
</objective>

<threat_model>
**ASVS L1 — operator CLI**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| T-26-CLI: JSON path leaks stderr noise | Low | Single `IO.puts/1` (or one write) for JSON; no `Mix.shell().info` on `--json` branch |
| T-26-CLI: Script breakage | Low | Default human output additive; `--no-class-summary` only suppresses rollup header |

No new credentials or env vars.
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add reconcile rollup field</name>
  <files>lib/scrypath/operator/reconcile.ex</files>
  <read_first>
    - lib/scrypath/operator/reconcile.ex (`run/3`, struct definition)
    - lib/scrypath/operator/failed_work.ex (`reason_class_counts/1`)
    - .planning/phases/26-operator-failure-rollups/26-CONTEXT.md (D-11, D-12)
  </read_first>
  <action>
    Add an **additive** field to **`%Scrypath.Operator.Reconcile{}`**, e.g. **`failed_work_counts`**, **not** listed in **`@enforce_keys`**, with default **`nil`** in **`defstruct`** OR default **`%ReasonClassCounts{...}`** — **preferred:** always set in **`run/3`** success path to **`FailedWork.reason_class_counts(failed_work)`** so it is never **`nil`** on ok reports.

    Update **`@type t`** to include **`failed_work_counts: Scrypath.Operator.ReasonClassCounts.t()`**.

    In **`run/3`**, after **`failed_work`** is bound from **`FailedWork.list`**, compute counts once and include in the **`%__MODULE__{...}`** map.

    **`@moduledoc`**: one sentence stating report-first triage includes per-class pileup counts.
  </action>
  <verify>
    <automated>mix test test/scrypath/operator/reconcile_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'failed_work_counts' lib/scrypath/operator/reconcile.ex` returns at least one line
    - `grep 'reason_class_counts' lib/scrypath/operator/reconcile.ex` returns at least one line
    - `mix test test/scrypath/operator/reconcile_test.exs` exits `0`
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: `OperatorTask` renderers — failed + reconcile</name>
  <files>lib/scrypath/cli/operator_task.ex</files>
  <read_first>
    - lib/scrypath/cli/operator_task.ex (`render_failed_work/2`, `render_reconcile_report/1`)
    - lib/scrypath/operator/reason_class_counts.ex
  </read_first>
  <action>
    Extend **`render_failed_work(schema, failed_work, opts \\\\ [])`** signature if needed to accept **`opts`** with **`:include_class_summary?`** (default **`true`**) — **or** overload with arity-3 only from Mix task; avoid breaking internal callers.

    **Human output (`render_failed_work/2` default):**
    - After **`Failed work: N`**, when **`N > 0`**, insert a **compact rollup block** (one line per class or one wrapped line) using **`FailedWork.reason_class_counts(failed_work)`** — **must call the same function** as API (26-CONTEXT D-01).
    - Each per-row line must include **`reason_class=`** token with string form of the atom (e.g. **`reason_class=transport`**).

    **`render_reconcile_report/1`:** after **`Failed work count:`**, add lines for **`Failed work by class:`** (same rollup formatter — extract private **`format_reason_class_counts/1`** in this module to DRY).

    Add **`render_failed_work_json(schema, failed_work)`** (or similar) returning an **`iodata`** or string map suitable for **`Jason.encode!/1`** including **`schema`**, **`entries`** (list of maps with **`id`**, **`reason_class`**, **`source`**, **`state`**, **`operation`**, **`retryable`** — minimal safe fields), and **`counts`** (**`version`**, **`total`**, **`by_class`** with string keys for JSON object consistency).
  </action>
  <verify>
    <automated>mix test test/scrypath/mix_tasks/operator_tasks_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'reason_class=' lib/scrypath/cli/operator_task.ex` returns at least one line
    - `grep 'Failed work by class' lib/scrypath/cli/operator_task.ex` returns at least one line OR `grep 'by class' lib/scrypath/cli/operator_task.ex` matches reconcile rollup
    - `grep 'Jason' lib/scrypath/cli/operator_task.ex` returns at least one line if JSON helper lives here; if JSON is only in Mix task, acceptance: `grep 'Jason.encode' lib/mix/tasks/scrypath.failed.ex` returns at least one line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: `mix scrypath.failed` — flags + JSON path</name>
  <files>lib/mix/tasks/scrypath.failed.ex</files>
  <read_first>
    - lib/mix/tasks/scrypath.failed.ex
    - lib/mix/tasks/scrypath.settings.diff.ex (`--json` / stdout pattern)
    - lib/scrypath/cli/operator_task.ex (after Task 2)
  </read_first>
  <action>
    Add **`@extra_switches`** **`[json: :boolean, no_class_summary: :boolean]`** (exact long names may be **`--no-class-summary`** via OptionParser convention).

    **`--json`:** When set, call **`Scrypath.failed_sync_work(schema, runtime ++ test ++ [reason_class_counts: true])`** (or build counts from list + **`reason_class_counts/1`** — must match API). Emit **one** JSON document using **`IO.puts/1`** only (no **`Mix.shell().info`** on this branch). Exit **`0`** on ok.

    **Human default:** Call **`failed_sync_work`** without inspection tuple; pass **`include_class_summary: false`** into renderer when **`--no-class-summary`**.

    Update **`@moduledoc`** with examples for **`--json`** and **`--no-class-summary`**.
  </action>
  <verify>
    <automated>mix test test/scrypath/mix_tasks/operator_tasks_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'json:' lib/mix/tasks/scrypath.failed.ex` returns at least one line
    - `grep 'IO.puts' lib/mix/tasks/scrypath.failed.ex` returns at least one line
    - `grep 'no_class_summary' lib/mix/tasks/scrypath.failed.ex` returns at least one line
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Tests + docs contract + guides + CHANGELOG</name>
  <files>test/scrypath/mix_tasks/operator_tasks_test.exs, test/scrypath/docs_contract_test.exs, guides/operator-mix-tasks.md, guides/drift-recovery.md, CHANGELOG.md</files>
  <read_first>
    - test/scrypath/mix_tasks/operator_tasks_test.exs (patterns for Mix tasks, `ExUnit.CaptureIO` or `capture_io`)
    - test/scrypath/docs_contract_test.exs (public API string list)
    - guides/operator-mix-tasks.md (existing **`scrypath.failed`** section)
    - CHANGELOG.md (Unreleased section)
  </read_first>
  <action>
    1. **`reconcile_test.exs`:** Assert **`report.failed_work_counts.total == length(report.failed_work)`** and **`MapSet.equal?(MapSet.new(Map.keys(report.failed_work_counts.by_class)), MapSet.new([:transport, :validation, :backend_rejected, :queue_exhausted, :unknown]))`** on an existing fixture report path (adapt if report uses stubs).

    2. **`operator_tasks_test.exs`:** Add coverage that human output contains **`reason_class=`** when failures exist; add **`--json`** test asserting parseable JSON (**`Jason.decode!`**) and **`counts["by_class"]`** has five string keys; add **`--no-class-summary`** suppresses rollup header substring you introduced (pick an exact substring in test, e.g. **`"Failed work by class"`** or the first line of rollup block).

    3. **`docs_contract_test.exs`:** Add any new **public** modules or significant **`@doc`** mentions required for ExDoc (e.g. **`ReasonClassCounts`** if re-exported — only if listed in contract file today).

    4. **`guides/operator-mix-tasks.md`:** Document rollup block, **`--json`**, **`--no-class-summary`**, and reconcile output cross-link.

    5. **`guides/drift-recovery.md`:** One short paragraph or bullet cross-linking failure rollups for triage (if file exists; if not present in workspace, skip with note in SUMMARY only — **do not** create file unless it exists).

    6. **`CHANGELOG.md`:** Under **Unreleased**, bullet for operator failure rollups (API + Mix).

  </action>
  <verify>
    <automated>mix format --check-formatted && mix test test/scrypath/mix_tasks/operator_tasks_test.exs test/scrypath/operator/reconcile_test.exs test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'failed_work_counts' test/scrypath/operator/reconcile_test.exs` returns at least one line
    - `grep '--json' guides/operator-mix-tasks.md` returns at least one line OR `grep 'json' guides/operator-mix-tasks.md` documents JSON for failed task
    - `grep -i 'rollup\\|reason_class\\|failed work by class' CHANGELOG.md` returns at least one line
    - `mix test test/scrypath/mix_tasks/operator_tasks_test.exs test/scrypath/operator/reconcile_test.exs test/scrypath/docs_contract_test.exs` exits `0`
  </acceptance_criteria>
</task>

</tasks>

<verification>
  <automated>mix test test/scrypath/operator/reconcile_test.exs test/scrypath/mix_tasks/operator_tasks_test.exs test/scrypath/docs_contract_test.exs</automated>
</verification>

<success_criteria>
  - Reconcile reports expose dense counts aligned with `failed_work` rows
  - Mix tasks and guides match shipped behavior; CHANGELOG records the feature
  - OPS14-01 traceability satisfied for operator entry points
</success_criteria>
