---
phase: 34
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - guides/golden-path.md
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements: [ADPT-02, ADPT-03, VRFY-01]
tags: [docs, golden-path, ci, contract-test, verification]

must_haves:
  truths:
    - "**`guides/golden-path.md`** **Integration smoke** section no longer tells readers the Phoenix example integration proof is only **“local”** or **“optional CI wiring”** — copy matches **`.github/workflows/ci.yml`** truth: job **`phoenix-example-integration`** runs on **pull requests** with **Postgres** + **Meilisearch** (see **034-CONTEXT.md** D-04–D-06)"
    - "Published docs and tests contain **no** substring **`optional CI wiring`** after this phase (`rg 'optional CI wiring' README.md guides/golden-path.md` empty)"
  artifacts:
    - path: "guides/golden-path.md"
      provides: "Honest CI narrative for example integration"
      contains: "phoenix-example-integration"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Contract lock for new CI wording"
      contains: "phoenix-example-integration"
---

<objective>
Close **`INT-GOLDEN-PATH-CI-STORY`**: golden-path prose matches **`.github/workflows/ci.yml`** for **`phoenix-example-integration`** on **pull requests**, supports **VRFY-01** operational honesty (same trust story as **CONTRIBUTING**), and keeps **ADPT-02 / ADPT-03** framing honest (no “optional CI” fiction for the consumer-shaped **`mix test`** path).
</objective>

<threat_model>
**ASVS V14 / documentation integrity (L1)**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| **T-34-DOC-01:** Invented workflow triggers (e.g. claiming nightly-only) | Medium | Before writing prose, read **`.github/workflows/ci.yml`** top-level **`on:`** and the **`phoenix-example-integration:`** job **only** — document **pull_request** (and **`push` to `main`** if both exist) without adding jobs not in YAML |
| **T-34-DOC-02:** Duplicating full **CONTRIBUTING** CI matrix inside golden path | Low | One short paragraph + **link** to **`CONTRIBUTING.md`** per D-06 |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: guides/golden-path.md — Integration smoke CI accuracy</name>
  <files>guides/golden-path.md</files>
  <read_first>
    - guides/golden-path.md (`## Integration smoke` ~L122–127)
    - .github/workflows/ci.yml (`on:` block; `phoenix-example-integration:` job through `mix test` step)
    - CONTRIBUTING.md (row **`phoenix-example-integration`** for service/env language to mirror)
    - .planning/phases/034-golden-path-readme-and-ci-alignment/034-CONTEXT.md (D-03–D-06)
  </read_first>
  <action>
    1. In **`guides/golden-path.md`** under **`## Integration smoke`**, **replace** the second bullet (~**L127**) that currently claims the example smoke is **“local / optional CI wiring”**.
    2. **New** bullet meaning (concrete): On **pull requests**, **GitHub Actions** runs job **`phoenix-example-integration`**, which executes **`cd examples/phoenix_meilisearch && mix test`** with **live Postgres** and **Meilisearch** (use the same **image** / **service** wording as **`ci.yml`** and **`CONTRIBUTING.md`** — e.g. **Postgres 16**, **`getmeili/meilisearch:v1.15`**, **`SCRYPATH_EXAMPLE_INTEGRATION=1`**, **`PGPORT`** if you echo CONTRIBUTING).
    3. **Remove** the literal substring **`optional CI wiring`** from the file entirely.
    4. **Optional** (D-03): Add **one** short sentence (outside the canonical schema fence, e.g. after the schema section or in Integration smoke intro) noting production apps may use **`validate_inclusion`** or **`Ecto.Enum`** for **`status`** — **without** changing the canonical **`field :status, :string`** block.
    5. Keep the first bullet (runbook link to **`examples/phoenix_meilisearch/README.md`**) and **`./scripts/smoke.sh`** ordering **scoped** to the example directory as fixed in Phase 33.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'optional CI wiring' guides/golden-path.md` exits **1** (no matches)
    - `grep -F 'phoenix-example-integration' guides/golden-path.md` exits **0**
    - `grep -Ei 'pull requests?' guides/golden-path.md` exits **0**
    - `grep -F 'GitHub Actions' guides/golden-path.md` exits **0**
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: docs_contract_test — retire `optional CI wiring` anchor; lock PR CI narrative</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (test **`"golden path scopes example smoke script to the phoenix_meilisearch example (Phase 33)"`** ~L327–336)
    - guides/golden-path.md (post Task 1)
    - .github/workflows/ci.yml
  </read_first>
  <action>
    1. In test **`"golden path scopes example smoke script to the phoenix_meilisearch example (Phase 33)"`**, **remove** **`"optional CI wiring"`** from the **`assert_contains_all(golden, [...])`** list.
    2. **Add** stable substrings that match the **new** golden-path bullet, including **`phoenix-example-integration`** and a **PR** phrase (e.g. **`pull request`** or **`pull requests`**) and **`GitHub Actions`** (or the exact phrase chosen in Task 1 — tests and doc **must match**).
    3. Optionally rename the test description to append **`(Phase 33–34)`** if clearer; not required.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'optional CI wiring' test/scrypath/docs_contract_test.exs` exits **1**
    - `mix test test/scrypath/docs_contract_test.exs` exits **0**
    - Golden-path test's **`assert_contains_all`** list includes **`phoenix-example-integration`**
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run **`mix test test/scrypath/docs_contract_test.exs`** from repository root after both tasks.
</verification>

<success_criteria>
- Golden path **Integration smoke** matches **`ci.yml`** for **`phoenix-example-integration`** on PRs
- **`optional CI wiring`** removed from repo docs and tests
- Contract tests green
</success_criteria>

## PLANNING COMPLETE
