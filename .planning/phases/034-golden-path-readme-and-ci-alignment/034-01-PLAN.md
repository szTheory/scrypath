---
phase: 34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements: [ADPT-01, ADPT-03]
tags: [docs, readme, golden-path, contract-test, adoption]

must_haves:
  truths:
    - "README **Quick Path** no longer carries **three** full duplicate Elixir fences (schema + context + controller) unless tests were **not** updated — preferred end state is **one micro-fence at most** plus prose CTA into **`guides/golden-path.md`** per **034-CONTEXT.md** D-09"
    - "README first-schema **`status`** representation matches **`guides/golden-path.md`** and **`examples/phoenix_meilisearch/lib/scrypath_demo/blog/post.ex`**: **`field :status, :string`** (or exact **`field(:status, :string)`** in example style if README uses parentheses consistently in that fence)"
  artifacts:
    - path: "README.md"
      provides: "Slim Quick Path + canonical status field"
      contains: "field :status, :string"
    - path: "test/scrypath/docs_contract_test.exs"
      provides: "Phase 34 README contracts + parity"
      contains: "field :status, :string"
---

<objective>
Deliver **ADPT-01** and **ADPT-03** hygiene for the **README** entry: **one** canonical first-schema story aligned with **`guides/golden-path.md`** ( **`INT-GOLDEN-VS-README-SCHEMA`** ), and **Quick Path** discipline (spine in guide, README routes) without breaking semver / verify messaging already covered by existing README sections.
</objective>

<threat_model>
**ASVS V14 / documentation integrity (L1)**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| **T-34-DOC-01:** Slimming README drops **Start here** / golden-path CTA or **`{:scrypath, "~> 0.3"}`** install anchors — adopters lose the spine | Medium | Preserve **phase 29** test anchors in **`docs_contract_test.exs`** for `**Start here:**`, **`guides/golden-path.md`**, **`## Versioning and upgrades`**, **`mix verify.phase11`**, **`docs/releasing.md`**, sync heuristics row — adjust **only** the Quick Path subsection assertions per tasks |
| **T-34-DOC-02:** Partial fence edit leaves **invalid Elixir** in README | Medium | Run **`mix test test/scrypath/docs_contract_test.exs`** (includes **`all Elixir code fences`** syntactic validity) |

</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: README — Quick Path slim + canonical `status` field</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file; focus `## Quick Path` ~L25–L88)
    - guides/golden-path.md (`## Schema` fence for parity)
    - examples/phoenix_meilisearch/lib/scrypath_demo/blog/post.ex
    - .planning/phases/034-golden-path-readme-and-ci-alignment/034-CONTEXT.md (D-01, D-02, D-09, D-10)
  </read_first>
  <action>
    1. In **`README.md`** **`## Quick Path`**, change the **schema** Elixir fence so **`status`** is **`:string`**, not **`Ecto.Enum`**, matching **`guides/golden-path.md`** and the example **`Post`** module.
    2. **Remove** the **two** following large fences (**`MyApp.Content`** module and **`MyAppWeb.PostController`** module) from Quick Path.
    3. **Replace** them with **short prose** (2–5 sentences) that names the three layers (schema metadata → context sync/search → thin web boundary) and **one CTA** sentence pointing readers to **`guides/golden-path.md`** for the **complete** copy-paste path.
    4. Optionally include **at most one** small ```elixir fence (micro-snippet): either a minimal **`use Scrypath, ...`** + **`schema`** lines including **`field :status, :string`**, **or** a single **`Scrypath.search/3`** callout — not both, per **D-09** (executor picks **one**; default **schema micro-snippet** if unsure).
    5. Do **not** remove or weaken unrelated README sections (**Installation**, **Integration smoke**, **Sync Modes**, **Versioning and upgrades**, **Search**, operator lists) except line-order adjustments required by edits.
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'field :status, :string' README.md` exits **0**
    - `grep -F 'field :status, Ecto.Enum' README.md` exits **1** (Quick Path must not keep enum **`status`** in the canonical snippet)
    - `grep -n '## Quick Path' README.md` and `grep -n '## When Scrypath Fits' README.md` — Quick Path section still exists **before** **When Scrypath Fits**
    - `grep -F 'guides/golden-path.md' README.md` exits **0**
    - `grep -F '**Start here:**' README.md` exits **0**
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: docs_contract_test — README Quick Path + Phase 34 schema parity</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>
    - test/scrypath/docs_contract_test.exs (tests `README opens with installation...` ~L84–100; `phase 29 golden path...` ~L103–131)
    - README.md (post Task 1)
    - guides/golden-path.md
    - .planning/phases/034-golden-path-readme-and-ci-alignment/034-CONTEXT.md (D-08, D-10)
  </read_first>
  <action>
    1. **Rewrite** test **`"README opens with installation, quick path, and phoenix wayfinding"`** so it **does not** require **`MyApp.Content`**, **`search_posts(query, opts \\\\ [])`**, **`publish_post(post, attrs)`**, **`MyAppWeb.PostController`**, or **`Phoenix Walkthrough`** substrings **inside** **`@readme`** (those may remain **only** in **`guides/golden-path.md`** — the existing **`"guide snippets stay aligned with the phoenix fixture contract"`** test already joins README + guides).
    2. **Add** assertions that **`@readme`** still contains: **`## Quick Path`**, **`{:scrypath, "~> 0.3"}`**, **`Scrypath owns its internal transport dependency.`**, **`If you want queued sync, add Oban`**, and **`field :status, :string`**.
    3. **Add** a **new** test, e.g. **`"phase 34 readme and golden path agree on canonical status field"`**, that asserts **`String.contains?(@readme, "field :status, :string")`** and **`String.contains?(@guides["guides/golden-path.md"], "field :status, :string")`** both pass.
    4. Keep **phase 29** golden-path / README ordering tests intact unless a heading rename forces a minimal string update (D-10).
  </action>
  <verify>
    <automated>mix test test/scrypath/docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/docs_contract_test.exs` exits **0**
    - File contains substring **`phase 34 readme and golden path agree on canonical status field`** OR equivalent test name including **`phase 34`** and **`status`**
    - `grep -A35 'test "README opens with installation, quick path, and phoenix wayfinding"' test/scrypath/docs_contract_test.exs | grep -F 'MyAppWeb.PostController'` exits **1** (no **`PostController`** in that test’s **`@readme`** assertions)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run **`mix test test/scrypath/docs_contract_test.exs`** from repository root after both tasks.
</verification>

<success_criteria>
- README Quick Path uses **`field :status, :string`** and is **slimmed** per D-09
- Contract tests encode the new README shape and **narrow** README ↔ golden-path **`status`** parity
- **`mix test test/scrypath/docs_contract_test.exs`** passes
</success_criteria>

## PLANNING COMPLETE
