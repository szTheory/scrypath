---
phase: 65
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
  - scrypath_ops/docs/playbook-schema-v1.md
  - guides/multi-index-search.md
autonomous: true
requirements:
  - OPS3-02
---

# Plan 03 — Structured failure UI, docs, telemetry

<objective>
Replace flash-only run failure UX with assign-backed panels using `RunFailure.enrich/2` + `DocResolver`, add **Copy diagnostics** control with allowlisted JSON, add/adjust maintainer doc sections so **two-hop** linking holds, and emit telemetry events `playbook_run_start` / `playbook_run_stop` with metadata described in CONTEXT D-22.
</objective>

<threat_model>
**ASVS L1:** Copy-to-clipboard JSON must use the enriched allowlist only — never raw `inspect(socket.assigns)`. Doc links are resolver-generated HTTPS URLs only. XSS: escape interpolated failure `message` in HEEx (default escape — do not use `raw/1` on user-derived strings).
</threat_model>

<tasks>

<task id="65-03-01" type="execute">
<read_first>
- scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex
- scrypath_ops/lib/scrypath_ops/playbook/run_failure.ex
- scrypath_ops/lib/scrypath_ops/playbook/doc_resolver.ex
- .planning/phases/65-playbook-run-lifecycle-opsui/65-CONTEXT.md
</read_first>
<action>
Add assign `run_failure_enriched` (map or nil) set in `handle_async` error path by calling `RunFailure.enrich(reason, copy_context)` and `DocResolver.resolve/1`.

In render: when `run_ui.phase == :error` and `run_failure_enriched` present, show DaisyUI `alert alert-error` with `role="alert"` containing `failure_class`, `message`, anchor tag for `doc.primary`, related links (max 2), and a **button** `phx-click="copy_run_diagnostics"` that sets flash info "Copied" or uses `push_event` to copy JSON built with **`Jason.encode!`** on the allowlisted map only.

Remove special-case **only** `search_many` guide link (D-20): delete or gate the `@guide_href` paragraph so **`search`** and **config** failures use the same registry-driven primary link. Keep `guides/multi-index-search.md` reachable as a **related** doc_ref for federation failures if appropriate.

Add `handle_event("copy_run_diagnostics", ...)` that is a no-op guard if no enriched failure.

Ensure success path shows persistent `alert alert-success` from `run_result` assign (already partially present — align with `run_ui.phase == :ok`).

Add doc sections: in `scrypath_ops/docs/playbook-schema-v1.md` add a **Troubleshooting** subsection with anchors for **no_schema**, **invalid_query**, **page_size_out_of_range** (symptom → cause → fix bullets). Ensure `DocResolver` primary URLs include fragment `#...` matching anchors.

In `mount` or first connected mount, use `:telemetry.execute([:scrypath_ops, :playbook_run, :start], %{system_time: System.system_time()}, %{run_id: id})` when scheduling a run and `:telemetry.execute([:scrypath_ops, :playbook_run, :stop], %{duration: ms}, %{run_id: id, result: :ok | :error | :cancelled | :timeout})` when a run completes in `handle_async` / timeout / cancel paths — **exact** event names and metadata keys as written here for Plan 04 assertions.
</action>
<acceptance_criteria>
- `grep -n "copy_run_diagnostics" scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex` returns at least one `handle_event` line and one template/button reference.
- `grep -n "RunFailure" scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex` returns at least one line.
- `grep -n "Troubleshooting" scrypath_ops/docs/playbook-schema-v1.md` returns at least one line.
- `grep -n ":telemetry" scrypath_ops/lib/scrypath_ops_web/live/playbook_live.ex` OR grep in new telemetry helper module path used by PlaybookLive returns at least one line.
- `mix test scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs` exits 0.
</acceptance_criteria>
</task>

</tasks>

<verification>
`mix test scrypath_ops/test/scrypath_ops_web/live/playbook_live_test.exs`
</verification>

<success_criteria>
- Forced failure test (Plan 04 may add) would see `https://` in rendered primary link — Plan 03 must leave hooks ready; minimum: enriched map always has `doc.primary` string starting with `http` for registry rows used in tests.
</success_criteria>
