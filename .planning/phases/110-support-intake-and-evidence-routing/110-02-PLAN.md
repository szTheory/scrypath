---
phase: 110-support-intake-and-evidence-routing
plan: "02"
type: execute
wave: 2
depends_on:
  - 110-01
files_modified:
  - test/scrypath/phase110_contract_test.exs
  - lib/mix/tasks/verify.adopter.ex
  - test/mix/tasks/verify_adopter_test.exs
  - test/scrypath/docs_contract_test.exs
autonomous: true
requirements:
  - SUP-01
  - SUP-02
must_haves:
  truths:
    - `mix verify.adopter` fast mode fails when support/intake authority drifts, when non-owner surfaces start carrying compatibility tuples, or when the Phase 110 evidence-routing vocabulary falls out of sync.
    - Maintainers can run one service-free command that proves Class A-D coverage, finding-bucket/action coverage, issue-template Evidence Block headings, and route-only public entrypoints.
    - `verify.adopter` help text and self-tests stay aligned to the real fast/live contract without adding a new `mix verify.phase110` command.
  artifacts:
    - path: test/scrypath/phase110_contract_test.exs
      provides: Focused SUP-01 and SUP-02 contract assertions for support authority, evidence routing, and non-owner surface drift.
    - path: lib/mix/tasks/verify.adopter.ex
      provides: Fast service-free adopter verification that includes the Phase 110 contract suite.
    - path: test/mix/tasks/verify_adopter_test.exs
      provides: Regression coverage for fast-test list, help text, and live prerequisite guards.
    - path: test/scrypath/docs_contract_test.exs
      provides: Existing evergreen docs-contract coverage updated only where `verify.adopter` fast-path expectations changed.
  key_links:
    - from: lib/mix/tasks/verify.adopter.ex
      to: test/scrypath/phase110_contract_test.exs
      via: `@fast_tests` service-free fast-path wiring.
      pattern: phase110_contract_test.exs
    - from: test/mix/tasks/verify_adopter_test.exs
      to: lib/mix/tasks/verify.adopter.ex
      via: Source/help assertions for the fast/live contract.
      pattern: verify.adopter
    - from: test/scrypath/phase110_contract_test.exs
      to: guides/outside-adopter-intake.md
      via: Token assertions for Class A-D, finding buckets, and maintainer action coverage.
      pattern: Class A|Bug in Scrypath|Needs Information
    - from: test/scrypath/docs_contract_test.exs
      to: lib/mix/tasks/verify.adopter.ex
      via: Evergreen assertion that the fast adopter contract reflects the checked-in file list.
      pattern: readiness_contract_test.exs|phase110_contract_test.exs
---

<objective>
Mechanize the Phase 110 support-routing contract in the existing fast adopter verification lane.

Purpose: Finish SUP-01 and SUP-02 by adding focused service-free proof to `mix verify.adopter` instead of introducing a new phase-specific gate or heavier CI job.
Output: A new `phase110_contract_test.exs`, updated `verify.adopter` fast-path wiring, and regression coverage that keeps help text and docs-contract expectations aligned.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/110-support-intake-and-evidence-routing/110-CONTEXT.md
@.planning/phases/110-support-intake-and-evidence-routing/110-RESEARCH.md
@.planning/phases/110-support-intake-and-evidence-routing/110-VALIDATION.md
@.planning/phases/110-support-intake-and-evidence-routing/110-PATTERNS.md
@.planning/milestones/v1.17-phases/69-adopter-verify-spine/69-01-SUMMARY.md
@.planning/milestones/v1.17-phases/69-adopter-verify-spine/69-02-SUMMARY.md
@lib/mix/tasks/verify.adopter.ex
@test/mix/tasks/verify_adopter_test.exs
@test/scrypath/readiness_contract_test.exs
@test/scrypath/phase98_contract_test.exs
@test/scrypath/phase99_contract_test.exs
@test/scrypath/docs_contract_test.exs
@README.md
@CONTRIBUTING.md
@guides/support-and-compatibility.md
@guides/outside-adopter-intake.md
@.github/ISSUE_TEMPLATE/outside-adopter-evidence.md
@docs/operator-support.md
@website/src/pages/docs.html
@website/src/pages/operators.html
@mix.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a focused Phase 110 docs-contract suite for support authority and evidence routing</name>
  <files>test/scrypath/phase110_contract_test.exs, test/scrypath/docs_contract_test.exs</files>
  <read_first>
.planning/phases/110-support-intake-and-evidence-routing/110-CONTEXT.md
.planning/phases/110-support-intake-and-evidence-routing/110-RESEARCH.md
.planning/phases/110-support-intake-and-evidence-routing/110-VALIDATION.md
test/scrypath/phase110_contract_test.exs
test/scrypath/docs_contract_test.exs
test/scrypath/readiness_contract_test.exs
test/scrypath/phase98_contract_test.exs
test/scrypath/phase99_contract_test.exs
README.md
CONTRIBUTING.md
guides/support-and-compatibility.md
guides/outside-adopter-intake.md
.github/ISSUE_TEMPLATE/outside-adopter-evidence.md
docs/operator-support.md
website/src/pages/docs.html
website/src/pages/operators.html
  </read_first>
  <behavior>
    - Test 1: non-owner surfaces `README.md`, `CONTRIBUTING.md`, `guides/outside-adopter-intake.md`, `docs/operator-support.md`, `website/src/pages/docs.html`, and `website/src/pages/operators.html` point to `guides/support-and-compatibility.md` and do not carry the explicit compatibility tuple literals from the support guide.
    - Test 2: `guides/outside-adopter-intake.md` and `.github/ISSUE_TEMPLATE/outside-adopter-evidence.md` expose Class A-D, `Bug in Scrypath`, `Doc or Contract Gap`, `App-Side Error`, `Environment Failure`, `Needs Information`, the D-08 maintainer actions, and the new `## Evidence Block (required)` field headings.
    - Test 3: route-only public entrypoints link to the support guide or intake guide, and `test/scrypath/docs_contract_test.exs` acknowledges the expanded `verify.adopter` fast file list without turning into a duplicate Phase 110 suite.
  </behavior>
  <action>Create `test/scrypath/phase110_contract_test.exs` per D-10 and D-13 using the direct-file-read helper pattern from `test/scrypath/phase98_contract_test.exs` and `test/scrypath/phase99_contract_test.exs`. The new suite must assert support-authority routing, absence of duplicated compatibility tuple values on non-owner surfaces, Class A-D coverage, finding-bucket coverage, maintainer-action coverage, issue-template Evidence Block headings, and the public redaction warning. Extend `test/scrypath/docs_contract_test.exs` only where the checked-in `verify.adopter` fast file list or support-routing wording now needs to mention `test/scrypath/phase110_contract_test.exs`; do not copy the full Phase 110 assertion set into the evergreen suite.</action>
  <verify>
    <automated>mix test test/scrypath/phase110_contract_test.exs test/scrypath/docs_contract_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `test/scrypath/phase110_contract_test.exs` exists and reads the checked-in docs/template/public route files directly.
    - The new test suite asserts `## Evidence Block (required)` plus the field labels `Path`, `Runtime vs support matrix`, `Reporter class guess`, `Reporter finding guess`, `Scrypath ref or Hex version`, `First failing command step`, and `Logs or artifacts`.
    - The new test suite asserts the D-08 maintainer action phrases `patch-sized bugfix issue`, `docs correction`, `correction guidance`, `environment fix request`, and `needs-info`.
    - `test/scrypath/docs_contract_test.exs` reflects the expanded `verify.adopter` fast-path file list and still keeps its broader evergreen scope.
    - `mix test test/scrypath/phase110_contract_test.exs test/scrypath/docs_contract_test.exs -x` passes.
  </acceptance_criteria>
  <done>Phase 110 has focused service-free contract proof for authority routing, evidence fields, routing vocabulary, and public entrypoint drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the Phase 110 contract into `mix verify.adopter` without adding gate sprawl</name>
  <files>lib/mix/tasks/verify.adopter.ex, test/mix/tasks/verify_adopter_test.exs</files>
  <read_first>
.planning/phases/110-support-intake-and-evidence-routing/110-CONTEXT.md
.planning/phases/110-support-intake-and-evidence-routing/110-RESEARCH.md
.planning/phases/110-support-intake-and-evidence-routing/110-VALIDATION.md
lib/mix/tasks/verify.adopter.ex
test/mix/tasks/verify_adopter_test.exs
test/scrypath/phase110_contract_test.exs
test/scrypath/docs_contract_test.exs
mix.exs
  </read_first>
  <behavior>
    - Test 1: `@fast_tests` and `mix help verify.adopter` both list `test/scrypath/phase110_contract_test.exs` alongside the existing readiness and mix-task self-tests.
    - Test 2: `mix verify.adopter` runs the new Phase 110 contract in fast mode while keeping the same progress marker and the same `--live` prerequisite failures.
    - Test 3: no `mix verify.phase110` alias, Mix task, or CI gate is added anywhere.
  </behavior>
  <action>Update `lib/mix/tasks/verify.adopter.ex` per D-11 and D-12 so the existing fast, service-free path appends `test/scrypath/phase110_contract_test.exs` to `@fast_tests` and the moduledoc/help text lists the new file explicitly. Keep `--live` behavior, environment checks, progress markers, and orchestration-only posture unchanged. Update `test/mix/tasks/verify_adopter_test.exs` so the source assertions and help-output assertions require the new fast test file while preserving the current stray-arg, unknown-flag, conflicting-mode, missing-env, and unreachable-service coverage. Do not add `mix verify.phase110`, do not touch `mix.exs` to register a new command, and do not add a new CI lane.</action>
  <verify>
    <automated>sh -c 'mix test test/mix/tasks/verify_adopter_test.exs -x && mix verify.adopter'</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/verify.adopter.ex` contains `test/scrypath/phase110_contract_test.exs` in `@fast_tests` and in the fast-path help text.
    - `test/mix/tasks/verify_adopter_test.exs` asserts the new fast file is present while leaving live prerequisite coverage intact.
    - `mix.exs` does not gain any `verify.phase110` preferred env entry, and no new `lib/mix/tasks/verify.phase110.ex` file is introduced.
    - `mix test test/mix/tasks/verify_adopter_test.exs -x` passes.
    - `mix verify.adopter` passes and now executes the Phase 110 contract test.
  </acceptance_criteria>
  <done>The Phase 110 support-routing contract is enforced through the existing fast adopter verification path, with no new phase-specific gate or CI sprawl.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in docs/template surfaces -> fast verification lane | Drift in support routing or evidence vocabulary must be caught by service-free tests before maintainers trust the docs. |
| `mix verify.adopter` help/source -> maintainer behavior | If help text and fast-test wiring diverge, maintainers may run the wrong proof or assume coverage that does not exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05 | Tampering | `test/scrypath/phase110_contract_test.exs` coverage | mitigate | Add explicit assertions for authority routing, tuple absence, Evidence Block headings, finding buckets, maintainer actions, and redaction guidance. |
| T-110-06 | Repudiation | `lib/mix/tasks/verify.adopter.ex` fast-path contract | mitigate | Append the new Phase 110 suite to `@fast_tests`, update help text, and keep source/help assertions in `test/mix/tasks/verify_adopter_test.exs`. |
| T-110-07 | Denial | verify-task sprawl and maintainer confusion | mitigate | Reuse `mix verify.adopter`, forbid `mix verify.phase110`, and keep the fast lane service-free and focused. |
| T-110-08 | Tampering | evergreen docs-contract expectations | mitigate | Update `test/scrypath/docs_contract_test.exs` only where the fast-file list changed so existing broad docs coverage remains accurate and bounded. |
| T-110-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are introduced; this plan is limited to ExUnit and existing Mix-task wiring. |
</threat_model>

<verification>
- `mix test test/scrypath/phase110_contract_test.exs test/scrypath/docs_contract_test.exs -x`
- `mix test test/mix/tasks/verify_adopter_test.exs -x`
- `mix verify.adopter`
</verification>

<success_criteria>
- `mix verify.adopter` fast mode includes focused Phase 110 contract coverage and stays service-free.
- Support authority, evidence routing, public redaction guidance, and route-only public entrypoint behavior are pinned by deterministic tests.
- No `mix verify.phase110` command or new CI lane is introduced.
</success_criteria>

<output>
Create `.planning/phases/110-support-intake-and-evidence-routing/110-02-SUMMARY.md` when done
</output>
