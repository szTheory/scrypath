---
phase: 104
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
  - examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/page_controller_test.exs
  - examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_test.exs
autonomous: true
requirements:
  - INT-01
  - INT-02

must_haves:
  truths:
    - "Operators can access the embedded scrypath_ops admin dashboard to observe indexing activity"
    - "Products are configured for indexing via Scrypath with Oban sync mode"
  artifacts:
    - path: "examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex"
      provides: "Admin dashboard mount"
    - path: "examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex"
      provides: "Search schema configuration"
      contains: "use Scrypath"
  key_links:
    - from: "examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex"
      to: "Scrypath.sync_record"
      via: "enqueueing Oban job for search sync"
---

<objective>
Mount the operations dashboard and establish the search schema for the Product domain model.

Purpose: To integrate Scrypath directly into the existing ecommerce application and verify basic indexing can be enqueued.
Output: A mounted admin dashboard and a searchable Product schema triggering sync on mutations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex
@examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex
@examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount scrypath_ops Admin Dashboard</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/router.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce_web/controllers/page_controller_test.exs
  </files>
  <behavior>
    - Test 1: GET /admin/search responds with success or appropriate authentication redirect/challenge
  </behavior>
  <action>Create an `/admin` scope in the router using the existing `:browser` pipeline. Import `ScrypathOps.Router` and mount `scrypath_ops_routes("/search")` inside the `/admin` scope to satisfy INT-01. Add tests in `page_controller_test.exs` to verify the route.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/controllers/page_controller_test.exs</automated>
  </verify>
  <done>Admin router scope is defined and mounts the operations dashboard</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure Product Schema for Search</name>
  <files>examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex</files>
  <behavior>
    - Test 1: Product schema compiles with Scrypath metadata correctly attached
  </behavior>
  <action>Add `use Scrypath` to the Product schema. Configure `fields: [:name, :description]`, `tenant_field: :tenant_id`, `faceting: [attributes: [:category_id]]`, and `sync_mode: :oban` with `oban_queue: :scrypath_sync` to satisfy INT-02.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix compile</automated>
  </verify>
  <done>Product schema exposes Scrypath metadata for sync operations</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Trigger Product Sync on Mutations</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_test.exs
  </files>
  <behavior>
    - Test 1: create_product enqueues a Scrypath.sync_record job
    - Test 2: update_product enqueues a Scrypath.sync_record job
    - Test 3: delete_product enqueues a Scrypath.delete_record job
  </behavior>
  <action>Update `create_product/2`, `update_product/3`, and `delete_product/2` to trigger `Scrypath.sync_record/2` (or `delete_record/2`) immediately upon successful database write. Since `sync_mode: :oban` is configured, this will automatically enqueue the sync job rather than making an inline HTTP call. Write accompanying tests in `catalog_test.exs`.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce/catalog_test.exs</automated>
  </verify>
  <done>Catalog correctly pushes product mutations to the search sync queue</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin Router | Protects internal dashboard endpoints from unauthenticated internet traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Information Disclosure | Admin Dashboard | mitigate | Place `scrypath_ops` inside a router pipeline ensuring future authentication compatibility |
</threat_model>

<verification>
cd examples/scrypath_ecommerce && mix test
</verification>

<success_criteria>
- scrypath_ops is reachable at `/admin/search` via the host router
- Products implicitly declare search configuration
- Product mutations enqueue Oban jobs for synchronization
</success_criteria>

<output>
Create `.planning/phases/104-search-integration-operations-proof/104-01-SUMMARY.md` when done
</output>