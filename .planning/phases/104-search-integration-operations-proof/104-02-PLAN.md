---
phase: 104
plan: 02
type: execute
wave: 2
depends_on:
  - 104-01-PLAN.md
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/workers/category_sync_worker.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
  - examples/scrypath_ecommerce/test/scrypath_ecommerce/workers/category_sync_worker_test.exs
  - examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_test.exs
autonomous: true
requirements:
  - INT-03

must_haves:
  truths:
    - "Changing a category name automatically triggers related product search index updates"
  artifacts:
    - path: "examples/scrypath_ecommerce/lib/scrypath_ecommerce/workers/category_sync_worker.ex"
      provides: "Fan-out worker for related data"
  key_links:
    - from: "examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex"
      to: "ScrypathEcommerce.Workers.CategorySyncWorker"
      via: "Ecto.Multi enqueue during update_category"
---

<objective>
Implement related-data propagation so that changing a Category automatically updates its associated Products in the search index.

Purpose: To demonstrate the outbox pattern handling eventual consistency for related data using Ecto.Multi and Oban.
Output: An Oban worker for category fan-out and a transactional update flow for categories.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Category Fan-Out Worker</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/workers/category_sync_worker.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce/workers/category_sync_worker_test.exs
  </files>
  <behavior>
    - Test 1: Worker perform/1 correctly batches and syncs products for the given category
  </behavior>
  <action>Create an Oban worker `ScrypathEcommerce.Workers.CategorySyncWorker` (queue: `:scrypath_sync`). Its `perform/1` function accepts a `%{"category_id" => id, "tenant_id" => tenant_id}` argument, fetches the Category, and calls `Scrypath.sync_related(ScrypathEcommerce.Catalog.Product, category)` to batch-update Meilisearch documents for the products belonging to the category. (Assuming `Scrypath.sync_related/2` exists, or simply fetching products and calling `Scrypath.sync_records`). Write a test for this behavior in `category_sync_worker_test.exs`.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce/workers/category_sync_worker_test.exs</automated>
  </verify>
  <done>Worker handles category ID payload and triggers product syncs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transactional Category Updates</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_test.exs
  </files>
  <behavior>
    - Test 1: update_category transaction enqueues CategorySyncWorker upon success
  </behavior>
  <action>Refactor `update_category/3` to use `Ecto.Multi`. The transaction should update the Category changeset and then enqueue `CategorySyncWorker` in the same transaction using `Oban.insert/1`. This ensures related data is only propagated if the database transaction succeeds. Add assertions to the `catalog_test.exs` file to verify the enqueue.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce/catalog_test.exs</automated>
  </verify>
  <done>Category updates reliably trigger background sync for related products</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Background Sync | Internal job queue communicating with external search index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-02 | Denial of Service | Oban Worker | accept | Queue limits (concurrency 5) handle burst updates to prevent Meilisearch overload |
</threat_model>

<verification>
cd examples/scrypath_ecommerce && mix test
</verification>

<success_criteria>
- The worker exists and acts on a category ID payload
- `update_category/3` uses Ecto.Multi to guarantee enqueueing
</success_criteria>

<output>
Create `.planning/phases/104-search-integration-operations-proof/104-02-SUMMARY.md` when done
</output>