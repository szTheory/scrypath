---
phase: 104
plan: 02
type: execute
wave: 2
depends_on:
  - 104-01-PLAN.md
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/category.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex
  - examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_test.exs
autonomous: true
requirements:
  - INT-03

must_haves:
  truths:
    - "Changing a category name automatically triggers related product search index updates"
  artifacts:
    - path: "examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex"
      provides: "Transactional related-data fan-out through Scrypath.sync_related/3"
  key_links:
    - from: "examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex"
      to: "Scrypath.sync_related"
      via: "Ecto.Multi update_category flow calling Scrypath.sync_related/3"
---

<objective>
Implement related-data propagation so that changing a Category automatically updates its associated Products in the search index.

Purpose: To demonstrate the outbox pattern handling eventual consistency for related data using Ecto.Multi and Oban.
Output: A transactional category update flow that uses Scrypath's built-in related-sync worker for category-to-product fan-out.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configure Category Fan-Out Metadata</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/category.ex
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_test.exs
  </files>
  <behavior>
    - Test 1: Scrypath related-sync metadata resolves category products and projects category names
  </behavior>
  <action>Configure `Category` fan-out metadata to use Scrypath's built-in related-sync path for products. Add a `Catalog.resolve_products_for_categories/1` resolver that reloads category-preloaded products and ensure `Product.search_document/1` includes `category_name` so category changes are visible in indexed product documents. Write focused catalog tests for resolver behavior and projection output.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce/catalog_test.exs</automated>
  </verify>
  <done>Category fan-out metadata resolves related products and projects category names</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transactional Category Updates</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_test.exs
  </files>
  <behavior>
    - Test 1: update_category transaction enqueues Scrypath.Sync.RelatedWorker upon success
  </behavior>
  <action>Refactor `update_category/3` to use `Ecto.Multi`. The transaction should update the Category changeset and then call `Scrypath.sync_related(Category, category, fan_out: :products)` in the same transaction so Scrypath enqueues `Scrypath.Sync.RelatedWorker`. This ensures related data is only propagated if the database transaction succeeds. Add assertions to the `catalog_test.exs` file to verify the enqueue.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce/catalog_test.exs</automated>
  </verify>
  <done>Category updates reliably trigger background sync for related products</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Background Sync | Internal job queue communicating with external search index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-02 | Denial of Service | Oban Worker | accept | Queue limits (concurrency 5) handle burst updates to prevent Meilisearch overload |
</threat_model>

<verification>
cd examples/scrypath_ecommerce && mix test
</verification>

<success_criteria>
- The worker exists and acts on a category ID payload
- Scrypath related-sync metadata resolves product fan-out for category changes
- `update_category/3` uses Ecto.Multi to guarantee enqueueing
</success_criteria>

<output>
Create `.planning/phases/104-search-integration-operations-proof/104-02-SUMMARY.md` when done
</output>
