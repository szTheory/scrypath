---
phase: 104
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog_fixtures.ex
  - examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex
  - examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_fixtures_test.exs
autonomous: true
requirements:
  - INT-04

must_haves:
  truths:
    - "System provides deterministic, hand-crafted specific product hierarchies for stable E2E testing"
  artifacts:
    - path: "examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog_fixtures.ex"
      provides: "Deterministic E2E seed scenario"
    - path: "examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex"
      provides: "Mix task to generate seed data"
  key_links:
    - from: "examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex"
      to: "ScrypathEcommerce.CatalogFixtures.scenario_e2e_search_catalog"
      via: "invoking fixture scenario on seed execution"
---

<objective>
Implement specific product hierarchies for stable E2E testing.

Purpose: To guarantee stable E2E testing in Phase 105 and clearly demonstrate related-data propagation when a category name changes (Decision D-04).
Output: Deterministic fixture scenario and mix seed task logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog_fixtures.ex
@examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create deterministic E2E scenario fixture</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog_fixtures.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce/catalog_fixtures_test.exs
  </files>
  <behavior>
    - Test 1: scenario_e2e_search_catalog creates a specific hierarchy of products and categories
  </behavior>
  <action>Add a `scenario_e2e_search_catalog/1` function to `ScrypathEcommerce.CatalogFixtures`. This should generate a specific tech product hierarchy (e.g., "Smartphones", "Laptops") with distinct products and variants designed for search assertions (e.g., specific terms like "Quantum" or "Cyber" that can be deterministically found). Add a test asserting it creates the correct number of entities.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce/catalog_fixtures_test.exs</automated>
  </verify>
  <done>Fixture provides repeatable hierarchy generation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire mix seed task to E2E scenario</name>
  <files>
    examples/scrypath_ecommerce/lib/mix/tasks/scrypath.seed.ex
  </files>
  <behavior>
    - Test 1: Running the seed task populates the DB using the E2E fixture (if unit tested)
  </behavior>
  <action>Update `Mix.Tasks.Scrypath.Seed.run/1` to invoke `ScrypathEcommerce.CatalogFixtures.scenario_e2e_search_catalog/1`. Ensure the script output mentions which scenario was seeded.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix compile</automated>
  </verify>
  <done>mix scrypath.seed effectively applies the deterministic test scenario</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testing Data | Not applicable to production environment security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-05 | Information Disclosure | Seed Data | accept | Fixture data is mock test data containing no PII |
</threat_model>

<verification>
cd examples/scrypath_ecommerce && mix compile
</verification>

<success_criteria>
- Deterministic seed data functions are exposed in fixtures
- Seed mix task runs cleanly and applies the intended hierarchy
</success_criteria>

<output>
Create `.planning/phases/104-search-integration-operations-proof/104-04-SUMMARY.md` when done
</output>