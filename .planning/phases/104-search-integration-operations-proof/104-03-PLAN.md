---
phase: 104
plan: 03
type: execute
wave: 3
depends_on:
  - 104-01-PLAN.md
  - 104-02-PLAN.md
files_modified:
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/live/search_live.ex
  - examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/live/search_live.html.heex
  - examples/scrypath_ecommerce/test/scrypath_ecommerce_web/live/search_live_test.exs
autonomous: true
requirements:
  - INT-04

must_haves:
  truths:
    - "Users can search and filter products via a facet-driven storefront UI"
    - "Search queries strictly isolate data by the active tenant"
  artifacts:
    - path: "examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/live/search_live.ex"
      provides: "LiveView URL-driven state sync"
    - path: "examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/live/search_live.html.heex"
      provides: "Unified search and facet form"
  key_links:
    - from: "examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/live/search_live.ex"
      to: "Scrypath.search"
      via: "handle_params calling search with tenant isolation"
---

<objective>
Build a zero-JS, URL-driven LiveView storefront with facet-driven search and tenant-safe access.

Purpose: To provide a realistic end-user experience proving that Scrypath's URL-driven patterns are ergonomic for Phoenix teams.
Output: A functional search UI filtering products and rendering facet counts with strict tenant isolation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/live/search_live.ex
@examples/scrypath_ecommerce/lib/scrypath_ecommerce/catalog/product.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Tenant-Safe URL-Driven Search</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/live/search_live.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce_web/live/search_live_test.exs
  </files>
  <behavior>
    - Test 1: URL parameters hydrate into a Scrypath.search call with the correct tenant context
  </behavior>
  <action>Update `handle_params/3` to act as the single source of truth. Parse `q` and `category_id` from the URL parameters. Resolve a default `tenant_id` (e.g., from `Catalog.list_tenants() |> List.first()`) to satisfy tenant isolation. Call `Scrypath.search(ScrypathEcommerce.Catalog.Product, q, filter: [category_id: category_id], tenant_id: tenant.id)` to fetch search results and facets. Assign the results and a `to_form` representation of the params to the socket. Add unit tests for the mount and handle_params logic.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/live/search_live_test.exs</automated>
  </verify>
  <done>LiveView correctly hydrates search state from URL and strictly enforces tenant scope</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handle Search Events</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/live/search_live.ex
    examples/scrypath_ecommerce/test/scrypath_ecommerce_web/live/search_live_test.exs
  </files>
  <behavior>
    - Test 1: "search" phx-change event maps to push_patch
  </behavior>
  <action>Implement a `handle_event("search", %{"search" => params}, socket)` callback. The event should map the incoming form data to URL query parameters and call `push_patch(socket, to: ~p"/?#{query_params}")`. Do not call `Scrypath.search` here; rely entirely on `push_patch` triggering `handle_params`. Verify via live view test.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/live/search_live_test.exs</automated>
  </verify>
  <done>Form events successfully translate to URL parameter patches</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build Unified Search Form UI</name>
  <files>
    examples/scrypath_ecommerce/lib/scrypath_ecommerce_web/live/search_live.html.heex
    examples/scrypath_ecommerce/test/scrypath_ecommerce_web/live/search_live_test.exs
  </files>
  <behavior>
    - Test 1: Render search form with debounced text input and checkboxes for facets
  </behavior>
  <action>Create a unified `<.form for={@form} phx-change="search">`. Inside the form, include a text input for the `q` parameter with `phx-debounce="300"`. Add a sidebar section mapping over the `results.facets.category_id` (if any), rendering checkboxes for each facet value. Ensure the main content area renders the resulting `results.hits` list. Assert presence of these elements in tests.</action>
  <verify>
    <automated>cd examples/scrypath_ecommerce && mix test test/scrypath_ecommerce_web/live/search_live_test.exs</automated>
  </verify>
  <done>UI correctly renders the form, debounced input, and auto-applying facets</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView Mount/Params | Entry point for user-controlled query data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-03 | Information Disclosure | SearchLive | mitigate | Hardcode or resolve `tenant_id` at the mount/params level and strictly enforce it in `Scrypath.search` calls to prevent cross-tenant data leaks |
| T-104-04 | Tampering | URL Parameters | mitigate | Ensure search inputs use standard Phoenix forms and do not execute raw eval or unescaped HTML interpolation |
</threat_model>

<verification>
cd examples/scrypath_ecommerce && mix test
</verification>

<success_criteria>
- A debounced text input updates the URL without custom JS
- Facet selections are reflected in the URL and filter results
- Cross-tenant data cannot be accessed
</success_criteria>

<output>
Create `.planning/phases/104-search-integration-operations-proof/104-03-SUMMARY.md` when done
</output>