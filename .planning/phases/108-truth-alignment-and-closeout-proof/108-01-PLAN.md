---
phase: 108
plan: 1
type: execute
wave: 1
depends_on:
  - 107-01
files_modified:
  - guides/related-data-and-reindexing.md
  - CONTRIBUTING.md
  - lib/mix/tasks/verify.phase108.ex
  - test/mix/tasks/verify.phase108_test.exs
  - test/scrypath/phase108_contract_test.exs
  - mix.exs
  - docs/jtbd-gap-map.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - TRUTH-01
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04: `guides/related-data-and-reindexing.md` presents `use Scrypath, fan_outs:` as the ordinary searchable-schema path, keeps hand-written `__scrypath__/1` as a supported owner-only escape hatch, and preserves the `sync_mode: :oban` plus dual-shape resolver warnings."
    - "D-08/D-09/D-10/D-11: `mix verify.phase108` is a focused, service-free gate that rejects stray args, runs only phase108 contract tests plus its task self-test, and does not promote `phase105-e2e` or add a new required CI job."
    - "D-05/D-06/D-07/D-12/D-13/D-14: `docs/jtbd-gap-map.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/PROJECT.md` describe v1.29 as a completed repair-closeout milestone that returns Scrypath to maintenance-and-evidence mode without reopening deferred feature breadth."
  artifacts:
    - path: "guides/related-data-and-reindexing.md"
      provides: "Canonical related-data wording for ordinary fan-out declarations and advanced owner-only escape hatch"
      contains: "use Scrypath, fan_outs:"
    - path: "lib/mix/tasks/verify.phase108.ex"
      provides: "Focused Phase 108 truth-alignment verification task"
      contains: "defmodule Mix.Tasks.Verify.Phase108"
    - path: "test/scrypath/phase108_contract_test.exs"
      provides: "Stable token and ordering checks across bounded truth surfaces"
      contains: "@moduletag :phase108_contract"
    - path: ".planning/PROJECT.md"
      provides: "Milestone closeout truth for v1.29 and post-closeout operating mode"
      contains: "maintenance-and-evidence mode"
  key_links:
    - from: "mix.exs"
      to: "Mix.Tasks.Verify.Phase108"
      via: "\"verify.phase108\": :test"
      pattern: "preferred_envs includes the phase108 task"
    - from: "lib/mix/tasks/verify.phase108.ex"
      to: "test/scrypath/phase108_contract_test.exs"
      via: "@focused_tests"
      pattern: "task runs only the phase108 contract suite and task self-test"
    - from: "test/scrypath/phase108_contract_test.exs"
      to: "guides/related-data-and-reindexing.md, docs/jtbd-gap-map.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, CONTRIBUTING.md"
      via: "File.read!/1 token and ordering assertions"
      pattern: "bounded truth-surface drift gate"
---

<objective>
Lock the final v1.29 truth surfaces so related-data guidance, contributor verification posture, and planning/JTBD closeout language all describe the repaired contract accurately without promoting `phase105-e2e` or reopening deferred feature breadth.

Purpose: Prevent truth drift at milestone closeout and give maintainers a fast local proof command for TRUTH-01.
Output: Updated authority docs plus a focused `mix verify.phase108` gate and self-tests that keep the closeout posture reproducible.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-truth-alignment-and-closeout-proof/108-CONTEXT.md
@.planning/phases/108-truth-alignment-and-closeout-proof/108-RESEARCH.md
@.planning/phases/108-truth-alignment-and-closeout-proof/108-VALIDATION.md
@.planning/phases/108-truth-alignment-and-closeout-proof/108-PATTERNS.md
@.planning/phases/106-fan-out-reflection-contract-repair/106-01-SUMMARY.md
@.planning/phases/107-ecommerce-readiness-regression-guard/107-01-SUMMARY.md
@.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-SCOPE-GUARD.md
@guides/related-data-and-reindexing.md
@docs/jtbd-gap-map.md
@CONTRIBUTING.md

<interfaces>
From `mix.exs`:
```elixir
preferred_envs: [
  "verify.phase99": :test,
  "verify.phase106": :test,
  "verify.phase107": :test
]
```

From `lib/mix/tasks/verify.phase106.ex` and `lib/mix/tasks/verify.phase99.ex`:
```elixir
@focused_tests [...]

def run(args) do
  ensure_no_args!(args)
  Mix.Task.run("app.start")
  Mix.Task.reenable("test")
  Mix.Task.run("test", @focused_tests)
end
```

From `test/scrypath/phase99_contract_test.exs`:
```elixir
assert_contains_all(...)
assert_absent_all(...)
assert ordered?(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 108-01-01: Rewrite the related-data authority around the repaired ordinary fan-out path</name>
  <files>guides/related-data-and-reindexing.md</files>
  <read_first>
  - `guides/related-data-and-reindexing.md`
  - `examples/phoenix_meilisearch/lib/scrypath_demo/blog/author.ex`
  - `.planning/phases/108-truth-alignment-and-closeout-proof/108-CONTEXT.md`
  - `.planning/phases/108-truth-alignment-and-closeout-proof/108-RESEARCH.md`
  - `.planning/phases/108-truth-alignment-and-closeout-proof/108-PATTERNS.md`
  - `.planning/phases/106-fan-out-reflection-contract-repair/106-01-SUMMARY.md`
  </read_first>
  <action>
  - In `guides/related-data-and-reindexing.md`, update the ordinary fan-out declaration section per D-01/D-02/D-03/D-04/D-05/D-06 so searchable schemas explicitly use `use Scrypath, fan_outs: [...]` as the default contract and rely on generated `__scrypath__(:fan_outs)`.
  - Keep `examples/phoenix_meilisearch/lib/scrypath_demo/blog/author.ex` framed per D-02 as the supported low-level owner-only escape hatch for schemas that intentionally do not `use Scrypath`; do not label hand-written `__scrypath__/1` as deprecated, legacy, or the normal copy-paste path.
  - Preserve the two operational warnings per D-03: `sync_mode: :oban` means the work is durably queued rather than searchable immediately, and resolver MFAs must accept both inline owning-record lists and Oban document-id lists.
  - Do not advertise or hint at deferred surfaces per D-04: no `Scrypath.FanOuts`, no `Scrypath.schema_fan_outs/1`, and no duplicate/nil fan-out validation tightening in this guide.
  </action>
  <acceptance_criteria>
  - `rg "use Scrypath, fan_outs:|generated `__scrypath__\\(:fan_outs\\)`|ordinary schemas" guides/related-data-and-reindexing.md` returns matches showing the declarative path is the ordinary contract.
  - `rg "owner-only|low-level escape hatch|intentionally do not `use Scrypath`" guides/related-data-and-reindexing.md` returns matches showing hand-written `__scrypath__/1` remains supported but advanced.
  - `rg "sync_mode: :oban|durably queued|document IDs|records" guides/related-data-and-reindexing.md` returns matches covering the two footgun warnings.
  - `rg "Scrypath\\.FanOuts|schema_fan_outs|duplicate fan-out|nil fan-out" guides/related-data-and-reindexing.md` exits non-zero.
  </acceptance_criteria>
  <verify>
  - `rg "use Scrypath, fan_outs:|generated `__scrypath__\\(:fan_outs\\)`|ordinary schemas" guides/related-data-and-reindexing.md`
  - `rg "owner-only|low-level escape hatch|intentionally do not `use Scrypath`" guides/related-data-and-reindexing.md`
  - `rg "sync_mode: :oban|durably queued|document IDs|records" guides/related-data-and-reindexing.md`
  - `! rg "Scrypath\\.FanOuts|schema_fan_outs|duplicate fan-out|nil fan-out" guides/related-data-and-reindexing.md`
  </verify>
  <done>
  - The related-data guide presents the repaired declarative fan-out path as ordinary, keeps hand-written reflection as an advanced owner-only escape hatch, preserves the Oban/resolver warnings, and does not leak deferred fan-out breadth into v1.29 authority text.
  </done>
</task>

<task type="auto">
  <name>Task 108-01-02: Add the service-free Phase 108 truth gate and keep contributor CI posture explicit</name>
  <files>CONTRIBUTING.md, lib/mix/tasks/verify.phase108.ex, test/mix/tasks/verify.phase108_test.exs, test/scrypath/phase108_contract_test.exs, mix.exs</files>
  <read_first>
  - `CONTRIBUTING.md`
  - `.github/workflows/ci.yml`
  - `lib/mix/tasks/verify.phase99.ex`
  - `lib/mix/tasks/verify.phase106.ex`
  - `lib/mix/tasks/verify.phase107.ex`
  - `test/mix/tasks/verify.phase99_test.exs`
  - `test/mix/tasks/verify.phase106_test.exs`
  - `test/scrypath/phase99_contract_test.exs`
  - `mix.exs`
  - `.planning/phases/108-truth-alignment-and-closeout-proof/108-VALIDATION.md`
  - `.planning/phases/108-truth-alignment-and-closeout-proof/108-PATTERNS.md`
  </read_first>
  <action>
  - Update `CONTRIBUTING.md` per D-05/D-06/D-08/D-11 so the v1.29 closeout proof is discoverable via `mix verify.phase108`, while the routine required merge blockers stay `main-ci`, `repo-hygiene`, `release-truth`, and `phase99-trust`, and `phase105-e2e` remains explicitly advisory.
  - Create `lib/mix/tasks/verify.phase108.ex` with module `Mix.Tasks.Verify.Phase108`, `use Mix.Task`, `ensure_no_args!/1`, `Mix.Task.run("app.start")`, `Mix.Task.reenable("test")`, and a focused test list containing exactly `test/scrypath/phase108_contract_test.exs` and `test/mix/tasks/verify.phase108_test.exs`.
  - Emit readable shell output containing the exact marker `verify.phase108: truth alignment and closeout proof checks`.
  - Create `test/mix/tasks/verify.phase108_test.exs` with stray-argument rejection coverage, `mix help verify.phase108` discoverability coverage, and source assertions that the task file names both focused test paths and the exact marker string.
  - Create `test/scrypath/phase108_contract_test.exs` per D-08/D-09/D-10 with stable token and ordering assertions over this bounded truth set only: `guides/related-data-and-reindexing.md`, `docs/jtbd-gap-map.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`, `CONTRIBUTING.md`, `mix.exs`, and `.github/workflows/ci.yml`.
  - Register `"verify.phase108": :test` in `mix.exs` next to the existing phase verify entries.
  - Do not add a new GitHub Actions job, do not edit `.github/workflows/ci.yml`, and do not broaden the gate to Meilisearch, Playwright, the ecommerce example stack, or `docs_contract_test.exs`.
  </action>
  <acceptance_criteria>
  - `test -f lib/mix/tasks/verify.phase108.ex` exits 0.
  - `test -f test/mix/tasks/verify.phase108_test.exs` exits 0.
  - `test -f test/scrypath/phase108_contract_test.exs` exits 0.
  - `rg "mix verify.phase108|phase105-e2e|advisory|phase99-trust" CONTRIBUTING.md` returns matches that keep the gate discoverable and the browser lane non-required.
  - `rg "defmodule Mix.Tasks.Verify.Phase108|test/scrypath/phase108_contract_test.exs|test/mix/tasks/verify.phase108_test.exs|verify.phase108: truth alignment and closeout proof checks" lib/mix/tasks/verify.phase108.ex` returns at least 4 matches.
  - `rg "\"verify.phase108\": :test" mix.exs` returns 1 match.
  - `mix test test/mix/tasks/verify.phase108_test.exs` exits 0.
  - `mix test test/scrypath/phase108_contract_test.exs` exits 0.
  - `mix verify.phase108` exits 0.
  - `git diff -- .github/workflows/ci.yml` produces no Phase 108 changes.
  </acceptance_criteria>
  <verify>
  - `test -f lib/mix/tasks/verify.phase108.ex`
  - `test -f test/mix/tasks/verify.phase108_test.exs`
  - `test -f test/scrypath/phase108_contract_test.exs`
  - `rg "mix verify.phase108|phase105-e2e|advisory|phase99-trust" CONTRIBUTING.md`
  - `rg "defmodule Mix.Tasks.Verify.Phase108|test/scrypath/phase108_contract_test.exs|test/mix/tasks/verify.phase108_test.exs|verify.phase108: truth alignment and closeout proof checks" lib/mix/tasks/verify.phase108.ex`
  - `rg "\"verify.phase108\": :test" mix.exs`
  - `mix test test/mix/tasks/verify.phase108_test.exs`
  - `mix test test/scrypath/phase108_contract_test.exs`
  - `mix verify.phase108`
  - `git diff -- .github/workflows/ci.yml`
  </verify>
  <done>
  - Contributors have a focused `mix verify.phase108` command, the task is wired and self-tested, the bounded truth surfaces are contract-checked service-free, and the repo still treats `phase105-e2e` as advisory with no CI promotion.
  </done>
</task>

<task type="auto">
  <name>Task 108-01-03: Close v1.29 in the planning and JTBD authorities without widening scope</name>
  <files>docs/jtbd-gap-map.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
  - `docs/jtbd-gap-map.md`
  - `.planning/ROADMAP.md`
  - `.planning/REQUIREMENTS.md`
  - `.planning/PROJECT.md`
  - `.planning/milestone-candidates.md`
  - `README.md`
  - `guides/overview.md`
  - `.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-SCOPE-GUARD.md`
  - `.planning/phases/108-truth-alignment-and-closeout-proof/108-CONTEXT.md`
  - `.planning/phases/108-truth-alignment-and-closeout-proof/108-RESEARCH.md`
  - `lib/mix/tasks/verify.phase108.ex`
  - `test/scrypath/phase108_contract_test.exs`
  </read_first>
  <action>
  - In `docs/jtbd-gap-map.md`, update the active-priority and closeout sections per D-05/D-06/D-12/D-13/D-14 so v1.29 reads as a completed repair-closeout milestone, the remaining confidence gap is outside-adopter evidence and proof stability, and future breadth reopens only with reviewed outside-adopter evidence or a concrete production bug.
  - In `.planning/PROJECT.md`, replace the active-v1.29 framing with completed-milestone wording that explicitly names the three closed outcomes: repaired generated `__scrypath__(:fan_outs)` for `use Scrypath, fan_outs:`, tenant-preserving ecommerce readiness regression proof, and aligned roadmap/JTBD truth; keep `phase105-e2e` advisory and make `maintenance-and-evidence mode` the default operating lane.
  - In `.planning/ROADMAP.md`, mark Phase 108 complete, mark the v1.29 milestone complete once `mix verify.phase108` is green, and update the `Next` section so it points to maintenance, release/support truth, and outside-adopter evidence instead of an active feature wedge.
  - In `.planning/REQUIREMENTS.md`, mark `TRUTH-01` complete and preserve the deferred/out-of-scope rows for `Scrypath.FanOuts`, `Scrypath.schema_fan_outs/1`, duplicate/nil fan-out validation tightening, deeper cross-tenant Playwright expansion, and `phase105-e2e` promotion.
  - Per D-06/D-07, treat `README.md`, `guides/overview.md`, and `.planning/milestone-candidates.md` as contradiction checks only. Do not edit them unless a direct contradiction to the new closeout truth is found; if that happens, make the minimal route-map correction rather than turning them into second authorities.
  </action>
  <acceptance_criteria>
  - `rg "maintenance-and-evidence mode|outside-adopter evidence|concrete production bug" docs/jtbd-gap-map.md .planning/PROJECT.md` returns matches that show the post-v1.29 operating mode and reopen policy.
  - `rg "✅ \\*\\*Phase 108: Truth Alignment and Closeout Proof\\*\\*|Plans: 1/1 plans complete" .planning/ROADMAP.md` returns matches showing the phase is closed.
  - `rg "\\[x\\] \\*\\*TRUTH-01\\*\\*" .planning/REQUIREMENTS.md` returns 1 match.
  - `rg "phase105-e2e|advisory" docs/jtbd-gap-map.md .planning/ROADMAP.md .planning/PROJECT.md` returns matches that keep the browser lane non-required in closeout language.
  - `mix verify.phase108` exits 0 after the planning/JTBD edits land.
  </acceptance_criteria>
  <verify>
  - `rg "maintenance-and-evidence mode|outside-adopter evidence|concrete production bug" docs/jtbd-gap-map.md .planning/PROJECT.md`
  - `rg "✅ \\*\\*Phase 108: Truth Alignment and Closeout Proof\\*\\*|Plans: 1/1 plans complete" .planning/ROADMAP.md`
  - `rg "\\[x\\] \\*\\*TRUTH-01\\*\\*" .planning/REQUIREMENTS.md`
  - `rg "phase105-e2e|advisory" docs/jtbd-gap-map.md .planning/ROADMAP.md .planning/PROJECT.md`
  - `mix verify.phase108`
  </verify>
  <done>
  - The JTBD and planning authorities close v1.29 as completed repair work, keep deferred breadth evidence-gated, preserve the advisory `phase105-e2e` posture, and point the repo back to maintenance-and-evidence mode.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer edits -> canonical truth surfaces | Unreviewed doc or planning edits can silently drift the ordinary fan-out contract or milestone closeout policy. |
| Contributor docs -> CI expectations | Wording changes can accidentally promote advisory `phase105-e2e` into perceived required merge policy. |
| Local verify command -> repository test/runtime surface | An over-broad phase gate can trigger slow or side-effectful services, hiding the point of a closeout proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering | `guides/related-data-and-reindexing.md`, `docs/jtbd-gap-map.md`, `.planning/*` | mitigate | Bound the authority set to the files in D-05, assert ordinary-vs-escape-hatch tokens plus reopen-policy tokens in `test/scrypath/phase108_contract_test.exs`, and keep deferred surfaces absent from related-data authority wording. |
| T-108-02 | Tampering | `CONTRIBUTING.md` and CI posture interpretation | mitigate | Keep required gate tokens fixed to `main-ci`, `repo-hygiene`, `release-truth`, and `phase99-trust`, assert `phase105-e2e` remains advisory, and forbid `.github/workflows/ci.yml` edits in this phase. |
| T-108-03 | Denial of Service | `mix verify.phase108` | mitigate | Implement a no-arg, service-free Mix task with an explicit two-file `@focused_tests` list and self-tests that reject broad or live-service expansions. |
| T-108-SC | Tampering | package-manager install surface | accept | No npm, pip, cargo, Hex, or other package-manager install work is in scope for Phase 108. |
</threat_model>

<verification>
- `mix test test/mix/tasks/verify.phase108_test.exs`
- `mix test test/scrypath/phase108_contract_test.exs`
- `mix verify.phase108`
- `rg "use Scrypath, fan_outs:|owner-only|low-level escape hatch|sync_mode: :oban|document IDs|records" guides/related-data-and-reindexing.md`
- `rg "mix verify.phase108|phase105-e2e|advisory|phase99-trust" CONTRIBUTING.md`
- `rg "\"verify.phase108\": :test" mix.exs`
- `rg "\\[x\\] \\*\\*TRUTH-01\\*\\*" .planning/REQUIREMENTS.md`
- `rg "maintenance-and-evidence mode|outside-adopter evidence|concrete production bug" docs/jtbd-gap-map.md .planning/PROJECT.md`
- `git diff -- .github/workflows/ci.yml`
</verification>

<success_criteria>
- Related-data docs describe `use Scrypath, fan_outs:` as the repaired ordinary contract and preserve the explicit owner-only escape hatch and operational warnings.
- `mix verify.phase108` provides a fast, deterministic, service-free proof for TRUTH-01 with task self-tests and bounded contract assertions.
- Planning and JTBD truth close v1.29 decisively, keep `phase105-e2e` advisory, and return Scrypath to maintenance-and-evidence mode.
- Phase 108 does not add runtime APIs, broaden docs authority unnecessarily, promote advisory CI lanes, or reopen deferred feature breadth.
</success_criteria>

<output>
Create `.planning/phases/108-truth-alignment-and-closeout-proof/108-01-SUMMARY.md` when done.
</output>
