---
phase: 08-reliability-and-contract-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/meilisearch.ex
  - lib/scrypath/meilisearch/tasks.ex
  - test/scrypath/meilisearch/tasks_test.exs
autonomous: true
requirements:
  - HARD-01
  - HARD-03
must_haves:
  truths:
    - "Inline Meilisearch task waiting returns explicit tuples for malformed payloads, terminal failures, cancellation, timeout, and success."
    - "Unknown or incomplete Meilisearch task payloads never pass through as successful normalized tasks."
  artifacts:
    - path: "lib/scrypath/meilisearch.ex"
      provides: "Strict enqueue-response task normalization boundary"
    - path: "lib/scrypath/meilisearch/tasks.ex"
      provides: "Strict poll-task normalization and inline wait result mapping"
    - path: "test/scrypath/meilisearch/tasks_test.exs"
      provides: "Deterministic contract coverage for malformed payload and terminal tuple semantics"
  key_links:
    - from: "lib/scrypath/meilisearch.ex"
      to: "lib/scrypath/meilisearch/tasks.ex"
      via: "shared normalized task map shape and status set"
      pattern: "invalid_task_payload|normalize_task"
    - from: "lib/scrypath/meilisearch/tasks.ex"
      to: "test/scrypath/meilisearch/tasks_test.exs"
      via: "explicit tuple assertions for D-01 through D-05"
      pattern: "invalid_task_payload|task_failed|cancelled|timeout"
---

<objective>
Harden the Meilisearch task contract so enqueue responses and polled task payloads are normalized through strict tuple-returning boundaries before inline orchestration uses them.

Purpose: Close the malformed-payload ambiguity called out by HARD-01 and D-01 through D-05 without widening the public API.
Output: Strict task normalization in the Meilisearch backend seam plus focused contract tests for explicit result tuples.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-reliability-and-contract-hardening/08-CONTEXT.md
@.planning/phases/08-reliability-and-contract-hardening/08-RESEARCH.md
@.planning/phases/08-reliability-and-contract-hardening/08-PATTERNS.md
@lib/scrypath/meilisearch.ex
@lib/scrypath/meilisearch/tasks.ex
@test/scrypath/meilisearch/tasks_test.exs

<interfaces>
From lib/scrypath/meilisearch.ex:
```elixir
@doc false
def normalize_task(response) do
  %{
    uid: response["taskUid"] || response[:taskUid] || response["uid"] || response[:uid],
    status: response["status"] || response[:status],
    type: response["type"] || response[:type],
    index_uid: response["indexUid"] || response[:indexUid],
    raw: response
  }
end
```

From lib/scrypath/meilisearch/tasks.ex:
```elixir
@spec wait_for_task(map(), keyword()) :: {:ok, map()} | {:error, term()}
def wait_for_task(task, config) when is_map(task)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden enqueue and poll task normalization per D-01 through D-05</name>
  <files>lib/scrypath/meilisearch.ex, lib/scrypath/meilisearch/tasks.ex</files>
  <read_first>.planning/phases/08-reliability-and-contract-hardening/08-CONTEXT.md, lib/scrypath/meilisearch.ex, lib/scrypath/meilisearch/tasks.ex</read_first>
  <acceptance_criteria>
    - `rg -n "invalid_task_payload" lib/scrypath/meilisearch.ex lib/scrypath/meilisearch/tasks.ex` finds the stable malformed-payload family from D-03.
    - `rg -n "stage: :initial|stage: :poll" lib/scrypath/meilisearch.ex lib/scrypath/meilisearch/tasks.ex` shows both stage values are emitted.
    - `rg -n ':enqueued|:processing|:succeeded|:failed|:cancelled' lib/scrypath/meilisearch.ex lib/scrypath/meilisearch/tasks.ex` shows the normalized public status set from D-05.
    - `rg -n ':queued' lib/scrypath/meilisearch.ex lib/scrypath/meilisearch/tasks.ex` shows compatibility handling is internal only and not emitted as a normalized public status.
  </acceptance_criteria>
  <action>Create strict task-normalization helpers at the existing Meilisearch seams, not a new subsystem. In `lib/scrypath/meilisearch.ex`, change `normalize_task/1` to return `{:ok, normalized_task}` or `{:error, {:invalid_task_payload, %{stage: :initial, task_uid: integer() | nil, problems: keyword(), payload: map()}}}` per D-02 and D-03. Validate required identity and status fields, collapse `"queued"` to `:enqueued` internally per D-05, and keep D-04 terminal families untouched. In `lib/scrypath/meilisearch/tasks.ex`, normalize the initial task and every polled response through the same strict contract with `stage: :poll` for poll failures, and stop the fallback clause that currently returns `{:ok, task}` for unknown statuses.</action>
  <verify>
    <automated>mix test test/scrypath/meilisearch/tasks_test.exs -x</automated>
  </verify>
  <done>Inline waiting cannot reach a success tuple from a malformed or unknown-status task payload, and enqueue/poll normalization share the same stable error family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand deterministic task contract tests for malformed payload and transport cases per D-11 through D-14</name>
  <files>test/scrypath/meilisearch/tasks_test.exs</files>
  <read_first>.planning/phases/08-reliability-and-contract-hardening/08-CONTEXT.md, test/scrypath/meilisearch/tasks_test.exs, lib/scrypath/meilisearch/tasks.ex, lib/scrypath/meilisearch.ex</read_first>
  <acceptance_criteria>
    - `rg -n "invalid_task_payload" test/scrypath/meilisearch/tasks_test.exs` finds assertions for both `stage: :initial` and `stage: :poll`.
    - `rg -n "unknown status|missing uid|transport" test/scrypath/meilisearch/tasks_test.exs` finds explicit coverage for malformed status, missing uid, and transport propagation.
    - `mix test test/scrypath/meilisearch/tasks_test.exs -x` exits 0.
  </acceptance_criteria>
  <action>Add fast ExUnit coverage in `test/scrypath/meilisearch/tasks_test.exs` for: malformed initial payload `%{"status" => "enqueued"}` returning `{:error, {:invalid_task_payload, %{stage: :initial, ...}}}`; malformed poll payload `%{"uid" => 301, "status" => "weird"}` returning `stage: :poll`; unusable uid values such as `nil`; preserved D-04 tuples for failed/cancelled/timeout paths; and plain transport error propagation from `client(config).task/2`. Keep these deterministic in the current sequenced client harness per D-11 through D-14 and do not add live-backend cases here.</action>
  <verify>
    <automated>mix test test/scrypath/meilisearch/tasks_test.exs -x</automated>
  </verify>
  <done>`test/scrypath/meilisearch/tasks_test.exs` covers the former malformed-payload confidence gap with stable tuple assertions and no live-service dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meilisearch HTTP response -> backend normalization | Untrusted external payload enters the library and must be validated before orchestration consumes it. |
| Inline task wait -> public tuple contract | Internal polling logic transforms backend state into caller-visible outcomes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | `lib/scrypath/meilisearch.ex` | mitigate | Reject missing uid and unknown status values with `{:invalid_task_payload, ...}` per D-01 through D-05. |
| T-08-02 | I | `lib/scrypath/meilisearch/tasks.ex` | mitigate | Prevent malformed poll payloads from being surfaced as success tuples by validating every poll response before recursion. |
| T-08-03 | R | `test/scrypath/meilisearch/tasks_test.exs` | mitigate | Lock tuple shapes in deterministic contract tests so regressions are caught without depending on live backend state. |
</threat_model>

<verification>
Run `mix test test/scrypath/meilisearch/tasks_test.exs -x` after each task and `mix test test/scrypath/meilisearch/tasks_test.exs test/scrypath/sync_test.exs -x` before writing the summary.
</verification>

<success_criteria>
Meilisearch task normalization is strict at both enqueue and poll boundaries, malformed payloads use the D-03 error family, and focused contract tests cover the inline wait outcomes required by HARD-01.
</success_criteria>

<output>
After completion, create `.planning/phases/08-reliability-and-contract-hardening/08-01-SUMMARY.md`
</output>
