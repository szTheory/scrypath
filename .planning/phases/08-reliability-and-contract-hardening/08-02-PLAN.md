---
phase: 08-reliability-and-contract-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/sync.ex
  - lib/scrypath/telemetry.ex
  - test/scrypath/sync_test.exs
  - test/scrypath/telemetry_test.exs
autonomous: true
requirements:
  - HARD-02
  - HARD-03
must_haves:
  truths:
    - "Empty sync and delete batches return one explicit no-op tuple across inline, manual, and oban modes."
    - "No-op results still emit common telemetry with `status: :noop` and `document_count: 0`."
  artifacts:
    - path: "lib/scrypath/sync.ex"
      provides: "Shared empty-batch no-op handling before backend dispatch and Oban enqueue"
    - path: "lib/scrypath/telemetry.ex"
      provides: "Public stop metadata support for no-op result envelopes"
    - path: "test/scrypath/sync_test.exs"
      provides: "Mode-by-mode no-op contract coverage"
    - path: "test/scrypath/telemetry_test.exs"
      provides: "No-op telemetry metadata assertions"
  key_links:
    - from: "lib/scrypath/sync.ex"
      to: "lib/scrypath/telemetry.ex"
      via: "Telemetry.stop_metadata(result)"
      pattern: "status: :noop|document_count: 0"
    - from: "lib/scrypath/sync.ex"
      to: "test/scrypath/sync_test.exs"
      via: "explicit no-op tuple assertions for sync and delete entrypoints"
      pattern: ":noop"
---

<objective>
Define the shared empty-batch contract so `Scrypath.sync_records/3` and `Scrypath.delete_documents/3` short-circuit to a common no-op envelope before backend or Oban work starts.

Purpose: Satisfy HARD-02 and the telemetry portion of HARD-03 with one library-owned behavior instead of mode-specific edge handling.
Output: Shared no-op behavior in `Scrypath.Sync`, corresponding stop metadata in `Scrypath.Telemetry`, and focused contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-reliability-and-contract-hardening/08-CONTEXT.md
@.planning/phases/08-reliability-and-contract-hardening/08-RESEARCH.md
@.planning/phases/08-reliability-and-contract-hardening/08-PATTERNS.md
@lib/scrypath/sync.ex
@lib/scrypath/telemetry.ex
@lib/scrypath/oban/enqueue.ex
@test/scrypath/sync_test.exs
@test/scrypath/telemetry_test.exs

<interfaces>
From lib/scrypath/sync.ex:
```elixir
@spec sync_records(module(), [struct() | map()], keyword()) :: {:ok, term()} | {:error, term()}
def sync_records(schema_module, records, opts \\ []) when is_list(records)

@spec delete_documents(module(), [term()], keyword()) :: {:ok, term()} | {:error, term()}
def delete_documents(schema_module, document_ids, opts \\ []) when is_list(document_ids)
```

From lib/scrypath/telemetry.ex:
```elixir
@spec stop_metadata(term(), keyword()) :: metadata()
def stop_metadata(result, extra \\ [])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared empty-batch no-op handling in `Scrypath.Sync` per D-06 through D-10</name>
  <files>lib/scrypath/sync.ex, lib/scrypath/telemetry.ex</files>
  <read_first>.planning/phases/08-reliability-and-contract-hardening/08-CONTEXT.md, lib/scrypath/sync.ex, lib/scrypath/telemetry.ex, lib/scrypath/oban/enqueue.ex</read_first>
  <acceptance_criteria>
    - `rg -n "status: :noop|document_count: 0|document_ids: \\[\\]" lib/scrypath/sync.ex lib/scrypath/telemetry.ex` finds the shared no-op envelope from D-08 through D-10.
    - `rg -n "dispatch_upsert|dispatch_delete" lib/scrypath/sync.ex` shows the empty-list guard happens before backend dispatch and before Oban enqueue per D-07.
    - `rg -n "job" lib/scrypath/sync.ex` does not show job metadata inside the no-op result path, satisfying D-09.
  </acceptance_criteria>
  <action>In `lib/scrypath/sync.ex`, short-circuit `sync_records/3` and `delete_documents/3` after projection or id collection and before `dispatch_upsert/3` or `dispatch_delete/3` when the resulting document list or id list is empty. Return exactly `{:ok, %{mode: mode, status: :noop, document_ids: [], document_count: 0}}` for `:inline`, `:manual`, and `:oban` per D-08, with no backend `task` map and no Oban `job`/`oban` metadata per D-09. Keep the existing telemetry span wrapping intact, then update `lib/scrypath/telemetry.ex` so `Telemetry.stop_metadata/2` preserves `status: :noop`, `mode`, and `document_count: 0` for these no-op maps.</action>
  <verify>
    <automated>mix test test/scrypath/sync_test.exs test/scrypath/telemetry_test.exs -x</automated>
  </verify>
  <done>Empty shared batch entrypoints now produce the D-08 no-op envelope consistently before any backend or Oban work can occur.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover no-op behavior and telemetry in deterministic contract tests per D-11 and D-12</name>
  <files>test/scrypath/sync_test.exs, test/scrypath/telemetry_test.exs</files>
  <read_first>.planning/phases/08-reliability-and-contract-hardening/08-CONTEXT.md, test/scrypath/sync_test.exs, test/scrypath/telemetry_test.exs, lib/scrypath/sync.ex, lib/scrypath/telemetry.ex</read_first>
  <acceptance_criteria>
    - `rg -n ":noop" test/scrypath/sync_test.exs test/scrypath/telemetry_test.exs` finds explicit no-op assertions.
    - `rg -n "refute_received \\{:upsert_documents|refute_received \\{:delete_documents|refute_received \\{:oban_insert" test/scrypath/sync_test.exs` proves empty lists do not reach backend or Oban paths.
    - `mix test test/scrypath/sync_test.exs test/scrypath/telemetry_test.exs -x` exits 0.
  </acceptance_criteria>
  <action>Add fast contract tests to `test/scrypath/sync_test.exs` for `Scrypath.sync_records/3` and `Scrypath.delete_documents/3` with `[]` under `:inline`, `:manual`, and `:oban`. Assert the exact D-08 tuple shape, assert no backend messages are received, and assert no Oban insert occurs for the `:oban` path. In `test/scrypath/telemetry_test.exs`, add stop-event assertions showing `status: :noop`, `sync_mode`/`mode`, and `document_count == 0` for no-op sync and delete spans, while preserving the low-cardinality contract already enforced in this file.</action>
  <verify>
    <automated>mix test test/scrypath/sync_test.exs test/scrypath/telemetry_test.exs -x</automated>
  </verify>
  <done>The shared no-op contract is locked in fast tests, including proof that empty batches never dispatch work and still emit public telemetry metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public sync/delete API -> backend dispatch | Caller-controlled batch size and mode selection must not create ambiguous backend work. |
| Public result envelope -> telemetry stop metadata | Result maps become emitted metadata and must stay low-cardinality and explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | D | `lib/scrypath/sync.ex` | mitigate | Short-circuit empty batches before backend and Oban dispatch so meaningless work is never enqueued or sent. |
| T-08-05 | I | `lib/scrypath/telemetry.ex` | mitigate | Emit explicit `status: :noop` and `document_count: 0` without backend-native metadata, keeping the public telemetry contract inspectable and low-cardinality. |
| T-08-06 | R | `test/scrypath/sync_test.exs` | mitigate | Assert exact tuple shapes and the absence of backend/Oban side effects for empty lists. |
</threat_model>

<verification>
Run `mix test test/scrypath/sync_test.exs test/scrypath/telemetry_test.exs -x` after each task and `mix test test/scrypath/meilisearch/tasks_test.exs test/scrypath/sync_test.exs test/scrypath/telemetry_test.exs -x` before writing the summary.
</verification>

<success_criteria>
`Scrypath.sync_records/3` and `Scrypath.delete_documents/3` treat empty inputs as defined no-op work across all modes, and telemetry reports that path explicitly without leaking backend or job metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/08-reliability-and-contract-hardening/08-02-SUMMARY.md`
</output>
