---
phase: 08-reliability-and-contract-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - test/scrypath/live_meilisearch_verification_test.exs
  - lib/mix/tasks/verify.phase8.ex
autonomous: true
requirements:
  - HARD-03
must_haves:
  truths:
    - "The reliability phase has a single verification command that runs focused fast tests and the narrow live Meilisearch seam."
    - "Live verification remains intentionally narrow and does not absorb deterministic malformed-payload or empty-batch contract cases."
  artifacts:
    - path: "test/scrypath/live_meilisearch_verification_test.exs"
      provides: "Tagged real-backend verification limited to the Phase 8 live seam"
    - path: "lib/mix/tasks/verify.phase8.ex"
      provides: "Canonical automated phase verification entrypoint"
  key_links:
    - from: "lib/mix/tasks/verify.phase8.ex"
      to: "test/scrypath/live_meilisearch_verification_test.exs"
      via: "integration test invocation with SCRYPATH_INTEGRATION gating"
      pattern: "integration|live_meilisearch_verification_test"
    - from: "lib/mix/tasks/verify.phase8.ex"
      to: "test/scrypath/meilisearch/tasks_test.exs"
      via: "focused fast reliability suite"
      pattern: "tasks_test|sync_test|telemetry_test"
---

<objective>
Finish the reliability phase with a canonical verification path that keeps live coverage narrow and deliberate while running the focused fast suite by default.

Purpose: Deliver the HARD-03 phase gate with the same verification posture the repo already uses: deterministic contract tests first, tagged live backend seam second.
Output: Updated live verification scope and a `mix verify.phase8` task that encodes the phase gate.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-reliability-and-contract-hardening/08-CONTEXT.md
@.planning/phases/08-reliability-and-contract-hardening/08-RESEARCH.md
@test/scrypath/live_meilisearch_verification_test.exs
@lib/mix/tasks/verify.phase5.ex
@test/test_helper.exs

<interfaces>
From lib/mix/tasks/verify.phase5.ex:
```elixir
def run(args) do
  Mix.Task.run("app.start")
  ...
  run_test!(["--include", "integration", "test/scrypath/live_meilisearch_verification_test.exs"], "live Meilisearch verification")
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep live Meilisearch verification intentionally narrow per D-11 through D-14</name>
  <files>test/scrypath/live_meilisearch_verification_test.exs</files>
  <read_first>.planning/phases/08-reliability-and-contract-hardening/08-CONTEXT.md, test/scrypath/live_meilisearch_verification_test.exs, test/test_helper.exs</read_first>
  <acceptance_criteria>
    - `rg -n "@moduletag :integration" test/scrypath/live_meilisearch_verification_test.exs` confirms the suite remains tagged live-only.
    - `rg -n "inline|reindex|external" test/scrypath/live_meilisearch_verification_test.exs` shows the suite still covers one inline write-and-wait path, one reindex/settings or cutover path, and one custom-id path per D-13.
    - `rg -n "invalid_task_payload|:noop" test/scrypath/live_meilisearch_verification_test.exs` returns no matches, satisfying D-14.
  </acceptance_criteria>
  <action>Review `test/scrypath/live_meilisearch_verification_test.exs` against D-13 and D-14, then trim or adjust only if needed so the live suite stays limited to three responsibilities: one real inline write-and-wait path, one real reindex/settings or cutover path, and one real custom document-id preservation path. Do not add malformed payload or empty-batch assertions here. Preserve the current `@moduletag :integration` contract and the existing `MeilisearchIntegration` helper usage.</action>
  <verify>
    <automated>SCRYPATH_INTEGRATION=1 SCRYPATH_MEILISEARCH_URL=http://127.0.0.1:7700 mix test --include integration test/scrypath/live_meilisearch_verification_test.exs</automated>
  </verify>
  <done>The live suite remains narrow, tagged, and aligned to the real-backend seam that Phase 8 intends to preserve.</done>
</task>

<task type="auto">
  <name>Task 2: Add the canonical phase verification command for HARD-03</name>
  <files>lib/mix/tasks/verify.phase8.ex</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/08-reliability-and-contract-hardening/08-RESEARCH.md, lib/mix/tasks/verify.phase5.ex, test/scrypath/meilisearch/tasks_test.exs, test/scrypath/sync_test.exs, test/scrypath/telemetry_test.exs, test/scrypath/live_meilisearch_verification_test.exs</read_first>
  <acceptance_criteria>
    - `rg -n "defmodule Mix.Tasks.Verify.Phase8" lib/mix/tasks/verify.phase8.ex` finds the new Mix task module.
    - `rg -n "tasks_test.exs|sync_test.exs|telemetry_test.exs" lib/mix/tasks/verify.phase8.ex` finds the focused fast reliability suite.
    - `rg -n "live_meilisearch_verification_test.exs|skip_integration|SCRYPATH_MEILISEARCH_URL" lib/mix/tasks/verify.phase8.ex` finds the live seam and environment gate.
    - `mix verify.phase8 --skip-integration` exits 0.
  </acceptance_criteria>
  <action>Create `lib/mix/tasks/verify.phase8.ex` following the existing Phase 5 pattern. The task must run `Mix.Task.run("app.start")`, then the focused fast suite `test/scrypath/meilisearch/tasks_test.exs`, `test/scrypath/sync_test.exs`, and `test/scrypath/telemetry_test.exs`, and optionally the tagged live suite `test/scrypath/live_meilisearch_verification_test.exs` unless `--skip-integration` is passed. Require `SCRYPATH_MEILISEARCH_URL` for the live run and set `SCRYPATH_INTEGRATION=1` before invoking it, matching the existing repo convention.</action>
  <verify>
    <automated>mix verify.phase8 --skip-integration</automated>
  </verify>
  <done>Phase 8 has a single canonical verification command that encodes the mixed fast-plus-live reliability gate required by HARD-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local verification command -> live Meilisearch service | Integration checks cross from local test code into an external service that may be absent or misconfigured. |
| Maintainer phase gate -> focused fast suite | The phase gate must reliably exercise the contract files that changed in Phase 8. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | D | `lib/mix/tasks/verify.phase8.ex` | mitigate | Gate live verification on `SCRYPATH_MEILISEARCH_URL` and preserve `--skip-integration` so the fast suite remains usable without a running service. |
| T-08-08 | R | `test/scrypath/live_meilisearch_verification_test.exs` | mitigate | Keep the suite narrow and tagged so flaky live behavior does not replace deterministic contract verification. |
| T-08-09 | T | `lib/mix/tasks/verify.phase8.ex` | mitigate | Encode the exact Phase 8 focused test files in the Mix task so later drift cannot silently weaken the gate. |
</threat_model>

<verification>
Run `mix verify.phase8 --skip-integration` first, then run `SCRYPATH_INTEGRATION=1 SCRYPATH_MEILISEARCH_URL=http://127.0.0.1:7700 mix verify.phase8` when a local or external Meilisearch instance is available.
</verification>

<success_criteria>
Phase 8 ends with a canonical verification entrypoint that runs the focused reliability suite by default and keeps real-backend coverage intentionally narrow and optional-by-environment.
</success_criteria>

<output>
After completion, create `.planning/phases/08-reliability-and-contract-hardening/08-03-SUMMARY.md`
</output>
