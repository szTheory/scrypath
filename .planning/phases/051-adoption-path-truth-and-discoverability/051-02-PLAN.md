---
phase: 051-adoption-path-truth-and-discoverability
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
autonomous: true
requirements:
  - ONBD-02
  - ONBD-03
user_setup: []

must_haves:
  truths:
    - CONTRIBUTING names the same first-hour narrative as README (golden path as spine).
    - CONTRIBUTING phoenix-example-integration row documents the exact CI commands including mix deps.get before mix test.
    - CONTRIBUTING distinguishes smoke.sh local harness from CI mix entrypoint.
  artifacts:
    - CONTRIBUTING.md
  key_links:
    - CONTRIBUTING CI table matches .github/workflows/ci.yml phoenix-example-integration job.
    - CONTRIBUTING links guides/sync-modes-and-visibility.md in a contributor pointer block.
---

<objective>
Make **CONTRIBUTING.md** byte-honest with **`.github/workflows/ci.yml`** for **`phoenix-example-integration`**, surface the single first-hour narrative and sync authority for contributors (**ONBD-02**, **ONBD-03**), and add a minimal “when you touch sync / operator / published guides” pointer block (**D-06**, **D-13**).
</objective>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03 | Information disclosure | CONTRIBUTING env examples | MITIGATE | Only document same non-secret defaults as ci.yml (`localhost`, example ports). |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@CONTRIBUTING.md
@.github/workflows/ci.yml
@.planning/phases/051-adoption-path-truth-and-discoverability/051-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>CI table — phoenix-example-integration exact steps</name>
  <files>CONTRIBUTING.md</files>
  <read_first>.github/workflows/ci.yml, CONTRIBUTING.md</read_first>
  <action>
    Locate the **`phoenix-example-integration`** row in the **## CI** table. Update the documented command sequence so it matches the workflow step **"Phoenix example integration"** exactly: after `cd examples/phoenix_meilisearch`, the job runs **`mix deps.get`** then **`mix test`** (both must appear in that order in the CONTRIBUTING text for this job). Preserve env vars already listed (`SCRYPATH_EXAMPLE_INTEGRATION=1`, `PGPORT=5433`, `SCRYPATH_MEILISEARCH_URL=http://127.0.0.1:7700`, Postgres 16 + Meilisearch v1.15) — align wording with lines ~273–343 of **ci.yml** if any version strings drift.
  </action>
  <verify>grep -n "phoenix-example-integration" -A2 CONTRIBUTING.md</verify>
  <acceptance_criteria>
    - `CONTRIBUTING.md` contains the substring `mix deps.get` in the same table cell or row description as `phoenix-example-integration`.
    - `CONTRIBUTING.md` contains `cd examples/phoenix_meilisearch` followed by `mix test` in documentation for that job (order: deps.get before test).
    - `grep -c "mix deps.get" CONTRIBUTING.md` is at least 1.
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

<task type="auto">
  <name>First-hour narrative + sync / doc pointer block</name>
  <files>CONTRIBUTING.md</files>
  <read_first>README.md, CONTRIBUTING.md, guides/sync-modes-and-visibility.md</read_first>
  <action>
    Add a short subsection under **Contributing** (suggested title: **First hour and canonical docs**) that states: (1) new contributors follow **README** Quick Path into **`guides/golden-path.md`** for the linear `:inline` first-hour story; (2) **sync modes, visibility, and operator lifecycle** are authored in **`guides/sync-modes-and-visibility.md`** — update that guide before duplicating semantics in README or here; (3) changing operator-facing copy or sync behavior should update doc contracts / **`mix test test/scrypath/docs_contract_test.exs`**. Keep to **≤ 8** bullet lines total in this subsection.
  </action>
  <verify>grep -n "First hour" CONTRIBUTING.md</verify>
  <acceptance_criteria>
    - `CONTRIBUTING.md` contains `guides/golden-path.md` and `guides/sync-modes-and-visibility.md` as path substrings.
    - `CONTRIBUTING.md` contains `docs_contract_test` OR `docs_contract_test.exs` as substring.
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

<task type="auto">
  <name>Smoke.sh vs CI wording pass</name>
  <files>CONTRIBUTING.md</files>
  <read_first>CONTRIBUTING.md, .planning/phases/051-adoption-path-truth-and-discoverability/051-CONTEXT.md</read_first>
  <action>
    In the **`phoenix-example-integration`** description and any adjacent example-app bullets, ensure explicit wording that **`./scripts/smoke.sh`** is a **local DX harness** under `examples/phoenix_meilisearch/`, while **CI** runs the **`mix deps.get` + `mix test`** sequence in the workflow — not bitwise identical to smoke.sh (**D-09**). Apply minimal edits; do not remove smoke.sh as a documented local path.
  </action>
  <verify>grep -n "smoke" CONTRIBUTING.md</verify>
  <acceptance_criteria>
    - `CONTRIBUTING.md` contains substring `smoke.sh` and substring `CI` in proximity within the same section OR same table row (same paragraph is enough).
    - `CONTRIBUTING.md` does not claim CI invokes `smoke.sh` as the test driver (must not contain phrase `CI runs` immediately followed by `smoke.sh` without negation).
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

</tasks>

<verification>
- [ ] `mix test test/scrypath/docs_contract_test.exs` exits 0
</verification>

<success_criteria>
- CONTRIBUTING satisfies `must_haves`
- CI parity grep spot-check passes against ci.yml
</success_criteria>

<output>
After completion, create `.planning/phases/051-adoption-path-truth-and-discoverability/051-02-SUMMARY.md`
</output>
