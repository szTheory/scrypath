---
phase: 051-adoption-path-truth-and-discoverability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - guides/golden-path.md
autonomous: true
requirements:
  - ONBD-01
  - ONBD-02
user_setup: []

must_haves:
  truths:
    - README names guides/golden-path.md as the canonical first-hour path and does not contradict golden-path dependency pins.
    - README contains exactly one explicit line stating that sync semantics and operator lifecycle authority live in guides/sync-modes-and-visibility.md (path may be markdown link form).
    - Golden path defers non-inline sync modes to sync-modes-and-visibility.md before production-shaped guidance.
  artifacts:
    - README.md
    - guides/golden-path.md
  key_links:
    - README links to guides/golden-path.md and to guides/sync-modes-and-visibility.md for authority.
    - Golden path “What is next” links to sync-modes-and-visibility.md.
---

<objective>
Align **README.md** and **guides/golden-path.md** with Phase 51 CONTEXT (**D-05**, **D-07**, **D-11**–**D-14**): single consumer first-hour narrative, explicit sync-authority surfacing without duplicating the sync guide, and a clear handoff from the `:inline` spine to **Sync Modes and Visibility**.
</objective>

<threat_model>
**Trust boundary:** Public repo markdown read by adopters and crawlers.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | README / golden-path links | MITIGATE | Use repo-relative `guides/...` links; no third-party URLs in new authority line. |
| T-51-02 | Information disclosure | Docs examples | MITIGATE | No real API keys or production hostnames in edits; keep placeholders. |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/051-adoption-path-truth-and-discoverability/051-CONTEXT.md
@.planning/REQUIREMENTS.md
@README.md
@guides/golden-path.md
@guides/sync-modes-and-visibility.md
</context>

<tasks>

<task type="auto">
  <name>README — sync authority line + first-hour map</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/051-adoption-path-truth-and-discoverability/051-CONTEXT.md, guides/sync-modes-and-visibility.md</read_first>
  <action>
    In **README.md**, after the existing **Start here** sentence that points to `guides/golden-path.md`, add **one** new sentence (separate paragraph or same paragraph if it stays readable) that states verbatim that **sync semantics, modes (`:inline` / `:oban` / `:manual`), eventual consistency, and operator lifecycle recovery language** are defined in **`guides/sync-modes-and-visibility.md`** — that file is the **single authority** for those topics (README must not restate the guide body). Keep **Phoenix Wayfinding** as thin links only. Ensure the **Integration smoke** paragraph still states that CI does **not** run `./scripts/smoke.sh` and points readers to **CONTRIBUTING.md** for job names (per **D-09**); if wording drifts toward implying CI runs smoke.sh, correct it.
  </action>
  <verify>grep -n "sync-modes-and-visibility" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains a line with substring `sync-modes-and-visibility` that also matches extended grep `(authority|single source|single authority)` case-insensitive: `grep -iE 'sync-modes-and-visibility.*(authority|single source|single authority)|(authority|single source|single authority).*sync-modes-and-visibility' README.md` exits 0.
    - `README.md` contains markdown link syntax to `guides/sync-modes-and-visibility.md` (pattern `](guides/sync-modes-and-visibility.md)`).
    - `README.md` matches `grep -E 'golden-path\\.md' README.md` (Quick Path / Start here still points to golden path).
    - `README.md` does not contain the substring `VRFY-` (published-doc hygiene).
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

<task type="auto">
  <name>Golden path — handoff clarity</name>
  <files>guides/golden-path.md</files>
  <read_first>guides/golden-path.md, guides/sync-modes-and-visibility.md</read_first>
  <action>
    In **guides/golden-path.md**, in the **## What is next** section, ensure the first bullet for sync modes explicitly names **`guides/sync-modes-and-visibility.md`** as the place for **`:oban`**, **`:manual`**, lifecycle, and recovery (strings may already be present — tighten only if the handoff is ambiguous). Do **not** add a second full manual for queue/manual sync; at most one short clause + link.
  </action>
  <verify>grep -n "What is next" -A6 guides/golden-path.md</verify>
  <acceptance_criteria>
    - `guides/golden-path.md` contains heading `## What is next` followed within 10 lines by `sync-modes-and-visibility.md`.
    - `guides/golden-path.md` still states early (first ~15 lines) that queueing/manual are deferred to that guide.
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

</tasks>

<verification>
- [ ] `mix test test/scrypath/docs_contract_test.exs` exits 0
- [ ] `mix format --check-formatted` if Elixir files touched (none expected here)
</verification>

<success_criteria>
- README and golden path satisfy `must_haves` above
- Docs contract suite passes
- No new internal planning tokens in published paths
</success_criteria>

<output>
After completion, create `.planning/phases/051-adoption-path-truth-and-discoverability/051-01-SUMMARY.md`
</output>
