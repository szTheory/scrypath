---
phase: 051-adoption-path-truth-and-discoverability
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - test/scrypath/docs_contract_test.exs
  - examples/phoenix_meilisearch/README.md
autonomous: true
requirements:
  - ONBD-01
  - ONBD-03
user_setup: []

must_haves:
  truths:
    - docs_contract_test asserts README sync-authority language and CONTRIBUTING CI command sequence against ci.yml.
    - Example README states the same mix entrypoint as CI for phoenix-example-integration.
  artifacts:
    - test/scrypath/docs_contract_test.exs
    - examples/phoenix_meilisearch/README.md
  key_links:
    - ExUnit attributes or reads stay in sync with files referenced in new tests.
---

<objective>
Lock **ONBD-01** and **ONBD-03** with **ExUnit** doc contracts: extend **`test/scrypath/docs_contract_test.exs`** so README / CONTRIBUTING / example README cannot drift from **`.github/workflows/ci.yml`** on the Phoenix example job, without introducing forbidden requirement tokens in published markdown.
</objective>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | Repudiation | Doc vs CI mismatch | MITIGATE | Tests read `README.md`, `CONTRIBUTING.md`, `.github/workflows/ci.yml` from disk at compile/test time. |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@test/scrypath/docs_contract_test.exs
@.github/workflows/ci.yml
@README.md
@CONTRIBUTING.md
@examples/phoenix_meilisearch/README.md
@.planning/phases/051-adoption-path-truth-and-discoverability/051-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>ExUnit — README sync authority invariant</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>test/scrypath/docs_contract_test.exs, README.md</read_first>
  <action>
    Add one `test` block (or extend an existing README test) that asserts **`@readme`** contains **both** (1) a markdown link closing `](guides/sync-modes-and-visibility.md)` and (2) on the same line **or** anywhere in the file within 200 characters of an occurrence of `sync-modes-and-visibility`, a case-insensitive match for one of: `authority`, `single source`, `single authority` (use `=~` / `Regex.match?` with an appropriate regex). This encodes **D-11** / README authority line without embedding internal `D-XX` tokens in user docs.
  </action>
  <verify>mix test test/scrypath/docs_contract_test.exs --trace 2>&1 | tail -n 5</verify>
  <acceptance_criteria>
    - `test/scrypath/docs_contract_test.exs` defines a test whose name includes `sync` and `authority` or `README` and `authority`.
    - `mix test test/scrypath/docs_contract_test.exs` exits 0 after the change.
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

<task type="auto">
  <name>ExUnit — CONTRIBUTING vs ci.yml phoenix-example-integration</name>
  <files>test/scrypath/docs_contract_test.exs</files>
  <read_first>test/scrypath/docs_contract_test.exs, CONTRIBUTING.md, .github/workflows/ci.yml</read_first>
  <action>
    Add a `test` that reads **`@contributing`** and **`@ci_workflow`** (already module attributes) and asserts: (1) **`@contributing`** substring **`phoenix-example-integration`** appears in a context that includes **`mix deps.get`** before **`mix test`** in the same contiguous block of lines **or** same table cell text block (simplest: assert `@contributing` contains `mix deps.get` after first occurrence of `phoenix-example-integration` with `mix test` still present in CONTRIBUTING); (2) **`@ci_workflow`** contains job id `phoenix-example-integration` and contains the ordered sequence `cd examples/phoenix_meilisearch`, `mix deps.get`, `mix test` in the `run:` block for that job. Adjust assertion style to match existing tests in the file (prefer `assert String.contains?` / regex consistent with file style).
  </action>
  <verify>mix test test/scrypath/docs_contract_test.exs</verify>
  <acceptance_criteria>
    - `mix test test/scrypath/docs_contract_test.exs` exits 0.
    - `grep -n "phoenix-example-integration" test/scrypath/docs_contract_test.exs` returns at least one line.
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

<task type="auto">
  <name>Example README — CI mix entrypoint honesty</name>
  <files>examples/phoenix_meilisearch/README.md</files>
  <read_first>examples/phoenix_meilisearch/README.md, .github/workflows/ci.yml</read_first>
  <action>
    Locate the section that describes **CI** / **`phoenix-example-integration`**. Ensure it documents the same **Mix** steps as the workflow: `cd examples/phoenix_meilisearch`, **`mix deps.get`**, **`mix test`**, with env vars **`SCRYPATH_EXAMPLE_INTEGRATION=1`**, **`PGPORT=5433`**, **`SCRYPATH_MEILISEARCH_URL=http://127.0.0.1:7700`**. If `mix deps.get` is missing from the CI description, add it. Keep **`./scripts/smoke.sh`** documented as local orchestration, not CI, per **D-09**.
  </action>
  <verify>grep -n "mix deps.get" examples/phoenix_meilisearch/README.md</verify>
  <acceptance_criteria>
    - `examples/phoenix_meilisearch/README.md` contains substring `mix deps.get` in a paragraph or list item that also mentions `phoenix-example-integration` OR `GitHub Actions` OR `CI`.
    - `examples/phoenix_meilisearch/README.md` contains substring `mix test` in the same file.
  </acceptance_criteria>
  <done>Acceptance criteria all satisfied.</done>
</task>

</tasks>

<verification>
- [ ] `mix test test/scrypath/docs_contract_test.exs` exits 0
- [ ] `mix test --exclude integration` exits 0 (full non-integration slice before merge)
</verification>

<success_criteria>
- New contracts prevent README / CONTRIBUTING / example README drift from ci.yml for the example integration job
- No `ONBD-`, `VRFY-`, or `D-` internal tokens added to README, CONTRIBUTING, or guides under `@published_markdown_for_hygiene` rules
</success_criteria>

<output>
After completion, create `.planning/phases/051-adoption-path-truth-and-discoverability/051-03-SUMMARY.md`
</output>
