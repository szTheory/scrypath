---
phase: 39-federation-scoring-weights
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scrypath/multi_search/entries.ex
  - lib/scrypath/search.ex
  - lib/scrypath/backend.ex
  - lib/scrypath/meilisearch.ex
  - test/support/fake_backend.ex
  - test/scrypath/multi_search/entries_test.exs
  - test/scrypath/search_many_test.exs
autonomous: true
requirements:
  - FED-01
tags:
  - federation
  - search_many
  - validation
must_haves:
  truths:
    - "`Entries.normalize/2` returns a **4-tuple** list `{schema, text, merged_opts, fed_opts}` where `fed_opts` is `[]` or `[federation_weight: float]`; `merged_opts` never contains `:federation_weight`."
    - "Invalid `federation_weight:` values fail before `Options.validate_search_options/2` with `{:error, {:invalid_options, {:federation_weight, _}}}`."
    - "When any `fed_opts` is non-empty (explicit weight) and `backend.search_many/2` is not exported, `search_many/2` returns `{:error, {:invalid_options, {:federation_merge_requires_native_search_many, _}}}` — never `run_sequential_search_many/3` for that call."
  artifacts:
    - path: "lib/scrypath/backend.ex"
      provides: "Updated `@callback search_many` documenting optional per-query federation keywords"
      contains: "search_many"
    - path: "lib/scrypath/search.ex"
      provides: "Quad-aware pipeline + sequential guard"
      contains: "run_search_many"
  key_links:
    - from: "Entries.normalize/2"
    - to: "Search.validate_search_triples/1"
    - via: "fed_opts carried beside merged_opts"
    - pattern: "strip entry-only keys before NimbleOptions"
---

<objective>
Extend **`MultiSearch.Entries.normalize/2`** to validate and isolate **`federation_weight:`**, change its return shape to **quads**, update **`Scrypath.Search`** to consume quads, strip merge-only paths when **`search_many/2`** is missing on the backend, and update the **`Scrypath.Backend`** callback plus **`Meilisearch`** / **`FakeBackend`** to accept **`{schema, %Query{}, fed_opts}`** rows (Meilisearch **ignores** `fed_opts` until Plan 02 wires `"federationOptions"`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-federation-scoring-weights/39-CONTEXT.md
@.planning/phases/39-federation-scoring-weights/39-RESEARCH.md
@.planning/phases/39-federation-scoring-weights/39-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quads in `Entries.normalize/2` + weight validation</name>
  <files>lib/scrypath/multi_search/entries.ex, test/scrypath/multi_search/entries_test.exs</files>
  <read_first>
    - lib/scrypath/multi_search/entries.ex (current normalize + reject_shared_only_in_entry)
    - .planning/phases/39-federation-scoring-weights/39-CONTEXT.md (D-01, D-05, D-06, D-08)
    - test/scrypath/multi_search/entries_test.exs (all assertions on normalize shape)
  </read_first>
  <action>
    1. Change `@spec normalize/2` success type to `{:ok, [{module(), String.t(), keyword(), keyword()}]}` where the **4th element** `fed_opts` is either `[]` or `[federation_weight: float]` only.
    2. In `normalize_one/2`, read optional `:federation_weight` from **entry** keyword **before** `Keyword.merge/3` with shared opts. Validate: if present, value must be **finite** (`is_float/1` or `is_integer/1`, then coerce integer→float); reject `:nan`, `:infinity`, `:"-infinity"`, non-numeric with `{:error, {:invalid_options, {:federation_weight, :non_finite}}}` or similarly **grep-stable** atom tag inside the tuple.
    3. Remove `:federation_weight` from the keyword passed into merge so **`merged` never contains `:federation_weight`**.
    4. If omitted, `fed_opts` is `[]` (not `[federation_weight: 1.0]`).
    5. Update **every** `Entries.normalize` test to expect 4-tuples (e.g. `{Post, "hi", opts, []}`).
  </action>
  <verify>
    <automated>mix test test/scrypath/multi_search/entries_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'federation_weight' lib/scrypath/multi_search/entries.ex` returns ≥ 1 line.
    - `mix test test/scrypath/multi_search/entries_test.exs` exits 0.
    - `grep -n 'fed_opts' lib/scrypath/multi_search/entries.ex` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    Normalize returns quads; weight validation covered by tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: `Backend` callback + `Search` quad pipeline + sequential guard + 3-tuple backends</name>
  <files>lib/scrypath/backend.ex, lib/scrypath/search.ex, lib/scrypath/meilisearch.ex, test/support/fake_backend.ex, test/scrypath/search_many_test.exs</files>
  <read_first>
    - lib/scrypath/search.ex (`run_search_many`, `validate_search_triples`, `run_sequential_search_many`)
    - lib/scrypath/backend.ex (`@callback search_many`)
    - test/scrypath/search_many_test.exs (`SequentialOnlyBackend` module)
    - .planning/phases/39-federation-scoring-weights/39-CONTEXT.md (D-13–D-15)
  </read_first>
  <action>
    1. Update `@callback search_many([{module(), Scrypath.Query.t(), keyword()}], keyword()) :: {:ok, map()} | {:error, term()}` in `lib/scrypath/backend.ex` — third element is **per-query federation opts** (empty list when unused). Document that only `:federation_weight` is defined in v1.8.
    2. Refactor `Search.validate_search_triples/1` to accept **quads** from `Entries.normalize/2`. For each `{schema, text, merged, fed_opts}`, call `Options.validate_search_options(schema, merged)` and on success produce `{schema, Query.new(text, search_opts), fed_opts}`.
    3. Update `run_native_search_many/3` to call `backend.search_many/2` with a list of **`{schema, %Query{}, fed_opts}`** (third element always a keyword list, often `[]`).
    4. **Guard (before native vs sequential branch):** If any prepared `fed_opts` is non-empty and `not function_exported?(backend, :search_many, 2)`, return `{:error, {:invalid_options, {:federation_merge_requires_native_search_many, %{backend: backend}}}}` — map must include key **`:backend`**.
    5. Update `Scrypath.Meilisearch.search_many/2` to destructure **`{schema_module, %Query{} = query, fed_opts}`** and **ignore** `fed_opts` in this plan (Plan 02 adds JSON). Preserve existing `queries` + top-level `federation` payload otherwise.
    6. Update `Scrypath.TestSupport.FakeBackend.search_many/2` to the same 3-tuple shape; keep current fake hits behavior (ignore weights until Plan 02).
    7. Add test in `search_many_test.exs`: entries `[{SearchablePost, "a", federation_weight: 2}, {FacetableMovie, "b"}]` with `backend: SequentialOnlyBackend` and `@base_opts` → expect `{:error, {:invalid_options, {:federation_merge_requires_native_search_many, %{backend: SequentialOnlyBackend}}}}`.
    8. Add test: entries **without** `federation_weight` + `SequentialOnlyBackend` still returns `{:ok, _}` (existing behavior).
  </action>
  <verify>
    <automated>mix test test/scrypath/search_many_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/scrypath/search_many_test.exs` exits 0.
    - `grep -n 'federation_merge_requires_native_search_many' lib/scrypath/search.ex` returns ≥ 1 line.
    - `grep -n 'Query.t(), keyword()' lib/scrypath/backend.ex` returns ≥ 1 line.
  </acceptance_criteria>
  <done>
    Backend callback documents 3-tuple rows; Search enforces native backend when weights used; FakeBackend compiles with new arity.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → `search_many/2` | Same as single-search: controlled modules and query strings. |
| Validation → errors | Weight errors use small atoms/tuples; no echo of arbitrary user strings in new paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Tampering | Malformed weight types | mitigate | Finite-number validation before any backend call. |
| T-39-01-02 | Elevation / Misrepresentation | Wrong merge order on sequential backend | mitigate | Hard error when weights require native `search_many/2`. |

No HIGH-severity items beyond library-local validation.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/scrypath/multi_search/entries_test.exs test/scrypath/search_many_test.exs` exits 0.
</verification>

<success_criteria>
- FED-01 (partial): validated **`federation_weight:`** API surface and **no silent** sequential merge for weighted calls.
- Callback and `Search` pipeline aligned on **3-tuple** `search_many` rows with optional `fed_opts`.
</success_criteria>
