---
phase: 39-federation-scoring-weights
plan: 02
type: execute
wave: 2
depends_on:
  - "39-01"
files_modified:
  - lib/scrypath/meilisearch.ex
  - lib/scrypath/meilisearch/federated_decode.ex
  - lib/scrypath/multi_search_result.ex
  - lib/scrypath/search.ex
  - lib/scrypath.ex
  - test/support/fake_backend.ex
  - test/scrypath/search_many_test.exs
  - test/scrypath/meilisearch/federated_decode_test.exs
  - guides/multi-index-search.md
autonomous: true
requirements:
  - FED-01
tags:
  - meilisearch
  - federation
  - docs
must_haves:
  truths:
    - "Meilisearch `search_many` JSON includes `\"federationOptions\" => %{\"weight\" => float}` **only** for queries whose `fed_opts` contains `:federation_weight` — omitted entirely when `fed_opts` is `[]` (D-05)."
    - "`%MultiSearchResult{}` exposes an **optional** merge-order trace (field name chosen in code) that is **non-nil** only when derived from a federated flat `hits` stream; `nil` on sequential path (D-09–D-10)."
    - "Public helper `Scrypath.MultiSearchResult.merge_projection/1` exists and is `@doc` documented."
    - "`guides/multi-index-search.md` contains heading **`## Federation weights`** (exact string) with at least one sentence referencing **`federation_weight:`** and cross-index **merge** vs per-schema scores."
  artifacts:
    - path: "lib/scrypath/meilisearch.ex"
      provides: "Per-query federationOptions.weight wiring"
      contains: "federationOptions"
    - path: "test/scrypath/meilisearch/federated_decode_test.exs"
      provides: "Decode + merge trace unit tests"
      contains: "FederatedDecode"
  key_links:
    - from: "FederatedDecode.per_schema_maps/2"
    - to: "MultiSearchResult.new/1"
    - via: "merge trace extraction"
    - pattern: "string keys only"
---

<objective>
Wire **Meilisearch** `federationOptions.weight`, derive **global merge trace** and optional **`merge_projection/1`**, extend **`FakeBackend`** for deterministic weighted ordering tests, expand **`search_many`** / **`Scrypath`** docs, and add the **`## Federation weights`** guide subsection reconciling merge vs per-schema relevance (D-16/D-17).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-federation-scoring-weights/39-CONTEXT.md
@.planning/phases/39-federation-scoring-weights/39-RESEARCH.md
@.planning/phases/39-federation-scoring-weights/39-PATTERNS.md
@guides/multi-index-search.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Meilisearch per-query `federationOptions`</name>
  <files>lib/scrypath/meilisearch.ex</files>
  <read_first>
    - lib/scrypath/meilisearch.ex (`search_many/2` full function)
    - lib/scrypath/meilisearch/query.ex (if payload merge needs helpers)
    - .planning/phases/39-federation-scoring-weights/39-CONTEXT.md (D-01, D-05)
  </read_first>
  <action>
    1. In the existing `Enum.map` over `paired_queries` (already 3-tuples from Plan 01), after `Map.put("indexUid", index)`, when `Keyword.get(fed_opts, :federation_weight)` is a **float**, merge `%{"federationOptions" => %{"weight" => float_value}}` into the per-query JSON map using **string keys** matching Meilisearch.
    2. When `fed_opts` is `[]` or `:federation_weight` absent, **do not** add `"federationOptions"` key at all.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'federationOptions' lib/scrypath/meilisearch.ex` returns ≥ 1 line.
    - `grep -n '"weight"' lib/scrypath/meilisearch.ex` returns ≥ 1 line.
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>
    Meilisearch client payload carries optional weights per query.
  </done>
</task>

<task type="auto">
  <name>Task 2: Merge trace + `MultiSearchResult` + `merge_projection/1`</name>
  <files>lib/scrypath/meilisearch/federated_decode.ex, lib/scrypath/multi_search_result.ex, lib/scrypath/search.ex</files>
  <read_first>
    - lib/scrypath/meilisearch/federated_decode.ex
    - lib/scrypath/multi_search_result.ex
    - lib/scrypath/search.ex (`build_multi_result`, `run_native_search_many`)
    - lib/scrypath/search_result.ex (hit id access pattern if needed)
  </read_first>
  <action>
    1. Add a pure function in `FederatedDecode` (e.g. `merge_hit_order/2`) that, given federated `response` map with flat `"hits"` and `indexed_schemas`, returns `{:ok, [{schema_module, id_term}, ...]}` in **engine order** where `id_term` is the primary key value from each hit map (`"id"` key first, else schema `document_id` string — document exact extraction in implementation).
    2. Extend `%MultiSearchResult{}` with a new **optional** field (e.g. `:merge_hit_order` — name may differ) included in `new/1` and `@type t`; default `nil`.
    3. In `Search.build_multi_result/4`, when `raw_response` is federated (flat hits path used by decode), set the new field from `FederatedDecode.merge_hit_order/2` result; otherwise `nil`.
    4. Implement `@spec merge_projection(t()) :: [{module(), map() | struct()}]` (or documented variant) in `MultiSearchResult` per CONTEXT D-11: walk merge order + `ordered` / `by_schema` to emit **`[{schema, hit_map}]`** using **existing** hit maps only (no duplicate payload lists).
  </action>
  <verify>
    <automated>mix test test/scrypath/meilisearch/federated_decode_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File `test/scrypath/meilisearch/federated_decode_test.exs` exists.
    - `grep -n 'merge_projection' lib/scrypath/multi_search_result.ex` returns ≥ 1 line.
    - `mix test test/scrypath/meilisearch/federated_decode_test.exs` exits 0.
  </acceptance_criteria>
  <done>
    Struct + decode + projection helper implemented with tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: `FakeBackend` + `search_many` ordering tests + public docs</name>
  <files>test/support/fake_backend.ex, test/scrypath/search_many_test.exs, lib/scrypath/search.ex, lib/scrypath.ex, guides/multi-index-search.md</files>
  <read_first>
    - test/support/fake_backend.ex (`search_many/2`)
    - test/scrypath/search_many_test.exs
    - lib/scrypath.ex (`search_many/2` @doc)
    - guides/multi-index-search.md (find insertion point before Phase 41 polish)
  </read_first>
  <action>
    1. Update `FakeBackend.search_many/2` so returned flat `"hits"` order reflects **weights** from `fed_opts` (e.g. repeat query list sorted by descending weight so higher weight appears earlier) — deterministic for two schemas.
    2. Add `search_many_test` asserting: with two entries and **different** `federation_weight:` values, **merged order** in `%MultiSearchResult{}` new trace field matches FakeBackend policy; assert `merge_projection/1` ordering matches.
    3. Expand `@doc` on `Scrypath.Search.search_many/2` and `Scrypath.search_many/2` to document **`federation_weight:`**, error tuples `{:federation_weight, _}`, `{:federation_merge_requires_native_search_many, _}`, and **`nil`** merge trace semantics (verbatim error **atoms** as implemented).
    4. Insert section **`## Federation weights`** in `guides/multi-index-search.md` containing substring **`federation_weight:`** and explaining merge vs per-schema score honesty (one short paragraph minimum).
  </action>
  <verify>
    <automated>mix test test/scrypath/search_many_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '## Federation weights' guides/multi-index-search.md` returns ≥ 1 line.
    - `grep -n 'federation_weight' lib/scrypath/search.ex` returns ≥ 1 line in `@doc` block (use `grep` with context or confirm line is inside comment/doc).
    - `mix test test/scrypath/search_many_test.exs` exits 0.
  </acceptance_criteria>
  <done>
    Tests prove ordering; public docs + guide subsection land FED-01 discoverability.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meilisearch JSON | Weight is numeric only — validated in Plan 01 before reaching HTTP. |
| Decode | Remote JSON uses string keys; no atomization of dynamic keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-01 | Information disclosure | Merge trace leaks unintended fields | mitigate | Trace stores only `{schema, id}` references per CONTEXT. |

LOW severity; library-local.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/scrypath/meilisearch/federated_decode_test.exs test/scrypath/search_many_test.exs` exits 0.
</verification>

<success_criteria>
- FED-01: weighted **`search_many/2`**, tests for ordering, public types/docs, guide **`## Federation weights`**.
- Meilisearch wire + decode + `%MultiSearchResult{}` + **`merge_projection/1`** complete.
</success_criteria>
