---
phase: 97
plan: 3
type: execute
wave: 2
depends_on:
  - 97-01
  - 97-02
files_modified:
  - lib/mix/tasks/verify.phase97.ex
  - mix.exs
  - test/mix/tasks/verify.phase97_test.exs
  - test/mix/tasks/workflow_wiring_test.exs
  - test/scrypath/docs_contract_test.exs
  - CONTRIBUTING.md
autonomous: true
requirements:
  - TRUTH-01
  - TRUTH-02
  - TRUTH-03
  - SCOPE-01
must_haves:
  - "Add a narrow `mix verify.phase97` seam that validates canonical statement and scope guard anchors."
  - "Use focused tests and stable anchor assertions; do not snapshot prose paragraphs."
  - "Document the phase gate without expanding required CI categories."
---

<objective>
Implement a focused verification seam (`mix verify.phase97` and targeted tests) that locks canonical statement IDs, traceability rows, and scope-guard anchors with low-churn assertions.
</objective>

<context>
This plan converts Phase 97 freeze artifacts into enforceable checks. It must protect truth anchors and scope boundaries while preserving the repository's lean required-check posture.
</context>

<tasks>
### Task 97-03-01 - Implement `Mix.Tasks.Verify.Phase97`
<read_first>
- `lib/mix/tasks/verify.phase96.ex`
- `lib/mix/tasks/verify.adopter.ex`
- `mix.exs`
- `.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-CONTRACT-STATEMENTS.md`
- `.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-CONTRACT-TRACEABILITY.md`
- `.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-SCOPE-GUARD.md`
</read_first>
<action>
- Create `lib/mix/tasks/verify.phase97.ex` following the `verify.phaseNN` structure:
  - starts app,
  - rejects extra arguments,
  - runs focused tests for phase-97 trust checks,
  - runs docs warnings gate if existing phase verify tasks do so.
- Set shortdoc text to identify "canonical contract freeze and scope guard verification".
- Ensure the task output includes a stable marker string containing `verify.phase97`.
</action>
<acceptance_criteria>
- `test -f "lib/mix/tasks/verify.phase97.ex"` exits 0.
- `rg "defmodule Mix\\.Tasks\\.Verify\\.Phase97" "lib/mix/tasks/verify.phase97.ex"` returns 1 match.
- `rg "ensure_no_args!|verify\\.phase97|Mix\\.Task\\.run\\(\"app.start\"" "lib/mix/tasks/verify.phase97.ex"` returns required matches.
</acceptance_criteria>
<verify>
- `test -f "lib/mix/tasks/verify.phase97.ex"`
- `rg "defmodule Mix\\.Tasks\\.Verify\\.Phase97" "lib/mix/tasks/verify.phase97.ex"`
- `rg "ensure_no_args!|verify\\.phase97|Mix\\.Task\\.run\\(\"app.start\"" "lib/mix/tasks/verify.phase97.ex"`
</verify>

### Task 97-03-02 - Wire task into CLI environment and task tests
<read_first>
- `mix.exs`
- `test/mix/tasks/workflow_wiring_test.exs`
- `test/mix/tasks/verify_adopter_test.exs`
- `lib/mix/tasks/verify.phase97.ex`
</read_first>
<action>
- Update `mix.exs` `preferred_envs` to include `"verify.phase97": :test`.
- Create `test/mix/tasks/verify.phase97_test.exs` to assert:
  - no-args contract,
  - stable task output marker,
  - focused command execution path.
- Extend `test/mix/tasks/workflow_wiring_test.exs` with one assertion for `"verify.phase97"` preferred env wiring.
</action>
<acceptance_criteria>
- `rg "verify\\.phase97" "mix.exs"` returns at least 1 match.
- `test -f "test/mix/tasks/verify.phase97_test.exs"` exits 0.
- `rg "verify\\.phase97|does not accept arguments|capture_io" "test/mix/tasks/verify.phase97_test.exs"` returns required matches.
- `rg "verify\\.phase97" "test/mix/tasks/workflow_wiring_test.exs"` returns at least 1 match.
</acceptance_criteria>
<verify>
- `rg "verify\\.phase97" "mix.exs"`
- `test -f "test/mix/tasks/verify.phase97_test.exs"`
- `rg "verify\\.phase97|does not accept arguments|capture_io" "test/mix/tasks/verify.phase97_test.exs"`
- `rg "verify\\.phase97" "test/mix/tasks/workflow_wiring_test.exs"`
</verify>

### Task 97-03-03 - Add focused anchor tests and contributor discoverability
<read_first>
- `test/scrypath/docs_contract_test.exs`
- `CONTRIBUTING.md`
- `.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-CONTRACT-STATEMENTS.md`
- `.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-CONTRACT-TRACEABILITY.md`
- `.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-SCOPE-GUARD.md`
</read_first>
<action>
- Update `test/scrypath/docs_contract_test.exs` with anchor-based assertions that check:
  - statement IDs `CST-TRUTH-01-INSTALL`, `CST-TRUTH-02-RELEASE-MAIN`, `CST-TRUTH-03-SUPPORT-AUTHORITY`,
  - `SCOPE-01` scope-boundary anchors sourced from `.planning/phases/97-canonical-contract-freeze-and-scope-guard/97-SCOPE-GUARD.md` (for example `PHASE97-SCOPE-GUARD` and reopen-policy wording),
  - frozen traceability requirement rows `TRUTH-01`, `TRUTH-02`, and `TRUTH-03` only.
- Keep assertions string- and ordering-based; do not add snapshot helpers (`assert_snapshot`, `snapshot_assert`, `matches_snapshot`).
- Update `CONTRIBUTING.md` verification section to include `mix verify.phase97` as the Phase 97 gate and note it is milestone trust-hardening coverage, not a new runtime feature gate.
</action>
<acceptance_criteria>
- `rg "CST-TRUTH-01-INSTALL|CST-TRUTH-02-RELEASE-MAIN|CST-TRUTH-03-SUPPORT-AUTHORITY" "test/scrypath/docs_contract_test.exs"` returns at least 3 matches.
- `rg "TRUTH-01|TRUTH-02|TRUTH-03" "test/scrypath/docs_contract_test.exs"` returns at least 3 matches.
- `rg "SCOPE-01|97-SCOPE-GUARD\\.md|PHASE97-SCOPE-GUARD|reopen policy" "test/scrypath/docs_contract_test.exs"` returns at least 2 matches.
- `rg "mix verify\\.phase97" "CONTRIBUTING.md"` returns at least 1 match.
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/docs_contract_test.exs"` exits 0.
- `mix test test/mix/tasks/verify.phase97_test.exs test/mix/tasks/workflow_wiring_test.exs test/scrypath/docs_contract_test.exs` exits 0.
</acceptance_criteria>
<verify>
- `rg "CST-TRUTH-01-INSTALL|CST-TRUTH-02-RELEASE-MAIN|CST-TRUTH-03-SUPPORT-AUTHORITY" "test/scrypath/docs_contract_test.exs"`
- `rg "TRUTH-01|TRUTH-02|TRUTH-03" "test/scrypath/docs_contract_test.exs"`
- `rg "SCOPE-01|97-SCOPE-GUARD\\.md|PHASE97-SCOPE-GUARD|reopen policy" "test/scrypath/docs_contract_test.exs"`
- `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/docs_contract_test.exs"`
- `rg "mix verify\\.phase97" "CONTRIBUTING.md"`
- `mix test test/mix/tasks/verify.phase97_test.exs test/mix/tasks/workflow_wiring_test.exs test/scrypath/docs_contract_test.exs`
- `mix verify.phase97`
</verify>
</tasks>

<threat_model>
- **T97-VERIFY-001 (Coverage bypass, High):** Phase 97 artifacts exist but no enforced gate validates them.  
  **Mitigation:** add `verify.phase97` task plus focused tests and wiring assertion.  
  **Detection:** `rg "verify\\.phase97" "mix.exs" "test/mix/tasks/workflow_wiring_test.exs"`.
- **T97-VERIFY-002 (Brittle test drift, Medium):** Assertions snapshot prose and fail on harmless edits.  
  **Mitigation:** enforce anchor and ID checks only; avoid paragraph snapshots.  
  **Detection:** `! rg "assert_snapshot|snapshot_assert|matches_snapshot" "test/scrypath/docs_contract_test.exs"` and `rg "CST-TRUTH-01-INSTALL|TRUTH-01|SCOPE-01|97-SCOPE-GUARD\\.md" "test/scrypath/docs_contract_test.exs"`.
- **T97-VERIFY-003 (Gate sprawl, Medium):** Phase gate documentation implicitly broadens required CI check categories.  
  **Mitigation:** `CONTRIBUTING.md` update must state milestone trust-hardening scope and preserve lean required-check policy.  
  **Detection:** `rg "main-ci|repo-hygiene|release-truth|verify.phase97" "CONTRIBUTING.md"`.
</threat_model>

<verification>
- `mix test test/mix/tasks/verify.phase97_test.exs`
- `mix test test/mix/tasks/workflow_wiring_test.exs`
- `mix test test/scrypath/docs_contract_test.exs`
- `mix verify.phase97`
- `rg "verify\\.phase97" "mix.exs" "CONTRIBUTING.md" "test/mix/tasks/workflow_wiring_test.exs"`
</verification>

<success_criteria>
- `mix verify.phase97` exists, rejects unexpected args, and runs focused phase checks.
- `mix.exs` and task wiring tests include `verify.phase97` in test environment mapping.
- Docs-contract tests assert Phase 97 statement/scope anchors without prose snapshots.
- Contributor docs expose the phase gate while preserving existing required-check boundaries.
</success_criteria>

<output>
- `lib/mix/tasks/verify.phase97.ex`
- `test/mix/tasks/verify.phase97_test.exs`
- Updated `mix.exs`
- Updated `test/mix/tasks/workflow_wiring_test.exs`
- Updated `test/scrypath/docs_contract_test.exs`
- Updated `CONTRIBUTING.md`
</output>
