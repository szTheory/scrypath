---
quick_task: 260416-if2
description: fix mix.exs GitHub URLs add a GitHub Actions publish job gated on release creation use HEX_API_KEY only in that publish job
status: in_progress
validate: false
---

# Quick Task 260416-if2 Plan

1. Update Scrypath package metadata so source, homepage, changelog, and docs links resolve to the canonical `szTheory/scrypath` repository.
2. Extend the Release Please workflow with a publish job that runs only when a release is created and exposes `HEX_API_KEY` only to that job.
3. Refresh maintainer release docs so the manual credential check and automated publish path describe the same release flow.
