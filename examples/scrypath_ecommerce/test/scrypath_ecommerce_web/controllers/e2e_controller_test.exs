defmodule ScrypathEcommerceWeb.E2EControllerTest do
  use ScrypathEcommerceWeb.ConnCase

  describe "E2E seed endpoint" do
    @tag :skip
    test "seed/2 dynamically invokes catalog fixtures", %{conn: _conn} do
      flunk("Not implemented: Should seed data for E2E tests using fixtures")
    end
  end
end
